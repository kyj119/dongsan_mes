-- 대표자 개인통장 구분 — 자금·판관비 집계에서 완전히 분리하고, 입금(수금) 반영 용도로만 남긴다
--
-- 정책(용준님 2026-08-13 확정 → 2026-08-19 웹 반영): **수금은 법인 실적이라 계좌는 살려 두되,
-- 출금은 법인 비용이 아니다.** 지금까지는 `scripts/finance-diagnose.cjs` 가 별칭(`account_alias LIKE '대표자%'`)
-- 으로만 걸러 왔고(그 파일 주석이 이 컬럼을 예고한다), 웹의 자금현황·재무상태·정기변동비 추정에는 그대로 섞여 있었다.
--
-- 대상 = `bank_accounts` #17 농협 08712205285 · 예금주 김진수 · 별칭 「대표자」(entity 1).
--   실측 2026-01~08: 입금 78건 4,140만(거래처 62 · 수금 연결 60) · 출금 38건 4,082만(계정분류 0 · IGNORED 36) ·
--   최신 잔액 694,847원이 법인 현금잔액 합계에 포함돼 있었다.
--   ⚠️ 청주 #10(하나 60291075824307 · 예금주 김용준)은 **제외** — 청주 법인카드 결제가 나가는 사업용 계좌다.
--
-- is_personal = 1 의 의미
--   ∙ 제외: 총 계좌잔액/예금잔액(자금현황·자금일보 시작잔액·재무상태 현금) · 정기변동비 추정(은행 출금 실적)
--   ∙ 유지: 거래내역 조회 · 자동매칭 · **입금 → 수금(payments) 연결**(매출채권 회수는 실제 일어난 일이다)
--   ∙ 출금은 집계에서 빠질 뿐 연결 자체를 막지는 않는다(용준님 선택 — 손으로 IGNORED 하던 걸 대체).
--
-- 되돌리기: UPDATE bank_accounts SET is_personal = 0 WHERE id = 17;   (컬럼은 D1 에서 DROP 하지 말 것)
-- 멱등: ALTER 는 1회성(재실행 시 duplicate column 오류 — 정상). UPDATE 는 절대값이라 무해.

ALTER TABLE bank_accounts ADD COLUMN is_personal INTEGER NOT NULL DEFAULT 0;

UPDATE bank_accounts SET is_personal = 1 WHERE id = 17;
