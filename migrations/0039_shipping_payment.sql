-- ============================================================================
-- Migration: 0039 - 주문별 선불/착불 구분 컬럼 추가
-- 값: 'PREPAID'(선불) / 'COLLECT'(착불) / NULL(해당없음)
-- ============================================================================

ALTER TABLE orders ADD COLUMN shipping_payment TEXT DEFAULT NULL;
