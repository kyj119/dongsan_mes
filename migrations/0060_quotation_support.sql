-- ============================================================================
-- Migration: 0060_quotation_support.sql (원래 0047, 중복 번호 해결을 위해 리네임)
-- 견적서 기능: orders에 QUOTATION 상태 + valid_until 컬럼 추가
-- NOTE: 이 마이그레이션의 내용은 0048_fix_quotation_check.sql에 완전히 통합됨.
--       0048이 orders 테이블을 재생성하면서 valid_until과 QUOTATION CHECK를 모두 포함.
--       이미 0048이 적용된 환경에서는 이 마이그레이션이 실행되면 에러 발생 (컬럼 중복).
--       안전을 위해 no-op으로 변경.
-- ============================================================================

-- no-op: 0048에서 이미 처리됨
SELECT 1;
