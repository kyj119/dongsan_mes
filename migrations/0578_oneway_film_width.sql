-- 0578: UV-Oneway film 에 폭을 넣는다 (137폭 단일 — 용준님 확인 2026-09-07)
--
-- 왜 필요한가:
--   `resolveLineMaterials` 의 ROLL 후보는 `width_mm != null` 인 행만 본다 — 폭을 모르면 원단에
--   출력물을 앉힐 수 없기 때문이다. `KMT-UVONEWAY` 는 폭이 비어 있어 후보에서 탈락했고,
--   그래서 이 자재를 쓰는 `SV-SHEET-PERF`(솔벤 타공시트) 11라인 매출 1,760,400원의
--   **재료비가 통째로 0** 이었다. `0576` 이 단위 축(롤/M/50 · 4,600원/M)만 맞춰 둔 상태다.
--
-- ★단위 축과 폭은 **둘 다 있어야** 원가가 나온다 — 한쪽만 고치면 조용히 0으로 남는다.
--   `0576` 주석에 「폭을 넣는 순간 바로 맞는다」고 적어 둔 그 후속이다.
--
-- 검증(실제 엔진 dry-run, 2026-09-07): 규격이 있는 5라인이 원가율 **18~19%** 로 들어온다
--   (80×189 ×4 = 40,891 · 104×175 ×2 = 19,780 · 100×283 = 15,879 · 94×205 = 11,378).
--   나머지 6라인은 규격이 0이라 NO_SIZE 로 원가 0 — 이건 폭과 무관한 별개 공백이다.
--
-- 되돌리기: `UPDATE items SET width_mm = NULL WHERE item_code = 'KMT-UVONEWAY'`.

UPDATE items
   SET width_mm = 1370
 WHERE item_code = 'KMT-UVONEWAY'
   AND width_mm IS NULL;
