-- ============================================================================
-- Migration 0070: 거래처별 세금계산서 발행 유형 (건별/월합산)
-- 컬럼이 이미 존재하는 경우를 위한 안전한 no-op
-- ============================================================================

CREATE TABLE IF NOT EXISTS _migration_0070_noop (id INTEGER);
DROP TABLE IF EXISTS _migration_0070_noop;
