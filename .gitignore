# Dependencies
node_modules/

# Build output
dist/
.wrangler/
vite.config.ts.timestamp-*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Environment variables
.env
.dev.vars

# PM2
.pm2/
pids/
logs/
*.log

# Backup files
*.backup
*.bak
*.tar.gz
*.zip

# Playwright
playwright-report/
test-results/
playwright/.cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# .NET build artifacts
**/bin/
**/obj/
**/publish/

# External tools
nssm-2.24/

# Reference materials - large binaries
참고?�료/TNSRip-X11/
LogWatcher/TNSRip-X1/

# Windows artifacts
NUL
dongsan_mes

# Business data files (sensitive)
거래�?ecount.xlsx
?�스�??�황_비교.md

# Preview/temp HTML
*-preview.html
design_preview.html

# Playwright MCP
.playwright-mcp/

# Debug/test screenshots
screenshots/

# Claude local settings (session-specific)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/.edit_counter
.claude/deployments/
.claude/projects/
# 에이전트 worktree 사본 (리포 전체 복제 — 절대 커밋 금지)
.claude/worktrees/

# Config files (may contain secrets)
.config/

# PowerShell typo artifacts (user terminal 오타)
h --force-with-lease origin main
tatus

export_parts/
import_*.sql
import_*.json
local_data.sql
run_import.ps1
token.txt
test_name.txt
_tmp_payload.json
wf4-*.png

# === History cleanup 2026-05-08 ===
**/publish/
**/bin/Release/
**/bin/Debug/
**/obj/
*.exe
*.dll
*.pdb
IllustratorAutomat/test-data/input/
LogWatcher/TNSRip-X1/CMS Demo Sample/
tmp_*.csv
tmp_*.json
# === PII/데이터 덤프 재유입 방지 (history purge 2026-06-09) ===
tmp_*
*.hwp
*.hwpx
품목정보.xlsx
/57825320
# 로컬 스크래치 (untracked)
arxiv_*.txt
queue*.txt
tools/8월/_preview/
tools/9월/_preview/
tools/10월/_preview/
tools/11월/_preview/
tools/8월/_jsx_results/
tools/9월/_jsx_results/
tools/10월/_jsx_results/
tools/11월/_jsx_results/

# === Strict ignore (large directories) ===
tools/
!LogWatcher/Tools/
LogWatcher/deploy_pkg/
LogWatcher/deploy_multi/
IllustratorAutomat/bin/
IllustratorAutomat/obj/
IllustratorAutomat/publish/
IllustratorAutomat/test-data/
IllustratorAutomat/TestRunner/bin/
IllustratorAutomat/TestRunner/obj/
LogWatcher/bin/
LogWatcher/obj/
LogWatcher/publish/
LogWatcher/TNSRip-X1/
EdgeAgent/bin/
EdgeAgent/obj/

# === Whitelist: tools 안의 실제 도구만 보존 ===
!tools/*.py
!tools/*.cjs
!tools/*.ps1
!tools/*.json
!tools/*.csv
!tools/*.txt
!tools/mesopen/
!tools/mesopen/**

# Claude Code 최적화
*.tsbuildinfo
.claude/.tsbuildinfo
.claude/.dom-baseline
queue/logs/
queue/running/
.claude/.queue-runner.lock

# 품목 마스터 작업 데이터 (대용량 xlsx, 코드 아님)
품목마스터/
insurance-report-2026-06-detail.png
payroll-ledger-employer-burden.png

# DB 백업 (PII 포함 — 절대 커밋 금지)
backups/

# 이관 작업 산출물 (거래처 이름·사업자번호·주소·Email·전화 = PII 포함 — 커밋 금지)
# 분석 문서(.md)와 규칙 모듈(.py)은 추적 대상. 데이터 덤프만 제외한다.
docs/dongsan-import/*.csv
docs/dongsan-import/backup/
__pycache__/
# 이관 데이터 SQL — prod 적용 완료분. 거래처 PII가 리터럴로 박혀 있어 git 히스토리에 남기지 않는다.
# (로컬/신규 D1 베이스라인은 db:reset 이라 이관 데이터가 없어도 무관 — project-db-bootstrap-squash)
migrations/0498_dongsan_import_clients.sql
migrations/0505_dongsan_import_clients2.sql
migrations/0506_dongsan_import_clients3.sql
# 매출 적재 생성 SQL — 담당자명·배송처주소 등 PII 포함. 생성기(gen_sales_sql.py)만 추적.
docs/dongsan-import/load/

# LogWatcher 전사 8색 배포 산출물 (Z: 로 복사, 저장소에는 두지 않는다)
publish-8c/

# 엑셀 lock 파일 — 검토 중 열어 두면 생긴다. 커밋에 섞이면 rebase 가 막힌다(2026-08-09)
~$*
LogWatcher/kit/out/

# zscan 로컬 캐시(정본 아님 — 지워도 서버 멱등으로 복구)
.zscan-ledger.json
.zscan-cycle.lock
zscan-cycle.log
prune_links.sql
sync_clients.sql

# 분석 산출물 데이터 덤프 (통장·카드 거래·거래처명 = PII/금융정보 — 커밋 금지)
# 분석 문서(.md)·롤백 SQL(백업테이블 참조라 리터럴 PII 없음)·규칙 스크립트(.py)는 추적 대상.
docs/analysis/*.csv
docs/analysis/*.xlsx
docs/order-file-matching/*.csv
