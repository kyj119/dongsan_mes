# 목록 화면 UX 갭 감사 — 이카운트 대비 (2026-08-08)

> 트리거: "주문관리 페이지 전체 주문 수량이 어떻게 산정되는지 모르겠다. 필터·기본값도 아쉽다."
> 범위: 코드 수정 없음. 주요 거래 목록 13개 페이지를 동일 항목으로 전수 대조.
> 산출: 갭 목록(G1~G12) + 우선순위 + 페이지별 현황 매트릭스 + 실행 순서.

## 0. 결론 3줄

1. **상단 통계 카드와 아래 목록은 서로 다른 모집단을 센다.** 통계는 전 기간·취소 포함·필터 무시, 목록은 최근 1개월·취소 제외·필터 반영. 두 숫자는 구조적으로 일치할 수 없다. 같은 결함이 주문·발주·견적·출고·구매요청 5개 페이지에 동일 패턴으로 존재한다.
2. **수량·금액 합계가 어디에도 없다.** 주문 목록에는 수량 열 자체가 없고(`item_count`는 품목 *건수*), 합계행이 있는 곳은 원장·견적 상세뿐이다. 이카운트 목록의 기본 합계행(수량·공급가·부가세·합계) 대응물이 없다.
3. **필터 상태를 기억하는 페이지는 주문 1곳뿐**이다. 나머지 12개는 페이지를 떠나면 조건이 초기화된다. 반대로 주문은 페이지 번호까지 복원하면서 그 사실을 화면에 알리지 않는다.

## 1. 확정 방침 (2026-08-08 사용자 결정)

| # | 항목 | 결정 |
|---|---|---|
| D1 | 통계 카드 | **현재 필터 조건에 연동** + 카드 클릭 시 해당 상태로 드릴다운. 카드에 집계 기준 캡션 표기 |
| D2 | 기본 조회 기간 | **최근 1개월 유지**. 단 화면에 `최근 1개월 ✕` 칩으로 상시 노출해 원클릭 해제 |
| D3 | 이번 작업 | **감사 문서까지**. 구현 착수는 별도 승인 |

D1·D2는 아래 G1·G3의 해결 방향으로 확정된 것이며, 본 문서는 구현 전 단계다.

## 2. 갭 목록

### P0 — 숫자를 믿을 수 없음 (오판 유발)

#### G1. 통계 카드 모집단 ≠ 목록 모집단
`/api/orders/stats`(`src/routes/orders/queries.ts:86`)는 `orders` 전량을 `GROUP BY status`로 세고, `total`에 모든 상태를 합산한다(`:100`). 즉:

- **기간 조건이 없다** — 이관분 포함 전 기간 누적
- **`CANCELLED`·`QUOTATION`도 `total`에 포함** — 목록은 기본으로 이 둘을 제외한다(`src/scripts/orders.js:402`)
- **검색어·거래처·배송방법·회계상태·우선순위·지연 필터를 전부 무시**

반면 목록은 기본 최근 1개월(`src/scripts/orders.js:321`)에 위 필터가 모두 걸린다. 화면 상단 "전체 주문 N"과 하단 "총 M건"이 크게 어긋나는 것이 정상 동작인 상태다.

동일 패턴 확산:

| 페이지 | stats 엔드포인트 | 반영하는 조건 | 무시하는 조건 |
|---|---|---|---|
| 주문 | `orders/queries.ts:86` | 법인 가시성만 | 기간·검색·상태 외 전부 |
| 발주 | `purchaseOrders/po-queries.ts:22` | 법인·법인간거래 토글 | 기간·검색·공급처 |
| 견적 | `quotations.ts:154` | 검색·거래처 | **기간** |
| 출고 | `shipments.ts:109` | 법인·취소제외 | 기간·검색 |
| 구매요청 | `purchaseRequests.ts:152` | 일부 조건 | 기간 |

견적만 검색을 반영하고, 기간을 반영하는 곳은 하나도 없다.

#### G2. 수량·금액 합계 부재
- 주문 목록 SELECT에 수량 집계가 없다(`src/routes/orders/core.ts:33-51`). `item_count`는 품목 **건수**이며 수량이 아니다.
- 테이블 헤더에도 수량 열이 없다(`src/pages/orders.ts:183-194`: 주문번호·거래처·품목·납기일·금액·상태·회계반영·등록일·액션).
- 조회 결과의 **금액 총합도 없다**. 페이지네이션 옆 "총 N건"(`src/scripts/orders.js:288,309`)이 유일한 집계다.
- 합계행(`tfoot`)이 있는 곳: 원장·급여·리포트·견적서/명세서 **출력물**뿐. 목록 화면에는 없다.

이카운트는 조회 즉시 하단 합계로 "이 조건의 총 수량·공급가·부가세·합계"를 보여준다. 현재는 CSV로 내려 엑셀에서 더해야 같은 답을 얻는다.

### P1 — 조작성

#### G3. 기본 기간 필터가 숨어 있음
기본 `date_from = 오늘-1개월`이 자동 적용되는데(`src/scripts/orders.js:321,1521`), 해당 입력은 "더보기" 접힌 패널 안에 있다(`src/pages/orders.ts:126-135`). 접힌 상태에서는 **왜 지난달 주문이 안 보이는지 화면에서 알 수 없다.** 활성 필터를 요약해 보여주는 칩이 없다(전 페이지 중 `materialForecast`·`shipments`에만 유사 UI 존재).

#### G4. 필터 상태 저장이 주문 1곳뿐
`localStorage.setItem` 호출 수 — 주문 10, **나머지 12개 페이지 전부 0**. 견적·발주·입고·출고·거래처·재고·세금계산서·원장·매입계산서·생산·지급요청·구매요청은 페이지 이탈 시 조건이 사라진다.

#### G5. 통계 카드에 드릴다운 없음
카드 4개는 표시 전용이다. "확정 12"를 눌러 확정 목록으로 가는 동작이 없어, 사용자가 상태 셀렉트를 다시 찾아 골라야 한다.

#### G6. 정렬 기본값·선택지 부족
- 기본 `created_at_desc`(`src/pages/orders.ts:119`). 이관 데이터는 `created_at`이 "이관 실행 시각"이라 업무상 무의미 — CLAUDE.md 정렬 규칙(업무일자 우선)과 충돌한다.
- 선택지가 등록일·납기일 4종뿐. **금액순·거래처명순이 없다.**

#### G7. 초기화 버튼 편차
`resetAllFilters` 계열이 있는 페이지는 주문·거래처 2곳뿐. 나머지는 조건을 하나씩 손으로 되돌려야 한다.

### P2 — 편의

| # | 갭 | 근거 |
|---|---|---|
| G8 | **조회조건 프리셋 저장 없음** (이카운트 "조회조건 저장" 대응물). 전 페이지 0건 | grep: `filterPreset\|savedFilter` 0 hit |
| G9 | **열 선택·순서·저장 없음.** 전 페이지 0건 | grep: `columnChooser\|visibleColumns` 0 hit |
| G10 | **CSV 3,000행 무고지 절단** (최대 5,000). 초과분이 조용히 잘려 합계가 어긋난다 | `orders/queries.ts:416-417` |
| G11 | **페이지 번호까지 복원**. 재방문 시 3페이지에서 시작하는데 표시가 없다 | `orders.js:1519` |
| G12 | **페이지당 건수 제각각**(20/50/100/500/1000), 사용자가 바꿀 수 없음. 일부 목록은 페이징 자체가 없다 | 견적 20 / 주문 50 / 원장 100 / 세금계산서 1000 |

## 3. 페이지별 현황 매트릭스

○=있음 △=부분 ✕=없음

| 페이지 | 통계카드 | 필터연동 | 합계행 | 필터저장 | 초기화 | 총건수 | CSV | 기본기간 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 주문 `/orders` | ○ | ✕ | ✕ | ○ | ○ | △ | ○ | 1개월(숨김) |
| 견적 `/quotations` | ○ | △ | △ | ✕ | ✕ | ✕ | ✕ | ✕ |
| 발주 `/purchase-orders` | ○ | ✕ | △ | ✕ | ✕ | △ | ○ | ✕ |
| 입고 `/receiving` | ○(발주 것 재사용) | ✕ | ✕ | ✕ | ✕ | ✕ | ○ | ✕ |
| 출고 `/shipments` | ○ | ✕ | △ | ✕ | ✕ | △ | ○ | ✕ |
| 거래처 `/clients` | ✕ | — | ✕ | ✕ | ○ | △ | ✕ | ✕ |
| 재고 `/inventory` | ○ | ✕ | ✕ | ✕ | ✕ | ✕ | ✕ | ✕ |
| 세금계산서 `/tax-invoices` | ✕ | — | △ | ✕ | ✕ | ✕ | ○ | ✕ |
| 원장 `/ledger` | ✕ | — | ○ | ✕ | ✕ | △ | ○ | ✕ |
| 매입계산서 | ✕ | — | ✕ | ✕ | ✕ | △ | ✕ | ✕ |
| 생산 `/production` | ○ | △(기간O) | ✕ | ✕ | ✕ | △ | ✕ | ✕ |
| 지급요청 | ○ | ✕ | ✕ | ✕ | ✕ | △ | ✕ | ✕ |
| 구매요청 | ○ | △ | ✕ | ✕ | ✕ | ✕ | ○ | ✕ |

`입고`가 발주 stats를 쓰는 것은 **정상**이다 — 입고대기·부분입고 카운트가 곧 발주 상태 `CONFIRMED`·`PARTIAL_RECEIVED`이기 때문(`src/scripts/receiving.js:65-77`). 단 이 숫자도 기간·검색을 반영하지 않는 점은 G1과 동일하다.

## 4. 제안 실행 순서

| 단계 | 내용 | 대상 | 비고 |
|---|---|---|---|
| ~~P1-1~~ | ~~주문 통계 카드 필터 연동 + 드릴다운 + 기준 캡션 (D1)~~ **구현완료(미배포) 2026-08-08** | `/orders` | `src/routes/orders/listFilter.ts` SSOT 신설 — 목록·카운트·통계·CSV 4경로 공유. 상세는 §5 |
| ~~P1-2~~ | ~~활성 필터 칩 + 기본기간 노출 (D2)~~ **구현완료(미배포) 2026-08-08** | `/orders` | 캡션 줄을 칩 행으로 승격 — 상세는 §5 |
| ~~P1-3~~ | ~~목록 하단 합계 바 (건수·수량·공급가·부가세·합계)~~ **완료 2026-08-08** | `/orders` | 조회조건 **전체** 기준. 상세는 §5 |
| ~~P1-4~~ | ~~정렬 기본값 업무일자 전환 + 금액·거래처순 추가~~ **완료 2026-08-08** | `/orders` | 정렬표도 SSOT로 이동(CSV와 공유) |
| ~~P2~~ | ~~P1 결과를 공통 컴포넌트로 추출 후 발주·견적·입고·출고에 이식~~ **완료 2026-08-08 (출고 제외 — §6)** | 3개 | 상세는 §6 |
| ~~P3~~ | ~~프리셋 저장 / 열 선택 / 페이지당 건수 선택~~ **완료 2026-08-08** | 4개 | 서버 저장(사용자별). 상세는 §8 |

주문 한 페이지를 완성해 패턴을 확정한 뒤 이식하는 순서를 권한다. 공통 컴포넌트를 먼저 만들면 13개 목록이 한 번에 회귀 위험에 노출된다.

## 5. P1-1 ~ P1-4 구현 기록 (2026-08-08)

### P1-3 — 합계 바 (G2)

목록 하단에 `건수 · 수량 · 공급가 · 부가세 · 합계`. **조회조건 전체 기준이며 현재 페이지 합이 아니다** — 여러 페이지일 때는 문구를 "조회조건 전체 합계 (현재 페이지 아님)"로 바꿔 오해를 막는다.

- 금액 3열 = `orders.total_amount`(공급가) / `vat_amount` / `final_amount`(합계). 카운트 쿼리에 `SUM` 을 얹어 **왕복 추가 없음**.
- **수량은 별도 쿼리**다. 주문:품목이 1:N 이라 목록 쿼리에 `JOIN` 하면 주문 행이 불어나 **금액이 중복 합산**된다.
- 수량은 **최상위 라인만**(`parent_item_id IS NULL`) — 자식 라인 수량은 부모에 이미 포함된다. 검증에서 자식 99를 심어 제외를 확인했다.
- 수량 집계 실패는 목록을 막지 않는다(try/catch → 0).
- 총 건수 표기를 합계 바로 옮기고 페이지네이션은 "N / M 페이지"만 남겼다(같은 숫자를 두 곳에서 말하지 않도록).

### P1-4 — 정렬 (G6)

- 기본값 `created_at_desc` → **`order_date_desc`(주문일 최신순)**. 이관분의 `created_at` 은 '이관 실행 시각'이라 업무상 무의미하다.
- 추가: 주문일 오래된순 · 금액 큰순/작은순 · 거래처명 가나다순 (4종 → 9종).
- 라벨에 기준 명시("주문일 최신순" / "등록 최신순").
- **정렬표도 `listFilter.ts` SSOT 로 이동.** CSV 에 4개짜리 사본이 따로 있어, 목록에서 고른 정렬이 CSV 에서 **조용히 기본값으로 떨어지고 있었다.**
- 모든 옵션에 `o.id` tie-break 유지, NULL 은 `col IS NULL` 선행(D1 `NULLS LAST` 회피). 알 수 없는 키는 기본값 폴백(화이트리스트 = SQL 주입 차단).

### P1-2 — 활성 조회조건 칩 (D2)

P1-1이 만든 "집계 기준" 캡션과 칩이 **같은 조건을 두 번 말하게 되므로, 캡션 줄을 칩 행으로 승격**시켰다(정보 중복 제거). 조건 표기가 화면에 한 곳만 존재한다.

- 기본 최근 1개월도 사용자가 고른 조건과 **똑같이 칩으로** 뜬다 — "왜 지난달이 안 보이지"가 화면에서 해소된다(G3).
- 칩의 `✕` = **그 조건만** 해제(나머지 유지). 기존 「초기화」 버튼은 기본값 복귀라 역할이 다르다.
- 조건이 없으면 `전체 기간`·`취소·견적 제외`를 **회색 비활성 칩**으로 표기 — 빈 줄이 되지 않게 하고 암묵 규칙을 드러낸다.
- 카드에 없는 상태(취소 등) 선택 시 **경고 칩**("위 카드는 이 상태를 포함하지 않음")으로 카드-목록 불일치를 숨기지 않고 드러낸다.
- 칩·카드 강조는 필터 값만으로 그린다 — `loadOrders()`에서 렌더하므로 **통계 API가 실패해도 조건 표시는 남는다**.
- 상태 라벨은 셀렉트 옵션 텍스트를 정본으로 읽는다(`ordStatusFilterLabel`). 라벨을 따로 적으면 두 벌이 된다.

⚠️ 작업 중 `check:dom` 훅이 페이지에서 제거한 `#orderStatsCaption`의 잔존 참조를 잡아냈다(5→6건). 훅이 의도대로 작동한 사례.

### P1-1 — 통계 카드 필터 연동·드릴다운 (D1)

**조치의 핵심은 카드 수정이 아니라 조회조건 SSOT 신설이다.** 카드만 고치면 다음 조건 추가 때 또 갈라진다.

| 파일 | 변경 |
|---|---|
| `src/routes/orders/listFilter.ts` | **신설.** `buildOrderListFilter(c, {skipStatus})` — 가시성·상태·검색·기간·제외상태·우선순위·금액·배송·회계·지연 전 조건을 한 곳에서 조립 |
| `src/routes/orders/core.ts` | 목록·카운트 WHERE 2벌(약 145줄)을 빌더 호출로 대체 |
| `src/routes/orders/queries.ts` | `/stats` 를 빌더 기반으로 교체(`skipStatus`), CSV export도 빌더 공유 |
| `src/pages/orders.ts` | 카드 4개를 `<button>` 으로 전환(드릴다운·강조), 기준 캡션 영역 추가 |
| `src/scripts/orders.js` | `ordReadFilters`/`ordBuildParams` 클라 SSOT, `loadOrderStats` 필터 연동, 칩·강조·드릴다운 토글(`ordRenderFilterChips`·`ordClearFilter`) |

곁가지로 드러난 실결함 2건 — 조건이 흩어져 있어 생긴 것이라 통합과 동시에 해소됐다:

1. **CSV export에 주문 가시성(법인) 필터가 없었다.** 타 법인 주문이 함께 내려갔다.
2. **카운트 쿼리의 금액 범위에 NaN 가드가 없었다.** 목록(가드 있음)과 총건수가 갈릴 수 있었다.

설계상 결정:

- 카드는 **status를 제외**하고 집계한다(`skipStatus`). 카드가 곧 상태 선택 수단이므로 자기 자신으로 걸러지면 드릴다운이 불가능해진다. 따라서 카드 = 현재 스코프의 상태별 분포, 전체 = 그 합.
- `status` 파라미터에 **복수 값(콤마)** 을 허용했다 — 「생산중」= `PRINTING,PRINT_DONE`.
- 카드에 없는 상태(취소 등)를 고르면 캡션이 "목록은 선택한 상태만 표시 — 카드와 다름"을 명시한다.

검증: tsc·build·`node --check`·smoke 110/110·`audit:entity` 누락 0·`sort-audit` P1 0건·`check:dom` 기준선 5건 유지.

로컬 실브라우저:
- P1-1: 9개 필터 조합과 드릴다운 3종 전량 카드==목록 일치, 토글·강조 정상.
- P1-2: 기간칩 ✕ 로 7→10건 확대, 다중 조건 칩 개별 표기, 배송칩만 해제 시 검색·지연 유지, 카드 드릴다운→상태 칩 생성, 취소 선택 시 경고 칩.
- P1-3: 3개 조건에서 수량 = 건수×15 (자식 라인 99 제외 확인), 공급가+부가세 = 합계 원 단위 일치, 다중 페이지 문구 전환.
- P1-4: 새 정렬 4종 목록 반영, 잘못된 키는 기본값 폴백, **CSV 도 같은 정렬·필터 적용**(배송방법 필터 2건 일치).

콘솔 에러 0.

## 6. P2 이식 기록 (2026-08-08)

### 공통 자산 추출

"무엇을 보여줄지"(어떤 조건이 있고 무엇을 합산하는지)는 페이지가 정하고, **"어떻게 그릴지"만** 공통으로 뺐다. 페이지마다 필터 종류가 제각각이라 그 이상 공통화하면 오히려 엉킨다.

| 자산 | 위치 | 내용 |
|---|---|---|
| CSS | `src/layout/shared-styles.ts` | `ds-stat*`(클릭 카드) · `ds-cond*`(조회조건 칩) · `ds-summary*`(합계 바) |
| 렌더러 | `src/scripts/layout/shell.js` `window.dsListUx` | `renderChips` · `renderSummary` · `markActiveStat` |

- 칩은 `innerHTML` 이 아니라 **DOM 생성 + addEventListener** 로 만든다. 검색어에 `<img src=x onerror=...>` 를 넣어 주입되지 않음을 확인했다.
- ⚠️ **이름 충돌 사고 1건**: 처음에 칩 클래스를 `.ds-chip` 으로 만들었는데 그 이름은 **이미 '클릭하는 필터 토글 칩'으로 정의돼 있었다**(737행, 출고 페이지 택배사 배지 등이 사용). 뒤에 온 정의가 기존 배지를 덮어쓰는 상태였다 → `.ds-cond` 로 개명. 출고 배지가 원래 스타일(흰 배경·`4px 12px`·pointer)을 유지하는지 실측해 확인했다.
- ⚠️ **템플릿 리터럴 함정 재확인**: `shared-styles.ts` 는 백틱 템플릿이라 **주석 안의 백틱도 문자열을 끊는다**(빌드 실패로 드러남).

### 페이지별 적용

| 페이지 | 조회조건 SSOT | 통계 카드 | 칩 | 합계 바 |
|---|---|---|---|---|
| 발주 `/purchase-orders` | `purchaseOrders/listFilter.ts` (신설) | 필터 연동 + 드릴다운 3종 | ○ | 건수·수량·공급가·부가세·합계 |
| 견적 `/quotations` | `quotationsListFilter.ts` (신설) | 필터 연동 + 드릴다운 3종 | ○ | 건수·수량·공급가·부가세·합계 |
| 입고 `/receiving` 이력탭 | `po-receipts.ts` `buildReceiptFilter` | (해당 없음 — 카드는 발주 대기 지표) | ○ | 건수·합격수량·불합격수량·입고금액 |

발주·견적·입고 모두 **목록·카운트·CSV 3벌 복사**가 있었고, 통합하면서 CSV 가 화면 조건(공급업체·지연·정렬 등)을 따르도록 정합시켰다.

### 이식 중 발견한 실결함

1. **견적의 작성일 from/to 가 죽은 UI였다.** 입력칸이 `loadQuotations(1)` 을 부르지만 그 값을 읽지 않았고 서버도 받지 않았다 → `date_from`/`date_to` 신설로 동작. 실측 5건 → 8월 이후 3건.
2. **견적 상태 '주문전환'도 동작하지 않았다.** 셀렉트에 값은 있는데 어느 코드도 처리하지 않았다 → 서버 조건 `has_orders` 로 구현.
3. **견적 '부분주문' 필터가 현재 페이지 행만 걸렀다.** 총건수·합계와 어긋나는 구조 → 서버 조건으로 승격.
4. **발주 정렬표가 CSV 에 따로 있었다**(주문과 같은 문제).

### 출고 `/shipments` — 적용하지 않음 (근거)

같은 패턴을 넣을 수 없어서가 아니라 **넣을 대상이 아니다.**

- 출고 실행 탭은 **하루치 작업 보드**다. 날짜 하나를 고르고 택배사별 섹션에서 처리하는 화면이라, 다조건 필터 + 페이징 목록이라는 전제가 성립하지 않는다.
- 택배사별 건수 배지는 **로드된 그 날짜 데이터에서 클라이언트가 세어** 표시한다. 별도 집계 쿼리가 없으니 G1(카드 ≠ 목록) 자체가 발생할 수 없다.
- `/api/shipments/stats` 는 **어느 화면도 호출하지 않는 죽은 엔드포인트**다(호출처 0건). 잘못된 숫자가 사용자에게 보일 경로가 없다.

출고를 손보려면 "하루 보드"가 아닌 **출고 이력 목록**을 새로 정의해야 하며, 이는 이 감사의 범위를 넘는 별도 설계 건이다.

## 7. P3 구현 기록 (2026-08-08)

G8(프리셋)·G9(열 선택)·G12(페이지당 건수)를 **주문·발주·견적·입고 4개 페이지**에 넣었다.

### 저장 위치 = 서버(사용자별)

같은 사람이 사무실 PC·디자인실 PC 를 오간다. localStorage 면 PC 마다 따로 놀고 공용 PC 에서는 남의 설정이 보인다 (사용자 결정).

- 마이그레이션 **0525** — `user_filter_presets`(이름 붙인 조회조건 + 기본 지정) · `user_ui_prefs`(열 표시·페이지당 건수 키-값).
- 기존 `settings` 는 **전역 키-값이고 ADMIN 전용**이라 쓸 수 없었다.
- API `src/routes/userPrefs.ts` — **user_id 는 토큰에서만** 온다. body 로 받으면 남의 설정을 조작할 수 있다. 삭제·기본지정은 `WHERE id = ? AND user_id = ?` 로 소유를 함께 건다.
- 상한: 페이지당 프리셋 30개, 값 8KB. 서버가 정본이고 UI 는 보조.

### 열 선택을 CSS 로 한 이유

각 페이지의 행 빌더는 거대한 템플릿 문자열이다. 이를 컬럼 정의 배열로 바꾸면 **4개 목록의 렌더링을 동시에 갈아엎는** 셈이라 회귀면이 크다.

대신 `<th>` 에 `data-col` 을 달고, 숨길 열의 `nth-child` 인덱스로 `display:none` CSS 를 주입한다. 행 빌더는 한 줄도 건드리지 않는다. 전제는 "thead 의 th 순서 = tbody 의 td 순서"이며 4개 목록 모두 그렇다. 빈 상태 행은 `colspan` 이라 영향받지 않는다(의도).

- 전부 숨기면 빈 표가 되므로 **최소 1열**을 강제한다.
- 체크박스·액션 열에는 `data-col` 을 달지 않아 숨김 대상에서 빠진다.

### 렌더 순서

도구모음을 먼저 붙여 저장된 설정을 읽고, **그 결과로 첫 조회**를 한다. 설정을 모른 채 조회하면 틀린 건수·열로 한 번 그린 뒤 다시 조회하게 된다. 기본 프리셋이 있으면 그 적용이 첫 조회를 대신한다(중복 조회 없음 — 실측 1회).

입고는 이력 탭이 **탭 전환 때 처음 뜨므로** 그 시점에 한 번만 마운트한다(표가 `hidden` 이면 마운트가 안 된다). 탭을 오가도 중복 마운트되지 않음을 확인했다.

### 검증

tsc·build·smoke 110/110·`audit:entity` 0·`sort-audit` P1 0·`check:dom` 기준선.
브라우저: 4개 페이지 도구모음 표시, 열 숨김이 헤더·본문 셀 모두 적용, 새로고침 후 서버에서 복원, 기본 프리셋 자동 적용, 페이지당 건수 변경이 요청 `limit` 에 반영(100 확인), 첫 조회 1회.
경계: 비인증 401 · 없는 프리셋 404 · 과대 값 400.

## 8. 잔여 목록 이식 (2026-08-09)

P1~P3 에서 만든 자산(조회조건 SSOT · 칩 · 합계 바 · 도구모음)을 나머지 목록에 적용했다.

### 적용 (6개)

| 페이지 | 조회조건 SSOT | 통계 카드 | 칩 | 합계 바 | 도구모음 |
|---|---|---|---|---|---|
| 구매요청 `/purchase-requests` | `buildPrFilter` 신설 | 필터 연동 | ○ | 건수·요청수량·예상금액 | ○ |
| 지급요청 `/payment-requests` | `buildPayReqFilter` 신설 | 필터 연동 + 드릴다운 4종 | ○ | 건수·금액 | ○ |
| 매입확정 `/purchase-invoices` | (이미 단일 WHERE) | — | ○ | 건수·금액 | ○(탭 진입 시) |
| 재고 `/inventory` | (이미 단일 경로) | — | ○ | 품목수·총수량·재고금액 | ○ |
| 세금계산서 `/tax-invoices` | `buildTaxInvoiceFilter` 신설 | — | ○ | 건수·공급가액·세액·합계 | ○(탭 진입 시) |
| 거래처 `/clients` | (이미 단일 경로) | — | ○ | — (금액 열 없음) | ○(페이지당 건수 제외) |

- **지급요청 KPI 가 '최근 90일' 고정이었다** — 라벨에 그 사실도 없이 목록과 다른 모집단을 셌다(G1 동형). 목록 조건에 연동하고 드릴다운을 붙였다.
- **구매요청·세금계산서는 WHERE 가 목록·카운트 2벌**이었다 → SSOT 로 통합.
- **구매요청 SSOT 에는 `MANAGER 는 본인 요청만` 규칙이 포함**된다. 이 조건이 빠지면 권한 경계가 무너지므로 통계·카운트에도 반드시 같이 걸려야 한다.
- **재고 합계는 `GROUP BY`/`HAVING` 결과를 감싸서** 집계한다(품목당 1행으로 접은 뒤라야 수량·금액이 맞는다).
- **거래처는 표를 JS 가 매번 새로 그린다** → 도구모음을 첫 렌더 뒤에 한 번 마운트하고, 열 숨김 CSS 는 클래스 선택자라 재렌더 후에도 유지된다(실측 확인).
- 거래처엔 이미 자체 '페이지당' 선택이 있어 도구모음의 건수 컨트롤은 `showPageSize: false` 로 뺐다. 두 개 두면 어느 쪽이 정본인지 모호해진다.

### 적용하지 않음 (3개) — 형태가 다르다

| 페이지 | 이유 |
|---|---|
| 출고 `/shipments` | **하루치 작업 보드**. 날짜 하나를 고르고 택배사별 섹션에서 처리하는 화면이라 다조건+페이징 목록 전제가 없다. 배지는 로드된 행에서 클라가 세므로 카드↔목록 불일치가 원천적으로 불가능하고, `/api/shipments/stats` 는 **호출처 0건**이다. |
| 원장 `/ledger` | **리포트 허브**. 탭마다 성격이 다른 표가 있고 이미 `tfoot` 합계행을 갖고 있다. 어느 표에 도구모음을 붙일지가 페이지 차원의 설계 문제이며, 일괄 이식 대상이 아니다. ⚠️**정정(2026-08-09)**: 여기 처음 적었던 "15개 탭"은 틀렸다 — `grep -c` 가 탭 버튼이 아닌 문자열까지 세서 나온 수치다. **실제 탭은 3개**(매출·매입·분석)이고 그 안의 표가 9개다. 설계 = `docs/specs/2026-08-09-ledger-query-ux.md` |
| 생산 `/production` | **대시보드**. 최근 이벤트·작업기록·미매칭 등 섹션 나열이지 필터+페이징 목록이 아니다. 설계 = `docs/specs/2026-08-09-production-history-list.md` |

이 3개는 "붙일 수 없어서"가 아니라 **붙일 대상이 아니어서** 제외했다. 손대려면 각각 "출고 이력목록", "원장 탭별 조회 UX", "생산 실적목록"을 새로 정의해야 하며 별도 설계 건이다.

## 9. 확인 필요 (판단 보류)

- **합계의 정의** — 취소분 포함 여부, 부가세 별도 표기 여부, 합배송·분할청구 건의 중복 합산 처리.
- ~~**CSV 절단 한도** — 3,000행 상향 vs 초과 시 경고 표시 중 택일.~~ → **둘 다 적용(§10 A).** 캡을 형제와 같은 5,000 으로 올리고 초과 시 안내행을 넣는다.

## 10. 사후 점검 라운드 (2026-08-09) — 이식 끝난 뒤 다시 훑어 나온 것

이식이 끝났다고 끝난 게 아니었다. **실측 위주로** 다시 훑어 9건이 나왔고 그중 3건은 이식 작업 자체가 만든 게 아니라
**원래 있었는데 안 보이던 것**이다.

### A. (P0) 주문 CSV 무고지 절단 — 8,778건 중 3,000건

`orders/queries.ts` 만 `#372` 스윕에서 빠져 있었다. 발주·입고·발주요청·자금계획·검수는 이미
`LIMIT 캡+1` + `CSV_TRUNCATION_NOTE` 로 고쳐졌는데 **가장 큰 목록 하나가 남았다** — 형제-불완전 스윕.

- 실측: 전체 기간 내보내기 → 3,000행, 마지막 행 `2026-05-15`, **5,778건(66%) 유실**, 안내 없음.
- 원인 구조: 주문만 `csvStreamResponse`(메모리 절약용 스트리밍)를 쓰는데 **이 함수에 `footerNote` 가 없었다.**
  안내를 붙일 수단 자체가 없으니 스윕이 지나간 것이다.
- 조치: `csvStreamResponse` 에 `footerNote` 추가(단일 소스 유지) → 주문도 캡 5,000 + 안내행.
- 검증: 캡을 임시로 5 로 낮춰 절단 분기를 재현(5행 + 안내행), 원복 후 미절단 시 안내 없음 확인.

### B. (P1) 번호 열이 전 행에서 잘려 식별자를 못 읽는다

| 대상 | 폭 | 필요 | 조치 |
|---|--:|--:|---|
| 주문 `order_number` | 108px | **135px** | 140px |
| 주문 `status` 배지 | 86px | 101px | 104px |
| 주문 `billing` 배지 | 82px | 101px | 104px |
| 발주 `po_number`(`.col-code`) | 132px | **~140px**(표준 15자) | 공통 규격 **148px** |

- 원인은 **번호 체계가 법인 접두(`E1-`)로 길어진 것**이다. 규격이 데이터를 못 따라갔다 → 페이지 패치가 아니라
  **공통 `.col-code` 를 고쳤다**(memory `feedback-systemic-css-fix`).
- 발주 이관분엔 34자짜리(`E1-PO-BK-PENDING-…`)가 섞여 있다. **2건 때문에 317px 로 넓히지 않는다** — 중앙값은 15자다.
  대신 `title` 로 전체를 남긴다. (그 셀만 `escapeHtml` 도 빠져 있어 옆 열과 맞췄다.)
- 주문 번호 옆 **긴급·타법인 배지를 아랫줄로** 내렸다. 합배송 배지는 이미 같은 이유로 내려가 있었는데
  **그 판단이 나머지 둘에는 적용돼 있지 않았다.**

### C. (P1) 원장 매출 프리셋이 기간을 잃는다

`getFilters` 는 `startDate/endDate` 를 저장하는데 `applyFilters` 가 `search` 만 되돌렸다.
13개 목록의 저장/복원 키를 **스크립트로 전수 대조**해 찾은 **유일한 비대칭**이다(수정 후 13/13 대칭).

### D. (P2) 자주 쓰는 축이 「더보기」 안에 접혀 있었다 (주문)

기간(기본 1개월)과 정렬이 접힌 패널 안이라 **바꾸려면 두 번 클릭**이고, 정렬은 있는 줄도 모른다.
정작 「지연만」 체크박스는 밖에 있었다 — 우선순위가 뒤집혀 있었다. 둘을 기본 노출로 올렸다.

> 같은 김에: 날짜 입력 라벨이 **「등록일 from」** 인데 실제 조건은 `o.order_date`(주문일)였다. 라벨을 고쳤다.
> 표에 `created_at`(등록일) 열이 따로 있어서 **다른 걸 거르고 있다고 믿기 쉬운** 상태였다.

### E. (P2) 출고 이력에만 정렬 컨트롤이 없었다

서버는 `ship_date_asc/desc` 를 지원하는데 클라가 `ship_date_desc` 를 **하드코딩 전송**했다. 셀렉트를 붙였다.

### F. (P2) 견적 CSV 신설 + 기본 정렬 SSOT 무력화 수정

- `/api/quotations/export/csv` 신설(목록과 같은 SSOT·캡·안내). `'/:id'` 앞에 등록해야 한다.
- ★ `QUOT_SORT_DEFAULT` 는 `quotation_date_desc` 인데 **핸들러가 `sort = 'created_desc'` 로 덮어써서**
  SSOT 가 선언한 기본 정렬이 무력화돼 있었다. 이관 견적은 `created_at` 이 '이관 실행 시각'이라 무의미하다.

### G. (P3) 위생

- `check:dom` 상시 5건(폐기된 소재 UI 잔재) 제거 → **OK(0건)**. 상시 경고는 회귀 신호를 죽인다.
- 클라 전 파일의 `/api/*` 참조를 마운트 목록과 대조 → **죽은 호출 0건**.
  (어제 "`/api/equipment` 죽은 호출이 하나 더 있다"고 적은 건 **오기**였다. 주석을 센 것 — 스펙 문서에 정정.)

### 남은 것

| 항목 | 왜 이번에 안 했나 |
|---|---|
| CSV 없는 목록 5개(지급요청·매입계산서·재고·거래처·출력실적) | **조회조건 SSOT 빌더가 없다.** 지금 붙이면 WHERE 사본이 5벌 생겨 이번에 없앤 결함(주문 CSV 법인필터 누락)을 그대로 재생산한다. 각 목록의 필터 추출이 선행 |
| `/api/reports/entity-attribution-audit` UI 없음 | 화면이 **어디에 붙어야 하는지가 제품 결정**이다. `/reports` 는 매출 분석 탭 묶음이라 데이터 품질 감사와 성격이 다르다 |
| 거래처·원장 기본 프리셋 시 2회 조회 | 표를 그린 뒤 도구모음을 붙이는 구조라 첫 조회를 앞당길 수 없다. 요청 1회·깜빡임 수준이고, 고치려면 마운트 순서를 재설계해야 해 이득 대비 회귀면이 크다 |
