# 슬라이스 I 결과 — 검사 37파일 / 22,207줄 (정독 14파일 · 부분정독 5파일 · 미정독 항목은 맨 끝에 명시)

### 조치 필요 (심각도순)

- `js/cut-main.js:2442` — **HIGH** — `hostAtLeast(SELALL_MIN_HOST)` 에 **배열이 아니라 문자열**(`'CUT-CEP-0.24.0'`)을 넘겨 항상 false → [◎ 전체] 버튼이 **영구히 동작 불가**. — 실제 호스트는 `CUT-CEP-0.30.0`(mes-cut-host.jsx:71)이고 `mesCut_selectAllTop`(:1943)도 있는데, 화면엔 "호스트가 구버전이라 쓸 수 없습니다 — Z: 의 mes-cut-host.jsx 를 배포하세요"라는 **틀린 진단**이 뜬다(=엉뚱한 재배포를 유도). — 대조: `hostAtLeast(min)` 는 `min[i]` 를 숫자로 읽는다(`:183~191`), 다른 게이트는 전부 배열(`CURVE_MIN_HOST=[0,5,0]` `:122`).
- `js/cut-main.js:238` — **MEDIUM** — 같은 결함 2번째 자리. `BAKE1_MIN_HOST = 'CUT-CEP-0.23.0'` → `hostSupportsOneBake()` 상시 false → `:1200 wantInk` 가 항상 false. — 2026-08-27 에 넣은 "도련 원색을 마스크 굽기와 같은 호출로 받기"가 한 번도 발동하지 않고 `buildBleedPngs`(`:1309`)가 매번 다시 굽는다(주석 실측 5.7초/회 낭비). 결과는 같고 느리기만 해서 조용히 잠복. — 대조: 호스트 `mesCut_nestBakeAll(…, tag, bleedTag)` 는 0.23.0 부터 `bleedTag` 를 받는다(mes-cut-host.jsx:2052).
- `mes-cut-host.jsx:3160` — **HIGH** — 재단 패널 등록 manifest 가 **저장 배율(1/S)을 반영하지 않는다**. `measured_cm` 은 판 문서 아트보드(`:3073` = 저장 좌표 = 실물/S)이고 `scale_pct` 는 `:3159` 에 **100 하드코딩**이다. — 저장 배율 1/2 로 판을 짜면 파일명은 실물 규격(cut-main.js `pairBaseName` 이 `toRealMm` 사용)인데 대기물은 **가로·세로가 절반**으로 올라가고 배율칸도 1 로 채워진다 → 주문 라인 규격이 1/S, 청구면적 1/S²(intake.js:620·:801 이 `width_cm` 을 그대로 넣고 `100/scale_pct` 를 배율칸에 넣는다). — 대조: A0 는 `scale_pct: Math.round(100 / sN)` + 실물 `measured_cm`(mes-a0-host.jsx:982~983). 호스트는 배율을 이미 알고 있다(`MESCUT_SCALE_N`, nestApply `:2546`).
- `ExtractGroups.jsx:492` — **MEDIUM** — `_aiOpts.pdfCompatible = true` — `.ai` 는 pdfCompatible=false 규칙 위반. — `<reqId>-work.ai` 가 같은 그림을 AI 편집부 + PDF 복사본 2벌로 담아 용량이 2배가 되고 첫 바이트가 `%PDF-` 라 `audit:ia-storage` 판정 대상이 된다. 소비자는 ProcessOrderItem 의 `app.open` 뿐이라 PDF 스트림이 불필요. — 대조: 같은 저장을 하는 4곳은 전부 false(mes-a0-host:666 · mes-cut-host:3084 · mes-core:362 · mes-sheet:314).
- `SheetLayout.jsx:586~620` — **MEDIUM** — 「사후 검증」 3종이 **닫힌 문서**를 읽는다. `newDoc.close()` 가 `:582` 로 앞에 있어 `newDoc.layers`·`artboards` 접근이 매번 예외 → 각 try/catch(`e_v1/e_v2/e_v3`)가 삼켜 `verifyErrors` 가 **영원히 빈 배열** = 항상 "모든 검증 통과". — 주석("저장 전이므로 newDoc 기준")이 사실과 반대다. 게다가 돈다 해도 ①`layerA.remove()`(`:563`) 때문에 `missing_layer:Design` 이 뜨고 ③기대 아트보드가 돔보 확장(`:516~521`, 변당 `CORNER_DIST+pad`≈40mm)을 빼먹어 상시 경고다. 검증을 close 앞으로 옮기고 기대식을 고칠 것.
- `SheetLayout.jsx:653~665` — **MEDIUM** — 최상위 catch 가 **로그를 먼저 쓰고 `_ia_status` 를 나중에** 설정한다. 그 로그 경로가 `new File($.fileName).parent.fsName`(`:659`)인데, 에이전트는 `DoJavaScript(문자열)` 로 실행해 `$.fileName` 이 쓸 수 없다는 것을 이미 알고 `_ia_trace_path` 를 주입하고 있다(Program.cs:3704~3708). — 그 쓰기가 던지면 `:664` 의 status 대입에 도달하지 못하고 반환이 ""가 되어 `JsxDiag()` 가 "JSX 반환 빈값(미실행/조기종료 의심 — 일러 모달 확인)"이라는 **바로 그 틀린 진단**을 UI에 띄운다(Program.cs:3729). status 를 먼저 대입하고 쓰기를 try 로 감쌀 것.
- `PackGroups.jsx:33` (그리고 `:556`) — **MEDIUM** — 파라미터를 읽기 **전에** `var _scriptDir = new File($.fileName).parent.fsName;` 를 무조건 실행한다. `_ia_params_override_path` 로 우회하는 다른 3종과 달리 이 한 줄이 남아 있고, catch 폴백(`:556`)도 같은 식이라 실패하면 `ia_error.log` 조차 안 남는다. — 대조: ExtractGroups.jsx:518 · ProcessOrderItem.jsx:38 은 override 경로를 먼저 본다.
- `mes-a0-host.jsx:330~351 · :458~470` — **MEDIUM** — 셸 자동 갱신 서명이 **파일 삭제를 처리하지 못한다**. `mesPanel_sign` = `버전/파일수/총바이트` 인데 `mesPanel_copyTree` 는 덧쓰기만 하고 여분을 지우지 않는다(주석도 인정). — Z: 배포본에서 파일이 하나라도 빠지면 설치본 파일수가 계속 많아 `after !== srcSign` → 매 로드 `ERROR verify` + 롤백, 2회 뒤 `skip;why=retrylimit` 로 **조용히 영구 중단**된다(그 뒤로 그 PC 는 자동 갱신이 안 되고 셸이 안 온 줄도 모른다). 삭제분 정리(또는 dst 여분 무시) 필요. — 나머지 계약(`.bak-*` 양쪽 제외 `:313`·백업을 extensions **밖** `_panel_backups` `:448`·롤백 성패를 재서명으로 판정 `:407`·2회 실패 중단 `:441`)은 명세대로다.
- `mes-a0-host.jsx:964` · `mes-cut-host.jsx:3141` — **MEDIUM** — manifest `entity_id` 가 **1 로 하드코딩**(재단은 패널이 보내지 않는 `R.ENTITY` 의 폴백 1). 에이전트는 manifest 를 그대로 POST 하고(Program.cs:1080~1092) 서버는 `Number(body.entity_id) || getWriteEntityId(c)` 라 **본문 값이 세션 법인을 이긴다**(workbench.ts POST /intakes). — `/intake-config` 는 "에이전트가 채워 넣을 귀속 법인"이라며 `entity_id` 를 내려보내는데(workbench.ts) 읽는 쪽이 없다 → 선명·청주 작업도 전부 법인 1 로 귀속.
- `js/main.js:1391` · `:1583` — **MEDIUM** — `mesA0_queueRemove(i)` 의 **결과를 보지 않고** 패널 큐를 `splice` 한다. 호스트 호출이 실패해도(문서 닫힘·`EvalScript error.`) 패널만 줄어 host `$.global.mesA0Q` 와 인덱스가 어긋나고, 이후 `mesA0_queueSelect(i)` 가 **다른 조각**을 골라 엉뚱한 행이 가공된다. — 바로 그 정합을 지키려고 내림차순·콜백 체이닝까지 해 둔 자리라(`:1578` 주석) 성공 판정만 빠졌다.
- `js/main.js:1014` — **MEDIUM** — `seedSilhouette()` 의 `if (hostBusy) return;` 이 **done 콜백을 부르지 않는다**. 호출자(묶음분리·자동감지·모아찍기 분리)는 아무 응답도 못 받고 화면이 직전 문구인 채로 멈춘다 — 버튼이 잠겨 있어 흔치 않은 경로지만, 이 패널이 특히 경계하는 "콜백 무음 사멸" 형태다. `done('작업 중입니다')` 로 닫을 것.
- `js/main.js:709` — **MEDIUM(낮음)** — 「조」 단위 표기가 **가시성 게이트 없이** 행에 실린다. `gatherParams` 는 `qty_unit: qtyUnitValue()` 를 무조건 넣는데(`#qtyUnit` 은 단건 탭 칸, index.html:111), 묶음·모아찍기 행은 수량을 `seedQty` 로 덮어써(`:1410`·`:1428`·`:1662`) **환산은 안 하고 단위만 'set'** 인 채 manifest 로 나간다 → 대기함 「N개 (M조)」 병기가 틀린다. — 키워드는 같은 함정을 `annotVisible()` 로 막아 뒀다(`:819~825`). ★환산 자체는 규칙대로 **정확히 한 번**이다(`:673` 단 한 곳, 서버는 `qty_unit` 을 계산에 쓰지 않는다 — workbench.ts 주석).
- `mes-sheet.jsx:224` · `mes-core.jsx:322` · `PackGroups.jsx:324·367·419·451` — **MEDIUM(낮음)** — `app.documents.add()` 로 만든 문서는 **눈금이 point** 다. DocumentPreset 경로(`feedback-illustrator-doc-units`)가 SheetLayout(`_iaNewDocMM`)·A0(`mesA0_newDocMM`)·재단(`mesCut_newDocMM`)에만 적용됐고 판짜기 축(mes-sheet)·구 mes-core·PackGroups 는 빠졌다. 기하는 불변이라 영향은 재오픈 시 눈금 표시뿐이지만, mes-sheet 는 디자이너가 다시 여는 `.ai` 를 만든다.

### 확인했지만 이상 없음 (1줄)
PDF 배치 순서(`mesCut_scaleAsPlaced` — resize→rotate→**embed** 앞, 기준 전부 `mesCut_inkBounds`, 검산도 잉크·회전 반영: mes-cut-host.jsx:2427~2509) · 변형이 있으면 배율 1배도 PDF 경로(`:2655`) · 도련 분할 규칙(`mesCutSplitBleed` cut-main.js:58~68 = 하한 min(1.5,요청)·맞붙임 제외·간격 축소 없음, [폭 추천]도 같은 규칙 `:2050`) · 도련 색(NN 축소 `imageSmoothingEnabled=false` `:1265`, 안정점 탐색 bleed.js:145~157, AI30 AA 플래그 미사용) · `evalScript` 인자 ASCII(잠금 라벨 `LOCK_LABELS`·DXF 경로 왕복 제거 `mesCut_exportDxfAuto`·Z: 버전 식 `Z_VER_JS` 가 정규식·따옴표 회피) · `host()` 래퍼가 `'EvalScript error.'` 를 실패로 잡음(cut-main.js:399~405) · `weldPiece`/`mainPart`/`isRectish` 잉크 bbox 판정 · 파일좌표(F)↔저장좌표(S) 분리(`fileToSave` 곱셈이 굽기·맞붙임·검산 3곳 모두에 있음) · 맞붙임 공유 변 1회 출력(butt.js `cutSegments`, 허용오차 없음) · 구멍 `H`/`HB` 직전 외곽 매달기 규약 양쪽 일치 · 패널↔config 필드명 전량 일치(`clients.client_name`·`items.item_name`·`workers.id/name`·`product_materials.p/m`·`post_processing.pp_category` — `sub_category` 를 쓰지 않아 500 재발 없음) · ES5+ 구문·미정의 폴리곤 의존 0(JSX 전 파일에서 let/const·화살표·forEach·JSON·trim·템플릿리터럴 미사용) · geometry.js 사본은 이제 **1벌**(A0 사본 소멸 = 분기 위험 해소).

### 기각한 후보 (최대 5줄, 이유)
- ProcessOrderItem/ExtractGroups/PackGroups 에 `_ia_status` 가 없다 → `JsxDiag()` 는 SheetLayout 렌더 경로에서만 쓰인다(Program.cs:1733·1757). 오진 문구가 뜨는 경로가 아니라 규칙 위반 아님.
- `publish/SheetLayout.jsx`(568줄, repo 안의 구버전 사본) → 런타임 축이 아니고 지시상 배포 드리프트 재보고 제외.
- `nesting.js:189` 스카이라인 폴백이 `fits` 없이 배치 → `maxSky` 아래는 정의상 빈 영역이고 폭은 이미 검사됨(겹침 불가).
- `fitsBits` 가 행 끝에서 다음 행 워드를 읽음 → 넘치는 비트는 항상 0 이라 AND 결과 불변(오탐 없음).
- 「조」 ×2 중복 적용 의심 → 패널 1곳뿐이고 A0 호스트·서버 모두 재환산 없음(위 항목은 표기 누수만 지적).

### 커버리지(정직하게)
전량 정독 = 패널 `js/{main,cut-main,butt,bleed,nesting,tabs}.js` · `jsx/host.jsx` · 에이전트 JSX 4종(SheetLayout·ProcessOrderItem·ExtractGroups·PackGroups) · index.html(구조·id) · 참조 메모리 10건.
부분 정독 = `mes-cut-host.jsx`(2380~2710·2960~3190) · `mes-a0-host.jsx`(270~494·930~1000) · `Program.cs`(1011~1175·3650~3810) · workbench.ts(intake-config·POST /intakes) · orderForm/intake.js(프리필).
**미정독** = `js/geometry.js`(960줄) · `mes-core.jsx` · `mes-sheet.jsx`(grep 만) · `mes-lock.jsx` · `AnalyzeStructure/PrepareArtboards/TestClipExtract.jsx` · `TestRunner/*.cs` · `BoundsExtractor/EdgeColorExtractor.cs` · `poc-a0/*` — 여기는 이번 결과에 반영되지 않았다.
