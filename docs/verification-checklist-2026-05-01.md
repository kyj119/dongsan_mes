# 검증 로드맵 — 2026-05-01 세션

> 이번 세션에서 변경된 모든 기능의 검증 체크리스트.
> 각 항목은 **로컬 서버**(192.168.0.94:3000)에서 직접 확인.

---

## 1. 마감 여백 시스템 (d54aa86)

### 1-1. 주문 저장 시 finishing JSON 저장
```
[ ] 주문서 작성 → 품목에 마감방식 선택 (사방 접어미싱)
[ ] 저장 후 DB 확인: order_items.finishing에 JSON 저장됨
    SELECT finishing FROM order_items WHERE order_id = {id}
```

### 1-2. 카드 생성 시 final_width/height 반영
```
[ ] 100×200cm + 접어미싱(4cm) 사방 주문 생성
[ ] 카드 확인: final_width=108, final_height=208
    SELECT width, height, final_width, final_height, finishing FROM cards WHERE order_id = {id}
[ ] 혼합 마감 (상하:접어미싱, 좌우:열재단) → final_width=100, final_height=208
```

### 1-3. IA 연동 (현장 테스트 필요)
```
[ ] IA 실행 → 마감방식 있는 주문 처리 시 콘솔에 "✂️ 마감:" 로그 출력
[ ] ia_params.json에 finishing 객체 포함 확인
[ ] EPS 출력물에 마감 여백(빈 공간) + M100 0.6pt 접는선 확인
[ ] 열재단(0cm)도 재단선 표시 확인
```

---

## 2. 재고관리 페이지 (c6b501b, 4feff61, bceaaa9)

### 2-1. 재고 실사 버그 수정
```
[ ] /inventory → 재고실사 탭 → "새 실사 시작"
[ ] 품목 목록이 0건이 아닌 실제 구매 품목으로 채워지는지 확인
[ ] is_purchase_item=1인 품목 수와 실사 품목 수 일치
```

### 2-2. 입고 취소 재고 롤백
```
[ ] /receiving → 입고 처리 → 검수에서 "취소" 판정
[ ] 재고 수량이 입고 전으로 복원되는지 확인
[ ] inventory_transactions에 RECEIPT_CANCEL 타입 역분개 기록 확인
```

### 2-3. 페이지 UI 정리
```
[x] 상단 통계 카드 3개: 부족 품목 / 최근 로스율 / 마지막 실사 (검증 완료)
[x] 입고 등록 버튼 없음 (검증 완료)
[x] 출고 등록 버튼 없음 (검증 완료)
[x] 재고 조정 버튼 있음 (검증 완료)
[x] JS 에러 없음 (검증 완료 — bceaaa9에서 수정)
```

---

## 3. 후가공 display_on_card (849ba08)

```
[x] /post-processing → 테이블에 "카드" 컬럼 존재 (검증 완료)
[x] 주석/오프셋/돔보: 눈 감김(fa-eye-slash) 상태 (검증 완료)
[x] 코팅/펀칭/마감방식: 눈 뜸(fa-eye) 상태 (검증 완료)
[ ] 눈 아이콘 클릭 → 토글 동작 확인 (표시↔숨김 전환 + 토스트)
[x] /cards → 카드 뱃지에서 주석/오프셋/돔보 미표시 (검증 완료)
```

---

## 4. 카드 마감방식 표시 — A-2 (b87b230)

```
[x] 마감방식 있는 카드: 노란 배경 + 가위 아이콘 + 마감 텍스트 (검증 완료)
[x] 사방 동일: "접어미싱 사방" 형식 (코드 확인)
[x] 개별 설정: "상:접어미싱 하:접어미싱 좌:열재단 우:열재단" (검증 완료)
[x] 마감 없는 카드: 마감 텍스트 미표시 (검증 완료)
[ ] 그리드 뷰에서도 동일 표시 확인
```

---

## 5. 진행률 뱃지 — B (b87b230)

```
[x] 다건 카드(2+): 우상단에 "X/Y" 원형 뱃지 (검증 완료 — 30개 확인)
[x] 단건 카드(1건): 뱃지 미표시 (검증 완료 — 4개 확인)
[ ] 인쇄 완료 토글 → 뱃지 숫자 실시간 변경
[ ] 전체 완료 시 초록색 + ✓ 표시
```

---

## 6. 메모 연동 — C (b87b230)

```
[x] 주문 메모 있는 카드: 칸반에 📝 아이콘 (검증 완료)
[x] 카드 상세: "주문 메모" 파란 배경 섹션 (검증 완료)
[ ] 카드 상세: "거래처 참고" 노란 배경 섹션 (데이터 있는 거래처로 테스트)
[ ] 메모 없는 카드: 아이콘/섹션 미표시 (에러 없이 동작)
```

---

## 7. 작업지시서 — D (142fe6a)

```
[x] 카드 상세 → 하단에 "작업지시서" 버튼 존재 (검증 완료)
[x] printWorkOrder 함수 존재 (검증 완료)
[ ] 버튼 클릭 → 새 창에 작업지시서 표시
    [ ] 주문번호, 거래처, 납기 정보 정확
    [ ] 품목 테이블 (품목명, 규격, 수량, 후가공/마감)
    [ ] QR 코드 이미지 렌더링
    [ ] 특이사항(internal_notes) 표시
    [ ] display_on_card=0인 후가공은 미표시
[ ] 브라우저 인쇄 다이얼로그 자동 표시
[ ] A5 크기에 맞는 레이아웃 확인 (인쇄 미리보기)
```

---

## 8. QR 스캔 확장 — E (142fe6a)

```
[x] processQrScan 함수 존재 (검증 완료)
[ ] PRINTING 카드 스캔 → "출력완료 처리됨" 토스트 + 상태 변경
[ ] PRINT_DONE 카드 스캔 → "출고 처리 완료" 토스트
[ ] HOLD 카드 스캔 → "보류 해제 → 진행중" 토스트
[ ] 존재하지 않는 카드번호 → 에러 토스트
```

---

## 검증 우선순위

### 즉시 가능 (로컬에서)
- [x] 2-3, 3, 4, 5, 6일부 — 브라우저 검증 완료
- [ ] 3 토글 동작, 5 실시간 변경, 6 거래처 메모, 7 인쇄 전체, 8 스캔 전체

### 실데이터 필요
- [ ] 1-1, 1-2 — 주문서에서 마감방식 설정 후 카드 생성
- [ ] 2-1 — 재고 실사 시작
- [ ] 2-2 — 입고 → 취소 플로우

### 현장 테스트 필요
- [ ] 1-3 — IA + Illustrator 실행 환경
- [ ] 8 — 실제 QR 스캐너 장비

---

> **검증 완료 표기**: [x]로 변경 + 날짜 기록
> **이슈 발견 시**: 항목 아래에 `⚠️ 이슈:` 메모 추가
