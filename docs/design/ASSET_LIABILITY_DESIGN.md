# 자산·부채 관리 설계 — 3단계(연결 고리) 설계안

**작성** 2026-08-04 · **전제** 1단계(대출 등록)·2단계(고정자산 화면)는 별도 진행
**상태** 설계안 — 착수 전 승인 필요

## 현황 요약

| 축 | 테이블 | API | 화면 | 데이터 |
|---|---|---|---|---|
| 부채 | `loans` + `loan_payments` + `loan_rate_history` | CRUD·상환·금리이력 | **`/cash-schedule` 대출 탭** | 0건 |
| 자산 | `fixed_assets` + `depreciation_records` | 등록·감가상각·처분·요약 | **`/accounting` 고정자산 탭**(2단계로 신설) | 0건 |

두 축 모두 단품으로는 완결돼 있다. **문제는 셋을 잇는 고리가 없다는 것**이다.

## 끊어진 고리 3개

### G1. 자산 ↔ 부채

`fixed_assets` 에 `loans` 를 가리키는 필드가 없다. 실제 사례가 이미 있다 — 케이엠테크 장비를 **우리금융캐피탈 리스**로 취득했고(잔액 45,598,220), 장비는 자산, 리스는 부채인데 시스템은 둘이 같은 건임을 모른다.

**설계안**: `fixed_assets.loan_id INTEGER REFERENCES loans(id)` 추가.
- 자산 상세에서 "이 자산의 잔여 부채" 표시
- 대출 상세에서 "담보/대상 자산" 역참조
- 리스 만기 도래 시 해당 자산을 함께 점검

**대안(기각)**: 중간 테이블 `asset_liabilities`. 자산 1건에 부채 여러 건이 붙는 경우(공동담보)를 표현할 수 있으나, 현재 사례가 전부 1:1이고 확인된 복합 사례가 없다. **YAGNI — 1:N이 실제로 생기면 그때 승격**.

#### G1 구현 완료 (2026-08-04)

- 마이그 `0513_fixed_assets_loan_link.sql` — `fixed_assets.loan_id` + 인덱스. **prod·local 적용 완료**
- `routes/fixedAssets.ts` — 목록·상세에 `loans` LEFT JOIN(채권자·잔액·만기), POST 에 `loan_id`, **`PATCH /:id/loan`**(연결/해제). 자산·대출 **양쪽 소유 검증** — 동산기획 자산에 선명 부채를 붙이는 교차연결을 차단한다
- `routes/cashFlow.ts` `/loans` — 역참조 `linked_assets`·`linked_book_value`(처분 제외)
- `/accounting` 고정자산 탭 — 「연결 부채」 열의 select 로 즉시 연결/해제, 등록 모달에도 필드 추가
- ⚠️ 대출 목록은 `active=1` 만 받는다. 연결된 부채가 비활성이면 select 에 선택지가 없어 **사용자가 건드리는 순간 조용히 해제**되므로, 현재 연결값은 목록에 없더라도 `(비활성)` 옵션으로 보강한다

#### ★연결할 짝이 아직 없다 — 부채 3건이 전부 미등록

등록된 대출 1건(우리금융캐피탈)의 **대상 자산이 미등록**이고, 등록된 자산 6건의 **부채가 미등록**이다. 지금 아무 쌍이나 이으면 틀린 연결이 된다.

| 자산 | 취득 경위(notes) | 대응 부채 | 상태 |
|---|---|---|---|
| OPTIMUM-EP3208 | 티피엠 매입 · 계약금 15,000,000 대표이사 가수금 · **월 3,617,000 할부** | **매입채무(AP)** | 이미 계상 |
| Extreme-1880T · 2280T ×2 | 케이엠테크 매입 | **매입채무(AP)** | 이미 계상 |
| 포터II · 중고차량 | 현대자동차·애드월드 매입 | 없음 | — |
| — | — | **우리금융캐피탈** 45,598,220 | 대상 자산 미등록 |

#### ★통장 역추적 결과 — 장비 할부는 대출이 아니라 매입채무다 (2026-08-04 정정)

처음엔 "장비 부채가 장부에서 빠져 있다"고 판단했으나 **틀렸다**. 통장 정기출금을 매입 데이터와 대조하니 전부 AP 로 계상돼 있었다.

| 거래처 | 발주(status≠DRAFT/CANCELLED) | 지급 | **AP 잔액** |
|---|---:|---:|---:|
| (주)티피엠 | 90,751,793 | 49,669,850 | **41,081,943** |
| 케이엠테크 | 155,375,900 | 83,300,000 | **72,075,900** |

- **티피엠 월 3,617,000** — 4/17·5/18·6/16·7/15 **4회**, 수취인이 캐피탈사가 아니라 **(주)티.피.엠. 본인**이다. 즉 판매자 할부 = 매입채무 상환이지 금융부채가 아니다. `loans` 에 또 등록하면 **같은 빚이 두 번 잡힌다** → **G1 연결 대상이 아니다**.
- **자산 notes 의 "한국아이비렌탈 캐피탈 전환분"은 근거가 약하다** — 케이엠테크 장비 대금은 케이엠테크에 직접 지급(83,300,000)됐고, 한국아이비렌탈 출금은 별개 흐름이다.

**진짜 미등록은 한국아이비렌탈 하나뿐**: 월 2,730,070(5/26·6/25·7/27) + **4/9 24,570,630 = 정확히 9회분**(2,730,070×9). 거래처 미등록·`fixed_expenses` 미등록·AP 0 → **연 32,760,840 이 어디에도 안 잡힌다**. 렌탈료(비용)인지 할부채무인지 미확정.

→ 남은 선결은 **2건뿐이고 계약서가 아니어도 된다**: ①한국아이비렌탈 = 세금계산서 1장(품목·계정이 찍힘) 또는 SmartA 거래처원장 조회 ②우리금융캐피탈 = 캐피탈사 잔액증명·상환스케줄 1장(2023년 리스 장비 식별).

### G2. 매입 → 자산 승격

`purchase_order_items` 에서 자산으로 넘어가는 경로가 없다. OPTIMUM-EP3208 53,096,000 이 티피엠 매입 라인에 품목 없이 떠 있고, 자산 등록은 **수기 재입력**이어야 한다. 금액·취득일·거래처를 두 번 입력하면 어긋난다.

**설계안**: 매입 라인에서 "자산으로 등록" 액션.
- `purchase_order_items.fixed_asset_id INTEGER REFERENCES fixed_assets(id)` 추가
- 클릭 시 자산 등록 모달을 **취득가=라인 금액, 취득일=PO 일자, 비고=PO 번호**로 프리필
- 등록 후 라인에 "자산 FA-xxx" 배지 → 중복 등록 방지

**주의**: 취득가에 VAT를 포함할지. 세무상 고정자산 취득가액은 **공급가액**(매입세액은 공제) → 라인 `amount`(공급가)를 쓰고 `final_amount`(VAT포함)를 쓰지 않는다. 2단계 화면에도 같은 기준을 적어둘 것.

### G3. 감가상각 → 손익

`depreciation` 을 참조하는 코드가 `routes/fixedAssets.ts` **한 파일뿐**이다. 부문별 손익(`/hr` 3탭)·경영진단·재무제표 어디에도 감가상각비가 들어가지 않는다. **지금 보고 있는 부문 이익은 감가상각 전 숫자**다.

**설계안**: `depreciation_records` 를 비용으로 편입.
- 부문 배부 기준이 필요하다 — 자산이 어느 부문 것인지. `fixed_assets.department` 또는 `equipment_id → 공정 → 부문` 경유
- 장비 연동 자산은 **equipment 경유가 자연스럽다**(이미 공정→부문 매핑이 있다). 차량·비품 등 장비 무관 자산은 `department` 직접 지정 또는 공통비 배부
- 월 감가상각 실행 → 해당 월 부문 비용에 가산

**⚠️ 파급**: 이걸 켜면 **지금까지 보던 부문별 이익 숫자가 전부 내려간다**. 과거 기간을 소급 반영할지, 시행월부터 전진 적용할지 먼저 정해야 한다. 소급하면 이미 공유된 경영진단 수치와 어긋난다.

## 권고 순서

1. **G1 먼저** — 컬럼 1개, 파급 없음, 실제 사례(캐피탈 리스)가 이미 있다
2. **G2 다음** — 컬럼 1개 + 화면 액션. 자산 등록의 정확성을 올린다
3. **G3 마지막** — 손익 숫자가 바뀌므로 **소급 범위를 먼저 합의**하고 착수. 부문 배부 기준 설계가 실질 작업량

## 1·2단계 실행 결과 (2026-08-04)

**고정자산 4건 133,096,000 등록** — 매입 라인에서 취득일·취득가를 그대로 가져왔다.
OPTIMUM-EP3208 53,096,000(티피엠 3/31) · Extreme-1880T 28,000,000(KM 2/11) · Extreme-2280T 26,000,000 ×2(KM 4/10, 개별 관리 위해 1·2호기로 분리). 내용연수 60개월 정액법.

**우리금융캐피탈 45,598,220 등록 + 상환 스케줄 14회**(2026-08~2027-09).
⚠️ 대출만 등록하면 자금 예측에 안 잡힌다 — `loan_payments` 가 비어 있으면 `cashflowEngine` 이 읽을 게 없다. **`POST /loans/:id/generate-schedule` 를 따로 호출**해야 한다(화면도 마찬가지).
⚠️ 스케줄은 `start_date` 부터 회차를 만든다. 계약 시작(2023-04-20)을 그대로 두면 **과거 40회차가 미납으로 쌓인다** → `start_date` 를 잔여 상환 구간(2026-08-20)으로 넣고 실계약일은 notes 에 남겼다.

### ★통장 대사로 확정 — 연 6.70% · 4개월째 상환 정지

`bank_transactions`(1/1~8/3, 5,693건)와 SmartA 미지급금 차변을 맞춰보니 **통장 출금 = 원금 + 이자**, **세무장부 차변 = 원금만**(이자는 이자비용 계정)이었다.

| 일자 | 통장 출금 | 원금(기장) | 이자 | 상환전 잔액 | 월이율 |
|---|---:|---:|---:|---:|---:|
| 1/20 | 324,194 | 15,416 | 308,778 | 55,303,609 | 0.558% |
| 2/20 | 1,774,366 | 1,483,547 | 290,819 | 52,086,187 | 0.558% |
| 3/20 | 3,525,350 | 3,252,506 | 272,844 | 48,850,726 | 0.559% |

세 시점 월이율이 셋째 자리까지 일치 → **연 6.70%** 확정(loans.current_rate 반영). 별건(1/21 3,202,006 · 2/23 1,751,914)은 이자 없이 원금 전액.

**★4월 이후 통장 출금이 0건이다** — 기장 누락이 아니라 실제로 상환이 멈춰 있다(4개월째). 같은 기간 현대캐피탈은 매월 정상 출금 중이라 계좌 수집 문제도 아니다. 적요상 만기(2026-04-20)와 맞물리므로 **만기 일시상환 미이행·재약정·연체** 중 무엇인지 확인이 필요하다. 등록한 스케줄은 상환 재개 가정이므로 조건 확인 후 재생성할 것.

### 현대캐피탈은 부채 등록 대상이 아니다 (조사 결과)

통장에서 매월 나가지만(1~4월 1,379,310 · 5월 2,232,943 · 6월 2,283,389), **부채로 잡을 잔액이 없다**.

- 미지급금(253) 48곳에 현대캐피탈 **없음**
- 장기미지급금(297) **미사용**
- 차량은 이미 **차량운반구(208)에 자산 계상**돼 있다 — 총 234,711,138. 현대캐피탈 명의 3대(48오5808 40,620,000 · 87러6814 27,926,791 · 287모5569 1,626,630)도 여기 포함
- 즉 월납은 **리스료(비용) 처리** 중으로 보인다 → 운용리스 성격이거나 할부가 이미 종료

### 차량 — 2026년 신규 취득 2대만 등록, 나머지는 식별 불가

208 계정 전표를 19개 거래처 전량 순회해 받았다. **개별 차량이 식별되는 것은 2026년 신규 취득분뿐**이고, 그 2대는 등록했다(취득부대비용은 회계 원칙대로 취득가액에 산입).

| 자산 | 취득일 | 본체 | 취득부대 | 합계 |
|---|---|---:|---:|---:|
| 포터II (833소8708) | 2026-04-21 | 27,157,273(현대자동차) | 1,460,548(취득세·지역채권·증지) | **28,617,821** |
| 중고차량 (838버5060) | 2026-03-24 | 17,600,000(애드월드) | 626,425(취득세·인지·채권차손) | **18,226,425** |

**나머지 188M 은 등록할 수 없다** — 전표가 전부 `전기이월(자산)` 한 줄이라 차량번호도 취득일도 없다. 특히 현대자동차(주) 159,236,304 에 여러 대가 뭉쳐 있어 대수조차 알 수 없고, 감가상각은 취득일이 없으면 계산 자체가 불가능하다.

식별되는 전기이월 2건도 취득일을 모른다 — 현대캐피탈(48오5808) 40,620,000 · 현대캐피탈(87러6814) 27,926,791(**6/15 청주 법인으로 13,980,000 매각 처리됨**).

이 잔여분을 채우려면 **차량등록증·보험증권 등 외부 자료**로 차량별 취득일·취득가를 재구성해야 한다. 세무장부만으로는 불가능하다. 감가상각누계액(209)도 같은 구조라 차량별 배분이 안 된다.

정리 대상: 법인간 이전(선명 −29,910,843 · 청주 −13,980,000) · 처분(임재양 −32,468,024 · 성일모터스 −5,778,000) · 미등록거래처 5,608,510.

### 감가상각 반영 — 정액법·취득월 소급 (2026-08-04)

3가지를 정하고 실행했다: **정액법 · 취득월부터 소급(2026-02~07) · 부문 손익 미반영(기록만)**.

| 자산 | 취득 | 경과 | 상각누계 | 장부가 |
|---|---|---:|---:|---:|
| OPTIMUM-EP3208 | 2026-03 | 5 | 4,424,665 | 48,671,335 |
| Extreme-1880T | 2026-02 | 6 | 2,800,002 | 25,199,998 |
| Extreme-2280T 1호기 | 2026-04 | 4 | 1,733,332 | 24,266,668 |
| Extreme-2280T 2호기 | 2026-04 | 4 | 1,733,332 | 24,266,668 |
| 포터II | 2026-04 | 4 | 1,907,856 | 26,709,965 |
| 중고차량 | 2026-03 | 5 | 1,518,870 | 16,707,555 |
| **계** | | | **14,118,057** | **165,822,189** |

취득가 179,940,246 − 상각 14,118,057 = 장부가 165,822,189. 자산별 `current_book_value` 와 `MAX(accumulated_depreciation)` 불일치 0건.
전 자산 가동 시 **월 2,999,004 / 연 35,988,048**. 적재 SQL = `docs/dongsan-import/load/depreciation_2026h1.sql`(멱등 아님 — `UNIQUE(asset_id, period)` 가 재실행을 막는다).

**정률법을 택하지 않은 이유**: 세법은 상각방법 무신고 시 건축물 외 자산에 **정률법**이 법정 기본이고, 정률법이면 같은 기간 29,635,307 로 **2.1배**다. 그런데 세무장부에서 실제 신고 방법을 확인할 수 없었다 — 2026년 상각은 연말 결산 때 계상돼 아직 없고, 20기(2025) 전환은 실패한 상태다. 관리회계 목적상 부문 손익 왜곡이 적은 정액법을 택했고, **세무 신고치와의 정합은 연말 결산에서 별도로 맞춘다**. 정률법으로 바꾸려면 `depreciation_records` 삭제 후 재계산 + `DECLINING_BALANCE` 분기를 세법 상각률(5년 0.451)로 고쳐야 한다(현재 코드는 이중체감 `2/내용연수`라 세법과 다르다).

**★`POST /fixed-assets/depreciate` 결함 2건 수정**(`routes/fixedAssets.ts:105`) — ⚠️**아직 prod 미배포**:
- **취득일을 전혀 보지 않았다**. 2026-01 로 실행하면 4월 취득 차량에도 1월분이 붙는다. `acquisition_date` 의 YYYY-MM 이 `period` 보다 크면 skip(세법도 월할·취득월 포함).
- `lastRecord?.book_value || ...` 의 `||` — 상각완료(장부가 0)가 falsy 라 `acquisition_cost` 로 되살아나 **무한 상각**된다. `??` 로 교체.

### 부문 손익(G3)은 아직 못 켠다 — 선행조건

`fixed_assets.equipment_id` 가 **6건 전부 NULL** 이라 기계→공정→부문 경로가 끊겨 있다. 지금 켜면 월 2,999,004 이 통째로 공통비 풀(`routes/departments.ts:258` `fixedCommon`)에 얹혀 **기계별 귀속 없이 인원·매출 비례로 전 부문에 배부**된다. 출력기 4대는 특정 공정 전속이라 이건 실질을 왜곡한다.
→ 순서: ①`equipment_id` 연결(`equipment.id` 는 **TEXT**다) → ②차량·비품용 `department` 직접지정 경로 → ③G3 착수.

## ★세무장부 정합 작업 (2026-08-04, ③→①→② 순)

출발점은 **`고정자산 → 고정자산관리대장`(SAAS0107)** 화면이다. 거래처원장(208)이 `전기이월` 한 줄만 주던 것과 달리
이 화면은 **자산 단위로 취득일·취득가·상각누계·상각률**을 준다 — 원본 = `docs/dongsan-import/SMARTA_FIXED_ASSETS.md`.
"차량 188M·2023년 리스 장비는 등록 불가"라던 판단은 **이 화면을 안 봐서 생긴 오판**이었다.

### ③ 우리금융캐피탈 상환 스케줄 삭제

거래처원장 총괄잔액에서 **964 기타보증금 45,600,000** 을 발견했다. 253 미지급금 잔액 45,598,220 과 **차이 1,780** 뿐이다.
→ 만기(2026-04-20) 보증금 상계 구조로 읽히며, **4월 이후 통장 출금 0건은 연체가 아니라 정상**일 수 있다.
등록해 둔 스케줄 14회(45,598,220)는 실제로 나가지 않을 현금을 자금예측에 넣으므로 **삭제**했다(부채 행은 유지).
931 이자비용 872,441 은 1~3월 이자 합(308,778+290,819+272,844)과 **원 단위 일치** — 대사 정확성 재확인.

### ① 감가상각 = 정률법으로 재계산

`rt_depre` 가 답이었다. **차량 0.451(정률 5년) · 기계 0.259(정률 10년) · 구축물 0.1(20년)**.
"확인할 방법이 없다"며 택했던 정액법 60개월은 **방법·내용연수 양쪽이 틀렸다**(기계는 세무장부가 10년).

- 마이그 `0514` — `fixed_assets.depreciation_rate`(연 상각률)
- 엔진(`routes/fixedAssets.ts`) — **기준액을 연초 미상각잔액으로** 교체. 세법은 사업연도 단위로
  `상각액 = 연초 미상각잔액 × 상각률 × 보유월수/12` 를 계산하므로 **같은 해 안에서는 월 상각액이 일정**하다.
  매월 장부가에 곱하면(월복리) 세무장부와 어긋난다. `depreciation_rate` 미설정 자산은 종전 이중체감으로 폴백.
- 2026년 취득 6건 재계산: **14,118,057 → 21,572,541**(+7,454,484)

### ② 세무장부 자산 33건 이관

| 구분 | 건수 | 취득가액 |
|---|---:|---:|
| 기계장치(2016~2025) | 28 | 959,854,546 |
| 차량운반구(2017~2024) | 5 | 201,846,892 |

**제외 22건** = 2006~2010년 기계. `rt_depre=0` 이고 상각누계가 취득가와 정합하지 않는다(예: 2006년 재봉기 14,000,000 에 누계 2,104,666).
완전상각 후 대장에서 값이 리셋된 것으로 보이며, 그대로 넣으면 20년 된 자산에 가짜 장부가 12M 이 생긴다.

- **잔존가액 1,000원**(세법 정률법 비망가액)을 전 자산에 설정 — 상각완료 자산이 0 으로 내려가는 걸 막는다
- 스타리아(87러6814)는 **2026-06-15 청주 법인 이전 13,980,000** → `DISPOSED`
- 2026-01~07 상각을 이관 자산에도 소급(연초 미상각잔액 기준)

### 최종 상태 (prod)

| 항목 | 값 |
|---|---:|
| 자산 | **39건** (기계 32 · 차량 6 · 처분 1 포함) |
| 취득가액 | 1,360,641,684 |
| 장부가액 | **502,190,141** |
| 2026-01~07 상각 | **85,032,283** (기록 209건) |

⚠️ 검증 쿼리의 "불일치 7건"은 **오탐**이다 — 장부가 1,000원(비망가액) 도달 자산은 2026년 상각 기록이 없어
`취득가 − MAX(누계)` 공식이 안 맞을 뿐이다. 이관 자산의 개시 상각누계는 records 가 아니라 세무장부에 있다.

⚠️ prod 엔진 실측으로 2026-08 상각이 **실제로 기록됐다가 롤백**했다(7월 마감 유지). `/depreciate` 는 조회가 아니라 **쓰기**다.

### ④ 한국아이비렌탈 정기비용 등록 (완료)

세무 처리 확인 결과 **자산도 부채도 아닌 렌탈료(비용)** — 131 선급금 24,570,630 / 253 미지급금 잔액 2,730,070 /
531 지급수수료 9,927,528 / 135 부가세대급금 992,752. 공급가 9,927,528 + VAT 992,752 = 10,920,280 = **2,730,070 × 4회** 로 정확히 맞는다.
→ `fixed_expenses` 에 월 2,730,070·LEASE·2026-04 개시로 등록(사업자 390-87-03027).
**131 선급금 24,570,630 은 넣지 않았다** — 2026-04-09 일시납이고 정확히 9회분(2,730,070×9)인데 아직 비용화 전이다.

### ⑤ ★`fixed_expenses` 법인 필터 누락 (같은 경로에서 발견·수정)

`routes/departments.ts:257` 의 고정비 합산에 **`entityFilter` 가 없었다**. #521 이 인건비·인원수엔 법인 스코프를 걸었는데
고정비만 형제 누락이었다. 결과적으로 **선명(월 12,681,300)이 동산기획(월 3,471,250) 부문 손익의 공통비 풀에 통째로 얹혀** 배부되고 있었다 — 연 1.5억 규모.

수정 후 실측 `fixed_common` = **6,201,320**(동산기획 8건만: 기존 3,471,250 + 아이비렌탈 2,730,070).
⚠️ **부문 이익 숫자는 올라간다** — 감가상각(G3)과 반대 방향이다. 잘못 얹혀 있던 남의 비용이 빠지기 때문.

## ★G3 감가상각 → 부문 손익 (2026-08-05, 완료)

### 방향 전환 — "대장 완성"이 아니라 "상위 자산 부문 지정"

용준님 지적이 옳았다: **세무장부를 통째로 복제하는 건 목적이 아니라 수단의 비대화**였다.

| 구간 | 건수 | 월 상각 비중 |
|---|---:|---:|
| 상위 10건 | 10 | 69.8% |
| 상위 20건 | 20 | 92.5% |
| 월 10만원 이상 | 24 | **97.2%** |

이관한 39건 중 **7건이 이미 죽어 있었다**(장부가 1,000원 비망가액·월 상각 0, 취득가로는 313,411,687).
→ 비품 169건(91% 상각완료) 전량 이관은 **하지 않는다**. 구축물·시설장치도 살아있는 것만 선별.

**삭제가 아니라 필터**를 택했다 — 지우면 자산 총계(취득가)가 줄어 보유 현황 파악이 어긋난다.
목록에 `depreciating=1`(장부가 > 잔존가액) 필터를 두고 화면 기본값으로 켰다. 실측 38건 → **31건**.

### equipment 경유가 막힌 이유 (구조적)

세무장부 자산명이 **묶음 단위**다 — `컴퓨터 및 주변기기 외` · `TELEIOS HEXA 외 5건` · `SUPERCOLOR H8(중고) 외`.
MES `equipment` 24대와 대조했을 때 **확정 매칭은 SUPERCOLOR H8 ↔ RIP-03**(`printer_name` = `Super Color New H8_A1`) 하나뿐이고 그마저 INACTIVE.
→ `fixed_assets.department_id`(마이그 `0515`)로 **자산에 부문을 직접** 붙인다.

### 배부 규칙 — 인건비와 동일

| 자산의 부문 | 흐름 |
|---|---|
| PRODUCTION | 해당 부문 **직접 귀속** |
| SUPPORT + `serves_department_id` | 대상 생산부문으로 |
| SUPPORT(serves 없음) · **미지정** | **공통풀** → 배부기준 안분 |

세 갈래 합이 항상 전체와 같아 **상각비가 소실되지 않는다**(실측 검증: 직접 5,679,959 + 공통 8,028,975 = 13,708,934 ✓).
`operating_profit = 공헌이익 − 감가상각 − serves − 공통안분`. 감가상각은 고정비이므로 공헌이익 **아래**에서 뺀다.

### 지정 결과 (2026-06 기준 월 13,708,934)

| 부문 | 건수 | 월 상각 | 비고 |
|---|---:|---:|---|
| 출력 | 9 | **5,623,594** | UV·수성 출력기 전량(FY-UV3500W·Extreme 4대·XTRA R18·OPTIMUM-EP3208·SUPERCOLOR H8) |
| 전사 | 1 | 56,365 | 태극기 절단거치대 — 근거는 추측이 아니라 `department_category_map` '태극기'→2 |
| 봉제/후가공 | 6 | 1,295,362 | 물리 귀속은 맞지만 **SUPPORT·serves 없음 → 결국 공통배부**. 현수막(→출력)·태극기(→전사) 양쪽을 받으므로 한쪽에 붙이면 오히려 왜곡 |
| (미지정) | 22 | 6,663,350 | 차량 3·에너지 효율화 설비·컴퓨터류 3(용준님 확인) + 이름으로 못 정하는 5건 |

**생산부문 직접 귀속 41.4%.** 나머지는 실제로 공용 자산이라 공통배부가 맞다 —
차량·공장 전체 설비·사무기기를 특정 공정에 태우는 게 오히려 왜곡이다.

⚠️ **이름만으로 부문을 정하지 않았다.** Extreme 계열·OPTIMUM-EP3208 은 용준님 확인 후 지정했고,
`TELEIOS HEXA 외 5건`(묶음이라 단일 귀속 자체가 불가) · `C11CL38405` · `파이프 전단기` · `SGC-1655HD` ·
`Air Compressor` 는 **미지정으로 남겼다**(월 합계 826,342). 확인되면 화면 select 로 즉시 바꾼다.
→ 근거 = [[feedback-item-naming-inference]] 성격의 교훈(리치코엘 오배정 전례).

⚠️ **소급 적용이다.** `depreciation_records` 가 2026-01~07 로 존재하므로 과거 기간 조회도 감가상각이 반영된다.
이전에 공유된 부문 이익 수치와 어긋난다 — 다만 비용이 실제로 발생한 기간이므로 이게 맞다.

## 미해결 질문 (착수 전 확인 필요)

- **G3 소급 범위**: 2026년 전체 소급 / 시행월부터 / 별도 표시(감가상각 전·후 병기)
- **부문 배부 기준**: equipment 경유가 안 되는 자산(차량·건물·비품)은 어떻게 나눌지
- **리스와 감가상각의 관계**: 금융리스면 자산으로 잡고 상각 + 이자비용, 운용리스면 리스료 전액 비용. 우리금융캐피탈 건이 어느 쪽인지 계약서 확인 필요 — **이게 G1·G3 양쪽 설계를 좌우한다**
