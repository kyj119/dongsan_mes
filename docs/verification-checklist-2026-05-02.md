# 검증 로드맵 — 2026-05-02 세션 (Q1~Q10)

> 이번 세션에서 변경된 기능의 검증 체크리스트.
> 로컬 서버(192.168.0.94:3000)에서 직접 확인.

---

## Q1. 마감방식 UI — 사방 드롭다운 제거 (93e93ce)

```
[ ] /orders/new 주문서 작성 진입
[ ] 마감 방식 섹션에 "사방 선택" 드롭다운이 없는지 확인
[ ] 프리셋 버튼 (사방 열재단, 사방 접어미싱 등) 정상 표시
[ ] 프리셋 클릭 → 4변 동일 적용 확인
[ ] "개별 설정 ▾" 클릭 → 상/하/좌/우 셀렉트 표시
[ ] 개별 설정에서 각 변 선택 후 여백 계산 표시 확인
[ ] 주문 수정 폼에서도 동일하게 동작하는지 확인
```

---

## Q2. IA 빈 파일 가드 (93e93ce)

```
[ ] AI 파일 없는 주문 생성 → IA 처리 시
    콘솔에 "⏭️ AI 파일 없음 — 건너뜀" 표시 확인
[ ] AI 파일 있는 주문은 정상 처리 (기존 동작 유지)
[ ] JSX 오류/크래시 없이 다음 품목으로 넘어가는지 확인
```

---

## Q5. 마감 여백 Math.max 대표값 수정 (93e93ce)

```
[ ] 같은 카드 내 품목별 다른 마감 주문 생성
    (1번: 열재단 사방, 2번: 접어미싱 사방)
[ ] DB 확인: cards.finishing이 "접어미싱" (마진 합 더 큰 쪽)
    SELECT finishing FROM cards WHERE order_id = ?
[ ] final_width/height가 접어미싱(4cm) 기준으로 계산됨 확인
[ ] 카드 표시와 final 크기 일치 확인
```

---

## Q6. 마감 표시 품목 라인 이동 (d054298)

```
[ ] /cards 접속 → 칸반 뷰
[ ] 마감방식 있는 카드에서 카드 레벨 마감 블록 없는지 확인
[ ] 품목 라인 구조: 1줄(품목명+내용+규격+수량) + 2줄(후가공+마감)
[ ] 마감 있는 품목: 노란 뱃지 "✂ 접어미싱 사방" 표시
[ ] 마감 없는 품목: 2줄째에 마감 뱃지 없음
[ ] 카드 클릭 → 상세 패널에서도 품목별 마감 표시 확인
[ ] 그리드 뷰에서도 동일 표시 확인
```

---

## Q7. 작업지시서 그룹 품목 중복 제거 (c5235aa)

```
[ ] 그룹 품목 있는 주문에서 작업지시서 인쇄
[ ] 부모 라인(포괄)이 자식 있으면 표시 안 됨 확인
[ ] 자식 라인만 각각 카드 형태로 표시
[ ] 그룹 아닌 일반 품목은 정상 표시
```

---

## Q8. 시각적 작업지시서 v2 (c5235aa)

```
[ ] /cards → 아무 카드 클릭 → 상세 패널 → "작업지시서" 버튼 클릭
[ ] �� 창 열림 확인
[ ] 헤더: 주문번호, 거래처, 납기, 연락처 정확
[ ] QR 코드 이미지 렌더링 확인
[ ] 특이사항(internal_notes) 파란 배경으로 표시 (있을 때)
[ ] 품목별 카드 형태:
    [ ] 썸네일 이미지 (있으면 표시, 없으면 placeholder)
    [ ] 품목명 + 규격 + 수량
    [ ] 내용(content) 표시
    [ ] 후가공 뱃지 (display_on_card=1인 것만)
    [ ] 마감 뱃지 "✂ 접어미싱 사방"
    [ ] 마감 다이어그램 (4변 박스, 각 변에 방식명)
[ ] 인쇄 다이얼로그 자동 표시 (window.print)
[ ] A5 크기 레이아웃 확인 (인쇄 미리보기)
[ ] 품목간 페이지 나뉨 없이 이어지는지 (page-break-inside:avoid)
```

---

## Q9. 재고 카테고리 정리 (c545eee)

```
[ ] /inventory 접속
[ ] 카테고리 필터에 "판재류", "원단류", "원자재" 3개 표시
[ ] "빈 카테고리" 항목 없음 확인
[ ] 판재류: 포맥스/폼보드 품목들
[ ] 원단류: 현수막 2코팅 + 기존 원단
[ ] 원자재: SPM + 잉크류
```

---

## Q10. 부분 재고 실사 (c545eee)

```
[ ] /inventory → 재고실사 탭 → "새 실사 시작" 클릭
[ ] 모달 표시: 실사 범위 드롭다운 (전체/판재류/원단류/원자재)
[ ] "원단류" 선택 → 생성 → 토스트: "[원단류] 실사 생성됨 (20건)"
[ ] 상세 패널에 원단류 품목만 표시 (판재류/원자재 미포함)
[ ] ���태 뱃지 옆에 "입력: 0/20" 진행률 표시
[ ] 품목 수량 입력 → 저장 → 진행률 "입력: 5/20" 업데이���
[ ] "전체" 선택 시 54건 전부 로드 확인
[ ] 제출 → 승인 → 재고 조정 정상 동작
```

---

## 검증 우선순위

### 즉시 가능 (로컬 브라우저)
- [ ] Q1 — 주문서 마감 UI
- [ ] Q6 — 칸반 품목 라인 구조
- [ ] Q8 — 작업지시서 시각 확인
- [ ] Q9 — 카테고리 필터
- [ ] Q10 — 부분 실사 모달

### 실데이터 필요
- [ ] Q5 — 품목별 다른 마감 주문 생성
- [ ] Q7 — 그룹 ��목 주문

### 현장 테스트 필요
- [ ] Q2 — IA 실행 환경 + AI 파일 혼합 주문

---

> **검증 완료 시**: `[ ]` → `[x]` + 날짜
> **이슈 발견 시**: 항목 아래 `⚠️ 이슈:` 메모
