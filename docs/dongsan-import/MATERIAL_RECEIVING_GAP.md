# 자재 입고 관리 진단 + 통장 근거 매입 갭 (2026-08-06)

회계(금액) 정합이 끝난 뒤 "자재 입고는 어디까지 됐나"를 점검한 결과다.
**결론: 금액 축은 거의 끝났고, 입고(수량·재고) 축은 시작도 안 했다.**

## 1. 입고 관리 = 미가동

| 항목 | 값 |
|---|---|
| `inventory_receipts` / `inventory_receipt_items` | **0 / 0** |
| `inventory_transactions` | **1건** |
| `inventory_fifo_layers` / `inventory_auto_deductions` / `pp_material_deductions` | **0 / 0 / 0** |
| `inventory` (동산기획) | 74행 · **수량≠0 은 1품목** |
| 원자재 마스터 (활성) | 787품목 |
| 2026 매입 이력 있는 품목 | 391품목 |

모듈은 완성돼 있다 — `src/routes/inventory.ts:319~` 가 입고 전표 → `inventory` upsert →
`inventory_transactions` 기록 → 검수까지 처리한다. **미구현이 아니라 미사용이다.**

### ⚠️ 착시 하나 — "입고 100% 완료"로 보인다

`purchase_order_items.received_quantity` 가 **전 라인 `= quantity`** 로 채워져 있고
`purchase_orders` 503건이 **전부 `RECEIVED`** 다. 이관이 만든 값이다.

실제 입고를 시작하면 **미입고 잔량이 0으로 계산돼 입고 대상이 아예 안 뜬다.**
입고 운영을 켜기 전에 `received_quantity` 를 0으로 되돌리는 결정이 선행돼야 한다
(되돌리면 반대로 "전량 미입고"가 되므로, 어느 시점부터 실입고를 잡을지 기준일이 필요하다).

### 매입 명세 축은 살아 있다

동산기획 2026 라인 1,079건 중 품목 미연결 53건(**95% 연결**), 수량 누락 0건.
"무엇을 얼마나 샀는지"는 있고 "창고에 들어왔다"는 기록만 없는 상태다.

## 2. 통장으로 본 매입 커버율

동산기획 2026 출금 46.7억 분해:

| 버킷 | 금액 | 비중 |
|---|---:|---:|
| 매입성(기타/미분류) | 21.2억 | 45.5% |
| 내부이체 | 11.2억 | 24.0% |
| 급여·4대보험 | 6.4억 | 13.8% |
| 대출·이자 | 3.6억 | 7.6% |
| 카드대금 | 2.1억 | 4.5% |
| 세금 | 1.2억 | 2.6% |
| 금융·리스 / 공과금·통신 | 0.9억 | 2.0% |

MES 매입지급 **13.9억(148건) → 적재 후 17.1억(201건)** · 커버율 **66% → 81%**

## 3. ★ AP 대사가 맞았는데 왜 매입이 빠져 있었나

**잔액만 맞췄기 때문이다.** 세무장부 251 은 세금계산서 기준 채권채무 **잔액**이라,
기초잔액 + 당기 일부만 넣어도 잔액은 일치한다. 그래서:

- **오다플래그처럼 즉시 결제해 251 잔액이 0인 매입처는 대사 그물에 아예 안 걸린다** (통장 1.29억)
- 진안알미늄은 잔액 6,554,200 이 정확한데 통장은 4,000만이 나갔다 — 매입도 지급도 나머지가 통째로 없었다

즉 **「AP 대사 완결」= 채무 잔액 정확**이지, **매입 거래 전수 적재는 아니다.**
앞선 `SMARTA_AP_RECONCILE.md` 의 완결 선언은 이 범위로 읽어야 한다.

## 4. 적재한 것 — 통장 근거 매입 갭

`load/ap_bank_gap_2026.sql` · 거래처 17곳 · 53건 · **321,198,907**

선정 방법: 동산기획 2026 출금 중 **해당 거래처의 `purchase_payments` 와 (일자, 금액) 이
하나도 겹치지 않는 건만** 골랐다. 겹친 건은 이미 적재돼 있어 제외했다
(유진프라스틱 06-02 8,000,000 · 한국엡손 07-15 2,956,910 등 — 총액만 보면 갭으로 보이지만 아니다).

### ★ AP 불변 설계

매입 PO 와 지급을 **동액·동일자**로 넣었다. `AP = PO − 지급 − 조정` 이므로 순증 0 →
세무장부 대사가 깨지지 않는다. 적재 목적은 잔액 교정이 아니라 **거래 이력·거래처 지출 가시성**이다.

검증: 적재 전후 거래처별 AP **변동 0곳**, AP 합계 883,052,682 동일.

| 거래처 | 금액 | 비고 |
|---|---:|---|
| 오다플래그 | 129,466,420 | 월 1,800~2,700만 정기. MES 기록 **0** 이었음 |
| 진안알미늄 | 33,004,350 | 잔액은 맞았고 거래가 없었다 |
| 대진국기사 | 30,530,500 | 거래처 **신규 생성** |
| 한국엡손 | 20,293,460 | |
| 극동잔넬 | 19,124,000 | 거래처 **신규 생성** |
| 바로 | 18,037,800 | |
| 허밍 프린트 솔루션 | 13,787,400 | |
| NTIC | 11,550,000 | |
| 부림엔에프 | 8,592,100 | |
| 화성프로세스 | 6,146,250 | |
| 스타플렉스 | 5,519,800 | |
| 명성연사(볼로프) | 5,296,500 | |
| 대보상사 | 4,743,200 | |
| 판다코리아 | 4,180,000 | |
| 대양산업 | 4,176,427 | |
| 신우팩키지 | 3,824,700 | 상대명 `이종승` = 대표자 |
| 우승상사 | 2,926,000 | |

### 한계 — 이 적재가 못 하는 것

- **품목 라인이 없다.** 통장은 금액만 알려준다. 자재 소진·재고와 연결되지 않는다
- **매입일이 지급일로 대체됐다.** 오다플래그처럼 전월 마감분을 결제하는 곳은 인식 시점이 뒤로 밀린다
- 신규 2곳(대진국기사·극동잔넬)은 **사업자번호 미확보** → `client_code` 가 `PENDING-*` 다.
  세금계산서에서 번호를 확보하면 `client_code`·`business_registration_number` 를 UPDATE 할 것
- **제외**: 우드케이 4,535,850 · 대진국기사 택배운임 1,650 = 운반비 성격이라 매입이 아니다
- **미반영 입금**: 오다플래그 05-11 2,050,000 · 대진국기사 04-30 66,000 (반품/상계 추정, 근거 미확보)

## 5. 남은 것

- **입고 운영 착수** — `received_quantity` 초기화 기준일 결정이 선행. 데이터 작업이 아니라 운영 결정이다
- 적재분 17곳의 **품목 라인 보완** — 거래명세서가 있어야 한다
- 통장 미분류 잔량: 개인 송금(대표 김진수 4,905만 · 정준택 1,650만 · 장선부 1,256만 등)
- 대진국기사·극동잔넬 사업자번호
- **★2026-07 매출·수금 이관** — 아래 참조

### 2026-08-06 추가 해소 2건

**`홀리과입금건` 34,202,740 = 선명커뮤니케이션 법인간 이체.** 적요에 회사명이 없어 미분류로 남아 있었다.
DB 전체에서 이 금액은 두 건뿐이고 그것이 짝을 이룬다 — `e1 2026-07-09 출금` / `e2 2026-07-09 입금 「(주)동산기획」`.
세무장부도 2026-07 251 차변(외상매입금 지급)으로 기장했다. **양측 대칭 반영**(e1 `purchase_payments` + e2 `payments`) —
한쪽만 넣으면 3법인 대사가 깨진다. 결과: e1 선명 미러 AP 81,163,297 / e2 동산기획 AR 76,425,443.

⚠️ 두 잔액의 차 4,737,854 는 **미러 구조차이며 결함이 아니다** — e1 미러는 부가세 포함, e2 `orders.total_amount` 는 공급가다
(미러 월합 52,116,385 = e2 주문 47,378,531 × 1.1, 원 단위 일치). 적재 전부터 있었고 이번 적재로 변하지 않았다.

**`652-73-159234` 는 미등록 정기 지출이 아니었다** — 이미 등록된 `loans` id 14 제일은행이다.
상세·교훈 = `SMARTA_FIXED_EXPENSES.md`

### ★ 다음 큰 건 — 2026-07 매출·수금 이관

`payments`(AR 수금)가 동산기획만 7월에 끊긴다:

| 법인 | 2026-05 | 2026-06 | 2026-07 |
|---|---:|---:|---:|
| 동산기획 | 449건 370,184,381 | 503건 700,705,183 | **3건 1,227,380** |
| 선명 | 50건 91,363,924 | 64건 119,434,216 | 15건 31,385,395 |

⚠️ **처음에 "7월 매출채권이 과대"라고 적었는데 틀렸다.** 수금만 보고 내린 결론이었다.
`orders` 를 같이 보면 **매출도 7월이 0건**이다 — 양쪽 법인 모두:

| 법인 | 2026-05 | 2026-06 | 2026-07 |
|---|---:|---:|---:|
| 동산기획 | 1,151건 520,593,028 | 1,241건 425,579,947 | **0건** |
| 선명 | 158건 99,237,293 | 178건 117,944,764 | **0건** |

매출·수금이 **함께** 없으므로 AR 은 과대도 과소도 아니다. 장부는 **2026-06-30 기준으로 정합**하고
7월 이후가 통째로 비어 있을 뿐이다(7월 수금 3건 1,227,380 만큼만 미세하게 과소).
왜곡이 아니라 **컷오프**다.

★ **교훈: 한쪽 축만 보고 "과대/과소"를 말하면 안 된다.** 차변·대변이 함께 비면 잔액은 멀쩡하다.

**남은 판단 — 7월 이관의 원본을 무엇으로 할 것인가**

| | 원본 | grain | 품목 | 비고 |
|---|---|---|---|---|
| A | **이카운트** | 주문 단위 | 있음 | 상반기 7,642건과 **동일 방식**. 이카운트 접근 필요 |
| B | 세무장부(SmartA) 매입매출장 | 세금계산서 단위 | 없음 | 거래처별 월합산이라 **상반기와 grain 이 달라진다** |

상반기가 A 로 되어 있으므로 A 가 정합적이다. B 로 하면 7월만 이질적인 덩어리가 되어
품목별 매출·원가 분석이 그 달만 끊긴다. 세무장부는 **검증용 통제금액**으로 쓰는 게 맞다.
