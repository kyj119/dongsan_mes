-- ============================================================================
-- 2026-08-26 생산장비 11품목(`GDS-EQ-*`) 성격 분리 — 용준님 구술 확인
--
-- 배경: 11품목 전부 `item_type='PRODUCT'`·`category='상품'` 이었다. 사서 **쓰는** 장비인데
--       "제품/상품"으로 잡혀 BOM 후보 모수에 들어가고, 일부는 판매 플래그까지 켜져 있어
--       **5,200만원짜리 장비가 주문서 선택기에 떴다**.
--
-- 매입 실측 = 2026-02-11 ~ 07-31 · 17라인 · **174,762,800원** (동산 1억 5,363만 / 선명 2,113만)
--   매입처 = 케이엠테크 8,215만 · (주)티피엠 5,960만 · 한국엡손 1,468만 · 다다모아 800만 · 더싸인 33만
--
-- ⚠️앞선 보고의 「장비 11건 1.59억」은 H2 조건(판매 실적 0)에 걸린 것만 센 값이었다. 전체는 1.7476억.
-- ============================================================================

-- ── 용준님 구술 → 성격 분류 ──────────────────────────────────────────────
--
-- ① **자사 사용 장비(고정자산) 8품목 · 153,450,000원** — `item_group='생산장비'`
--    | 품목 | 매입 | 배정 |
--    |---|---|---|
--    | `GDS-EQ-EXT1880T` Extreme-1880T 1600mm | 02-11 케이엠테크 2,800만 | 전사출력실 **KM전사 1번**(`FLEXI-01`) ✅확정 |
--    | `GDS-EQ-EXT-AUX` 집진기·핸드미스트·헤드 | 02-11 215만 + 04-10 무상 | 위 장비들과 한 묶음. 헤드 8개 = 2280T **2대에 4개씩** |
--    | `GDS-EQ-EXT2280T` Extreme-2280T 2000mm ×2 | 04-10 케이엠테크 5,200만 | 전사출력실 **2번**(`FLEXI-TOYO1`)·**3번**(`FLEXI-03`) ✅확정 |
--    | `GDS-EQ-CF2000` CF2000 발색기 2000폭 | 03-24 다다모아 800만 | 전사출력실 **발색기**(equipment 미등록=의도) |
--    | `GDS-EQ-EP3208` OPTIMUM EP3208 | 03-31 티피엠 3,500만 | **TPM-03** ✅확정 |
--    | `GDS-EQ-EP1854` OPTIMUM EP1854 | 03-31 티피엠 1,380만 | **TPM-01** ✅확정 |
--    | `GDS-EQ-EPS39405` 엡손 SC-S8140 본체 | 06-30 한국엡손 1,410만 | **EPSON-02** ✅데이터 확증(printer_name='EPSON SC-S8140') |
--    | `GDS-EQ-EPSLIFT` 9140/8140 Lift Kit | 06-30 39.5만 | EPSON-02 부속 |
--    ⇒ `item_type` 은 **PRODUCT 로 그대로 뒀다** — 3종(PRODUCT/GOODS/MATERIAL) 중 고정자산에 맞는 값이 없다.
--      자산 축은 [[design-fixed-asset-depreciation]] 의 「비품에 생산장비 46건」과 같은 문제다.
--    ★`GDS-EQ-EP3208`·`GDS-EQ-EP1854` 는 `is_sales_item=1` 이었다 → **0 으로 내렸다**(자사 장비가 주문서에 뜸).
--
-- ② **사서 되판 상품 2품목 · 21,130,000원** — `item_group='장비 상품'` · `item_type='GOODS'`
--    `GDS-EQ-EP1852` — 매입 03-23 티피엠 11,000,000 → **판매 03-24 반산기획 12,700,000**
--                       매입 07-14 티피엠  9,800,000 → **판매 07-14 새물광고 12,700,000**  (마진 합 460만)
--    `GDS-EQ-EYEPUNCH` 자동 아일렛펀칭기 — 매입 07-20 더싸인 330,000 (선명)
--      ✅2026-08-26 용준님 정정: **판매한 게 아니라 재고 보유**다(아직 안 팔림).
--        ⚠️단 `inventory` 행이 0 이라 MES 재고에는 안 잡힌다 — 용준님 「크게 신경쓰지마」이므로 조치하지 않는다.
--
-- ③ **소모품 1품목 · 182,800원** — `item_group='장비 소모품'` · `item_type='MATERIAL'`
--    `GDS-EQ-EPS10153` 엡손 유지보수 키트 — **EPSON-01(SC-S9140)에 사용**. 장비가 아니다.
--
-- ★`item_group` 을 붙인 이유 — 성격을 가르자마자 `audit:items` **H4a 가 3건 떴다**.
--   `GDS-EQ-` 접두를 계열로 폴백했기 때문인데, 그 접두는 유형이 아니라 「장비 관련」이라는 뜻이라
--   계열 축으로 부적합하다. 그룹을 붙이니 8/2/1 로 갈려 H4a·G1 **0건**. G3(그룹 미지정) 182 → 171.
--
-- 백업: _bak_0826_gdseq (11행 · item_type·플래그·category·description·search_keywords)
-- ============================================================================

-- 롤백
UPDATE items SET
  item_type       = (SELECT b.item_type       FROM _bak_0826_gdseq b WHERE b.id = items.id),
  is_sales_item   = (SELECT b.is_sales_item   FROM _bak_0826_gdseq b WHERE b.id = items.id),
  is_purchase_item= (SELECT b.is_purchase_item FROM _bak_0826_gdseq b WHERE b.id = items.id),
  description     = (SELECT b.description     FROM _bak_0826_gdseq b WHERE b.id = items.id),
  search_keywords = (SELECT b.search_keywords FROM _bak_0826_gdseq b WHERE b.id = items.id)
WHERE id IN (SELECT id FROM _bak_0826_gdseq);
-- ⚠️item_group 은 백업 전에 비어 있었으므로 롤백하려면 별도로 NULL 로 되돌린다:
-- UPDATE items SET item_group = NULL WHERE item_code LIKE 'GDS-EQ-%';

-- ============================================================================
-- ✅ 호기 배정 확정 (2026-08-26 용준님)
--
--   | 품목 | 장비 | equipment.printer_name (채움) |
--   |---|---|---|
--   | `GDS-EQ-EXT1880T` 2,800만 | **KM전사 1번** `FLEXI-01` | Extreme-1880T 1600mm |
--   | `GDS-EQ-EXT2280T` 5,200만 (2대) | **KM전사 2번** `FLEXI-TOYO1` · **3번** `FLEXI-03` | Extreme-2280T 2000mm |
--   | `GDS-EQ-EXT-AUX` | 위 3대 공용. 무상 I3200-A1 헤드 8개 = 2280T 2대에 **4개씩** | — |
--   | `GDS-EQ-EP1854` 1,380만 | **TPM-01** | OPTIMUM EP1854 |
--   | `GDS-EQ-EP3208` 3,500만 | **TPM-03** | OPTIMUM EP3208 |
--   | `GDS-EQ-EPS39405` + `EPSLIFT` | **EPSON-02** | EPSON SC-S8140 (기존) |
--   | `GDS-EQ-CF2000` 800만 | 전사출력실 **발색기** | — (아래 참조) |
--
--   ★**CF2000 발색기가 `equipment` 에 없는 건 의도된 것**(용준님) — 인터넷을 연결하지 않아 로그 수집
--     대상이 아니다. 「미등록 = 누락」이 아니므로 **등록하자는 제안을 다시 만들지 말 것**.
--
--   `equipment.printer_name` 을 채웠다(비어 있던 5대). 표시용 라벨이고 매칭 키가 아니다 —
--   로그 매칭은 `print_events.printer_name`(RIPLOG 원본)과 `equipment_id` 축이라 영향이 없다.
--   백업 `_bak_0826_equip_name`(6행).
--
-- ✅ `TOPM-01` 의 printer_name 이 `TPM-01` 과 같은 `EP1854` 인 것은 **오기가 아니다** — 같은 중국산 모델을
--    쓰는 **별개 장비 2대**다(용준님 확인). 둘 다 출력 실적이 있다(TPM-01 1,045건 · TOPM-01 750건).
--    ⇒ 모델명이 겹친다고 중복으로 판단하지 말 것. equipment.notes 에 기록해 뒀다.
-- ⇒ 남은 조치 **없음**.
-- ============================================================================
