# 병행테스트 — 직원이 쓰고, 결함은 여정이 된다

> 2026-09-15 시작 조건 충족(J0~J7 36단계 3사이클 + 제안 P1~P12 전부 판정). 기간 **2주**. 정본 절차 = `/journey-loop` 스킬.
> 원칙: 막지 않는다(불편은 기록하고 계속 쓴다) · 판정은 재현으로만(「어색하다」는 제안 축) · 고친 것은 반드시 여정 단계로 승격.

## 1. 누가 무엇을 (2026-09-16 용준님 확정)

| 역할 | 담당(이름) | 매일 하는 것 | 화면 | 대응 여정 |
|---|---|---|---|---|
| 주문 입력 | 동산 **인호동 · 정소은 · 김보연 · 김영주**(디자이너, 주문서) · **정해선**(관리부, 주문서+견적서) / 선명 **최상호 · 임선미 · 강지영** (09-16 용준님 재확정 — 정송엽은 빠짐) | 실제 주문을 MES 주문서로(견적이 있으면 견적서 → 전환 — 견적서는 SALES 인 정해선·최상호·강지영). 자재 유통 주문 1건 포함. 디자이너는 여신 초과 시 PENDING+결재로 넘어간다 | `/order-form` `/quotation-form` `/orders` | J1·J1b·J6·J7 |
| 출력실 | 동산 **정보람 · 김용덕 · 모니르** | 카드 보드에서 출력완료 확인(출력 이벤트가 자동으로 찍는다) → 출고. 잘못 출고한 건은 **용준님**이 주문 상세 「출고 취소」(관리자 전용) | `/cards` `/shipments` | J2 |
| 경리 | 동산 **신현서 · 김진수(과장)** | 출고된 주문 회계반영 → 원장에서 입금 등록 | `/orders` `/ledger` | J3 |
| 구매 | 용준님 · 강지영 | (진행 중인 2주 테스트 그대로) 발주 → 입고 → 검수. 발주 없이 온 물건은 「발주 없이 입고」 | `/purchase-order-form` `/receiving` `/purchase-orders` | J4·J4b |
| 재고 | 동산 **한두선** · 선명 **강지영** | 주간 실사 1회(자기 구역 — 승인까지 본인) | `/inventory` 실사 탭 (`/inventory-counts` 라는 화면은 없다) | J5 |

### 1-a. 계정·권한 — 2026-09-16 새벽 prod 반영 완료(로그인·화면 200 실측)

| 누구 | 로그인 | 반영한 것 |
|---|---|---|
| 정송엽 | 아이디 **`1234`**(기존) | 역할 OPERATOR → **SALES**(주문서·견적서가 열린다) |
| 강지영 | 기존 | SALES 에 `/inventory`(실사는 재고 화면의 실사 탭·선명2 구역 담당이라 승인까지 본인) |
| 정보람 · 모니르 | 기존 | OPERATOR 에 `/cards`(카드 보드, 역할은 그대로). 출고 `/shipments` 는 원래 열림 |
| 김용덕 | 아이디 **`김용덕`** 신설(OPERATOR, 동산) | 카드 보드·출고 200 확인 |
| 신현서 · 김진수(과장) | 아이디 **`신현서` · `김진수과장`** 신설(ACCOUNTANT, 동산) | ACCOUNTANT 에 `/orders`(회계반영 버튼) — 주문 목록·원장 200 확인 |
| 한두선 | 기존 | 변경 없음 — OPERATOR 는 `/inventory` 가 열리고 출력실 구역 담당이라 실사·승인 가능 |
| 출고 취소 | 용준님 | 관리자 전용 그대로 |
| 강지영 · 정해선 · 최상호(SALES) | 09-16 경로 대조에서 발견 | SALES 에 **`/purchase-orders`**(발주서·발주 목록·검수 대기) 가 없어 구매 담당 강지영이 발주서를 못 열었다 → 매트릭스에 access+edit 추가. 실측: SALES 계정으로 발주서·발주 목록·입고·재고 전부 200 |
| 인호동 · 정소은 · 김보연 · 김영주 · 임선미 | 기존(DESIGNER) | 변경 없음 — DESIGNER 는 주문서·주문 목록·거래처가 열린다(견적서는 안 열림 → 견적은 SALES 담당이) |
| 정해선(동산) · 최상호(선명) | 아이디 **`정해선`(E1) · `최상호`(E2)** 신설 완료(SALES, 용준님 `!` 실행 09-16) | 주문서·견적서·거래처 200 실측. 직원 연결 완료(`.journey/tmp/link-users-2.sql`, user_id 29·30, 읽기 실측) |

⚠️ 신설 3계정의 초기 비밀번호는 용준님이 구두로 전달하고, 첫 로그인 후 본인이 변경(우측 상단) — 문서에 적지 않는다. ✅ 신설 계정 3개 ↔ 직원 레코드 연결 완료(09-16 03:11, 용준님 `!` 실행 — `employees.user_id` 26·27·28, 읽기 실측). API 가 없어 수동 SQL 이었다(`.journey/tmp/link-users.sql`, 재실행해도 no-op). 카드 처리자 표기·급여명세 셀프교부가 본인으로 잡힌다.
권한 매트릭스 = `/settings` 권한 탭(`role_page_permissions`). 계정 = `/users`. ★API 로 한글 계정을 만들 땐 Node `fetch`(curl 은 Git Bash 에서 한글이 깨져 아이디가 「����」로 들어갔다 — 하드 삭제 후 재생성).

## 2. 결함 기록 — 이 네 줄이면 여정이 된다

```
화면:          (예 /order-form)
하려던 것:      (예 견적서에서 넘어온 주문에 라인 하나 추가)
실제로 된 것:   (예 저장 뒤 규격이 사라짐 · 문구 「…」 · 아무 반응 없음)
다시 하면 또?:  예 / 아니오 / 가끔
```
어디에: 카톡 「MES 병행」 방 또는 `docs/journeys/PROPOSALS.md` 에 P번호로. **재현이 되는 것만 결함**, 안 되는 것은 제안(P)으로 남긴다.

## 3. Claude 쪽 일일 루틴

1. 아침 `JOURNEY_BASE_URL=http://localhost:3001 npm run journey:cycle` (또는 `/loop`) — 36단계 통과 확인.
2. 기록된 결함 → 재현 → 안전 수정 화이트리스트 안이면 고치고 **여정 단계 추가** → 배포(`ship:gate` 가 여정을 돌린다).
3. 화이트리스트 밖(구조·정책)은 PROPOSALS 에 P번호로 올리고 용준님 판정을 받는다.
4. 주말: 2주 뒤 판정 재료를 센다 — 직원별 MES 직접입력 주문 수(`orders.created_at` 이 09:00:00 이 아닌 것) · 결함 건수 · 여정 단계 수.

## 4. 2주 뒤 판정 기준

- MES 직접입력 주문이 **주 10건 이상**이고 결함 재현율이 떨어지면 → 전환(이카운트 병행 종료 검토 · 보안 묶음 P6 착수).
- 직접입력이 안 늘면 → 화면이 아니라 **업무 순서**가 문제다(08-25 「6월 급락」 오독 교훈) — 이름 옆에 「왜 안 썼나」 한 줄부터.
