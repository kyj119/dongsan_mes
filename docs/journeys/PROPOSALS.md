# 여정 루프 — 제안 대장 (자동 수정 밖의 발견)

> 한 건 1줄: 현상 · 재현 · 판단 근거. 코드는 여기서 안 고친다 — 용준님이 ①고침 ②정책 유지 ③보류를 고른다.
> 정본 절차 = `/journey-loop` 스킬. 자동 수정 화이트리스트에 드는 것은 여기 오지 않고 바로 커밋된다.

## 2026-09-11 사이클 1 (J1~J4 16단계 통과)

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P1 | API 응답 | 입금 등록에서 「1분 이내 동일한 결제」 거절이 **500 「서버 오류가 발생했습니다」** 로 나갔다(`ar-payments.ts` 가 `DUPLICATE_PAYMENT` 를 안 받아 바깥 catch 로) — 경리는 사유를 못 보고 모니터링엔 장애로 집계 | 같은 거래처·같은 금액으로 1분 내 두 번 입금 | 통장 매칭 경로(`bank.ts`)는 이미 400·문구로 처리 → 같은 형태로 통일 | ✅ 고침(09-11) — J3 에 거절 단계 추가 |
| P2 | UI | 카드를 체크하면 **일괄 바가 둘** 떴다 — 표준 `#cardBulkBar`(ds-bulk-bar, z-35: 상태 선택·일괄 변경·선택 해제)와 레거시 `#bulkBar`(rip.js, z-40: ✓출력완료·⏸보류·해제). 둘 다 고정 하단이라 **레거시가 표준 바를 항상 덮어** 표준 바의 버튼이 눌리지 않았다(처음엔 「상단 버튼이 가려진다」로 오진) | /cards 카드 체크 → 「일괄 변경」 클릭 | 같은 선택에 같은 기능이 두 벌 — 표준(ds) 하나로. 레거시 DOM·`bulkChangeStatus` 제거, 옛 이름은 표준으로 위임 | ✅ 고침(09-11) — J2 가 표준 바로 출력완료를 찍는다 |
| P3 | 규칙 확인 | 진행중(출력대기·출력중) 카드의 개별 버튼은 **RIP 전송·보류**뿐, 출력완료 버튼은 없다 | /cards 진행중 카드 | **설계다** — 출력완료는 출력 이벤트(에이전트 `print_events`)가 찍고, 사람이 손으로 찍는 예외 경로는 체크→일괄 변경(P2 로 살아났다). 코드에 죽은 가지 하나(`buildGridCard` 의 `columnType==='printing'` — 그리드는 완료 칼럼에만 쓴다)는 무해 | ③ 유지(설계). 병행테스트 때 출력실이 「버튼이 없다」고 하면 그때 안내 문구·버튼 결정 |
| P4 | 시각 | `shipped_at` 등 datetime 이 UTC(`CURRENT_TIMESTAMP`)로 저장된다(실측 KST 01:49 → `16:49`) | 출고 후 DB 조회 | **정상** — 저장=UTC·표시=`window.formatKST`(`toKstDate`+`Asia/Seoul`, shell.js:157)가 정본이고 출고 시각 표시가 이 경로다(`orders.js:1050`). 6곳 전부 같은 방식 | ③ 유지(정상). DB 를 직접 읽을 때만 +9h 를 기억 |
| P5 | 운영 | 로그인 API 가 **IP 당 5회/분**(`index.tsx`) — 병행테스트 기간에 같은 공유기 뒤의 직원 여러 명이 아침에 동시에 로그인하면 3~4명째부터 「요청이 너무 많습니다」 | 60초 안에 6번 로그인 | 사무실 NAT 는 한 IP 다 → **계정당 5회/분**(무차별 대입) + **IP 당 30회/분**(계정 스프레이). 포털·비밀번호 변경은 그대로 | ✅ 고침(09-11) — J0 로그인 여정 신설(6번째 429·다른 계정 통과) |

## 2026-09-11 배포 검증에서 추가

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P6 | 운영/보안 | **prod 의 로그인 한도는 간헐적으로만 걸린다** — 같은 계정 12회 연속 틀리기: `401×7 → 429 → 401×2 → 429 → 401`(로컬은 정확히 6번째 429). `middleware/rateLimit.ts` 가 **isolate 메모리** 카운터라 Cloudflare 가 요청을 다른 isolate 로 보내면 각자 0부터 센다 → 실효 한도 ≈ 5 × isolate 수. **기존 IP 방식도 같은 성질**이었으므로 이번 변경(P5)의 회귀는 아니다 | prod 에서 존재하지 않는 계정으로 60초 안에 10번 이상 | 무차별 대입 방어가 「있다」고 믿으면 안 된다. 제대로 하려면 상태를 isolate 밖에 둔다 — Cloudflare **Rate Limiting 바인딩**(wrangler `[[ratelimits]]`, 계정·IP 키) 또는 D1/KV 카운터. 로컬 여정 J0 는 단일 workerd 라 계속 정확히 잡는다 | ③ 보류(09-11) — 「보안강화=실사용 전환 시」로 이미 정한 묶음(`project-full-review-2026-09-03`, #626 평문PW 수용과 같은 축)에 넣는다. 병행테스트 진입을 막지 않는다. 전환 시 Rate Limiting 바인딩 우선 검토 |

## 2026-09-11 사이클 2 (J5 실사·J6 견적→주문 신설 중 발견)

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P7 | UI 결함 | **견적서에서 품목 검색칸에 Enter 를 치면 폼이 제출된다** — 검색 대신 「1개 품목의 단가가 0원입니다. 계속하시겠습니까?」 확인창. 「확인」이면 **반쯤 채운 견적이 저장**된다. 주문서는 Enter=검색(itemRow.js keydown) | /quotation-form 품목칸에 「현수막」 입력 후 Enter | `quotationForm.js` 의 품목칸에 `input` 리스너만 있고 keydown 이 없어 `<form>` 기본 제출. 거래처칸은 Enter=검색이라 영업은 같은 손버릇으로 친다 | ✅ 고침(09-11) — Enter=검색 실행, 주문서와 동일. 회귀 = J6 「견적서 작성·저장」 |
| P9 | UI 부재 | **완전 출고된 주문의 출고 취소에 화면에서 닿을 수 없다** — 주문이 SHIPPED 가 되면 카드 보드가 카드를 숨기고(`exclude_order_status=SHIPPED`), `/cards/:id` 엔 출고 버튼만 있다. 보드 모달의 「출고 취소」는 부분 출고(주문이 아직 SHIPPED 아님) 카드에서만 보인다. `/shipments` 는 `PATCH /:id/status CANCELLED` API 는 있으나 상태 변경 UI 는 확인 못 함 | 카드 출고 → 보드에서 카드 검색 | 환원 로직(`restoreStockLinesOnUnship`·STOCK_RESTORE 로그)은 세 경로(카드 unship·주문 core·출고 취소) 다 있는데 **누르는 곳이 없다**. 병행테스트에서 「잘못 출고했다」가 나오면 첫날 막힌다. J2 는 API 로 환원만 검산 | ✅ 고침(09-15, 용준님 「남은 부분 진행」) — **주문 상세에 「출고 취소」**(ADMIN·MANAGER, SHIPPED 이고 회계반영 전). 서버 `PATCH /api/orders/:id/unship`(`orders/lifecycle.ts`): 재고 환원(`restoreStockLinesOnUnship`) → 카드 `shipped_at` 전부 NULL → 주문 PRINT_DONE(카드 없는 유통 주문은 CONFIRMED) + status_history + activity UNSHIP. 회계반영(BILLED/PAID)이면 400. 보드 필터안은 기각 — 완전 출고된 주문은 카드가 아니라 주문 단위로 되돌리는 게 맞고, 보드는 「할 일」 화면이다. 회귀 = J2 「출고 취소」가 API 폴백 없이 이 화면 경로를 탄다 |
| P10 | UX | 주문서에서 거래처를 안 고르고 저장하면 앱 문구(「거래처를 선택하세요」) 대신 **「선불/착불」 select 의 브라우저 required 말풍선**이 먼저 뜬다(기본 배송방법=대신택배 → 선불/착불 required, 기본값 빈칸). 제출 이벤트 자체가 안 떠서 앱 검증(거래처→납기→…)이 한 번도 안 돈다 | /order-form 에서 품목만 채우고 저장 | 진짜 빠진 건 거래처인데 말풍선은 선불/착불을 가리킨다 — 영업이 헤맨다. 선불/착불 기본값을 두거나(택배=선불), required 대신 앱 검증에 합류 | ✅ 고침(09-15) — **앱 검증에 합류**: `client.js` 가 `required` 대신 `data-needs-payment` 만 남기고, `calc.js` 제출 검증(이미 있던 「선불/착불을 선택하세요」)이 거래처→납기→품목 뒤 같은 자리에서 막는다. 기본값(택배=선불)은 두지 않았다 — 착불 주문을 조용히 선불로 저장하는 사고가 더 비싸다. 회귀 = J1b ① 이 선불/착불을 비운 채 저장해 앱 문구를 본다 |
| P8 | 관찰→결정 | 견적서 품목 검색은 `type=sales` 만 걸어 「현수막」 결과가 **50행**(주문서는 `excludeType=MATERIAL` 로 6행). 세 화면(제작 주문서=체크박스 기본 OFF·유통 주문서=전부·견적서=전부)이 자재를 다르게 다뤘다 | 같은 검색어를 두 화면에서 | **영업은 자재를 유통한다**(용준님 09-14) — prod 6월 이후 라인의 27%·금액 31%가 자재·상품. 「자재를 보여줄까」는 주문마다 정할 일이 아니라 누가 검색하느냐 | ✅ 고침(09-14, **나안**) — 주문서·견적서 같은 토글 「원자재 포함」, 기본 ON, `user-prefs orderform.includeMaterials` 로 계정별 기억(shell.js 공용). 회귀 = J1 「자재 포함 토글」 |
| P11 | 구조 제안 | **유통 주문서(`/order-form?type=dist`)는 한 번도 안 쓰였다** — prod 주문 11,670건 전부 `PRODUCTION`, `DISTRIBUTION` 0건. 제작 주문서가 이미 라인별로 유통 모드를 탄다(`applyDistRowMode`: 규격 텍스트·가로세로 잠금·후가공 숨김) + 부속품 행도 있다. 유통 주문서만의 실효 차이는 **`order_type=DISTRIBUTION` → 카드 미생성·shipment_ready·자재 갭 미계산** 셋뿐 | `/order-form` 상단 「유통(상품) 주문서로 전환」 링크 | 같은 사람이 「이건 제작인가 유통인가」를 화면 고르기로 매번 결정한다 — 결정은 라인에 있다 | ✅ 고침(09-14, 용준님 「①②③ 순서대로」) — **①** 유통 주문서 페이지·`orderFormDist.js` 삭제, `?type=dist` 는 주문서로 302, 주문 목록 「새 주문」 링크 고정 **②** `helpers.deriveOrderType`: 라인 전부 `production_required=0` 이면 DISTRIBUTION(카드 미생성·shipment_ready), 하나라도 제작이면 PRODUCTION, 마스터에 없는 라인=제작. 명시 `order_type` 은 API 호환 **③** `/api/items?with_stock=1` → 자재·상품 행에 「재고(이 법인 합계, base 단위)·최근단가(금액÷수량, 판매일)」 — 검색 모달 열 2개 추가(id 로 묶어 2쿼리, 상관 서브쿼리 없음). 회귀 = **J7**(리다이렉트·자재만 주문=DISTRIBUTION·카드 0·모달 재고 칸·일괄 출고→SHIPPED·OUT 원장·재고 −수량) |

## 2026-09-14 규격 축 질문에서 (「규격 1칸 상품·3축 품목은 어떻게 입력하나」)

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P12 | 구조 결정 + 결함 3 | **3축 품목(아크릴 박스·큐브간판·입간판 `W*D*H`)에 과금축이 없다** — 그리고 ② 주문서 FIXED 제작품 행엔 규격 텍스트 칸이 없고(유통 행에만) ① 견적 라인엔 `specification` 컬럼 자체가 없어 전환에서 소실 ③ 09-04 소급 분해가 3숫자 규격을 두 칸 cm 로 넣어 광학산PC 큐브 2건의 단가가 6,333,333·1,166,667원/㎡(직전가 제안 오염) | prod 2026년 `order_items.specification LIKE '%*%*%'` | 진짜 3축은 **9건/8개월**, 전부 간판 계열·건별 손 단가·7만~48만 → 전개면적/부피 축을 만들면 산식 쌍·픽스처·스냅샷·문서 5곳이 늘고 쓰는 라인이 없다. 「면적 판정=규격 종류 수」로도 3축 품목은 규격이 1회성 → **FIXED + 규격 텍스트 1칸이 정본**. 되돌릴 조건 = 월 5건 이상 반복 + 실제 ㎡×전개면적 규칙 | ✅ 고침(09-15, 용준님 「①②③ 전부」) — **②** 주문서 규격 텍스트 칸을 품목칸 아래로 옮기고 표시 조건을 「유통」→「`pricing_method !== 'AREA'`」(`itemRow.js syncSpecField`), 가로·세로는 참고 입력 유지, 수정화면의 유통 판정은 규격 유무가 아니라 `item_type`(`core.ts`) **①** `0614` `quotation_items.specification` + 견적서 같은 칸·INSERT/SELECT/전환 복사·프리필·견적 문서 표기(specification 우선) **③** `0615` 오분해 5라인 정정(width/height NULL, AREA 2건은 라인 축 FIXED + 단가=금액/수량). 회귀 = J1 「FIXED 제작품 3축 규격」 · J6 「규격 텍스트 왕복」. 설계 = `docs/superpowers/specs/2026-09-14-three-axis-and-single-spec-lines.md` |

## 2026-09-15 J4b 「발주 없이 입고」 신설 중 발견

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P13 | 결함 | **「발주 없이 입고」가 발주만 만들고 입고 모달을 못 연다** — 수량 prompt 뒤 「발주서를 만들었습니다」 직후 「발주 생성 실패: loadPendingPOs is not defined」. 발주(`adhoc_source='RECEIVING'`)는 CONFIRMED 로 남고 입고는 사람이 목록에서 다시 찾아야 한다 | /receiving → 발주 없이 입고 → 공급처·품목·수량 | `receiving.js adhocCreate` 가 #328 에서 지워진 `loadPendingPOs()` 를 부른다(같은 파일 140줄에 「dead … 실경로는 loadReceivingQueue」 주석이 있다). 0611 기능이 붙을 때 옛 이름을 썼고 **어떤 게이트도 안 잡았다** — `check:dom` 은 getElementById 만 본다 | ✅ 고침(09-15) — `loadReceivingQueue()` 로. 회귀 = J4b(모달이 곧바로 열려 입고 확정까지). 제안: `?raw` 스크립트의 **미정의 전역 함수 호출 감사**(check:dom 의 함수판) — 다음 게이트 후보 |

## 판정 기록
(용준님 판정 후 여기로 옮긴다: 날짜 · 번호 · 결정 · 반영 커밋)

- 2026-09-15 · P13 · ①고침 — `receiving.js adhocCreate` `loadPendingPOs`→`loadReceivingQueue`. 회귀 = J4b.

- 2026-09-15 · P12 · 결정=새 과금축 없음(FIXED+규격 텍스트) + ①②③ 고침 — `0614`·`0615`, `itemRow.js`·`parent.js`·`quotationForm.js`·`quotations.ts`·`core.ts`·견적 뷰 2곳. 회귀 = J1·J6 각 1단계(36단계).

- 2026-09-11 · P1 · ①고침 — `routes/ledger/ar-payments.ts` DUPLICATE_PAYMENT → 400 「1분 이내 동일한 입금이 이미 등록되었습니다」(bank.ts 와 동일). 회귀 = J3 「같은 입금을 1분 안에 또 넣으면 …」
- 2026-09-11 · P5 · ①고침 — `middleware/rateLimit.ts` 에 `perAccount` 옵션(요청 body 의 username 버킷), `index.tsx` 직원 로그인 = IP 30/분 + 계정 5/분. 회귀 = J0 「한 계정을 5번 틀리면 6번째는 429, 다른 계정은 통과」. 포털 로그인·비밀번호 변경·refresh 는 종전 그대로(외부 IP 분산).
- 2026-09-11 · P2 · ①고침 — `pages/cards.ts` 레거시 `#bulkBar` DOM·CSS 제거, `scripts/cards/rip.js` `bulkChangeStatus` 제거·`updateBulkBar`/`clearSelection` 은 표준 바로 위임. 회귀 = J2 「보드에서 카드를 찾아 출력완료」(표준 바 경로).
- 2026-09-11 · P3 · ③유지(설계) — 출력완료는 출력 이벤트가 찍는다. 병행테스트에서 출력실 피드백이 오면 재검토.
- 2026-09-11 · P4 · ③유지(정상) — UTC 저장 + `formatKST` 표시가 정본.
- 2026-09-14 · P8 · ①고침(나안) — 주문서·견적서 「원자재 포함」 토글 기본 ON + `user-prefs orderform.includeMaterials` 계정별 기억(`shell.js` 공용 초기화), 견적서 검색은 토글 OFF 때만 `exclude_type=MATERIAL`. 회귀 = J1 「자재 포함 토글」(기본 ON·끄면 기억·견적서 동일값).
- 2026-09-14 · P11 · ①②③ 고침 — 유통 주문서 폐지·`order_type` 라인 파생·검색 모달 재고/최근단가. 회귀 = J7. **라인별 규격 1칸(`300*90`) 회귀는 하지 않는다** — 09-04 결정(축=`pricing_method`)·`0601`(자 `3*6` 을 cm 로 읽어 100배) 그대로: 저장은 계산칸 2개(cm)+표기 1칸(`specification`), 입력 편의(`*` 분리 타이핑)는 원하면 별도 제안.
- 2026-09-15 · P9 · ①고침 — `PATCH /api/orders/:id/unship` + 주문 상세 「출고 취소」 버튼(`orders.js unshipOrder`). 회귀 = J2 출고 취소(화면 경로).
- 2026-09-15 · P10 · ①고침(앱 검증 합류) — `client.js` required 제거·`data-needs-payment`, `calc.js` 제출 검증 유지. 회귀 = J1b ①.
