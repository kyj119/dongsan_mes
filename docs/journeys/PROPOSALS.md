# 여정 루프 — 제안 대장 (자동 수정 밖의 발견)

> 한 건 1줄: 현상 · 재현 · 판단 근거. 코드는 여기서 안 고친다 — 용준님이 ①고침 ②정책 유지 ③보류를 고른다.
> 정본 절차 = `/journey-loop` 스킬. 자동 수정 화이트리스트에 드는 것은 여기 오지 않고 바로 커밋된다.

## 2026-09-18 단위 축 전수 점검 (구역 라벨 수정에서 번진 것)

> 「문제되는 품목이 더 없나」를 재다가 **같은 계열 5곳**이 더 나왔다. 둘은 표기가 아니라 **계산·쓰기**다.
> prod 품목 분류 = **A 진짜 다단위 149**(환산 필요·재고 보유 68품목/11,522 base) · **B 편의계수 50**(환산 금지) · D 단일 1,214.

| # | 축 | 무엇 | 판정 |
|---|---|---|---|
| P23-① | 표기 | **자재 부족 경고의 재고 라벨이 관리단위**였다(값은 base) — `materialShortageCheck` 가 `stock.itemId ? stock.unit : mat.base_unit` 로 **재고가 있으면 오히려 틀린 쪽**을 골랐다. 「무광코팅지: 부족 N **롤** (재고 1,586…)」 = 1,586은 M 다 | ✅ 고침(09-18) — `unit: mat.base_unit`(= `resolveStockUnit` 정본). 실측: 라벨이 `롤`→`m` |
| P23-② | **계산** | 같은 경고의 **발주중을 관리단위 그대로 base 재고에 더했다** — 1롤(=61M) 들어오는 중인데 1 로 셌다 → 부족량 과대 = **헛경고** | ✅ 고침(09-18) — `packFactor` 로 base 환산 |
| P23-③ | **계산** | 주간 매입 추천도 같은 혼입(`available = 현재고(base) + 발주중(pack)`) | ✅ 고침(09-18) |
| P23-④ | **쓰기** | **권장 수량(base)이 관리단위 라벨로 발주요청에 들어갔다** — 1,586M 필요 → 「1,586**롤**」 발주 → 입고 시 ×61. prod `purchase_requests` **0행**(한 번도 안 씀)이라 과거 손해 0, **병행 발주 트랙이 켜면 그날 난다**(P14/P15 와 같은 모양) | ✅ 고침(09-18) — 판정을 `utils/purchaseSuggestion` **순수 모듈**로 빼고 `올림(부족 ÷ 계수)`. ★올림은 **환산 뒤**(먼저 올리면 217M 가 217롤). 화면은 「4롤 (217 M)」 병기 |
| P23-⑤ | 운영 함정 | **안전재고·ROP 입력칸에 단위가 없었다** — 담당자가 「10롤」 생각으로 10 을 넣으면 10 M 로 저장돼 임계가 61배 낮아지고 **부족 경고가 영영 안 뜬다** | ✅ 고침(09-18) — 칸에 재고 단위 표시 + 다단위면 「롤 1 = 61 M 입니다」 안내, 현재고도 환산 표기 |

- **게이트 = `npm run test:purchase-suggestion`**(test:calc 편입) — ①환산 가드(★**편의계수 50품목은 절대 환산 안 한다**를 픽스처로 고정) ②추천 산식 ③**소스 스캔**(발주 잔량 `received_quantity` 와 재고를 같은 파일에서 쓰면 환산을 거쳐야 한다 — 현재 4파일) ④표시 단위가 재고 축인가.
- **이상 없음으로 확인**: 품목 검색 모달은 서버가 `stock_unit = base_unit || unit` 로 이미 base 라벨 · 자재 소요 예측은 `_yd` 로 축 명시 · 재고 부족 알림·자재 선택 모달·장비 소모품은 두 값이 같은 축 · 실사·재고 이동·증감내역은 이미 `uomFormatStock` 경로.

## 2026-09-17 미판정 4건 상세점검 (병행 직전)

> 넷을 다시 재니 **다섯**이었다. 새로 나온 P22 는 **화면 경로**이고 로컬에서 재현됐다.

| # | 축 | 무엇 | 판정 |
|---|---|---|---|
| P22 | 결함 | **출고 취소가 [상태 동기화] 한 번에 되살아난다** — `PATCH /orders/:id/unship`(P9 로 만든 화면 버튼)이 `auto_complete_date`·`billable_after` 를 안 지운다. `sync-statuses` Step 1 은 `auto_complete_date` 하나만 보고 다시 SHIPPED 로 올리는데, 조건의 `NOT EXISTS(미출고 카드)` 가 **카드 없는 주문(유통·기성·이관)에서는 무조건 참**이다. 재현: 출고→취소(CONFIRMED)→동기화→**SHIPPED 부활**, `billable_after` 는 09-21→**09-19 로 당겨짐** → 재고는 환원됐는데 출고·청구된 주문이 되고 `auto_billing` 거래처는 자동 회계반영까지 간다. 동기화는 출고/배송·세금계산서 화면의 버튼이라 경리가 매일 누른다 | ✅ 고침(09-17) — `utils/shipBilling.clearShipBillingStmt` 정본 + 되돌리는 문 **4곳** 배선(주문 출고취소·카드 출고취소·출고 CANCELLED·출고 PREPARING 복귀). CANCELLED 분기는 `auto_complete_date` 만 지우고 **`billable_after` 를 빠뜨리고 있었다**. 게이트 = `test:ship-billing` ③ 되돌리기 소스 스캔 + ⑤ 동작 · 회귀 = J2 출고 취소 2줄 |
| P16 | 읽기·표기 | 실측으로 다시 보니 **넷 중 둘은 이미 닫혀 있었다** — 실사 단위 라벨(`count_unit`)·실사 기본 포장수(서버가 `role_count` 계수로 스탬프)는 0620 에서 처리됨 | ✅ 부분 고침(09-17) — **구역 재고 라벨**만 고쳤다: `routes/storageZones.ts` `/:id/stock` SELECT 에 `base_unit·pack_size·stock_mode` 3열 + `pages/storageZones.ts` 에 `UOM_JS` 주입(가드가 있어 회귀 0). 실측: 「비조명용 후렉스 50」이 롤/M×50 이라 종전엔 **「50 롤」로 읽혔다**. ③ **재고 입력 단위 셀렉트 2택 고정은 보류** — prod 에 3단위 품목이 **0개**(1단위 1,228·2단위 185)라 지금은 실화가 아니다. 세 번째 단위를 만드는 날 함께 |
| — | 관찰 | **`salesQtyLabel` 미적용 7곳은 지금 효과 0** — prod `item_units.forms=0` · `order_items.sales_unit` **0행** · `quotation_items.sales_unit` 0행 · `purchase_order_items.unit_factor` 0행. 전 화면이 같은 폴백(그냥 수량)으로 보인다 | ③ 보류 — 폼 스위치를 켜는 날 한 번에 |
| — | 관찰 | **출고 알림 축 실측**(B안 판정 대기) — `shipments` **0행**(출고/배송 화면 미사용) · PP 자재매핑 5건인데 `pp_material_deductions` **0행**(한 번도 안 돌았다) · 알림톡 33건 중 **실패 22**, 출고 템플릿은 성공 1·실패 4 · 이메일은 `RESEND_API_KEY` 미설정 + 도메인 미인증으로 전부 실패. **지금 자동 발송을 켜면 조용히 실패한다**(fire-and-forget + catch). 게다가 출고 확정 시점엔 **송장번호가 아직 없다** | **판정 대기** — 가=송장 확정 시점 자동 / 나=수동 유지 + 미발송 표시 / 다=출고 즉시 자동(`ensureShipmentForOrder` 가 11경로 공통 관문) |
| — | 관찰 | `PATCH /shipments/:id/status` PREPARING 복귀의 **재고 미환원**은 그대로 남았다(화면 호출처 0건). 날짜 두 칸은 P22 에서 같이 지워진다 | **판정 대기** |

## 2026-09-17 출고·배송비 축 (용준님 결정 「나」 → 1~5 진행)

> 전수 조사 결과 **출고 확정 경로는 3개가 아니라 10개**였고, 같은 「출고」인데 경로마다 부작용이 달랐다.
> 결론 = 창구를 하나로 합치지 않고 **「출고 처리」(현장)와 「출고 확정」(청구에 영향을 주는 값)을 두 단계로 나눈다.**

| # | 축 | 무엇 | 판정 |
|---|---|---|---|
| P17 | 결함 | **카드 경로·주문 상태변경 경로가 `billable_after` 를 안 세운다** — 자동 회계반영은 그 값을 조건으로 쓴다(`orders/lifecycle.ts:663`). 즉 `auto_billing=1` 거래처 주문을 인쇄현장이 카드 보드에서 출고하면 **자동 회계반영에서 영영 빠진다**. 화면에 아무 표시도 안 난다 | ✅ 고침(09-17) — 식을 `utils/shipBilling` 한 곳에 두고 카드 3경로·주문 상태변경에 배선. 게이트 `test:ship-billing` 이 **소스를 훑어** 새 출고 경로가 스탬프를 빠뜨리면 실패한다 |
| P18 | 구조 | **배송비는 주문 라인인데 실제 배송은 박스(출고 묶음)에서 일어난다** — 합포장해도 두 주문에 각각 붙어 이중청구. prod 2026 배송비 1,625라인 762만원, 수량 1 이 94%, 하우사인 4,200원×665건(= 택배 1박스 기본요금), `shipments` 0행 | ✅ 고침(09-17, `0621`) — `items.is_shipping_fee`(마스터가 배송비 품목을 안다) + `order_items.fee_source='SHIPMENT_BOX'`(라인 표시). 박스 수를 저장하면 **대표 주문 = 박스 수, 부속 = 0**. 청구된 주문은 제외·멱등. 게이트 `test:shipping-fee` |
| P19 | UI 공백 | **한진 섹션에만 박스 칸이 없었다** — 배송비 라인의 73%가 한진 | ✅ 고침(09-17) — 박스 열 추가, 송장 저장과 동반 저장 |
| P20 | 구조 | 출고 확정 창구 통일 — **API 통합은 불가**(카드 축 = `/shipments` 권한 없는 인쇄현장의 유일한 문 · 주문 상태변경 = 미완료 카드 탈출구 · 배치 = subrequest 한도로 재고 미차감이 의도). 통합하면 깨지는 곳이 문서·주석으로 확인됨 | ✅ 「단계 분리」로(09-17) — 출고 페이지에 **「확정 대기」 섹션** 신설(`GET /api/shipments/pending-confirm`). 출고는 됐는데 박스 수·송장이 안 들어온 건이 여기 모이고, 채우면 배송비·청구그룹이 확정된다 |
| P21 | 경고 공백 | **주문 목록 일괄 출고에는 합배송 파트너 프롬프트가 없었다**(출고 페이지에만 있었다) — 박스가 함께 나갔는데 한쪽만 SHIPPED 되면 배송비가 따로 청구된다 | ✅ 고침(09-17) — `POST /api/shipments/consolidation-pending` 공용 판정 + 주문 목록 일괄 출고 사전 확인(함께 출고 / 2차 확인). **차단이 아니라 경고** — 차단은 8곳에 걸어야 하고 묶음 해제라는 정식 우회로가 남는다 |

- **차단을 안 넣은 근거**: 공통 초크포인트가 없다 — `ensureShipmentForOrder` 는 출고 레코드 직접 생성·스캔이 안 부르고, 호출 시점도 주문 상태를 올린 **뒤**다. 실제로는 8곳에 걸어야 하고 `POST /shipments/unmerge` 가 예약 포인터를 지우는 정식 우회로다.
- **같이 확인된 비대칭(미판정)**: `PATCH /shipments/:id/status` 의 `PREPARING` 복귀가 재고를 안 되돌린다(호출처 0건) · 후가공 자재 차감·출고 이메일·알림톡이 `POST /api/shipments` **한 경로에만** 있는데 그 경로도 호출처 0건 → 지금 자동 출고 알림은 사실상 안 나간다.

## 2026-09-17 단위 축 전수 점검 (0619·0620 배포 뒤, 서브에이전트 감사)

> 셋 다 **오늘 변경이 만든 것이 아니다**(0610·0611 및 그 이전). 셋 다 **과거 손상 0**인데, **병행테스트가 켜는 기능**이 정확히 이 셋이다 — 「지금 멀쩡한 건 그 기능을 안 쓰고 있다는 뜻」의 전형(memory `design-stock-base-unit-rebase`).

| # | 축 | 현상 | prod 실측 | 고치는 방향 | 판정 |
|---|---|---|---|---|---|
| P14 | 발주·입고 (50배) | 발주서·입고 화면의 **「롤」 보조칸이 `pack_size>0` 이면 무조건 열린다**. 그 칸은 현수막 AQ 형(`pack_size`=실사 편의계수, `base_unit` 없음)용인데, 진짜 다단위(`롤`=50`M`)에도 열려 **수량칸이 M 로 채워지고 입고가 다시 ×50** 한다 | 다단위 품목 발주 라인 **643건/195발주**, 그중 **롤칸 사용 0건** → 과거 손상 없음. 병행 구매 트랙이 이 칸을 쓰는 순간 발생 | 보조칸을 `packFactor(item)===1` 일 때만 연다(= AQ 형만). 다단위는 롤수↔수량 1:1 | ✅ 고침(09-17) — `poRollPack()` 가드: 보조칸은 `base_unit` 이 없거나 `unit` 과 같은 AQ 형에서만 연다. 모달 경로는 payload 에 `base_unit` 이 없어 `poFetchPackSize` 로 항상 조회. 다단위는 롤수↔수량 1:1 |
| P15 | 출고 차감 (pack배) | `stockShip.selectShippableLines` 가 `SUM(oi.quantity)` 를 **환산 없이** base 재고에서 뺀다. 유통 롤 판매 라인의 수량은 관리단위라 pack_size 배 **과소 차감** | `reference_type='ORDER'` OUT 원장 **전 기간 0행** → 과거 손상 없음. 2026 유통 다단위 판매 라인 **1,140건/583주문** 이 대기 중 | 라인 `unit_factor` 우선, 없으면 `utils/salesBaseQty` 와 **같은 규칙**(원가 축이 이미 쓰는 판정)으로 base 환산. 환원은 원장값 되짚기라 자동 대칭. 게이트 `test:ship-stock` | ✅ 고침(09-17) — 라인 `unit_factor` 스냅샷(0620)이 있으면 `quantity` 가 이미 base, 없으면 `salesBaseQtySql` 로 환산. 환원은 원장값 되짚기라 자동 대칭. 게이트 `test:ship-stock` 20/20 |
| P16 | 읽기·표기 | 재고 입력 단위 셀렉트·실사 기본 포장수·구역 재고 라벨이 `item_units` 를 안 본다(`inventory.js` 셀렉트 2개 고정 · `inventoryCount.js` 기본값이 `pack_size` 고정인데 서버는 `role_count` 계수 우선 · `storageZones.ts` SELECT 3열 누락으로 base 수량에 관리단위 라벨). 숫자는 맞고 **표기·기본값만** 어긋난다 | 표기 축이라 금액·재고 영향 없음 | 각 SELECT 에 단위 열 추가 + `/api/items/:id/units` 로 셀렉트 채움 | **판정 대기** |

- 같이 나온 **표기 누락**(숫자는 맞음): 작업지시서·출고/포장 라인·세금계산서 품목·고객 포털·견적 목록 펼침·카드 보드 수량·분할청구 자식 라인이 아직 `salesQtyLabel`(「10조(20EA)」)을 안 쓴다. 판매단위를 실제로 쓰기 시작할 때 한 번에.
- **이미 고친 것**(같은 감사, 오늘 변경이 원인이라 즉시 수정 `91c6c646`): 레거시 열 경로의 잉크·AQ 다단위 차단 · 주문 수정 시 판매단위 스냅샷 유실 · 주문 복사 스냅샷 누락 · 이관/그룹/변종 경로의 단위표 동기 누락 · 발주 6경로의 라인 계수 미전달(`backfillPoLineFactors`).

## 2026-09-11 사이클 1 (J1~J4 16단계 통과)

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P1 | API 응답 | 입금 등록에서 「1분 이내 동일한 결제」 거절이 **500 「서버 오류가 발생했습니다」** 로 나갔다(`ar-payments.ts` 가 `DUPLICATE_PAYMENT` 를 안 받아 바깥 catch 로) — 경리는 사유를 못 보고 모니터링엔 장애로 집계 | 같은 거래처·같은 금액으로 1분 내 두 번 입금 | 통장 매칭 경로(`bank.ts`)는 이미 400·문구로 처리 → 같은 형태로 통일 | ✅ 고침(09-11) — J3 에 거절 단계 추가 |
| P2 | UI | 카드를 체크하면 **일괄 바가 둘** 떴다 — 표준 `#cardBulkBar`(ds-bulk-bar, z-35: 상태 선택·일괄 변경·선택 해제)와 레거시 `#bulkBar`(rip.js, z-40: ✓출력완료·⏸보류·해제). 둘 다 고정 하단이라 **레거시가 표준 바를 항상 덮어** 표준 바의 버튼이 눌리지 않았다(처음엔 「상단 버튼이 가려진다」로 오진) | /cards 카드 체크 → 「일괄 변경」 클릭 | 같은 선택에 같은 기능이 두 벌 — 표준(ds) 하나로. 레거시 DOM·`bulkChangeStatus` 제거, 옛 이름은 표준으로 위임 | ✅ 고침(09-11) — J2 가 표준 바로 출력완료를 찍는다 |
| P3 | 규칙 확인 | 진행중(출력대기·출력중) 카드의 개별 버튼은 **RIP 전송·보류**뿐, 출력완료 버튼은 없다 | /cards 진행중 카드 | **설계다** — 출력완료는 출력 이벤트(에이전트 `print_events`)가 찍고, 사람이 손으로 찍는 예외 경로는 체크→일괄 변경(P2 로 살아났다). 코드에 죽은 가지 하나(`buildGridCard` 의 `columnType==='printing'` — 그리드는 완료 칼럼에만 쓴다)는 무해 | ③ 유지(설계). 병행테스트 때 출력실이 「버튼이 없다」고 하면 그때 안내 문구·버튼 결정 |
| P4 | 시각 | `shipped_at` 등 datetime 이 UTC(`CURRENT_TIMESTAMP`)로 저장된다(실측 KST 01:49 → `16:49`) | 출고 후 DB 조회 | **정상** — 저장=UTC·표시=`window.formatKST`(`toKstDate`+`Asia/Seoul`, shell.js:157)가 정본이고 출고 시각 표시가 이 경로다(`orders.js:1050`). 6곳 전부 같은 방식 | ③ 유지(정상). DB 를 직접 읽을 때만 +9h 를 기억 |
| P5 | 운영 | 로그인 API 가 **IP 당 5회/분**(`index.tsx`) — 병행테스트 기간에 같은 공유기 뒤의 직원 여러 명이 아침에 동시에 로그인하면 3~4명째부터 「요청이 너무 많습니다」 | 60초 안에 6번 로그인 | 사무실 NAT 는 한 IP 다 → **계정당 5회/분**(무차별 대입) + **IP 당 30회/분**(계정 스프레이). 포털·비밀번호 변경은 그대로 | ✅ 고침(09-11) — J0 로그인 여정 신설(6번째 429·다른 계정 통과) |

## 2026-09-11 배포 검증에서 추가

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P6 | 운영/보안 | **prod 의 로그인 한도는 간헐적으로만 걸린다** — 같은 계정 12회 연속 틀리기: `401×7 → 429 → 401×2 → 429 → 401`(로컬은 정확히 6번째 429). `middleware/rateLimit.ts` 가 **isolate 메모리** 카운터라 Cloudflare 가 요청을 다른 isolate 로 보내면 각자 0부터 센다 → 실효 한도 ≈ 5 × isolate 수. **기존 IP 방식도 같은 성질**이었으므로 이번 변경(P5)의 회귀는 아니다 | prod 에서 존재하지 않는 계정으로 60초 안에 10번 이상 | 무차별 대입 방어가 「있다」고 믿으면 안 된다. 제대로 하려면 상태를 isolate 밖에 둔다 — Cloudflare **Rate Limiting 바인딩**(wrangler `[[ratelimits]]`, 계정·IP 키) 또는 D1/KV 카운터. 로컬 여정 J0 는 단일 workerd 라 계속 정확히 잡는다 | ③ 보류(09-11) — 「보안강화=실사용 전환 시」로 이미 정한 묶음(`project-full-review-2026-09-03`, #626 평문PW 수용과 같은 축)에 넣는다. 병행테스트 진입을 막지 않는다. 전환 시 Rate Limiting 바인딩 우선 검토 |

## 2026-09-11 사이클 2 (J5 실사·J6 견적→주문 신설 중 발견)

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P7 | UI 결함 | **견적서에서 품목 검색칸에 Enter 를 치면 폼이 제출된다** — 검색 대신 「1개 품목의 단가가 0원입니다. 계속하시겠습니까?」 확인창. 「확인」이면 **반쯤 채운 견적이 저장**된다. 주문서는 Enter=검색(itemRow.js keydown) | /quotation-form 품목칸에 「현수막」 입력 후 Enter | `quotationForm.js` 의 품목칸에 `input` 리스너만 있고 keydown 이 없어 `<form>` 기본 제출. 거래처칸은 Enter=검색이라 영업은 같은 손버릇으로 친다 | ✅ 고침(09-11) — Enter=검색 실행, 주문서와 동일. 회귀 = J6 「견적서 작성·저장」 |
| P9 | UI 부재 | **완전 출고된 주문의 출고 취소에 화면에서 닿을 수 없다** — 주문이 SHIPPED 가 되면 카드 보드가 카드를 숨기고(`exclude_order_status=SHIPPED`), `/cards/:id` 엔 출고 버튼만 있다. 보드 모달의 「출고 취소」는 부분 출고(주문이 아직 SHIPPED 아님) 카드에서만 보인다. `/shipments` 는 `PATCH /:id/status CANCELLED` API 는 있으나 상태 변경 UI 는 확인 못 함 | 카드 출고 → 보드에서 카드 검색 | 환원 로직(`restoreStockLinesOnUnship`·STOCK_RESTORE 로그)은 세 경로(카드 unship·주문 core·출고 취소) 다 있는데 **누르는 곳이 없다**. 병행테스트에서 「잘못 출고했다」가 나오면 첫날 막힌다. J2 는 API 로 환원만 검산 | ✅ 고침(09-15, 용준님 「남은 부분 진행」) — **주문 상세에 「출고 취소」**(ADMIN·MANAGER, SHIPPED 이고 회계반영 전). 서버 `PATCH /api/orders/:id/unship`(`orders/lifecycle.ts`): 재고 환원(`restoreStockLinesOnUnship`) → 카드 `shipped_at` 전부 NULL → 주문 PRINT_DONE(카드 없는 유통 주문은 CONFIRMED) + status_history + activity UNSHIP. 회계반영(BILLED/PAID)이면 400. 보드 필터안은 기각 — 완전 출고된 주문은 카드가 아니라 주문 단위로 되돌리는 게 맞고, 보드는 「할 일」 화면이다. 회귀 = J2 「출고 취소」가 API 폴백 없이 이 화면 경로를 탄다 |
| P10 | UX | 주문서에서 거래처를 안 고르고 저장하면 앱 문구(「거래처를 선택하세요」) 대신 **「선불/착불」 select 의 브라우저 required 말풍선**이 먼저 뜬다(기본 배송방법=대신택배 → 선불/착불 required, 기본값 빈칸). 제출 이벤트 자체가 안 떠서 앱 검증(거래처→납기→…)이 한 번도 안 돈다 | /order-form 에서 품목만 채우고 저장 | 진짜 빠진 건 거래처인데 말풍선은 선불/착불을 가리킨다 — 영업이 헤맨다. 선불/착불 기본값을 두거나(택배=선불), required 대신 앱 검증에 합류 | ✅ 고침(09-15) — **앱 검증에 합류**: `client.js` 가 `required` 대신 `data-needs-payment` 만 남기고, `calc.js` 제출 검증(이미 있던 「선불/착불을 선택하세요」)이 거래처→납기→품목 뒤 같은 자리에서 막는다. 기본값(택배=선불)은 두지 않았다 — 착불 주문을 조용히 선불로 저장하는 사고가 더 비싸다. 회귀 = J1b ① 이 선불/착불을 비운 채 저장해 앱 문구를 본다 |
| P8 | 관찰→결정 | 견적서 품목 검색은 `type=sales` 만 걸어 「현수막」 결과가 **50행**(주문서는 `excludeType=MATERIAL` 로 6행). 세 화면(제작 주문서=체크박스 기본 OFF·유통 주문서=전부·견적서=전부)이 자재를 다르게 다뤘다 | 같은 검색어를 두 화면에서 | **영업은 자재를 유통한다**(용준님 09-14) — prod 6월 이후 라인의 27%·금액 31%가 자재·상품. 「자재를 보여줄까」는 주문마다 정할 일이 아니라 누가 검색하느냐 | ✅ 고침(09-14, **나안**) — 주문서·견적서 같은 토글 「원자재 포함」, 기본 ON, `user-prefs orderform.includeMaterials` 로 계정별 기억(shell.js 공용). 회귀 = J1 「자재 포함 토글」 |
| P11 | 구조 제안 | **유통 주문서(`/order-form?type=dist`)는 한 번도 안 쓰였다** — prod 주문 11,670건 전부 `PRODUCTION`, `DISTRIBUTION` 0건. 제작 주문서가 이미 라인별로 유통 모드를 탄다(`applyDistRowMode`: 규격 텍스트·가로세로 잠금·후가공 숨김) + 부속품 행도 있다. 유통 주문서만의 실효 차이는 **`order_type=DISTRIBUTION` → 카드 미생성·shipment_ready·자재 갭 미계산** 셋뿐 | `/order-form` 상단 「유통(상품) 주문서로 전환」 링크 | 같은 사람이 「이건 제작인가 유통인가」를 화면 고르기로 매번 결정한다 — 결정은 라인에 있다 | ✅ 고침(09-14, 용준님 「①②③ 순서대로」) — **①** 유통 주문서 페이지·`orderFormDist.js` 삭제, `?type=dist` 는 주문서로 302, 주문 목록 「새 주문」 링크 고정 **②** `helpers.deriveOrderType`: 라인 전부 `production_required=0` 이면 DISTRIBUTION(카드 미생성·shipment_ready), 하나라도 제작이면 PRODUCTION, 마스터에 없는 라인=제작. 명시 `order_type` 은 API 호환 **③** `/api/items?with_stock=1` → 자재·상품 행에 「재고(이 법인 합계, base 단위)·최근단가(금액÷수량, 판매일)」 — 검색 모달 열 2개 추가(id 로 묶어 2쿼리, 상관 서브쿼리 없음). 회귀 = **J7**(리다이렉트·자재만 주문=DISTRIBUTION·카드 0·모달 재고 칸·일괄 출고→SHIPPED·OUT 원장·재고 −수량) |

## 2026-09-14 규격 축 질문에서 (「규격 1칸 상품·3축 품목은 어떻게 입력하나」)

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P12 | 구조 결정 + 결함 3 | **3축 품목(아크릴 박스·큐브간판·입간판 `W*D*H`)에 과금축이 없다** — 그리고 ② 주문서 FIXED 제작품 행엔 규격 텍스트 칸이 없고(유통 행에만) ① 견적 라인엔 `specification` 컬럼 자체가 없어 전환에서 소실 ③ 09-04 소급 분해가 3숫자 규격을 두 칸 cm 로 넣어 광학산PC 큐브 2건의 단가가 6,333,333·1,166,667원/㎡(직전가 제안 오염) | prod 2026년 `order_items.specification LIKE '%*%*%'` | 진짜 3축은 **9건/8개월**, 전부 간판 계열·건별 손 단가·7만~48만 → 전개면적/부피 축을 만들면 산식 쌍·픽스처·스냅샷·문서 5곳이 늘고 쓰는 라인이 없다. 「면적 판정=규격 종류 수」로도 3축 품목은 규격이 1회성 → **FIXED + 규격 텍스트 1칸이 정본**. 되돌릴 조건 = 월 5건 이상 반복 + 실제 ㎡×전개면적 규칙 | ✅ 고침(09-15, 용준님 「①②③ 전부」) — **②** 주문서 규격 텍스트 칸을 품목칸 아래로 옮기고 표시 조건을 「유통」→「`pricing_method !== 'AREA'`」(`itemRow.js syncSpecField`), 가로·세로는 참고 입력 유지, 수정화면의 유통 판정은 규격 유무가 아니라 `item_type`(`core.ts`) **①** `0614` `quotation_items.specification` + 견적서 같은 칸·INSERT/SELECT/전환 복사·프리필·견적 문서 표기(specification 우선) **③** `0615` 오분해 5라인 정정(width/height NULL, AREA 2건은 라인 축 FIXED + 단가=금액/수량). 회귀 = J1 「FIXED 제작품 3축 규격」 · J6 「규격 텍스트 왕복」. 설계 = `docs/superpowers/specs/2026-09-14-three-axis-and-single-spec-lines.md` |

## 2026-09-15 J4b 「발주 없이 입고」 신설 중 발견

| # | 축 | 현상 | 재현 | 판단 근거 | 판정 |
|---|---|---|---|---|---|
| P13 | 결함 | **「발주 없이 입고」가 발주만 만들고 입고 모달을 못 연다** — 수량 prompt 뒤 「발주서를 만들었습니다」 직후 「발주 생성 실패: loadPendingPOs is not defined」. 발주(`adhoc_source='RECEIVING'`)는 CONFIRMED 로 남고 입고는 사람이 목록에서 다시 찾아야 한다 | /receiving → 발주 없이 입고 → 공급처·품목·수량 | `receiving.js adhocCreate` 가 #328 에서 지워진 `loadPendingPOs()` 를 부른다(같은 파일 140줄에 「dead … 실경로는 loadReceivingQueue」 주석이 있다). 0611 기능이 붙을 때 옛 이름을 썼고 **어떤 게이트도 안 잡았다** — `check:dom` 은 getElementById 만 본다 | ✅ 고침(09-15) — `loadReceivingQueue()` 로. 회귀 = J4b(모달이 곧바로 열려 입고 확정까지). **게이트 = `npm run check:fn`**(09-15 밤 신설, `scripts/check-fn-refs.cjs`) — `?raw` bare 호출·`on*` 핸들러를 **페이지 번들** 기준으로 대조(TypeScript 파서·typeof 가드 제외·자가시험 7건). 편집 훅·커밋 훅·CI·`ship:gate` 배선. 첫 실측 잔여 2건은 고침: `settings.ts` 가 `storageZones.js` 만 싣고 의존 `zonePicker.js` 는 안 실었다 → 동반 적재 · `/bank` 단독에 허브 함수 `hubGoto` 없음 → `bank.js` 폴백(허브로 이동) |

## 판정 기록
(용준님 판정 후 여기로 옮긴다: 날짜 · 번호 · 결정 · 반영 커밋)

- 2026-09-17 · P14·P15 · ①고침(용준님 「단위표 4단계 다 진행」에 포함) — P14=`purchaseOrderForm.js poRollPack()` 가드(보조칸은 AQ 형만·모달은 `poFetchPackSize` 필수)+`purchaseOrders/core.ts` 가 다단위면 `item_pack_size` 를 NULL 로 내린다. P15=`utils/stockShip.selectShippableLines` 가 라인 `unit_factor` 스냅샷 우선·없으면 `salesBaseQtySql`. 둘 다 과거 손상 0 이었고 **병행테스트가 켜는 기능**이라 선제 수정. 회귀 = `test:ship-stock` 20/20 · `test:item-units`. prod `4f3330c5`~`1e50a2e2`.
- 2026-09-17 · P17~P21 · 위 표 참조(전부 ①고침, prod `4a4b4f3f` → 자동완성 경로 보정 `1e50a2e2`). **미판정으로 남은 것 = 출고 비대칭 2건**(`PATCH /shipments/:id/status` PREPARING 복귀 재고 미환원 · 후가공 자재차감·출고 이메일·알림톡이 호출처 0건인 `POST /api/shipments` 한 경로에만 = **지금 자동 출고 알림이 안 나간다**)와 **P16**(읽기·표기).

- 2026-09-15 · P13 · ①고침 — `receiving.js adhocCreate` `loadPendingPOs`→`loadReceivingQueue`. 회귀 = J4b.
- 2026-09-16 밤 · 관찰(제안 아님) — 전체 사이클 38/40, J6 「견적 저장」·J7 「일괄 출고」가 **GET 500** 으로 실패(`/api/prices?…context=sales`·`/api/notifications/unread-count`). 라우트 변경 없음·직후 curl 200·`--only=j6|j7` 재실행 7/7 → 로컬 D1 잠금 경합(다른 세션 :3000 서버와 같은 로컬 DB)의 일시 오류. prod 영향 없음. 재발하면 러너에 「HTTP 500 1회 재시도」를 넣을지 판정.
- 2026-09-15 밤 · P13 · ②게이트 — `check:fn`(미정의 전역 함수 호출, 페이지 번들 기준). 정규식 프로토타입은 5건 보고 → 5건 전부 오탐이라 파서로 갔다. 전역 집합만 보면 P13 은 잡히지만 「다른 페이지에만 실리는 함수」는 통과하므로 pages 의 `?raw` import 로 번들을 만들어 대조. 잔여 2건(settings↔zonePicker 동반 적재 · bank hubGoto 폴백) 고침 → 0건, 기준선 없음.

- 2026-09-15 · P12 · 결정=새 과금축 없음(FIXED+규격 텍스트) + ①②③ 고침 — `0614`·`0615`, `itemRow.js`·`parent.js`·`quotationForm.js`·`quotations.ts`·`core.ts`·견적 뷰 2곳. 회귀 = J1·J6 각 1단계(36단계).

- 2026-09-11 · P1 · ①고침 — `routes/ledger/ar-payments.ts` DUPLICATE_PAYMENT → 400 「1분 이내 동일한 입금이 이미 등록되었습니다」(bank.ts 와 동일). 회귀 = J3 「같은 입금을 1분 안에 또 넣으면 …」
- 2026-09-11 · P5 · ①고침 — `middleware/rateLimit.ts` 에 `perAccount` 옵션(요청 body 의 username 버킷), `index.tsx` 직원 로그인 = IP 30/분 + 계정 5/분. 회귀 = J0 「한 계정을 5번 틀리면 6번째는 429, 다른 계정은 통과」. 포털 로그인·비밀번호 변경·refresh 는 종전 그대로(외부 IP 분산).
- 2026-09-11 · P2 · ①고침 — `pages/cards.ts` 레거시 `#bulkBar` DOM·CSS 제거, `scripts/cards/rip.js` `bulkChangeStatus` 제거·`updateBulkBar`/`clearSelection` 은 표준 바로 위임. 회귀 = J2 「보드에서 카드를 찾아 출력완료」(표준 바 경로).
- 2026-09-11 · P3 · ③유지(설계) — 출력완료는 출력 이벤트가 찍는다. 병행테스트에서 출력실 피드백이 오면 재검토.
- 2026-09-11 · P4 · ③유지(정상) — UTC 저장 + `formatKST` 표시가 정본.
- 2026-09-14 · P8 · ①고침(나안) — 주문서·견적서 「원자재 포함」 토글 기본 ON + `user-prefs orderform.includeMaterials` 계정별 기억(`shell.js` 공용 초기화), 견적서 검색은 토글 OFF 때만 `exclude_type=MATERIAL`. 회귀 = J1 「자재 포함 토글」(기본 ON·끄면 기억·견적서 동일값).
- 2026-09-14 · P11 · ①②③ 고침 — 유통 주문서 폐지·`order_type` 라인 파생·검색 모달 재고/최근단가. 회귀 = J7. **라인별 규격 1칸(`300*90`) 회귀는 하지 않는다** — 09-04 결정(축=`pricing_method`)·`0601`(자 `3*6` 을 cm 로 읽어 100배) 그대로: 저장은 계산칸 2개(cm)+표기 1칸(`specification`), 입력 편의(`*` 분리 타이핑)는 원하면 별도 제안.
- 2026-09-15 · P9 · ①고침 — `PATCH /api/orders/:id/unship` + 주문 상세 「출고 취소」 버튼(`orders.js unshipOrder`). 회귀 = J2 출고 취소(화면 경로).
- 2026-09-15 · P10 · ①고침(앱 검증 합류) — `client.js` required 제거·`data-needs-payment`, `calc.js` 제출 검증 유지. 회귀 = J1b ①.
