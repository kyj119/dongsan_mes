# A0 패널 — 디자이너 PC 배포 방법

> **복구 문서**: 2026-07-30 09:17 작성분이 실수로 `C:\Program Files\nodejs\npx.ps1` 에 기록돼
> npx 실행을 깨뜨린 것을 발견해, 내용을 이 파일로 옮기고 npx.ps1 은 npm 정본으로 되돌렸다.
>
> **이후 진행분(2026-07-30 배포 완료)**: 축2 로직 = `A0-CEP-0.1.4`(pdfCompatible=false + 임베드
> 여분 계측) · **축3 Z: 갱신 완료**(백업 `_scripts\_backup\20260730-panel\`) · 4축 드리프트 0.
> 따라서 아래 "현재 해야 할 일"·Step 1 은 **이미 끝났고**, 남은 것은 **Step 2(각 PC 설치)** 뿐이다.
> 버전 확인은 `0.1.3` 이 아니라 **`A0-CEP-0.1.4`** 로 본다.

---

디자이너 PC 배포 방법

전제조건 (여기서 대부분 실패합니다)

NAS를 반드시 Z: 드라이브 문자로 매핑 — 경로가 3곳에 하드코딩입니다.

┌────────────────────┬─────────────────────────────────────────────────────────────┐
│        파일        │                          하드코딩                           │
├────────────────────┼─────────────────────────────────────────────────────────────┤
│ jsx/host.jsx:16    │ Z:/DESIGNS/IA-등록/_scripts/mes-a0-host.jsx (로직 정본)     │
├────────────────────┼─────────────────────────────────────────────────────────────┤
│ mes-a0-host.jsx:15 │ Z:/DESIGNS/IA-등록 (등록 산출물 저장 루트)                  │
├────────────────────┼─────────────────────────────────────────────────────────────┤
│ js/main.js:12      │ Z:/DESIGNS/IA-등록/_config/config.json (가공자·거래처 목록) │
└────────────────────┴─────────────────────────────────────────────────────────────┘

이 PC 기준 Z: = \\192.168.0.122\공유폴더. 다른 문자로 매핑하면 패널이 ERROR 정본 없음으로 뜹니다.

구조 — 2단 배포인 이유

축: 축2 로직
대상: mes-a0-host.jsx
런타임 위치: Z:\DESIGNS\IA-등록\_scripts\
갱신 방법: Z: 1개 교체 = 전 PC 즉시 반영. PC 순회 불요
────────────────────────────────────────
축: 축3 껍데기 배포본
대상: index.html·main.js·style.css·manifest·CSInterface·host.jsx
런타임 위치: Z:\...\_scripts\a0-panel\com.mes.a0.panel\
갱신 방법: repo → Z: 복사 (설치 원본)
────────────────────────────────────────
축: 축4 껍데기 설치본
대상: 같은 6파일
런타임 위치: 각 PC %APPDATA%\Adobe\CEP\extensions\com.mes.a0.panel
갱신 방법: PC별 install-a0-panel.ps1

껍데기는 jsx/host.jsx가 Z: 정본을 $.evalFile 하는 스텁이라 로직은 안 들어 있습니다. 그래서 로직 수정은 축2만, 화면 수정은 축3+4가 필요합니다.

현재 해야 할 일 (실측)

- 축1 에이전트 JSX 4/4 일치 — 불요
- 축2 3/3 일치 — mes-a0-host.jsx 이미 Z: 최신(07-29 18:04). 로직은 손댈 것 없음
- 축3 3건 드리프트 — index.html(07-28 13:22)·css/style.css(07-27 18:43)·js/main.js(07-28 13:23)이 구버전 → 갱신 필요
- install-a0-panel.ps1은 Z:와 repo 해시 동일(6D1775CA) — 갱신 불요
- 축4 이 PC 6/6 일치 (테스트 완료 상태)

Step 1 — 축3 Z: 갱신 (관리 PC에서 1회)

⚠️ Z: 교체 순간 전 디자이너 PC의 다음 설치가 신버전을 받습니다. 백업 먼저.

$repo = 'C:\Users\user\dongsan_mes\IllustratorAutomat\designer\poc-a0-cep\com.mes.a0.panel'
$z    = 'Z:\DESIGNS\IA-등록\_scripts\a0-panel'
Copy-Item "$z\com.mes.a0.panel" "$z\com.mes.a0.panel.bak-$(Get-Date -Format yyyyMMdd-HHmmss)" -Recurse
Copy-Item "$repo\*" "$z\com.mes.a0.panel\" -Recurse -Force
cd C:\Users\user\dongsan_mes; npm run audit:ia-jsx    # 축3 6/6 일치 확인

Step 2 — 각 디자이너 PC (PC별 1회, 관리자 권한 불요)

# 1) 일러스트레이터 완전 종료
# 2) PowerShell 에서 (네트워크 경로 스크립트라 정책 우회 필요할 수 있음)
powershell -ExecutionPolicy Bypass -File "Z:\DESIGNS\IA-등록\_scripts\install-a0-panel.ps1"
# 3) 일러 재시작 → 창(Window) > 확장(Extensions) > MES A0 Panel
스크립트가 하는 일: ①껍데기 복사(기존은 .bak-<시각>으로 자동 백업) ②PlayerDebugMode=1(HKCU CSXS 10/11/12 — 미서명 확장 허용) ③Z: 로직 정본 존재 확인 ④host.jsx가 스텁인지 검증(구 로직본이면 경고).

Step 3 — PC별 확인

1. 탭이 단건 / 모아찍기 / 묶음 → 신버전. 가공 / 후가공 / 큐면 구버전이 깔린 것(Step 1 누락)
2. 패널 우상단 버전 = A0-CEP-0.1.3. ERROR ...면 Z: 매핑 문제
3. 가공자 드롭다운에 4인(인호동·김보연·김영주·정소은) → config.json 읽힘 확인 (Z: 09:10 갱신본 존재)
4. 패널 탭을 다른 독으로 드래그 → 네이티브 도킹
5. 객체 선택 → ↻ 실측 → 1건 실등록 → Z:\DESIGNS\IA-등록\<폴더>에 work.ai·EPS·thumb·manifest 생성 → MES 대기함 등장

▎ ⚠️ 우상단 버전은 로직(축2) 버전입니다. 이번엔 껍데기만 바뀌므로 버전 표시로는 갱신 여부를 알 수 없습니다 → 1번(탭 이름)으로 판별하세요.

롤백

- PC 단위: install-a0-panel.ps1 -Uninstall 또는 %APPDATA%\...\com.mes.a0.panel.bak-<시각> 되돌리기
- Z: 단위: Step 1에서 만든 com.mes.a0.panel.bak-<시각> 복원

앞으로 — 무엇을 고치면 어디까지 가야 하나

┌──────────────────────────────────┬───────┬───────┬────────┬──────────────────────────────────┐
│            수정 대상             │ 축2   │ 축3   │  PC    │               추가               │
│                                  │  Z:   │  Z:   │  순회  │                                  │
├──────────────────────────────────┼───────┼───────┼────────┼──────────────────────────────────┤
│ mes-a0-host.jsx (로직)           │ ✅    │ —     │ 불요   │ MESA0_VERSION 상향(:14)          │
├──────────────────────────────────┼───────┼───────┼────────┼──────────────────────────────────┤
│ index.html·main.js·style.css     │ —     │ ✅    │ 필요   │ 탭 이름으로 육안 확인            │
│ (화면)                           │       │       │        │                                  │
├──────────────────────────────────┼───────┼───────┼────────┼──────────────────────────────────┤
│ IllustratorAutomat/*.jsx         │ —     │ —     │ —      │ dotnet build(자동복사) 또는      │
│ (에이전트)                       │       │       │        │ --sync-agent                     │
└──────────────────────────────────┴───────┴───────┴────────┴──────────────────────────────────┘