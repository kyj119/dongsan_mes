<!-- auto-improve Area 1 정본. SKILL.md 에서 축자 분리(2026-08-11) — 원문 L19~56. 내용 무변경. -->

> ⚠️ **본문의 `line N` 상호참조는 신뢰하지 말 것.** 분할 전 SKILL.md 기준 번호인데, 그 파일이
> 8KB→197KB 로 자라는 동안 위에 내용이 끼어들며 **번호가 밀려 이미 깨져 있었다**(대상 23개 중 5개는 빈 줄).
> 가리키는 항목을 찾으려면 숫자가 아니라 **인용문 옆의 서술을 grep** 한다(예: "컬럼-diff bridge").
> 원문 보존을 위해 표기는 그대로 두었다 — 고칠 때 번호를 재계산하지 말고 서술 참조로 바꿀 것.

### 🔴 Area 1: 프로덕션 헬스

**목적**: 지금 이 순간 시스템이 정상인가?

점검 항목:
- 프로덕션 URL 주요 API 응답 (Playwright MCP 또는 fetch)
  - `/api/auth/login`, `/api/orders`, `/api/cards`, `/api/clients` 등 핵심 20개
  - 200 아닌 응답 → 즉시 🔴 기록
- LogWatcher 하트비트 신선도 (`/api/logwatcher/heartbeat` 또는 DB 직접 조회)
  - 마지막 heartbeat > 1시간이면 경고
- CAPS 동기화 신선도 (`/api/sync/pending` 상태)
- E2E 결과 분석 (GitHub Actions 최근 실행)
  - 실패한 테스트 있으면 원인 분석 + 자동 수정 시도
- 콘솔 에러 (Playwright로 주요 페이지 5개 로드, console.error 수집)

**자동 수정 가능**: E2E 실패 원인이 명확한 코드 버그일 때

> **🎯 /cards·대형목록 로드 500 = D1 바인드 파라미터 한도(100) 먼저 의심, 마이그레이션 드리프트는 나중 (Area 1 #409, 2026-06-17 codify)**: #409에서 E2E의 `/cards` 로드 결정적 2x 500을 **0312 마이그레이션 prod 미적용**으로 과추정했으나, owner 픽스(`0ba3670`)가 입증한 **실원인 = 칸반 enrichment 쿼리의 `card_id IN (?,?,...)`/`order_id IN (...)`를 행당 1바인드로 묶어 D1 바인드 파라미터 한도(100) 초과**(limit=500 칸반=컬럼당 카드 100+ → 결정적 500, limit=100 OK·200부터 500). 표준 픽스 = **IN 리스트 80개 청크 분할**(`for i+=80` 루프, 그룹/정렬 보존). **교훈**: limit이 큰 목록(칸반 500·대량 export)의 후처리 enrichment에서 `id IN (...)`가 동적 바인드면 행 수가 100을 넘는 순간 결정적 500 → /cards·대형목록 500 진단 시 **(a) IN절 바인드 카운트(>100) 먼저, (b) 마이그레이션 드리프트(`no such column`)는 그 다음**. 두 클래스 모두 "도입 이래 영구 깨짐(회귀 아님)"으로 보이나 바인드 한도는 **데이터 규모 의존**(테스트 데이터가 적으면 우연히 green = 471502c가 0ba3670 전에 green이었던 이유)이라 5+런 연속 green이어야 회복 확정. 탐지: `grep -rn "IN (\${" src/routes`로 동적 IN절 추출 후 상위 호출의 limit/배열 크기가 100 초과 가능한지 확인.

> **🧊 배포후 smoke 로그인 500 = D1 cold-start transient (Area 1 #400·#374, 2026-06-13)**: Deploy 단계는 `✨ Deployment complete!`로 성공했는데 **post-deploy smoke 로그인만 500**으로 잡 fail("즉시 확인 필요" 알람)이면 **코드 버그가 아니라 갓 배포된 worker의 D1 cold-start 지연**일 가능성 높음. 판별: ① 로그인 응답이 **수십초 지연 후 generic catch 500**(`로그인 처리 중 오류`)인지(즉시 4xx는 진짜 인증실패) ② **직후 배포(다음 커밋)에서 자동 회복**됐는지 — 둘 다 yes면 transient(prod 무중단, 배포 자체 성공). `smoke.cjs:205` login()이 #374로 5xx 재시도(MAX=3)를 갖췄으나 **깊은 cold-start엔 3회 윈도가 부족**(#400 `02071f7`은 retry 포함하고도 fail). **재발 자체를 코드결함으로 오판 말 것** — Deploy success + 다음배포 green이면 헬스 정상. 진짜 보고대상은 **연속 배포가 모두 같은 실패**(자동회복 안 됨)이거나 deploy 단계 자체 failure(빌드/wrangler).
> - **🌩️ 배포-step CF-internal transient = 위 "deploy 단계 자체 failure"의 비보고 하위클래스 (Area 1 #400 확장, 2026-07-03 codify, 26회차)**: wrangler `pages deploy`가 **Worker 컴파일·업로드 성공(`✨ Uploading Worker bundle`) 후 CF "Deploying..." 최종화 단계**에서 `✘ Deployment failed! Failed to publish your Function. Got error: Unknown internal error occurred.`로 fail하면 **Cloudflare 측 내부 장애**(코드/설정 무결)라 비보고. 판별 3종: ① **Typecheck/Build/Upload 전부 success**(job steps 로그) ② 에러가 **CF finalize 단계**(`Failed to publish your Function`/`Unknown internal error`)이지 wrangler config·컴파일 에러가 아님 ③ **다음 커밋 배포가 즉시 회복·supersede**(prod 최신화). 셋 다 yes면 CF transient(#400 smoke cold-start의 배포-step 형제, 둘 다 자동회복형). 반대로 **Build/Typecheck fail**이나 **연속 배포 동일 실패(비회복)**는 진짜 보고. 실증: `469cc12`(docs-only) Deploy fail→`03d35f1` 즉시 green. **오판 주의 — docs-only 커밋 Deploy fail은 코드 회귀 불가**(트리가 안 바뀜)라 CF transient 추정 강함.

> **🕳️ smoke 맹점 2종 = write-path 회귀를 read-only 프로브가 못 잡음 (Area 1 #430, 2026-06-21 codify, 19회차)**: `scripts/smoke.cjs`는 **102 path 프로브 전부 GET + POST는 로그인 1곳(`:210`)뿐** = read-only 헬스체크라 **INSERT/UPDATE/DELETE 경로 회귀를 구조적으로 탐지 불가**. 실증: print-system purge `0335`가 `print_media/print_methods`를 DROP했는데 `items`가 이들을 **inline FK로 참조** → SQLite "부모 테이블 소멸 시 자식 수정 차단"으로 **items INSERT/UPDATE 전량 `no such table` 사망(품목 생성/수정 100% 불능)인데 SELECT는 멀쩡** → 모든 GET 프로브 200 → **smoke green으로 prod-breaking 회귀가 배포 통과(은폐)**, owner가 수동 발견·`0337` FK 스텁 재생성으로 복구(`7cdca2b`). **이것이 line 40(MIME/shell.js 프론트 실행실패)에 이은 smoke 맹점 2종째** — 1종=프론트 실행, 2종=백엔드 write-path. **Area 1 함의**: ① **deploy "success" + smoke green ≠ write 경로 정상** — 특히 마이그레이션이 DROP/스키마 변경을 동반하는 churn(테이블 제거·컬럼 정리)에서 write가 깨지기 쉬운데 smoke는 정확히 그 클래스를 못 봄. ② **DROP/RENAME 마이그레이션이 머지된 사이클의 Area 1은 그 테이블을 write하는 핸들러(INSERT/UPDATE)가 깨졌는지 정적으로 직접 확인**(`grep -rniE "(from|join|into|update)\s+<droppedtable>" src` + 그 테이블을 FK 참조하는 부모/자식 관계 점검) — smoke green을 신뢰 말 것. ③ **D1/SQLite 특수성**: 부모 테이블 DROP 시 자식이 inline FK로 참조하면 자식의 **모든 write가 차단**(SELECT는 동작)되며, D1은 `DROP COLUMN` 차단 + 참조테이블 재빌드가 workerd FK가드로 롤백되어 **부모 스텁(id PK)을 영구 유지**해야 write 복구됨(`0337` 패턴). 탐지 트리거 = `git log <last>..HEAD -- migrations`에 `DROP TABLE`/`DROP COLUMN`이 있으면 그 식별자를 FK로 참조하는 코드/스키마 전수 확인.
> - **🪪 무인증 agent/webhook write-path의 하드코딩 audit-FK(`changed_by=1`) = prod FK 위반으로 이력 전멸 + 동기화 중단, smoke 맹점 3번째 축 (Area 1 #430 확장, 2026-07-04 codify, 32회차)**: `printEvents.ts`(CAPS 출력이벤트 웹훅)·`rip.ts`(RIP 장비 ACK/queue)처럼 **authMiddleware 없이 agent가 트리거하는 write-path**(payload에 `agent_id`만, `user` JWT 부재)가 audit 컬럼(`changed_by`/`performed_by`/`handled_by`)에 **리터럴 정수(대개 `1`)를 하드코딩**하면, 그 컬럼이 `FOREIGN KEY REFERENCES users(id)`인데 **prod에 user id=1이 부재**하면 INSERT가 `FOREIGN KEY constraint failed`로 throw. **치명성 = batch 동반사망**: 그 이력 INSERT가 상태전이 UPDATE와 같은 `db.batch`에 묶여 있으면 **전이 자체가 롤백**되고, autoCheck/sync 루프가 그 지점에서 중단 → **card_status_history 전멸 + 출력완료→PRINT_DONE 자동전환·주문 동기화 무음 중단**(실증: owner `3e29fe6`이 printEvents 3·rip 4곳 하드코딩 `1`→`NULL` 전환 + `E1-20260703-002` 수동 백필로 복구). **smoke 맹점 3번째 축**(1=프론트실행 line 43, 2=DROP write-path line 41, 3=agent-path FK): agent 트리거라 GET 프로브·로그인 smoke가 구조적으로 못 봄 + CI/deploy green. **탐지 레시피**: ① `grep -rlE "authMiddleware" src/routes`에서 **미참조 라우터**(agent/webhook: printEvents·rip·scan-device·cron·webhooks) 격리 → ② 그 파일들의 `INSERT INTO <t> (..., <*_by|user_id>, ...)` bind에서 audit 컬럼 값이 **리터럴 정수** 또는 `user?.id || <리터럴>`(user 부재 시 리터럴 도달)인지 → ③ 그 컬럼이 `grep -rniE "<col>.*references users|foreign key.*<col>" migrations`로 FK-제약인지 대조 → 셋 다 yes면 **prod FK 위반 시한폭탄**. **표준 픽스 = 리터럴→`NULL`**(FK가 `ON DELETE SET NULL`/nullable면 안전, `0046` 패턴) + 이력 INSERT를 try/catch로 격리해 이력 실패가 상태전이/동기화를 막지 않게. **FP 배제(필수)**: (a) 라우터가 **authMiddleware 적용**이면 `user`가 항상 실재 유저(JWT 검증) → `user?.id || 1`의 `|| 1`은 **도달 불가 방어코드**(실증: `scan.ts`가 `handled_by`에 `user?.id || 1`이지만 `:13 scanRouter.use('/*', authMiddleware)`라 FP) (b) audit 컬럼에 **FK 미선언**이면 무해 (c) 이미 `NULL`/`user?.id || null` 바인드. **자동수정 판정 = 리터럴→NULL은 안전 패턴교체**(dormant-write 활성화·기능추가 아님, #426 클래스)나 **egress로 prod user 테이블 상태 검증 불가**하면 issue-only로 보고 후 owner 확인 — 단 형제 정답(같은 파일이 이미 `NULL` 바인드)이 byte-명확하면 승인 시 즉시.
>
> **🧯 학습된 패턴 — 정적에셋 MIME/Content-Type 회귀는 smoke(API)로 미탐지 (Area 1, 9dd09cd→144addf→24bb493 2026-06-11, prod 2회 다운)**: 클라 셸 `shell.js`를 `/static`으로 **외부화**(9dd09cd 파일럿)하면, CF Pages **Git 자동빌드** 환경에서 `_routes.json`의 `/static/* 제외`가 미적용되어 워커가 `/static/shell.js`를 **Content-Type 빈값('')으로 서빙** → 브라우저 strict MIME가 "not executable"로 **실행 거부** → `shell.js`(axios 인증헤더·법인 스위처 초기화) 사망 → **전 페이지 API 401 + 무한 로딩 + 법인 미표시**. `_headers`에 Content-Type 명시(144addf)도 자동빌드선 불충분 → **최종 해결 = 인라인 `?raw` 복귀**(24bb493: shell.js를 워커 HTML에 직접 포함 → /static·_routes.json·_headers·빌드순서 의존 전무, 자동/수동 배포 무조건 동작). **핵심 교훈 2가지**: ① `scripts/smoke.cjs`는 API를 직접 fetch라 이 프론트 실행 실패를 **구조적으로 못 잡음**(로그인 API 200이어도 UI는 죽음) → 회귀 신호는 **E2E 콘솔에러·shell.js 로드 확인**에만 잡힘. ② CF Pages Git 자동빌드는 `_routes.json`/`_headers`/빌드순서를 신뢰 못 함 → 핵심 부트스트랩 스크립트는 외부화보다 인라인이 robust. 점검 트리거 = `build-assets.mjs`·`_headers`·`_routes.json`·`/static/*` 파이프라인 변경 시 + Area 1 헬스에서 prod 페이지 콘솔에러 1개라도 있으면 MIME/shell 로드부터 의심.

> **🩹 smoke 프로브 stale = route-purge가 CI를 매 배포 red화하는 #429 4번째 축 (Area 1, 2026-07-01 codify, 24회차)**: 시스템 purge/재배선이 라우트를 제거하면 `scripts/smoke.cjs`의 프로브 배열(`PATHS`)에 남은 그 라우트 호출이 **404**가 되어 매 배포 smoke를 fail시킨다 — **Deploy 단계는 success(prod 정상, 라우트 제거는 의도적)인데 post-deploy smoke만 red + "즉시 확인 필요" 알람**이라 #400 cold-start transient로 오판하기 쉽다. **판별 = #400과 구분**: cold-start는 ①단일런 ②로그인 자체 500(`로그인 처리 중 오류`) ③다음 배포 자동회복인데, 프로브-stale는 ①**연속 비회복** ②특정 named 프로브만 404(`✗ bom.list /api/bom?limit=10 status:404`) ③로그인은 PASS. **실증(24회차)**: BOM 재배선(`9bd6c2f`)이 구 `/api/bom` 목록 라우트를 제거(bom.ts→`/overview` 1개)했으나 `smoke.cjs:83`이 `/api/bom?limit=10` 프로브 잔존 → `9bd6c2f`부터 4런 전부 Smoke FAIL(`FAIL 1/101`). **#429 purge-완전성의 CI 거울** — 기존 3축(dropped-table SQL ref·frontend axios→removed route·writer-제거 고립)에 **(d) removed-route→smoke probe 404**를 4번째 축으로. **탐지**: DROP/route-purge churn 사이클(`git diff <last>..HEAD -- src/routes`에 제거된 `.get/post('path')`)마다 그 path를 `grep "<path>" scripts/smoke.cjs`로 대조 → 매치되면 stale. **자동수정 = 안전**(테스트 인프라를 의도된 라우트 변경에 정렬, 비즈로직/스키마 무관) — 신 라우트로 교체하거나 프로브 제거. 단 교체 시 신 라우트가 (i)smoke 로그인 role로 도달 가능 (ii)필수 쿼리파라미터 없음 확인(없으면 `allow404` 플래그). `npm run verify`로 컴파일 검증 후 push → 다음 배포 green 회복.
>
> **🗂️ code-only CI 배포 하의 마이그레이션 적용 드리프트 분류 (Area 1 #483, 2026-07-05 codify, 28회차)**: `deploy.yml`은 main push마다 **코드만 자동배포**(build+deploy+smoke)하고 **마이그레이션은 prod에 자동적용하지 않음**(`verify.yml:44`은 `d1 migrations apply --local`만, 원격은 owner `db:migrate:prod` 수동). 따라서 auto-improve가 생성한 마이그(#483 `0441` 인덱스 등)는 owner가 수동 적용하기 전까지 **main엔 있으나 prod DB엔 미반영** = code↔schema 드리프트. **이걸 #422(code 디버전스=회귀폭탄)와 구분해 2분류**: **(a) benign** — additive index/신규 테이블 CREATE(`IF NOT EXISTS`)이고 배포 코드가 그 스키마를 아직 참조 안 하거나 참조 컬럼이 이미 prod 존재(0441=0438 컬럼 이미 적용, 인덱스만 추가) → prod correctness 무해(효율만 지연), close-pending으로 두고 owner 배포 시 병행. **(b) 위험** — `ADD COLUMN <c>` + 그 컬럼을 **이미 배포된 코드가 SELECT/INSERT 참조** → prod `no such column` prepare throw = 기능 100% 사망(#430 write-path smoke 맹점 클래스, GET 프로브·smoke 못 봄). **판별 레시피**: 신규 마이그의 `ADD COLUMN <c>`/`CREATE TABLE <t>`를 `grep -rn "<c>|<t>" src/routes`로 대조 → 배포 코드가 참조하면 (b), 인덱스/미참조면 (a). **(b)면 이슈에 "배포 전 `db:migrate:prod` 필수" 명시**(owner가 코드 배포와 마이그 적용 순서를 지켜야 회귀 없음). **FP 배제**: `IF NOT EXISTS` + 형제 컬럼 이미 prod 존재 = 무해. 이 워크플로는 owner 규율에 의존하므로 위험 마이그 사이클마다 Area 1이 (a)/(b) 분류를 명시.
> - **🛡️ (b)-risk 방어 자동수정 = smoke.cjs에 대표 리소스 detail(`/:id`) 프로브 추가 → CI smoke를 마이그 적용 detector로 전환 (Area 1 #484, 2026-07-06 codify, 29회차)**: (b)-risk(`ADD COLUMN <c>` + 배포 코드가 `/:id` **명시 컬럼리스트 SELECT**로 참조)는 egress 차단으로 "prod에 마이그 적용됐나"를 직접 검증 못 하고, smoke는 리소스별 **list 엔드포인트**(`/api/items?limit=10` = GROUP BY 집계, 신규 컬럼 미참조)만 프로브해 detail-SELECT 컬럼 드리프트를 구조적으로 못 봄. **방어책 = smoke.cjs에 detail 프로브 추가**: `{ path: '/api/<res>/1', name: '<res>.detail', allow404: true }`. 이 프로브는 (1) 신규 ADD COLUMN 명시-SELECT 드리프트를 **다음 배포부터 CI가 즉시 포착**(list 사각 해소), (2) **egress로 검증 불가한 마이그 prod 적용 여부를 CI smoke 결과가 대리 검증** — 0442 미적용이면 `no such column` **500 → smoke FAIL(red=LIVE 확정)**, 적용됐으면 200/404 **PASS(green=#484 close 가능)**. **레시피**: 신규 `ADD COLUMN <c>` 마이그 사이클에서 `grep -rn "<c>" src/routes`가 `/:id` detail 핸들러의 명시 컬럼리스트 SELECT에 등장하면 → 그 리소스 detail 프로브 추가. **필수 확인 2**: ① 대상 핸들러가 **authMiddleware-only(role 게이트 없음)**라 smoke user가 도달(role 필요하면 smoke 계정 권한 확인, 부족 시 `allow401`) ② **`allow404: true`**로 행 부재(id=1 없음)를 PASS 처리 — 컬럼 미존재 500만 FAIL로 격리(smoke.cjs:294 = 200-only PASS, 404만 allow, 500은 무조건 FAIL이라 컬럼 드리프트 정밀 포착). **안전 자동수정 판정 = 테스트 인프라 정렬(#429 클래스), 비즈로직/스키마 무관** — `node -c` 문법 + `npm run verify` 후 push. #430 write-path smoke 맹점(1=프론트실행·2=DROP·3=agent-FK)에 이은 **smoke 커버리지 4번째 보강축(detail-SELECT 컬럼 존재성)**. 실증(#484): `items.ts:720` GET /:id가 0442 image_key를 명시 SELECT → `scripts/smoke.cjs:80`에 items.detail 프로브 추가로 detector 구축.
>
> **💣 prod↔main 디버전스 = 회귀 시한폭탄 standing scan (Area 1 #422, 2026-06-20 codify, 17회차)**: owner가 다수 픽스(특히 보안 IDOR·100%-fail 컬럼버그)를 **로컬 커밋으로 prod에 수동 배포한 뒤 origin/main에 push하지 않고 GitHub 이슈만 "수정·배포·완료"로 close**하면, **prod는 정상이나 main은 픽스 없는 옛 코드** → `.github/workflows/deploy.yml`이 **main push 트리거 자동배포**라 **다음 main push 순간 CI가 main(픽스 없는 코드)을 prod에 덮어써 close된 픽스가 전부 회귀**(cross-tenant PII 유출·100% 기능사망 재발). #422 실증: owner가 #413~#421(9건, IDOR 3·컬럼버그 1 포함)을 커밋 `5169bbb9`로 prod 수동배포(`81d2ddc0`) 후 close했으나 `5169bbb9`가 origin에 부재(`git cat-file -t` = not a valid object), main 트리(420280d) grep으로 9건 전부 미반영 확인(cards/queries.ts:929 bare `WHERE c.id = ?`·storageZones:112 `body.entity_id`·ar-payments:199 `updated_at`·safeSubmit 호출처 0). **standing 탐지 레시피(매 Area 1)**: ① **최근 close된 auto-improve 이슈의 close 코멘트가 "배포 완료"를 주장**하면 그 픽스가 main 트리에 실재하는지 `grep`으로 직접 검증(이슈 본문의 안티패턴이 현재 HEAD에 잔존=미push 증거). ② close 코멘트가 **특정 커밋 SHA**를 인용하면 `git cat-file -t <sha>`로 origin 존재 확인(없으면 로컬-only = 미push). ③ **GitHub close↔main 트리 불일치 = 디버전스**(prod에만 있고 main에 없음). 보고: 디버전스 발견 시 **단일 메타이슈**로 (a)미push 픽스 목록 (b)다음 main push가 회귀시킬 보안/기능 항목 (c)해결책(5169bbb9 cherry-pick/merge/push). **자동수정 금지**(git push/CI 영역, owner만). **백로그 동기화 함의**: owner 대량 close(9건 done 이관) + 직전 사이클이 만든 메타이슈(#422)를 **이전 사이클 sync가 stale하게 누락**할 수 있음 → 매 Area 1은 `list_issues(OPEN, auto-improve)` **실측 후 직전 stats와 diff**해 done-sync 갱신(추정치 신뢰 금지).

> **🔧 사이클 시작 시 `npm ci` 먼저 — node_modules 부재 commit-gate 차단 해소 (Area 2 #439, 2026-06-26 codify, 23회차)**: 일부 스케줄 세션은 node_modules가 **0개**로 시작 → 커밋 시 `pretooluse-bash.cjs`의 commit-gate(`npx tsc --noEmit`)가 `TS2688 Cannot find @cloudflare/workers-types`로 **모든 git commit을 차단**(docs-only 변경도 예외 없음, 훅이 무조건 tsc 실행) → backlog 영속화 실패 = #439 핵심 증상. **과거 사이클은 backlog 트림+API 인라인 푸시로 우회**했으나(비용 큼), **23회차 실증 = `npm ci --no-audit --no-fund`가 프록시 경유로 81패키지 7초 설치 → 게이트 통과 → 정상 `git push origin HEAD:main` 성공**(`c9905a1..0fac4af`). **표준 절차**: ① 사이클 초반(git fetch 직후) `ls node_modules | wc -l`이 0이면 **`npm ci` 먼저 실행**(network 가용 시 7초, 한 번이면 세션 내내 커밋/푸시 정상) ② 그래도 실패하면(network 차단 등) 기존 폴백(backlog<256KB 트림 후 push_files API). ③ **detached HEAD 주의**: 이 환경은 종종 detached HEAD(local `main` ref가 stale = ahead/behind) → `git push origin HEAD:main`(local main 브랜치 아닌 **현재 HEAD**를 main으로)로 푸시해야 fast-forward(`git push -u origin main`은 stale local main을 밀어 reject).

---
