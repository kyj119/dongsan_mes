<!-- auto-improve Area 4 정본. SKILL.md 에서 축자 분리(2026-08-11) — 원문 L162~210. 내용 무변경. -->

> ⚠️ **본문의 `line N` 상호참조는 신뢰하지 말 것.** 분할 전 SKILL.md 기준 번호인데, 그 파일이
> 8KB→197KB 로 자라는 동안 위에 내용이 끼어들며 **번호가 밀려 이미 깨져 있었다**(대상 23개 중 5개는 빈 줄).
> 가리키는 항목을 찾으려면 숫자가 아니라 **인용문 옆의 서술을 grep** 한다(예: "컬럼-diff bridge").
> 원문 보존을 위해 표기는 그대로 두었다 — 고칠 때 번호를 재계산하지 말고 서술 참조로 바꿀 것.

### 🔵 Area 4: 데이터 정합성

**목적**: DB 데이터가 논리적으로 맞는가?

점검 항목 (D1 직접 쿼리):
- 고아 레코드: order_items.order_id가 존재하지 않는 orders 참조
- 상태 불일치: orders.status=SHIPPED인데 cards.status=PRINTING
- 중복 데이터: 같은 client_code, 같은 order_number
- 누락 필수값: delivery_date NULL인 CONFIRMED 주문
- 인덱스 효율: 자주 쿼리되는 컬럼에 인덱스 있는지
- entity_id=0 또는 NULL인 트랜잭션 레코드

**자동 수정 가능**: 인덱스 추가 (마이그레이션), 데이터 정합성 경고

> **🔐 prod D1 FK 강제 = Area 4 탐지 전제 (Area 4 #413/25d1b8e, 2026-06-16 codify, 14회차)**: `25d1b8e`(#394 후속)가 prod에서 **D1가 batch 안에서 FK를 강제**함을 입증 — `inventory_transactions.handled_by`(=users(id) FK)에 `'system'` 문자열 바인딩 → FK 위반 → 승인 100% 500. 이로부터 두 standing scan:
> - **① users(id) FK 비-id 바인딩 sweep**: `PRAGMA foreign_key_list`로 `users`를 참조하는 FK 컬럼 전수 추출 후, 각 INSERT/UPDATE write 사이트의 바인딩이 **`c.get('user')?.id || null`/`user.id`/NULL 정규 패턴**인지 확인. **리터럴 문자열(`'system'`/`'admin'`)·username(`user?.username`)·검증 안 된 `X-User-Id` 헤더**가 FK 컬럼에 닿으면 100% 500. **FP 배제**: 동명 컬럼이라도 그 테이블의 그 컬럼이 `fks.json`에 FK로 안 잡히면 비-FK TEXT(예: `inventory_counts.approved_by/submitted_by`·`price_change_history.changed_by`는 비-FK라 `'system'` 안전). 2026-06-16 14회차 = 69 FK 컬럼 전수 net-new 0(25d1b8e가 유일·픽스됨).
> - **③ 비-FK audit TEXT 컬럼도 sweep 대상 — 죽은/미신뢰 소스 바인딩 = silent 오기록 (Area 4 #436, 2026-06-23 codify, 21회차)**: ①은 FK 컬럼의 **throw(500)**만 잡지만, **비-FK TEXT audit 컬럼**(`submitted_by`/`approved_by`/`changed_by` 등 = `fks.json` 미등록이라 ① FP-배제 대상)이 **프론트가 전혀 안 보내는 헤더나 리터럴 폴백**에 바인딩되면 **throw 없이 영구 잘못된 값**(silent 오기록). 실증 #436: `inventoryCount.ts:199/223`이 `const userId = c.req.header('X-User-Id') || 'system'` → 그 값을 `inventory_counts.submitted_by`/`approved_by`(비-FK TEXT)에 저장. **결정적 3단 증거**: (a) `grep -rni "X-User-Id" src/scripts`=0(프론트 미전송) (b) 백엔드 전체 `X-User-Id` 읽는 곳 2줄뿐(나머지 `c.get('user')`로 마이그됨) (c) **같은 핸들러가 형제 FK 컬럼 `handled_by`는 #394로 `c.get('user')?.id`로 이미 고침** = **절반 마이그레이션 잔재**(FK만 고치고 audit TEXT 누락) → 헤더 항상 undefined → 모든 실사 제출/승인 audit이 `'system'` 고정 + 프론트가 raw 표시(`inventoryCount.js:34`). **탐지 레시피**: ①의 FK sweep에서 **FP로 배제한 비-FK `*_by`/`*_id` audit 컬럼**을 버리지 말고 → 그 바인딩 소스가 (i)`c.req.header('X-...')`(미전송 커스텀 헤더) (ii)리터럴 `'system'`/`'admin'` (iii)그 외 비-JWT 소스면 → `grep -rni "<header>" src/scripts`=0(미전송) + 형제 컬럼이 `c.get('user')`를 쓰는지(불일치=잔재) 확인. **클래스 = #413(FK throw)의 silent 거울** — FK는 500으로 시끄럽게 죽고 비-FK는 조용히 틀린 값을 남김. **자동수정 판정**: 저장 형식(id vs username) 선택이 API응답/UI표시 변경이라 issue-only(#436), 단 형제 패턴(`c.get('user')`)이 명확하면 owner 승인 시 즉시.
> - **④ `getWriteEntityId`류 멀티법인 write-fix의 형제-완전성을 "도메인 문서 생성 INSERT의 entity_id 바인드"로 확장 = entity 오기록 3번째 축(0-sentinel 문서귀속) (Area 4 #487, 2026-07-06 codify, 30회차)**: 전체모드(`getEntityId(c)===0`)의 조용한-동산(1) 유령귀속을 막으려 owner가 `getWriteEntityId`(0→null→400 게이트)를 도입하면, 그 fix는 대개 **재고/집계 write-path**에만 적용되고 **같은 도메인의 헤더/문서 생성 INSERT**(반품·주문·전표 등)는 누락되기 쉽다 → 그 INSERT가 entity_id에 여전히 bare `getEntityId(c)`(전체모드 **0**)를 명시 바인드하면, 컬럼이 `NOT NULL DEFAULT 1`이어도 **0이 그대로 저장**(명시 바인드가 DEFAULT 무시) → `entityFilter`(entity-scoped에 `AND entity_id=N≥1`)로 **특정법인 ADMIN에 영구 비가시**(문서 실종) + 형제(번호·라인) entity와 분열. **실증 #487**: `a6b319f`가 `getWriteEntityId`를 반품 재고처리(승인) 경로(`returns.ts:127 sessionEid`)에만 적용, **반품 생성 헤더**(`returns.ts:51`)는 bare `getEntityId(c)`로 전체모드 entity_id=0 저장 — 같은 핸들러 번호(`:43 ||1`=E1)·라인(`:61 ||1`=1)과 삼중 불일치. **탐지 레시피**: `git log <last>..HEAD`에 `getWriteEntityId`/`ENTITY_ALL_MODE_WRITE_ERROR` 도입 커밋이 있으면 → 그 도메인 테이블의 **모든 생성 INSERT**(`grep -rnE "INSERT INTO <domain>" src/routes`)를 열거 → 각 INSERT의 entity_id 바인드가 (a)`getWriteEntityId` 게이트 (b)`getEntityId(c) || 1` coerce (c)부모(order 등) 파생 중 하나인지 확인 → bare `getEntityId(c)`(→0)면 후보. **결정적 신호 = 같은 핸들러의 번호/라인 바인드는 `|| 1`인데 헤더만 bare**(내부 비대칭). **FP 배제**: (a) 0이 정당한 전역/공용 의미인 테이블(super-admin 소유 공유자원) (b) 이미 게이트/coerce 적용. **자동수정 판정 = `|| 1` 정렬은 안전**(specific-entity는 `N||1=N` 불변, 전체모드만 0→1 교정, 번호·라인·주석 불변식에 맞춤) — 단 근본 게이트(전체모드 생성 400)/주문파생 entity는 UX·정책 변경이라 owner. #413(FK throw 500)·#436(비-FK audit silent 오기록)에 이은 entity 오기록 3번째 축.
> - **⚠️ ①②③ 스캔 regex 필수 — 컬럼명 앞 word-boundary `(?<![a-z0-9_])` (Area 4, 2026-06-23 codify, 21회차)**: CHECK/FK literal-write 스캔(line 148)을 `new RegExp(col+"\\s*=\\s*'...'")`로 짜면 **prefix 컬럼 tail 매칭으로 전량 FP** — `status`로 검색 시 `credit_status='REJECTED'`·`billing_status='BILLED'`·`rip_status='ERROR'`·`pp_status=`·`render_status='queued'`가 전부 매치(21회차 실측 CHECK 스캔 27건 전부 이 FP). **반드시 `(?<![a-z0-9_])`+col 로 leading 경계 강제** 후 재실행(수정 후 net-new 0). line 148의 "(b) prefix 컬럼 tail 매칭" 경고를 regex 레벨로 구체화 — 컬럼명만으로 grep하는 모든 Area 4 스캔(CHECK·FK·컬럼존재성)에 적용.
> - **② cascade "고아" 후보는 ON DELETE 절 확인 후에만 보고**: 서브에이전트가 "부모 DELETE가 자식 X 미정리 → 고아"로 올리면, **먼저 그 FK의 `on_delete` 액션을 `PRAGMA foreign_key_list`로 확인**. **CASCADE/SET NULL이면 prod FK 강제 환경에서 자동 발동 = FP 즉시 드롭**(핸들러 수동정리 불필요). 서브에이전트의 "D1 FK 미강제라 수동정리 필요" 전제는 25d1b8e 입증과 모순 = FP 근원. **진짜 위험은 반대 방향**: `ON DELETE NO ACTION`(기본) 자식이 존재하면 **부모 삭제가 RESTRICT로 차단(500)** → 핸들러가 삭제 batch에서 **DELETE 전에 그 자식을 수동 SET NULL/DELETE 하는지** 확인(purchaseOrders/core.ts:751/753이 NO ACTION인 purchase_adjustments#312·purchase_invoices#324를 선정리하는 정상 패턴). 2026-06-16 = 서브에이전트 cascade 후보 3건(payments SET NULL·pr_comments CASCADE·converted_po_id SET NULL) 전부 ON DELETE 절로 FP 차단.
>
> **✅ CHECK IN 제약 ↔ literal write 불일치 standing scan (Area 4, 2026-06-18 codify, 15회차)**: ground-truth `sqlite_master.sql`에서 `CHECK(col IN ('A','B',...))` 제약을 전수 추출(이 코드베이스 36개: users.role·orders.status·cards.status·auto_process_jobs.status[**소문자** pending|processing|done|approved|failed]·card_transactions.status 등) → 코드의 `INSERT/UPDATE`가 그 컬럼에 **허용집합 밖 literal 값**을 쓰면 constraint throw로 기능 사망(INSERT/UPDATE 컬럼-존재성 스캔이 못 잡는 사각 — 컬럼은 존재하나 값이 위반). **핵심 FP 회피 = 대상 테이블 정확 매칭**: 컬럼명만 grep(`status='X'`)하면 (a)CHECK 없는 동명 컬럼 테이블(inventory_counts·claims·cash_schedule·vat_reports 등)에서 대량 FP, (b)`pp_status`/`rip_status` 같은 prefix 컬럼이 `\bstatus\s*=` regex에 tail 매칭됨. → **반드시 `UPDATE <t> SET ...`/`INSERT INTO <t> (cols) VALUES(...)`로 대상 테이블을 확정한 뒤 그 테이블의 CHECK 집합과만 대조**(INSERT는 컬럼↔값 positional 매핑). **read-only(SELECT/CASE WHEN)는 constraint 위험 0 → write만**. 충돌 위험 높은 테이블 우선 직접 검증: **소문자 enum**(auto_process_jobs) vs 대문자 혼동, **CANCELLED 없는 cards.status**(orders.status엔 있어 복붙 위험). 2026-06-18 15회차 = literal write CHECK 위반 net-new 0. 탐지: CHECK 추출 후 `byTable[t][col]` Set 구성 → write문 대상테이블 묶어 literal 대조.
> - **🔁 INSERT는 positional 매핑으로 별도 스캔 (Area 4, 2026-06-19 codify, 16회차)**: 15회차 CHECK-literal 스캔은 `UPDATE <t> SET col='X'`(col=값 직접 인접)에 강하나 **`INSERT INTO <t> (cols) VALUES (vals)`는 cols·vals 분리 = positional 매핑 필요**(i번째 컬럼 ↔ i번째 값). VALUES를 depth-0 콤마로 split(괄호/배열 보존)해 `cols[i]`가 CHECK 컬럼이면 `vals[i]` 리터럴을 허용집합과 대조. `?` 바인드·표현식은 SKIP(FP 회피), `vals.length!==cols.length`면 동적이라 SKIP. 16회차 = INSERT positional CHECK 위반 net-new 0(신규 `original_archives.status`·`sheet_layouts.status` 포함 38제약). UPDATE-only로는 INSERT 경로 사각.
> - **📐 신규 write-path의 denormalized aggregate 증분(delta) 정합성 standing check (Area 4, 2026-06-19 codify, 16회차)**: 기존 주문/발주에 라인을 **append**하는 부분-write가 비정규 집계 컬럼(`orders.total_amount/vat_amount/final_amount`·`purchase_orders.received_quantity` 등)을 **증분 갱신**(`SET total_amount = COALESCE(total_amount,0) + ?`)하면 두 가지를 검증: ① **delta 공식이 full-recompute와 일치**하는지 — 불변항(discount·동결 청구그룹·BILLED/PAID)이 보존되고 `final += totalDelta+vatDelta`가 `final = total - discount + vat` 재계산과 동치인지(주문 append `create.ts:POST /:id/items`는 discount 불변 + `recalcOrderBillingGroups` 동결그룹 보존 = 정합). ② **증분 중복적용 방어** — 멱등 가드 없이 재시도/더블클릭하면 delta가 2회 더해짐(#420 `po-receive.ts:228 received_quantity 가산` 클래스의 데이터-정합성 거울). 16회차 append(+333줄)는 ①정합·entity 격리·카드번호 충돌회피·status history 모두 clean, ②는 Area 3 #420으로 이미 보고됨. **탐지**: `git diff <last-area4>..HEAD`에서 신규 `UPDATE ... SET <aggcol> = ... + ?` 증분문 → 해당 aggcol의 full-recompute 공식(create 경로)과 대조 + 멱등 가드 유무 확인.
>
> **🔺 신규 feature가 기존 OPEN "인덱스 누락" 이슈 컬럼에 read hot-path 추가 → 중복 이슈 대신 blast-radius 코멘트 + 합배송 cross-entity 배지 FP 클래스 (Area 4 #483 확장, 2026-07-05 codify, 29회차)**: (1) **prod-pending "missing index" 이슈 재확대 처리** — 이미 OPEN인 "인덱스 누락" 이슈(#483 `orders.consolidate_with_order_id`)의 컬럼에 이번 사이클 신규 feature(`c6e1c66` 합배송 UX)가 조회 predicate를 추가하면(목록 배지 `WHERE col IN(...)` = 모든 `GET /orders`, 상세 멤버 `OR col=?` = 모든 주문 상세), **기존 인덱스가 신규 predicate를 커버하는지 재검증**(`idx`가 IN/`=`/OR-optimization 분기 적용) → **커버되면 신규 마이그/이슈 남발 금지, 기존 이슈에 blast-radius 코멘트로 우선순위 갱신**(원 범위 `/daily`→목록·상세 전역). 커버 안 되는 신규 predicate(다른 컬럼 조합)만 새 인덱스 후보. (2) **cross-entity 파생 배지 FP 클래스** — consolidation/합배송 "candidate·묶음" 쿼리는 `queries.ts:46`처럼 `⚠️ entityFilter 의도적 미적용(최소 필드 cross-entity)` 주석이 명시된 **도메인 예외**(같은 거래처 타법인 주문 묶음 판단이 목적) → 그 소스에서 파생된 배지/멤버 쿼리(`core.ts` 목록 배지·상세 멤버)가 `orderVisibilityFilter` 미재적용이어도 **leak로 오플래그 금지**. 판별 = 그 쿼리 소스/형제에 `의도적 미적용` 주석 명시 여부(#365 서빙프록시 예외의 합배송 도메인 버전). (3) **read-path 파생 feature 정합성 렌즈** — 신규 배지/집계가 read-only + try/catch 격리(실패 시 본 응답 무영향)이고, write는 client_id 검증+체인해소, 순수 order 포인터 2단 체인은 `shipmentHelper` merge가 min-id 대표+자식 재지정으로 self-heal(dangling 0)이면 net-new 정합성 버그 0.
>
> **🤖 NOT NULL no-default + 전체 컬럼셋 자동 diff (Area 4 standing scan, 2026-06-15 codify, 13회차)**: ground-truth DB에서 테이블별 ① NOT NULL no-default 컬럼셋(`PRAGMA table_info` `notnull===1 && dflt_value===null && pk!==1`) ② 전체 컬럼셋을 추출 → `src/**/*.{ts,js}`의 `INSERT INTO <t> (cols) VALUES/SELECT` 컬럼리스트를 정규식 파싱해 **`missing`(NOT NULL 누락→constraint throw)·`unknown`(존재X 컬럼→no such column throw)** 자동 격리. 단순 콤마 리스트만(서브쿼리/괄호 포함 컬럼셋·`${동적}` 템플릿은 스킵=FP 회피). #394(missing 3+unknown 4)·#406(unknown 6+severity+오타)·**#408(migration.ts items.unit_price unknown)** 전부 이 스캔이 한 번에 격리. **핵심 사각 — import/migration 핸들러 포함**: `/items/preview`(SELECT만)는 통과하나 `/items/import`(INSERT/UPDATE)만 존재X 컬럼 throw = **미리보기 OK인데 실제 실행만 전량 실패하는 침묵 함정**. preview↔execute 분리 핸들러는 execute 경로의 write 컬럼셋을 별도 검증.
>
> **⚙️ 마이그 파서 자동스캔의 필수 2규칙 — 문장 소스순서 처리 + RENAME TO 추적 (Area 4 #454, 2026-06-28 codify, 25회차)**: 위 컬럼존재성/CHECK/INSERT 자동스캔을 위해 `migrations/*.sql`를 node로 파싱해 ground-truth 스키마를 만들 때 **두 함정이 mass-FP를 유발**(false-clean 아닌 false-alarm 방향): ① **파일 내 문장을 종류별로 일괄 처리하면(모든 CREATE 먼저 → 모든 DROP 나중) 같은 파일의 `DROP TABLE x; CREATE TABLE x (...)` 재빌드가 역전**되어 CREATE가 먼저 적용된 뒤 DROP이 테이블을 통째 지움 → 그 테이블은 이후 `ALTER ADD COLUMN`만 남아 **컬럼 거의 전부 "unknown"으로 오탐**(25회차 실측: inventory_transactions·orders·tasks·quality_issues 등이 entity_id 1컬럼만 남아 **INSERT 스캔 297 가짜 findings**). → **반드시 CREATE/ALTER/DROP/RENAME을 `match.index` 기준 소스 순서로 정렬해 순차 적용.** ② **`ALTER TABLE x_new RENAME TO x` 미추적**이면 재빌드(CREATE x_new → copy → DROP x → RENAME x_new→x) 후 정의가 `x_new`에 갇혀 `x`는 빈 테이블 → 동일 mass-FP(SKILL line 33 "테이블 rebuild" FP 클래스의 파서-레벨 근본원인). → **RENAME 핸들러 추가**(`tables[to]=tables[from]; delete tables[from]`). **sanity check**: 핵심 테이블(orders·cards 등)의 파싱된 컬럼 수가 한 자릿수면 파서가 재빌드를 놓친 신호 → raw findings 보고 금지. 두 규칙 적용 후 25회차 = INSERT 0·SELECT 10(전부 `SELECT 1 FROM`→`.1` + NULL 리터럴 FP)·CHECK 3(전부 cross-statement 윈도 bleed FP: `work_records.status='PAUSED'`/`credit_status='REJECTED'`를 인접 `UPDATE cards`/`UPDATE orders` 블록으로 오귀속) = **net-new 0**.
>
> - **⚙️ 파서 필수 규칙 3·4 — `check_*` 컬럼 CHECK-오판 + `CREATE TABLE IF NOT EXISTS` 덮어쓰기 (Area 4 #469, 2026-07-01 codify, 28회차)**: #454의 파서 2규칙에 이어 mass-FP 유발 2함정 추가(둘 다 false-alarm 방향, ground-truth 재구성 시 실측 격리). ③ **컬럼명이 `check`로 시작**(`check_in_time`/`check_out_time`/`check_item`/`check_type`/`check_result`/`checklist`)하면 CREATE 본문 컬럼 파싱 루프에서 `part.toUpperCase().startsWith('CHECK')`로 **CHECK 테이블제약으로 오판→그 컬럼 통째 누락**→그 컬럼을 INSERT하는 코드가 전부 "unknown" 오탐(28회차 실측: attendance·inspection_template_items·inspection_result_items 등 13건 전부 이 FP). → **CHECK 제약 판정은 반드시 `startsWith('CHECK ')`/`startsWith('CHECK(')`(공백/괄호 경계)** 로. ④ **`CREATE TABLE IF NOT EXISTS x`를 무조건 `tables[x]=parse(...)`로 덮어쓰면**, 같은 식별자가 앞선 마이그의 상세 정의(0054 collection_logs `contact_person` 포함) 위에 뒤 마이그의 축약 재선언(0069 IF NOT EXISTS = 실 DB선 no-op)을 덮어 **원 컬럼 소실→unknown 오탐**. → **`ifne && tables[x] 이미 존재`면 skip**(실 DB의 no-op 시맨틱 복제). 4규칙(소스순서·RENAME·CHECK경계·IFNE-skip) 적용 후 28회차 = INSERT-unknown 13→**0**·missing-notnull 0·CHECK 0. **sanity 유지**: 핵심테이블 컬럼수 두 자릿수 확인(orders51/cards48).
> **🔗 비-FK 참조 컬럼의 부모-삭제 dangling 고아 standing scan (#443/#454 클래스, Area 4, 2026-06-28 codify, 25회차)**: `tax_invoice_id`/`order_id`/`card_id`처럼 **부모를 가리키나 `FOREIGN KEY ... REFERENCES`로 선언 안 된 plain INTEGER 컬럼**은 D1 `ON DELETE CASCADE` 자동정리 대상이 아니라, 부모 DELETE 핸들러가 **수동으로 SET NULL/DELETE 안 하면 dangling 고아**(읽기 쿼리 오염·R2 누수). #443(tax_invoices cancel→payments.tax_invoice_id)·#454(orders 하드삭제→inventory_auto_deductions·pp_material_deductions·original_archives·print_file_map.order_item_id) 2회 누적. **레시피(node)**: ① migrations 파싱으로 **선언 FK 컬럼 집합**(`FOREIGN KEY(col) REFERENCES` + 인라인 `col TYPE ... REFERENCES`) 추출 → ② 각 테이블의 `*_id` 컬럼 중 **FK 미선언분**의 추정 부모(`col` 접미 `_id` 제거 + `s`/`es`/`ies`)가 실재 테이블이면 referer 등록 → ③ `DELETE FROM <parent>` 핸들러 파일에서 그 referer가 **같은 파일 내 `UPDATE <child> SET <col>=NULL` 또는 `DELETE FROM <child>`로 정리되는지** 확인, 안 되면 후보. **결정적 FP 배제 4종(실측)**: (a) **RESTRICT count-가드** = 삭제 전 `SELECT COUNT(*) FROM child WHERE col=? AND is_active=1 > 0`이면 차단(price_lists·storage_zones) → 정리 불요(단 비활성/미점검 형제 테이블은 잔존 가능, 경미). (b) **자식이 부모와 함께 DELETE** = 부모 삭제 batch가 그 자식 행도 DELETE(주문 삭제 시 order_items 등) → 고아 아님. (c) **생산단계 게이팅** = 삭제 경로가 "생산흔적 0" 가드 뒤에만 자식을 지우면 인쇄/가공-기반 referer 테이블은 그 시점에 빈 테이블이라 고아 불가(orders/update.ts 재생성 경로 FP). (d) **rollback DELETE** = 생성 실패 보상삭제는 같은 트랜잭션 신규행이라 외부 referer 없음. **보고 전 2단**: 후보 referer가 실제로 (i)부모 삭제가 도달가능한 status/role에서 (ii)생성돼 있을 수 있는지(생산이력 보유 취소주문 등) + 그 고아가 읽는 쿼리(`grep -rn "FROM <child>"`)를 오염시키는지(costs.ts가 inventory_auto_deductions 읽음 = 삭제주문 차감 잔존). **자동수정 금지**(파괴적 삭제 batch의 mutation 추가 = 비즈니스 동작 변경 + 보존 SET NULL vs DELETE 정책 판단 + egress 검증 불가 → issue-only).
>
> - **⚡ churn-트리거 재스캔 = 신규 `*_id`/`*_into_id` 참조 컬럼이 기존 큐레이션된 삭제 batch 이후 추가되면 그 컬럼만 재스캔 (Area 4 #477, 2026-07-02 codify, 26회차)**: dangling 고아 삭제-정리 배치(orders/core.ts 등)는 한 번 #454로 정성껏 큐레이션되면 "완결"로 보이나, **그 후 마이그가 새 비-FK 참조 컬럼을 ALTER ADD**하면 기존 배치가 그 신규 컬럼을 모르는 채로 통과 = 회귀 없이 도입-즉시 dangling. 실증 #477: 0437이 `shipments.merged_into_id`(합포장 대표 참조, 비-FK) 신설 → `orders/core.ts:591`의 `DELETE FROM shipments WHERE order_id=?`(대표 주문 하드삭제)가 **타 주문(order_id 상이) 자식의 merged_into_id를 정리 안 함** → 삭제된 행 가리키는 dangling. **결정적 = cross-parent 참조**(합포장은 order_id가 다른 복수 주문에 걸쳐 자식이 부모 삭제배치에 안 걸림, FP배제 (b) "자식이 부모와 함께 DELETE"의 반례). 무음 소실 2중: (i) write-redirect(`applyShipmentFieldPatch:753`)가 삭제된 대표로 `UPDATE...WHERE id=<삭제>`=0행인데 `meta.changes` 무시하고 `return true`(:789) → 라벨/송장 편집 무음 소실+success 응답 (ii) 읽기 `LEFT JOIN ... spm.id=merged_into_id`(:162)가 NULL 폴백 → 자식 자신 필드(미기록) 공백. **취소 경로 형제도 확인**: 대표 shipment CANCELLED(`shipments.ts:889`)도 merged_into_id 미정리 → 정본이 취소행. **레시피**: `git diff <last-area4>..HEAD -- migrations`에 `ADD COLUMN <x>_id`/`_into_id`(비-FK)가 있으면 → 그 부모테이블의 `DELETE FROM <parent>`/`status='CANCELLED'` 핸들러가 그 신규 컬럼을 가리키는 외부 자식을 `SET NULL`/재선출하는지 전수. **자동수정 금지**(#454와 동일 issue-only).
>   - **🎯 FP 정밀화 — 모든 신규 비-FK 포인터가 부모-제거 정리 필요는 아님; 판별축 = 읽기/쓰기 경로가 "삭제행으로 리다이렉트"(실버그)냐 "dangling 관용"(FP)이냐 (Area 6, 2026-07-02 codify, 30회차)**: churn-트리거 재스캔이 신규 `*_id` 포인터를 잡으면 **dangling 자체가 곧 버그가 아니라, 그 포인터를 소비하는 경로의 방어 여부가 severity를 결정**한다. **실버그 조건(정리 필수)** = 포인터가 (i) **write/read를 삭제·취소된 행으로 리다이렉트**하고(예 #477 merged_into_id: `applyShipmentFieldPatch:753`이 삭제 대표로 `UPDATE...WHERE id=<삭제>`=0행+`return true` 무음소실, 읽기 JOIN이 status 미필터로 취소행을 정본 사용) (ii) 그 리다이렉트가 status 가드/존재확인 없이 phantom을 신뢰. **양성 FP 조건(정리 불요)** = 포인터 소비 쿼리가 **① `status NOT IN ('CANCELLED','DELETED')` 필터 + ② 역방향(reverse) 조회로 생존 형제만 수집**해 dangling을 자연 관용. 실증 30회차: 0438 `orders.consolidate_with_order_id`(합배송 예약 포인터, 0437 merged_into_id 직후 착륙=동일 churn-bridge 갭)는 부모(root주문) 삭제/취소 시 자식 포인터 미정리로 dangling하나, `applyConsolidationIntents:147-155`가 `WHERE (o.id=rootId OR o.consolidate_with_order_id=rootId) ... AND o.status NOT IN ('CANCELLED','DELETED')`로 forward(삭제/취소 root)는 무매치·필터, reverse(`consolidate_with_order_id=rootId`)로 생존 형제만 병합 → **write 리다이렉트 전무 + 읽기가 dangling 관용 = 정리 불요(clean FP)**. **판정 절차**: 신규 포인터 발견 시 (a) `grep -rn "<col>" src`로 소비처 전수 → (b) 각 소비처가 포인터로 **UPDATE/DELETE 타깃을 정하거나 write를 리다이렉트**하면 실버그축(#477), **SELECT의 JOIN/WHERE 매칭키로만 쓰고 status필터+역방향 조회가 있으면** FP축. merged_into_id(리다이렉트=실버그)와 consolidate_with_order_id(매칭키+관용=FP)의 대비를 reference로.
>   - **🧨 재스캔을 "order_items 통째-교체하는 주문 수정 경로"로 확장 + CASCADE-부모 비대칭 = 삭제 clean ≠ 수정 clean (Area 4 #480, 2026-07-03 codify, 27회차)**: #477 재스캔 레시피는 신규 `*_id` 컬럼의 부모테이블 **DELETE/CANCELLED 핸들러**만 봤으나, `order_items`처럼 **주문 수정(`PUT /orders/:id`, `orders/update.ts`)이 자식을 통째 삭제 후 재작성하는 replace-path**도 별개 RESTRICT-차단 지점 — 하드삭제와 동일하게 `DELETE FROM order_items WHERE order_id=?`(update.ts:216 batch·:237 standalone)를 실행하는데 정리 배치는 core.ts에만 있음. **핵심 = CASCADE-부모 비대칭**: 신규 자식(0439 `shipment_checks`)이 **`shipment_id … ON DELETE CASCADE`(부모 shipments) + `order_item_id … NO ACTION`(부모 order_items)** 이중 FK를 가지면, ① **하드삭제(core.ts)** 는 `:591 DELETE shipments`를 `:599 order_items`보다 먼저 실행 → shipments CASCADE가 shipment_checks를 선제거 → order_items 삭제 안전(FP) ② **주문 수정(update.ts)** 은 shipments를 삭제하지 않아 CASCADE 미발동 → order_item_id NO ACTION이 살아남아 `DELETE order_items`가 **FK 위반 throw → 주문 수정 100% 500**(generic catch, 원인 은폐). 실증 #480: `/pack` checklist(`shipments.ts:466`)가 shipment_checks 생성한 주문을 수정하면 사망. update.ts:187이 `cards.order_item_id`만 NULL 해제하고 shipment_checks 누락 = 형제 미처리. **부차 데이터손상**: 카드 보존 경로(:237 standalone `.run()`)가 throw하면 그 전 커밋된 헤더 UPDATE·`cards.order_item_id=NULL`이 롤백 안 됨 = 카드 부분분리 잔재(order_items 미교체인데 카드 detached). **확장 레시피**: `git diff <last>..HEAD -- migrations`에서 `REFERENCES (order_items|shipments)`(ON DELETE 절 없음) 신규 자식 발견 시 → `grep -rn "DELETE FROM order_items WHERE order_id" src/routes`로 **하드삭제(core.ts)+수정(update.ts) 모두** 열거 → 각 사이트가 그 신규 자식을 order_items 삭제 前 정리(`DELETE`/`SET NULL`)하는지 확인. **FP축(정리 불요) = 대상 부모의 CASCADE로 이미 선제거되는 경로뿐**(core.ts처럼 부모 shipments를 먼저 삭제). **판정 = issue-only**(파괴적 삭제 batch mutation 추가 + 검수 리셋 정책 판단 + egress, #454/#464 선례). **교훈 = "삭제 핸들러 clean"이 "수정/교체 핸들러 clean"을 함의하지 않는다** — replace-path는 부모를 안 지우므로 CASCADE 보호를 못 받는다.
>
> **🧭 Ground-truth 기법 (Area 4)**: 프로덕션 D1 직접 접근 불가 시 → `migrations/*.sql` 전체를 로컬 D1에 적용해 **실제 해석 스키마**(테이블/인덱스/UNIQUE) 확보 후 정적분석과 교차검증.
> 인덱스·UNIQUE 누락 후보는 대부분 오탐(컬럼 존재하나 hot query path 아님 / 이미 복합 인덱스 존재) → ground truth로 반증 필수. (Area 4에서 tax_invoices·shipments 2건 오탐 차단)

> **🔀 status-consistency 제외 픽스의 완전성 sweep (Area 4, 2026-06-22 codify, 20회차)**: owner/에이전트가 "특정 status를 가진 행을 보드/대시보드에서 숨김" 픽스(예: CANCELLED 주문의 카드를 보드/대시보드에서 제외)를 추가하면 **#377 부분픽스 규칙 적용** — 같은 엔터티를 집계/나열하는 **모든 글로벌 표면을 전수 sweep**해 누락 없는지 확인. **핵심 분류(FP 회피)**: `grep -rn "FROM <entity>" src/routes` 후보 중 ① **글로벌 보드/대시보드 집계**(전체 카운트·칸반·요약) = 제외 적용 대상, ② **단일 컨텍스트 쿼리(`WHERE <parent>_id=?`)** = 특정 주문 상세 등 문맥상 그 부모 소속이라 제외 불필요(정상), ③ **작업이력/생산실적 집계**(production/oee의 work_records·quality_items JOIN) = 실제 수행된 작업이라 **사후 취소돼도 집계 포함이 정합**(제외하면 오히려 실적 왜곡). 실증 `f81359c`: 취소주문 카드 HOLD 주차 → 보드 영구잔존 → owner가 `cards/queries.ts`(보드 summary/list/칸반)·`dashboard.ts`(카드카운트 6종)만 제외 → 나머지 card 쿼리는 ②③이라 미수정이 정합, 완전. **+ JOIN 존재성 확인**: status를 부모 JOIN 컬럼(`o.status`)으로 거를 때 그 쿼리에 해당 JOIN(`LEFT JOIN orders o`)이 실재하는지 확인 — 없으면 픽스 자체가 `no such column` 회귀 유입(f81359c는 list `where`의 `o.status`에 대응 JOIN 보유=자기정합). 취소카드가 다른 status(HOLD)로 전이되면 status 필터(`status='PRINTING'`)는 **자연 제외**되므로 그런 쿼리는 후보 아님(aiInsights PRINTING join FP 배제).
>
> **🕒 업무일자 UTC vs KST 탐지 규칙 (Area 4 #366, 2026-06-08)**: SQLite `date('now')`/`datetime('now')`는 **UTC**라, **업무 의미를 갖는 날짜**(처분일·주문일·자동완료일·납기경과·연체판정 등)에 raw로 쓰면 KST 00:00~09:00 입력 건이 하루 전일로 어긋남. 같은 코드베이스가 KST를 알고 있음이 증거 = `hr.ts:801` `Date.now()+9h`·`hr.ts:816` `'now','+9 hours'`(근태) → KST가 의도인데 나머지 미보정 = 불일치.
> - **우선순위**: 저장되는 DATE 컬럼(`disposed_at`/`order_date`/`auto_complete_date`)은 시간정보 없어 **영구 off-by-one**(회계 귀속·매출 집계 직결) > 비교 필터(매 쿼리 재계산되어 9시 이후 정상화, 일시적).
> - 탐지: `grep -rn "date('now')\|datetime('now')" src/routes` 후 각 사용처가 (a)업무일자(비즈니스 의미) 인지 (b)순수 감사 타임스탬프(`created_at`/`updated_at`=UTC 정상)인지 분류. 업무일자는 `'+9 hours'` 보정 필요. **자동수정 금지**(날짜 시맨틱=비즈니스 로직, 사용처 분류 선행, 잘못 보정 시 UTC 감사로그 훼손).

---
