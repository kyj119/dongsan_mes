<!-- auto-improve Area 5 정본. SKILL.md 에서 축자 분리(2026-08-11) — 원문 L211~268. 내용 무변경. -->

> ⚠️ **본문의 `line N` 상호참조는 신뢰하지 말 것.** 분할 전 SKILL.md 기준 번호인데, 그 파일이
> 8KB→197KB 로 자라는 동안 위에 내용이 끼어들며 **번호가 밀려 이미 깨져 있었다**(대상 23개 중 5개는 빈 줄).
> 가리키는 항목을 찾으려면 숫자가 아니라 **인용문 옆의 서술을 grep** 한다(예: "컬럼-diff bridge").
> 원문 보존을 위해 표기는 그대로 두었다 — 고칠 때 번호를 재계산하지 말고 서술 참조로 바꿀 것.

> **🕸️ shallow-clone 앵커 유실 함정 (Area 5, 2026-08-25 codify)**: 이 세션의 컨테이너는 매번 `depth 50` 정도의 shallow clone으로 시작한다 — 백로그가 기록한 직전 사이클 자신의 앵커 커밋(`git log <앵커>..HEAD` 대조용)이 그 depth 밖에 있으면 `git cat-file -t <sha>` 가 실패하고 `git log <앵커>..HEAD`는 `fatal: bad revision`을 던진다. **히스토리 재작성(force-push)이 아니라 단순 fetch depth 부족**이니 당황해서 원격 히스토리 훼손을 의심하지 말 것. 대응: 앵커 조회가 실패하면 `git fetch --unshallow origin main` 으로 전체 이력을 먼저 확보한 뒤 churn diff를 재시도한다. 6개 Area 전부가 매 사이클 같은 앵커-대조 패턴을 쓰므로 이 함정은 어느 Area에서도 재현 가능 — 영역 무관 공통 습관으로 기억.

### 🟣 Area 5: 보안 + 인프라

**목적**: 취약점과 인프라 문제

점검 항목:
- SQL 바인딩 없이 문자열 삽입하는 쿼리 탐지
- XSS: innerHTML 사용처 vs escapeHtml 적용 여부
- 인증 누락: authMiddleware 없는 라우트
- Rate limiting 커버리지 (인증·비밀번호 변경 엔드포인트 우선)
- .env / 시크릿 노출 (git history, 코드 내 하드코딩)
- Cloudflare Workers 설정 (호환성 플래그, 보안 헤더)
- GitHub Actions 보안 (시크릿 접근, 권한 범위)

**필수 grep (Area 5 #338 net-new)**:
- 시크릿 폴백: `grep -rnE "c\.env\.[A-Z_]+ *\|\| *'" src` → 암호화 키/JWT 폴백 리터럴
- 기본 비밀번호: `body.password || '리터럴'`, CI yml `secrets.X || 'admin'`
- 이전 #314가 "하드코딩 없음" 단언으로 놓쳤던 패턴 → 매 Area 5 반복

> **🔓 IDOR 비대칭 탐지 규칙 (Area 5 #349/#356)**: 같은 라우터에서 **목록(list)은 `entityFilter` 적용**하면서 **단건 조회/변경(`GET/PUT/PATCH/DELETE /:id`, submit/approve/cancel)은 `WHERE id = ?`만** 쓰면 = 의도적 전역공유 아니라 **격리 누락 버그**. list가 entityFilter를 쓰는 게 격리 의도의 증거. approve/차감이 호출자 `getEntityId(c)`를 쓰면 정합성 훼손까지. **단, 보고 전 도달성 선검증(#334)** — 호출처 0건이면 dead-code. 6모듈 클러스터(#356)를 이 규칙으로 발견. egress 차단 환경에선 런타임 검증 불가라 자동수정 금지, 이슈 보고.
> - **🧩 대형파일 도메인 분할 보안 회귀 점검 (Area 5 2026-06-12, PO 5분할 f428617~6c89232)**: 라우터를 도메인별 N개 서브파일로 쪼개면(예: `purchaseOrders/core.ts` → po-queries/po-receipts/po-receive/po-special + templates/stock-alerts, 바렐 `purchaseOrders.ts`가 `app.route('/', subRouter)` 집계) 보안 회귀 3가지를 **git 분할 전 커밋과 대조**로 확인: ① **각 서브라우터가 자체 `.use('/*', authMiddleware, requireAnyPagePermission(...))`를 재선언**했는지(Hono 미들웨어는 라우터 스코프 — 한 파일만 빠뜨리면 그 파일 라우트 전체 무인증). ② **마운트 순서 = 구체경로 서브라우터 전부 → core(`/:id`) 마지막**(아니면 core의 `/:id`가 `/stats`·`/templates`·`/receipts`를 섀도잉). ③ **단건/변경 핸들러의 `entityFilter`·`requireRole`이 분할 전과 동일**한지(분할 중 누락). 점검법: `for f in core po-queries ...; do grep -n ".use(" $f; done` + `git show <분할직전>:<원본> | grep "requireRole\|entityFilter"`로 핸들러별 대조. PO 분할은 3항 전부 보존(회귀 0) — 분할 자체는 깨끗했고 노출된 갭(receive requireRole 부재)도 분할 전부터 존재한 의도된 page-permission gating(위 FP 참조). **교훈**: 분할 PR은 "보안 속성 보존" 관점에서 git 대조가 가장 빠른 검증.
> - **변종 — 클라이언트 플래그로 필터 무력화(#368)**: list가 필터를 갖춰도 `?all_entities=1`류 쿼리 파라미터를 **역할 검증 없이** 신뢰해 필터를 끄면 우회(storageZones.ts:13/21, STAFF가 전 법인 열람). 비대칭 규칙은 "list가 필터를 쓴다"가 전제라 이 변종을 놓침 → `grep -rn "c.req.query(" src/routes` 중 entity/필터 분기를 제어하는 파라미터가 ADMIN/`getEntityId===0` 게이팅 없이 동작하는지 점검. (security-audit SKILL에 상세 codify)
> - **🧬 신규 스캔 대상 — 하위자원 append 엔드포인트(`POST /:id/items` 류) write-isolation (Area 5, 2026-06-19 codify, 17회차)**: 기존 신규기능 비대칭 스캔(#417 mass-assignment·#418 read-leak)은 list/단건/생성(`POST /`) 위주였으나, ia-editor가 추가한 **부모자원의 하위 라인을 끼워넣는 sub-resource POST**(`orders/create.ts POST /:id/items`)는 별도 형태의 공격표면. **올바른 패턴(이번 사이클 clean 입증) = (a)부모 조회를 `entityFilter(c,'orders')`로 격리** → (b)자식 INSERT의 entity는 **그 entityFilter-검증된 `order.entity_id`에서 파생**(`billingEntityId = Number(order.entity_id) || ...`)하고 **`body.entity_id`를 절대 신뢰 안 함**(= #417 mass-assignment의 정답 거울). **함정 구분**: body의 `assigned_entity_id`는 격리키가 아니라 **생산 배정(타법인 협업 라우팅)** free-field라 body 신뢰가 정상(create와 동형) — 격리키(entity_id)와 혼동 말 것. **탐지**: churn에서 `POST /:id/<sub>` 하위경로 핸들러 → ① 부모 lookup이 entityFilter인지 ② 자식 INSERT의 entity_id가 부모파생인지(body 아닌지) ③ 새 read 핸들러가 동반됐으면 #418 read-filter 점검. sub-resource write를 신규기능 write-isolation 스캔 목록에 정식 편입.
> - **🏦 외부 금융/연동 API 머지의 단건-write IDOR 형제 완전성 (Area 5 #437, 2026-06-23 codify, 20회차)**: 신규 외부연동(바로빌 등)이 한 테이블의 단건 핸들러 일부에만 entityFilter를 적용하고 **PUT(수정)만 빠뜨리는** #360/#377 부분픽스. 실증 #437: `bank.ts`가 GET목록(:45)·DELETE(:182)·refresh(:225)는 `${ef.clause}` 적용(DELETE/refresh는 바로빌 churn에서 **새로 추가**)하면서 **PUT /accounts/:id(:144/151)만 bare `WHERE id=?`** → entity-scoped ADMIN이 타법인 계좌 master(account_number 등) cross-tenant 덮어쓰기. **결정적 격리-의도 증거 2종**: ① **형제 도메인이 같은 클래스를 이미 픽스** — corporate_cards(cardExpenses.ts) PUT/DELETE/refresh 전부 `entityFilter(c) // #360: 타법인 법인카드 수정 차단`(=#360에서 카드 PUT IDOR 막음). ② **같은 churn에서 DELETE/refresh는 entityFilter 받았는데 PUT만 bare**(강한 누락 신호). **탐지 레시피**: 신규/변경 라우트에서 `grep -nE "FROM <table> WHERE id = \?"` 단건 핸들러 전수 → `${ef.clause}`/`entityFilter` 미적용분 격리 → (a)같은 파일 형제(GET목록/DELETE/refresh)가 entityFilter를 쓰는지(=격리 의도) (b)형제 도메인(corporate_cards↔bank_accounts처럼 평행)이 같은 클래스를 이미 픽스했는지 대조. 둘 중 하나라도 yes면 누락 PUT=버그. **FP 배제**: `entityFilter`가 `getEntityId===0`(super-ADMIN)만 빈절 → entity-scoped는 실제 절 생성이라 IDOR 유효. 단건이 호출자 entity로 자식을 스탬프만 하면(import류) cross-tenant write 영향 낮음(존재 enumeration 정도, minor). **자동수정 금지**(IDOR #349/#356 + 프로젝트 IDOR=owner 픽스 워크플로 #360/#427/#434), 단 형제 패턴 byte-명확하면 승인 시 즉시.
> - **🤖 형제-비대칭 IDOR를 자동 standing scan으로 승격 — A-027(명시-SELECT 존재성)의 IDOR 거울 (Area 6 #452, 2026-06-27 codify, 자기진화)**: #437(PUT만 bare)·#444(콜백만)·#447(read만)·#451(rules write만) **4회 누적 + 기계적 탐지**라 위 수동 레시피(「IDOR 비대칭 탐지 규칙」)를 INSERT/SELECT 컬럼존재성·A-027과 동급 **매 사이클 자동스캔**으로 승격. **churn-한정 수동 감사의 사각 = 코드베이스 전수**: #452 실증 = 자동 스캔이 churn 무관 전수로 **23회 Area 5가 놓친 net-new IDOR 클러스터 5모듈**(clients 포털계정 PATCH/DELETE[타법인 거래처 포털 비번재설정=credential]·accounts-payable purchase-payment PUT/DELETE[금융+채무잔액 조작]·messageTemplates PATCH/DELETE·year-end confirm·ar-dunning collection-log) 격리, **동시에 기존 OPEN #444(workbench:614/948)·#451(priceList:173)을 재포착**(스캔 신뢰도 검증). **스크립트 레시피(node, ephemeral)**: ① `migrations/*.sql` 파싱 → `CREATE TABLE(...entity_id...)` + `ALTER TABLE x ADD COLUMN entity_id` = **entity_id 보유 테이블 집합** ② `\w+Router\.(get|post|put|delete|patch)\(['"\`]path` 로 **핸들러 블록 분할** → 각 블록이 **격리 0**(블록 내 `entityFilter\s*\(`/`\$\{...clause\}`/`\bef\.params\b`/인라인 `AND\s+entity_id\s*=`/`entity_id\s*=\s*\?`/`getEntityId\s*\(` 전무)이면서 ③ `(UPDATE|DELETE FROM)\s+(\w+)...WHERE\s+\w+\s*=\s*\?` 의 `<T>`가 entity_id 보유 테이블 → 후보. **결정적 FP 배제 4종(실측)**: ① **인라인 entity 격리** = `entityId > 0 ? ' AND entity_id = ?'` 삼항(헬퍼 `entityFilter()` 미사용·raw `getEntityId()`+절 직삽, `cardExpenses.ts:968` categories PUT/DELETE) → **반드시 isolated 판정에 인라인 `AND entity_id`/`entity_id = ?`/`getEntityId(` 포함**(미포함 시 cardExpenses류 대량 FP). ② **entity_id 부재 자식 테이블**(work_records·inspection_templates 등)의 bare는 정상(부모 귀속) → entity_id 테이블 집합 교차로 자동 배제. ③ **부모 read-gate가 헬퍼/상위 스코프로 분리** = 블록 밖 격리 → 블록 단위라 못 봄(수동 확인 필요). ④ **employee-self-scope**(attendance/work_records가 `c.get('user')` 본인 한정). **2단 confirmed 게이트(보고 전 필수)**: 후보를 직접 Read해 (a)같은 파일 형제(GET 목록/POST 생성)가 entityFilter/getEntityId 격리하는지(=격리 의도 증거) (b)프론트 axios 도달성(#334) — 둘 다 yes만 confirmed. #452는 28후보 중 5모듈만 직접 Read confirmed, 나머지(production/attendance/hometax/purchaseRequests/aiAnalysis/hr/cards-defects/printEvents)는 **미검증 후보로 명시 분리**(mass-FP 보고 금지, 다음 Area 5 triage). **자동수정 금지**(IDOR=owner 워크플로 + egress 차단). **승격 의의 = "churn 퇴화 사이클의 Area 6은 전수 자동스캔으로 누적 클래스의 코드베이스 잔존분을 격리"** — 수동 감사는 항상 churn-한정이라 도입 이래 잔존한 형제-비대칭은 누락됨.
> - **🪙 FP클래스 ⑤ 확장 — "entity_id 없는 전역 마스터 테이블"의 bare mutate는 정상(그 파일이 entityFilter로 도배돼 있어도) (Area 5, 2026-07-04 codify, 28회차)**: IDOR 자동스캔이 `clients` PUT(`clients.ts:1002`)·soft-DELETE(`:1049`)를 bare `WHERE id=?`로 플래그하나 **`clients` 테이블 자체는 entity_id 컬럼 부재**(`grep -nE "clients" migrations/*.sql | grep entity_id` = **0건**) → 거래처 마스터는 **전 법인 공유 전역 마스터**(테넌시 격리는 마스터가 아니라 **거래 테이블 orders/payments에** 적용). 함정 = **그 파일(clients.ts)이 entityFilter를 대량 사용**해 스캔·육안 모두 "entity-scoped 파일"로 오인하기 쉬우나, 그 `entityFilter(c,'o')`/`(c,'p')`는 전부 **JOIN된 거래 서브쿼리**(`FROM orders WHERE client_id=c.id${ef.clause}`)·형제 자식 테이블(`client_notes`/`client_accounts`는 0264서 entity_id 보유)에 걸린 것이지 `clients` 행에 건 게 아님. `client_accounts`(entity_id 有)의 bare는 진짜 IDOR(#473 open)지만 `clients`(entity_id 無)의 bare는 정상. **결정적 판별 = 플래그된 `<T>`(UPDATE/DELETE 대상 테이블)가 migrations에 entity_id를 실제 보유하는지 직접 grep** — 파일이 entityFilter를 쓴다는 사실만으로 그 파일의 모든 mutate 테이블이 entity-scoped라 추정 금지(entityFilter는 대개 다른(JOIN/자식) 테이블 대상). FP클래스 ②(entity_id 부재 **자식** 테이블=부모귀속)의 **부모/마스터 버전**: 자식은 부모귀속이라 정상, 마스터는 애초 전역이라 정상. **레시피 보강**: #452 스크립트의 `<T> ∈ entity_id-보유 집합` 교차는 이미 이 FP를 자동 배제하나, **육안 triage에서 "clients.ts가 entityFilter 천지인데 PUT만 bare = 비대칭!"으로 과대보고하는 것을 차단** — 반드시 대상 마스터의 entity_id 실재를 migrations로 재확인.
> - **✅ #452 미검증 후보 8종 triage 완료 — net-new 2모듈 confirmed + FP 4클래스 명문화 (Area 5 #455, 2026-06-28 codify, 25회차)**: #452가 "다음 Area 5 triage"로 명시 분리한 미검증 후보(production/attendance/hometax/purchaseRequests/aiAnalysis/hr/cards-defects/printEvents)를 직접 Read 전수 검증 → **confirmed 2모듈(5핸들러)**: ① `purchaseRequests.ts` approve/reject/convert/auto-convert(`:529/:588/:620/:732`, 전부 bare `WHERE id=?`·requireRole ADMIN — **같은 파일 PUT `:437`/DELETE `:917`은 efPut/efDel entityFilter 게이트**라 강한 비대칭, convert는 INSERT INTO purchase_orders에 `getEntityId(c)` 스탬프=타법인 PR을 공격자 PO로 절도) ② `hometaxInvoices.ts:497` `/:id/match`(bare·형제 GET `:361/:126` entityFilter). **FP/의도 배제 4클래스(triage 실증)**: ⓐ **블록내 read-gate 선행** = purchaseRequests PUT/DELETE는 bare UPDATE/DELETE **앞에** `SELECT WHERE id=?${ef.clause}` 404 게이트가 같은 블록에 있어 안전(자동스캔이 후행 bare mutate만 보면 FP — **반드시 mutate 직전 SELECT의 ef.clause 동반 확인**, FP클래스 ③의 블록내 변종). ⓑ **문서화된 에이전트 콜백 의도 예외** = aiAnalysis thumbnail `:207`/patch `:358`은 코드 주석 `"에이전트 전용 콜백 — Bearer 인증으로 충분, entityFilter 미적용(분석 행 entity≠에이전트 토큰 entity일 때 404 방지)"` 명시(#444 클래스의 의도 버전, upload는 자기-생성 행) → 주석이 정책 결정을 명시하면 보고 전 그 trade-off 인용. ⓒ **형제 GET도 미격리(read 격리 자체 부재)** = production work-records/quality-issues PATCH는 형제 GET도 `WHERE 1=1`(entityFilter 0)이라 "list 격리·단건 누락"의 **깨끗한 비대칭이 성립 안 함** + 프론트 호출 0(#334 dead) → 모듈 전반 read 미격리는 별개 사안(배치장 공유 의도 가능성). ⓓ **dead 단건 mutate** = attendance DELETE `:375`는 bare지만 프론트 `axios.delete` 0건(#334 dead). **교훈 = 자동스캔 후보의 confirmed 게이트에 "mutate 직전 같은 블록 SELECT의 ef.clause" + "형제 GET이 실제 격리하는지(WHERE 1=1이면 비대칭 무효)" 2축을 명시 추가** — 이 둘 없이 bare-mutate만 보면 purchaseRequests PUT/hometax 등에서 대량 FP. issue-only(IDOR=owner 워크플로 + egress 차단).
> - **🤝 FP클래스 확장 — "문서화된 의도적 cross-entity mutation 기능" (Area 5, 2026-07-02 codify, 26회차)**: 자동 IDOR 스캔이 `법인 무관`류 주석으로 **cross-entity를 명시 설계**한 기능(합포장 shipments `/merge`·`/unmerge`·write-redirect PATCH `/:id`)의 mutate를 "격리 0"으로 플래그하나 **confirmed 아님**. 판별 2축(둘 중 하나면 의도된 cross-entity): ① **형제 mutate가 동일 미격리** — FP클래스ⓒ("형제 GET 미격리=비대칭 무효")의 **mutate 버전**: `/unmerge`(bare)의 형제 `/merge`도 bare `SELECT ... FROM orders WHERE id IN(...)`(entityFilter 무)라 "list 격리·단건 누락"의 깨끗한 비대칭이 성립 안 함. ② **설계 주석이 cross-entity 정책 명시**(FP클래스ⓑ "문서화된 콜백 예외"의 변종) — `// 같은 날 + 같은 거래처의 복수 주문(법인 무관)을 한 박스로` 처럼 주석이 cross-entity를 정책으로 선언하면 그 trade-off 인용 후 드롭. **write-redirect의 함정**: 합포장은 자식 shipment 편집을 **대표(cross-entity 정본)로 리다이렉트**(`applyShipmentFieldPatch:753`)하는데, 리다이렉트 자체는 cross-entity지만 **진입 mutate가 부모자원 소유를 entityFilter로 먼저 검증하면 안전**(shipments PATCH `/:id`는 write-redirect **전** `entityFilter(c)` SELECT 게이트로 자식 shipment 소유 확인=블록내 read-gate). 즉 "cross-entity 리다이렉트" 자체는 IDOR 아님 — **진입점이 호출자 소유 자원만 받는지**가 관건. 보고 전 확인: (a)형제 mutate도 미격리인가 (b)cross-entity 설계 주석이 있는가 (c)진입 mutate가 부모/자식 소유를 entityFilter로 게이트하는가 — (a)나 (b)면 의도된 기능, (c)까지 있으면 write-redirect도 안전.
> - **🧭 FP클래스 확장 — "client-scoped(entity 아님) 격리키를 쓰는 태생적 cross-entity 기능"의 write path (Area 5, 2026-07-05 codify, 29회차)**: 합배송(consolidation)처럼 **같은 거래처(client)의 타법인 주문을 한 박스로 묶는** 게 기능 본질인 write path는, 자식/대상 조회가 `WHERE id = ?`(entityFilter 무)라 IDOR 스캔에 bare-mutate로 잡히나 **격리키가 entity_id가 아니라 client_id**라 confirmed 아님. **실증 29회차(c6e1c66)**: `orders/create.ts:52`·`update.ts:65`가 `consolidate_with_order_id` 대상 조회 후 **`Number(target.client_id) === Number(orderData.client_id)`로 게이트**(entityFilter 아님) → 같은 거래처 내에서만 묶음 성립 = cross-tenant(타 거래처) 절도 불가. entityFilter를 안 쓴 게 누락이 아니라 **client가 올바른 격리 스코프**(consolidation은 정의상 cross-entity·intra-client). **판별 3축(셋 다 yes면 의도된 client-scoped 기능, 드롭)**: ① 대상 조회가 `WHERE id=?` 뒤 **코드에서 `client_id` 일치를 명시 검증**(entityFilter 대신) ② 기능이 "같은 거래처"를 전제로 설계(주석·배너 문구가 intra-client cross-entity 명시, 예 P2 배너 `합배송은 별도 주문서를 유지한 채 배송만 한 박스`) ③ 파생 read(배지·멤버 목록)가 **최소 필드(id·order_number·billing_status enum)** 만 노출하며 소스 쿼리에 `entityFilter 의도적 미적용(최소 필드 cross-entity)` 주석(`queries.ts:44-47`). **함정**: `orderData.client_id`가 user-supplied라 "공격자가 client를 바꿔 타법인 주문 열거" 우려하나, 묶으려면 그 client로 **실제 주문을 생성/수정**해야 하고 이는 그 거래처 주문 권한 스코프 내 → 오라클 아님. **read 측 안전 확인**: 상세 `GET /:id`가 **자체 visibility 게이트**(`core.ts:462` `ownEntity===viewerEntity || hasAssigned else 404`)를 파생 쿼리 **전에** 통과시키면, 신규 멤버-링크(clickable sibling order)는 order_number 문자열 노출뿐이고 링크 클릭은 대상 order의 게이트로 재차 404 → 최소 필드 노출에 수렴(#365 서빙프록시 예외의 consolidation 버전, Area 4 `queries.ts:46` FP와 동일 클래스의 write-path/security-lens 확장). 이 3축을 「entity_id 없는 전역 마스터」(FP클래스⑤)·「문서화된 cross-entity mutation」과 나란히 IDOR 스캔 FP 카탈로그에 편입.
> - **🔁 부분픽스 재검증을 "같은 사이클 머지된 `fix(security)` 커밋"으로 확장 — closed 우산 이슈뿐 아니라 in-cycle 보안픽스도 형제 완전성 검증 (Area 5 #481, 2026-07-03 codify, 27회차)**: #452/#473은 **closed 우산 이슈**의 서브모듈 픽스 실재를 재grep했으나, **owner가 churn 윈도에서 한 테이블의 IDOR 클러스터를 픽스하는 `fix(security)` 커밋**(여러 핸들러에 `entityFilter`/`// #NNN` 게이트를 동시 추가)도 #377 부분픽스 트랩 대상 — 픽스가 목록·approve·convert 같은 주요 mutate는 격리하면서 **댓글·부가 하위자원(`POST /:id/comments`·append) mutate를 놓치기** 쉽다. **실증 #481**: `7e4b6bf`(전체 코드리뷰 P1/P2)가 `purchase_requests`의 approve/reject/convert/auto-convert/PUT/DELETE 6종에 `// #455` read-back 404 게이트를 붙였으나 **`POST /:id/comments`(`:220`)만 bare `WHERE id=?` 잔존** → entity-scoped ADMIN/MANAGER/DESIGNER가 타법인 PR 스레드에 cross-tenant 댓글 주입(단 렌더가 `escapeHtml`이면 XSS 아님=LOW). **레시피**: `git log <last-area5>..HEAD --oneline`에서 `fix(security)`/`IDOR` 커밋 발견 시 → 그 커밋이 게이트를 추가한 테이블(`git show <sha> | grep -oE "FROM \w+|// #[0-9]+"`)의 **모든** mutate 핸들러를 `grep -nE "\.(post|put|patch|delete)\('/:id" src/routes/<file>` 전수 → 각 핸들러 read-back에 `entityFilter`/`ef.clause`가 있는지 대조 → 없는 형제 = 픽스가 놓친 잔존 IDOR. **FP 배제**: 생성(`POST /` = getEntityId 스탬프)·employee-self scope·entity_id 부재 자식 테이블(부모 귀속)·mutate 직전 같은 블록 상위 read-gate. **severity 조정**: 놓친 형제가 승인/변환/삭제 같은 파괴적/재무 write면 HIGH, 댓글/메모 같은 부가 write면 렌더 escape 여부로 LOW(XSS 무)~MED(XSS 유).
> - **🆕 변종 — 신규기능 머지의 "write 격리 ↔ read 필터 누락" 비대칭 (Area 5 #418, 2026-06-18 codify)**: 새 멀티-entity 기능을 머지하면 개발자가 **INSERT는 `getEntityId(c)`로 격리 저장**(격리 의도 인지)하면서 그 데이터를 **읽는 list/조회 핸들러에 entityFilter를 빠뜨리는** 비대칭이 반복(IDOR 비대칭 #349/#356 "list 필터·단건 누락"의 신규기능 변종 = **write는 격리·read만 누락**). 실증 #418 = `kakao.ts` INSERT 9곳 전부 `entity_id=getEntityId(c)` + `GET /template-defaults`는 entityFilter인데 **`GET /logs`만 entity 필터 0** → MANAGER(구체 entity)가 타법인 수신자 PII(receiver_num/content) cross-tenant 열람. **탐지**: ① 테이블에 entity_id 존재 확인(`grep -rn "<table>" migrations | grep entity_id`) ② 그 테이블에 INSERT하는 코드가 entity_id에 `getEntityId(c)`를 쓰는지(=격리 의도) ③ 그 테이블을 SELECT하는 **모든** 핸들러가 entityFilter/cardEntityFilter를 갖는지 전수 — 한 곳이라도 빠지면 cross-tenant read. **함정 — `requireRole('ADMIN','MANAGER')` 라우터**: "ADMIN-gated니 전체열람 정상"이라 넘기면 안 됨 — **빈 필터(전체조회)는 `getEntityId(c)===0`(super-ADMIN)에만** 부여되고 **MANAGER는 항상 구체 entityId**라 격리 필수. 머지 churn(`git diff --stat <last-area5>..HEAD -- src/routes`)에서 +라인 큰 라우트 우선.
> - **🌐 "전역 dimension" 신규 라우터가 곁다리로 다루는 entity-scoped 테이블까지 전염 무격리 — FP클래스⑤의 반대 방향 실버그 (Area 6 #521, 2026-07-14 codify, 41회차)**: FP클래스⑤(「entity_id 없는 전역 마스터」)는 "**entity_id 없는** 마스터 테이블의 bare mutate는 정상"이었으나, 이번 실증은 그 역 — **신규 라우터 자신이 다루는 주 테이블은 진짜 전역(entity_id 없음, 설계 문서화됨)이지만, 그 라우터가 JOIN/부수적으로 함께 다루는 인접 테이블은 entity-scoped**인데 "이 도메인은 전역"이라는 설계 판단이 인접 테이블까지 잘못 전염돼 격리를 통째로 빠뜨리는 패턴. 실증: `departments.ts`(부문=전역 차원, 설계 문서 명시)가 `GET /employees`(role 게이트조차 없음, list) + `PATCH /employees/:id`(requireRole만 있고 entity 검증 없음)로 **entity_id 보유 sibling 테이블 `employees`**를 다루면서 entityFilter를 전혀 적용 안 함 — 같은 `employees`를 다루는 sibling 라우터 `hr.ts`는 목록·단건 전부 `entityFilter(c,'e')`로 철저히 격리(형제 비교로 격리 의도 명백). **탐지 레시피**: 신규 라우터가 "전역 차원" 주석/설계문서를 가진 테이블(사업장 무관 마스터·부문·카테고리 등)을 다루면서, 그 안에서 `JOIN`/`SELECT`/`UPDATE`하는 **다른** 테이블이 entity_id를 보유하는지(`grep migrations`) 별도 확인 — 라우터의 "주 테이블 전역성"이 부수 테이블에 자동 상속된다고 가정 금지. **강화 신호 = list 엔드포인트에 role 게이트조차 없음**(단순 `authMiddleware`만) — 이 경우 STAFF 포함 임의 인증 사용자가 cross-tenant 데이터를 열람하므로 severity가 IDOR보다 한 단계 위(access-control 부재 자체). **레시피 보강 — 페이지권한 이행 불완전도 함께 확인**: `permission_pages` INSERT 마이그레이션은 있는데 라우터 `.use('/*', ...)`에 `requirePagePermission` 실제 호출이 없으면(DB 등록 ≠ 코드 연결) 메뉴만 숨겨지고 API는 무방비 — CLAUDE.md "신규 페이지→권한 등록" 원칙이 절반만 이행된 신호.

**오탐 제외**:
- `webhooks.ts allowedPrefixes` Popbill IP 목록 → 의도적 보안 화이트리스트, 하드코딩 아님
- dev server 전용 취약점 (vite/esbuild SSRF 등) → 프로덕션 영향 없음, 보고 가치 없음
- CORS `!origin → '*'` (index.tsx:213) → Bearer 인증(쿠키 미사용)이라 실질 무해
- rate limiter in-memory Map (rateLimit.ts:6) → isolate 분산 한계는 기존 인지 아키텍처 제약
- **rate-limit "누락" 보고 (라우트 파일에 inline 미들웨어 없음)** → rate limit은 `index.tsx`에서 `app.use('/api/...', rateLimitMiddleware(...))`로 **앱 레벨 전역 등록**(240-246: auth/portal login·users/portal change-pw·refresh·self-auth·verify-document·verify-token). 라우트 핸들러만 보면 항상 inline 부재로 오탐 → 보고 전 index.tsx 등록처 확인 필수 (Area 5 2026-06-06)
- **"escapeHtml 헬퍼 전무(`grep -c escapeHtml`=0) → XSS"** → `layout.ts:1185`가 `window.escapeHtml`를 **전역 정의**(+`portalLayout.ts` 포털용). 메인 SPA·포털 셸을 통해 로드되는 스크립트는 로컬 정의 없이 전역 헬퍼 호출 가능 → 그 파일에 escapeHtml 미정의/미참조는 취약 증거 **아님**. 올바른 판정: 실제 `innerHTML` 싱크의 보간값이 (a)사용자 제어 free-text **이고** (b)미escape인지 직접 확인. `Number()` 강제 숫자·시스템 채번코드(order_number)·서버 하드코딩 문자열은 싱크 아님 (Area 6 2026-06-06, #335 portalBalance.js 잔여 오탐 차단)
  - **⚠️ 예외 — 독립 HTML 페이지는 전역 escapeHtml 없음 (Area 5 2026-06-10, 9회차)**: `c.html(\`...\`)`로 자체 `<head>/<script>`를 통째 반환하는 **독립 출력 페이지**(`pages/payslip.ts`·`pages/yearEnd.ts` 급여명세서/연말정산, `/payslip/:id`·`/year-end/:id` 인쇄 경로)는 layout.ts 셸을 거치지 않아 **`window.escapeHtml`가 정의돼 있지 않음**. 여기서 직원 마스터 free-text(성명/부서/직책/연락처)를 `innerHTML`에 raw 연결하면 **진짜 stored XSS**(HR ADMIN/MANAGER가 마스터에 페이로드 저장 → 인쇄 시 실행). 위 "전역 헬퍼 있으니 오탐" 논리를 **독립 페이지에 적용 금지**. 판별: 파일이 `import ... layout`/shell을 쓰지 않고 `c.html` 안에 자체 script를 넣으면 전역 헬퍼 부재 → 로컬 `esc()` 추가가 정답(escapeHtml 누락 = 안전 자동수정 범주). 탐지: `grep -rln "c.html(" src/pages` 후 각 파일이 free-text를 innerHTML/문자열연결로 렌더하는지.
- **쓰기 핸들러 `requireRole` 부재를 "권한 누락 HIGH"로 과대평가 — page-permission gating이 곧 접근제어** (Area 5 2026-06-12, 10회차) → 이 프로젝트의 데이터 API는 라우터-와이드 `requireAnyPagePermission('/purchase-orders','/receiving')`(`middleware/permissions.ts:66`)로 게이트됨 = **role별 `getAccessiblePages` 조회 후 미보유 시 403**인 실제 RBAC. 따라서 특정 쓰기 핸들러(예: `POST /:id/receive`, po-receive.ts:20)에 `requireRole('ADMIN','MANAGER')` inline이 없어도 **STAFF가 임의 실행** 아님 — 해당 role이 `/receiving` 페이지 권한을 부여받았을 때만 도달. **입고/검수 같은 워크플로는 ADMIN/MANAGER가 아니라 창고담당(=`/receiving` 권한)이 수행하는 게 의도**라 requireRole 미적용이 정상(`core.ts:17` "쓰기는 각 엔드포인트 requireRole" 일반론의 **의도된 예외**). 보고 전 확인: ① 라우터 `.use`에 `requireAnyPagePermission`/`requirePagePermission`이 있는지 ② 그 미들웨어가 `getAccessiblePages`로 실제 403하는지(no-op 아닌지) ③ 해당 동작이 비-ADMIN role의 정당 업무인지. 셋 다 yes면 드롭(서브에이전트가 "writes role-gated" 주석 일반론만 보고 receive 예외를 HIGH로 과대보고한 사례 차단).
- **무인증 self-service auth 엔드포인트 "브루트포스/열거 HIGH" 과대평가** (Area 5 2026-06-09, 8회차) → `/api/hr/self-auth`(사원번호+생년월일6자리)·portal `/verify-document`(토큰+BRN)처럼 **계정 없는 사용자용 간이 2팩터 인증**은 authMiddleware가 없는 게 **설계 의도**(공개 진입점). 보고 전 ① `index.tsx:240-246` rate limit 전역 등록 확인(self-auth=5/분·verify-document=10/분 이미 적용) ② 두 팩터 결합(열거 가능 식별자 + 추측가능 비밀) 형태가 동일 코드베이스의 **이미 "설계 정상" 판정된 패턴**(verify-document)과 동형인지 확인. rate-limit-by-IP의 IP로테이션 한계는 **모든 로그인 엔드포인트 공통 + 기존 인지 아키텍처 제약**(rateLimit.ts in-memory Map)이라 self-auth 단독 HIGH 아님. timing-attack도 두 분기(emp 없음 vs birth 불일치) 모두 단일쿼리+문자열비교라 유의미 차이 없음. **진짜 보고 대상**: rate limit 자체가 누락됐거나(index.tsx 미등록), 한 팩터만으로(식별자 없이) 인증되거나, scope/만료 없는 영구 토큰을 발급하는 경우.

> **📤 CSV Formula Injection 탐지 (Area 5 #367, 2026-06-08)**: CSV export 헬퍼가 셀 값의 **선행 `=` `+` `-` `@`(탭/CR)**를 이스케이프하지 않으면 자유입력(거래처명·품목명·메모 등)이 다운로드 PC Excel에서 수식 실행(HYPERLINK 유출/DDE). `,"` 개행만 따옴표 처리하는 건 **부족**. 점검: `grep -rn "includes(','" src` 후 각 CSV 헬퍼가 선행 특수문자 가드하는지. **이 코드베이스는 4개 구현 산재**(csv.ts generateCsv/escapeCsvField·tax-agent csvField·shipments 인라인 esc) — 하나만 고치면 우회. 가드 추가 시 **금융 음수금액(`-1000`)이 텍스트로 깨지지 않게 숫자-안전**(`typeof val!=='number' && /^[=+\-@\t\r]/.test(str) && isNaN(Number(str))`) 필수.

> **🩹 SPA `innerHTML` free-text stored XSS = 고생산성 standing scan (Area 5/6 codify, 2026-06-14)**: `src/scripts/**/*.js`가 DB free-text(거래처명·품목명·직원명·비고/사유·부서·후가공명 등)를 `escapeHtml()` 없이 `innerHTML`/`insertAdjacentHTML`/`.html()`에 보간하면 stored XSS. **누적 실측 4사이클 = A-020(leaves.js)·A-021(activityLog/insuranceReports)·#399(quotation.js)·A-022(purchaseRequestForm 3 + postProcessing 5)** → "존재X 컬럼"급 반복 클래스라 **매 Area 5 standing scan**. **핵심 특징(오버사이트 패턴)**: 거의 항상 **같은 파일이 일부는 escape하고 일부만 누락** — postProcessing.js는 `:521~624`에서 escapeHtml 쓰면서 `:36/:63/:67` 테이블/체크박스 누락, leaves.js는 드롭다운만 escape·표 행 누락, quotation.js는 `escapeHtml(err.message)` 쓰고 본문 누락. 또 **onclick/onmousedown 속성용 `.replace(/'/g,...)` escape는 있는데 HTML 텍스트 노드용 escapeHtml은 누락**한 경우 多(purchaseRequestForm `:151/:193` attr escape O, `:154/:195` 텍스트 escape X). **빠른 탐지**: `grep -rn "innerHTML\|insertAdjacentHTML\|\.html(" src/scripts` 후 각 싱크의 보간값이 (a)DB free-text **이고** (b)escapeHtml 미래핑인지 직접 Read. **FP 배제**(보고/수정 금지): `Number()`·숫자(qty/price)·날짜(`fmt()`)·시스템 채번코드(order_number/employee_code, ⚠️ 단 item_code/option_code 등 사용자편집 마스터코드는 싱크)·상태배지/enum 라벨 함수·이미 escape된 것. **자동수정 판정**: `window.escapeHtml`(shell.js:62 전역, 셸 경유 스크립트 전부 가용)로 누락 필드 래핑 = **안전 자동수정**(escapeHtml 추가는 동작 무변·SKILL 허용 범주) — **단 단순 테이블/드롭다운/모달 렌더에 한함**. **예외: ~18필드+img-src 섞인 복합 출력 문서 렌더러(quotation.js #399)는 owner 검증(issue-only)** — 렌더 회귀 검증 불가. (독립 HTML 페이지 c.html `pages/payslip/yearEnd`는 별도 — 전역 escapeHtml 부재라 로컬 `esc()` 추가, 위 XSS FP-row 예외 참조.)

> **🕳️ XSS sweep 사각 3종 (Area 5/6 A-024, 2026-06-15 codify) — "106파일 전수" 자칭이 5사이클 후에도 14 sink 누락**: 페이지별 sweep가 반복적으로 놓치는 3패턴을 매 sweep 명시 점검: ① **escapeHtml 0회 사용 파일**(`grep -c escapeHtml <file>`=0 → "이 파일은 escape 컨벤션 자체가 없음" 신호, innerHTML 싱크 전수 의심 — items/modals.js가 0회인데 item_name 4 sink). ② **`<option value="X">텍스트</option>` 드롭다운** — value **속성**도 미escape면 `"` 속성탈출 가능(텍스트만 보지 말 것 — orderForm/finishing.js m.name이 value+텍스트 양쪽). ③ **전역/공통 컴포넌트**(`layout/shell.js` 사이드바 품목검색 등) — 페이지 단위 sweep가 셸 컴포넌트를 건너뜀(전 페이지 노출이라 노출면 최대). 또 **마스터 자유입력 필드 확대**: 장비명/프린터명/구역명(equipment)·창고구역명/담당자명(storage_zones)·후가공방식명·프리셋명·item_group도 싱크(거래처명/품목명/직원명 외 추가). **agent JSON 유입 주의**: heartbeat 자동등록처럼 외부 agent payload가 마스터 컬럼(equipment.name)으로 들어가면 출력이력 등으로 2차 전파.
> **🔁 같은 파일 "부분 escape 형제 데이터소스" (Area 5 A-025, 2026-06-16 codify) — 이미 픽스한 파일이 6사이클 연속 형제 sink 노출**: A-024(finishing.js `:32 m.name` 마감방식)→A-025(같은 파일 후가공 `option_name`/`option_code` `:178/:204/:321`+data-pp-* 속성) = **파일에 escapeHtml이 N회 있어도(컨벤션 확립) 다른 쿼리/루프(finishOpts vs methods)는 별도 누락 가능**. **"이 파일은 픽스됨" 파일 단위 판정 금지** — `grep -c escapeHtml`>0이어도 안심 말고 **각 innerHTML 보간 데이터소스(다른 axios.get 결과·다른 forEach 루프)별로 sink 전수**. 빠른 탐지: 파일 내 `innerHTML`/`html +=` 보간 변수를 데이터소스(어느 API/배열에서 왔나)별로 그룹핑 → 그룹마다 escapeHtml 적용률 확인(한 그룹만 0%면 누락). `<option>` value+텍스트+data-* 속성 전부 sink(escapeHtml은 dataset round-trip 안전).
> **🧷 prefix-scoped escape 래퍼(`sgpEsc`/`iaeEscape`)의 `typeof===function` fallback = 셸 경유 페이지에서만 안전 (Area 5, 2026-06-21 codify, 18회차)**: 신규 self-service 페이지가 `?raw` 전역 스코프 충돌 회피용 자체 prefix 래퍼 `function sgpEsc(s){ return (typeof escapeHtml==='function') ? escapeHtml(s==null?'':String(s)) : String(s); }`를 도입하면 sink 전수 적용 시 clean(specGroups.js +391 = innerHTML sink 전수 sgpEsc 일관, checkbox `value=` 속성 포함, net-new 0). **단 핵심 함정 = fallback이 raw `String(s)`**라 전역 `escapeHtml` 부재 컨텍스트에선 **escape가 silent disable**. SPA 셸 경유 페이지(specGroups 포함)는 shell.js가 `window.escapeHtml` 전역 정의 → fallback 미발동=안전. **그러나 독립 `c.html` 페이지(payslip/yearEnd, 「독립 HTML 페이지 escapeHtml 부재 예외」)에서 같은 prefix 래퍼를 쓰면 전역 헬퍼 부재로 fallback 발동→무방비 XSS**. **레시피**: 신규 prefix-escape 래퍼(`grep -rn "function \w*[Ee]sc(" src/scripts`) 발견 시 (a)각 innerHTML sink 적용률 + (b)그 페이지 로드 경로(셸 SPA vs 독립 c.html)를 함께 확인 — 독립 페이지면 fallback이 raw가 아니라 로컬 실제 escape여야 안전. 신규 관리 페이지가 처음부터 prefix 래퍼+전수 적용이면 clean이나 **검증 자체는 Area 5/6 standing meta-check 필수**(A-024/A-025 부분누락 클래스).
> **🪆 agent-payload JSON 배열 → innerHTML = stored-XSS sink 형태 확장 (Area 5, 2026-06-22 codify, 19회차)**: 위 「agent JSON 유입 주의」의 구체 확인 — 외부 agent(logwatcher)가 보낸 **파일명 배열**이 백엔드에서 `JSON.stringify(nest_members)`로 컬럼 저장(`printEvents.ts`, `?` 바인드라 SQLi 0)된 뒤, 프론트가 `JSON.parse` → `.map()`으로 `<li>` 렌더(`production.js:311`)하는 **JSON-배열 sink**는 기존 "단일 free-text 컬럼" sink와 다른 형태이나 같은 클래스. **올바른 패턴(이번 사이클 clean 입증)**: ① 배열 원소를 **텍스트·title 속성 양쪽** escape(`'<li title="'+escapeHtml(String(m))+'">'+escapeHtml(base)+'</li>'`) ② 동반 `file_name`은 **변수 정의 지점에서 `escapeHtml(ev.file_name)`** 1회 escape 후 모든 렌더에서 그 변수 재사용(`production.js:268` `var fileName = escapeHtml(...)` → :318/:322 재사용 = 정의-지점 escape 전파 패턴, 매 사용처 중복 escape 불필요). **탐지**: agent/외부소스가 채우는 컬럼(file_name·nest_members·equipment.name·heartbeat payload)이 `JSON.parse(...).map(`/`forEach(`로 innerHTML 보간되면 (a)배열 원소 텍스트 (b)그 원소를 쓰는 title/data-* 속성 (c)정의-지점 단일 escape의 전파 완전성 3가지 확인. **FP 배제**: 정의 지점에서 escape된 변수는 하위 렌더 재사용이 안전(중복 escape "필요"로 오보 금지 — `&`가 `&amp;amp;`로 이중인코딩되지 않게 정의-지점 1회가 정답).

> **🤖 SPA innerHTML free-text XSS를 자동 standing scan으로 승격 — #452(IDOR)의 XSS 거울 (Area 6, 2026-06-28 codify, 자기진화)**: A-020~A-025·#399·#431로 **6+ 사이클 누적**된 innerHTML free-text sink 누락을 수동 sweep으로만 잡아왔으나 A-024가 "106파일 전수 자칭이 5사이클 후에도 14 sink 누락"을 명시 = 수동의 구조적 한계 → INSERT/SELECT 컬럼존재성·형제-비대칭 IDOR(#452)와 동급 **매 사이클 node 자동스캔**으로 승격. **스크립트 레시피(ephemeral)**: ① `grep -rl innerHTML src/scripts --include=*.js`의 각 파일에서 `${...}` 보간을 가진 HTML-빌드 라인 추출 ② 보간식이 **free-text 속성명**(`name|title|memo|note|reason|desc|description|content|address|label|client_name|item_name|employee_name|department|position|spec|category|method|preset|subject|message|file_name|nest_members|manufacturer|location`) 매치 ③ AND escape 래퍼(`/[A-Za-z_]*[Ee]sc[A-Za-z]*\s*\(/`, 파일별 가변 — escapeHtml·esc·escH·wbEscape·iaeEscape·lvEscapeHtml·sgpEsc 전부 커버) 미적용 ④ AND 숫자/날짜 SAFE(`Number|parseInt|fmt|toLocaleString|toFixed`) 아님 → 후보. **결정적 FP 4클래스(28→14 후보 중 실측)**: ⓐ **하드코딩 enum 라벨** = `s.label`/`urgency.label`이 리터럴 맵(`{DRAFT:{label:'작성중'},...}`)에서 옴 = 시스템 상수, free-text 아님(approvals/orders/schedule/portal/portalInvoices의 `${s.label}` 전부 이 클래스 — **보고 전 그 `.label`의 소스가 DB냐 리터럴 맵이냐 grep 확인**) ⓑ **삼항 조건 자체** = `${order.notes ? `...` : ''}`의 바깥 `${...}`는 **조건식**이라 매치되나 실제 sink는 중첩 내부(`<p>${order.notes}</p>`) → 라인 단위 스캔은 조건과 sink를 같은 라인서 잡으니 중첩 안쪽 보간을 직접 확인 ⓒ **에러 메시지** = `${err.message}`/`${err.response?.data?.error}`는 서버 응답이라 stored-XSS 저위험(reflected 한정, 보통 FP) ⓓ **숫자 치수/시스템값** = `${g.width_mm}`/`${order.main_item_width}` 등 치수·`${g.index}`·`${g.vision_confidence}`는 비-free-text. **이번 사이클 net-new 5 sink 격리·자동수정**(orders.js:961 `order.notes` 모달 비고[같은 함수서 order_number는 escape인데 notes만 raw=A-024 부분누락]·iaBatchTest.js:276/278 `g.name`/`g.item_types`[도달 `/ia-batch-test`·escapeHtml 0회 파일]·postProcessing.js:108/112 `data.key`/`data.label` 파라미터스키마 에디터 input value attr). **자동수정 판정**: free-text 텍스트노드/attr round-trip은 escapeHtml 래핑이 동작 무변=안전 자동수정(단순 모달/테이블 한정, 복합 출력문서 #399는 issue-only). **승격 의의**: 수동 sweep은 항상 "이번 페이지" 한정이라 같은 파일 형제 데이터소스(A-025)·escapeHtml 0회 파일을 놓침 → 전수 자동스캔이 free-text 속성명 휴리스틱으로 미escape sink를 코드베이스 전수 격리.

**자동 수정 가능**: escapeHtml 추가, SQL 바인딩 수정

---
