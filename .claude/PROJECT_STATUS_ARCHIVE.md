## 📦 2026-08-18 주문 배송지 3축 분리 (마이그 `0535` · prod `40bc935f`)

> 「주문서·발주서에 배송처 상세주소 칸이 있어야 하지 않나」(용준님)에서 출발해 prod 실측으로 판정.

| 실측 | 값 |
|---|---|
| `orders.delivery_info` 보유 | 8,758 / 10,075 (86.9%) |
| 그중 「층/호」를 본문에 섞어 넣음 | 3,973 (45.4%) |
| `[우편번호]` 프리픽스 | **0건** |
| `clients.address_detail` 보유 | **990 / 2,873 (34%)** — 주문서 자동채움이 **버리고 있었다** |
| `clients.postal_code` | 0건 |
| `purchase_orders.delivery_location` | **0 / 911 (전량 미입력)** |

→ **주문서는 필요, 발주서는 불필요**. 발주서는 기존 「납품 장소」칸조차 아무도 안 쓴다(오타 「낙품장소」만 정정).

**설계 — 파싱 마이그레이션 없음.** `delivery_info` 는 **합본(도로명 + 상세)을 유지**한다(소비처 12곳 무변경). 추가는 `delivery_postal`·`delivery_detail` 둘뿐. 복원은 **접미 매칭만** 한다(주소 파싱 금지) — 접미가 아니면 전체를 도로명칸에 돌려 레거시 8,758건에서도 손실이 0. 왕복 무손실을 `audit:orderform-roundtrip` + 헬퍼 5케이스로 실증.

**칸 추가와 별개로 이미 버그였던 3종.**
1. `orderForm/client.js` 자동채움이 `cl.address` 만 읽어 `address_detail`·`postal_code` 를 버렸다 → 거래처 990곳의 동/호가 주문에 안 실렸다.
2. `syncDeliveryInfo()` 가 출고방법·거래처 변경 시 **손입력을 무조건 덮어썼다**. → 손댄 값 보호 + **거래처 교체 순간만** 강제 동기화(`_forceDeliverySync`). ★자동채움 이력이 없을 때(null)도 보호해야 한다 — 처음엔 이 분기를 빼서 상세가 지워졌고 브라우저 실측에서 잡았다.
3. 주소검색 콜백이 값을 전면 대입. 공용 헬퍼는 이미 `detailFocusId` 를 지원했는데 주문서만 콜백으로 우회하고 있었다.

**죽어 있던 기능 2개 복구.**
- 한진 대량등록 엑셀 `받는분우편번호` = **하드코딩 공란**(`shipments.ts`). 우편번호가 어디에도 없었으니 채울 것도 없었다.
- 권역 묶음(우편번호 앞 3자리) 파생이 `[nnnnn]` 프리픽스 의존 → prod 0건 = **상시 무발동**. `delivery_postal` 우선 + 레거시 프리픽스 폴백으로 교체, 로컬에서 045** 권역 3건 그룹 발동 실증.

**대신화물 함정.** 이 모드의 주소칸은 「화물 터미널」이라 주소가 아니다. 라벨·placeholder만 바꾸고 끝내면 **숨긴 상세가 payload 에 섞여** 터미널명 뒤에 상세주소가 붙는다(택배→화물 전환 시 재현). 수집을 `collectDeliveryFields()` 한 곳으로 모아 대신화물이면 상세·우편번호를 제외한다.

**우편번호 확보는 습관에 달렸다.** 주소검색을 쓰거나 직접 입력해야 채워진다. 실측상 그 습관이 아직 없어(프리픽스 0건) 우편번호 칸을 readonly 로 두지 않았다. 한진 엑셀·권역 묶음의 실효는 여기에 종속된다.

**검증.** 타입체크·빌드 · entity 61/61 · 드리프트 0(0535 선적용) · prod smoke **111/111** · 페이지 9/9 · prod DOM 3칸+대신화물 전환+합성값 실측 · 레거시 행(상세 NULL) 무변경 재저장 바이트 동일.

**남은 것.** 견적서 폼은 배송 입력 자체가 없어 제외(`quotations.delivery_info` 는 컬럼만 존재).

## 📦 2026-08-18 주간 재고실사 개통 — 구역 선택 UI 결함 + 출력실 32품목

> 현황판에서 이관. 운영 정본 = memory `design-weekly-stock-count`.

**출발점** — 동산·선명은 재고 실사가 사실상 불가능하다는 용준님 판단. 그런데 확인해 보니
**이미 주단위로 하고 계셨다**(`C:\Users\user\Desktop\재고\재고\재고`, 2026-05-22~08-12,
현수막=jpg 인쇄양식+손글씨 · 시트=txt). 없던 건 방식이 아니라 **집계**뿐이었다.

**결함 1 — 생성 UI에 구역 선택이 없었다.** `src/scripts/inventoryCount.js` 가 `category` 만 보내고
`storage_zone_id` 를 아예 안 보냈다. 백엔드(`count_type='ZONE'`)·구역 뱃지·미배정 품목 패널은 전부
구현돼 있는데 **구역 실사를 만들 방법이 화면에 없던** 상태. 분류 실사는 원자재 790품목이 통째로 떠서
주간 실사에 못 쓴다. ⇒ 모달에 구역 드롭다운 추가(`/api/inventory/dashboard/zones`),
구역 선택 시 분류 셀렉트 비활성화(서버가 구역 우선이라 둘 다 고르면 오해), 토스트에 구역명.

**결함 2 — INNER JOIN 진입장벽.** `inventoryCount.ts:114` 가 `JOIN inventory` 라 그 구역에 재고 행이
없으면 **품목이 0개**로 뜬다. 재고가 있어야 실사를 하는데 실사를 해야 재고가 생긴다.
2026-07-08 실사 1건이 **라인 0건 껍데기**였던 게 이것. ⇒ `inventory` 에 (품목·구역·qty 0) 행을 넣어 뚫었다.
수량을 0 으로 둔 건 의도적이다 — 추정값을 넣으면 그게 나중에 근거가 된다.

**안전 확인** — 안 센 품목은 0으로 확정되지 않는다(`:498` 이 `counted_quantity IS NULL` 을 보정에서 제외).
790개가 떠도 필요한 것만 적고 나머지는 비우면 된다. 단 **0 을 적으면 「재고 없음」으로 확정**된다.

**단위** — 현수막 원단은 `yd`. **롤 수로 세면 금액이 안 나온다** — 롤당 야드가 112~135로 제각각이다
(쓰던 롤이 섞임, 청주 실사 실측). 호홍 계열(패트·코팅지·켈·켈그레이)은 품목 단위가 `롤` 이라 환산 안 함.

**7/31 적재** — 사진 판독 + 롤당 **130yd 고정** 환산(용준님 지시, 임시값). 실사 #3 APPROVED,
`count_date=2026-07-31`, 출력실 **23,146,978원** / 32품목. 다음 주 세부 실사가 이 값을 덮어쓴다.

**용준님 정정 3건 (내가 틀린 것)**
1. 종이 「현수막 재고」는 **현수막실이 아니라 출력실** 재고다 — **파일명의 자재군 ≠ 구역**.
   zone 6 으로 넣었다가 zone 1 로 전량 이관(`_bak_0818_zonefix`).
2. 코팅지는 **MCP120(120g)** 이다. MCP180(180g)과 코드가 나란해 헷갈린다.
3. 켈그레이 단가 = 60cm **32,250** · 152cm **71,500**.

**⚠️`AQ2-200` 은 신설이 아니었다** — 이미 있던 **비활성** 품목이고 `WHERE NOT EXISTS` 가 중복 생성을
막아 줬다([[feedback-item-duplicate-before-create]] 재발 방지 실증). 활성화만 했고 **단가가 0** 이다.

**⚠️`STOCK`(손익)에는 반영하지 않았다** — 출력실만이라 동산 전체 재고가 아니다. 부분값을 전체로 넣으면
재고 증가분이 과소 계상돼 원가가 부풀려진다. 전 구역 실사 후에 반영한다.

**검증** — 격리 워크트리(`new-session.ps1`)에서 **단일 파일만** 배포(워킹트리에 타 세션 변경 28파일 섞여 있었다).
타입체크·빌드·번들 마커 6·prod HTML 마커 실측·**smoke 110/111**
(실패 1 = `clients.nameIndex` 404 = 08-13 배포 유실 기존 건, 이번 변경과 무관).

**부수 산출물** — `scripts/stock-count-sheet.py`: 종이 실사표 생성기(ABC 부분실사).
실측 **동산 431품목 중 56개가 매입액의 80%**·선명 359 중 76개. 95% 컷으로 동산 153·선명 174품목.
★열 설계 = **①롤·포장 수 / ②롤당 수량 분리**(원단 단가가 야드당이라 ①만 적으면 금액이 수백 배 틀린다).

