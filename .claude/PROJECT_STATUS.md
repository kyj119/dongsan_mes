# PROJECT_STATUS.md — 프로젝트 현황판

> **✅ 2026-09-26 `a9a9c86f`(session/money-flow, 미배포) — 돈 흐름 점검(주문서→청구→계산서→입금 · 발주→입고→지급)** — 실측 재현 13건 수정(단가미정 청구·계산서 걸린 청구취소/주문취소/거래처변경·재청구 옛금액·전체모드 입금귀속·재발주 헤더·입고0건 마감 AP 0 등). 남은=결정 13건(청구 후 증액 불가·취소발행 후 미수 유지·DRAFT 중복 등) `docs/audits/2026-09-26-money-flow.md` · 배포는 요청 시

> **✅ prod 2026-09-26 `a72a8f63`·main `21978fef` — 리뷰 잔여 마감** — 바로빌 WSDL 대조(계산서 상태 필드 교정·페이지 응답 음수 오류·재시도 중복 관리번호) · 훅이 worktree 를 검사 · 현장 축 코드(LogWatcher 큐 유실·★FLEXI 억제키 수명=밤 9시 이후 selftest 실패 원인·caps-worker 실패 삼킴·IA 문서 미닫힘·오래된 ia_error.log). **Z: 준비 완료**: caps-worker src(백업 `index.js.bak-20260926-221455`)·LogWatcher-kit(git 21978fef). IA 에이전트 23:16 재시작·드리프트 0 ✅. 바로빌 코드표=공식 가이드로 확인(1 전송전·2/3 중·4 성공·5 실패) + 팝빌 잔재 분기 제거 `b368176b`prod 배포(smoke 134/134·write 5/5). 남은=경리PC/장비PC 설치

> **✅ prod 2026-09-26 `20a0d4ef`…`e1d74814` — 리뷰 결정 1~6차 24건 마감(결정 대기 0)** — 계산서 취소발행 · PAID 폐기 · 연차 회계연도+비례 · 부분입고 발주 마감=입고분 · 세액표 폴백 · 주문 삭제 순서 · /bank→자금관리·경리 통장 · 포털 사업자번호 · 거래처 가져오기 · 출력 연결·보류·출고복귀 · 재무스냅샷 · 광고 명칭 · 백업 R2 90일 · 결재 후속 알림 · 이관 기초잔액 막음 · /ship·auto-improve 규칙. 보류=지출결의·재주문·신용점수. 남은=바로빌 WSDL 3 · 웹 밖 축 4(LogWatcher·IA·caps) · 훅 cwd · ⚠️사용자=계정 동기화 스킬 삭제·백업 R2 도달 확인. 정본 `docs/audits/2026-09-26-full-review.md`

> **✅ prod 2026-09-26 `6c3c7c12`+`48c743f4` — 전체 코드 리뷰(확정 89) 중 54건 반영** — ★**D1 백업이 R2 에 한 번도 안 올라갔다**(`--remote`) · 급여·권한·재고·계산서 결함 · 스캔 출고 본체 통합 · `check:fn` 독립페이지 판정(→거래명세서 수량표기 결함 적발) · `end-session` 이 남의 dev 서버 죽이던 것. 남은=결정 대기(세금계산서 취소 미연동·PAID 미수·연차 기준·백업 90일 정리) + 바로빌 WSDL·IA/LogWatcher 축 — `docs/audits/2026-09-26-full-review.md`. ⚠️내일 백업 run R2 도달 확인.

> **✅ prod 2026-09-24 `2cc2d564` — 배송 알림 정리** — 방문수령·직접수령=대상 아님(템플릿 보존·재검토 예정) · `POST /api/shipments` 자동 발송(알림톡·이메일) 제거(`test:shipment-notice` ⑨) · 「한진택배 출고」 심사 신청 → 발송 때마다 승인(3/S) 템플릿만 읽어 **승인되면 배포 없이 알림톡**, 전엔 문자(`applyTemplateApproval`). 검증 journey 40/40·e2e 4/4·smoke:prod 134/134·write 5/5. 남은=승인 후 내부 번호 1건 시험 발송

> **✅ prod 2026-09-23 `65af4e65` — 출고 현장 피드백 5건** — 확정 대기 상태 열(확정됨·알림만 남음)+체크박스 일괄 확정/알림 · 준비상태→**출고 예정·실적**(출고완료 포함·이월 7일·예정표 인쇄) · 주문 원가=ADMIN/MANAGER/경리만(서버 제거·마진 산출라인 기준) · 작업지시서 바코드·JsBarcode 제거. 검증 journey 40/40·e2e 4/4·smoke:prod 134/134·write 5/5·render-junk 0. 남은=방문수령 알림 대상 여부 결정

> **✅ prod 2026-09-23 `33024885` — 전사 주문 연동 1차 + 표시 2겹 고정 + 마이그 0627(prod 적용)** — 주문서 후가공에 끈고리(상/중/하)·하도매 상단/측면·봉미싱 cm 자리, 가로등 품목 「조」후보(`item_units.forms` 스위치 필요), open_lines 에 판매단위·후가공·품목코드 → 전사 탭 [주문 불러오기](tr-order.js·`cut:trorder`). 표시=검정 심+백 테두리 고정·비율 must·롤 폭 칸 제거(호스트 0.8.0·패널 0.11.0 — **ia:deploy 는 아직**). 검증: journey 40/40·local-e2e 4/4·smoke:prod 134/134·prod 마커(cm 토글·PP-LOOP·프리셋) 실측. 남은=ia:deploy(축2 실기)·주문 입력 후 패널 확인

> **✅ Z: 2026-09-24 `990b9bf6` — 재단 조각 번호 Phase 1 + 실기 정정 3건(호스트 0.53.0·셸 0.95.0)** — 번호 = 원본 이미지·판 꼬리표·조각 목록에 같은 id. 도련 집게·꼬리표 위치 정정 · ia:deploy 되돌림 차단(`8e59f3fb`). Z:=main·5축 드리프트 0. 남은=Phase 2 웹(조각 목록 글자·판 묶음 경고). 정본 = memory `design-piece-number` 

> **✅ prod 2026-09-24 `2437b47b` — 조각 번호를 작업지시서에서 읽히게** — 번호 = 조각 한가운데·조각 안에서 최대(셸 0.95.1 · `labelLayout` · 겹침 0 게이트 M7). 재단 패널 줄(`CUT-CEP-*` 고해상도)은 작업지시서에서 줄 아래 **A4 폭**으로(인쇄 숫자 0.66→≈3mm). 검증 journey 40/40·CI ✅·smoke:prod 134/134·`/cards` 마커. 남은=**패널 `ia:deploy`(0.95.1)**·실물 등록 1건 작업지시서 확인

> **✅ 2026-09-26 재단 판짜기 작업지시서·도련 모서리 — main `8883caff` · 웹 prod · IA 셸 0.102.0/호스트 0.58.0 배포 · 에이전트 재빌드** — 판짜기 1회=가로 A4 한 부(원본 전체+판별 색 테두리·목록표·후가공) · 카드 상세 표시 · 테두리 건너뛰기 모서리 축별 2mm · 대기함 코팅 자동 선택(`7fe13363`). 실기 테두리 확인. 남은 것 = 주문 저장 후 인쇄 첫 장(가로) 실물 확인 · 사각 빠른 경로 기본값 판단. 정본 = memory `project-cut-rect-fast-path`
> **✅ prod 2026-09-23 `c758c047` — 화면 쓰레기 문자열 감사 `audit:render-junk` 신설** — 「undefined」가 화면에 뜨는 축을 처음으로 게이트에 올렸다. ★**정적 대조는 폐기** — 라우트 SELECT ↔ 스크립트 `o.필드` 시제품이 **후보 566건**. 오탐이 그만큼이면 아무도 안 본다 → **증상을 직접 본다**: prod 57화면에 **1건**, 그게 진짜 결함이었다. **기준선 0건이 정상** · 자가시험 양방향 11항목 · `/deploy-verify` Phase 4 + CLAUDE.md 배선. 바로 잡은 결함 = 장비별 표가 `e.ok`·`e.error`(실제 `ok_count`·`error_count`)를 읽어 ★`undefined > 0` 이 **항상 false** → **에러 수가 영영 안 떴다**, `e.sqm` 은 SELECT 에 없어 **면적·비율이 늘 0**. 함께 = **0626 에서 빠뜨린 둘**(#660) — `menu.ts` 에 `/feedback` 없어 **사이드바 진입점 부재**(권한과 표시는 **다른 소스**) · 신고 알림 **수신 역할 = 열람 권한**(MANAGER 추가). 검증=자가시험 11/11·prod 0건·smoke 134/134. 경위=`PROJECT_STATUS_ARCHIVE.md` §2026-09-23


> **✅ Z: 2026-09-21 `ae9ed182` — IA 5축 배포: 전사 패널 첫 반영 + 「Z: 를 못 읽었습니다」의 진짜 원인** — 스텁(`jsx/host.jsx`)이 호스트를 **이름 손목록**으로 읽어 `mes-tr-host.jsx` 를 아예 안 불렀다 → 패널은 그걸 「Z: 접근 실패」로 표시했는데 **Z: 는 멀쩡했다**(폴더 열거로 전환). 함께 나간 것 = 돔보 중심이격 17→**7mm**(코드가 틀렸고 그 파일 주석의 30쌍 실측이 옳았다)·펀칭마크 1/2·`<Clipping Path>` 제거·**p0.png 실패 근본수정**(굽기 폴더 고정이름 → 실행별 이름+오래된 것 스윕. ⚠️스윕 건수를 `MESCUT_EFS_WHY` 에 넣었다 뺐다 — 실패 사유 전용 채널이다)·네스팅 파일명 `-N장` = **그 판의** 조각 수. 축1·2·3·5 ✅ · 축4는 일러 재기동에 자동(표식 실패 0회). 남은=**실기 확인 미완**(전사 탭 `host TR-CEP-0.2.0` · 파일명 판별 분리) · 맞붙임 OFF 사유 미재현 · p0.png 나머지 원인 2

> **✅ 2026-09-21 `9617ee31` — 표 열잘림 자동 게이트 신설(직전 배너의 「게이트 없음」 해소) + 기준선 44→37** — `audit:table-clip`(prod 56화면·탭 포함, `/deploy-verify` Phase 4). 4겹 예방 = `.ds-chip`(배지가 inline 이라 td 의 ellipsis 를 못 물려받던 것)·`dsTd()`/`dsChip()` 헬퍼·dev 전용 clip watcher·게이트. 12파일 열폭 수정분 중 **7건 해소 반영**. ★**`--update` 가 대상을 안 보고 썼다** — 기본 대상이 localhost 라 `--base` 없이 돌리면 prod 기준선이 로컬 측정치로 덮여 **44→14 로 깎인다**(실제로 그랬고 되돌렸다). 대조 경로에만 경고가 있고 **되돌릴 수 없는 쓰기 경로가 무방비**였던 것이 원인 → 대상 불일치면 exit 2(`--force-base` 로만 강행). ⚠️`min-width` 는 fixed 에서 무시라 넓힌 만큼 옆 `auto` 열이 준다(내 수정이 내 게이트에 걸렸다 — 과한 열에서 회수). 남은=`col-tag` 92px 4건·날짜형식 2·단위접미 5


> **✅ Z: 2026-09-18 `b03a82d3` — 같은 날 같은 이름 등록 = 픽업이 덮는다 → 경고(P0)** — `_출력/<날짜>` 는 **날짜당 한 자리**이고 에이전트가 덮어써서 **재단기 실물만 나중 것 하나**가 되는데 등록·주문·카드가 멀쩡해 아무 화면에도 안 나왔다. **이름은 안 바꾼다**(그 파일명이 RIP 규약 = 08-27 매칭 0% 를 되살린 근거) → 등록 때 픽업 폴더 + **오늘의 다른 등록 폴더**를 봐서 가공 warn `P`·재단 `pickdup=` 으로 알린다. 호스트 0.20.0/0.47.0·패널 0.27.0/0.88.0, 게이트 `panel:smoke` §14(양방향). ★**`a0:flow` 가 정상 배치를 전부 「잔해」로 보고하던 오탐 정정**(배치는 `manifest_N.json`인데 도구가 `manifest.json` 한 이름만 봤다 — 없는 고장을 만들어 사람을 움직였다). 단가 최근가 범위·100×50 2EA 답변은 조치 없음(용준님 사내 단가 규칙 정리 중). 남은=**축4 설치본 2건(개발 PC만)** · 미매칭 배지 없음 · 규격 뒤집힘 4.0% 미인식. 경위=ARCHIVE §2026-09-18 픽업


# PROJECT_STATUS.md — 프로젝트 현황판


> **운영 규칙(2026-08-10 확정)**: ①완료(✅)=아래 인덱스 1줄(제목+남은 것)만, 경위 전문은 `PROJECT_STATUS_ARCHIVE.md`에 직접 쓴다 (**「✅ 최근 완료」 항목 400자 상한** — 2026-08-19 게이트 강제, 초과 시 큰 것부터 지목) ②의미 없는 대기=보류함으로 과감히 이관 ③대기 항목=빠른 처리 우선 ④"커밋·미배포" 기록은 믿지 말고 prod 실측(deploy.yml=main push 자동배포). 게이트=`node scripts/doc-diet-audit.cjs`(훅·세션 시작 배너 연동).


> **📋 2026-09-11 조사 — 주문↔파일 연결·패널 배포 점검** — 결론=**배선은 정상, 쓸 기회가 없었다**(라이브 매칭 전 기간 1건 · 주문 11,669/11,670 이 이관분). 교훈=CLAUDE.md §파일↔주문 연결 · §IA 5축. 문서=`claude.ai/code/artifact/f8880a69-f5cf-45d1-ab9f-d88cf7cb1625`. 남은=**⚙ 환경 점검 출력+실패 줄 수령(선행)** · `PUT /orders/:id` 의 `print_file_map` 재연결 정책 결정 · `test:print-match` 실행 경로 배선


> **📋 2026-09-04 조사 — 게릴라·원단 두 건 다 「전제가 바뀌었다」** — 경위 전문 = `PROJECT_STATUS_ARCHIVE.md` §2026-09-04 게릴라·원단 조사. 남은=**배너 900원 미만 229라인 업무판단** · 타공시트·머리띠·투명패트·UV텐트천 **4종 자재 판단**


## ✅ 최근 완료 — 후속 대기 인덱스 (경위·상세 전문 = `PROJECT_STATUS_ARCHIVE.md`)

- **✅ 09-23 전사 표시 정정·마감 통합(패널 0.10.0·호스트 0.8.0)** — 시접 띠 위 표시·접는선/끈고리·마감 4종·2겹 고정·비율 must. 남은=ia:deploy+실기


- **✅ 09-11 패널 구조 점검 3건(배포·실측 완료: 호스트 0.13.0·패널 0.21.0·재단 0.43.0)** — ①`mes-core`·`mes-sheet` 본체 은퇴(repo 삭제·Z: `_retired/`·감사 제외) ②**빈 catch 390곳 전수 분류**: 363 사유 주석·27 실패 기록(호스트 0.13.0 warn C/D/N·재단 0.43.0 `dupfail=`/`cutfail=`/`dxf_error`·에이전트 `warn.log`) + 게이트 `audit:empty-catch`(편집 훅 차단·커밋 훅·ia:deploy) ③모아찍기 S4 잔재 정리(패널 0.21.0, 로드 경고 0). 경위=ARCHIVE §2026-09-11 패널 구조
- **✅ 09-11 펀칭 규칙 통일 「나」(IA·웹 배포 완료 `7a23c954`)** — 변N=**양끝포함**(상하좌우3=8)을 호스트 정본으로 두고 에이전트 `ProcessOrderItem.jsx`·라벨 쌍·주문서를 맞췄다. 표기=실물 「8개(모서리 4, 4변 1)」(패널·카드·주문서 한 문장)·파일명=위치어+총개수(사방펀칭8)·「꼭짓점」→「모서리」·패널 0.20.0 이 결과를 센다. 게이트 `test:finishing-label` 60(사본 대조)·`panel:smoke` 7f. 잃는 것=옛 「모서리 따로+안쪽N」 해석. 경위=ARCHIVE §2026-09-11 펀칭
- **✅ 09-11 S급 9건 prod 배포(`908c7e56`)** — #622·645·641·634·633·630·640(UI/폼)·#638 cardSpend WIP문서화·#639 마이그번호충돌 CI게이트. 남은 open=#616·617(현장)·626·629·646
- **✅ 09-11 이슈 8건 prod 배포(`3f498350`+`11b767fa`)** — 법인격리 #632·635·631·643·마진축 #642·이월=is_voucher #628·고아라우터 #627·에이전트현황 #625·#613 vite5→8(audit 11→0·★esbuild devDep 함정). #626 평문PW 수용

- **✅ 09-11 일러 패널 한글 IME + 주석 소실 정정(`ia:deploy` 완료·드리프트 0)** — ★IME=CEP 12 결함(CEP-3029): CEF 99 `TSFImeSupport` 기본 ON → manifest `--disable-features=TSFImeSupport` 1줄, 실키로 composition 정상 확인. ★주석=`bL~bB` 가 경계선 `if` 안에 선언돼 **OFF(08-06 기본값)면 NaN→빈 catch** 가 한 달 삼킴 → 호스트 0.12.0·`annotation_error`·게이트 7e. 재시작 재검증=호스트 0.12.0·셸 0.19.0·플래그·주석 상1/하1. 경위=ARCHIVE §2026-09-11. 남은=**각 PC 일러 완전 재시작+한글 1회 타이핑**
- **✅ 09-07 자금예측 LOAN 축 대칭 — 이미 나간 상환을 또 세던 것(배포 `3e9db9ea`)** — `loan_payments` **344회차 전부 통장 미연결** → 지난 회차 15건 1,925만이 SCHEDULED 로 남고 `carryOverdue` 가 예측 시작일로 끌어와 **중복 계상**. 매입·매출과 같은 형태의 마지막 축. **파생 대사**(`utils/loanSettlement`) — ★후보가 둘이면 **안 붙인다**(안 낸 회차가 낸 것이 된다). 5건 1,054만 해소·carried 15→10. 게이트 `test:loan-settle` 24. 경위=ARCHIVE §09-07 LOAN. 남은=**대출계좌 자동이체 10건**
- **✅ prod 09-08 `0591` 대출 식별 키를 컬럼→규칙으로** — `account_no` 는 은행 대출에만 통한다. 키 없는 11건 중 **9건이 적요 이름으로 이미 잡혔다**(`CMS 오릭스코리아`·`신한카드할부`·사업자번호). `loan_match_keys`(ACCOUNT·ANCHOR) → 유형이 늘어도 **행만 는다**. ★앵커는 모호하다(`중진공대출`=3건) — 금액이 가르되 **근사가 아니라 월별 1:1 배정**이어야 한다(근사면 엉뚱한 대출이 먼저 가져간다). 그 결과 **미등록 할부 2건**이 드러났다: `현대캐피탈` 505,225×8회·446,341×6회. 하네스 **127항목**. 경위=ARCHIVE §09-08 식별키. 남은=**미등록 할부 2건 확인·B(파생 전환)**
- **✅ prod 09-08 `0588`~`0590` 묶음 C — 대출 이자가 손익에서 빠져 있었다** — 적요가 `{대출계좌번호}-{일련번호}` 인 걸 해독하니 **만기일시 대출 월 이자 44건 4,023만이 「차입금상환」(NOT_EXPENSE)**에 있었다. MES 이자비용 **318만 → 4,944만**(세무장부 정본 5,696만의 87%). 계좌번호를 `loans.account_no` 로 승격 + 판정 순수모듈 + 스윕 연결. 게이트 **`audit:loan-bank`** 신설. ★★그 「미등록 2건」은 **미등록이 아니라 #21·#22** 였다 — 담을 칸이 없어 `notes` 에만 있었다(`0590`). 경위=ARCHIVE §09-08 묶음C. 남은=**계좌번호 없는 차입금 11건**
- **✅ prod 09-08 `0587` 묶음 B — 자금이동 라벨 + 정체 확인** — 새 계정 대신 **기존 이체 짝짓기 소급**(`detect-transfers days=0`) **66쌍 5.66억**(279→411건). ⛔금액만 맞으면 붙어 **개인통장**까지 섞인다 → 양쪽 자사 표기만 인정. ★★**DB에 답이 있었다** — `오다플래그`=`entities` #4·거래처 #1655(발주 9건) → 10건 1.48억 **원장 연결**(이중계상 0) · `중진공` 1억=`loans` #7 **개시일·금액 일치**. 잔량 20.5억→**8.41억**. 경위=ARCHIVE §09-08 묶음B. 남은=**602980계열 35건 2.95억·대표자 13건 9,420만**
- **✅ prod 09-08 `b5bfb5e9` 사람 판정 큐 A — 교차검증 후 705건 반영** — 이름과 **독립된 축**(그 거래처와 실제 거래가 있었나)으로 걸러 적용, **261건이 기존 원장에 연결**(사람이 넣어둔 것과 일치). 제안 921→**256건**·강함 329→5. ★★게이트가 **내가 만든 결함**을 잡았다 — `0583`이 지운 입금 운반비를 스윕이 다시 만들었다(**데이터를 지워도 규칙은 남는다**) → 입금엔 NOT_EXPENSE 외 금지. ⚠️`switch-entity`=**body** 파라미터(E2·E3가 조용히 E1로 돌았다). 경위=ARCHIVE §09-08 큐A. 남은=**약함 221건·미매칭 373건 11.5억**
- **✅ prod 09-07 `eeacfeeb`(`0584`·`0585`) 미처리 잔량 3축 — 승격·계정역할·카드대금** — ★스윕이 `UNMATCHED` 만 봐 **확정이력 규칙이 기존 제안 830건에 영영 안 닿았다** → 승격 **267건**(창에 승격분이 남아 131에서 멈추던 것 재수정). ⛔적요 묶음=1.6배라 기각, 축은 **금액**(상위 100=61%). ★★손익 제외를 **계정 이름**이 정했다(12.3억) → `expense_categories.role`, `CAT_ROLE` 사본 쌍 **폐기**. ★`IGNORED` 겸직 → **카드대금 86건 2.73억** 분리. 판관비 11.1억 불변. 경위=ARCHIVE §09-07 잔량3축. 남은=**약한 제안 238건 5.03억 사람 판정**
- **✅ 09-07 분리·매칭 점검 + 계정 게이트 신설(`0581`~`0583`)** — 분리 계정은 **잘 갈리고 있다**(식대/복리후생 3%·유류/차량 2%·사무용품·4대보험 0%). ⚠️세금과공과/수도광열비 **14%** — 0579 WHERE 가 `지방세·세외수입·취등록세·등록면허세`를 놓쳤다(→3%, 총액 불변) · **카드 축 누락**도 있었다(`0582`). ★★`차입금상환` 에 **차입 실행 입금 2건 1.2억**(0578 통합 부작용) → `차입금` 분리, 선명 상환 1,203만이 묻혀 있었다. 매칭은 건강(**확정 이력 132건 불일치 0**). 게이트 **`audit:expense-category`** 신설. 경위=ARCHIVE §09-07 분리매칭
- **✅ 09-07 계정과목 표준 명칭 정리(`0579`·`0580`)** — 국세청 표준계정으로 개칭: **임대료→지급임차료**(받는 게 아니라 **내는** 것)·공과금→수도광열비·세금→세금과공과·대출상환→차입금상환·고정자산취득→유형자산취득·괄호 제거·기타→잡비. ★`CAT_ROLE` 이 **이름으로** 판정해 사본 쌍 동시 수정. ★★**꺼져 있던 표준계정 3종에 8건이 갇혀 있었다** — `수선비` 는 결번이 아니라 **비활성**(`is_active` 를 안 보면 혼동). **전 법인 공통 19종** 확보. 경위=ARCHIVE §09-07 계정명
- **✅ 09-07 비거래처 계정 매핑 + 마스터 정리(`0576`~`0578`)** — 305건 2.39억 분해: **매입대금 성격이 최대 덩어리**(계정 아님)·카드/자기이체 24건 IGNORED·**계정 64건 1,043만 적용**. ★특허공제 8건=**자산**(`공제부금(자산)` — `CAT_ROLE` 이 **이름으로** 판정해 사본 쌍 둘 다 등록). ★★**이름 불일치 2쌍 통일**(대출상환·지급수수료) — 같은 돈이 두 줄로 갈리고 있었다. 결번 22→**19종**(남은 건 실사용 0). ⛔`외상대출금` 2.76억=**대출 아닌 매입대금**(키워드 오탐). 경위=ARCHIVE §09-07 계정. 잔여 **216건 1.53억**
- **✅ 09-07 통장매칭 — 사람이 이미 답해 놓은 걸 안 읽고 있었다(배포 `bf60a477`)** — 적요 `홍익`→**온오프컴퍼니**(용준님 발견). 세 거래처의 **접두**라 이름 규칙 전부 침묵 + 금액 우연일치. ★근본=규칙이 **수동매칭에서만 학습** → 한 거래처로만 확정된 적요 **1,136종 중 규칙 266종**. → **확정 이력을 파생으로**(2회↑=0.95·1회=0.75)·금액단독=이름 없을 때만. 홍익 8건 전부 확정·금액일치 단독 **0건**. 경위=ARCHIVE §09-07 매칭이력. 남은=**사람 판정 922·477**
- **☠️ 09-07 리스료 이중계상(판단 대기)** — `비씨카드` 2,332,300·`신한카드할부` 745,630 은 `loans` **#10**(설비 카드할부 잔 8,395만)·**#13**(포터 할부 잔 933만)인데, 같은 출금 14건이 통장에서 **cat=91 리스료 비용**으로도 APPLIED. `loan_payments` 50회차 **전부 통장 미연결**. 무이자(#10)면 전액 원금이라 비용 0 이어야 한다(「대출원금=비용 아님」). ★**대표자 개인통장 카드(NH농협)는 등록 안 함 확정** — 그 몫은 귀속 불가가 정상. 경위=memory `design-card-billing-cycle`
- **✅ 09-07 카드 제외 사유 마커 `offset_reason` — 백필 390건(`0575` prod)** — `is_offset` 이 네 뜻을 겸해 **청구 기준을 계산할 수 없던 것**을 분리. ★근거=**memo 자유텍스트**(추론 아님) → 390/390·미분류 0, 독립대조 224/224·10/10. 사이클 절대오차 **95.9M→56.9M**, 누적 **전북 −0.6%·하나 −1.3%**. ⛔**「할부탓」은 오진**(재발·우리 할부 0). 경위=ARCHIVE §09-07 카드마커. 남은=**미등록 카드사 15.2M**
- **✅ 09-07 매칭 우선순위 결함 — 금액 우연일치가 이름 근거를 밀어냄(배포 `5728fecf`)** — `박영석(산그라픽스)`→**광고월드**(용준님 발견). 19,800원이 미수잔액과 우연 일치해 `금액일치` 0.5 채택, 괄호 상호(0.7)는 게이트가 `<0.5`라 **조회조차 안 됨** — **점수보다 평가 순서가 이긴다**. 재스윕 결과 2건이 아니라 **135→35건**(100건 7,710만 재배치). ★계좌 신선도도 정정 — 평소 간격 배수 기준(마통 27일·SC제일 18일은 **정상 리듬**, 수집해보니 신규 0건·중복 590). ★즉시조회 「코드 0」=`parseInt||0` 이 비숫자 응답을 뭉갠 것. 경위=memory `design-bank-matching-sweep`
- **✅ 09-07 계좌 잔액·신선도 표시 + 갱신 1버튼(배포 `98438b3f`)** — 계좌 카드가 `last_synced_at`(prod **전 계좌 NULL**)을 봐서 늘 「동기화 안됨」 → **잔액+최종거래일+경과일**(7일 주황·14일 빨강, 잔액은 자금현황과 같은 식). ★마통이 **27일**, SC제일 **18일** 멈춘 게 그제야 보였다. 「즉시조회」(요청만)와 「동기화」(다른 탭)를 이어 누르길 기대한 게 공백의 원인 → **갱신 1버튼**, 수집구간=**최종거래일~오늘**(오늘 기본값이면 밀린 27일이 영영 안 들어온다). ⛔기간귀속 기각 → **09-07 뒤집힘**. 경위=memory `design-card-billing-cycle`
- **✅ 09-06 통장 잔여 4건 — 취소예정 봉쇄·정책 하네스·매칭 2규칙·매입지급 가드(배포 `99891e73`)** — ★발주 취소가 `cash_schedule` 을 **양쪽 경로 다 안 건드렸다** → 28건 5,200만 정리 + `audit:orphan-schedule`. ★매칭 규칙은 **실측으로** — 적요포함 하한 **4자** 채택, startsWith 는 **기각**(모호 45%). ★출금 apply 가 **비용을 채무로 둔갑**(4,316만) → 확정발주 0이면 중단. UNMATCHED 658→**544건**. 경위=ARCHIVE §09-06 잔여4건. 남은=**비거래처 87건 계정 매핑 결정**·사람 판정 1,490건
- **✅ 09-06 통장 매칭 백로그 소진 — 원장 753건 7.42억 생성(배포 `4a0cefed`)** — 통장 9/5 vs 원장 반영 **8/11 정지**(=예측이 말하던 입력밀림). ★최대 덩어리는 코드가 아니라 **스캔 범위**(90일·500건). 결함 3=**은행앱 접두**·카드정산 오염·**전각 `（주）`**. ★★적용 직전 **자기 계좌간 이체 54건 5.02억이 수금으로 확정**돼 있던 것 발견(자동매칭 상시 결함). UNMATCHED 1,590→**658건** · 입력밀림 31·37→**4·1일**. 게이트 `test:counterpart` 32. 경위=ARCHIVE §09-06 통장. 남은=**SUGGESTED 856·UNMATCHED 658 사람 판정**·취소발주 잔존 28건
- **✅ 09-06 자금예측 매입·매출 대칭 — 낸 돈을 빼고 연체는 실적 속도로 분산(배포 `995ef162`)** — 확정발주 전액을 유출로 세던 것(이미 낸 24.8억 포함) → **FIFO 파생**(발주↔지급 연결 536건 중 **1건**). 취소 잔존 52.0M·내부법인 460.3M 제거. 전량 만기도래라 첫날 일괄=곡선 사망 → **완결월 런레이트 분산**. ★관측지연 **−111일=이관 흔적** → 표본 정정 후 **+38일**. 위험일 **91/91→45/90**. 게이트 47+52. 경위=ARCHIVE §09-06. 남은=**취소 잔존행 28건(E2) 실삭제**·입력 밀림 31·37일
- **✅ 09-05 자금계획 달력+통계 한 화면 · 예상수금 FIFO 파생(배포 `df039437`)** — ★세로 목록=`col-span-7`만 준 블록. 탭 4개→대시보드, 엔진 3회→`/schedule/overview` 2회(prod 247ms). ★★수금 매칭 완전일치→**FIFO 파생**(부분·합산·상계)→**미수 과대계상 해소**. 게이트 `test:cash-settle` 21항목. 경위=ARCHIVE §09-05
- **✅ 09-05 시작잔액=계좌별 지정(`0574`·배포 `b84e7138`)** — 마통 잔액을 합산해 **-4.2억에서 출발**→위험일 91/91(경고 무의미). `include_in_cash_plan`(예금1·마통0 초기화)로 **+7,555만**. `is_overdraft`(사실)와 분리=보증금 예치형 예금도 제외 가능. 경위=ARCHIVE §09-05

- **✅ 09-04 재고실사 담당자 개통(배포 `fc49e255`·`0563`)** — 원인은 UI 가 아니라 **권한 벽 둘**, 병목은 입력이 아니라 **승인**(18%). 담당자 승인 개통 + **낡은 회차 승인 차단**. 실사표=전체화면·규격표시·장부는 입력후. 미배정 44행 정리 + **뿌리 폴백**(게이트 `test:inventory-zone`). 경위=ARCHIVE §2026-09-04 재고 UX. 남은=**담당자 실사용 판정**
- **✅ 09-04 품목 축 원칙 확립 + 1~6월 정리(585→403) · 전표 플래그(`0563`·`d597f9f0`)** — ★원칙=**조합 폭발 축은 규격으로, 정체성 축만 품목, 체계 밖은 (특수규격)**. 수기대=깃대길이만·SET=2축·부착/도색=후가공. ★★`voucherOrderSql` 이름의존→**`is_voucher`**(`E1-ACCT` 115만이 6월 매출에 새고 있었다). ★1~6월 403 중 **197=회계이월**(정상). ⛔족자봉 **중복 6번째**·이후 3연속 회피. 경위=ARCHIVE §2026-09-04 (2). 남은=**실대상 ~192라인**

- **✅ 09-04 이카운트 판매현황 재이관(배포 `3c83857e`) — 7·8월 원천 대사 차이 0** — ★원천 교체(주문서현황은 **출고되면 사라진다**)→판매현황=거래처코드/품목코드/규격 별도열. 8월 **1,257전표 4억 6,803만**·7월 **3억 1,440만**(325곳 일치)·품목 미해소 **96→0**·규격 262라인 분해. ★★규격 한칸 회귀 검토→축은 **pricing_method**. ⛔EP115 중복 신설(계열열거 5번째). 경위=ARCHIVE §2026-09-04. 남은=**★출력↔주문 소급매칭**(카드 1,246 생겨도 매칭 0)

- **✅ 09-04 IA 축2 배포 — 재단 판짜기 99.4→31.4초 + 사각 칼선·부스러기 제거(호스트 0.37.0)** — ★**적용 111.9초 중 98.1초가 조각별 폴백**이었고 원인은 굳힌 PDF 가 조각별 그룹이 아니라 **낱개 패스**(4조각=57개)로 풀리는데 **최근접 중심**으로 배정해 크기 편차에서 옆 조각으로 넘어간 것 → **셀 상자 포함**으로. ★export 는 **호출당 고정비**(46회→2회, 18.5→9.0초). ★격자가 **PDF 200인치 한계** 초과로 조용히 죽고 있었다. ★사각 칼선=embed 가 만든 클립 2겹 · 부스러기 0.1mm 하한. 경위=ARCHIVE §2026-09-04 IA 재단. 남은=**정소은 님 실사용 판정**

- **✅ 09-04 선명 실사 개통 + 잉크 용량축 정리(`0559`~`0562` prod)** — 선명 재고 228행이 **구역 미배정**이라 실사표가 0줄이었다(실사 0회·원장 0행·4,695만 동결) → `선명2` 배정 = **212줄**. ★잉크테크 1L 519통은 **선명 재고**(동산 0) · 티피엠 18L=전표 오기 · 잉크 용량은 **이름에만**(`pack_size` 금지). 경위=ARCHIVE §2026-09-04 잉크. 남은=**선명 실사 실행**

- **⏸ 09-04 8월 주문 재이관 — 적재했다가 롤백(원천이 8/11 스냅샷)** — `_bak_0813_aug_*` **DROP**(덤프=`backups/2026-09-04-aug-bak-dump/`). 425전표 적재해 원천 대사 **차이 0**·**카드 1→408**(주문 생성이 만든다)·원가 **80.2%** 를 실증했으나 Excel 이 8/11 수출본이라 이후 수정·취소 미반영 → 전량 삭제(고아 0). ★임포터는 전표 마커 멱등이라 **덮어쓰기 불가**=지우고 다시가 유일. ★미해소 8전표 **2,973만**(충남문화관광재단 1건이 2,727만). 경위=ARCHIVE §2026-09-04. 남은=**크롬으로 이카운트 최신본 수출**


- **✅ 09-03 전체 코드 리뷰(Fable 5.1) — 코드 0 수정** — 게이트 20종 전부 통과 · 에이전트 20개(보안 3·Area2/4·QA·슬라이스 9·/code-review) → **CRITICAL 2·HIGH 89·MEDIUM 142**, HIGH 이상은 메인이 소스 재검증. ★C1=포털·직원셀프 토큰이 내부 API 통과(auth.ts:22+permissions.ts:54) ★C2=purchaseInvoice.ts:6 무인증 반사 XSS. 정본=`docs/audits/2026-09-03-full-review.md`(+상세 15파일). 남은=**수정 묶음 1(인증 경계)부터 착수 여부 결정**

- **✅ 09-02 S2 주문 라인 원가(BOM 기반) — 미배포** — ★`total_cost` 전량 0 = **`cost_standards` prod 0건**. ★★소요량이 `width`=폭 **고정**이라 86% 라인 폭·길이 뒤바뀜 → **2.5배 과소**. ★★★**분할 자동탐색 제거**(이음비 0→900폭 5~7분할 폭주·원단은 영업이 고르는 축). ★잉크=**방식별**(수성189·전사89·솔벤1011·UV1174). ★**단가 0 자재 1종이 제품 전체를 면적기준 강등**(18제품·7개는 원가 0)→`0555`. 게이트 25건. 남은=배포+`0554`+`0555`+백필 · 나머지 16제품 · 실사 수성잉크20+AQD3


- **08-18 그룹 연결손익 — `npm run diagnose:group` 신설 + 청주 재고 실사 반영** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-18 그룹 연결손익 — npm ru. 남은=**동산·선명 재고 미실사**(증감 0 취급) · 이자비용 실측분만 · 오다플래그·우회발행 미반영


- **★디자이너 PC 4대 공통 잔여** — `install-a0-panel.ps1` 미실행(인호동14·김보연8·김영주15·정소은16, 원격 수단 없음·사람이 실행) → 실행 전까지 해당 PC는 구 껍데기(고장 아님) · 전 PC 설치 확인 후 shim 제거 · A0 리모델(0.1.3)·work.ai 축2 반영분 포함

> 이전 배포 이력 전량 → `.claude/PROJECT_STATUS_ARCHIVE.md` (매 세션 읽을 필요 없음)

---

## 🔴 진행 중


- **★MES 실사용 전환 (전략 방향 확정 2026-08-11 — 자체 ERP+MES 일원화, 이카운트 단계적 해지)**: ★**「6월 2,348건 급락」은 오독**(08-25 실측 — 2,275가 `e2e_tester` 로봇, 사람 활동은 전부 `admin`). **급락이 아니라 시작된 적이 없다**. 직원 48명 vs 실직원 계정 6·로그인 3·업무기록 0. 살아 있는 건 **사람 입력이 필요 없는 자동수집 4종뿐**. 구조는 완비돼 있고 **데이터만 비어 있다**(단가표 11테이블 0건). **전환 관문 = 경리가 MES 에 매출을 직접 입력할 수 있는가**. 다음 = 잔여 단가 판단 → 경리 1명 병행입력 → 월말 채권 대사 3개월 오차 0 → 이카운트 해지. **목표선 = 관리회계까지. 복식부기·재무제표·세무신고는 만들지 않는다**(WEHAGO·세무사 유지). 경위·대응 = memory `project-employee-adoption-protocol` · 계획 `~/.claude/plans/greedy-exploring-stroustrup.md`(repo 밖)

> 착수 핸드오프 = `memory/session-context.md` (2026-08-11 — 5건 각각 다음 단계·착수 지점·주의 정리)

- **동산 이카운트 이관 — Phase 0 완료(3중 교차검증 오차 0·코드 커버리지 100%), Phase 1 대기**: 다음=세부점검 4종(①거래처 미등록 ②품목 매칭률 ③월별 금액 대사 ④법인간거래 중복)→승인→적재. 정본=`docs/dongsan-import/EXPORT-SPEC.md`(확보 파일 9개·매입은 ⏸보류 중)
- **IA 멀티소스 임포지션(모아찍기)**: ★S4(08-05)가 /ia-editor 페이지·스크립트를 삭제 — 웹 축은 `routes/workbench.ts`로 이관 생존. 남은=P4(에이전트 EPS 실저장·preview_only publish)만 유효(브랜치 머지 항목은 무효·잔재 정리 완료 08-10). spec=`2026-07-08-ia-editor-multisource-imposition.md`
- **분할청구(split billing) — P1~P4+◐P5 prod**: 다음=P5-continued(잔여 ~6파일 그룹화)→전량검증 후 레거시 컬럼 제거→P6 내부정산(다법인 실거래 발생 후). 핸드오프=`2026-06-10-split-billing-IMPLEMENTATION-PLAN.md`
- **품목 미등록 갭 — 즉시등록 영역 소진(0398~0422), 잔여 갭 6.4억=간판 사업부 전체**: 간판 BOM 1차(자재·원가)=08-01 완결 — 다음=2차 조립견적(아래 설계 대기와 동일 건)
- **LogWatcher 후속**: **EPSON 취소 실측 확정 완료(2026-08-11)** — 완료=12·취소=2(13/14 폐기), 진행중↔취소 구분은 정착 규칙 쿼리(뒤에 완료 잡 생긴 2만 수집·수거 DB 51건 실증). 파서(`89097982`)는 이미 main 포함(구 기록 "미포함"은 구식). config 2대(EPSON-01/02) 키트 준비 — **현장 [2] 실행만 남음** · 이희섭 6/8 펀치 1건 · 6월 선명 급여 재계산 필요

## 🟡 결정·확인 대기 (사용자)


- **재단 굽기 0.38.1 (축2 배포 대기)**: 「조각 N AOoC」=격자·폴백이 아트를 **아트보드 밖**에 놓았다(새 문서 아트보드=[0,h,w,0]) · 못 담으면 **문서 분할**. 게이트 `cut:e2e`. 남은=**Z: 배포+실기**
- **★뭉친 전표 청구서 3곳 요청(08-27 갱신)**: 수량 없는 매입 **71행 1.025억**. ★`notes` 전수 확인 결과 요청 대상은 **3곳** — NTIC 3,035만·에코컴퍼니 1,452만·유진프라스틱 1,284만 = **5,871만**(적요가 「금액 전용」이라 청구서가 유일한 길). 영광엔터 1,242만은 적요에 수량이 있어 **880만이 청구서 없이 풀렸다**. ★뭉침은 **`purchase_order_items.notes` 부터** — 바로는 4전표 중 3건이 거기서 풀렸다. 풀리면 깃대 계열 8품목 판매 4,851만의 원가 공백이 채워진다. 명단=`npm run report:lump` · 전문=memory [[project-lump-invoice-requests]]
- **외주가공 5건 1.90억**: 깃발외 임가공 1.37억·깃발 제작 2,770만·갈바 1,284만·가로봉 진공증착 650만·수기봉 도금 597만 — **용역**이라 품목 3종 어디에도 안 맞는다. 별도 축 필요
- **IA 도련 1.5mm 실기 확인**: 축1~5 배포·드리프트 0. ★1.5mm 는 업계 통상(2~3mm)·기계 공차(0.5~1.5mm) 하단 — **첫 판에서 실제 재단 밀림을 재고** 부족하면 간격 4mm(도련 2mm)로. 롤백=Z: `_backup/20260826-082420`
- **워크벤치 P1 노출**: 코드·권한 prod 배포 완료 — 메뉴 노출 여부 결정만 남음
- **급여 신현서 케이스 = 입력차이(코드 정상)**: 부양가족·차량유지비 비과세 입력 필요 → [[payroll-calc-ecount-diff]]
- **실검증 대기**: 수신번호 1개 확정 → 알림톡+MMS 실발송 검증 동시 해제(~400원)
- **자연검증(실사용 시 자동 소화)**: #310 직접발행 첫 건 · RIP 전송 현장 테스트 · 휴가→근태 첫 실휴가
- **동산 매입 계산서 축 부재(08-25 발견)**: `purchase_invoices` 213건이 **전부 선명** — 동산·청주 0건. `/accounting` 지출이 `purchase_invoices` 기준이라 동산 지출이 그만큼 과소로 보인다. (가)동산은 원래 미등록 운영인지 (나)6월 이관분을 아직 안 넣은 것인지 확인 필요

## 🆕 설계 확정 — 구현 대기

- **패널 재판정(08-26)**: ★「실사용 0=거절」 **철회** — 원인은 배포(축4 수동). ★완성 파일로 과정 규명 금지. ★출력완료 매칭 **0%**(8월 print_events 5,554건 중 0·작업파일 962건에 주문코드 0). ★`resolveCard` 자릿수 확장 **철회**(실제 파일명은 3자리·`rip_filename`은 TNS 잡 이름). ★흐름=**가공→주문서**라 키는 앞이 아니라 **흡수 시점에 붙인다**(별칭학습 선례 `workbench.ts:934`), 단 intake 파일명≠RIP 파일명이라 **패널 「작업폴더 내보내기」가 유일한 연결점**. ★판짜기 2조각+ 하루 3건·7조각+ 1.2건·월 12~15시간(★DXF는 `BLOCKS`/`INSERT` 풀어야 조각이 보인다 — 두 번 틀림). spec `2026-08-26-panel-flow-restructure`
- **채택 계기판(직원 요구 발굴 1부)**: 실사용 전환 관문을 눈으로 보는 지표판(하루 규모). ★`activity_logs` 를 채택 지표로 쓸 땐 **`user_id` 로 `e2e_tester`·`admin` 을 반드시 걷어낼 것**(안 걷으면 「6월 2,348건 급락」 같은 오독이 재발). 프로토콜 정본=`docs/superpowers/specs/2026-08-25-employee-requirements-protocol.md`
- **파일 규격 자동 판독(일러 불요, 08-24 「다」 확정)**: 업로드 시 헤더 파싱(EPS BoundingBox·PDF MediaBox)→주문서 규격 프리필+배율 {1,2,5,10} 역산+불일치 경고. P1~P5·함정(.ai 비호환=판독불가) = spec `2026-08-24-file-dimension-probe`. 별도 세션(`new-session.ps1`) 착수
- **품목·단가·재고 통합 개편(north-star·A안 채택)**: 다음=P1c(기존103 dedup·ecount매핑·활성화 — 바인드 버그는 해소됨)→P1d 단가배선(GRADE 룩업·㎡단가표)·기성 PRODUCT 토글
- **마감 스티커 A5(작업지시+검수)**: 설계·목업 확정·**코드 0**. ⛔프린터 구매→실물 컬러 테스트 전 착수 금지. 정본=memory `design-pack-sticker`
- **간판 조립견적(BOM 2차)**: 간판=구성요소 라인 합·calc_type 4종. 구성요소 전수 확정 선행. spec `2026-06-13-signage-component-estimate-structure`
- **LogWatcher 장비중심 P4**: '/rip' 권한 정리+prod 배포+LogWatcher 재배포. spec `2026-06-15-logwatcher-equipment-centric`
- **설계 확정 spec 7종(2026-06-11)**: alimtalk-golive · ontime-kpi · large-file-split · card-cashflow · hanjin-courier · self-order-portal · web-canvas-workbench — 전부 `docs/superpowers/specs/2026-06-11-*.md`
- **운영 결정(용준님 확정, 유효분)**: #377 활성화=`ia_auto_enabled` 플래그 게이트(기본 OFF) · SMS 대체문자 안 함 · 미수금 독촉=추천+수동승인 · 정적에셋 P0 재시도 금지(옵션 A PoC만) · **★실사용 전환 시 admin 계정 삭제+E2E 데이터 정리+평문 비밀번호 잔여 1계정 재설정(#626, 09-11 수용 결정)(트리거 잊지 말 것)**

## 🗄️ 보류함 (필요성 재확인 전 착수 금지 — 상세=ARCHIVE·spec)

- 원본 파일 아카이브(D1~D9) · 수신 파일 간편 편집기 · IA 세션루프 v2 · 배송 출고 대기 보드+알림톡 자동발송(option C) · 한진택배 자동화 · 기존 계약 엑셀 등록 · MMS 규격 상향(300KB로 운영 중)
- **⏸ 단가 이상 1건(09-09 보류 결정)**: `E1-20260429-I021`(2026-04-29 · 에이원기획(대전) · 선플란트치과) UV 포맥스 10T 백색 **58×90cm 1장 400,000원** — 규격은 `580x900mm` 로 맞는데 정가(48,100원/㎡ × 0.54㎡ ≈ 26,000원)의 **15배**다. 부자재·시공이 라인에 묶인 것으로 보이나 원천 전표(이관 2026-07-30) 확인이 필요하고 금액이 작아 **용준님 보류 결정**. ★건드릴 거면 `amount` 가 아니라 **라인 구성**을 봐야 한다 — 단가만 되나누면 그 값이 그대로 다음 견적 제안으로 나간다.
- **법인간 3% 소급(08-25 보류 결정)**: `E2-20260515-010`(선명→청주 700만) 전 라인이 매입가 그대로 넘어가 3% 미적용 — 소급 210,150원. 같은 주 `E2-20260513-011`(970만)에는 +3.1~3.9%가 붙어 있어 **주문 단위로 정책이 갈린 것**이 확인된 상태. 금액이 작아 용준님 보류 결정

## 🔒 편집 중 (충돌 방지)

- (없음) — iaEditor.js 잠금 해제: `2fe74b91` main 포함 + S4(08-05)에서 파일 자체 삭제, 2026-08-10 실측

## ⚠️ 잠복·블로커

- **품목 단가 전역(블로커)**: 원가 avg_unit_cost 315개 backfill 완료 — 남은=매출 base_price·무이력 514·자재비 소진연결 → [[project-item-pricing]]
- **✅ 생산현황 장비 31대 전수 진단(2026-09-22)** — ①TRANS-8C-02 규격 100% 결손 수정(형제 파서보다 엄격한 정규식+립 폴백) ②**HSM-05 원인 확정**: 설정 `C:TNSRip-X1`(2023-10-14 잔해) ↔ 실제 `D:TNSRip-X1`(당일) — **드라이브만 다른 같은 경로**. 새로 넣은 립 축 점검이 첫 실행에 짚었고 조건부 자동 교체까지 배선. ③HSM-01·06·07=**장비 결함으로 미사용**(실적 0 이 정상). 경위=ARCHIVE §09-22 장비진단. 남은=**HSM-05 에서 START.bat 재실행**(경로 자동 교체 적용) · 그 뒤 립 이름 붙는지 확인
- 핸드오프 정본=`memory/session-context.md` + [[project-workflow-master-plan]](Phase1~5 잔여)

## 📌 기존 에러

- (없음)

> 📦 완료 이력·다이어트 압축 전 원본 전문 = `PROJECT_STATUS_ARCHIVE.md`
