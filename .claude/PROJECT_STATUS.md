# PROJECT_STATUS.md — 프로젝트 현황판

> **운영 규칙(2026-08-10 확정)**: ①완료(✅)=아래 인덱스 1줄(제목+남은 것)만, 경위 전문은 `PROJECT_STATUS_ARCHIVE.md`에 직접 쓴다 (**「✅ 최근 완료」 항목 400자 상한** — 2026-08-19 게이트 강제, 초과 시 큰 것부터 지목) ②의미 없는 대기=보류함으로 과감히 이관 ③대기 항목=빠른 처리 우선 ④"커밋·미배포" 기록은 믿지 말고 prod 실측(deploy.yml=main push 자동배포). 게이트=`node scripts/doc-diet-audit.cjs`(훅·세션 시작 배너 연동).

> **✅ IA 축2~4 배포 2026-09-03 — 패널 검색이 공백을 무시한다(셸 0.10.0/0.71.0)** — 「한글이 안 써진다」의 정체 = 일러 CEP 가 **IME 조합을 웹뷰에 안 넘긴다**(composition **0건** · 마지막 글자를 스페이스로 확정 → 그 공백이 이름에 남는다). CEF 라 못 고치지만 **검색이 깨지던 원인**이 이것 — 제품 이름 **97%(254/263)가 공백** 포함. ★id 해소는 **원문 정확일치 우선**, 공백무시는 **후보 하나일 때만** · 부분일치 금지. 게이트 `panel:smoke` **130**·`cut:smoke` **462**. 경위=ARCHIVE §2026-09-03 IA. 남은 것 = 없음

> **✅ IA 축1 동기화 2026-09-03 — 리뷰수정 JSX 3종이 실행 중 에이전트에 미반영이었다** — `git push`도 `ia:deploy` 도 안 닿는 축(정본=실행 중 exe 폴더). 런타임 SheetLayout 이 **08-25 판**이라 8/26 도련 경계분할·mm 통일 + 9/3 리뷰수정이 빠져 있었다 — ★검증 3종이 **문서를 닫은 뒤** 돌아 catch 가 삼켜 **로그는 항상 「통과」** · 최상위 catch 가 `$.fileName` 로 로그 쓰다 실패 → `_ia_status` 미설정 → **「JSX 반환 빈값」 오진**. `--sync-agent` 반영, repo↔런타임 **sha256 4/4 일치**. .cs 무변경이라 재빌드·재시작 불요. 남은=**다음 모아찍기 잡의 SheetLayout 로그로 검증**

> **✅ 2026-09-03 키트 `b321417c` + prod 83쌍 정리 — KM 전사 이중계상 종결** — PrintExp 스탬프가 RIPLOG 출력시작보다 장비마다 일정하게 늦는데(전사3=**+7초**) 조인 창이 **5초**라 매번 깨져 유령+6h 폴백 = 같은 인쇄 2건(83쌍 중 **78쌍이 이 한 대**). ★내용키+FIFO 재설계는 **검증에서 기각** — 미인쇄 립이 큐에 쌓여 오귀속 4·16 · 내용키 검증은 정상 조인 거부 2·6 → 창 90초 **비대칭**(립이 앞선 방향만). 수거로그 재생=유령 13→0 · 전사2 28/28 무회귀. prod 병합=취소 **20건이 정상출력**으로 잡혀 있었다 · 카드 연결 **0건**이라 파급 없음. 남은=**3대 [2]**

> **✅ prod 2026-09-03 `1c5f5277`(→`3d309b48`) — S2 주문 라인 원가(BOM) + 잉크 방식별 + 분할 자동탐색 제거** — ★`total_cost` 22,973줄 전량 0 의 원인 = **`cost_standards` prod 0건**. ★★소요량이 `width`=폭 **고정**이라 86% 라인 폭·길이 뒤바뀜 → **2.5배 과소**(주간발주·부족체크가 사용). ★★★**분할 자동탐색 제거**(이음비 0 → 450×300이 900폭 **5분할**. 원단·분할은 영업이 고르는 축). ★잉크 **방식별**(수성189·전사89·솔벤1011·UV1174·태극기0) — 공통값이면 UV 7배 과소. ★**단가 0 자재 1종이 제품 전체를 면적기준 강등**(18제품) → `0555`. ★배포 직전 **코드 리뷰가 내 diff 8건**을 잡아 전부 선반영. ★★**게릴라가 전부 `AQ-BANNER` 로 들어가 있다**(`AQ-GERILLA` 판매 0라인 · AQD-090 매입 9,343만) → 재료비 **2,333만 과대**. 검증=smoke **114/114** · 백필 10,074주문 ERROR 0(라인 65.7%). 경위=ARCHIVE §2026-09-03. 남은=**게릴라 품목 재배선** · 단가0 46종 · 리뷰 CRITICAL 2건

> **✅ prod 2026-09-02 `2670f837` — 품목 연결 전수 점검(`0550`~`0553`)** — 패널 목록은 **이름 문자열만** 나가는데 폭 축으로 행이 갈려 같은 글자가 최대 24번 떴다(자재 334행=**135이름**) → 이름 기준 중복 제거. ★**2026-08-08 11:15~12:25 에 자재 9행이 PRODUCT 로** 등록돼 있었다(형제 전부 MATERIAL·매입 있음·주문 0) → 8행 정정 · 솔벤 텐트천 매핑 **수성→솔벤** 재배선 · 판매플래그 8행 · 과금방식 3행. ★미연결 767줄은 3중 대조 결과 **회수 0줄**(형제 라인이 반증), 원인은 **만국기 규격 공백**이었다 → 3종 신설 후 5줄 연결(767→**754**). ⚠️기존 중복 감사는 **두께가 다르면 「정상」**이라 이걸 못 잡는다. 경위 전문=ARCHIVE §2026-09-02. 남은=**판재 12종 기준단가**·아크릴/스카시 과금축·만국기 특호 168×112

> **✅ prod 배포 2026-09-01 `d1de3334` — 생산현황 실적 날짜 축 정정** — 점검에서 결함 2건. ①일별 차트는 `date(created_at)`=**UTC**, 같은 응답의 오늘 요약은 KST — **00~09시 출력이 전날 막대로** 갔다(당일 9건). ②둘 다 `created_at`(적재 시각) 기준 — 전 기간 8,982건 중 **1,792건(20%)** 이 출력일≠적재일, 최대 **894시간**, **08-10 하루 1,750건**(에이전트 최초 롤아웃 백필)이 그날 「오늘 실적」을 통째로 오염시켰다. ★식을 `utils/printEventDay.ts` 단일 소스로 뽑고(완료→시작→적재, 완료 결측 **0건**) 이미 옳던 호출부까지 흡수 — **베껴 쓴 식이 갈라진 게 원인**. 검증=오늘 요약 259 == 일별 첫칸 259(수정 전 249 vs 240)·prod smoke **113/113**

> **✅ prod 배포 2026-09-01 `1106198e` — 장비 그룹 축 = 출력방식(`0546`)** — `equipment_processes`·`constants/process.ts`·/equipment 공정 배지·필터가 6월에 다 만들어져 있는데 **테이블이 0건**이라 전 장비 「공정 미지정」이었고, 그 빈자리를 자유텍스트 `location_zone` 이 **EPSON-01 한 대만** 메워 그 한 대가 「출력실」 그룹으로 튀었다. ★근거로만 배정 — 현장이 [1] 에서 고른 공정(TOPM-01·HYB-3200=UV) + 파일명 소재. 수성17·전사7·솔벤4·UV3·**평판1**(`0547` 분리 — 재고는 UV 로 접어 둔다). **위치=`zone_id` / 출력방식=`equipment_processes`** 로 축 분리, 자유텍스트 은퇴(0건). 검증=drift 0·dom OK·prod smoke **113/113**·공정 미지정 **0**

> **✅ prod 배포 2026-09-01 `9ec2c454` — 에이전트가 키트 버전·파서 종류를 스스로 보고(`0545`)** — 「[2] 가 돌았나 / 어떤 파서로 도나」를 Z: 수거 폴더로 추측했고 09-01 에 **그 추측이 틀렸다**(폴더 비었는데 전환 3대·흔적 없는 1대). 수거 위치는 키트 실행 위치에 따라 갈린다(`$KitRoot`) — **폴더는 증거가 아니다.** heartbeat 에 `kit_version`·`parser_type`(런타임 실제 파서) 추가, upsert=`COALESCE`(구버전이 값을 안 지운다). 검증=drift 0·entity 0·prod smoke **113/113**·`/agents` 2컬럼 실측. 남은=**다음 [2] 부터 채워진다**

> **✅ prod 배포 2026-09-01 `0bf01c4e` — 주문서 이중입력 제거 3종(품목·단가 / 펀칭 / 법인)** — 대기함이 품목을 못 실어 라인마다 사람이 품목·단가를 다시 골랐다(prod 대기물 476건 중 주문 라인 연결 **0건**) → `/intake-config` 품목 818건 + 두 패널 품목칸(**정확일치만** — 부분일치는 틀린 단가를 싣는다). 펀칭은 PP 섹션이 **품목 종속**이라 행에 얹어 두고 섹션 생성 시 반영. 대기물 법인은 **주문에 붙을 때 확정**(술어=`order_item_id IS NULL` — 상태로 하면 「취소는 되는데 복구 불가」). 검증=CI 통과·prod smoke **113/113**·마커 4/4. 남은=**IA 축2·3·4 배포**(패널 품목칸)
> **✅ IA 축2·3·4 배포 2026-09-01 — 품목=제품만 · 품목→자재 파생 · 자재/후가공 목록을 config 로 · 수량 「조」 · 자작나무 6t 실면적(`0548`·`0549`)** — 품목을 `is_sales_item` 으로 거르니 **원자재가 섞였다**(축은 `item_type`). 자재는 `product_materials` 로 좁히고 매핑 없으면 자유 입력 유지. ★**자재·후가공 하드코딩 22개를 config 로** — MES 에서 고치면 배포 없이 따라온다. ★「조」=파일 1장→낱개 2장, 패널에서 **한 번만 ×2**. ★자작나무 6t 만 `min_billing_side_cm=0`(다른 판재는 규칙이 제각각이라 손대지 않았다). 검증=`audit:ia-jsx` **드리프트 0**(축1~5)·CDP 실측 화면 0.8.0/호스트 A0-CEP-0.5.0·재단 셸 0.69.0/CUT-CEP-0.30.0·품목 자동완성·자재 좁히기 334→1~2·「3조=6개」·굽기격자 0.25. ★축4 는 ping 자동동기화가 **PC 방문 없이** 따라왔다(재시작 2회). 게이트 `panel:smoke` **129**·`cut:smoke` **462**

> **✅ IA 축2·3·4 배포 2026-09-01 — 가공 자동감지의 「무제」 창 제거(`imageCapture`)** — 굽기용 임시 문서가 **창을 띄웠다**(2.4초). 캡처는 문서를 안 만든다(344ms). ★**아무 때나 못 바꾼다** — 캡처는 그 영역에 **그려지는 전부**를 찍어 **잠긴 개체·50mm↓ 노이즈**가 섞이고 해상도 하한이 **72dpi** 다 → `source=auto` + **그려지는 개체 수 == 후보 수** + 픽셀 예산, 셋 다 맞을 때만 쓴다. 두 마스크 동일성 실증(1456×416·잉크 320,000). 셸 0.8.0 에 포함되어 반영 확인


> **✅ IA 축2·3 배포 2026-08-31 `16bdcbd7` — 배율·회전이 배경을 날리던 문제 + 재발 방지 2종** — `resize`·`rotate` 둘 다 **불투명도 마스크를 두고 간다**(★1배 회전만 해도 배경 절반 회색) → 변형을 PDF 로 굳혀 **`embed()` 앞에서** 처리 · 기준을 **잉크 경계**로 통일(재단선 800 을 5mm씩 넘던 것 정정). ★**패널은 호스트를 열 때 1회만 읽는다**(배포≠반영, 반나절 소모) → Z: 버전 대조 경고 + **판짜기 차단**. ★게이트 **`cut:e2e`** 신설 — 픽스처로 실제 판을 짜 **마젠타 비율**로 판정(회귀 시험: 숫자 6개 통과·색만 실패). 호스트 0.29.0. 남은=**정소은 님 실사용 판정**


> **✅ 2026-08-19 훅 게이트 33.7% 무작동 수정** — 경위=ARCHIVE §훅 게이트. 남은=**커밋**


> **🗑️ prod 데이터 2026-08-13 — 8월 주문 510건 전량 삭제(용준님 요청)** — 백업 `_bak_0813_aug_*` prod 상주 · **롤백 SQL=`docs/analysis/2026-08-13-aug-orders-delete-rollback.sql`**. 경위 전문 = `PROJECT_STATUS_ARCHIVE.md` §2026-08-13 8월 주문 삭제. 남은=**재적재 여부 미정**


> **⛔ 2026-08-12 8월 주문서 420전표 적재 — 데이터는 08-13 삭제됨** (적재 경위·규칙·멱등·단가 함정 전문 = `PROJECT_STATUS_ARCHIVE.md` §2026-08-12 이카운트 주문 적재). 남은=재적재 시 8/12 이후분 재수출 필요


> **⛔ 08-12 8월 주문 소급 보정 + 파일 연결 582건 — 데이터는 08-13 삭제됨** (원문 = `PROJECT_STATUS_ARCHIVE.md` §2026-08-12 소급 보정). ★`POST /api/orders` 가 `created_at`·`order_number`·`status` 를 안 받아 **소급은 후처리가 유일**. 남은=정리 SQL 24건 미실행(`prune_links.sql`)

## ✅ 최근 완료 — 후속 대기 인덱스 (경위·상세 전문 = `PROJECT_STATUS_ARCHIVE.md`)

- **✅ 09-04 이카운트 판매현황 재이관(배포 `3c83857e`) — 7·8월 원천 대사 차이 0** — ★원천 교체(주문서현황은 **출고되면 사라진다**)→판매현황=거래처코드/품목코드/규격 별도열. 8월 **1,257전표 4억 6,803만**·7월 **3억 1,440만**(325곳 일치)·품목 미해소 **96→0**·규격 262라인 분해. ★★규격 한칸 회귀 검토→축은 **pricing_method**. ⛔EP115 중복 신설(계열열거 5번째). 경위=ARCHIVE §2026-09-04. 남은=**★출력↔주문 소급매칭**(카드 1,246 생겨도 매칭 0)

- **✅ 09-04 IA 축2·3·4 배포 — 재단 판짜기 2배속 + 사각 칼선·부스러기 제거(호스트 0.34.0 / 셸 0.74.0·0.11.0)** — 회전 굳히기가 **조각마다 임시문서**라 3.2초/조각 → 판당 격자 PDF 1장 + 회전값당 마스터 1개(한계 4.2→**0.2초**). ★사각 칼선 = **PDF embed 가 만든 사각 클립 2겹**(원본엔 없다·회전 조각만). ★칼선 131개 중 **15개가 0.01mm 부스러기**(칼이 제자리에서 오르내린다) → 0.1mm 하한. 경위=ARCHIVE §2026-09-04 IA 재단. 남은=**정소은 님 실사용 판정** · **0.35.0 미배포**(굽기 18.5→9.0초)

- **✅ 09-04 선명 실사 개통 + 잉크 용량축 정리(`0559`~`0562` prod)** — 선명 재고 **228행이 구역 미배정**이라 구역 실사가 INNER JOIN 으로 **0줄**이었다(실사 0회·원장 0행·4,695만 동결) → 기존 기본구역 `선명2` 배정 = **212줄**(음수 28). ★잉크테크 1L **519통은 선명 재고**·동산기획 0 — 「미검증」이 아니라 **범위 밖**. 티피엠 **18L=전표 오기**→20L 통합. 코스테크 혼재→**2L 이름 고정**(잉크 110종 `pack_size` 전부 NULL=용량은 이름에만). 아누코 35통 원장 동반 0. 경위=ARCHIVE §2026-09-04 잉크. 남은=**선명 실사 실행**

- **⏸ 09-04 8월 주문 재이관 — 적재했다가 롤백(원천이 8/11 스냅샷)** — `_bak_0813_aug_*` **DROP**(덤프=`backups/2026-09-04-aug-bak-dump/`). 425전표 적재해 원천 대사 **차이 0**·**카드 1→408**(주문 생성이 만든다)·원가 **80.2%** 를 실증했으나 Excel 이 8/11 수출본이라 이후 수정·취소 미반영 → 전량 삭제(고아 0). ★임포터는 전표 마커 멱등이라 **덮어쓰기 불가**=지우고 다시가 유일. ★미해소 8전표 **2,973만**(충남문화관광재단 1건이 2,727만). 경위=ARCHIVE §2026-09-04. 남은=**크롬으로 이카운트 최신본 수출**

- **✅ 09-03 base_unit 공백은 결함이 아니다 — 채우려던 작업을 취소(미배포)** — `unitConvert.ts:6`=**NULL은 unit과 동일**(설계된 상태). 공백 142종 중 **20종이 AQ* 47종**(pack_size 130=실사 편의 계수)과 겹쳐 채우면 원가 **459→59,670**. ★08-27 스윕은 **쓰기 5곳만**이었고 **읽기**(수익성·부문손익)는 밖 → 판정식을 `isMultiUom` 에 맞춤(prod 노출 0=예방). ★`audit:items` **F3 가 이미 띄우는데 참고 등급**. 게이트 §⑤(29항목). 경위=ARCHIVE §2026-09-03 base_unit. 남은=**배포**

- **✅ 09-03 prod 배포 — 전체 리뷰 결함 수정(11 worktree 오케스트레이션)** — CRITICAL 2·HIGH 89·MEDIUM 143 → 194건 수정·보류 4·백필 8,779건(에러 0)·검증 blocking 0. 경위=ARCHIVE §2026-09-03 리뷰수정. 남은=**IA 축2·3·4 ia:deploy**·storageZones 후속

- **✅ 09-03 전체 코드 리뷰(Fable 5.1) — 코드 0 수정** — 게이트 20종 전부 통과 · 에이전트 20개(보안 3·Area2/4·QA·슬라이스 9·/code-review) → **CRITICAL 2·HIGH 89·MEDIUM 142**, HIGH 이상은 메인이 소스 재검증. ★C1=포털·직원셀프 토큰이 내부 API 통과(auth.ts:22+permissions.ts:54) ★C2=purchaseInvoice.ts:6 무인증 반사 XSS. 정본=`docs/audits/2026-09-03-full-review.md`(+상세 15파일). 남은=**수정 묶음 1(인증 경계)부터 착수 여부 결정**

- **✅ 09-02 S2 주문 라인 원가(BOM 기반) — 미배포** — ★`total_cost` 전량 0 = **`cost_standards` prod 0건**. ★★소요량이 `width`=폭 **고정**이라 86% 라인 폭·길이 뒤바뀜 → **2.5배 과소**. ★★★**분할 자동탐색 제거**(이음비 0→900폭 5~7분할 폭주·원단은 영업이 고르는 축). ★잉크=**방식별**(수성189·전사89·솔벤1011·UV1174). ★**단가 0 자재 1종이 제품 전체를 면적기준 강등**(18제품·7개는 원가 0)→`0555`. 게이트 25건. 남은=배포+`0554`+`0555`+백필 · 나머지 16제품 · 실사 수성잉크20+AQD3

- **✅ 09-01 MES 진단 51R + S0 prod 실측 (6판)** — ★**거래처 13곳 월 7,750만 감소**(연 9.3억·신규가 상쇄해 총액엔 안 보임)=최우선·개발0 · ★간판 월 **−2,026만**·**정리는 이탈 13.4%면 손해**(원스톱 73곳) → 유지 · ★주문 22,973라인 **원가0·에누리0** · ★원단 **47만㎡** 격차=S1 첫 과제 · S1 대상=**수성 현수막**. 전문=`docs/analysis/2026-08-31-adoption-diagnosis.md`. 남은=S0.5 이탈 회수

- **✅ 08-31 백로그 9건 소진 (배포 완료)** — entity 격차 4 + 화면 미연결 3 + #620 오진 판정 (**D1 버그가 아니라 값의 공백**) + #608 카나리를 deploy.yml 로. 경위=ARCHIVE §2026-08-31 백로그. 남은=**#616·#617 LogWatcher 실기·Z: 미배포** · #613


> **✅ 하네스 2026-08-31 — 자동압축 때 조용히 사라지던 세션 맥락에 스냅샷 훅** — `PreCompact`(공식 이벤트 32개 중 우리는 5개만 썼다)로 압축 직전 transcript 백업 + 브랜치·미커밋·최근커밋 마커를 남기고, 압축 후 첫 프롬프트에서 `userpromptsubmit` 이 1회 주입·소비한다. **PreCompact stdout 은 디버그 로그로만 가서 모델에 안 닿는다** — 잔소리형 훅은 원리적으로 불가. 백업 최근 5개 유지·gitignore. 남은 것 = 없음(다음 압축 때 실측).


- **✅ 08-31 직배 납품시간 = 오전/오후 2택 + 완료기한 파생 (`0544`·배포 `f7454bce`)** — ★완료기한은 납품시간과 **다른 축**: 오전편 = **전날 18:00** 이라 시각만 담는 `delivery_time` 에 자리가 없다. 슬롯만 저장하고 마감은 파생(`utils/productionDeadline.ts` ↔ `shared/deliverySlot.js`) · 칸반 마감·긴급도·정렬을 마감 기준으로 교체 · 오전편은 전날 접수건만(UI 가드). 게이트 `test:delivery-slot` 67항목. 경위=ARCHIVE. 남은=**현장 실사용 판정**
- **✅ 08-31 간판 견적 화면 — 리플레이 + 동종 계산기 벤치마크 (`session/sign-estimate`·미배포)** — 막히던 5곳(직접입력·이름·순서·**가산은 단가에**·**규격 mm→㎡**) 수정 후 DSPSIGN 구조로 **사양을 한 줄 한 축**으로 폄. ★그게 **시드의 축 뭉침**을 드러냄 — LED `KPL/전구색3000k` 는 **금액이 틀리고 있었다**(선택이 어느 행에도 안 맞아 0원·25,000 누락). 아크릴·돌출간판도 분리. 지주간판 신설 → 커버리지 **100%**(203/203). ⛔제작방식 10종=실측 0건 미채택. 게이트 42+8항목. 근거=spec 부록 F. 남은=**주문서 병합**·0544~0547 미적용

- **✅ 08-28 미연결 24행 「연결 안 함」 종결** — 수량매입 18.12억의 **0.96%**·재고 0·새로 설 원가는 판매 84만어치뿐인데 반대편은 붕괴 위험: 「솔벤원단 S+250 90×5,050」의 5,050 이 `SVM-250` **M당 단가**(90=롤 아니라 M) → 붙이면 4,500M 로 나눠 **57배 저평가**. 벽이 ⓐ품목 자체 없음(7곳 **연결이력 0**) ⓑ축 다름뿐이라 **연결로는 못 넘고 선결은 납품명세서**. 감사엔 애초에 안 뜨므로(뭉침=`quantity=1`) 태그도 안 심음. 판정=`2026-08-27-unlinked-qty-lines-link.sql` §종결
- **✅ 08-28 매입 실명세 요청 팩 20곳 = `docs/analysis/2026-08-28-invoice-request-pack.md`** — ★요청이 **두 종류**다: A=품목 연결됐고 금액만 뭉침 → **품목별 명세**(69행 9,204만) / B=품명조차 모름(「실사원단」·「각관」·「N월 거래건」) → **거래처원장**(170행 1.64억, 받아도 품목 신설이 선행). ★1순위는 금액이 아니라 **깃대 계열** — 매입 0건이라 원가 0인 판매품목 8개 **4,851만**. ⛔엘포 1,825만=지급 역산 라인이라 **청구 불가**. 남은=발송
- **✅ 08-27 원가 backfill 이 시한폭탄이었다 — 돌렸으면 재고 평가액 6,147만→30억** — `backfill_avg_cost.sql` 이 **base 리베이스(08-19/20)를 모른다**. `poi.quantity` 는 롤 수인데 원가 축은 yd/M → 원단 43품목이 **정확히 pack_size 배**. 뭉침 라인도 분모에 1 로 들어가 +16~32%. → `×packFactor` + 수량 라인만. ★하드코딩 제외명단 **폐기**(오늘 채운 4품목을 도로 0 으로 만들 참). 게이트 `audit:avgcost`
- **✅ 08-27 원가 정합화 24품목 — 라운드 수동값을 매입 실적으로** — 잉크 14가 전부 12,000·11,000·200,000 이었다(가중평균이 우연히 그럴 수 없다=손입력). 매입 3~6건 쌓인 지금은 실적이 정본 — `RM-I0057` 12,000→**13,773**(11% 낮게 잡히던 것). 재고 평가액 **+157,048**(e1 6,147만의 0.26%·예측과 실측 일치). ⛔코드 손나열 대신 조건식+백업. 어긋남 36→**12**=뭉침만 8(대상 아님)+오늘 갱신 4(타 세션 존중). 경위=ARCHIVE
- **✅ 08-27 미연결 뭉침 분류 + 수량라인 연결 — 2.21억→1.64억** — 갈래① 5,675만 제거(장비 3,780만 연결 · 리스/수선/용역 7행=`notes` **`[분류:]` 태그**). ★키워드 손목록 대신 태그 — 근거가 데이터에 남고 **없으면 목록에 남는다(fail-open)**. ★★수량 27행 중 **2행만 연결**: 「SBO20 M137 100ea@5,480」이 `FLEXD-137` **M당 단가와 일치** = 그 100 은 롤이 아니라 M. **미연결 수량이 관리단위라는 보장이 없다**. 경위=ARCHIVE. ✅남은 24행은 08-28 **연결 안 함**으로 종결
- **✅ 08-27 뭉침 보고서 사각지대 — `JOIN items` 가 219행 13.7억을 말없이 잘랐다** — 메모리에 적힌 함정을 보고서가 그대로 밟았다. 대부분 **구조적 이월**(27행 11.4억=전표가 아니라 잔액)이라 제외는 맞지만 **말없이 자르면 9,204만이 「전부」로 읽힌다**. → `LEFT JOIN`+분류, 감사·보고서 둘 다 출력
- **✅ 08-27 「영광엔터」 적요 분해 — 1,242만 중 1,038만 해소** — ★「수량 있는 적요 2건」이라 본 게 전수로 펼치니 **7건**. 세무장부 적요는 거래명세서 **대표품명 1줄만** 싣는다(적요분<공급가가 정상) → 규약=「수량줄+(미상)잔액줄」. 단가 교차확인 22,000·35,000=수량라인 존재, 11,000=적요 단일근거. 전표합계 불변식 diff 0. **판매품목인데 avg 0** 3건 충전(`ACC-051`·`ACC-053`·`ACC-054-NS`). 뭉침 1.025억→**9,204만**. 롤백=`docs/analysis/2026-08-27-yeonggwang-lump-split.sql`. 남은=**청구서 3곳 5,871만**+영광 잔액 204만
- **✅ 08-27 「바로」 전표 4건 전량 분해 + 환봉나무 3규격 + 단위 라벨 409건** — ★★**답이 이미 `poi.notes`(세무장부 적요)에 있었다**(3건 즉해). 신설 90cm·70cm·**25파이(350원)**. ★**정수로 떨어져도 증거가 아니다**(2회 오판). ★기본값 ROLL→라벨 **yd** 409건+게이트 **F4**. ★03-24 **로프 누락 481만 등록**·`ACC-038` 35,000→**37,233**. 경위=ARCHIVE §08-27 바로. 남은=**07-22 잔액 462만**(08-28 좁힘: 재단로프 125개로만 정수분해 — 적용 보류·**7월 원장** 요청)
- **✅ 08-26 prod 데이터 — 잉크 단위 L→「통」 전면 통일**(용준님 확정·코드 0) — 잉크는 **자동차감이 없어** L 로 들 실익 0. ★매입이 원래 병 개수 단위 ⇒ **통 단가=매입 단가**. 126품목 축 통일·엡손 수량 ÷1.5·R50 92,000(/L)→184,000(/통). ⚠️재주문점·실사이력도 같이 환산(안 하면 허위 부족경고). 평가액 −161만. 롤백=`docs/analysis/2026-08-26-ink-unit-unification.sql`. **TPM 18L 4품목 통합·하드삭제**. 남은=명칭 점검 후속

- **✅ 08-25 D1 실행계획 붕괴 — prod 최초 ANALYZE + 쿼리 재작성 + 렌더 개편 (배포 완료)** — ★prod 에 `sqlite_stat1` 이 **없었다**. 정본=CLAUDE.md §D1 실행계획 · 게이트 `audit:query-cost` · 경위 전문=ARCHIVE. 후속 3트랙은 아래 항목에서 이어감


- **✅ 09-01 [2] 현장 일괄 — HSM 13대 중 11대가 2축, 침묵 2대 부활** — 자동전환 **8대**(01·04·05·08·09·10·11·13, `RIC_` 접두사 포함). ★**소급 오염 방어가 실제로 발동**: HYB 79.2MB>68.8MB·SOLV 97.3MB>83.9MB → 0 이 아니라 **파일 끝 정렬**, 7일 초과 적재 **0건**. 13일 침묵하던 HYB-3200-01·SOLV-3200-01 이 **첫 실적**(HYB 취소 1건 포함). 남은=**출력실2 [2] 미실행**·HSM-07·TPM 3대 → [[design-probe-cancel-test]]
- **✅ 08-31 Z: 키트 `4a7917c3` 배포 — 밀기 전 점검에서 3건 잡음** — ①**소급 오염 차단기** `max_event_age_days`(7일): 위치 어긋나면 2~3년치가 카드를 뒤집는다(재생 20,217건 중 **20,184건이 7일 초과**)·HYB/SOLV-3200 은 prod 이벤트 **0건**이라 멱등키 무방비였다. ②**2축 파서가 둘째 축 없다고 멈추던 것**을 립 폴백으로 강등. ③PS5.1 이 BOM 없는 UTF-8 JSON 을 ANSI 로 읽어 **자동전환이 조용히 실패**하던 경로. 남은=**[2] 실행** → [[design-probe-cancel-test]]
- **✅ 08-31 HSM-04 [1] — 9대 문제의 답: PrintExp 가 있었다** — `RIC_PrintExp_X64_V5.7.6.5.90.BS`. 08-25 센서스는 루트가 `C:\TNSRip-X1` 뿐이라 못 봤다. 3라운드 = R1 OK(**립 2초 vs 실인쇄 23초**)·R2 CANCEL·**R3 유령 OK** → `tns_printexp` 그대로. ★키트 자동전환 글롭 `PrintExp*`→**`*PrintExp*`**(RIC_ 접두사를 통째로 놓쳤다). 나머지 9대는 **[2] 한 번이면 판정**(admin-install.log 회수) → [[design-probe-cancel-test]]
- **✅ 08-31 UV-3200 2축 편입 — Flora 레코드 크기 자동판별** — Flora 기록은 **기종마다 크기가 다르다**(평판 2376B / UV-3200 **2248B**) — 꼬리 3필드가 128B 앞으로 밀린다. 끝 기준 역산 + 매직 간격 자동판별로 고침. 재생 A/B: 평판 **바이트 동일**, UV-3200 15,218건=15,065+40+113 정확. UV-3200 취소가 `Print.log` 기준 **0.33% → 18.8%**(수거본으로 확정 — [1] 불요). 남은=**키트 재빌드·[2] 4대** → [[design-probe-cancel-test]]


- **08-18 그룹 연결손익 — `npm run diagnose:group` 신설 + 청주 재고 실사 반영** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-18 그룹 연결손익 — npm ru. 남은=**동산·선명 재고 미실사**(증감 0 취급) · 이자비용 실측분만 · 오다플래그·우회발행 미반영

- **08-14 손익 재산출 — 진단 도구가 통장 비용을 안 쓰고 있었다 (`scripts/finance-diagnose.cjs`)** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-14 손익 재산출 — 진단 도구가. 남은=리스료 1.69억 금융리스 여부


- **08-14 선명(e2) 비용 분리 — 이중계상 없었다** — 카드 소급 신규 0건 · 엑셀 미보유 74건은 전부 2025년분 · **카드 가맹점 ↔ e2 매입처 19곳 교집합 0**(선명은 매입을 전부 계좌이체로 — 동산은 23곳 4,279만 겹침). 통장 잔여 12건+동산 8건 판정해 prod 적용(`_bak_0814_lastmile`·`_bak_0814_e2card`): 하나생명=보험료·우리금융캐피탈=리스료·「다성정밀」=**다선정밀 표기오차**(id 528)·과입금 반환=가수금. 남은=**티피엠_장비대금 3,617,000** 미확정 · 선명 2025년 카드 74건 적재 여부


- **08-12 자재원가 배선 3단(코드 0·prod 직접) — 상위20 완비 8→12품목·매출 커버 10.0억→13.38억(57.3%)** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-12 자재원가 배선 3단(코드 0·. 남은: **BOM 자체 없음**(1호 450×300·특호 3종 = 원단 규격 미매입 5,743만 · 좌우보필액자 2,956만) · 수량 없는 1식 2종(`GDS-FB1-CASE` 에코컴퍼니 1,452만 · NTIC 액자) — ⚠️`ACC-034` 깃대는 08-27 해소(avg 11,000)
- **08-12~13 통장·카드 비용 성격 확정 → 통장 미분류 71.5%→0.7%(114건 3,553만)·카드 미분류 0건** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-12~13 통장·카드 비용 성격 확정 →. 남은: 통장 109건 3,318만 — **개인명 인건비 다수**(김민규·문찬미·양동규알바비·윤봉현·권용현) · `KB오픈(주)` 700만 · 정운비원단 269만 · 미등록 10곳 등록 판단 · NH농협카드 804만 성격 판정(대표 개인계좌 출금) · ⚠️바로빌 통합포인트 1,984원 소진 임박


- **08-11 미수금 경고 FIFO 재정의 + 대손 명단 정본화 prod** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-11 미수금 경고 FIFO 재정의. 남은: **대손 상각 4곳 268,500,615 실행(세무 협의 후 `adjustments`)** — 오프라인광고 178,505,080·바로디자인(법인) 54,951,410·동산기획청주(717) 21,224,250·마루&아라 13,819,875, 결정은 전부 `clients.notes` 기록됨. 선명 후보 2곳(세이광고·그리미)은 **거래 재개로 철회**(잔액 증가) → [[design-ar-overdue-fifo]]
- **08-11~12 Z: 작업파일→주문서·카드 자동생성 = 성립, 배선·병행 안전장치까지 완료**(정본 `docs/order-file-matching/` · 초보자용 `HOW-IT-WORKS.md` · 경위 전문 `PROJECT_STATUS_ARCHIVE.md`) — 유효 커버리지 58.8%·전량일치 76.7%·개입 「확인만」 46.5%. 남은: 파일명 규칙 배포(용준님) · 매핑표 15종 33건 판단 · 전사 품목 자동인식 · **0532·0534 prod 적용+배포**
- **08-11 LogWatcher 현장키트 1차 가동**(`1793f6ac`~`30c977c7`·`Z:\Designs\LogWatcher-kit`) — 5대 업데이트(KM전사1~3·8색 2대) 전수 검증·KM전사4=INACTIVE·코스테크-전사-01 등록+config 준비(현장 [2] 실행만 남음). 남은: 수성·간판(선명) 진단 · 레거시 구빌드 PC [2] 순회 (솔벤3200 분석·EPSON 수거·신규 6대 설치는 08-12 완료)

- **08-11 품목 단가 배선 3종 + base_price 백필 155품목 prod** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-11 품목 단가 배선 3종 + ba. 남은: **의도적 보류 29품목(간판=BOM 조립견적으로 풀 문제·운임=거리별) · N 표본부족 165 · X 이력없음 387**(근거=`docs/pricing/derived-prices.csv` 등급 컬럼에 사유 기재) → [[design-order-line-billing-rules]] · [[feedback-imported-unit-price-semantics]]
- **08-11 예산 감시 통합(바로빌 잔액 + Cloudflare 사용량) prod** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §08-11 예산 감시 통합(바로빌 잔액. 남은(용준님): **🔴바로빌 잔액 1,984원 — 충전 필요**(마르면 계좌·카드 수집 cron 이 조용히 실패) · 대시보드 Billing usage alert(지출 하드 상한 기능 자체가 없어 앱 감시로 대체 불가) → [[design-nav-badge-cost-guard]]
- **08-08~08-01 재단 패널·재단선 모델(host 0.20.0·shell 0.55.0)** — 남은: 플로터/CNC 실기 투입 미검증 · MIN_HOLE_MM 물리 근거 · 라운드 조각 공유 변(라벨 맵) · 패널 이식 2건(A0→재단 클리핑·재단→A0 경계정확도, 확정·미착수) · 칼선 파일 가드(잉크<5%, 미결정) · `mesCut_rasterizeItem` 임시문서 재사용
- **★디자이너 PC 4대 공통 잔여** — `install-a0-panel.ps1` 미실행(인호동14·김보연8·김영주15·정소은16, 원격 수단 없음·사람이 실행) → 실행 전까지 해당 PC는 구 껍데기(고장 아님) · 전 PC 설치 확인 후 shim 제거 · A0 리모델(0.1.3)·work.ai 축2 반영분 포함
- **08-08 목록 UX 이카운트 갭 P1-1~4**(`39f16b50`) — 남은: 반품 2단계(등록 UI=/quality 기존) · `orders.is_voucher` · 분석탭 명세 칩 · CSV 없는 목록 5개(조회조건 SSOT 빌더 선행 — WHERE 사본 5벌 금지) · `entity-attribution-audit` UI(제품 결정)

> 이전 배포 이력 전량 → `.claude/PROJECT_STATUS_ARCHIVE.md` (매 세션 읽을 필요 없음)

---

## 🔴 진행 중

- **★재고실사 실행 + 불편 접수 개설 (08-25)** — 경위 전문=`PROJECT_STATUS_ARCHIVE.md` §2026-08-25 재고실사 · memory `project-employee-adoption-protocol`·`design-weekly-stock-count`. ★병목=옮겨적기 · ★부족경고 판정축=**`reorder_point`**(`safe_stock` 은 판정에 안 쓰인다) · ★차단점=`inventoryCount.ts:10` 한 줄 · ★법인별 취급축=`inventory` 행 · ★**일일 알림 4종이 아무에게도 안 보이고 있었다**(`target_role=MANAGER` 인데 MANAGER 계정 0명 → ADMIN 예외). ✅08-25 완결·배포 `d64a15c3`(5구역·전사 재고 첫 반영 21,652yd·한국엡손 뭉침 4전표 분해·safe/reorder 48행). 남은=카톡방
- **★MES 실사용 전환 (전략 방향 확정 2026-08-11 — 자체 ERP+MES 일원화, 이카운트 단계적 해지)**: ★**「6월 2,348건 급락」은 오독**(08-25 실측 — 2,275가 `e2e_tester` 로봇, 사람 활동은 전부 `admin`). **급락이 아니라 시작된 적이 없다**. 직원 48명 vs 실직원 계정 6·로그인 3·업무기록 0. 살아 있는 건 **사람 입력이 필요 없는 자동수집 4종뿐**. 구조는 완비돼 있고 **데이터만 비어 있다**(단가표 11테이블 0건). **전환 관문 = 경리가 MES 에 매출을 직접 입력할 수 있는가**. 다음 = 잔여 단가 판단 → 경리 1명 병행입력 → 월말 채권 대사 3개월 오차 0 → 이카운트 해지. **목표선 = 관리회계까지. 복식부기·재무제표·세무신고는 만들지 않는다**(WEHAGO·세무사 유지). 경위·대응 = memory `project-employee-adoption-protocol` · 계획 `~/.claude/plans/greedy-exploring-stroustrup.md`(repo 밖)

> 착수 핸드오프 = `memory/session-context.md` (2026-08-11 — 5건 각각 다음 단계·착수 지점·주의 정리)

- **동산 이카운트 이관 — Phase 0 완료(3중 교차검증 오차 0·코드 커버리지 100%), Phase 1 대기**: 다음=세부점검 4종(①거래처 미등록 ②품목 매칭률 ③월별 금액 대사 ④법인간거래 중복)→승인→적재. 정본=`docs/dongsan-import/EXPORT-SPEC.md`(확보 파일 9개·매입은 ⏸보류 중)
- **IA 멀티소스 임포지션(모아찍기)**: ★S4(08-05)가 /ia-editor 페이지·스크립트를 삭제 — 웹 축은 `routes/workbench.ts`로 이관 생존. 남은=P4(에이전트 EPS 실저장·preview_only publish)만 유효(브랜치 머지 항목은 무효·잔재 정리 완료 08-10). spec=`2026-07-08-ia-editor-multisource-imposition.md`
- **분할청구(split billing) — P1~P4+◐P5 prod**: 다음=P5-continued(잔여 ~6파일 그룹화)→전량검증 후 레거시 컬럼 제거→P6 내부정산(다법인 실거래 발생 후). 핸드오프=`2026-06-10-split-billing-IMPLEMENTATION-PLAN.md`
- **품목 미등록 갭 — 즉시등록 영역 소진(0398~0422), 잔여 갭 6.4억=간판 사업부 전체**: 간판 BOM 1차(자재·원가)=08-01 완결 — 다음=2차 조립견적(아래 설계 대기와 동일 건)
- **LogWatcher 후속**: **EPSON 취소 실측 확정 완료(2026-08-11)** — 완료=12·취소=2(13/14 폐기), 진행중↔취소 구분은 정착 규칙 쿼리(뒤에 완료 잡 생긴 2만 수집·수거 DB 51건 실증). 파서(`89097982`)는 이미 main 포함(구 기록 "미포함"은 구식). config 2대(EPSON-01/02) 키트 준비 — **현장 [2] 실행만 남음** · 이희섭 6/8 펀치 1건 · 6월 선명 급여 재계산 필요

## 🟡 결정·확인 대기 (사용자)

- **★뭉친 전표 청구서 3곳 요청(08-27 갱신)**: 수량 없는 매입 **71행 1.025억**. ★`notes` 전수 확인 결과 요청 대상은 **3곳** — NTIC 3,035만·에코컴퍼니 1,452만·유진프라스틱 1,284만 = **5,871만**(적요가 「금액 전용」이라 청구서가 유일한 길). 영광엔터 1,242만은 적요에 수량이 있어 **880만이 청구서 없이 풀렸다**. ★뭉침은 **`purchase_order_items.notes` 부터** — 바로는 4전표 중 3건이 거기서 풀렸다. 풀리면 깃대 계열 8품목 판매 4,851만의 원가 공백이 채워진다. 명단=`npm run report:lump` · 전문=memory [[project-lump-invoice-requests]]
- **외주가공 5건 1.90억**: 깃발외 임가공 1.37억·깃발 제작 2,770만·갈바 1,284만·가로봉 진공증착 650만·수기봉 도금 597만 — **용역**이라 품목 3종 어디에도 안 맞는다. 별도 축 필요
- **IA 도련 1.5mm 실기 확인**: 축1~5 배포·드리프트 0. ★1.5mm 는 업계 통상(2~3mm)·기계 공차(0.5~1.5mm) 하단 — **첫 판에서 실제 재단 밀림을 재고** 부족하면 간격 4mm(도련 2mm)로. 롤백=Z: `_backup/20260826-082420`
- **워크벤치 P1 노출**: 코드·권한 prod 배포 완료 — 메뉴 노출 여부 결정만 남음
- **급여 신현서 케이스 = 입력차이(코드 정상)**: 부양가족·차량유지비 비과세 입력 필요 → [[payroll-calc-ecount-diff]]
- **실검증 대기**: 수신번호 1개 확정 → 알림톡+MMS 실발송 검증 동시 해제(~400원)
- **자연검증(실사용 시 자동 소화)**: #310 직접발행 첫 건 · RIP 전송 현장 테스트 · 휴가→근태 첫 실휴가
- **동산 매입 계산서 축 부재(08-25 발견)**: `purchase_invoices` 213건이 **전부 선명** — 동산·청주 0건. `/accounting` 지출이 `purchase_invoices` 기준이라 동산 지출이 그만큼 과소로 보인다. (가)동산은 원래 미등록 운영인지 (나)6월 이관분을 아직 안 넣은 것인지 확인 필요

## 🆕 설계 확정 — 구현 대기

- **✅ 08-26 셸 자동 갱신 — 축4가 스스로 따라온다(5축→2축)** — `mesA0_ping`→`mesPanel_syncShell`. ★트리거가 셸이 아니라 **ping** 이라 구 셸 PC 도 붙는다(PC 방문 불요). 규칙 정본=CLAUDE.md §IA 스크립트. 게이트 `cut:shellsync`. 남은=없음
- **패널 재판정(08-26)**: ★「실사용 0=거절」 **철회** — 원인은 배포(축4 수동). ★완성 파일로 과정 규명 금지. ★출력완료 매칭 **0%**(8월 print_events 5,554건 중 0·작업파일 962건에 주문코드 0). ★`resolveCard` 자릿수 확장 **철회**(실제 파일명은 3자리·`rip_filename`은 TNS 잡 이름). ★흐름=**가공→주문서**라 키는 앞이 아니라 **흡수 시점에 붙인다**(별칭학습 선례 `workbench.ts:934`), 단 intake 파일명≠RIP 파일명이라 **패널 「작업폴더 내보내기」가 유일한 연결점**. ★판짜기 2조각+ 하루 3건·7조각+ 1.2건·월 12~15시간(★DXF는 `BLOCKS`/`INSERT` 풀어야 조각이 보인다 — 두 번 틀림). spec `2026-08-26-panel-flow-restructure`
- **채택 계기판(직원 요구 발굴 1부)**: 실사용 전환 관문을 눈으로 보는 지표판(하루 규모). ★`activity_logs` 를 채택 지표로 쓸 땐 **`user_id` 로 `e2e_tester`·`admin` 을 반드시 걷어낼 것**(안 걷으면 「6월 2,348건 급락」 같은 오독이 재발). 프로토콜 정본=`docs/superpowers/specs/2026-08-25-employee-requirements-protocol.md`
- **파일 규격 자동 판독(일러 불요, 08-24 「다」 확정)**: 업로드 시 헤더 파싱(EPS BoundingBox·PDF MediaBox)→주문서 규격 프리필+배율 {1,2,5,10} 역산+불일치 경고. P1~P5·함정(.ai 비호환=판독불가) = spec `2026-08-24-file-dimension-probe`. 별도 세션(`new-session.ps1`) 착수
- **품목·단가·재고 통합 개편(north-star·A안 채택)**: 다음=P1c(기존103 dedup·ecount매핑·활성화 — 바인드 버그는 해소됨)→P1d 단가배선(GRADE 룩업·㎡단가표)·기성 PRODUCT 토글
- **마감 스티커 A5(작업지시+검수)**: 설계·목업 확정·**코드 0**. ⛔프린터 구매→실물 컬러 테스트 전 착수 금지. 정본=memory `design-pack-sticker`
- **간판 조립견적(BOM 2차)**: 간판=구성요소 라인 합·calc_type 4종. 구성요소 전수 확정 선행. spec `2026-06-13-signage-component-estimate-structure`
- **LogWatcher 장비중심 P4**: '/rip' 권한 정리+prod 배포+LogWatcher 재배포. spec `2026-06-15-logwatcher-equipment-centric`
- **설계 확정 spec 7종(2026-06-11)**: alimtalk-golive · ontime-kpi · large-file-split · card-cashflow · hanjin-courier · self-order-portal · web-canvas-workbench — 전부 `docs/superpowers/specs/2026-06-11-*.md`
- **운영 결정(용준님 확정, 유효분)**: #377 활성화=`ia_auto_enabled` 플래그 게이트(기본 OFF) · SMS 대체문자 안 함 · 미수금 독촉=추천+수동승인 · 정적에셋 P0 재시도 금지(옵션 A PoC만) · **★실사용 전환 시 admin 계정 삭제+E2E 데이터 정리(트리거 잊지 말 것)**

## 🗄️ 보류함 (필요성 재확인 전 착수 금지 — 상세=ARCHIVE·spec)

- 원본 파일 아카이브(D1~D9) · 수신 파일 간편 편집기 · IA 세션루프 v2 · 배송 출고 대기 보드+알림톡 자동발송(option C) · 한진택배 자동화 · 기존 계약 엑셀 등록 · MMS 규격 상향(300KB로 운영 중)
- **법인간 3% 소급(08-25 보류 결정)**: `E2-20260515-010`(선명→청주 700만) 전 라인이 매입가 그대로 넘어가 3% 미적용 — 소급 210,150원. 같은 주 `E2-20260513-011`(970만)에는 +3.1~3.9%가 붙어 있어 **주문 단위로 정책이 갈린 것**이 확인된 상태. 금액이 작아 용준님 보류 결정

## 🔒 편집 중 (충돌 방지)

- (없음) — iaEditor.js 잠금 해제: `2fe74b91` main 포함 + S4(08-05)에서 파일 자체 삭제, 2026-08-10 실측

## ⚠️ 잠복·블로커

- **품목 단가 전역(블로커)**: 원가 avg_unit_cost 315개 backfill 완료 — 남은=매출 base_price·무이력 514·자재비 소진연결 → [[project-item-pricing]]
- **HSM-05·HSM-07 실적 0건(잠복)**: 1.1.0 에이전트가 매분 heartbeat 는 보내는데 `print_events` 가 **한 건도 없다**(전 기간). HSM-01 도 8월 1건뿐. 2축 3대(02·03·06)는 취소 6~7%인데 단축 7대는 **전부 0%** — [2] 때 같이 확인.
- 핸드오프 정본=`memory/session-context.md` + [[project-workflow-master-plan]](Phase1~5 잔여)

## 📌 기존 에러

- (없음)

> 📦 완료 이력·다이어트 압축 전 원본 전문 = `PROJECT_STATUS_ARCHIVE.md`
