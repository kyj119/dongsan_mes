# PROJECT_STATUS.md — 프로젝트 현황판

> **✅ prod 데이터 적재완료 (2026-08-10 #72, 계정 기준 매입 스윕 — 253·533·530·520 판정 + 원재료(153) 전량, commit `c40e76ba` — ★코드 변경 0·배포 없음·prod D1 데이터만)**: #69(껍데기 84건 종결)의 후속. **①잔액 대사만으로는 원리적으로 못 보는 구멍을 메웠다** — 잔액은 「아직 안 갚은 것」만 보여 주므로 **기중 발생·기중 완제 매입은 통째로 안 보인다**. 그래서 잔액이 아니라 **계정**으로 훑었다. **②253 미지급금 233,520,182 = 매입 성격 0건**(금융성·고정자산·공과금·개인·옛 이월·테크원 −52.8M 역분개) → 적재 대상 없음. **③533 외주가공비 15곳 175,849,800 전량 적재.** **④530 소모품비 95곳 24,821,373 · 520 수선비 12곳 4,444,619 = 경비라 발주로 넣지 않는다**(판정 근거를 문서에 남김 — 뒤집으려면 이 줄이 기준). **⑤153 원재료 스윕 완결** — 배치 A~J 로 **65곳 · 90,747,810 적재(99.7%)**. 검산 = 90,747,810 + 선명 271,029 = **91,018,839** 로 갭 문서 총액과 원 단위 일치. 남은 1곳 **(주)선명커뮤니케이션 271,029 는 법인간거래라 의도적 제외**(발주 아님 · 회계허브 정책). **⑥새 함정 3가지** — **같은 거래처 전표가 153 과 530 에 걸쳐 있다**(소액 철물·볼트·전기자재 구간 전반, 거래처명으로 "자재상"이라 뭉뚱그리면 경비가 매입에 섞인다) · **적요 「A X B」 순서가 거래처마다 다르다**(경원GH=수량×단가, 삼도직물=단가×수량 — 곱은 같아도 배정이 뒤집히므로 확실한 것만 실수량) · **인용 헬퍼 `q()` 에 null 을 넘기면 NULL 이 아니라 문자열 `'null'` 이 된다**(신설 4곳 대표자 오염 → `material_153_rep_fix.sql` 로 교정·생성기 수정, 타입체크가 못 잡는 종류). **⑦★진척률을 `notes LIKE '%원재료(153)%'` 로 세면 안 된다** — 껍데기 채우기 발주에도 같은 문구가 있어 93,992,810/224건으로 부푼다. 정본은 배치 spec 의 expect 합. 무결성 = entity 1 **껍데기 0 · 라인합 불일치 0 · VAT 불일치 0 · 고아라인 0**. ⚠️**용준님 조치 3건** = 정운교역 6·7월 청구서 확보(지메일 0건·네이버 차단) · 진안알미늄 07-17 전표 50001 삭제(세무 담당 — 삭제 후 251 잔액 −222,900·부가세대급금 −616,100) · 케이엠테크 07-31 전표 50016 장비 정산 확인. 근거·재현 = `docs/analysis/2026-08-07-sales-cost-analysis.md` §8-Z-15~27 · `docs/dongsan-import/MATERIAL_153_GAP.md`


> **✅ prod 결함복구 (2026-08-10 #71, ★일괄 회계반영 잠복 장애 + 마이그 감사 전수 전환 + `db:migrate:prod` 차단 — 코드 변경 없음·배포 없음)**: #70 감사 도구를 **전수(0001~)로 넓히자마자 실제 결함이 나왔다**. **①`PATCH /api/orders/bulk-bill` 이 prod 에서 100% 실패 상태였다** — `orders.receipt_type` 에 쓰는데 그 컬럼이 없었다. 사슬 = **0189 가 컬럼 추가 → 0262 가 orders 재빌드하며 누락 → 코드는 계속 참조**. `/tax-invoices` 회계반영 버튼 2경로(`taxInvoices.js:1098`·`:1131`)가 모두 이 UPDATE 를 타므로 누르는 즉시 batch 전체 실패. **아무도 못 느낀 이유 = BILLED 10,073건이 전부 이관분**(`billed_at` 전부 09:00:00 · 마지막 07-31) → UI 회계반영이 아직 미사용. **첫 실사용에 바로 터질 상태였다.** **②0189 prod 적용 완료** (ALTER + INDEX 2문 · 10,073행 보존 · `receipt_type` 전량 NULL 정상). bulk-bill 의 UPDATE 원문을 무매칭 행(`id=-1`)에 실행해 컴파일까지 확인. 스모크 110/110. **③★감사 기본값을 「추적 밖」 → 「전수」로 변경** (`272bc11e`) — **`d1_migrations` 는 0189 를 적용됨으로 기록하고 있었다**. 즉 추적이 틀렸고, 추적을 믿고 창을 좁히면 이 결함을 영영 못 본다. 원격 질의 횟수는 동일하고 커버리지만 **465 → 3,322 객체**. **④`npm run db:migrate:prod` 차단** (`scripts/no-migrate-prod.cjs`) — 실행하면 미추적 219개를 prod 에서 재실행한다(DELETE·비멱등 INSERT 포함 → 데이터 삭제·중복). 대안 절차(감사 → `execute --file -y` → 재감사)를 메시지로 안내하고 exit 1. **⑤추적 동기화는 하지 않기로 결정** — 데이터 마이그 142건은 후속 마이그가 같은 데이터를 다시 바꿔(0325 가 넣은 P-0001 을 0341 이 삭제 · 0322 분류를 0332 가 8개로 단순화) 사후 검증이 원리적으로 불안정하고, 무엇보다 추적 자체가 틀려서 맞추는 게 **잘못된 신뢰만 늘린다**. (파일 자신의 가드를 뒤집는 마커로 73건은 적용 확인 · 69건 판정불가)

> **✅ prod 회귀방지 도구 등록 (2026-08-10 #70, 마이그레이션 드리프트 감사 — ★코드 변경 0·배포 없음)**: #68 의 prod 장애(0528 미적용 → `bank_accounts.is_overdraft` 참조 → `/api/bank/accounts` 500)를 **다시 못 내게** 만든 축. **①왜 아무도 못 잡았나** — `d1_migrations` 추적이 0313 에서 멈춰 있고(이후 219개는 `execute --file` 직접 적용) 타입체크·빌드·스모크는 SQL 스키마를 모른다 → 스키마 축을 볼 도구가 아예 없었다. **②`npm run audit:migration-drift`** = 파일 **목록**이 아니라 **결과물**을 본다. 마이그레이션의 CREATE/DROP/RENAME 을 번호순 시뮬레이션해 "최종적으로 있어야 할 스키마"를 만들고 실제 `sqlite_master` 와 대조. 재빌드 임시 테이블(`x_new`·`_bak_*`)은 시뮬레이션이 스스로 걷어내 오탐 0. 창은 `d1_migrations` 마지막 추적분 **다음 번호부터 자동**(번호 하드코딩 없음 → 추적 정상화되면 창이 저절로 좁아짐). **③현재 prod = 드리프트 0** (219파일·객체 465개 대조). **④자가검증 완료** — 없는 컬럼·인덱스·테이블을 요구하는 임시 마이그레이션을 넣어 3종 전부 탐지·exit 1 확인 후 삭제(항상 통과하는 게이트는 무용). **⑤`/deploy-verify` Phase 2-B 로 편입**. ⚠️**직전 보고 정정** — `idx_storage_zones_facility` 를 "실질 드리프트 1건"으로 보고했으나 **드리프트가 아니다**: 0448 이 `storage_zones` 를 DROP·재빌드하면서 인덱스도 함께 사라졌고(SQLite 정상 동작) 파일도 그걸 다시 만들지 않는다 → **prod 는 파일과 정확히 일치**. 되살리려면 수동 CREATE INDEX 가 아니라 **새 마이그레이션 파일**이어야 파일↔DB 가 안 갈린다(대상 5행·라우트 필터 없음 → 실익 없음).

> **✅ prod 데이터 적재완료 (2026-08-10 #69, 매입 껍데기 84건 전량 종결 + 전 거래처 AP 대사, commit `47711390` — ★코드 변경 0·배포 없음·prod D1 데이터만)**: **①통장 출금 역산 껍데기(`E1-PO-BK-*`) 283M/84건 → 0건.** SmartA 거래처원장을 정본으로 21+8곳을 전표 단위로 재구성. 무결성 = entity 1 발주 **껍데기 0 · 라인합≠공급가 0 · 공급가+VAT≠합계 0 · 고아라인 0**. **②원장 읽는 법이 바뀌었다** — SmartA 는 RealGridJS **캔버스**라 화면 스크롤로 훑으면 놓친다(오다플래그 129M 을 「장부에 없다」고 오판했다). `Grids.getActiveGrid().getDataSource()` 로 전량 직접 읽는다. 「잔액」탭+계정과목 251+거래처 비움 = **전 거래처 잔액표 한 방**(거래처칸은 키보드로 안 지워짐 → `location.reload()`). **③AP 대사 24곳 중 22곳 원단위 일치, 미설명 불일치 0.** 남은 2건은 원인 확정 — **정운교역 +155,985,385 = 계산서 3~4개월 지연발행(정본은 MES)** · **진안알미늄 −6,777,100 = 장부 07-17 전표 50001 중복**. **④새 유형 4가지** — 통장 역산은 지급의 **하한**(상계·현금·이관후분은 안 보인다, 오다플래그 21.4M) · 매입·지급이 **같은 금액으로 동시 누락**되면 AP 가 우연히 맞는다(NTIC·현대상사) · **전기이월 지급을 매입으로 역산**하면 그 해 매입이 부푼다(바로 9.6M → `E1-PO-OPEN-*`) · **251 미경유 즉시결제**는 잔액에도 통장에도 안 남는다(화성프로세스·우승상사). **⑤적요 「수량 X 단가」는 맞을 때만 실수량**으로 넣고, 한 품목에 실수량·금액전용이 섞이면 backfill 제외. 거래처 신설은 **사업자번호로 전수조회** — 동아디자인·보영텐숀은 이미 있었고(상호만 달랐다) 대보미싱만 신설. ⚠️**용준님 조치** = 정운교역 6·7월 청구서 확보(지메일엔 없음·네이버 차단) · 진안 07-17 전표 50001 삭제(세무 담당)

> **✅ prod 배포완료 (2026-08-10 #68, 기간 셀렉트 상태 일원화 + ★prod 장애 복구(마이그 0528), push `9c30fb14`·배포 `b41c9107`)**: **①기간 셀렉트와 날짜가 서로 다른 말을 하던 것** — 셀렉트는 「직접입력」인데 날짜칸엔 값이 있었다. ⚠️**내가 #66 에서 「#65 의 미해결」로 보고한 건 오진이었다** — prod(프리셋 0건)는 처음부터 정상이었고, 로컬의 테스트 프리셋 「긴급 VF」가 원인이었다. 다만 **프리셋 보유자에겐 실재하는 문제**다: 복원 경로가 둘(초기 localStorage · 툴바 프리셋)인데 각자 규칙을 갖고 있었고 **프리셋이 나중에 적용되며 초기값을 덮었다**. 기간 도입 전 프리셋엔 `datePeriod` 가 없어 셀렉트가 「직접입력」으로 떨어지고 날짜는 **저장 당시의 고정값**으로 되돌아온다 — 그 값은 저장 시점 기준 1개월이라 시간이 갈수록 창이 넓어진다(실측 33일). → 규칙을 **`ordNormalizeDatePeriod` 한 곳**으로: 개월 있으면 오늘 기준 재계산 / 없고 to 도 없으면 = 기능 이전 **자동** 기본값이 굳은 것이라 기본 기간 승격 / from+to 둘 다면 사용자가 고른 범위라 보존. **②「날짜 초기화」가 재진입 때 되살아나던 것** — 키를 지우면 「제한 없음」과 「저장값 없음」이 구분되지 않는다 → `all` 마커를 명시 저장(직후 `loadOrders` 가 지우지 않도록 저장 분기도 3상태로). 검증 = 신규·기능이전값·의도적범위·초기화·3개월을 각각 **재진입까지** + 옛 프리셋(기간만 정규화, 검색·긴급 필터는 불변)·새 프리셋(datePeriod=6). **③★배포 중 prod 장애 발견·복구** — `/api/bank/accounts` **500**. 내 변경과 무관하고 **타 세션 커밋 `c001936d`(마이너스통장 분리)이 `bank_accounts.is_overdraft` 를 쓰는데 마이그 0528 이 prod 미적용**이었다. 코드는 원격 main 에 있어 **내 배포에 실려 나가며 표면화**됐다. 용준님 승인 후 `0528` prod 적용(ALTER ADD COLUMN·데이터 무손실) → PRAGMA 로 컬럼 확인 → API 200 → **prod smoke 109/110 → 110/110** 회복. ⚠️**교훈 = 코드는 push 로 남이 배포하지만 마이그레이션은 따라가지 않는다.** 스키마를 요구하는 커밋을 push 할 땐 prod 마이그를 같은 호흡에 돌리거나, 최소한 배포자가 알 수 있게 남겨야 한다. **검증** = tsc·build·entity-audit 0·sort-audit P1 0·check-dom OK·로컬 smoke 110/110·**prod smoke 110/110**·prod 마커 4/4·페이지 4종 200(콘솔 0)·prod 시나리오 4종 실측

> **✅ prod 배포완료 (2026-08-10 #67, 자금현황 대출 분리 + 마이너스통장 분리집계 + 대출·카드 점검, commit `c001936d`·배포 `b41c9107`(#68 동반)→최종 `e24fb7a4`)** ※타 세션 작성 · **배포와 마이그 0528 적용은 #68 세션이 수행**(코드가 원격 main 에 있어 동반 배포됐고, 마이그 미적용으로 `/api/bank/accounts` 500 이 나 복구): 용준님 요청 5건. **①/bank 자금현황 개편** — KPI 3→4분할(총 계좌잔액=**마이너스통장 제외 예금**·마이너스통장 사용액·대출 잔액·순자금) + **대출 현황 표 신설**(기관·번호·원금·잔액·월납입·금리·만기·만기미확인 배지, 읽기전용 + 「대출 관리 →」 링크). fund-summary 가 loans 목록·deposit/overdraft 분리합을 반환(하위호환 total_balance 유지). **순자금 정의 불변**(마통은 잔액 음수로 이미 반영이라 전체합−대출 그대로). **②마이그 0528** `bank_accounts.is_overdraft` — 계좌 등록/수정 모달 체크박스. ⚠️**배포 시 할 일**: `db:migrate:prod`(0528) + **prod 마킹 1건** `UPDATE bank_accounts SET is_overdraft=1 WHERE account_number='60291004389904'`(e1 하나 마이너스통장, 실잔액 −495,725,564 — 이게 총잔액에 섞여 예금이 4.96억 과소로 보였다). **③대출 점검** = 활성 17건 2,067,615,858 · 세무장부 대사값(08-05)과 **원 단위 일치**. 발견 = **original_amount 16/17건이 계약원금이 아니라 등록시점 잔액**(진행률 전부 0%로 보임·자금예측 무영향) · 만기미확인(mc0) 9건 placeholder 2027-07-31 · **우리금융캐피탈 만기 2027-10-20 은 실제(2026-04 도래·보증금 45,600,000 상계 순유출 ~1,780)와 다름 — BULLET 이라 2027-10 에 4,560만 가짜 유출 예측** · 73142 회전형 잔액은 세무장부 293 주기 갱신 필요 · 8월 회차 16건 전부 SCHEDULED([납부] 처리 미사용, 8/7 경과 4건도 미처리 — 이자만이라 잔액 왜곡 없음) · **★선명(e2) 차입금 0건 등록**(통장엔 대출 원리금 출금 16건·보증서담보대출 계좌 존재 — 선명 세무장부 대사 미실시). **④카드 결제일 점검** = e1 BC·하나 pay15 실측 일치 ✓ · e2 BC pay10 일치 ✓ · **★e2 하나카드 3장 pay15 등록인데 실출금 7개월 연속 10일(주말보정 일관) = 오등록 의심**(결제예정·자금예측 5일 어긋남, 명세서 확인 후 수정) · **★미등록 카드 4장+** = e1 전북은행 3장(끝 7988/7996/9243·22일 결제·월 최대 1,282만)+NH농협(15일) — corporate_cards 에 없어 사용내역 수집 사각 · cutoff_day 는 통장으로 검증 불가(명세서 필요, 전 카드 cut2 복붙 의심 — e2 BC 만 cut25). **⑤spec** = `specs/2026-08-10-entity-expense-management.md` — 양법인 통장+카드 지출 관리 P1(미등록 카드·결제일 정정·e2 recurring 스윕)~P4, 원칙=카드대금 이중계상 차단·자동매칭 정밀도 유지. **검증** = tsc·build·check:dom·audit:entity 0·sort-audit P1 0·**로컬 smoke 110/110**·로컬 D1 실측(fund-summary 분리합·PUT 토글·키 미포함 유지). **⑥spec P1 실행완료(당일 후속)** = prod 카드 등록 id 13~15 전북(7988/7996/9243·pay22·cutoff NULL=명세서 대기)+16 농협(pay15) · e2 하나카드 3장 **pay15→10 정정**(실출금 7개월 근거) · '전북' 카드사 옵션+JBBANK 바로빌 맵(`1d92e806`) · **기장료 counterpart 오타 정정**(강민구→강세구정민수세무 — 자동매칭 불가 상태였음) · e2 반복지출 스윕 → **미등록 후보 = 신운식 월 2,200,300 정액(성격 미상·세무장부 확정 대기)·SKT 월 ~14.7만**, CNCITY전기=변동 제외, 대출계좌 출금 2건(60298019970342/60298022057942)=선명 차입금 대사 실마리. **⑦배포(당일)** = push `e0150651` → 마이그 0528 prod 적용 시도가 duplicate column — **#68 세션이 장애 복구로 이미 선적용한 것**(PRAGMA 로 확인, ⚠️처음엔 wrangler 재시도로 오진) → deploy `e24fb7a4` → **마통 마킹 3행**(60291004389904) → **prod smoke 110/110** · fund-summary 실응답(**예금 77,033,063 / 마통 −495,725,564 / 대출 17건 2,067,615,858**) · 페이지 마커 7종 **별칭==배포URL 동일 확인** · '전북' 옵션 마커 OK · 페이지 9종 200. 콘솔 수집은 미실시(SPA 인증 fetch 검증으로 대체). **세션 최종(당일 후속 7배포, 최종 prod 1c61d60f)** = 신운식 임대료(9월 종료)·SKT 통신비 e2 고정비 등록 · 선명 보증서담보 70,000,000 등록(e2 1호)+73142 잔액 WEHAGO 293 실측 갱신(191,167,098) · **e1 카드 7장 전부 바로빌 연동 완결**(전북 3장 — 재등록 차단 버그 2건 근본수정: 앱 중복검사 비활성 포함 + **DB UNIQUE(last4,entity) 0249 가 비활성 행까지 차단 → 재활성 UPDATE**) · 대출 탭 🪙 잔액 갱신 버튼+기준일(직원 셀프 갱신) · 구매자금대출 관리=(가) 월1회 갱신 확정 · **★print-events 홍수 근본수정**(★D1 LIKE 패턴 50바이트 한도 실측 → substr 접두일치, 밀린 출력완료 1,963건/30분 회수 — memory feedback-d1-like-pattern-limit). **남은 것** = 선명 05805 대출 등록(은행 캡처 대기) · 우리금융캐피탈 정산 문의 · 현대캐피탈 87러6814 확인 · D1 LIKE 전역 감사. 상세 = memory/session-context.md #67

> **✅ prod 배포완료 (2026-08-10 #66, 혼재 주문 작업지시서 라인 섹션 + 검색 딥링크)**: #64 후속. **①「작업지시서 기반이 뭐냐」 점검** — 혼재 주문(출력+전사+간판+상품 4라인)을 실제로 만들어 확인: **카드는 라인별로 3장**(출력/전사·태극기/간판), 상품은 카드 미생성 + `shipment_ready=1`(제작 대상 아님 = 설계대로). 지시서가 **3개 공존**하고 기반이 갈려 있었다 — `/cards/:id`(카드)·「작업지시서」인쇄(**주문**)·「봉제작지」(카드). **②카드 모달의 「작업지시서」가 어느 카드를 열든 주문 전체를 찍었다** → 용준님 선택 = **주문 기준 유지 + 라인별 섹션**. `■ 출력 / ■ 전사·태극기 / ■ 간판 / ■ 상품(제작없음·출고만)` 로 나누고 섹션마다 그 라인에서만 의미 있는 지시만(봉제 4종은 전사 전용, 출력은 마감·후가공·축척). 상품 섹션은 지시 없이 동봉 안내, 항상 맨 뒤. ★**라인 귀속을 프론트에서 재구현하지 않는다** — 카드에서 역으로(`order_item_id → card.category_name`) 읽는다. 카드그룹 판정은 서버(`getCardGroup`)가 정본이고 카테고리·기성품 여부·item_type 이 다 필요해 프론트에서 다시 짜면 반드시 갈린다. 카드에 안 담긴 라인 = 출고만 섹션. **③★「봉제작지」 버튼이 영구 미노출이었다** — 조건이 `'전사'`·`'TRANSFER_FLAG'` 완전일치인데 저장값은 **`'전사/태극기'`**. 정작 그 양식이 필요한 라인에서만 안 떴다. ⚠️앞선 검증에서 내가 함수를 콘솔로 직접 호출해 확인한 탓에 놓쳤다 — **호출 가능 ≠ 사용자가 누를 수 있음**. **④출력 카드 체크리스트가 「봉제(2면열재단)」** — 열재단은 봉제가 아니다. 라인별 `봉제`/`마감` 분기(helpers.ts). 화면 표는 #64 에서 갈랐는데 체크리스트 라벨이 남아 있었다. **⑤전역검색 딥링크 마무리** — #64 에서 카드만 상세화했던 것을 주문 `?view=<id>`·견적 `?view=<id>` 까지. `?view` 를 읽는 곳이 이미 둘 있었는데(승인·재가공 후 모달 새로고침) **초기 진입 처리기가 없어** 링크로 들어오면 목록만 떴다 → 양쪽 페이지에 초기 처리기 추가. **⑥타 세션 날짜필터(#65)와 같은 체크아웃 충돌** — `orders.js`·`shell.js` 를 공유해 한때 커밋 분리가 불가했다. 그 세션이 먼저 커밋·배포(`b66a45b9`/`8c54f791`)해 rebase 로 흡수됐고 내 배포는 그 위에 얹힌다. ★대기 중 **내가 그 기능을 실클릭 검증**해 뒀다(3개월 선택→from 재계산·to 비움·재조회, 손수정→기간 해제, autocomplete off 2곳, 콘솔 0) — 그 세션은 브라우저 점유로 못 했다고 배너에 적었다. ⚠️**셀렉트 기본값이 「직접입력」으로 뜨고 from 만 채워진다** — #65 의도(기본 1개월 노출)와 다름, **미해결로 넘김**. **검증** = tsc·build·entity-audit 0·sort-audit P1 0·check-dom OK·**로컬 smoke 110/110** + 혼재 주문 실측(섹션 4개·상품 지시 없음·마감 라벨 라인별·봉제작지 버튼 전사에만·체크리스트 「마감(2면열재단)」)

> **✅ prod 배포완료 (2026-08-10 #65, 주문관리 날짜 필터 — 자동채우기 억제 + 상대 기간 프리셋, commit `b66a45b9`·배포 `8c54f791`)**: 용준님 제보 2건. **①특정 PC에서 주문일 from 클릭 시 브라우저 자동채우기가 flatpickr 달력을 가림** — `.js-fp` 가 `type="text"` 라 크롬이 그 PC의 과거 입력값 드롭다운을 띄운 것(PC마다 입력 이력이 달라 특정 PC에서만 재현). 페이지별 패치가 아니라 **공용 초기화 `hrInitDatePickers`(shell.js)에서 전 `.js-fp` 에 `autocomplete="off"`** 부여 — 날짜 달력 쓰는 전 페이지 일괄 해결, flatpickr CDN 미로드여도 적용. **②「기간」 셀렉트 신설**(`/orders` `#ordDatePeriod`) — 직접입력·최근 1/3/6개월(반기)/1년. ★핵심 = 고정 날짜가 아니라 **상대 기간으로 저장**: '조건 저장' 프리셋·localStorage(`orders_filter_date_period`)에 개월 수가 남고 `ordApplyFilters`/초기 복원이 **적용하는 날 기준으로 재계산**(지난달 저장한 "최근 3개월"이 오늘도 최근 3개월). 날짜 손수정 = 기간 자동 해제(`ordDateManualChange` — 안 풀면 다음 방문 때 재계산 값이 손수정을 덮는다), 끝일은 비워 선주문 포함, 칩 라벨 「주문일 최근 3개월 (날짜 ~)」, 기본값(최근 한달)도 셀렉트에 '1' 로 노출. **③★멀티세션 격리 배포** — 메인 체크아웃에 타 세션의 미커밋 WIP(전역검색 `?view=` 딥링크 + `routes/orders/helpers.ts`·`cards/detail.js` 진행 중)가 공존해 **worktree `session/date-filter` 로 제 수정만 재적용해 빌드·배포**(dirty WIP 휩쓸림 차단 — multi-UOM 0395 패턴 회피). fc.exe 대조로 worktree↔메인 차이가 타 세션 훅 3개뿐임을 확인. **검증** = node --check·check-dom·tsc·build·entity-audit 0 · 로컬 smoke 110/110 · **prod smoke 110/110** · **prod 마커 실측 6/6**(SPA 인증 fetch — 셀렉트·핸들러·재계산 코드) · 별칭이 새 배포를 서빙(autocomplete 마커로 판정) · 페이지 5종 200. ⚠️메인 체크아웃 작업트리에 같은 내용의 수정이 남아 있음 — 타 세션이 커밋 전 `git pull --rebase` 하면 동일 내용이라 충돌 없이 정리됨

> **✅ prod 배포완료 (2026-08-10 #64, 담당자 범위·대기함 프리필·작업지시서 라인 분기·진입동선 A~E, push `95223984`·배포 `76624062`)**: 용준님 지시 4건. **①담당자 = 디자이너+관리자** — `employees` 재직자 전원을 뿌리던 셀렉트를 **사용자 관리(users) 역할 기준**으로 좁혔다. 규칙은 서버 상수(`ORDER_SALES_REP_ROLES`)+전용 엔드포인트 `GET /api/orders/sales-rep-options`(job_role 우선 → employees 로 환원). prod 실측 **정확히 5명**(김보연·김영주·인호동·정소은 + 김용준). 데모(`디자이너`)·테스트(`123`) 계정은 employees 미연결이라 자동 제외. ⚠️**★좁히면 과거 담당자가 옵션에서 사라진다** — 옵션에 없는 값을 select 에 넣으면 조용히 `''` 가 되어 **수정 저장마다 담당자가 지워진다**(0523 때 겪은 함정의 재발 지점). `?include=` 로 현재 담당자를 합류시키고 「(현재 담당)」 표기 — 로컬에서 후보 밖 담당자(9003)가 붙고 값이 유지되는 것까지 실측. **②가공대기함 프리필이 빈 라인을 대체** — `ofIntakePrefillOne` 이 무조건 `addItemRow()` 라 **빈 라인1이 매번 남았다**. `claimItemRow`/`isItemRowEmpty`(itemRow.js) 로 재사용(판정 = 품목·검색어·내용·대기물 전부 빈 것 — 규격/수량은 기본값이라 제외). 묶음(부모+자식)은 구조상 재사용 불가 → `dropOneEmptyItemRow`. **③작업지시서가 봉제 양식 하나로 전 라인에 찍히던 것을 분기** — 봉제방법·하도매·부직포·수술은 **전사/태극기 전용**, 출력·간판은 **원단·마감·후가공·축척**. ★출력의 후가공(코팅·아일렛 등)은 그동안 **화면 어디에도 없었다**(봉제 3종 PP만 뽑고 있었다) → `extractPPList` 신설. **★규격 단일화** = 우측 대표 규격(첫 품목 기준 「35×50」이 무엇의 치수인지 모호)을 제거하고 **시안 바로 아래 한 곳**(단위 cm·크게). 대표값은 다품목에서 오작업 위험이라 표에도 규격 열을 두지 않았다. 부수 = 하도매 표기를 체크리스트와 통일(`5호2구`→`5호 2구`)·수술 행 빈 칸 제거. **④진입 동선 A~E** — 점검 결과 병목이 하나였다: **찾기는 되는데 카드 한 장에 도달을 못 한다**(링크 목적지가 전부 목록). A 스캔 `CARD→/cards/:id` · B 전역검색 카드결과→상세 · C 칸반에 「지시서」 버튼(3클릭→1클릭) · D 인쇄물에 **그 카드**의 QR+카드번호 · E 주문 상세에서 카드 1장이면 「작업지시서」 직행. ⚠️**★A 를 넣다가 더 큰 걸 발견** — `/api/scan` 이 레거시 `CARD-{날짜}-{seq}` 만 인식하는데 카드번호는 이미 `{주문번호}-NN`(`E1-20260810-001-01`) 체계라 **현장에서 카드 QR 을 찍으면 UNKNOWN 이었다**. 패턴을 또 추측하지 않고 **card_number 동등 조회**로 판정(유일키가 정본) — 주문번호는 ORDER 로, 품목코드는 ITEM 으로 계속 정확히 갈리는 것 확인. 부수로 다품목 표 `overflow-x`(#611, 태블릿 768px 에서 본문이 밀리지 않게 · 인쇄에선 해제). **검증** = tsc·build·entity-audit 0·sort-audit P1 0·check-dom OK · **로컬 smoke 110/110 + prod smoke 110/110** · 로컬 브라우저로 A~E 전 경로 실측(칸반 버튼 카드당 1개·검색 결과 `/cards/:id`·스캔 4종 분기·인쇄 QR 겹침 0·주문 모달 직행) · **prod 실측**(마커 16/16 — 별칭==배포URL · 페이지 5종 200·JS 에러 0 · **담당자 셀렉트에 실제 5명 노출**). ⚠️prod `cards` 0건이라 카드 렌더 경로는 로컬 E2E 가 정본. **남은 것** = 전역검색의 **주문·견적 결과는 여전히 목록**(카드만 상세화) · 실사용 카드 생긴 뒤 봉제작지 실물 대조 1회

> **✅ prod 배포완료 (2026-08-10 #63, 작업지시서 결함 13건 근본 수정, push `b00e4ce4`·배포 `a71e51eb`)**: 「이미지가 안 나온다」 제보에서 출발한 전수 점검. **①★봉제작지가 통째로 공백 출력** — `detail.js` 가 `card._items` 를 읽는데 **단건 카드 API 는 `items`** 로 준다(목록만 `_items`). 캡처 결과 `<div class="design-area"></div>`·작업수량 **0EA**·원단/규격/봉제/하도매 전부 `-`. 방어코드(`items || _items`)가 소비처마다 흩어져 있던 게 뿌리 → **접근자 SSOT `window.cardItems(card)`**(shell.js) 신설하고 전 소비처(cardDetail·detail·core 21곳) 통일. ⚠️지역변수 `cardItems` 가 전역 접근자를 가리는 함정 → `sewItems` 로 개명. **②★주문 수정이 출력중 카드를 파괴** — 보존 판정이 **상태 열거**(`PRINT_DONE/HOLD`+rip)인데 **PRINTING 이 빠져** 있었다. 재현: 카드 PRINTING·체크 2/6 에서 **비고 한 줄 수정** → 카드 삭제·재생성(id 7→8), 상태 **PRINT_PENDING 역행**, 체크 이력(=감사 로그 정본) **전량 소실**, `/cards/7` **404**(봉제실 QR·열어둔 탭 사망), `needs_reissue` 도 **0**. RIP 를 안 거치는 봉제·간판은 `rip_status` 로도 안 걸린다 → 판정을 **「사람 손이 닿았는가」**(≠PRINT_PENDING · rip · print_events · **체크 1건 이상** · 품목 출력완료)로 재정의. 보존 범위를 넓힌 대가 = 신규 라인 카드 누락 → **보존 경로에 append 생성**(`itemIdsFilter`) + 지시현황 '누락' 을 **카드 0건 주문 → 카드 없는 제작 라인**으로 바꿔 **부분 누락**까지 포착. **③★자동완료가 토글에서만 평가** — 출력대기에 전 스텝을 먼저 체크해 두면 이후 PRINTING 으로 올려도 **영영 전이 안 됨**(체크를 풀었다 다시 걸어야 동작). 불변식을 **`maybeAutoCompleteCard` 한 곳**으로 뽑아 체크토글·단건상태·일괄상태 **3 진입점**에서 평가. 여기서 `card_items.print_completed` 도 함께 세팅 → 「카드 출력완료인데 생산현황 0/2 0%」 불일치 동시 해소. 부수 = 일괄 전이표에 `PRINT_PENDING` 키가 없어 **일괄 출력시작이 400** 이던 비대칭 수정. **④인쇄물에 사이드바가 찍힘** — 페이지별 `.no-print` 로는 전역 셸을 못 잡는다 → `shared-styles.ts @media print` 에서 sidebar/top-bar/토스트/모달 숨김 + `.main-content` 마진 리셋(**전 페이지 인쇄 공통**). **⑤QR 영구 미출력** — `qrcode@1.5.4/build/qrcode.min.js` **404**(1.5.2+ 부터 npm 패키지에 `build/` 자체가 없다) → **1.5.1** 고정 + `onerror` 경고. **⑥다품목 카드 오작업 위험** — 규격·원단·봉제·후가공을 **첫 품목 기준**으로만 표시(100×50/60×40 카드가 "100×50 3면쌍침" 한 줄) → 균일하면 기존 요약, 갈리면 **경고 배너 + 품목별 지시표**. **⑦시안 확대 불가** — `zoomThumb` 이 `/cards` 번들에만 있어 카드 상세에서 `ReferenceError` → shell.js 전역 이동. **⑧썸네일 키** `item_name` → **`order_item_id`**(동명 라인 덮어씀) + 카드 대표 1장이 아니라 **라인별 시안**(작업지시서·봉제작지 양쪽). **⑨체크리스트 라벨** 「부직포 **부직포** 7」 → 「부직포 7cm」(이름 중복 제거·숫자값 단위). **⑩지시현황 '누락' 행만 링크 없음** → `/orders?search=` (`?view=` 는 초기 로드에서 안 읽힌다). **⑪레거시 `POST /cards/generate/:orderId`** — `card_items` 미생성(=품목 0개·이미지 0개 유령 카드)·체크리스트 미파생·**없는 테이블 `ai_analysis` 참조** → `generateCardsForOrder` 위임(177줄→56줄) + 카드 있는 주문 400. **⑫`/api/cards/:id` R2 hydrate 직렬 → 병렬.** **⑬배포 검증 중 발견** = 없는 카드를 열면 axios 원문 「Request failed with status code 404」가 그대로 노출 — **봉제실이 오래된 QR·북마크로 들어오는 화면이 정확히 여기**라 한국어 안내 + 복귀 링크로 교체(`b00e4ce4`). **검증** = tsc·build·entity-audit 0·sort-audit P1 0·check-dom OK · **로컬 smoke 110/110 + prod smoke 110/110** · **로컬 E2E 재현 전 항목**(봉제작지 0EA→5EA·규격 복구 / 카드 보존 시 체크·상태·needs_reissue=1 / 신규 라인 append `-02` / 출력시작만으로 auto_done+print_completed / 품목별 이미지 distinct 2 / 라벨·링크·토글) · **prod 실측**(마커 19/19 — **별칭과 배포URL 동일 확인** · 페이지 9종 200·콘솔 에러 0 · 전역 `cardItems`/`zoomThumb`/`QRCode` 로드 · 인쇄 시 sidebar·topbar `display:none`·본문 마진 0 · 404 문구 교체). ⚠️**prod `cards` 0건**(주문 10,073 전량 이관 SHIPPED)이라 **데이터 경로 실검증은 로컬 E2E가 정본** — prod 는 마커·API·페이지 수준. prod 에 시험 주문을 만들지 않았다(주문번호 채번 소비 + [[design-order-line-discount]] entity_id 미적용 함정). **남은 것** = 기존 체크리스트 행의 라벨은 DB 저장값이라 소급 안 됨(신규 카드부터) · `print_methods` 는 `(id)` 스텁이라 `print_method_card_group` 분기 영구 미발동(주석 고정, **조인만 넣으면 500**) · 실사용 카드가 생긴 뒤 봉제작지 실물 대조 1회 필요

> **✅ IA 4축 배포완료 (2026-08-08, 재단 패널 — host `CUT-CEP-0.20.0`·shell `0.55.0`, `audit:ia-jsx` 드리프트 0)**: 웹 배포와 **분리된 수동 축**이라 `deploy:prod` 와 무관. **①맞붙임(여백 0·간격 0)** — 맞닿은 재단선이 **한 줄만** 나간다. 래스터 네스터로는 원리상 불가(픽셀 격자 양자화)라 `js/butt.js` 로 **mm 산수** 분리. **벡터보다 우선한다** — 벡터는 조각마다 실루엣을 따로 그려 맞닿은 변이 반드시 두 줄이라, `!useVec` 게이트가 가장 필요한 경우를 정확히 막고 있었다. 패커는 선반→**MAXRECTS**(실물 5장 3,725→**2,585mm**, 래스터 2,628보다 짧다). ★단위 사고 = `mesCut_nestSizes()` 는 **파일 좌표**인데 배치는 **저장 좌표** → 안 나누면 1/10 로 깔려 **전부 겹친 판**이 나갔고 `anyOverlap` 은 같은 공간만 보므로 못 잡았다 → **굽은 마스크와 대조하는 검산** 신설. **②굽기 테두리 아티팩트** — PNG 맨 아래 1~4행이 전폭으로 칠해져 나오는데(일러엔 없는 것) 이 **하나가 증상 셋**을 만들었다(간격 0인데 벌어짐·맞붙임 거절·테두리 중복). pad ≥1mm 라 진짜 아트는 테두리에 못 닿는다 → 축소 전에 제거. **③직선↔호 전환 튐** — 코너 판정이 각도만 보는데 그 지점은 접선이 연속이라 미검출 → 한 베지어가 직선+호를 함께 근사하며 부푼다(윗변 앵커 전부 y=980인데 bbox 980.16). 직선 매듭 강제 + **bbox 초과를 분할 사유로**. 라운드사각 23→**12세그**·정사각 5→**4**. ⚠️여백>0 이면 팽창 결과가 라운드사각이라 사각 단축은 여백 ≤0 에서만. **④판짜기 구멍** — `H`/`HB`+compound path 이식(ㅇ·ㅁ·0·8 속). **도련은 손댈 게 없었다**(엔진이 이미 구멍 안쪽을 채운다). 실기 = 도넛 여백 3mm → 구멍 **73.7mm**(=80−6). **⑤DXF 경로** — evalScript **인자는 ASCII·반환은 한글 OK**. 경로를 받아 되넘긴 게 원인이라 `___260724____cut.dxf` 가 나왔다 → **왕복 제거**(호스트가 정하고 내보내기까지) + **`.ai` 옆** 저장. 실기 `통우통신 휴식중 260808_cut.dxf` 확인. **⑥안전망** = 콜백 예외를 화면에 띄우고 락 해제 · 미선언/선언전사용 게이트(`scripts/cut/undeclared.mjs`) · 실물 5장 회귀 게이트. 게이트 `cut:smoke` **371**·`panel:smoke` 111·`cut:bench`·`cut:butt` 42·`cut:nest`·`cut:bleed`. ⚠️**각 디자이너 PC `install-a0-panel.bat` 미실행**(축2는 Z: 1파일이라 이미 전 PC 반영, 껍데기는 PC별). 남은 것 = **플로터/CNC 실기 투입 미검증**·`MIN_HOLE_MM` 물리 근거·라운드 조각 공유 변(라벨 맵)

> **✅ prod 배포완료 (2026-08-08 #62, P4 담당자별 실적 리포트 — §3 을 MES 안으로, 배포 `c2c423bf`)**: 담당자 필드(#60) 소급이 끝나 **이카운트 원본 없이 MES 에서 담당별 실적이 나온다.** prod 실측이 분석문서 §3 과 **원 단위 일치**(정해선 1,694,251,199 · 53.0%). **①★기존 `designer-stats` 를 대체하지 않고 나란히 뒀다** — 묻는 게 다르기 때문이다. 그건 `created_by`(누가 **입력**했나)를 `users` 에서 `role IN (DESIGNER,ADMIN,MANAGER)` 로 거르는데, **정해선이 통째로 빠진다**(매출 52.9% 담당인데 MES 계정이 없다). 새 것은 `orders.sales_rep_id → employees`(누가 **책임**지나). 두 카드 제목에 기준을 명시했다(「주문서 담당자 · 공급가 기준」 vs 「등록자 기준 · 처리량」) — 나란히 두면 차이가 눈에 보인다. **②★총계가 아니라 월별로 낸다** — 담당 이탈은 총계가 줄어드는 게 아니라 **그 열이 비는 것**으로 나타난다(신은주 5월·최상호 6월 이탈·임선미 7월 424만 급감 = 매출 추세 해석의 전제). 총계만 내면 이 신호가 사라진다. **③퇴사자를 빼지 않는다** — 과거 실적의 주인이고, 빼면 월별 합이 전사 매출과 어긋난다. 이탈 판정은 `status='RESIGNED'` 가 아니라 **마지막 실적 월**로 한다(재직 중 담당 이동도 잡으려면 그래야 한다) → 뱃지 「2026-04 이후 없음」. **④위치** = `/reports` 매출분석 탭의 담당자 패널(신규 페이지 아님 → 권한 등록 불요, 흡수-탭 패턴). 엔드포인트 `GET /api/reports/sales-rep-stats?months=N`(entityFilter 적용·월축/담당축 서버에서 접어 반환). **검증** = tsc·`node --check`·build·**prod smoke 108/108**·prod 실응답(월 7개·총계 3,194,084,576·이탈 뱃지)·페이지 마커 4/4. **남은 Phase** = **P5 entity 자동판정에 담당자 반영(청주 화이트리스트 은퇴)** — 법인 귀속이 바뀌므로 **별도 확인 필요**.


> **✅ 매입 대사 2차 종결 (2026-08-05 #47, AP 정합 — 세무장부 대비 +2,018만 3.1%)**: 하나 73142 구매자금대출 등록 여부를 판정하려고 **MES AP ↔ 세무장부 251 대조**에서 출발했다. **★중간에 내가 "AP 2.56억 과대"라고 잘못 진단했고 정정한다** — MES `purchase_orders` 에는 `SMP-` 접두의 **선명(e2) 매입이 함께 들어 있는데 그 합산치를 동산기획만의 세무장부 251 과 비교**했다. 운산직물이 "2.9배 과대"로 보인 것도 같은 이유였다(1월 12건 중 `E1-PO-US-*` 3건 15,278,835 는 **세무장부와 원 단위 일치**, 나머지 9건이 전부 선명분). **법인별 분리 = 발주 e1 1,982,182,966 / e2 580,639,406 · 지급 e1 1,319,588,377 / e2 506,338,936.** ⇒ **동산기획 AP 662,594,589 vs 세무장부 642,411,496 = +20,183,093(3.1%)**. **★그러나 2026-07 지급 누락은 실재했다**(법인과 무관) — 세무장부 36건 236,239,329 vs MES 5건 40,196,467(그마저 전부 [은행연동] 자동매칭). **MES 매입 지급 이관이 2026-06-30 에서 끊겨 있었다.** 29건 161,901,076 적재 → **e1 AP 824,495,665 → 662,594,589**(세무장부 대비 +1.82억 → +0.20억). **보류 7건 74,338,253** = 선명커뮤니케이션(clients 1271=내부법인) 34,202,740 · 현대상사/대진국기사/NTIC 12,177,000(**MES 거래처 확정 매칭 없음** — 「현대광고」「대한국기사」 등 유사명 다수, 추측하면 엉뚱한 AP 가 준다) · 운산직물/케이엠테크/경원GH 27,958,513(**같은 달 [은행연동] 지급이 이미 있고 1:1 대응 안 됨**). 보류분을 마저 넣으면 오히려 **1,995만 과소**가 되므로 지금이 적정 지점이다. **★73142 등록 완료** — 적재분에 「구매자금대출 결제」 적요가 90,956,851(서울경금속·정운교역·엘이디포유) 있고 회계상 **251 차변 ↔ 293 대변**이라 빚이 매입처→은행으로 옮겨간 것인데 MES 는 AP 만 줄이고 부채를 안 들고 있었다 → 제외 전제가 해소. **회전형이라 원금 스케줄 없음**(2026 상환 257,434,806/신규 244,080,431) → `maturity_confirmed=0` + `INTEREST_ONLY` + 이자만 12개월(월 802,245 = 931 이자 5,615,712÷7 · 역산 5.458%). ⚠️**잔액은 세무장부 293 에서 주기 갱신** 필요. **★차입금 대사 완결 = 세무장부 2,067,615,858(293 1,849,163,285 + 253 금융성 218,452,573) vs MES 활성 17건 2,067,615,858 원 단위 일치**(세션 시작 시 1건 45,598,220 뿐이었다). **검증**=smoke 105/105. **남은 것**=ⓐ잔여 +2,018만(매입 계상 시차 — 세무장부=세금계산서 발행일 vs MES=거래명세 일자) ⓑ보류 7건 ⓒ현대상사·대진국기사·NTIC 거래처 확정. 근거·재현 = `docs/dongsan-import/SMARTA_LOANS.md`
>

>
>
>
>
>
>
>
>
>
>
>
>


> **✅ Z: 4축 배포완료 (2026-08-01 #33, 선 도안 판정 — host `CUT-CEP-0.6.0`·shell `0.11.0`, 웹 변경 0, 드리프트 0)**: 두 패널 구조 탐색 + 실루엣 추출 방식 탐구 요청의 후속. **★계측 결과 1차 결론이 틀렸고 정정했다** — "래스터 +12%, 벡터가 참값" 으로 봤으나 도형별로 분해하니 도넛 하나(+33.7%)에 전부 몰려 있었다. 원인 = **`polarity`(패스 방향)** — `compoundPathItems.add()` + 자식 추가는 자식 polarity 를 안 잡아 **구멍이 안 뚫린다**(일러는 꽉 찬 원을 렌더). 디자이너 방식 `Object > Compound Path > Make` 는 `-+` 로 잡는다. **테스트 도형 결함이었고 래스터는 정상**이었다 — 제대로 만든 아트에서 두 경로는 **0.3~0.7%** 내로 일치(밴드 0.05mm). ⇒ **벡터 전면 전환은 근거 약함**(정확도 이득 없고 polarity·투명도·효과를 우리가 재현해약 하는 위험만 생긴다 — 래스터는 공짜로 얻는다). **벡터의 진짜 이득은 "면 없는 선" 하나** — 래스터로는 **원리적으로 불가능**한 판단. → **범위를 좁혀 구현**: 좌표는 **판정에만**(`mesCut_artKind`), 마스크는 계속 래스터, 채움은 **임시 문서에서만**(`mesCut_fillClosedIn`) → 원본 무손상 + "마스크 = 일러가 렌더한 것" 원칙 유지. UI `선 도안` = 자동(면 0 && 닫힌선>0)/항상/안함 · **무엇을 했는지 항상 표시** · 구 호스트면 판정 불가를 알림(조용한 폴백 금지). **실기**(시트컷 실도안 글자 18조각): 판정 `filled=0;stroked=20;closed=20` · **효율 1.1%→45.1%** · bbox 절반이상 겹침 **6쌍→0** · 조각별 칼선 20→**18**(=조각 수) · 칼선 여백 2.51~3.33mm(요청 3). **검증**=`cut:smoke` **125/125**(3j 9항목) · `panel:smoke` 114/114 · `cut:bench`·`cut:nest` · `audit:ia-jsx` 드리프트 0. ⚠️**사고 1건** — 계측 중 문서를 일괄로 닫는 스크립트로 용준님이 열어 둔 `동산 260713 삼남초,낙민초.ai` 를 **저장 없이 닫았다**(디스크 파일은 무손상·저장 안 된 편집분만 유실). 앞으로 내가 만든 문서만 골라 닫는다. **남은 것**=ⓐ패널 이식 2건(A0→재단 클리핑 처리 · 재단→A0 경계정확도) — **다음 건으로 확정·미착수** ⓑ디자이너 PC 4대 설치 ⓒ`mesCut_rasterizeItem` 임시문서 재사용. 정본=spec §6.26~6.27 · [[design-irregular-nesting]]
> **✅ Z: 4축 배포완료 (2026-08-01 #32, 재단선 모델 재정의 — 여백·간격 분리, shell `0.10.1`·host `0.5.0` 무변경, 웹 변경 0, 드리프트 0)**: 용준님 "기본 디자인의 테두리 + 여백이 재단선이고, 재단선을 기준으로 여백이 다시 붙어야 한다". 그전까지 두 값이 **하나로 묶여** 있었다 — `간격`이 디자인↔디자인이고 칼선은 그 중간(간격/2)에 놓여 **인접 칼선이 맞닿았다**(사이 재료 0). B 테스트에서 글자 도안 조각이 서로의 속으로 들어가 보인 것도 같은 뿌리다. **신모델** = 재단선 = 디자인 + **여백**, 조각 사이는 **칼선끼리** 간격 → 디자인 사이 = 여백×2 + 간격. 배치 마스크 = `여백 + 간격/2` 팽창 · 칼선 = `여백`만 팽창 → 두 값이 독립한다. **★여백은 음수 허용** — 도련이 이미 들어간 아트는 칼선이 **잉크 안쪽**에 놓여야 한다(도련 2mm → 여백 −2, `insetMask`). 여백 0(칼선을 디자인 경계에)도 실사용 사례. **★안전 여유 = 격자 반 칸(mmpp/2)** — 배치는 거친 격자, 칼선은 미세 격자라 두 마스크가 최대 mmpp/2 어긋나고 그만큼 간격에서 깎인다. 실측: 여유 없이 요청 5mm → **4.33mm**(= 5 − mmpp 0.92 와 정확히 일치) · `mmpp/2 + fine` 는 **1.5mm 과다** · **`mmpp/2` 가 정답**. 부족은 재단 사고, 과다는 재료 낭비 → 넘넘한 쪽. **`snapResolution` 을 `round`→`ceil`** — round 는 격자가 **더 거칠어지는** 쪽으로 떨어질 수 있고(half 2.25 @1.0 → 1.125mm/px), 거친 격자는 경계 오차와 각짐을 동시에 키운다. **실기 검증**(통제 사각 6개·롤1370·회전없음): 여백3/간격5 → 디자인 12.00·**칼선 5.53**·여백 2.91~3.40 / 여백0/간격5 → 디자인 6.00·**칼선 5.92**·여백 −0.25~0.08. **검증**=`cut:smoke` **116/116**(3f 디자인사이=모델+안전·칼선보장≥요청·여백을 키워도 간격 불변 / 3i 여백0→칼선=디자인크기·여백−2→칼선이 4mm 작음) · `panel:smoke` 114/114 · `cut:bench`·`cut:nest`·`verify` · `audit:ia-jsx` **드리프트 0**. 검증 문서는 전부 닫고 사용자 파일 무손상. **남은 것**=ⓐ실제 도안으로 자연검증 ⓑ디자이너 PC 4대 설치 ⓒ이미 칼선인 파일(면 없는 선) 가드 — 잉크 비율 <5%%면 경고/속채우기 선택(미결정). 정본=spec §6.25 · [[design-irregular-nesting]] — **직전(#31)**: 공선 부푼 6.21mm·오프셋 오차·미세마스크 칼선.
> **✅ Z: 4축 배포완료 (2026-07-31 #31, 재단선 "구부러짐" 근본 2건 — shell `0.9.0`·host `0.5.0` 무변경, 웹 변경 0, 드리프트 0)**: 용준님 "외곽선을 제대로 못 찾고 재단선이 구부러진다" → **실제 시트를 뜯어 조각을 프로덕션 경로로 다시 굽어 헤드리스 분석**. ⚠️Illustrator MCP 는 간단한 표현식 외에 **연결이 계속 끊겨**(COM) **패널 CDP(8889) evalScript** 로 전환했다 — 앞으로 이 경로가 정본(비활성 문서 `pageItems` 순회는 느려 타임아웃 → `app.activeDocument` 전환 후 읽을 것). **①곡선을 "단순화된 점"에 맞춘 것이 원인** — 긴 직선 변은 단순화하면 **끝점 2개**만 남아 최소제곱을 구속할 내부 표본이 사라지고, **오차 판정도 그 2점에서만 재니 통과해 버린다**. 실측: 글자 배너 310×65mm 가 **코드 275mm 세그 1개 → 6.21mm 부풂**(면적오차 6.05%). → **원본 컨투어에 피팅**하고 단순화본은 코너 위치 찾기 전용으로 → **0.70mm·0.03%**. ⚠️그것만 하면 **픽셀 계단(진폭 ~0.7px)을 쫓아 폭증**(원 r=380: 39→**134세그**) → **구간별 이동평균(5점) 평활화** 병행 → **23세그**. **②오프셋 오차는 안티에일리어싱보다 해상도가 지배적** — 100×60mm 사각 실측 0.25mm/px 오차 0.25mm vs **0.75mm/px 오차 1.50mm**. → **미세 격자(0.25mm/px 상한)로 굽고 배치 격자로 축소**(`downsampleMask` 피복 50%) + 알파 임계 **16→128(50% 피복)** → 배치 격자 기준 **1.50mm → 0.25mm**. ⚠️임계를 올리면 **반투명 아트가 통째로 사라진다** → 연결요소 수가 줄면 낮은 임계로 되돌리고 **결과에 표시**(조용히 잃는 게 최악). ⚠️`pickResolution` 12M 상한은 **EDT(Float32) 비용** 기준이라 굽기 격자·시트 버퍼(Uint8Array)에 그대로 쓰면 안 된다. **하네스 = 점별 편차 게이트 신설**(≤2px) — **면적오차만으로는 못 잡는다**(부풀고 꺼진 게 상쇄되면 면적은 멀쩡). 케이스로 **라운드사각**(긴 직선+둥근 코너 = 코너 검출이 안 걸리는 형태) 추가, 이번 결함이 정확히 그 모양. **실기 재검증**(통제 사각 6개·롤1370·gap 3mm): 조각 크기 **정확**(120.00×70.00) · 조각 간 최단 **4.00mm(부족 0)** · 칼선 오프셋 1.00~2.21mm(**계통 편차 소멸** — 예전엔 항상 +0.6mm). **검증**=`cut:bench` ⑧⑨ · `cut:smoke` **80/80** · `panel:smoke` **114/114** · `cut:nest` · `verify` · `audit:ia-jsx` **드리프트 0**. 검증용 문서 3개는 닫고 사용자 파일은 무손상. **남은 것**=ⓐ칼선 오프셋 잔여 ±0.7mm(배치 격자 양자화 — 조각 칼선을 **미세 마스크에서** 뽑으면 상쇄된다) ⓑ디자이너 PC 4대 설치 ⓒ`mesCut_rasterizeItem` 임시문서 재사용. **추가(#31-b)**: 조각 칼선을 **미세 마스크**에서 뽑도록(shell `0.9.1`, spec §6.24) — 배치 격자로 뽑으면 윤곽이 격자 한 칸(0.75mm) 단위로 뭉툭해진다. 배치 좌표 격자 오차는 **아트와 칼선이 함께 움직여 상쇄**되므로 **아트가 놓이는 자리(`I` 줄과 같은 식)** 기준으로만 놓으면 정확도가 미세 격자(0.25mm)가 된다. ⚠️거친 마스크 기준으로 놓으면 상쇄가 깨져 **정밀해진 만큼 더 틀린다** → 정렬 수식을 `cut:smoke` **3i** 로 고정(미세·거친 같은 자리·아트−gap/2·회전 90° 유지·크기 아트+gap)하고 **게이트가 무는지까지 확인**(baseX 에서 rPx×mmpp 빼면 3건 FAIL, 되돌리면 88/88). 미세 마스크 보관 예산 60M px 초과 시 그 조각만 거친 경로. `cut:smoke` **88/88**. ⏭일러가 종료돼 **실기 재확인은 미완**. 정본=spec §6.23~6.24 · [[design-irregular-nesting]] — **직전(#28)**: 재단 패널 실사용 3건(간격 해상도 스냅·곡선 칼선·효율%/폭추천) + CEP 확장 ID 중복(백업 폴더가 확장으로 등록돼 shell 만 구버전) 해소 — **아래 다른 세션 트랙(#29·#30)과 무관**.

> **✅ 4축 배포완료 (2026-07-31 #28, 재단 패널 실사용 보고 3건 — host `CUT-CEP-0.5.0`·shell `0.8.1`, 웹 변경 0, `audit:ia-jsx` 드리프트 0)**: 용준님 보고 3건 전부 처리. **①간격 3mm 가 3mm 가 안 나온다 = 실재 결함**(spec §6.20) — 원인은 배치가 아니라 **해상도**였다. `offsetMask` 는 `dist <= r` 인데 축방향 거리가 정수라 **r 의 소수부가 통째로 버려진다**: 3mm @1.0mm/px → r=1.5px → **실팽창 1px → 보장 2mm(1mm 부족)**. `pickResolution` 이 **롤 1370·1520 에만 1.0mm/px** 를 주므로 **기본 시트 + 간격 3mm 가 정확히 최악 조합**이었고, `gap/2` 가 픽셀 정수배인 2·4·10mm 는 멀쩡해서 "가끔 맞는다"로 보였다(5mm 도 4mm 로 부족). → **`snapResolution()` 신설 = gap/2 가 정수 px 이 되는 해상도로 스냅**(3mm@1370 → 0.75mm/px·r=2px), 불가능하면 **올림 폴백**으로 절대 부족하지 않게. ⚠️**예산을 `pickResolution` 것(12M)으로 쓰면 안 된다** — 그건 EDT(Float32 4바이트) 비용이고 네스팅 시트 버퍼는 `Uint8Array`(1바이트)라, 그대로 썼더니 0.75 대신 **1.5mm/px** 로 떨어져 칼선이 더 각져졌다 → `NEST_MAX_PX=24e6` 별도. 조각 좌표 보정도 `gap/2` → **실제 팽창량(`rPx×mmpp`)**. 결과에 **실보장 간격 항상 표시**(조용히 좁게 나가는 게 이 결함의 본질). **실측 재검증**: 혼합20 회전허용 @1370 → 실기하 하한 **3.00mm** ✅ · gap5 → 5.83mm ✅ · gap2@914 → 2.00mm ✅. **②곡선 칼선**(§6.21) — 각져 보인 원인 2개: ⓐ해상도(1mm/px 라 원 둘레 정점 18개·변 최대 16mm) ⓑ표현(전부 직선 점). `fitCurves`(Schneider) 로 큐빅 베지어 + params 접두사 **`B`/`HB`** 신설, 호스트가 PathPoint 핸들 직접 설정. **진짜 코너는 55° 임계로 보존**. ⚠️`pointType` 을 SMOOTH 로 두면 일러가 핸들을 강제 정렬해 코너까지 둥글린다 — 기본값(CORNER)에 핸들만. **함정 4건(실측으로만 드러남)**: 앵커 집합에서 코너를 찾으면 **픽셀 계단의 90°** 가 전부 코너(원이 42세그로 쪼개짐)→성근 단순화(tol×3)에서 찾고 좌표 되매핑 / 끝점 접선을 **바로 옆 점**으로 잡으면 1px 지그재그가 45° 틀어 최소제곱이 긴 핸들로 보상(L자 면적오차 3.6%)→**구간 15% 지점** + 핸들 클램프 / 닫힌 고리 이음새는 **가로지르는 접선** / 코너 사이 2점(직선)을 건너뛰면 **사각형 칼선이 통째로 사라진다**. **③효율% 교정 + 폭 추천**(§6.22) — 효율은 **표시되고 있었고 값이 틀렸다**(§6.19 의 "표시만 없다"는 오기): 분자가 gap 팽창 잉크·분모가 usable 이라 **60.0% 표시 ↔ 실제 35.6%(1.7배)** → 분자=팽창 전 잉크·분모=**실제 소요 재료**(폭 전체 ×(길이+돔보 여백)). **[폭 추천] 신설** = 롤 5종 성근 스캔 **218~342ms**(조각은 한 번만 굽고 해상도도 **가장 넓은 후보 기준 한 번** — 폭마다 다르면 비교 불성립). 지표는 효율%가 아니라 **면적(폭×소요길이)** — 실측에서 실제로 갈렸다(조각 20개: 1270 이 길이 최소인데 면적은 914 가 최소). **④★배포 스큐 방어(배포 직전 추가)** — 용준님이 "구버전 호스트가 `B` 를 받으면 칼선이 사라진다"를 지적. 축2(Z: 호스트)와 축3·축4(껍데기)는 **배포 시점이 다르고**, 이 PC 만 껍데기를 올리거나 일러를 재시작하면 **새 패널 + 구 호스트**가 실제로 생긴다. 구 호스트는 `B` 를 아는 접두사가 아니라 **조용히 무시**한다 → **호스트 버전 게이트** 신설: `mesCut_ping()` 파싱해 `< 0.5.0` 이면 **곡선을 직선으로 낮추고** 결과·시작메시지·툴팁 3곳에 이유 표시(버전을 못 읽으면 차단 = 안전한 쪽). ⚠️`applyGates()` 를 ping 콜백에서 **다시 불러야** 한다(초기 호출은 ping 이전이라 항상 '구버전'). 스모크 스텁 ping 도 `CUT-CEP-TEST` → **실제 버전 형식**으로 교체(가짜면 게이트가 항상 차단으로 떨어져 다른 검증까지 왜곡). **검증**=`cut:bench` ⑦⑧ 신설 통과(**소수 반경 1.5px→실팽창 1px 을 먼저 확인**해 게이트 전제까지 고정)·`cut:smoke` **80/80**(신규 31, 3h 버전게이트 0.4.3/0.4.9 차단·0.5.0/1.0.0 허용)·`panel:smoke` **114/114**(A0 회귀 0)·`cut:nest` 통과·`verify`(tsc+build) 통과·geometry.js repo 사본 2벌 동일. **배포 완료(2026-07-31)**: ⓐ축4 이 PC 재단패널 ← repo 직접(일러 미실행 상태라 baseUrl 문제 없음) ⓑ축2 Z: `mes-cut-host.jsx` → 0.5.0(백업 `.bak-20260731-184500`, 해시 일치) ⓒ축3 Z: 재단패널 + A0 `geometry.js`(백업 `_backup\cut-panel-20260731-185000`) ⓓ축4 A0 재설치 → **`audit:ia-jsx` 4축 드리프트 0**. ⚠️축2 는 전 PC 즉시 반영이나 **하위호환 확보** — 새 호스트는 구 패널의 `P`/`H`/`C` 를 종전과 동일 처리(추가된 건 `B`/`HB` 분기와 `mesCut_bezPath` 뿐)라 패널 0.7.3 인 PC 는 무영향. **남은 것**=ⓐ**실기 검증**(호스트 베지어 그리기는 스모크가 못 덮는다 — 일러에서 곡선이 실제로 매끈한지·간격 3mm 실측·DXF 왕복) ⓑ디자이너 PC 4대 `install-cut-panel.ps1`+`install-a0-panel.ps1` ⓒ`mesCut_rasterizeItem` 임시문서 재사용. **직전 관측 정정**: 용준님이 본 `shell 0.7.2` 는 설치본(0.7.3)보다 오래된 것 = **열려 있던 패널이 설치 전 로드된 페이지**였다(디스크는 최신). 설치 스크립트는 `Copy-Item` 제자리 덮어쓰기라 **패널을 닫았다 열면 반영**된다(§6.4). 정본=spec §6.20~6.22 · [[design-irregular-nesting]] — **직전**:
> **✅ Z: 3축 배포완료 (2026-07-31 #27, 재단 패널 실사용 점검 5건 — host `CUT-CEP-0.4.3`·shell `0.7.3`, 웹 변경 0)**: 용준님 실사용 점검 5건 중 **4건 수정·1건 답변**. ①**조각을 하나씩 옮기는 이유** = 문서 간 duplicate 가 원본 active 를 요구하는 것은 사실이나 **조각마다 왕복할 이유는 없었다** → `nestApply` 를 "전부 파싱 → 시트별 일괄 복제 → 일괄 변형" 으로 재구성, 활성문서 전환 **조각당 2회 → 시트당 2회**(20조각이면 40→2). 전환마다 화면 갱신이 붙어 조각 수에 비례해 느려졌다. ②**칼선 DXF = 현재 배치 그대로 맞다**(재단선 레이어만 — 실제 CNC 파일이 외곽+돔보뿐이라 그림은 뺀다. ⚠️레이어 숨기기로는 안 된다, 일러는 숨긴 레이어도 내보낸다). ③**거래처 입력 불가 = 내 회귀** — 어제 UI 를 2그룹으로 나누며 모아찍기 그룹을 **접힌 상태로 뒀다**(거래처·수량·가공자·등록이 통째로 그 안). `open` 복원 + "그룹은 구분이 목적이지 숨기는 게 목적이 아니다" 주석. 거래처 2,759건 config 로드는 정상이었다. ④**테두리·돔보가 시트 크기 기준 = 결함** → **조각+조각별칼선 전체 bbox** 기준으로 변경(칼선이 조각보다 gap/2 바깥이라 조각만으로 잡으면 삐져나간다)·아트보드도 축소 → 재단기가 빈 여백을 안 돌고 롤도 절약, 등록 `measured_cm` 도 실크기. ⑤**DXF 등록 폴더 저장**(spec §9-7 해소) — `Z:\DESIGNS\IA-등록\<건별>\` 에 EPS 와 같은 이름으로 굽고 `manifest.files.dxf` 에 실음(전엔 항상 null·%TEMP% 에만). 실패 시 null 유지 = **없는 파일을 가리키지 않게**. 단품 [칼선 만들기]는 등록 절차가 없어 계속 %TEMP%. **검증**=`cut:smoke 49/49`·`cut:nest` 통과·`audit:ia-jsx` 4축 0. **⚠️호스트 3건(①④⑤)은 실기 미검증** — 패널 계산부와 달리 스모크가 못 덮는다. **남은 것**=ⓐ일러 재시작 후 실파일 확인 ⓑ`mesCut_rasterizeItem` 임시문서 재사용(조각마다 열고 닫는다) ⓒ**효율%·추천폭**(spec §6.19 착수 전 점검 완료 — 효율은 이미 계산됨·표시만 없음·단 gap 팽창분 과대계상 보정 필요 / 추천폭은 `opts.tries` 성근 스캔 5폭 → 승자만 정밀 재실행, 래스터 재사용 가능·**스캔 시간 미측정**). 정본=spec §6.18~6.19 — **직전**:
> **✅ Z: 3축 배포완료 (2026-07-31 #26, A0 묶음분리를 잉크 실루엣으로 완전 대체 — host `0.1.10`·shell `0.1.12`·`panel:smoke` 103→114, 웹 변경 0)**: 용준님 질문("모아찍기·묶음분리가 대각선 여백을 못 잡는데 cut panel은 되는 게 구조가 다른 건가") → **구조가 다른 게 맞았다**. 세 엔진이 조각을 아는 방식: ia-editor 모아찍기=`{id,w,h}` **숫자 2개**(`iaEditor.js:1805`) / A0 묶음분리=bbox `[L,T,R,B]`(`mesA0_cluster`) / 재단 패널=픽셀 실루엣. **근본 원인 1개 = "사각으로 본다"** — 비스듬히 놓인 디자인은 잉크가 떨어져 있어도 bbox 가 겹쳐 거짓 병합됐고, 완화책이 이미 두 번(클립 존중 경계·음수 gap) 들어가 있었다. ①**A0 실루엣 대체**(용준님 "완전 대체" 선택): `mesA0_seedBegin`(후보 수집→임시문서 복제→PNG **1회** 굽기) → 패널 `inkMask→offsetMask(gap/2)→components→아이템별 최다중첩 배정` → `mesA0_seedApply`(공간정렬·큐 적재는 호스트 단일 소스 유지). `gap` 의미 보존(양수=팽창·0=닿을때만·음수=침식), 치수는 **아이템 union bbox 그대로**(재료 소요=외곽 사각이 맞다 → 회귀 0). ②**★잉크 판정 = `alpha`(용준님 지적으로 정정, 배포 직후)** — 재단 패널의 `'white'`(흰 픽셀=배경)를 그대로 복사했던 게 **A0 에선 틀렸다**: 재단은 *외곽을 따는* 용도라 맞지만 분리는 "디자인이 어디 있나"라서 **흰색도 그림**이다. `white` 면 흰 글씨·흰 바탕 배너가 사라져 한 디자인이 쪼개지고 흰 요소만인 개체는 noink 로 제 디자인과 못 합쳐진다. 임시문서 배경이 투명이라 alpha>0 = 아트 있음이 정확. **대가**=전면 덮는 배경 판이 섞이면 전부 이어져 1건 → "개체는 N개인데 잉크가 전부 이어져 1건" 경고로 드러냄(**실패가 시끄러운 쪽 선택** — white 의 실패는 행이 조용히 늘어 그럴듯해 보인다). 회귀 = smoke 신규 1건, `white` 로 되돌리면 **실제로 FAIL 하는 것까지 확인**. ③**한계 2가지를 코드가 말하게 했다**: ⓐ**배정 단위=개체** → 선택이 그룹 1개면 1행. 원인 판정은 **개체 사실(`grp`=그룹 개체 수, 호스트 보고)** 로 — 처음엔 `comps>1`(잉크 덩어리 수)로 했는데 **틀린 접근**이었다(흰 요소 때문에 한 디자인도 여러 덩어리 → 엉뚱하게 "그룹을 푸세요") ⓑ**분리 해상도=`mmPerPx`**(상한 12M px) 항상 표시·1mm/px 초과 시 경고. ③**폴백은 시끄럽게** — 굽기 실패 시 옛 사각으로 큐를 채우되 ⚠ 표시(조용히 옛 동작 복귀가 최악). ④**⚠️버전 스큐 방어** = 축2(Z: 호스트)는 전 PC 즉시 반영인데 축3·축4는 PC별 수동 설치 → 구버전 `main.js` 용 `mesA0_queueAddBatch`/`mesA0_autoDetect` **호환 shim 유지**(지웠으면 설치 안 끝난 PC 는 묶음분리 전멸). ⑤**geometry.js 사본 2벌 리스크** = 정본은 재단 패널(Node 하네스가 그 파일을 로드 → "검증한 코드=배포된 코드"). 축1~4 감사는 repo↔런타임만 봐서 못 잡는다 → `audit:ia-jsx` 에 **repo 사본 일치 검사** 신설. **검증**=`panel:smoke` **114/114**(신규 5 = bbox겹침+잉크분리→2건 배정 `0;1` · 개체1개→1건+원인지목 · 굽기실패→폴백 경고. **스모크가 캔버스로 진짜 PNG 를 만들어** `inkMask→components→배정` 을 헤드리스에서 그대로 태운다) · `cut:smoke` 49/49 · `cut:nest` 통과 · `audit:ia-jsx` 사본 일치 OK. **배포 완료**(2026-07-31 16:28): 축3 패널→Z:(백업 `_backup\a0-panel-20260731-163606`) · 축2 호스트→Z:(백업 `mes-a0-host.jsx.bak-20260731-163606`) · 축4 이 PC 설치 → **`audit:ia-jsx` 4축 드리프트 0**. **남은 것**=ⓐ**일러 재시작 후 실파일 검증**(비스듬히 나열된 실제 디자인으로 분리 결과 확인) ⓑ**디자이너 PC 4대 `install-a0-panel.ps1` + 일러 재시작** — 그때까지 그 PC들은 shim 경유 옛 사각 동작(고장 아님) ⓒ전 PC 설치 확인 후 shim 제거. 정본=[[design-silhouette-vs-bbox-split]] · spec §6.17 — **직전**:
> **A. 후가공·마감 추가 실패**(deploy `fa873f53`) — ①**직전 세션 진단은 오진**: "중복인데 성공=UNIQUE 안 걸림"의 원인은 **Git Bash curl 의 CP949 한글**(기록된 함정). ⚠️그 진단이 만든 **mojibake 2행이 `is_active=1` 로 드롭다운에 노출 중**이라 제거. ②**실패 정체 = 이름 UNIQUE 가 `method_group` 무관 전역**(프리셋·CEP 가 이름으로 방식을 찾으므로 전역 유일이 요구사항) → 봉제 칸 이름을 출력 칸에 넣으면 거부되는데 **보는 목록엔 없어 이유 불명**이었다. 전역 유일 유지 + 문구에 '어느 칸인지' 명시. ③형제 2곳(`PUT /methods/:id`·`option_code` 중복)도 `서버 오류`→409+안내. ④후가공 코드 자동생성 `MAX(id)+1`→**코드 최대번호+1**(prod 실측 id 22 인데 PP-017).
> **B. 가공 대기함 3건**(deploy `4b0309f5`) — 🔴**썸네일 39건 전부 있는데 한 장도 안 떴다**: 목록이 `groups_json LIKE '%thumbnail_base64%'` 로 판정하는데 R2 이관 코드가 저장 시 **그 키를 삭제**하고 `thumbnail_r2_key` 로 바꾼다(실측 base64 0 / r2_key 39). `/thumb` 는 정상 PNG 8,320자를 반환하는데 **프론트가 요청조차 안 했다** → 판정 수정 1곳으로 **주문서 트레이·ia-editor 대기함·주문 라인 프리필 3곳 동시 해소**. 🟠**건수 3중 불일치**(헤더 39 / 그룹 12 / 실제 행 12) → **보이는 건수를 정본**으로(`ofTrayRender` 가 갱신) + 배지에 `(이 거래처 N)`. 🟠**검색창 타이핑 무반응**(Enter 필수) → 자동검색은 서버 부하로 미채택(사용자 결정), `Enter 로 검색` 안내로 오해 해소.
> **C. 주문 라인 에누리**(deploy `8004bd20`·`cd59e08f`, 마이그 0501) — 금액칸 수동 수정 UI가 있는데 **합계가 무시하고 저장도 안 됐다**(payload 에 amount 없음·서버 미참조, prod 수동조정 이력 0건). **의미 확정=행 에누리**(사용자): 단가·치수·수량 불변 → 단가 마스터 보존 + 에누리가 에누리로 잡혀 회계 정합. **★설계 축 = `amount` 의 의미를 '에누리 반영 후 최종'으로 불변** → 소비자(`taxInvoices/helpers.ts:453`·`departments.ts:183`·`reports.ts:126,143`)가 **코드 수정 0 으로 자동 정합**. 신규 컬럼 `auto_amount`·`line_discount`·`discount_reason`·`discount_by`(백필 2,878행·NULL 0·오탐 에누리 0). **`utils/orderLineAmount` 신설 = 산식 단일소스**(서버 5곳 복붙 통합·총액·부가세도 최종액 기준). **부가세율 이중정본 해소**(클라 하드코딩 0.1 → settings 주입). **죽은 `switchAiTab` 제거**(check:dom **9→5**). **prod 실주문 E2E**: 자동 20,000→최종 18,000·에누리 2,000·총액 28,000·부가세 2,800·회계 경로 28,000 실측 후 **전건 삭제**. ⚠️**`entity_id` 는 주문 생성 body 로 안 먹는다**(99 로 보냈는데 `E1-` 실운영 생성).
> **D. A0 패널 P1·P2·P3 전량 종결 — 재현 프로브 8/8 해소·`panel:smoke` 50→100** (`210ae670`·`8e89ba26`·`7a3a0086`, shell **0.1.10**) — 구조 결함 8건을 헤드리스로 재현하고 전량 조치. **P1**: 기본창 320×260(콘텐츠 1/3·**실행버튼·결과창이 화면 밖**)→340×640·MinSize 폭 300 / **행 연동이 탭·용도와 독립**(모아찍기 행이 single 로 변질돼 등록 잠김 H1, 묶음 행 마감 소실 H2)→탭 전환 해제+행 mode 고정 / **확정 실패분 유실**→성공분만 제거 / 가공자 명단 하드코딩 제거(정본=도메인 매핑) / **껍데기 버전 표기**(재설치 확인 수단이 없었다). **P2**: ★**호스트 잠금 단일화**(`setHostBusy`+`BUSY_BTN_IDS`) — 뿌리는 "버튼별 임시 disable" 이라 **새 진입점이 계속 새어 나간 것**(`mesA0_paramsPath()` 파일 1개 공유 = 설정 섞임) / **진행 취소 신설**(호스트 호출 사이에서 멈춤) / change 위임 **제외목록→허용목록** / 적용 버튼 묶음 탭 전용 / **숨은 키워드 칸이 행에 쓰지 못하게**(가시성 판정) / 실측은 포커스 복귀 시 갱신. **P3**: 액션 동사 `등록` 어간 통일(`1건 등록`/`조각 N건 등록`/`N건 등록`) · "선택" 3의미 분리 · 죽은 `composeAnnot` 제거 · **`.debug` 감사 편입**(삭제 아님 — CDP 핫스왑 의존). **배포 전 게이트에서 3건 더**: 🔴**가공자 미선택 오등록**(매핑 0건 폴백 시 맨 위가 테스트 계정 `123` = 기본값 → 남의 worker_id 로 기록) → `(가공자 선택)` 빈 항목 + `requireWorker()` 등록 3경로 차단 / **잠금 누수**(`#scale`·`#trimInk` 는 select 라 안 잠겨 배치 중 실측이 끼어듦) → `refreshMeasure` 진입점 가드 / **영구 잠김 탈출구**(호스트 콜백 미복귀 시 패널이 굳음) → 취소 2회=강제 해제. **★`worker_domains` 매핑 4건 등록 완료**(인호동·김보연·정소은=현수막 / **김영주=전사**) → 명단이 **정확히 4명으로 정리**(`123`·`디자이너` 자동 제외 = `users` 무변경으로 목표 달성) + 경고 소멸 + **전사(봉제) 3종 개방**. config 는 에이전트가 **~5분 주기 브로드캐스트**라 반영에 4분 걸렸다(캐시 아님). **검증**=tsc 0·build·entity 60/60·sort P1 0·check:dom 9→5·**prod 스모크 104/104**(3회)·페이지 9/9·**prod 마커 5/5**·**헬퍼 단위 14/14**·**panel:smoke 100/100**·**재현 프로브 0/8**·**4축+`.debug` 드리프트 0**·CI success. **남은 것**=ⓐ**디자이너 4대 `install-a0-panel.ps1`+일러 재시작**(원격 수단 없음·라벨 변경 안내 필요) ⓑ`main.js` 분할 의도적 보류 ⓒ분할청구 자연검증(prod 실사용 0건). 정본=[[design-a0-panel-structure]]·[[design-order-line-discount]] — **직전**:

>
> 이전 배포 이력 전량 → `.claude/PROJECT_STATUS_ARCHIVE.md` (매 세션 읽을 필요 없음)

## ⚠️ 미해결 잠복 (2026-07-27 다이어트 시 승격 — 완료 이력에 묻혀 있던 것)

> **2026-07-28 prod 실태 전수 재검증 완료.** 아래 6건 중 **4건이 이미 배포된 상태**였다(기록만 낡음).
> `deploy.yml`이 **main push 자동배포**라 main에 코드가 있으면 이미 prod다 — "커밋·미배포" 기록을 그대로 믿지 말고 **prod 실측으로 확인**할 것.

| 건 | 재검증 결과 (2026-07-28) | 남은 것 |
|---|---|---|
| 워크벤치 P1 시안 검수 | ✅ **해소** — 0307은 **이미 적용돼 있었고**(prod `permission_pages`에 `/workbench` 존재) 메뉴도 등록됨(`menu.ts:98`). 실제 원인은 `INSERT OR IGNORE`가 기존 행을 못 덮어써 **DESIGNER만 `can_access=0`** 이었던 것 | 없음. prod UPDATE로 DESIGNER 개방 완료 |
| 휴가→근태 자동연동 | ✅ **이미 배포됨** — `src/utils/leaveAttendance.ts` main 존재, `leaves.ts:615` `markLeaveAttendance` 호출, 이후 `a4081ba9`로 hardening까지. prod `source='LEAVE'`가 0건인 건 **`leave_requests` 자체가 0행**이라 그런 것(정상) | 없음. 실사용 시 자연 검증 |
| LogWatcher 장비중심 | ✅ **P1~P3 배포됨** — prod `equipment` 24행 운영 중 | P4만 잔여(spec `2026-06-15-logwatcher-equipment-centric.md`) |
| IA 멀티소스 임포지션 | ✅ **P1 배관 배포됨** — `6ff41f40`이 main에 포함, `iaEditor.js` 마커 13개. 브랜치 `feat/ia-multisource-imposition`은 **잔재**(삭제 가능) | P2 이후 미착수 |
| 세션5 미배포분 | ⚠️ **원 기록 무효** — 그 세션 워킹트리는 소실. 다만 N+1 청크 패턴이 **8파일 중 6개에 이미 적용**돼 있어 실질 해소 | `quotations.ts`(동적 IN절 1·for 5)·`taxInvoices/batch.ts`(동적 IN절 2)만 **별건 재감사** 대상 → auto-improve Area 2 |
| 품목 마스터 신모델 | 유지 — 단가 전부 0(전역 과제)·간판 BOM 보류 | **정본=auto-memory 품목 마스터 항목**. ⚠️구 기록의 "정본=memory/session-context.md" 포인터는 그 파일이 세션마다 덮어써져 **이미 깨짐** |

### 🛑 중단 — 판단 후 재개할 것

| 건 | 왜 멈췄나 |
|---|---|
| ~~`session/ia-web-sunset` 머지~~ | ✅ **해소(2026-07-28)** — 커밋 분리 채택: `31c88d00`(조각 로딩 실버그) 그대로 + `69f6535c`(shelf)는 겹침 1줄 수정 후 main 반영, 하네스 1005/1005. 남은 것=**Z: 동기화 + 디자이너 PC 일러 실측**(판짜기 E2E와 동일 회차). 브랜치는 삭제 가능 |
| MMS 이미지 규격 상향 | 바로빌 개발자센터가 SPA라 공식 규격을 못 읽음. 확보한 건 통신사 표준(300KB·SKT/KT 1280×960)뿐. 현재 `MAX_BYTES=300KB`(표준 일치)·`MAX_PX=1000`(보수적). **상향 자체는 `constants/barobillCodes.ts:160` 1줄**(→`layout.ts:212`가 `window.MMS_IMAGE`로 주입 = 서버검증·클라리사이즈 단일소스). 막힌 건 공수가 아니라 **근거** → 계단식 실발송(400→600→800→1000px)이 가장 빠른 결론, 실패분 과금 0원. **테스트 수신번호 확정이 유일한 블로커(알림톡과 동일)** |
| 알림톡 대량발송 실검증 | 수신번호 확정 필요(실발송 7원). **MMS 규격 실측과 블로커가 같음 — 번호 1개 확정 시 2건 동시 해제**(알림톡 7원 + MMS 100원×4 ≈ 400원) |
| IA 실가공 자연검증 · #310 직접발행 폼 | 현장/실사용 작업. IA는 **판 출력 경로가 2026-07-28 복구됨**(EPS 근본원인 해결) → 남은 건 `/ia-editor` 네스팅 실클릭 + 디자이너 흐름 1건 통과 |

> **현재 초점**: 워크플로우 마스터 기획(`docs/superpowers/specs/2026-07-01-workflow-improvement-master-plan.md`) 실행 + 인사/급여/근태 강화. **배포완료**: Phase0·X2·X4·X5·G-1·G-7 + HR(B1 급여확정잠금·B2 CAPS경보·B4 연차무인+촉진소멸배너·미사용연차수당 주입) + 급여관리 기본뷰=급여대장 전환 + **출고관리 v2(검수·전량출고·합배송 확장·/pack)** + **창고 배치도(0440)** + 전체 코드리뷰 P1/P2. **HR ✅B1~B5 완결**(B3 셀프·B5 7월요율 2026-07-18 배포). 그외=Phase1~5(단가·실원가·간판BOM 등).
> **⚠️ 급여계산 진단(신현서 케이스, 코드정상·입력차이)**: MES 공제가 ECOUNT보다 높음 = **①부양가족 0명(→본인1, 소득세 +66k 최대) ②차량유지비 20만 비과세 미처리(taxable=400만) ③국민연금 base=당월급여(400만) vs ECOUNT 기준소득월액(~371만)**. MES 간이세액표 룩업·100/120옵션·요율(9.5/7.19/13.14%)·상한(637만) 전부 정상. 조치=부양가족·차량유지비 비과세 입력 / 개선여지=국민연금 기준소득월액 필드. → [[payroll-calc-ecount-diff]]
> **블로커**: 품목 단가 전부 0(전역 과제) · 간판 BOM=brainstorming 후 보류. (선명 매입이관·미지급·품목매칭·경영진단·재고정비·품목삭제UI=2026-07-16 완료)
> **다음 액션**: ~~①B5 7월요율 ②B3 직원셀프 ③국민연금 기준소득월액 필드~~ ✅완료(2026-07-18). ④품목 단가 전역(블로커): **원가 avg_unit_cost 315개 매입 backfill prod완료(2026-07-19, 입력값그대로)** — 남은=매출 base_price(단일가47 시드/변동가 ㎡표/무실적)·무이력514 원가·자재비 소진연결. 정본=[[project-item-pricing]]. ⑤간판 BOM. **재고 후속(07-06 배정완료 기반)**: (운영)실물 수량 입력=재고조정/실사→기본창고 자동안착·임계치 설정·태극기46/상품172 배정(선택, 같은 모달) / (코드)임계치 자동추천 골격 C=미착수. 출고v2 후속(선택)=P5 사진 증빙·토트 스캔(보류)·prod admin 기본비밀번호 강화(#336, owner 트리거 대기).
> **핸드오프 정본** = `memory/session-context.md`(품목) + `memory/project-workflow-master-plan.md`(워크플로우·HR).
> **🔧 멀티세션**: 동시 작업은 `scripts/new-session.ps1 <이름>`(worktree 격리, 메인 직접작업 지양) → `docs/WORKTREE_WORKFLOW.md`.
> 완료 이력 → `PROJECT_STATUS_ARCHIVE.md` (매 세션 읽을 필요 없음, 필요 시 참조).

---

## 🔒 편집 중 (충돌 방지)

- `src/scripts/iaEditor.js` — worktree `session/iae-tray-search`(커밋 `2fe74b91`, **main 미머지·미배포**). 같은 파일 수정 시 조율 필요.

---

## 🔴 현재 진행 중 (미완료만 — 완료분은 ARCHIVE)

- **✅ 현장 설치완료 [2026-08-09] 전사 8색 LogWatcher 2대 (`4d242c87`)**: `TRANS-8C-01`(DESKTOP-5C9D04J)·`TRANS-8C-02`(PC-202605141926) **둘 다 온라인·신빌드·경로 실측 확인**. 서버는 08-05 배포분 그대로(마이그 `0518`·`0519`, 스모크 107/107). **★"같은 장비 2대"가 안 같았다** — 같은 PrintExp 인데 로그가 1호기 `<설치>\Log\main\` / 2호기 `<설치>\Log\`(중첩 복사폴더 안, **실물 확인함**), **줄 시각이 `[13:29:44]` vs `[2026/08/06 00:00:00]`**. ⚠️**중국어 마커는 동일해서 블록 인식은 양쪽 다 된다** → 시각 형식을 놓치면 "잘 되는 것처럼" 보이면서 전 이벤트가 **그 날 00:00** 으로 찍히고, 멱등키(`file_path`+`print_completed_at`)가 뭉개져 **하루 재출력분이 유실**된다(2호기 141건 전부 0분이던 것). 2호기 로그 사본을 Z: 에 미리 올려둔 덕에 **설치 전에** 잡았다 → 새 전사 장비는 `--test` **소요시간이 전부 0분인지부터** 본다. **★`install-service.bat` 이 2026-06-15(`6221acba`)부터 고장나 있었다** — nssm `AppDirectory` 용으로 뒤 백슬래시를 뗀 `APP_DIR` 을 파일 경로에도 써서 `C:\Logwatcherappsettings.json` 판정 → **항상 즉시 종료**. 그 사이 이 스크립트로 설치된 장비가 없어 안 드러났고(인벤토리 `⬜` 방치가 방증), UTF-8 bat 이 콘솔 코드페이지 불일치로 **한자로 보여 "실행됐다"로 오독**됐다. 정본은 ASCII 메시지+`APP_DIR_NS` 분리판 — **현장 `LogWatcher-v2` 에 이미 있던 수정이 repo 로 안 돌아와 있었다**(repo 미반영 수정은 다음 배포에 되살아난다). 그 외 수정 = **자정 롤오버가 전날 로그를 0부터 재read**(설치 당일 밤엔 설치 전 작업까지 소급 적재) · 하트비트 로그경로 폴백 · `make-equipment.ps1`(손편집 금지 — JSON `\P` = `invalid escapable character`, 경로를 `/` 로 씀. PS5.1 은 **BOM 없는 .ps1 을 ANSI 로 읽어** 한글이 깨진다). 현장 함정 = 관리자 창은 UAC 별도 세션이라 **매핑 드라이브 Z: 가 안 보인다**(창 2개로 분리) · `sc stop` 도 관리자 권한 · `robocopy` 는 `/R:2 /W:2` 없으면 30초×100만 회. **남은 것 = 실출력 1건 자연검증**(일요일이라 아직 이벤트 0건) · 첫 건은 카드 미연결이 정상 → `/production` 「출력파일 연결」 1회 후 학습. 정본 = `docs/LOGWATCHER_EQUIPMENT_INVENTORY.md` · `LogWatcher/README-transfer-8c.md` · [[project-neostampa-rip-integration]]

- **✅ prod 배포완료 [2026-08-08] 목록 UX 이카운트 갭 — 감사 + P1-1~P1-4 (커밋 `39f16b50`·배포 `28980b1c`)**: 감사 정본 = `docs/audits/2026-08-08-list-ux-ecount-gap.md`(갭 G1~G12·페이지별 매트릭스 13개·실행순서). **★근본원인은 "카드가 필터를 무시한다"가 아니라 조회조건이 3벌 복사돼 있었다는 것** — 목록(`core.ts`)·카운트(`core.ts`)·CSV(`queries.ts`)가 각자 WHERE 를 적고 통계는 아예 안 적어서, 상단 "전체 주문"(전 기간·취소 포함·필터 무시)과 하단 "총 N건"(최근 1개월·취소 제외·필터 반영)이 **구조적으로 일치할 수 없었다**. → `src/routes/orders/listFilter.ts` **SSOT 신설**, 네 경로가 공유. **곁가지로 드러난 실결함 2건** = ①**CSV 에 법인 가시성 필터가 통째로 없었다**(타 법인 주문 유출) ②카운트 쿼리의 금액 범위에 NaN 가드가 없어 목록과 갈렸다. **설계** = 카드는 `skipStatus` 로 집계(자기 자신으로 걸러지면 드릴다운 불가) → 카드 = 현재 스코프의 **상태별 분포**, 전체 = 그 합(상태 미선택 시 목록 총건수와 일치). 상태 복수값(`PRINTING,PRINT_DONE`) 허용 = 「생산중」 카드용. **캡션**으로 산정 기준 상시 표기(D1). 카드에 없는 상태(취소) 선택 시 "카드와 다름" 경고 표기. **검증** = tsc·build·`node --check`·**smoke 110/110**·`audit:entity` 누락 0·`sort-audit` P1 0 · **로컬 실브라우저**: 9개 필터조합 + 드릴다운 3종 전량 card==목록 일치, 클릭/재클릭 토글·강조·캡션 정상, 콘솔 에러 0. ⚠️**로컬 D1 은 `db:reset:full` 로 재생성**(기존 3건 소실·`orders.sales_rep_id` 없어 목록 500 나던 상태였다) + 검증용 `VF-%` 주문 12건 잔존. ⚠️`db:migrate:local` 은 0475 중복컬럼으로 실패(추적 불일치·기지의 함정), `db:seed` 는 `item_categories` UNIQUE 로 실패. **P1-2 도 완료(같은 날)** = 활성 조회조건 **칩 행**(D2). ★P1-1 이 만든 "집계 기준" 캡션과 칩이 **같은 걸 두 번 말하게 돼** 캡션 줄을 칩 행으로 **승격**(중복 제거, 조건 표기는 화면에 한 곳뿐). 기본 최근 1개월도 사용자가 고른 조건과 똑같이 칩으로 뜬다 → G3 해소. 칩 `✕` = **그 조건만** 해제(「초기화」는 기본값 복귀라 역할이 다르다). 조건 없으면 `전체 기간`·`취소·견적 제외` 회색 칩으로 암묵 규칙을 드러낸다. 카드에 없는 상태(취소) 선택 시 **경고 칩**. 칩·강조는 `loadOrders()` 에서 그려 **통계 API 실패해도 조건 표시는 남는다**. 상태 라벨 정본 = 셀렉트 옵션 텍스트(`ordStatusFilterLabel`). ⚠️작업 중 `check:dom` 훅이 제거한 `#orderStatsCaption` 잔존 참조를 5→6건으로 잡아냄(훅 의도대로 작동). 검증 = smoke 110/110·`check:dom` 기준선 5 복귀·실브라우저 8시나리오(기간칩 ✕ 7→10건·배송칩만 해제 시 검색/지연 유지·드릴다운→상태칩·취소→경고칩)·콘솔 0. **P1-3 합계 바** = `건수·수량·공급가·부가세·합계`, **조회조건 전체 기준(현재 페이지 아님)** — 여러 페이지면 문구가 바뀐다. 금액 3열은 카운트 쿼리에 `SUM` 을 얹어 왕복 추가 0. ★**수량만 별도 쿼리** — 주문:품목이 1:N 이라 목록 쿼리에 JOIN 하면 주문 행이 불어나 **금액이 중복 합산된다**. 최상위 라인만 세고(자식은 부모에 포함), 실패해도 목록은 뜬다. 총건수 표기는 합계 바로 옮기고 페이지네이션은 "N/M 페이지"만. **P1-4 정렬** = 기본 `created_at_desc`→**`order_date_desc`**, 4종→9종(금액 큰/작은순·거래처명순 추가), 라벨에 기준 명시. ★**정렬표도 SSOT 로 이동** — CSV 에 4개짜리 사본이 있어 목록에서 고른 정렬이 **CSV 에선 조용히 기본값으로 떨어지고 있었다**. **prod 검증** = smoke **110/110**·페이지 마커 **10/10**(구 캡션 제거 확인 포함)·별칭 실서빙 확인(summary 필드 존재)·실데이터 대조: 최근1개월 848건에서 **카드 848 = 목록 848 = 합계바 848**·카드 내부합 일치(0+0+848)·공급가 222,826,482+부가세 20,937,022=**합계 243,763,504 원단위 일치**·전체기간 8,778건 **155ms**·드릴다운/칩해제/검색 병행 정상·콘솔 0. **P2 도 배포완료**(커밋 `4a940137`+`557cc95c`, 배포 `6a144fdb`) = 공통 자산(`shared-styles.ts` ds-stat/ds-cond/ds-summary + `shell.js` `window.dsListUx`) 추출 후 **발주·견적·입고 3개 이식**. 칩은 innerHTML 아니라 **DOM+리스너**(검색어 `<img onerror>` 주입 안 됨 실측). ★**이름 충돌 사고** = 칩을 `.ds-chip` 으로 만들었는데 그 이름이 **이미 '클릭하는 필터 토글 칩'으로 정의**돼 있어(737행, 출고 택배사 배지) 뒤 정의가 기존 배지를 덮어썼다 → `.ds-cond` 로 개명 + 출고 배지 스타일 실측 확인. ★**shared-styles.ts 는 백틱 템플릿이라 주석 안 백틱도 문자열을 끊는다**(빌드 실패로 발각). **이식 중 실결함 4건 발견·수정** = ①견적 작성일 from/to 가 **죽은 UI**(입력칸이 로더를 부르는데 값을 아무도 안 읽음·서버 미지원) ②견적 '주문전환' 옵션도 처리 코드 없음 → `has_orders` 신설 ③견적 '부분주문'이 **현재 페이지 행만** 걸러 총계와 어긋남 → 서버조건 승격 ④발주 CSV 가 자체 정렬표로 공급업체·지연을 무시. **출고는 의도적 미적용** — 하루치 작업 보드라 다조건+페이징 전제가 없고, 배지는 로드된 행에서 클라가 세므로 G1 자체가 불가능하며 `/api/shipments/stats` 는 **호출처 0건 죽은 엔드포인트**. ★**금액 3열 불일치는 데이터 사정**(prod 발주 627건 중 **84건이 공급가 미기재·4.8억**) → 계산오류 오해 방지로 합계바에 사유 라벨 노출. ⚠️**멀티세션 사고 1건** = push 거부(타세션 선행 푸시)인데 **배포는 이미 나가** 타세션 XSS 수정이 prod 에서 빠질 뻔 → 병합 후 **합집합 재배포**로 복구([[feedback-multi-session-deploy]] 재확인). **prod 검증** = smoke 110/110·마커 5/5·합집합 확인(내 변경+타세션 XSS 동시 존재)·발주 실데이터 289건 카드=목록 일치. **P3 도 배포완료**(커밋 `7b7842ac`·배포 `b2417879`·마이그 **0525** prod 적용) = 프리셋·열 선택·페이지당 건수를 **주문·발주·견적·입고 4개**에. ★**저장은 서버(사용자별)** — 같은 사람이 사무실·디자인실 PC 를 오가서 localStorage 면 PC 마다 따로 놀고 공용 PC 에선 남의 설정이 보인다(용준님 선택). 기존 `settings` 는 **전역 키-값·ADMIN 전용**이라 재사용 불가 → `user_filter_presets`+`user_ui_prefs` 신설. **user_id 는 토큰에서만** 오고 삭제·기본지정은 `WHERE id=? AND user_id=?` 로 소유 동시 확인(body 로 받으면 남의 설정 조작 가능). 상한 = 페이지당 프리셋 30·값 8KB. ★**열 숨김을 nth-child CSS 로** 한 이유 = 행 빌더(거대 템플릿 문자열)를 컬럼 정의 배열로 바꾸면 **4개 목록 렌더링을 동시에 갈아엎는** 셈이라 회귀면이 크다. `<th data-col>` 만 달고 CSS 주입 → 행 빌더 무수정. 빈 상태 행은 colspan 이라 영향 없음(의도)·최소 1열 강제·체크박스/액션 열은 data-col 미부여로 제외. ★**렌더 순서** = 도구모음 먼저 붙여 저장값 읽고 **그 결과로 첫 조회**(모르고 조회하면 틀린 건수로 그린 뒤 재조회 = 2회). 기본 프리셋이 첫 조회를 대신. 실측 **1회**. 입고는 이력탭이 **탭 전환 때 처음 뜨므로** 그때 1회 마운트(표가 hidden 이면 마운트 실패)·탭 왕복해도 중복 없음. **검증** = smoke 110/110(로컬·prod)·entity 0·sort P1 0·4페이지 브라우저(열 숨김 헤더+본문·새로고침 서버복원·기본프리셋 자동적용·건수 100 이 요청 limit 반영·첫 조회 1회)·경계(비인증 401·타인 프리셋 404·과대값 400). ⚠️**배포 직후 첫 요청이 404**(전파 지연) — 코드 문제로 오인 말 것, 재호출 200. prod 테스트 흔적(프리셋·pref)은 **삭제 완료**. **잔여 목록 이식 배포완료 [2026-08-09]**(커밋 `080df2b9`·배포 `8f8f3329`) = **6개 추가**(구매요청·지급요청·매입확정·재고·세금계산서·거래처). **이식 중 실결함 2건** = ①**지급요청 KPI 가 '최근 90일' 고정**인데 라벨에 그 사실도 없어 목록과 다른 모집단을 셌다(G1 동형) → 목록 조건 연동 + 드릴다운 4종 ②구매요청·세금계산서는 WHERE 가 **목록·카운트 2벌** → SSOT 통합. ★**구매요청 SSOT 에는 `MANAGER 는 본인 요청만` 규칙이 포함**된다 — 통계·카운트에서 빠지면 권한 경계가 무너진다. ★**재고 합계는 GROUP BY/HAVING 결과를 감싸서** 집계(품목당 1행으로 접은 뒤라야 수량·금액이 맞다). ★**거래처는 표를 JS 가 매번 새로 그린다** → 첫 렌더 뒤 1회 마운트, 열 숨김 CSS 는 클래스 선택자라 재렌더 후에도 유지(실측). 자체 '페이지당' 선택이 이미 있어 `showPageSize:false`(두 개 두면 정본이 모호). **미적용 3개 = 붙일 수 없어서가 아니라 붙일 대상이 아니다** — 출고(하루치 작업 보드·배지는 클라 집계·stats 호출처 0건)·원장(15탭 리포트 허브·이미 tfoot 합계행)·생산(대시보드 섹션). 각각 별도 설계 건. **prod 검증** = smoke 110/110·6개 엔드포인트 summary 응답(재고 1,073품목·거래처 2,787)·브라우저 실데이터(검색 시 칩·합계·총건수 동시 반영, 조회 1회). ⚠️지급요청 KPI 는 0건일 때 `SUM(CASE)` 가 NULL 반환 — UI 가 `|| 0` 로 처리하나 COALESCE 로 다듬을 여지. **출고 이력목록 1단계 배포완료 [2026-08-09]**(커밋 `8e505412`·배포 `3ab6664d`, 설계=`docs/specs/2026-08-09-shipment-history-list.md`) — ★**조사가 전제를 두 번 뒤집었다**: ①`GET /api/shipments` 는 **이미 완성된 이력 API 인데 호출처 0건**(죽은 엔드포인트) ②**prod `shipments` 0건** → 정본을 `orders.status='SHIPPED'`(10,073건)로 전환. **새 테이블·새 엔드포인트·전용 마이그레이션 없이** `/api/orders` 확장으로 끝냄(전용 API 를 만들면 조회조건이 또 두 벌 = 이번 감사에서 계속 잡아온 그 문제). ★**출고일 결측 1,420건이 전부 2026-07월**(최근 달) — shipped_at 만 쓰면 7월이 통째로 비고, COALESCE 로 조용히 메우면 「출고일」 열의 14%가 실제로는 주문일이 되어 **말없이 왜곡**된다 → **대체하되 숨기지 않는다**: 행에 「주문일 대체」 배지 + `summary.ship_date_missing` 경고 칩. 백필하면 자동으로 조용해진다. ★**회계 전표 195건(5.4억)이 status=SHIPPED**(기초채권 `E1-OPEN-*`·법인간미러 `ICM-*`) → `exclude_vouchers` **옵트인**으로 제외 + 제외 건수 칩 표기(기본 주문목록 동작은 불변). ⚠️`ship_date_to` 는 `+' 23:59:59'`(shipped_at 이 DATETIME 이라 날짜만 걸면 당일 오후 건이 빠진다) · `getBillingStatus*` 는 orders.js 전용이라 shipments 에서 부르면 ReferenceError · 「주문일 대체」 배지가 열 폭에 잘려 안 보였다(158px+overflow visible). **prod 검증** = smoke 110/110·전체 8,583건(전표 195 제외로 5.38억 감소)·7월 1,136건이 주문일 대체로 조회됨·6월 결측 0·89~160ms·화면 최근1개월 793건. **배송방법 `직접배송` 통일 배포완료**(커밋 `e65167c3`·배포 `61daee70`·마이그 **0526** prod 적용) — 법인별 표기차(직배=동산·직접배송=선명)를 통합해 **1,605건**(1,027+567+실주문 11), ★**`배송` 206건 중 195건은 전표라 NULL 로 비웠다**(합쳤으면 가짜 직접배송 195건 발생). `직배` 는 표시값이 아니라 **거래처 검증 화이트리스트·합배송 후보 탐지 집합**에 박혀 있어 `constants/deliveryMethod.ts` SSOT 신설, **과거 표기는 하위호환 유지**(이관·외부유입이 옛 값을 보내면 조용히 필터에서 빠진다). **7월 `shipped_at` 백필 완료**(마이그 **0527** prod 적용·커밋 `14936b7f`·배포 `de03fcee`) — NULL 1,420 → **0**. ★**백필하며 전제가 또 뒤집혔다**: ①**이카운트 원본에 출고일 항목 자체가 없다**(판매현황=전표일자 하나, `gen_sales_sql.py` 가 order_date·accounting_date·billed_at 에 같은 날짜) ②**마이그 0520 이 이미 상반기 8,653건에 `order_date 00:00:00` 을 넣어뒀다** ③prod 실측 **실제 출고시각 보유 행 0건**. ⇒ 백필은 '없는 값을 채우는 일'이 아니라 **7월을 나머지와 같은 규칙으로 맞추는 일**이었고, 더 중요한 건 **화면이 상반기 8,653건을 실제 출고일인 양 보여주고 있었다는 것**. → 판정을 `shipped_at IS NULL` 에서 **추정 지문 `shipped_at = order_date || ' 00:00:00'`**(`SHIP_DATE_ESTIMATED_SQL`)으로 바꿔 배지 「추정」·칩 '출고일 추정 N건 (거래일 사용)' 로 전환. **실제 출고시각이 기록되기 시작하면 시분초가 달라져 자동으로 빠진다.** **prod 검증** = smoke 110/110·전체 8,583건 전부 추정 표기·7월 1,136·6월 1,240(기존 백필분도 이제 추정으로 드러남)·합배송 후보 엔드포인트 정상(shipped_at NULL 을 미출고로 읽던 경로 해소). **생산 출력실적 탭 배포완료 [2026-08-09]**(커밋 `3d1300d8`·배포 `9935cfdf`, 설계=`docs/specs/2026-08-09-production-history-list.md`) — ★**출고 때와 같은 구조의 발견**: `cards`·`production_logs` **prod 0건**인데 `print_events` 는 **2,651건이고 8월에도 344건 쌓이는 중**. 주문·출고는 7월에 멈췄는데 **장비 로그만 살아 있다** → 정본 = print_events. `card_id` 연결이 **1건뿐**이라 거래처·금액을 못 붙인다 → **장비 실적**이지 매출 실적이 아님을 칩에 명시. ★**실적 = 건수가 아니라 면적(㎡)** — A4 한 장과 10m 현수막이 같은 1건일 수 없다. `CAST(w AS REAL)*CAST(h AS REAL)*copy_total/1e6`. **실측 3건 전부 해소**: ①치수는 **mm**(40mm~27.8m)인데 **TEXT 컬럼**이라 CAST 필수(안 하면 `minw='1000' > maxw='990.141'` 로 뒤집힘) ②`print_duration_sec` 은 1초~14,293초에 **100장 작업이 45초**·NULL 211건 → **합계에서 제외**, 열로만 표시(memory `reference-logwatcher-duration-semantics` 경고 그대로) ③타일 행은 **타일마다 폭이 다르다**(2473/1807mm) → 행이 이미 타일 단위라 `tile_count` 곱하면 중복, `copy_total` 만 곱한다. ★**취소 203·오류 6 을 기본 제외**(포함 시 면적 **5.4% 부풀려짐** 실측) — 합계에서 빼되 별도 항목으로 병기. ⚠️**`/api/equipment` 는 존재하지 않는다(404)** — 정본은 `/api/rip/equipment`. `production.js:1440` 기존 호출도 같은 404 를 조용히 삼키는 중(**별건 잔여**). ⚠️출력완료 열이 로케일 표기로 잘려 `MM-DD HH:mm`(KST) + title 전체값으로 교체. ⚠️**배포 후 브라우저 캐시로 구버전이 보일 수 있다** — 마커는 신버전인데 화면만 구버전이면 강제 새로고침. **prod 검증** = smoke 110/110·정상 2,442건 **61,818㎡** 장비21·8월 344건 12,416㎡·29~37ms·화면 1,215건 40,753㎡. **원장 탭별 조회 UX 배포완료 [2026-08-09]**(커밋 `7afcfa87`·배포 `4b6c76a5`, 설계=`docs/specs/2026-08-09-ledger-query-ux.md`) — ★**앞선 감사의 '원장 15탭'은 오기**(grep 이 탭 버튼 아닌 문자열까지 셈). **실제 3탭**(매출·매입·분석)·표 9개. 감사 문서에도 정정 남김. 9개 표를 성격별 4부류로 갈라 **A부류(거래처 채권·공급처 채무) 2개에만** 칩+도구모음. 12행짜리 집계표에 붙이면 도구가 내용보다 커진다. ★**실측이 잠복 결함을 드러냈다** — 서버는 #497 로 cap 대응 검색을 만들어 뒀는데(주석에 '로컬 필터는 cap 된 배열만 봐서 무음 누락'이라고 적혀 있다) **클라가 `search` 를 한 번도 안 보내고 `capped`·`totalCount` 도 안 읽었다.** 페이지의 `clientsCapNote` 경고 엘리먼트도 **아무도 안 채워** 늘 숨어 있었다. **prod 854/1,000(85% 소진)이라 아직 안 물릴 뿐** — 146곳만 늘면 검색 누락+합계 과소집계가 시작된다. → **매출 검색을 서버로 전환**(디바운스 350ms)·`matchedSummary` 신설(검색 뷰 합계가 표시행 합산이 되지 않게)·cap 경고 연결. **매입은 로컬 필터 유지**(공급처 38곳이라 cap 이 성립 안 함 — 규모가 다르면 답도 다르다). **합계 바는 안 붙였다** — `tfoot` 이 정본이고 둘이 어긋나면 어느 쪽이 맞는지 알 수 없다. 페이징이 없어 페이지당 건수도 뺐다(`showPageSize:false`). ⚠️분석 탭 명세 3표 칩은 **이번 제외**(드릴다운이라 컨테이너 위치가 다름·§7 잔여). **prod 검증** = smoke 110/110·검색 시 서버 호출 확인·합계행이 매칭분 기준(1개 거래처 123,666,000)·칩 '1/235개 거래처'·매입 탭 열선택 5열. **✅ 사후 점검 라운드 prod 배포완료 [2026-08-09]**(커밋 `2536d33c`·배포 `0b162bbc`, 감사 문서 §10) — 이식이 끝난 뒤 **실측 위주로 다시 훑어** 9건. 3건은 이식이 만든 게 아니라 **원래 있었는데 안 보이던 것**이다. ★**(P0) 주문 CSV 가 8,778건 중 3,000건만 내려가고 안내가 없었다** — 실측으로 마지막 행 `2026-05-15`·**5,778건(66%) 유실** 확인. 발주·입고·발주요청·자금계획·검수는 `#372` 스윕에서 이미 고쳐졌는데 **가장 큰 목록 하나만 남았다**(형제-불완전 스윕). 원인 구조 = 주문만 `csvStreamResponse`(스트리밍)를 쓰는데 **그 함수에 `footerNote` 가 없어** 안내를 붙일 수단 자체가 없었다 → 유틸에 추가(단일 소스 유지)·캡 5,000·캡+1 감지. **캡을 임시로 5 로 낮춰 절단 분기를 재현 검증**(5행+안내행) 후 원복. ★**(P1) 번호 열이 전 행에서 잘려 식별자를 못 읽었다** — 주문번호 108px(필요 **135**)·상태 86(101)·회계반영 82(101), 발주번호 `.col-code` 132(표준 15자 **~140**). 원인은 **번호 체계가 법인 접두(`E1-`)로 길어진 것** = 규격이 데이터를 못 따라간 것이라 페이지 패치가 아니라 **공통 `.col-code` 를 148px 로** 고쳤다. 발주 이관분 34자(`E1-PO-BK-PENDING-…`)는 **2건 때문에 317px 로 넓히지 않고**(중앙값 15자) `title` 로 남김 — 그 셀만 `escapeHtml` 도 빠져 있어 옆 열과 맞췄다. 주문 **긴급·타법인 배지를 아랫줄로** 이동(합배송 배지는 이미 같은 이유로 내려가 있었는데 **그 판단이 나머지 둘에 적용돼 있지 않았다**). ★**(P1) 원장 매출 프리셋이 기간을 잃었다** — `getFilters` 는 `startDate/endDate` 를 저장하는데 `applyFilters` 가 `search` 만 복원. **13개 목록 저장/복원 키를 스크립트로 전수 대조**해 찾은 유일한 비대칭(수정 후 13/13). ★**(P2) 기간·정렬이 「더보기」 안에 접혀 있었다**(주문) — 기본 1개월을 바꾸려면 두 번 클릭이고 정렬은 있는 줄도 모른다. 정작 「지연만」은 밖에 있어 **우선순위가 뒤집혀** 있었다 → 기본 노출. 겸사 **날짜 라벨이 「등록일」인데 실제 조건은 `o.order_date`**(주문일)라 표의 `created_at` 열과 헷갈리게 돼 있던 것도 수정. ★**(P2) 출고 이력에만 정렬 컨트롤이 없었다**(서버는 지원하는데 클라가 `ship_date_desc` 하드코딩 전송) → 셀렉트 추가. ★**(P2) 견적 CSV 신설** + **`QUOT_SORT_DEFAULT`(견적일순)를 핸들러가 `'created_desc'` 로 덮어써 SSOT 기본 정렬이 무력화**돼 있던 것 수정(이관 견적은 `created_at` 이 '이관 실행 시각'이라 무의미). ★**(P3)** `check:dom` 상시 5건(폐기된 소재 UI 잔재) 제거 → **OK 0건**(상시 경고는 회귀 신호를 죽인다). 클라 전 파일 `/api/*` 참조를 마운트 목록과 대조 → **죽은 호출 0건**(어제 "`/api/equipment` 죽은 호출 하나 더"는 **오기** — 주석을 셌다. 스펙 문서 정정). **검증** = tsc·build·**smoke 110/110**·`sort-audit` P1 0·`audit:entity` 0·`check:dom` **OK**·프리셋 대칭 13/13. **prod 검증** = smoke **110/110**·페이지 8/8 200·**마커 11/11**·주문 CSV 전체기간 **5,000행+안내행**(3,000 무고지에서 해소)·견적 CSV 200·주문 표 **잘린 열 0**(이전 3열×50행)·발주 번호열 148px 로 표준번호 해소, 34자 이관번호 2건만 남고 **title 로 복구 확인**·원장 프리셋 8월→적용→**3월 복원**(입력칸까지, 검증용 프리셋 삭제 완료)·출고 이력 정렬 6종에서 기본↔금액순 결과 상이·콘솔 0. **후속 열폭 스윕 배포완료 [2026-08-09]**(커밋 `66e16d84`·`72112df7`·`5eb43d16`, 배포 `2908345d`→`41ec5bed`→`c01bf6a4`) — `.col-code` 를 148px 로 넓힌 뒤 **그 클래스를 쓰는 20개 페이지를 전수 실측**했다. ★**광폭화로 새로 생긴 잘림은 0건** — flex 열이 자기 `min-width` 아래로 눌린 표는 `/hr` 하나뿐이었고 그것도 **변경 전부터 이미 잘리고 있었다**(필요 187px, 변경 전 146·변경 후 130). 대신 **기존 잘림 6개 페이지**가 드러났다. 고친 것 = ①**`/hr` 사번**이 `col-code`(148px)인데 실제 `DS-044` 는 80px 면 된다 → 88px 로 줄여 60px 를 이름에 넘김(이름 130→**190**, 잘림 0). ②**`/bank`(자금관리) 계좌번호**가 `col-tag`(92px)인데 179px 필요라 **10/10행이 잘리고 title 도 없어 계좌번호를 아예 못 읽었다** → 188px. 잔액도 음수 9자리가 넘쳐 134px. ③**정기지출 「최소~최대」**(금액2개+물결표)가 `col-amount`(120px)에 **35/35행 잘림** → 200px, 재원은 `col-status`(96px)인데 '6'·'변동' 뿐인 개월·성격에서(60/68px). ★**한 열을 넓히면 옆 flex 열이 대신 잘린다** — 범위만 넓혔을 때 통장표기가 새로 잘려, 같은 표 안 과대폭 열에서 재원을 찾아야 했다. 남은 것 = 통장표기 1/35행(322 vs 327px·내용 무한가변이라 추격 무의미)·`/equipment` 이름 2/25(title有)·`/activity-log` 일시(로케일 표기 188px)·`/tasks`·`/storage-zones` 의 `col-tag` 2~51px 초과 — **전부 기존 결함이고 내 변경과 무관**. ⚠️**멀티세션 사고 재발** = push 거부(타세션 선행 2건)인데 **배포가 먼저 나갔다**. 다만 선행분이 전부 `.md` 라 코드 되돌림은 없었고 병합 후 정상화([[feedback-multi-session-deploy]] — **push FIRST 를 또 어겼다**). ⚠️**배포 후 브라우저가 구버전을 보여준다** — 서버 번들엔 새 폭이 있는데 화면은 옛 폭이라 "미반영"으로 오판했다. 캐시버스터로 재확인. ⚠️**검증 스크립트 함정 2건**(내 오판) = ①SPA 페이지 응답은 **JSON** 이라 `id="x"` 생문자열 매칭은 이스케이프 때문에 전부 NG 로 나온다 → `pageContent` 파싱 후 검사 ②도구모음은 **마운트 시점에 프리셋 목록을 읽는다** → API 로 만든 프리셋은 새로고침 전엔 셀렉트에 없다. **남은 것** = 반품 2단계(returns 0건 — 등록 UI 는 `/quality` 에 **이미 있다**)·`orders.is_voucher` 플래그·분석탭 명세 칩·**CSV 없는 목록 5개**(지급요청·매입계산서·재고·거래처·출력실적 — 조회조건 SSOT 빌더가 없어 지금 붙이면 WHERE 사본 5벌이 생겨 이번에 없앤 결함을 재생산한다. 필터 추출이 선행)·**`/api/reports/entity-attribution-audit` UI 없음**(어디에 붙을지가 제품 결정 — `/reports` 는 매출 분석 탭 묶음이라 데이터 품질 감사와 성격이 다르다)

- **▶ [2026-07-30] 동산기획(entity 1) 이카운트 이관 — ✅Phase 0 내보내기 완료, Phase 1(세부점검) 대기**: **확보 파일 9개**(`품목마스터/원천주문데이터/`): 판매현황 `동산_판매현황_2601~2606.xlsx` 6개(라인 **17,335**·전표 7,705·공급가 **2,942,066,965**·부가세 270,306,095) + `동산_수금현황_2601-2606.xlsx`(라인 2,934·**3,043,174,438**) + `동산_거래처별채권_2601-2606.xlsx`(821행·잔액>0 205곳·잔액 **633,929,864**) + `동산_거래처등록_20260730.xlsx`(**3,294건**·사용중 2,759). **★★3중 교차검증 성공**: 채권파일 재고매출 3,212,373,060 = 판매현황(공급가+부가세) **정확 일치** / 수금합계 = 수금현황 파일 **정확 일치** / 잔액 검산 **오차 0**. 선명과 달리 원천 단계에서 이미 정합. **★품목코드·거래처코드 커버리지 100%**(17,335/17,335) → 품목 정밀매칭·BRN 매칭 기반 확보(선명 이름매칭 리스크 원천 제거). **다음=세부점검 4종**(①거래처 미등록 목록 ②품목 매칭률+신규 후보 ③월별 금액 대사 ④법인간거래 중복) → 승인 → 적재. 스펙·레시피·함정 정본=`docs/dongsan-import/EXPORT-SPEC.md`.
  - 이하 Phase 0 경위: 스펙 정본=`docs/dongsan-import/EXPORT-SPEC.md`. 선명(entity 2) 플레이북 재사용(`docs/sunmyung-import/`·[[project-sunmyung-item-import]]). **결정**=①Chrome 대행 내보내기(비밀번호 미수령, 용준님 직접 로그인) ②**상반기 2026-01-01~06-30 우선** ③품목 매칭 **금액 95%+ 정밀**. **prod 실측**: entity1 orders **10**·purchase_orders **8**·payments **3**(백지 = 초기 적재, 병합 아님). 기존 10건 중 7건=`ICM-AR-E1-*` 법인간거래 채권 118.6M → 이카운트 내부거래(선명·청주) 라인과 **중복 주의**. 나머지 3건=테스트 주문(삭제 여부 미결). **★보유 파일 3개(`동산1.1~3.3` 등)는 사용 불가** — `주문서현황내역` 양식이라 **거래처코드(BRN)·품목코드·부가세 없음**+품목명/규격 병합 → 선명에 쓴 **`판매현황` 양식으로 재내보내기 필요**. 규모=15,957라인/7,016주문/거래처 770/품목라벨 6,770 = **선명의 약 7배**(이카운트 리포트 ~5,000행 한계 → 2~3개월 분할). **매칭 기반 준비됨**: `clients` 2,696건 client_code 100%가 사업자번호 형식 → BRN 직결(선명 교훈 "BRN ≫ 이름매칭"). 필요 리포트 5종(판매현황·구매현황·거래처별채권·거래처별채무·거래처목록)+선택 2(통장거래내역·재고현황) **전부 미보유**. ⚠️**매입은 `/migration` 미지원 → SQL 직접**, `purchase_invoices`만 넣으면 미지급·재무제표 전량 누락(정본=`purchase_orders`). ⚠️금융 write는 분류표 CSV→승인→SQL(+롤백) 2단계. 다음=이카운트 로그인(용준님) → 리포트 5종 내려받기.

- **✅ [2026-07-30 #2] work.ai 용량 근본 조치 — `pdfCompatible=false` 축2 반영 + 임베드 여분 계측·경고 + 용량 감사 신설**: **결정 = 가) 단독**, 단 근거가 바뀌었다. ①**★디자이너 등록 0건 실측** — Z: 등록 폴더 호스트가 **전부 `DESKTOP-62VJ5FA` 31건·타 PC 0건** = 디자이너는 패널을 아직 안 쓴다 → "Z: 교체=전 PC 즉시 반영" **위험 노출이 0**. 그래서 껍데기 배포와 묶을 이유가 없고, 지금 넣어야 **디자이너 첫 사용 + 새 저장옵션 동시 투입**(이상 시 원인 분리 불가)을 피한다. ②**★"1줄"이 아니었다 — `.ai` 저장 3곳**: `mes-a0-host.jsx:345`(대형 전량 출처. 상위 9개 884MB 전부 `script_version=A0-CEP-0.1.0`·`source=cep` 실측) · `mes-sheet.jsx:311`(판 .ai = 조각 work.ai를 실물크기로 실어 조각 수만큼 곱해짐) · `mes-core.jsx:359`(07-28 은퇴지만 Z:에 남아 실행 가능 = 지뢰) → **3곳 전부 수정**. ③**리스크 검증 통과** — `place`(링크) 배치가 있으면 PDF 스트림 필요로 깨지는데 **0건**: 소비는 전부 `app.open`(`mes-sheet.jsx:237`·`SheetLayout.jsx:150`), 에이전트 `Program.cs:1547`은 경로 반환/바이트 복사만·PDF 라이브러리 의존 0. ④**근본 원인 개입(host `0.1.4`)** — 스크립트로 줄일 수 없는 임베드 래스터 여분을 **계측해 디자이너에게 되돌린다**: `rasterItems.geometricBounds` vs 아트보드 교집합으로 밖 비율 산출 → `MESA0_RASTER_OUT_PCT=30` 초과 시 `preflight.oversize_rasters`·`oversize_max_pct` + `files.work_bytes` 기록 + `warn 'E'`. 패널은 단건뿐 아니라 **모아찍기 행별로 용량(`work.ai NNMB`)·경고 노출**(사고 경로가 모아찍기였는데 거기엔 표시가 **아예 없어** 5건 524MB 쌓인 뒤 발견된 게 근본 원인). ⑤**신설 `npm run audit:ia-storage`**(`scripts/ia-storage-audit.cjs`) — `.ai` **첫 바이트로 판정**(`%PDF-`=켜짐=같은 그림 2벌). 날짜 상수를 안 박고 **배포된 로직 본문의 `pdfCompatible=false` 유무 + 수정시각**으로 자기 교정해 **회귀만 exit 1**(미반영은 정상으로 통과). 베이스라인 실측 = Z: 총 **2,758MB**(work 58개 971.7 · EPS 37개 1,774.4 · 판 1개 10.1 · thumb 1.9), PDF호환 59개 981.8MB → **절감 여지 511MB**. ⑥**★EPS가 work.ai보다 크다**(1,774MB) — DOS-EPS 헤더 실측으로 **TIFF 미리보기는 0.7~0.8%뿐**이고 323MB가 PS 본문(임베드 래스터) → pdfCompatible과 **무관**. `_출력` 사본 1,066MB는 **RIP·재단 픽업 지점**(spec D8 = C단계 제거 예약)이라 **손대지 않음**. ⑦**검증** = `node --check` 4파일 · **`panel:smoke` 30/30**(용량 표시·임베드 경고 어서션 2건 신설, 스텁에 `bytes/oversize/warn:'E'` 추가) · `audit:ia-jsx` **축2 3/3 일치** · `audit:ia-storage` exit 0 · Z: 백업 `_scripts\_backup\20260730-pdfcompat\` · 이 PC 축4 재설치(`-Source` repo). ⑧**CLAUDE.md 정정** — 축 3개→**4개**(축4=PC 설치본) + **축3은 즉시 반영이 아니다**(PC별 `install-a0-panel.ps1`) 명시. ⚠️**미검증(정직)** = 실등록 1건 자연검증(**일러 재시작** 후 패널로 등록 → work.ai 첫 바이트가 `%!PS`·용량 절반 확인). **소급 안 됨** — 기존 59개 981MB는 그대로(재저장 배치는 일러 점유 필요·별건). ⑨**축3 디자이너 배포 실행**(사용자 지시, 같은 회차) — Z: 배포본 6파일 갱신(백업 `_scripts\_backup\20260730-panel\`) → **4축 전부 일치·드리프트 0** → **Z: 배포본을 `-Source` 로 이 PC 재설치까지 실기기 검증**(디자이너가 받을 경로와 동일). 배포된 로직 = `A0-CEP-0.1.4`. **남은 것** = ⓐ**각 디자이너 PC에서 `Z:\DESIGNS\IA-등록\_scripts\install-a0-panel.ps1` 1회 실행 + 일러 완전 재시작**(원격 실행 수단 없음 → 사람이 해야 함. DESIGNER 4인=인호동14·김보연8·김영주15·정소은16) ⓑ기존 981MB 재저장 여부 ⓒia-editor 브랜치 미머지. 정본 = memory `design-ai-save-pdfcompatible`.

- **🟡 [2026-07-30] work.ai 110MB 원인 규명(★전제 정정 — 조치 완료 = 07-30 #2 / ia-editor 브랜치 미배포 잔여)**: ①**★"클립 마스크 밖 벡터"가 아니었다** — COM 미사용(일러에 `네스팅23-95x329-4건.eps` WS 1.88GB 열려 있어 프로브 회피, 12분 wedge 전례)으로 **파일 바이트를 PDF 오브젝트 단위 분해**. 107MB = **PDF 합성부 55.7MB(52%) + AI 편집부 51.3MB(48%)**, 그런데 **벡터 경로는 압축해제 0.11MB(0.1%)** 뿐이고 내용물은 **임베드 래스터 49개 54.1MB**다. 아트보드(22.4×28.0cm) 대비 배치 판정 = 안 완전포함 39개 12.8MB / **경계 걸침 10개 41.3MB**(배치 53.6×23.0cm = 아트보드 폭의 2.4배, 각 이미지 **면적의 67%가 밖**) / **밖 완전이탈 0개 0MB**. 즉 **지울 수 있는 "밖 오브젝트"가 0개** — 밖에 있는 건 별개 아트가 아니라 대형 래스터의 잘려나간 부분이라 "밖 아트 삭제"는 **용량 0이고 삐져나옴도 안 고쳐진다**. 2번째 파일(109.9MB)도 51.9/48.0으로 재현. ②**선택지** — **가) `IllustratorSaveOptions.pdfCompatible = false`**(`mes-a0-host.jsx:345` **1줄**) = **−52%(107→51MB) · 처리시간 감소(쓸 데이터 절반) · 결과 무변경** / 나) 밖 오브젝트 삭제 = **0MB(실측)** 채택가치 없음 / 다) 걸친 래스터 물리 크롭 = −55MB지만 재래스터화·투명도 병합(SMask 50개)·**해상도 고정**이고 판짜기가 work.ai를 실물크기로 확대하므로(`mes-sheet.jsx:250`) 화질 굳음 + 임베드 이미지 크롭 API 없음(`Crop Image`=대화형) / 라) 300DPI 다운샘플 = −23MB인데 **큰 이미지는 이미 237dpi**라 이득 전부가 1013dpi 소형 로고에서 나옴. **권고 = 가 단독**(다는 가와 절감분이 겹쳐 추가 이득 27.5MB인데 리스크를 다 짊어짐). ⚠️`ExtractGroups.jsx:492`의 `pdfCompatible=true`는 주석상 이유가 **저장 대화상자 차단**(옵션 객체 명시 자체가 목적)이지 PDF 호환성이 아니라 제약이 아니다. **소비자 전수 확인=전부 일러**(`mes-sheet.jsx` app.open · `SheetLayout.jsx` · 에이전트 `ResolveSourceAiAsync`), MES 웹은 경로 문자열만 씀 → 잃는 것은 탐색기 미리보기·Bridge 썸네일(같은 폴더 `thumb.png`로 대체). ③**용량 분포** — work.ai 58개 0.95GB 중 **9개가 884MB(93%)**, 중앙값은 0.49MB. 109.87MB 5건은 바이트 크기 차이가 수십 바이트라 **같은 소스 디자인의 반복 등록**이다 → 전역 문제가 아니라 특정 디자인 2~3건 문제이고, 근본은 디자이너 원본의 임베드 이미지(별건). ④**미측정(정직)**: 가의 실제 저장시간 변화·다의 소요는 일러 유휴 시 타이밍 JSX 1건으로 확인해야 정확. 위 시간은 **폴더명 시각→파일 기록시각 실측**(105MB=7~13초·10MB=2초 ≈ 0.1초/MB)+데이터량 비례 추정. **가는 기존 884MB를 소급 축소하지 않는다.** ⑤**ia-editor 모아찍기 대기함 #576 이식 완료**(worktree `session/iae-tray-search` `2fe74b91`, **main 미머지·미배포**) — 주문서 트레이만 #576으로 서버검색·절단경고를 받았고 ia-editor는 응답의 `total`·`truncated`를 **받아놓고 쓰지 않아** 200건 상한 초과 시 헤더의 "전체 N건"이 거짓(`all.length` 표시)이고 오래된 대기물 도달수단이 아예 없었다. `q`·`date_from`·`date_to`를 서버로 내림(`q`=거래처·담당자·메모·키워드·묶음 LIKE → **담당자 이름으로도 상한 밖에 닿음**)·헤더를 서버 `total` 기준으로·검색중엔 '전체'가 아니라 **'검색결과'** 라벨(거짓 방지)·절단 경고 배너·빈화면 원인 3분기 안내(내작업/검색조건/진짜없음)·**조회 실패 시 검색줄까지 사라져 재시도 수단이 없던 것** → 버튼 유지. 검증=tsc 0·build OK·`node --check`·check:dom 9(기준선 불변)·번들 마커·로컬 API 실호출(파라미터 수용·`total/returned/truncated` 응답)·**DOM 기능검증 33/33 PASS**(linkedom 스크래치패드 격리, 1234건/200건 절단 상태 재현 — 로컬 D1 `designer_intakes` 0행 + dev 서버(포트 3000)가 타 세션 것이라 브라우저 E2E 불가). ⑥**축 드리프트 실측**(`npm run audit:ia-jsx`) — 축1 4/4 · **축2 3/3 일치**(로직 `mes-a0-host.jsx`는 Z: 이미 최신) · **축3 3건 드리프트**(`index.html`·`css/style.css`·`js/main.js` — Z: 배포본이 07-27~28자 구버전) · 축4 이 PC 6/6. **디자이너 배포 순서 = ①축3 Z: 갱신 → ②각 PC `install-a0-panel.ps1`**(순서를 뒤집으면 구버전이 깔린다). 사용자 지시로 **배포 시점 대기**.

- **✅ [2026-07-29] A0 패널 리모델·버그 3건 — 실기기 검증 통과, 디자이너 PC 배포 대기**: **실사용 검증 완료**(패널 `A0-CEP-0.1.3`으로 등록된 impose 4건이 크기 정상 199.5×49.5·42×210·48.5×98.5·115×30 + `qty=1` + 전부 `absorbed` = 판까지 완주). **MES 연결 전수 점검**: Z: manifest **58건 = .ingested 58건 · .rejected 0건 · 미처리 0건** · prod `designer_intakes` 64건 중 **크기 누락 0 · 분석 연결 누락 0 · 단건 24건 전부 EPS 보유**(file_path=EPS 규칙 성립) · 목적지 라우팅 실측(주문서 트레이 `mode=single,both`=23건 전부 single·누출 0 / ia-editor `mode=impose,both`=0건, 전량 absorbed·void라 정상) · EPS 디스크 실존 4/4. **남은 것**=①클립 밖 아트 물리 제거 여부 판단(work.ai 110MB·출력은 정상) ②축3(Z: 패널 배포본) 갱신 + 디자이너 PC `install-a0-panel.ps1` — 미설치 상태라 대기 ③manifest `border_line`은 ingest가 무시(추적용으로만 남음, 필요하면 컬럼 추가). ⚠️패널 껍데기 수정은 **PC별 재설치** 필요(Z: 갱신만으로 반영 안 됨). 이하 상세: **★탭 = 작업 갈래로 재편**(사용자 지시). 이전 탭은 `가공/후가공/큐` = **설정 종류** 기준이라 `용도` 라디오 하나가 세 곳을 원격 조작했다(실행 버튼 의미·후가공 탭 잠금·힌트) — 그 구조에서 "모아찍기인데 전체가 1건(539×243.3cm·work.ai **110MB**)으로 등록"이 났다. 이제 **`단건`/`모아찍기`/`묶음`** 3탭이고 **탭이 곧 용도**(라디오 제거, `modeValue()`=활성탭 파생). 후가공은 최상위 탭에서 **단건 탭 안 접이식으로 강등**(모아찍기엔 존재 자체가 없어 "잠긴 죽은 탭" 소멸). 공통 정보(가공자·실측·거래처·수량·배율)는 **탭 밖 상단 고정**. 모아찍기 탭은 분리→목록→등록 **자체 완결**(호스트 `mesA0_queueAddBatch`/`mesA0_autoDetect`·`runBatchConfirm` 재사용, host.jsx **무변경**). `setMode`는 용도가 같으면 탭을 안 옮긴다(묶음 탭에서 행 클릭 시 단건 탭으로 튕기는 것 방지). **★1덩어리 경고 신설** — 분리 결과가 1개면 "선택이 그룹 하나인지 확인(Ctrl+Shift+G)·분리간격 낮추기·자동감지" 안내하고 **자동 등록하지 않음**(이전 P2의 "조각 1개면 즉시 등록" 경로가 사고의 직접 원인이라 철거). 큐는 1개를 두 탭이 각자 렌더(`renderQueueInto`), 용도 섞이면 등록 거부. **★배율은 정상**이었음 — 539=파일 53.9cm×10, manifest `scale_pct:10`·`measured_cm{539,243.3}`로 확정. 배율 버그가 아니라 **잡힌 범위가 조각이 아니라 전체**였다. **★패널 헤드리스 스모크 신설** `npm run panel:smoke`(`scripts/panel-smoke.mjs`) — Chromium + `window.__adobe_cep__` 스텁(CSInterface 를 갈아끼우면 패널 자체 shim 이 덮어써서 무의미)으로 19항목 검증: 초기화 무예외·탭=용도·후가공 자동초기화·분리→목록→등록 게이트·행별 수량·재분리 거부·1덩어리 경고·묶음 탭 유지·P3 선택 호출. **이 스모크가 실제 버그 1건을 잡았다**: `#finBody`(id) 규칙이 `.hidden`(클래스)을 이겨 후가공 접힘이 안 먹던 것 → `#finBody.hidden` 로 되받음. 사용자 지적 4건 전부 구현. **P2**=`[모아찍기 추출]`이 선택분을 **자동 개별 분리**(1클릭=분리·큐 적재·크기 목록 표시 → 2클릭=조각별 등록. 조각 1개면 확인 없이 바로 등록해 기존과 클릭 수 동일). 분리는 `mesA0_queueAddBatch`(큐 탭 [＋묶음분리]), 등록은 `runBatchConfirm`([일괄 확정] 루프를 함수로 추출) **재사용** — 산출 구조가 batch734와 동일. 큐에 잔여 행이 있으면 **거부**(남은 큐를 휩쓸어 등록하는 사고 차단). **모아찍기 전용 큐는 검토문서 게이트 면제**(검토문서는 마감·돔보·주석 배치를 보는 장치인데 모아찍기는 그 셋이 없음. 단건이 섞이면 게이트 유지) + `검토문서 ✓` 라벨은 `reviewedRev===queueRev` 기준으로 정정(면제된 큐가 검토한 적 없이 ✓로 보이던 것). **P3**=큐 행 클릭 시 `mesA0_queueSelect(i)`로 **원본 조각을 일러에서 선택**(검토·확정 루프가 쓰던 함수 재사용). 실패(문서 닫힘·참조 무효)해도 폼 연동은 유지. **P1**=`clearFinishing()` 신설 → ⓐ모아찍기 전환 시 ⓑ단건 가공 완료 후 ⓒ일괄 확정 완료 후 호출 + `gatherParams()`가 impose면 `finishing`·`punch`·`annot_pos`를 **전송 단계에서도 잘라냄**(disabled 입력도 `.value`는 읽히므로 화면 잠금만으론 못 막음 — intake #28이 mode=impose인데 `finishing.left=접어미싱 4cm`·`post_desc=접쫑접어미싱`으로 기록된 실증) + **후가공·펀칭·주석위치를 localStorage 영속 대상에서 제외**(안 빼면 패널 재시작 시 되살아나 리셋 정책이 무의미). **P4**=큐 행에 수량 인라인 입력(키워드와 동일 패턴)·`params.qty` 동기화·연동 행이면 폼 수량도 갱신·재렌더 안 함(포커스 유실 방지, 그래서 메타 문자열에서 수량 제거). **★배포 구조 발견**=패널은 **2단**(로직 `mes-a0-host.jsx`=Z: 1개 교체로 전 PC 반영 / 껍데기 `index.html`·`main.js`·`style.css`=**PC별 복사 설치**, `install-a0-panel.ps1` 재실행 필요). 이 PC 설치본이 07-27자로 낡았고 `jsx/host.jsx`가 스텁이 아니라 **구 로직 본체**였음(해시로 확정: 설치본 host.jsx == `mes-a0-host.jsx`) = Z: 로직 수정이 이 PC에 전혀 반영되지 않던 상태 → 재설치로 해소(축4 6/6 일치). **감사 도구에 축4(PC 설치본) 추가**. 디자이너 PC는 **아직 미설치** → 이 PC 테스트 완료 후 배포 예정(Z: 배포도 대기). 검증=`node --check` OK·4축 감사 통과.

- **📝 [2026-07-23] IA 편집기 세션루프 v2 설계 spec + Phase A0 PoC (문서화만·코드 무변경)**: 일러 JSX "선택→실행" 방식 심층 브레인스토밍 → 세션루프 UX 전반 재설계. **정본 spec**=`docs/superpowers/specs/2026-07-23-ia-palette-session-loop.md`(D1~D9·기능표 A/B/C/D·Phase·3차 검토 반영). 핵심결정=**ScriptUI 도킹 팔레트**(네트워크 불요·저리스크)·**반자동 큐**(사람선택+ExtractGroups 자동감지 시드, 혼합 컨테이너라 단독자동 금지)·**검토=일러 검토문서(아트보드)+확정 게이트**(size-aware 순차 폴백=대지 577cm 한계)·**거래처 단일입력+상속(clients.id)**·**대기물=batch_key(원본파일) 그룹핑+"내 작업"=MES 로그인**·**오퍼레이터=기존 카드/생산+/ship 확장**·파일 SSOT/취소시 유지. **검토 3회**(설계공백3+코드결함A~F+G~K: 원본불가침·EPS파일명충돌·배치흡수 order_item_id매핑·브랜치정합 등). **A0 PoC**=`IllustratorAutomat/designer/poc-a0/`(도킹 팔레트·roster·대지한도 probe·README, node --check 3/3). **별건 Task#1**=사용자 하드삭제(FK가드). **다음=디자이너 최신 일러서 A0 도킹 Go/No-go→A1(worktree) or CEP승격**. spec/PoC 미커밋(untracked). 정본=`memory/session-context.md`·[[project-ia-designer-loop]].
- **🚧 [2026-07-08] IA 멀티소스 임포지션(모아찍기) — P1 배관 prod 배포·E2E 검증 완결(main `6ff41f40`, dep `0420cf3a`)**: 여러 파일의 아트보드를 한 판(시트)에 모아 배치·출력. brainstorming 확정(산출=EPS/JPG/DXF Export-first·이종+동일 혼합·자동패킹·배율×N·롤5(90/105/127/137/152)/평판2(900×1800·1200×2400)·도련=조각별bleed·돔보=전체판원+DXF조각외곽선). **P1**=단일소스 가드 4곳 제거+placement별 analysis_id 소스 라우팅(전부 하위호환, sources 없으면 단일 폴백=기존 네스팅 회귀0). 4파일: **SheetLayout.jsx**(_params.sources[] 각 문서 open→groupsByAid, placement.analysis_id로 소스 선택 크로스-도큐먼트 복제·srcDocs 전부 close)·**Program.cs**(job.sources[] 각 .ai 로컬/R2 다운로드 ResolveSourceAiAsync→ia_params.sources·placement analysis_id 통과)·**workbench.ts**(render-queue가 source_analysis_ids 배열의 각 file_path를 sources[]로 반환·sheets render aids.length!==1 제거→각 aid done 확인)·**iaEditor.js**(iaeCanSyncSheet placement.analysis_id=o.fid·reassign anchor 축소 제거·export source_analysis_ids 배열 통일=기존 String 비배열 render 실패 잠복버그 해소). 빌드 tsc·vite·dotnet build 0err. **E2E**: sheet_layout(analysis15+16→롤137판 2조각) 에이전트 실렌더 로그 "소스.ai req_15+req_16·멀티소스 2개 준비 완료" **2소스 로드 확인**(배포 전=단일폴백 req_15만 → 배포 후=2소스). ⚠️시각 구분(서로 다른 디자인) 검증은 req_12(17MB) COM 모달 hang로 보류(15/16 동일 디자인) → 실사용/P4 확인. 에이전트 PID 43340 실행 중. **P2 임포지션 UI ✅구현·검증 완료(브랜치 `feat/ia-imposition-p2` `a3d37ecb`, 미배포)** — 대지편집 탭 서브모드 토글(네스팅|모아찍기 신설, 기존 단일그룹 네스팅 회귀0 보존)·다중선택 팔레트(iaeCanAllGroups 체크박스)·조각별 인스펙터(배율×N·개수·도련)·자동패킹(iaeNestPackBest 재사용, 이종 소스 한 판, id로 소스 역매핑→placement별 analysis_id). 도련=재단여백만 확보(아트워크 원본크기 유지, 재단선=DXF·P4). 출력=P1 경로 재사용(source_analysis_ids 배열). 결정(사용자 부재→권장안): ①별도 서브모드 ②도련=재단여백만. 검증=tsc·vite 0err + Node vm 로직 20/20(이종2소스→3조각 distinct analysis_id·group_index 라우팅·배율·export payload·평판다중판 id보존·단일소스 회귀) + 패널 렌더 스모크 14/14. **P3 하이브리드 미리보기 ✅구현·검증(브랜치 `feat/ia-imposition-p2` `1b9cb30a`, 미배포)** — **P3-a 웹 썸네일 배치도**(iaeNestRenderPreview 파란박스→조각별 실제 썸네일 채우기, placement analysis_id:group_index로 소스 이미지 매핑·회전 조각 rotate transform·썸네일 없으면 폴백) + **P3-b '실제 렌더' 버튼**(SheetLayout `preview_only` JPG-only 조기종료 모드: EPS/DXF saveAs·R2·이력 스킵. 4레이어=iaEditor.js 버튼/폴링/jpg_base64·jpg_r2 양쪽 처리·workbench render-queue가 canvas_json 통과라 마이그 불필요+preview 시트 1h TTL·Program.cs ProcessSheetRenderAsync preview 분기 temp/jpg_base64/temp삭제·SheetLayout.jsx eps→jpg 가드·조기종료). 회귀0(preview_only 없으면 기존 byte-identical). 검증=tsc·vite·dotnet build 0err + Node vm P3 10/10(멀티소스 썸네일·회전·폴백·preview_only 주입·회귀) + P2 회귀 20/20·14/14. **+로컬 브라우저 실증(Playwright, 데모 D1 시드 2파일×2아트보드)**: 모아찍기 팔레트 4그룹·이종 2소스 선택·조각별 개수(2+4)→자동배치 placement analysis_id {1:2,2:4} 정확·**썸네일 실렌더**(teal/orange 6조각 시각 구분·회전 코너)·실제 렌더 버튼→preview 시트 생성(is_preview=1·source_analysis_ids[1,2])·네스팅 단일그룹 회귀0·임포지션 콘솔에러 0. **★프리픽스 버그 발견·수정**(`cc6e8258`): 썸네일 raw base64에 `data:image/png;base64,` 미부착으로 실 이미지 깨짐(1×1 플레이스홀더라 잠복)→iaeThumbSrc 헬퍼. **+사용자 피드백 반영**(`7328171d`): ①패킹 **MaxRects 고정**(선반 드롭다운 제거, 두 패널)·iaeShelfBinPack 함수는 유지(폴백) ②**가장 효율 좋은 롤 폭 추천**(iaeImposeRecommendWidth: 전 롤 프리셋 MaxRects 패킹→재료면적 최소 폭 자동 적용, 롤 모드). 로컬 실증=드롭다운 제거·폭추천 1050→1520mm→효율 67%→91%. **✅P4 배포·출력 E2E 완결(prod, 2026-07-08)**: origin/main rebase(봇 결제예정 위)→push `19ea47ff`→**deploy:prod `--branch main`**(dep `4a315535`)·apex 함수 검증 라이브·콘솔0. **에이전트 재publish**(PID 43340 중지→dotnet publish[previewOnly 포함]→PID 29676, appsettings=prod). **prod 실렌더 E2E(analysis 15+16)**: 임포지션 {15:2,16:2} 라우팅 → ①**preview_only**(P3-b): done·jpg_base64 262KB·**eps_r2=null**(EPS 미생성 정확)·preview:true·실제 IA렌더 JPG 프론트 인라인 "75×11cm" ②**EPS 출력**: multi-source EPS+DXF+JPG 전부 R2·유효 시그니처(EPS `c5d0d3c6` 3.7MB·JPG `ffd8`·DXF `SECTION` 179KB). 테스트 시트 정리. ⚠️15/16 동일 디자인이라 시각적 소스구분은 미확인(메커니즘 입증·P1 동일 한계). **★임포지션 전체(P1~P4) prod 라이브·완결.** **✅[2026-07-10] 사용자 실사용 버그 수정(배율×N EPS 실패)**(`a326c143`, 에이전트 전용·publish+재기동 반영·웹배포 불요): "배율10 EPS 출력 실패(SheetLayout 결과 없음)" 근본원인 2건 확정(prod 실패시트·재현·COM 실렌더) — ①SheetLayout이 조각을 native 크기로만 배치(placement 크기 리사이즈 누락)→배율×N이 도안 안키우고 판만 N배→621.9cm가 Illustrator 한계(~577cm) 초과→documents.add PARM ②에이전트 축소로직 `500/maxDim`(<1)이 오히려 확대. 수정=SheetLayout 조각 duplicate 후 placement 크기 resize(배율=1이면 스킵·회귀0)+Program.cs `scaleFactor=ceil(maxDim/500)`(≥1) 축소렌더→scale_factor=N RIP 확대. 검증=COM 실렌더(gi3 5.8×29.3→29×146.5cm 리사이즈)+prod 풀파이프라인(622cm 시트 done·5조각 정상·scale_factor=2). ⚠️운영: 축소렌더 시 RIP는 파일명 실물크기로 출력. **+Illustrator 한계 경고·축소권장 UI**(`e4340249`): 모아찍기·네스팅 예상치가 >560cm면 amber 경고("1/N 축소 렌더·배율/개수 축소 권장")+자동배치 토스트, 에이전트 임계 500→560. **+#505 IDOR ✅해결**(`66c59e37`, worktree 격리배포·apex 403·이슈 close): POST /sheets가 source_analysis_ids entity 소유 무검증→타법인 분석ID 주입 시 render-queue file_path 노출+에이전트가 타법인 디자인 렌더. entityFilter 가드(미소유 403), /process 패턴 미러. **⏸이형(비사각형) 네스팅=추후(사용자 지시)**: 현행 MaxRects bbox 유지 / 수동 인터록 재도입(W5서 자유드래그 제거됨) / 이형자동 NFP·SVGnest(큰 작업·다이라인 필요·brainstorming 선행). 현 추출은 이미 아트보드 기반(v6.2)이라 아트보드 단위≠비사각형. ※EPS 실저장은 에이전트 기동+실파일+배포(P4) 필요 — 로컬 데모(가짜 r2 경로·에이전트 오프라인)론 큐만 쌓임. ※로컬 `/api/workbench/process` 500=ia_process_jobs 로컬 미적용(마이그 0386, 가공 이력 보드·임포지션 무관·prod 정상). spec=`docs/superpowers/specs/2026-07-08-ia-editor-multisource-imposition.md`. ⚠️preview_only 실렌더는 에이전트 publish 필요(구 에이전트는 jpg_r2 폴백으로 동작)→P4에서 함께. ⚠️브랜치→main 머지(봇 커밋 위 rebase).

- **▶ [2026-06-30] 품목 미등록 갭 감사 + 판재 등록 (worktree `session/master-banner`)**:
  - **갭 감사**(에이전트, 원천 18,491라인 vs prod 371등록): 등록률 79%. **미등록 갭 6.4억의 본질=간판 사업부 전체**(채널·프레임·돌출·LED/SMPS·갈바·각관 ~900라인·3.19억, 마스터에 `간판` 분류 자체 없음). 그다음 게양/부속(~618·1.54억)·판재(~606·0.51억)·자재원단(~128)·상품(~78). **출력물은 사실상 전량 등록**(배너 마무리로).
  - **✅ 판재 dual 등록**(0412+0413): 포맥스·아크릴·폼보드·스카시·광확산PC·알마이트 × 두께 × **색상** × **사이즈**. **보드 MATERIAL 39**(원자재·BOARD·장·dual, **사이즈 변종**: 포맥스 3×6+4×8·스카시 3×6·나머지 4×8) + **UV 평판 출력 PRODUCT 29**(AREA·사이즈무관, 자작나무 패턴) + 링크39. prod 검증 39/29/39. 단가 보류. ★보드만 사이즈분리(UV는 운영 보드선택).
  - **✅ 게양/부속 하드웨어 GOODS 22**(0414·0415, ACC-029~050): 게양16(깃대5·까치발·꽂이·로프3·수기대·아연날개·황금봉·국기봉·대대기·멕기, item_group `게양 부속품`) + 현수막6(아일렛·인치밴드·큐방·쌍클립·족자봉·삼각접착고리, `현수막 부속품`). 단독 유통판매도 발생.
  - **✅ 깃발 제품 6**(0416·0417): 태극기 수기 2(30×20·45×30 나염) + 자수 3(정기·태극기정기·근조기) + 본염 정기 1. 태극기 분류·기존 호수깃발 패턴(PRODUCT·FIXED·매입+판매 외주·ROLL·차감없음). **★SET(수기대조립·자수삼발이)=깃발제품+부속 라인 모델**(완제품 SKU 없음=이중계상 방지). 깃대조립SET=기존 호수깃발+깃대 부속으로 커버.
  - **✅ 자재/원단·상품 18**(0418·0419): 자재/원단 MATERIAL(열전사원단·타포린·그라스화이바·원형나무·각목 + 200g코팅지 3(판매용·NONE) + 3M인쇄비닐 + SPM011M(무광·재고1롤 소진용)) + 상품 GOODS(좌우보필형 액자·A3자석액자·국기함3등급·청사초롱·탁상용국기·지퍼가방). **스킵**: 저밀도원단(기등록 동일)·LD59THG(LD59HTG 오타). (~~솔벤캔버스 미취급~~ → 2026-07-01 등록됨 0420)
  - **★남은 (2026-07-01 갱신)**: ①**간판 구성요소 BOM·조립견적**(설계선행 보류) — ⚠️간판 *자재*는 2026-07-01 등록 완료(0421/0422, 157품목), 남은 건 견적구조(BOM)뿐 ②**단가**(전 품목 base_price=0, 매입 통해 차차). 참고(보류): 가로기 깃발(품목 자체 없음=오탐 확인)·미러천(취급X). 태극기/근조 본염·복수규격은 2026-07-01 규격별 분리 완료(0428).
  - **세션 누적(0398~0419)**: 배너 출력8형+형옵션범용+부속18 / 판재 보드39+UV29 / 게양·현수막부속22 / 깃발제품6 / 자재·상품18. = **즉시등록 영역 사실상 소진**. (worktree `session/master-banner`, 매 단계 prod 적용·검증·push)


- **▶ [2026-06-26] LogWatcher 후속 (정본 `memory/project-logwatcher-rollout.md`)**:
  - **EPSON 취소/실패 파서 — 보류**(커밋 `89097982` push): SqliteDbParser `PrintStatus="OK"` 하드코딩 → status-aware 개조(`status_column`+`status_cancel`/`status_error` 매핑, query `LEFT JOIN Log`+`JobStatus IN(종료)`). ⚠️EPSON Edge JobStatus enum 비공개 → **13/14 임시추정·실코드 EPSON PC `Data.db` 확인 대기**(`SELECT JobStatus,COUNT(*) FROM Job GROUP BY JobStatus`) → 확정 후 equipment.json + EPSON 2대 재배포.
  - **교훈**: 장비ID=현장 appsettings EquipmentId(변경=현장 재시작이 본질)·하트비트 자가치유는 agent/ip/log만(이름·설정·과거이력 잔존→수동 재귀속)·GetLocalIp 첫IPv4=dual-NIC 127.x 오보고.
  - **선명 CAPS 6/1~8 복구완료** 29/30(사용자 SM PC 런북 실행, 검증=prod 조회). 잔여=이희섭 6/8 출근펀치 누락 1건. 6월 선명 급여 생성 시 재계산 필요. ([[caps-backfill-recovery]])

- **▶ [2026-06-13] 휴가→근태 자동연동 — 커밋·미배포** *(동반이던 GitHub 이슈 18건 전수픽스는 완결 → ARCHIVE 이관 2026-07-05)*:
  - **휴가→근태 자동연동(큐06)** — ▶**커밋됨·미배포**: `leaves.ts` 승인→`markLeaveAttendance`(start~end 날짜별 attendance UPSERT)·반려/삭제→`clearLeaveAttendance` 롤백, `caps.ts` 동기화 가드(휴가 attendance_type/status 보존·출퇴근시각 갱신·지각0), 신규 `utils/leaveAttendance.ts`. **★결정**: 반차/반반차=attendance_type 코드 그대로(시간맵 기준출근 지각보정), **종일휴가(ANNUAL/SICK/경조)=`VACATION`**(payroll work_days `NOT IN(ABSENT,VACATION,HOLIDAY)` 정합 — raw 'ANNUAL'은 근무일 오집계). `source='LEAVE'`로 CAPS skip 회피. **★승인-취소 엔드포인트 없어 롤백은 PENDING만 도달(방어배선, 필요시 cancel-approved 추가)**. verify(393모듈)+로컬 SQL 7/7 PASS. 정본→[[design-leave-attendance-link]]. **▶다음: prod 배포 + 실 반차 승인 E2E**.

- **🟡 [워크벤치 P1] 시안 검수 페이지 — 코드 커밋·prod 배포됨(2026-06-11, `b0df71c4`), 권한 0307 미적용=비노출 유지**: spec `2026-06-11-web-canvas-ia-workbench.md` P1 구현. 전체 `npm run verify`(tsc+build) PASS 확인(이번 세션). **▶ 다음: ①`db:migrate:prod`(0307 권한) ②본인 계정 /workbench 실확인 후 권한 개방.** (이하 원 구현 상세 ↓) 신규: `src/routes/workbench.ts`(orders/analyses/:orderId/match, orderVisibilityFilter+그룹범위 검증) · `src/pages/workbench.ts` · `src/scripts/workbench.js`(전역 showToast·IIFE 하단·getElementById 가드) · `migrations/0307_workbench_permission.sql`(ADMIN/MANAGER/DESIGNER). index.tsx 라우트 2건+menu.ts 생산그룹. **검증**: 신규 파일 격리 tsc(strict) PASS·node --check PASS·check:dom 회귀 0. ⚠️ Cowork 마운트 불안정으로 전체 tsc/build 미실행 → 커밋 hook+CI 게이트 의존. **▶ 다음: ①로컬 `npm run verify` ②`db:migrate:local`+확인 ③커밋·push ④`db:migrate:prod`(0307) ⑤본인 계정 /workbench 실확인 후 권한 개방**

- **🟢 [구현중] 주문 청구 법인 분할(split billing) (2026-06-10) — P1·P2·P3·P4 완료 + ◐P5 부분 prod 배포**: 청구법인 = '로그인 법인 자동' → **'담당 생산법인 기준 분할'**. 한 주문에 동산·선명 혼재 시 각 생산법인이 자기 품목 직접 청구(세금계산서·매출·미수금 분리). 기존 "매출=청구법인 단일"(2026-06-04) 뒤집음. 신규 `order_billing_groups`(주문×법인), `clients.balance` 캐시 폐기·파생. 6 Phase(P1~P5 MVP, P6 내부정산 후속), 세션 분리. **✅ P1(스키마+백필) prod**: 마이그 `0305`, 435주문→435그룹 1:1, BILLED 3 동결. **✅ P2(주문 생성/수정+UI)**: 헬퍼 `recalcOrderBillingGroups`(POST/PUT, `COALESCE(assigned_entity_id,주법인)` group by, tax/discount 비례배분+주법인 잔차흡수, BILLED/PAID 동결), 청구법인 셀렉트 제거→도출칩, 상세 분할요약. **✅ P3(청구 billing + 미수금 파생) prod 배포·라이브 검증 (2026-06-11, 커밋 66b2fea6, deploy 7d6f77bc, push 17852929)**: 헬퍼 `setOrderBillingStatus`(그룹 billing_status + orders 미러 batch, `IS NOT 'BILLED' AND IS NOT 'PAID'`) 6개 청구경로 교체 + `deriveClientBalance`(거래처×법인 파생 = `order_billing_groups[BILLED] JOIN orders(≠CANCELLED) − payments − adjustments`, entityFilter g/p/a). `clients.balance` 캐시 **전면 폐기**(읽기/쓰기 ~20지점 파생전환: receivables·overdue·collection-period·독촉·ledger·settlement·dashboard TOP10·financialReports·payment/adjustment new_balance, UPDATE 전부 제거). orders.billing_status/billed_* **미러 유지**(P5 매출집계·롤백용). 검증: 로컬 혼합주문 2그룹·미수금 분리(동산22k/선명11k)·입금차감·smoke 103/103, prod 라이브 receivables·balance-snapshot AR −9,224,710(파생 정본)·14페이지·API. **배포 전 prod비교 60k 차이=취소주문155 stale캐시 1건(파생 0이 정답, 배포로 교정)**. **✅ P4(세금계산서 법인 자동분할) prod 배포·검증 (2026-06-11, 커밋 68472035, dep 8d7009f 자동빌드)**: 헬퍼 `createSplitInvoices`로 발행 단위=(주문×법인). 혼합주문→법인당 1장(공급자·채번·금액·품목 각 법인), 단일법인=기존 동일 1장. 단건/일괄/월합산/직접발행 전부 분할, `issueTaxInvoice`=해당 법인 그룹만 BILLED+`group.tax_invoice_id` 연결(orders 미러는 전 그룹 청구완료 시만), `cancel`=해당 계산서 그룹만 초기화. 연결=기존 `order_billing_groups.tax_invoice_id` FK(spec 신규 M:N 테이블 대신, 1계산서:N그룹이라 충분). 마이그 0306(supply/tax 백필 435건·계산서연결·인덱스). 검증: 로컬 E2E 혼합주문 2장(동산110k/선명55k), 발행 부분청구·취소 그룹스코프, prod 페이지·API 0err, smoke 103/103. **✅ monthly-eligible 섀도잉 수정 + ◐P5 부분 (2026-06-11, 커밋 20f06907, dep auto)**: `/:id{[0-9]+}` 숫자제약→월합산 대상조회 200 복구. P5부분=`financialReports`(/pnl·monthly·CSV 매출=청구그룹)+`cashSchedule` 입금예정 (주문×법인) 물질화. **✅ P5 cashflowEngine (커밋 dce9f50b, dep auto)**: 자금예측 §4를 청구그룹 단위로 통합 — **2버그 수정** ①§4b가 P3 폐기 stale `clients.balance`를 미수 cap으로 읽던 것(balance 미유지)→(거래처×법인) 파생잔여 ②§4a order단위라 혼합주문 부분청구 §4b와 이중계산→그룹분할. 차감법 검증(stale balance=0+혼합: 법인1 기여=110k 파생·법인0=165k 무중복). prod forecast 200. 단일법인 무변화. **P5 잔여(P5-continued, 단일법인 현재 무영향·혼합 대비)=`dashboard.ts`(month_billed52·aging345·total_receivables366·billed_order_count336·clients_with_balance368)·`accounts-receivable.ts`(closing/profit/collection 월매출 2009/2044/2090/2150/1497)·`clients.ts`(has_balance102·총미수297/425·balance433)·`aiInsights.ts`(14/23)·`portal.ts`·`bank.ts`(1736)·레거시 컬럼제거**. smoke 103/103. **▶ 다음 = P5-continued(잔여 ~6파일 그룹화, 변환패턴=session-context) → 전량검증 후 orders.billing_status/billed_* 레거시 컬럼 제거 + clients.balance/recalculate 정리 → P6 내부정산.** 핸드오프 = `docs/superpowers/specs/2026-06-10-split-billing-IMPLEMENTATION-PLAN.md`, 설계정본 = `2026-06-10-split-billing-by-entity.md`, 메모리 = `design-split-billing`


---

## 🟡 대기 중 (사용자 선택/승인 필요)

> **📐 설계 확정 spec (2026-06-11, 결정 완료 → 구현 대기)**: ①`alimtalk-golive-package`(D1 결과모달+재발송/D2 bulk 단일화/D3 한진 skip — **다음 세션 권장**, #378+자동발송+go-live) ②`ontime-kpi-redesign`(#380 — COALESCE 출고일·마지막출고·SHIPPED+COMPLETED·납기월) ③`large-file-split-plan`(orders/core부터, #377 동세션) ④`card-cashflow-forecast`(일시불·AVG_3M 혼합·기존 OUT 흐름) ⑤`hanjin-courier-decision`(통합 솔루션 방향, import 동시 진행) ⑥`client-self-order-portal`(제작품 포함 최종 — **IA 안정화 선행**, brainstorming 필요) ⑦`web-canvas-ia-workbench`(**설계 확정 2026-06-11, Cowork 상세화 완료**: D-A 업로드 즉시 변환 큐잉 / **D-B bleed=미세확대 → createEdgeStrip 폐기, 오프셋 디버깅 영구 제거** / D-C 시트 잠금 / D-D 하이브리드 자동배치. placement JSON=기존 ia_params 포맷 재사용, Tier1은 썸네일 기반이라 pdf.js 불요. 마이그 1건+workbench.ts+2페이지. 잔여=spec §8 로컬 PoC 5항목(★좌표 왕복) 후 P1). 전부 `docs/superpowers/specs/2026-06-11-*.md`
> **⚖️ 운영 결정 (2026-06-11 용준님 확정)**: ⓐ**#377 활성화=플래그 게이트** — `ia_auto_enabled` settings 키 + `/pending` 응답 게이트, 기본 OFF→stale pending 정리→수동 테스트 1건→ON ⓑ~~정적 에셋 P1~P3~~ **무효(2026-06-11 P0 폐기·롤백 — prod 2회 다운, 재시도 금지)** → cards.js·shell.js 분할은 `?raw` 유지 전제(layout.ts 다중 import 방식)로 진행 ⓒ**SMS 대체문자 안 함**(`smsReplyOf=N` 유지, 발신번호 승인 액션 불요) ⓓ**미수금 독촉=추천+수동승인 목록**(후속 0.5세션, alimtalk spec Phase 4) ⓔ**test_mode=0 = go-live 패키지 검증 직후 같은 세션** ⓕ**admin 비번·E2E prod 오염 = 테스트 단계 동안 현행 유지, 실사용 전환 시 admin 계정 삭제+E2E 데이터 정리** (전환 시점 트리거 — 잊지 말 것) ⓖ~~split-billing = 수요 시 착수~~ **무효(결정 당시 이미 구현 진행 중이었음 — P1~P5 부분 prod 배포 완료, 위 항목 참조)** ⓗ**하네스 P2+P3 채택** → ⑱ 등록 ⓘ**정적 에셋 = 옵션 A PoC 승인**(`static-assets-rootcause-redesign` spec §6 — env.ASSETS+명시 MIME, 비임계 1파일 무중단 실측, 우선순위 낮음. ⓑ 금지의 해제 조건 충족 시에만 확대) ⓙ**정기변동비 Phase 4·5 = 카드예측 spec과 동세션**, 선행=split-billing P5-continued 완료(cashflowEngine 충돌 방지) ⓚ**미수금 고도화 3건(median lag·충당률 UI·조기경보) = 실사용 검증 후 선별** ⓛ**split-billing P6 내부정산 = 다법인 실거래 발생 후**(정산 단가·상계 주기는 그때 결정)

### 🆕 신규 설계 — 구현 미착수 (2026-06-13~16, 문서정리 세션서 STATUS 등록)
- **[재단 패널 `com.mes.cut.panel`] — 설계 확정·구현 미착수 (2026-07-31 brainstorming)**: ★**목적 재정의** — "이형 네스팅"이 아니라 **칼선(재단 파일) 직접 제작**이 1차(지금은 원형·타원만 수동, **어려운 라인은 외부에서 들어옴**). 네스팅은 P3로 하향. **A0 패널에 통합하지 않고 별도 CEP 패널 분리개발 → 안정화 후 탭 병합**(D1). 핵심 실측: **오프셋 엔진(거리변환) 1개가 기본도형·타공까지 전부 커버** — 6/6 도형 팽창량 오차 0·**사각+오프셋=라운드사각 자동**(코너R 12.7↔이론 12.4)·**섬 병합=정확히 gap/2**(글자 흩어짐 방지 규칙)·타공=내부 오프셋 경로 등간격. ⇒ **도형 그리기 UI·타공 알고리즘 별도 제작 불요**(D3). 해상도가 비용 결정(CNC 판 0.5mm/px=524ms·154MB **상한**, 0.25mm/px=2.5초·617MB). ⚠️**DXF의 6mm 원은 타공이 아니라 돔보**(A0 기능·`mes-a0-host.jsx:553/566/578`이 DXF 실측 404.7/412.8/168.7과 소수점 일치) → **재구현 금지**(D4). 재활용=**DXF 내보내기 기존 구현**(`ProcessOrderItem.jsx:876~888`)·출력 규약(레이어 `'재단선'`·M100 0.6pt). 분리 위험 3건(**크로스 패널 잠금 부재**·params 경쟁·`panel-smoke.mjs:8`/`ia-jsx-audit.cjs:70~74` 경로 하드코딩=감사 사각) → **P0에서 선행 해소**. **✅ P0 완료(미배포)**: `scripts/cut/{geometry,bench,probe-cut-file}.mjs`(엔진·하네스·파일해부) + `ia-jsx-audit.cjs` **패널 레지스트리 일반화**(`PANELS`·DESIGNER_JSX 실재필터 → 새 호스트 자동편입) + `panel-smoke.mjs` 절대경로 제거(`--panel=a0|cut`) + `cut-panel-smoke.mjs` **26항목** + 패널 골격(`designer/cut-panel/com.mes.cut.panel`·**.debug 8889**=A0 8888과 분리) + `mes-cut-host.jsx`(잠금 API) + `install-cut-panel.ps1`. 검증 = **cut:bench 통과 · cut:smoke 26/26 · panel:smoke 100/100(A0 회귀 0) · verify 통과**, audit이 신규 축3·축4 인식. npm = `cut:smoke`/`cut:bench`/`cut:probe`. **✅ 배포완료**(축2·3·4, `audit:ia-jsx` **4축 드리프트 0** — 전부 신규파일이라 A0 자산 무변경). **✅ 크로스 패널 잠금 실측 완결**: `$.global` 가설이 **실측에서 뒤집힘** — **CEP는 확장마다 별도 ExtendScript 엔진**(A0에서 심은 전역이 재단에서 `undefined`, 함수도 상호 불가시, `$.engineName` 상이) → **잠금을 파일 매개로 교체**(`%TEMP%\mes_host_lock.txt`·`owner|label|epochMs`·TTL10분·PC로컬 범위). 교체 후 양방향 재측정 **전항목 통과**(`seen:a0:batch` 가시·`busy:a0` 차단·`notowner:a0` 보호·실패널 UI 확인). 부수 실측=**접두사 규약 유효**(A0 엔진에 재단 호스트 로드해도 `mesA0_*` 전량 생존). 함정 2건=`install-*.ps1` **UTF-8 BOM 필수**(PS5.1 CP949 파서에러)·CDP 포트 A0 8888/재단 8889 분리. **✅ A0 연동 완료**: 잠금 정본을 공용 모듈 `mes-lock.jsx`(`mesLock_*`)로 분리→두 호스트가 `$.evalFile`(복제 시 형식 어긋나면 양쪽 다 "잠금 없음"이 된다). A0는 **추가만**(기존 무변경)+`setHostBusy()`가 잡고놓기, shell 0.1.10→**0.1.11**. 실엔진 검증=A0획득→재단 `seen:a0:single`·`busy:a0:single` 차단·`notowner:` 보호. **panel:smoke 100→103**(잠금 획득·해제·라벨ASCII 어서션). ⚠️지금은 **publish만** — A0가 재단잠금 보고 경고하는 역방향은 P1에서 UI와 함께(`out()`은 작업결과가 즉시 덮어써 안 보인다). ⚠️잠금 라벨 **ASCII 매핑 필수**(evalScript 브릿지가 한글 파괴, 그냥 지우면 '단건 가공'→공백). **★★설치 함정 발견·수정**: `install-*.ps1`의 `Move-Item` 백업 → **열린 패널이 이동된 백업폴더를 계속 읽는다**(A0 href가 `...bak-20260730-205701/index.html`이었음). "설치완료"+**감사 드리프트 0인데 화면은 구버전** → 백업을 `Copy-Item`·**제자리 덮어쓰기**로 변경+백업 최근3개만 유지(17개 누적됐었음). CEP baseUrl은 로드시점 고정이라 리로드 불가=**패널을 닫았다 열어야** 반영. **✅ P1 완료·실동작 검증됨**: 오프셋 칼선(bbox/실루엣)+타공+DXF. 기하 정본=**패널 `js/geometry.js`**(UMD)—`cut:bench`가 이걸 직접 검증(검증코드=배포코드). ⚠️repo `type:module`이라 Node는 `await import()`+`globalThis.MesCutGeom`. **실파일 2건 실측**: 오프셋 오차 **0.1~0.23mm**(좌3.00/우2.90/상3.00/하2.90 @기대3mm)·bbox 5mm도 동일·타공 8개 지름6mm·`재단선` M100 0.6pt closed·DXF(AC1021·mm·**칼선만** 1.19MB→0.17MB). 사용자 작업문서 무손상·잠금 잔재 0. `cut:smoke` 27→**37**. **★실동작에서만 드러난 ExtendScript 함정 4건 수정**(코드리뷰·헤드리스로는 못 잡음): ①**문서간 `duplicate`는 원본이 active일 때만 동작+예외 미발생**(Document 0/Layer 0/active복귀 99) ②**래스터 여백 없으면 오프셋이 경계에서 잘림**(잉크 닿은 변만 0mm=조용한 오류) ③**DXF export는 숨긴 레이어도 내보냄**→임시문서 방식 ④**`selection`은 live**—add() 전 배열 고정. 진단도구 2건도 수정(`.trim()`이 UTF-8 끝 `0xA0`를 NBSP로 잘라 한글 깨짐·돔보 판정은 격자위치로). **✅ 구멍(내부 홀) 추출 추가**(2026-07-31 후속, host 0.2.1·shell 0.2.1): 사용자 지적("도넛형 추출 안 됨") 확인 결과 **구멍 전혀 미추출**이었음(ㅇ자 면적오차 **53.2%**=글자 컷팅 치명적). 구멍=배경 연결요소 중 테두리 미접촉 → **오차 0.12~0.23%**. **오프셋 뒤 마스크에서** 뽑아 구멍도 축소·오프셋>반지름이면 소멸(칼날 물리 일치). 일러=**compound path**. ⚠️**최소구멍 필터 필수**(`MIN_HOLE_MM` 잠정2mm — 좁은 틈이 오프셋으로 막혀 미세구멍 144개 발생). 실파일 검증=한글6줄 칼선6·구멍120(중앙값 17.2mm). **★재실행 오염 버그 발견·수정**: 전체선택이 `재단선` 레이어도 잡아 **이전 칼선이 입력에 섞임**(조각255→외곽1) → `mesCut_isCutItem()` 3곳 제외. `cut:bench`+구멍4케이스·`cut:smoke` 37→**40**. **P3 네스팅 확장 타당성 실측 완료**: 래스터 true-shape가 bbox 대비 **삼각20개 40%·혼합18개 13% 절감**, gap은 `offsetMask(gap/2)` 공짜·충돌은 픽셀AND라 **NFP 불요**·구멍 안 배치 자동. ⚠️회전 휴리스틱 미흡·큰시트 최적화 필요. **✅ P2 완료 — Image Trace 불요**(shell 0.3.0): 이미 파이프라인이 래스터화를 거쳐 트레이스가 불필요했고 진짜 문제는 **불투명 래스터**였다(실측: alpha 마스크 **87.5%=사각형** vs 흰배경제거 **14.7%=별 모양**). → **배경 판정 UI**(자동/투명/흰배경제거, 실루엣 전용) + 자동전환(흰배경 제거로 잉크 30%↓면 채택). ⚠️흰배경 제거는 **흰색 그림도 지움** → 결과에 사용 모드 **항상 표시**. 실동작=일러에서 불투명 래스터 별 → 자동전환, 래스터 사각 141.1mm → **칼선 106.2×101.5mm·앵커 28개**(사각이면 4~8) = 별 실루엣 정확. `cut:smoke` 40→**44**. 복잡한 배경 사진은 디자이너 누끼 후 넘기는 게 품질·통제상 나아 범위 제외. **✅ P3 네스팅 실용버전 완료**(shell 0.4.0): `js/nesting.js` 정본 + `cut:nest` 하네스(npm 등록). 롤5종/평판2종 프리셋·gap·회전(0/90/180/270)·다중시트·효율%. 흐름=잠금→`nestBegin`→`rasterizeItem` 반복→gap 팽창→`nest()`→params(S/I)→`nestApply`(**새 문서**, 원본 무손상). **★"최적화"가 역효과였다**: 스카이라인 후보축소가 순수 bottom-left보다 **느리고 나쁨**(528px·198ms vs **360px·13ms** — 오목한 틈을 놓침) → 순수 BL 복귀 + 정렬 `입력순서`/`큰것먼저` **둘 다 돌려 나은 쪽**(큰것먼저=정석인데 맞물림 순서를 깨 544px로 악화). 하네스 절감=**삼각 20개 40%**·회전 25%·혼합 10.4%, 판정=전수배치·**겹침0(결과 재스탬프 독립검증)**·경계·절감·다중시트. 실동작=삼각12개→롤 1370×87mm **12/12·이탈0·여백1.5mm(=gap/2)**·효율47.5%. `cut:smoke` 44→**49**. ⚠️**ExtendScript quirk 발견: 비활성 문서 `artboards` 읽으면 활성 문서 값이 나옴**(사용자 파일까지 같은 크기로 보임)→activeDocument 전환 후 읽을 것. **통합(/ia-editor) 병목=엔진 아니라 마스크 조달**: nesting/geometry는 순수JS라 이식 자유·출력도 bbox+rotation이라 계약 불변, 그러나 웹 썸네일엔 알파가 없어 **패널이 만든 폴리곤을 API로 올리는 경로** 필요. 기존 `iaeMaxRectsPack`은 그대로+옵션 분기. **✅ 표준 벤치마크 대조 + 튜닝 완료**(shell 0.4.1): `npm run cut:benchmark` — ESICUP/sparrow 인스턴스 5종(fu·jakobs1/2·shapes0/1)을 `scripts/cut/data/*.json`에 **커밋**(원격의존 금지). best-known 출처=arXiv 2509.13329 Table3. **평균 격차 17.0%p·미배치 0·1.2초 이내**, **bbox 대비 20~26% 절감**(표준 인스턴스). ⚠️문헌은 연속기하(NFP)+수분~수십분 탐색이라 직접비교 부당 → **bbox 절감**을 핵심 지표로. **튜닝 실측**: 투입순서 다양화 21.7→**18.0%p**(채택) · **tries 8→32 무효**(17.9, 순서 포화) · **step이 실질 병목**(3→1: 18.0→16.4) · **2단계 정밀화**(성근격자로 순서선택→이긴 순서만 촘촘히) **17.0%p·3배 빠름**(채택) · **scale 6→16은 55~56% 정체**=해상도 아닌 배치규칙 한계(더 좁히려면 NFP·SA=범위초과). 자체 하네스도 개선(혼합 10.4→**14.8%** 절감). geometry에 `rasterizePolygon` 추가(폴리곤→마스크, 웹 통합에도 필요). 사용법 문서=`docs/CUT_PANEL_USAGE.md`. **✅ 주문서 직결 완료(N1~N4)**(host 0.4.0·shell 0.6.1): **ia-editor 경유 없이** 패널→주문서 대기함. 새 API 0 — A0의 **파일 경유 ingest**(Z: IA-등록 manifest)를 그대로 탐. **서버·에이전트·스키마 변경 0**(`mode:'impose'` 이미 지원). 왕복 실측=파일4종 생성→`.ingested`→**`designer_intakes` id93 · mode=impose · status=waiting · 137×7.5cm**(검증 후 정리). ⚠️함정=`_config/config.json`은 API응답 중계라 **`{success,data:{}}` 언랩 필수**(빠뜨리면 거래처·가공자 목록이 조용히 0개). **✅ 돔보 필수화**: 재단 산출물엔 돔보+재단선 무조건(재단기 기준점). A0와 같은 규약(6mm·17mm·50cm·60mm)이되 **A0의 아트보드 23mm 확장은 금지**(롤/평판 폭 고정) → **배치영역을 안쪽으로 축소**(폭·좌표·롤길이 **세 곳 모두** 맞춰야 함 — 길이 누락으로 조각이 22.1mm 삐져나간 실측). 검증=1370×119mm·돔보 9개·이탈0·여백 좌24.5/상24.5/하23.9. **★등록 mode 정정(중요)**: `mode:'impose'` 로 보내면 **주문서에 안 뜬다** — `orderForm/intake.js:81`이 `single,both`만 조회하고 "모아찍기용은 ia-editor 담당"이라 일부러 거른다(`iaEditor.js:311`은 `impose,both`). `impose`=대지로 가져와 **재배치할 조각**, 재단 패널 시트는 **완성 1건**이라 `single`이 맞다 → host 0.4.1에서 정정, 주문서 필터에 잡히는 것 실측 확인. ⚠️흡수(absorb)는 **주문서에만** 있다(`orderForm/intake.js:859`). **✅UI 2그룹+조각별 칼선**(shell 0.7.2·host 0.4.2): 「단품 칼선」/「모아찍기(네스팅→등록)」로 분리, [네스팅]과 [등록]은 **합치지 않음**(재배치 반복 시 대기함 오염). 조각별 칼선 기본 ON(재단은 조각 단위라 시트 둘레만으론 못 뗌). **★버그 2건 수정**: ①**레이어 인덱스** — 조각을 `doc.layers[0]`에 복제하는데 재단선 레이어 생성 시 `layers.add()`가 **맨 위(index0)**로 들어가 **조각 5개가 재단선 레이어로 감**(칼선 6→11로 보인 원인) → `artLayer` 고정 ②얇은 꼭짓점 파편 → 면적비례 하한. 검증=`재단선(16)`=돔보9+시트1+조각6/6·`레이어1(6)`·이탈0. **▶ 다음 = ①플로터/CNC 실기 투입(사용자 예정) ②패널 통합(A0 탭 흡수)은 실기 검증 후 ③다중 문서 네스팅**. 미해결=오프셋 기본거리(역산 실패)·반컷/완컷 구분·miter 코너·DXF 저장위치(현 %TEMP%)·돔보 간격규칙 파일별 상이(500 vs 400). spec `docs/superpowers/specs/2026-07-31-cut-file-panel.md`, 메모리 [[design-irregular-nesting]]
- **[품목·단가·재고 통합 개편] — north-star 수렴 · 게이트 통과(B-1 검증→A 채택, 2026-06-19)**: 표준품목 3역할(PRODUCT/GOODS/MATERIAL) + 단가 자동계산(기본 ㎡단가표×거래처 override) + 소재=자재 재고연결 + 프리셋 입력. 목적="ECOUNT가 못 하던 것"(전품목 통계·재고/원가 정확·단가 자동, 입력속도 아님). **게이트 결과(prod 검증)**: B-1 겹업물건=프로덕션 63/103 다중역할(MATERIAL 53건이 판매겸업) → **dual 플래그 유지=역할 정본, item_type enum collapse 금지**(A 채택), **전면 재설계(다) 불요**. realign Phase 2 '단일출처화' 단계 삭제. B-2(깃발 호수·간판 BOM)·B-6(단가 다형성)은 단가 개편서 plug-in 흡수. → [[design-item-role-multi-flag]]. 옵션(축) 시스템=EAV·규격 듀얼모드(출력물 NUMERIC/자재·상품 LIST)·동적 화면·카테고리 상속·권한 = v3 구조 확정. spec 4: `2026-06-13-item-pricing-inventory-FINAL`·`item-axis-realign-plan`·`item-master-review`·`option-axis-system-design`. **▶ 진행(2026-06-19): 마스터 로드 먼저** — 결정 확정(인쇄방식 분리·1차 단가엔진 AREA+FIXED+GRADE(깃발 호수)·기존103↔신규298 병행+매핑·겸업 2행). 로드원본=`표준품목_등록구조_수정본.xlsx`(298행). **P1a 스키마(`0322`카테고리9·`0323`pricing_profile(B-6)·`0324`size_grade_prices) + P1b 마스터로드(`0325`=231품목 **staged is_active=0**+14겸업링크) = prod 적용·검증 완료**(execute --file --remote, 추적드리프트 회피). prod 검증: 카테고리13·pricing_profile백필103·231 staged(active0·기존103 미변경)·GRADE34·링크77(기존63+14)·category NULL0·**prod 스모크 103/103**. 스테이징=picker(is_active=1 필터) 무오염. 정본 spec=`docs/superpowers/specs/2026-06-19-item-master-load-phase1.md`. **✅ 바인드 한도 버그 = 수정·배포 완료**(`5fd0dfcd` B안=IN→`IN(SELECT id FROM items WHERE is_purchase_item=1 AND is_active=1)` 서브쿼리/JOIN, 바인드 0개 → active 무관. push→CF 자동빌드 `420280` 라이브·**prod 스모크 103/103**). P1c 활성화 전 차단조건 해소. **다음=P1c(기존103 dedup·ecount매핑·인쇄방식카테고리 비활성·바인드픽스 → 활성화)·P1d 단가배선(GRADE룩업·㎡단가표)·기성PRODUCT 토글.** 미확인=원단53 재고이중장부 PoC. 이름확인16·간판52·선명19=후속.
- **[간판 구성요소 견적(조립 견적/BOM)] — 방향 확정, 세부 보류**: 간판=면적 아닌 "구성요소 라인 합"(채널·LED·SMPS·알루미늄바·까치발…). 기존 items+order_items.parent_item_id 위 "조립 견적 품목" 확장, calc_type 4종(FIXED/PER_QTY/BY_SIZE/BY_SPEC_QTY). **▶ 품목 개편과 병행, 구성요소 전수 확정 선행.** spec `2026-06-13-signage-component-estimate-structure`
- **[원본 파일 아카이브] — 결정 확정(D1~D9), 미착수**: 현행 IA가 고객 원본을 가공 후 폐기(Program.cs:1760) → 작업 EPS와 동일 규칙으로 `Z:\원본\...` 별도 트리 영구보존 + `/workbench` 보드 조회. **▶ ia-editor §14 P1.** spec `2026-06-16-ia-editor-nesting-intake`(`incoming-file-board` 흡수)
- **[수신 파일 간편 편집기] — 방향 확정, 보류**: 들어오는 AI/EPS를 일러 없이 웹서 수정("그림 수정❌, 처리 설정⭕"로 축소 재정의). 워크벤치 P2 후 착수. spec `2026-06-11-incoming-file-editor`

### 🆕 [LogWatcher 장비중심 모델] — P1~P3 구현·미배포, P4 대기 (2026-06-15)
- 마이그 0312+heartbeat 자동등록+/equipment 보강(P1)·LogWatcher heartbeat 확장(P2)·/equipment 표시+/rip 페이지 폐기(P3) 빌드 완료. **▶ P4: permission_pages '/rip' 정리+스모크+prod 배포+LogWatcher 재배포.** spec `2026-06-15-logwatcher-equipment-centric`, 메모리 `project-logwatcher-rollout`

### [기성품/유통 즉시출고] — ✅ 전체 완료 (Phase 1+2+3 + UI 클릭검증 + 태극기 지정)
- 코드/마이그(0285·0286) prod 반영. 기성/유통 = 카드 미생성·즉시 출고가능·SHIPPED 전이·출고 시 재고차감(음수 허용·멱등)·주문서 재고부족 경고.
- **태극기 9종(수기·1~6호·특호·탁상용) 기성품 지정 완료**. 향후 추가 기성 PRODUCT는 품목 UI '기성품' 체크로 지정.

### [포털 rate-limit] — ✅ 프로덕션 배포됨 (2026-06-02)
- portal verify-document/verify-token에 `rateLimitMiddleware`(10·30/분) 적용(commit 4a2fc28). prod 활성화.
### [#310 직접발행 폼] — 실사용 검증 대기 (2026-06-01)
- 백엔드(POST /tax-invoices/direct)+UI 배포됨. 세금계산서 '직접발행' 첫 발행 테스트 권장 (tax_invoices 0건)

### [바로빌 전환] — 통합 완료, 잔여 작업 대기
- 전환 완료: `messaging_provider=barobill`, 실데이터 조회 성공. 통장→수금 반자동 플로우 구현됨. 자금관리 탭 정리 완료(바로빌 통장 탭→은행 연동 통합).
- **✅ 알림톡 발송 동작 확정(2026-06-10)**: 템플릿 4종(대신화물 출고·대신택배 출고·방문 수령 준비 완료·미수금) 승인 완료, 실발송 성공(SendKey 수신). 3버그(SenderID/SmsReply/성공판정) 수정·배포(커밋 9bf1cb2e). **대기**: SMS 발신번호 승인(대체문자 E/A용), 한진택배 템플릿 등록, **barobill_test_mode=0 전환(go-live)**, 나머지 카드/계좌 등록
- **알림톡 코드 정합 완료**(2026-06-03): 출고 4종·주문접수·미수금 템플릿 코드 연동. **버튼 미전송**(sendATS) → 링크는 본문. **한진 송장 수동입력**(자동화 조사 완료)

### [선명2 CAPS Worker 설치] — PC 설정 대기
- S2 사이트 DB 등록 완료, API_KEY 발급됨. 선명2 PC에 caps-worker 폴더 복사 + .env 설정 + 실행 필요

### [배송 관리 최적화] — 출고 대기 보드
- 배송방법별 그룹화 + 마감시간 카운트다운 + 일괄 출고 + 카카오톡 발송
- **✅ 수동 알림톡 발송 정합(2026-06-10, 커밋 0a329a02)**: autoCodeMap 실제 템플릿명 교정 + 모달 본문순서. **자동발송=다음 세션**(option C, order.delivery_method 매핑; 트리거 shipments.ts:504 이미 배선, template_code+resolveMsg만 채우면 됨). '배송' 381건은 E2E noise였음(실데이터는 택배사 정상 지정)
### [기존 계약 일괄 등록] — 엑셀 import 스크립트 제공 대기
### [라벨 프린터 인쇄] — 프린터 모델 확인 필요 (외부 의존)
### [RIP 전송] — 코드 완료, 현장 테스트 대기 (외부 의존)
### [LogWatcher PrintExp] — 구현 완료, 현장 배포 대기 (외부 의존)
### [한진택배 자동화] — 솔루션 선정 대기 (사용자 결정 필요)

### [GitHub 이슈 백로그] — ✅ **OPEN 0건** (2026-07-29 실측)
- 2026-07-29 세션에서 잔여 11건 전량 소진(10건 픽스 배포 + #580 close). 그 전에 open으로 기록돼 있던 #336·#340·#341·#342·#350·#366·#369·#370·#392·#393은 **이미 CLOSED/COMPLETED**였다(기록만 낡아 있었음).
- ⚠️ **교훈**: 이 파일의 이슈 목록은 쉽게 낡는다. 판단 전 `gh issue list --state open`으로 **실측**할 것 — [[feedback-autoscan-false-positives]]·"open≠unfixed"의 역방향(closed인데 open으로 적혀 있음).
- 후속(이슈 아님, 실사용 검증): 발송 계열 소량 1건 실발송 자연검증 · #584 기본 상한 500 운영 적정성 판단.

---

## 📌 기존 에러
- (없음) — 2026-05-19 확인: 3건 모두 200 정상

---

> 📦 **2026-06-05 이전 완료 항목 + 2026-06-09~10 완료 세션 + 2026-06-24 다이어트 이관분은 `PROJECT_STATUS_ARCHIVE.md`로 이관됨**
