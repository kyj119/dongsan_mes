# Improvement Backlog
<!-- last_run_area: 1 -->
<!-- last_run_at: 2026-07-06T06:00:00+09:00 -->

> 자율 점검·개선 에이전트(auto-improve)가 6개 영역을 순환하며 발견한 항목.
> 용준님이 주기적으로 리뷰하여 상태를 변경 (new → approved → done, 또는 rejected).

## 통계
| 상태 | 건수 |
|------|------|
| 🆕 new | **12 (#473~#484, Area6 33회차 net-new 1 — 컬럼-diff bridge가 신규 feature `bda6a50`(품목 사진) 마이그 `0442` (b)-risk 격리)**. **🔺 #484 (신규, HIGH·잠재 LIVE)**: `0442 ADD COLUMN image_key`를 품목 상세 GET(`items.ts:719 GET /api/items/:id`)이 **명시 SELECT** 참조 — `bda6a50` deploy.yml **코드 자동배포 완료(@14:47 success)**했으나 deploy.yml에 마이그 스텝 없음(수동 `db:migrate:prod`, 커밋 "prod 배포 대기") + post-deploy smoke는 `items.list`(GROUP BY 집계, image_key 미참조)만 프로브 → **0442 미적용 시 품목 편집/상세 모달(`modals.js:54`) 100% 500(no such column) + smoke green 은폐**(#483 (b)-risk + #430 맹점의 SELECT-detail 버전). owner 판별=`PRAGMA table_info(items)`로 image_key 확인, 없으면 `db:migrate:prod` 즉시(코드 재배포 불요). issue-only(egress 검증불가·마이그 순서=owner). 이전 11건(#473~#483)은 아래 유지 — ⏳ **close-pending 적체 4건**(fixed-in-tree, owner batch-close 대상): **#479** pack showPrompt(`b274bb1`)·**#480** update.ts shipment_checks 선정리(`99c1b94`)·**#481** PR댓글 entityFilter(`4fa8713`)·**#483** orders 인덱스 0441(`bb49684`, prod 배포 대기·`db:migrate:prod` 필요). **#475 범위 축소**: `?raw` concat shadow 클러스터 전수 재스캔에서 `refreshStatus`(cash stub이 tax NTS 상태폴링을 shadow → 세금계산서 상태새로고침 100% 400)를 **자동수정**(`67732b6`, tax 라우트 실재+cash 의도적 stub이라 엔드포인트 결정 불요=순수 ti격리, #475 코멘트) → #475 남은 범위=`openPrintURL` 단독(cash print-url 라우트 결정 owner 대기). **실제 미픽스 7건**(issue-only, owner 확인 대기): #473·#474·#475·#476·#477·#478·#482 — 32회차 재grep 전량 안티패턴 잔존 확정(정상 open). 32회차 SKILL codify: **close-pending 캐시 — fixed-in-tree 픽스 형제완전성은 파일 churn 0이면 재검증 skip(#480/#481 파일 `3e29fe6` 무관 = clean verdict 캐시)**. 아래 상세: **#483** 인덱스 누락(Area4 자동수정): `orders.consolidate_with_order_id`(0438 접수시점 합배송 예약 포인터)가 형제 0437 `shipments.merged_into_id`(idx_shipments_merged_into 보유)와 달리 **인덱스 없이 도입** — `grep consolidate_with_order_id migrations` = ALTER 1줄뿐. hot-path 3곳 미인덱스 orders 풀스캔: ① `/daily` 상관 서브쿼리(`shipments.ts:155-160`)가 일별 주문 행마다 `consolidate_partner_pending_date` 배지 산출(`OR o2.consolidate_with_order_id = COALESCE(...)`) = `일별주문수 × orders전체` 스캔 ② `shipmentHelper.ts:151` applyConsolidationIntents 출고확정마다 파트너 조회 ③ `shipments.ts:422` unmerge. **자동수정 완료** — `migrations/0441_orders_consolidate_with_index.sql` = `CREATE INDEX IF NOT EXISTS idx_orders_consolidate_with ON orders(consolidate_with_order_id)`(0437 byte-미러, additive·멱등). migration-check 통과(orders 실재+컬럼 실재 0438+NOT NULL/DEFAULT 무관). `npm run verify` tsc0/build0. owner 액션=다음 `db:migrate:prod` 적용. **부차 FP(보고 안 함): 하드삭제 dangling** — consolidate_with_order_id 비-FK라 root 하드삭제(core.ts) 시 자식 포인터 정리 안 됨(#477 클래스)이나 **모든 read-path가 status 가드**(shipmentHelper `:154`·/daily `:159` `NOT IN CANCELLED/DELETED`, write는 root 해소 시 유효검증) + merged_into_id와 달리 write-redirect 없음 → dangling 무해. improvement small [Area 4, **자동수정**(인덱스=스키마변경 금지 예외)]. **SKILL codify: 인덱스 누락 형제-완전성 — 신규 `*_id` 포인터 컬럼 도입 시 형제 컬럼(같은 feature의 다른 테이블)이 인덱스를 받으면 대조(0437 인덱스↔0438 미인덱스)**. | **#482** 창고구역 생성/수정(`storageZones.ts:309 POST`·`:367 PUT`)이 중복검사를 **법인별로만**(`:332`/`:393` `WHERE zone_name=? AND entity_id=?`)하는데 `storage_zones.zone_name`(`0098:7` 전역 `TEXT NOT NULL UNIQUE`)·`zone_code`(`0098:8` 전역 `TEXT UNIQUE`)는 **전역 UNIQUE** → 법인 B가 법인 A가 쓴 표준 구역명(자재창고 등) 생성 시 앱검사 통과→INSERT→전역 UNIQUE 위반 throw→**generic 500**(catch에 UNIQUE-catch 없음, 형제 `leaves.ts:534`는 보유). `entity_id`는 0232서 나중 추가(주석 "창고는 법인 소속" = 법인별 독립 의도)인데 제약은 전역 잔존 = 스키마↔로직 스코프 불일치. zone_code는 앱검사 아예 없음. `697902d`(창고 배치도 독립·facility 매핑 폐기) 취지 무력화. 도달성 LIVE(/storage-zones ADMIN 모달). bug small [Area 2, issue-only(DB 제약 변경=테이블 재빌드 스키마변경 자동수정금지+egress). 수정=신규 마이그 복합 UNIQUE(entity_id, zone_name)/(entity_id, zone_code) 재빌드[migration-check] + 병행 catch에 UNIQUE→409. **SKILL codify: 앱 유일성검사 스코프(entity_id 포함) vs DB 제약 스코프(전역) 불일치 standing scan(Area2 #482)**]. 직전 Area6(31회차) done-sync 유지: #473~#481 중 3건이 fixed-in-tree/close-pending-user(#479 `b274bb1`·#480 `99c1b94`·#481 `4fa8713`) → owner close 대상. 실제 미픽스 = #473·#474·#475·#476·#477·#478. 상세: **#481** 발주요청 댓글 `POST /:id/comments`(`purchaseRequests.ts:220`)가 entity 소유 검증 없이 bare `WHERE id=?` → **`7e4b6bf`(#455 IDOR 픽스)의 부분픽스 형제**. 같은 파일 mutate 형제 6종(PUT `:438 efPut`·approve `:530`·reject `:590`·convert `:623`·auto-convert `:736`·DELETE `:922 efDel`)은 전부 `entityFilter` read-back 404 게이트(`// #455` 주석)인데 **댓글 POST만 누락**. entity-scoped ADMIN/MANAGER/DESIGNER(라우터 `:11 requireRole`)가 `POST /api/purchase-requests/{타법인 PR id}/comments {content}`로 **타법인 발주요청 스레드에 임의 텍스트 주입**(cross-tenant 사용자명·이름 노출 + 내용 오염). **XSS 아님(FP 배제)**: 댓글 렌더 `purchaseRequests.js:238 escapeHtml(cm.content)` = stored XSS 없음 → severity **LOW**(내용 주입). 도달성 LIVE(`purchaseRequests.js:245/246 submitPRComment`). bug small security [Area 5, issue-only(IDOR=owner 워크플로+egress, #455 선례). 수정=read-back에 `${ef.clause}`+bind 추가(#455 형제 byte-미러). **SKILL codify: 부분픽스 재검증(#377/#452)을 "같은 사이클에 머지된 `fix(security)` 커밋"으로 확장 — owner가 한 테이블의 IDOR 클러스터를 픽스하면 그 테이블 나머지 mutate 핸들러를 재grep해 픽스가 놓친 형제 격리(Area5 #481)**]. | **#480** 주문 수정(`PUT /orders/:id`, `orders/update.ts`)이 신규 FK `shipment_checks.order_item_id`(`0439:18`, ON DELETE 절 없음=NO ACTION/RESTRICT)를 order_items 삭제 전 정리 안 함. `/pack` 검수 체크리스트가 열린 주문(shipment_checks 존재)을 수정하면 `DELETE FROM order_items`(update.ts:216 batch·:237 standalone)가 **FK 위반 throw → 주문 수정 100% 500**(generic `서버 오류`, 원인 은폐). 삭제(`core.ts`)는 `:591 DELETE shipments`(shipment_id ON DELETE CASCADE)가 `:599 order_items`보다 먼저 실행돼 cascade 선제거+`#464/#454` RESTRICT-자식(return_items/print_file_map) 명시 정리 패턴 보유=**안전(FP)**, update.ts만 shipments 미삭제라 cascade 미발동+정리 패턴 미적용=비대칭. update.ts:187은 `cards.order_item_id`만 NULL 해제, shipment_checks 누락 형제. 부차: 카드 보존 경로(:237 standalone) throw 시 헤더 UPDATE(:158)·`cards.order_item_id=NULL`(:187) 선커밋 롤백 안 됨 = **카드 부분분리 잔재**(order_items 미교체인데 카드 detached, 데이터 정합성 MED). 도달성 LIVE(`/pack` 0439 MANAGER/OPERATOR + PUT SHIPPED 포함 전상태 허용). bug small [Area 4, issue-only(파괴적 삭제 batch mutation 추가+검수 리셋 정책+egress, #454/#464 선례). 수정=update.ts 2경로에 `DELETE FROM shipment_checks WHERE order_item_id IN (SELECT id FROM order_items WHERE order_id=?)` order_items 삭제 前. **SKILL codify: churn-트리거 재스캔(#477)을 하드삭제/취소 핸들러뿐 아니라 order_items 통째-교체하는 "주문 수정 경로"로 확장 + CASCADE-부모 비대칭(삭제=cascade안전 vs 수정=cascade미발동 RESTRICT) Area4 #480**]. | **#479** 출고검수(`/pack`, v2 P4 `cd492fd`) 부분수량 프롬프트가 `showPrompt(msg, options)`(shell.js:933)의 2번째 인자에 `String(cur)` 문자열을 오전달 → `options.defaultValue` undefined(shell.js:958) → **textarea가 현재수량 미리채움 실패, 빈 상태로 열림**. 형제 `leaves.js:353`은 options 객체 정확 전달=격리 증거. `packSetPartial`(pack.js:131)이 빈값→null=전량 해석이라 **이미 부분수량(3/10) 설정된 라인 재편집 시 확인만 누르면 무음 전량 리셋 후 `PATCH /checklist/:id` 저장(무음 데이터 손실)**. 도달성 LIVE(0439 `/pack` MANAGER/OPERATOR, `pack.js:85` "부분" onclick). **자동수정 완료**(`b274bb1`: `{title,defaultValue,placeholder}` 객체로 정정, #426 codify상 options 정확전달=안전 패턴교체) — `npm run verify` tsc0/build0. bug small [Area 3, **자동수정**]. | **#478** 상태 동기화(`sync-statuses`, `orders/lifecycle.ts:512` 수동 버튼)의 신규 P1 출고정합 루프(`:557`)가 **LIMIT 없는 `toShip`**(auto_complete_date 도래분 전부)에 주문당 `ensureShipmentForOrder`(`shipmentHelper.ts:46`, 내부 6~9 순차 D1 서브요청) 호출 = 데이터-스케일 N+1. Step1 상태전이는 `db.batch`(청크80)로 최적화됐으나 헬퍼 루프만 순차. **~110~165건에서 Worker 서브요청 한도(1000) 소진** → 이후 주문은 status는 이미 SHIPPED 커밋인데 헬퍼는 per-item catch(`:561`)로 **무음 실패** = P1이 없애려던 "SHIPPED인데 shipment 없음" 불일치를 대량 동기화에서 재생성 + 후속 회계 batch(`:566`) 동반 throw. 응답 `data.shipped`=Step1 카운트라 실패 은폐. 도달성 LIVE(`shipments.js:1294`·`taxInvoices.js:1269`). improvement small [Area 2, issue-only(출고/회계 write-path+배치 세만틱·카운트 정책 판단+egress, #474/#458 선례). 수정=`toShip` LIMIT+반복클릭 백로그 소진+remaining/실패 카운트 응답. **SKILL codify: "주문당 헬퍼-루프 서브요청 소진" = IN절 미청크와 별개 N+1 3번째 축(Area2 #478)**]. | **#477** 합포장 대표(primary) shipment 제거 시 자식 `merged_into_id` 미정리 = 비-FK dangling 고아(#443/#454 클래스). 0437이 `shipments.merged_into_id`(합포장 대표 참조, 비-FK) 신설했으나 부모-제거 2경로 누락: ① `orders/core.ts:591` 대표 주문 하드삭제(`DELETE FROM shipments WHERE order_id=?`)가 **타 주문(order_id 상이) 자식**의 merged_into_id 미정리 → 삭제행 dangling → write-redirect(`applyShipmentFieldPatch:753`)가 삭제된 대표로 `UPDATE...WHERE id=<삭제>`=0행인데 `:789 return true`(meta.changes 무시) = **자식 송장/라벨/수신자 편집 무음 소실+success 응답**, 읽기 `LEFT JOIN spm.id=merged_into_id`(:162) NULL 폴백 = 자식 공백. ② shipment CANCELLED(`shipments.ts:889`)도 merged_into_id 미정리 → 정본이 취소행. cross-parent 참조라 FP배제(b) 반례. 도달성 LIVE(합포장 `shipments.js:295`+주문 하드삭제 ADMIN). bug small [Area 4, issue-only(파괴적 삭제/취소 batch mutation 추가+재선출vs해제 정책+egress, #454 선례). **SKILL codify: churn-트리거 재스캔(신규 `*_id`/`*_into_id` 컬럼이 큐레이션된 삭제배치 이후 ALTER 추가되면 그 컬럼만 재스캔) Area4 #477**]. | **#475** `/tax-invoices` 인쇄/PDF 버튼 두 탭 모두 404 — `taxInvoices.ts:995`가 taxScript+cashReceiptsScript를 동일 전역 스코프 concat, 둘 다 IIFE 미래핑 top-level `openPrintURL` 함수선언 → 나중 로드 cash 버전(`cashReceipts.js:372` `/api/cash-receipts/:id/print-url`=부재)이 tax 버전(`taxInvoices.js:1251` `/api/tax-invoices/:id/print-url`=실재 `queries.ts:423`)을 silent shadow. 헤더 `#tax-cash-scope`가 동일 충돌을 인지·`ti`격리(statusLabels 등)했으나 openPrintURL 누락 형제. 세금계산서 인쇄(`:178/:552`)·현금영수증 인쇄(`:103/:310`) 동반 사망. bug small [Area 3, issue-only(엔드포인트 신설/버튼제거 판단+egress). 수정=tax `openPrintURL`→`ti`프리픽스 리네임+onclick 2곳(즉시회복) & cash print-url 라우트 신설 or 버튼제거]. **SKILL codify: `?raw` concat 전역 스코프 함수선언 충돌 standing scan(Area 3 #475)**. | **#476** 거래처 상세 여신설정 저장(`clientDetail.js:367 saveCreditSettings`→`PATCH /api/clients/:id/credit`)·활성토글(`:444 toggleClientActive`→`PATCH /:id/toggle-active`) dead-button — clients 라우터 PATCH는 `/:id`·`/portal-account`·`/billing-group`뿐(둘 다 부재, Hono exact-match상 `/:id`로 안 잡힘→항상 404). 프론트 배선·렌더 완료(도달성 LIVE `/clients/:id`)인데 백엔드 미구현 → 여신한도/여신보류 저장 불능(HIGH)·활성토글 불능(MED). bug small [Area 3, issue-only(백엔드 API 신설=Area3 자동수정금지). 수정=2 라우트 신설+entityFilter #437]. | **#474** 바로빌 은행 동기화 2루프(`bank.ts:508`/`:1781` `POST /sync-barobill`·`/auto-sync`)가 이미 존재하는 UNIQUE 인덱스(`idx_bt_codef_id (bank_account_id, codef_transaction_id)` = `migrations/0043:46`)를 **건별 dup-check SELECT로 재확인** → 거래당 SELECT+INSERT 2 라운드트립 순차 await. 넓은 기간 백필(bank.js:880 계좌개설일부터·bank.js:923 임의범위) 시 계좌×날짜×거래(일 500) = 수천 순차 SELECT → Worker 한도 시 날짜별 catch(`:527`/`:1798`)가 무음 부분적재. 수정=`INSERT ... ON CONFLICT(bank_account_id, codef_transaction_id) DO NOTHING` + `meta.changes`로 카운트 보존(의미 동일: NULL codef distinct라 refKey無 거래는 현행처럼 항상 insert). improvement small [Area 2, issue-only(재무 write-path+egress 검증불가, #458/#462 선례)]. | **#473** 거래처 포털계정 GET/PATCH/DELETE(`clients.ts:1070/1138/1182`)가 entity 소유 검증 없이 `WHERE client_id=?`만 → **#452(closed=done)의 부분픽스** — #452가 지목한 5모듈 중 messageTemplates(`:66/99` entityFilter)·accounts-payable purchase-payment(`:435/506`)는 픽스됐으나 clients 포털계정만 bare 잔존. 같은 파일 POST(생성)는 `entity_id=getEntityId(c)` 스탬프·client_notes DELETE(#471)는 방금 픽스 = 격리 의도 명백. entity-scoped ADMIN이 `PATCH /api/clients/{타법인 client id}/portal-account {password}`로 **타법인 고객포털 비번 재설정=계정 탈취**(credential IDOR), DELETE=계정 삭제, GET=login_id/연락처/최근로그인 열람(MANAGER 도달). 도달성 LIVE(clientDetail.js:463/552/562/570). Area5 22:00 스캔이 재격리했으나 "#452 이미보고"로 오FP 드롭 → closed≠fixed 교훈. 수정=3핸들러 WHERE에 `${entityFilter(c).clause}`(#471 미러). bug small [Area 6, issue-only(IDOR=owner 워크플로+egress, #452/#471 선례), **다중모듈 우산 이슈 부분픽스 재검증 standing scan codify(SKILL Area6 #473)**]. 부차(latent/dead): `fixedAssets.ts:148` dispose도 형제-비대칭 bare지만 fixed-assets 프론트/메뉴 참조 0=dead(#334 보안 우선순위 낮음) |
| ✅ approved | 0 |
| 👀 reviewed | 0 (#372 CSV도 owner close-completed → done 이관) |
| ✔️ done | **163 (157 + 본 Area6 done-sync 6건 이관**[#465 bom_items 무-writer split-brain `d2ebed6`·#466 발송단가 SOAP fan-out 캐시 `760c3de`·#467 연차촉진 배너 파라미터 `665d191`·#469 미사용연차수당 재계산 `a1d8eb8`·#470 payroll sync-attendance IDOR `4d78be6`·#471 client_notes 삭제 IDOR `4d78be6`] — **전 픽스 in-tree 검증 완료**(HEAD=origin/main `28c14c2` 0/0 동기 = #422 디버전스 0; #470 `core.ts:599 efP`+집계CTE 연쇄격리·#471 `clients.ts:604/608 client_id+ef` diff 대조]) |
| ❌ rejected | 3 |

> 📦 **과거 사이클 로그**는 `IMPROVEMENT_BACKLOG_ARCHIVE.md`/git 히스토리로 이관됨 (2026-06-10 1차 분리, 2026-06-25 2차 트림 343KB→192KB, **2026-06-25T10:00 3차 트림 — 06-17~06-23 사이클 로그를 git 히스토리로 이관: node_modules 부재 세션에서 commit-gate(typecheck) 차단으로 API 푸시 필요 → 파일 축소로 비용 절감 + 256KB 한도 회복**). 신규 로그는 계속 이 파일 상단에 추가. 본 파일은 직전 2일치(06-24~) 사이클 로그 + 영구 참조 섹션(Approved/New/Auto-fixed/Done/Rejected/FP 카탈로그)만 유지. 이관분은 `git log -p -- IMPROVEMENT_BACKLOG.md`로 복원 가능.

> **Area 1 프로덕션 헬스 (2026-07-06T06:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→d3685f4` force-update, **HEAD=origin/main `d3685f4` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 1 **29회차** — egress 차단(prod 직접 fetch·Playwright 도달 불가)이라 CI/E2E는 GitHub Actions API로 검증. 직전 Area1(`48b30c3`, 07-04T06:00, 28회차) 이후 코드 churn = **`157ff2e feat(inventory) 기본창고 일괄배정`·`bda6a50 feat(batch) 출고 품목내용+품목사진 1차`·`ccaf61a docs`**(owner "피드백 6건 배치", prod 배포 대기) + Area2~6 chore. **신선 각도 = #484(직전 Area6 net-new) 배포 체인 실측 검증 — bda6a50 image_key SELECT 코드가 prod 자동배포됐는지 CI로 확정 + smoke 컬럼-드리프트 사각 방어 자동수정.**
> - **🔺 #484 = LIVE 위험 확정(코드 prod 배포 실측)**: deploy.yml main 최근 15런 **전부 success** — 그중 **`bda6a50`(품목 사진 1차 = `items.ts:720` GET /:id 명시 SELECT에 image_key 참조) completed/success**. deploy.yml은 build+deploy+smoke이므로 **success = image_key SELECT 코드가 prod webapp에 자동배포 완료**. 반면 `0442 ADD COLUMN image_key`는 수동 적용(deploy.yml에 마이그 스텝 0, `ccaf61a` "prod 배포 대기") → **0442 미적용 시 품목 상세/편집 모달(`modals.js:54`) + 사진 핸들러 3종(`:749/:788/:807`) 100% 500(no such column)**. #484가 "잠재 LIVE"로 보고했으나 **Area 1이 코드-배포 실측으로 위험 구간 확정**(잠재→코드측 확정, 마이그 적용 여부만 미검증=egress 차단). smoke green은 은폐 지속 — smoke가 `/api/items?limit=10`(items.list=GROUP BY 집계, image_key 미참조)만 프로브(#430 맹점의 SELECT-detail 축).
> - **🟡 방어 자동수정 1건(이슈 신설 X) — smoke.cjs items.detail 프로브 추가(#484 방어)**: Area6 SKILL이 "방어 자동수정 후보(별건)"로 지목한 detail 프로브를 구현 — `scripts/smoke.cjs:80`에 `{ path: '/api/items/1', name: 'items.detail', allow404: true }` 추가. **컬럼-드리프트 정밀 격리**: GET /:id는 authMiddleware만(role 무·smoke user 도달)이라 0442 적용 시 200/404(allow404 PASS), 0442 미적용 시 `no such column` **500 → FAIL**(smoke.cjs:294 200-only PASS, 404만 allow, 500은 FAIL). items.list(집계)가 못 보는 명시-컬럼 SELECT 드리프트를 다음 배포부터 CI가 포착 = **#484 클래스 영구 detector + 이 push의 CI smoke 결과가 0442 prod 적용 여부를 확정**(red=미적용 LIVE / green=적용됨 #484 close 가능). `node -c` 문법 OK·`npm run verify` tsc0/build0. 테스트 인프라 정렬(#429 클래스)이라 안전 자동수정, 신규 이슈 신설 안 함(#484 방어).
> - **🟢 DROP/RENAME 마이그 write-path standing scan(#430) = net-new 0**: `git diff 48b30c3..HEAD -- migrations`의 `+DROP (TABLE\|COLUMN)\|RENAME TO` = **0건**. 0442=`ADD COLUMN`(additive)·0443=`DELETE FROM role_page_permissions/permission_pages WHERE page_key='/workflow'`(dead-row 삭제, 0382 선례 미러 — 라우트/사이드바 코드 선제거 완료). 스키마 DROP·테이블 재빌드 0.
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` = 0327/0412/0416/0420(전부 이번 churn 이전분, prior 사이클 확정) + **0442/0443 고유·비중복**.
> - **🟢 라우트 제거 → smoke 프로브 stale(#429 d축) = clean**: `git diff 48b30c3..HEAD -- src/routes`의 제거된 `.get/.post('path')` = **0건**(157ff2e/bda6a50은 라우트 추가만) → smoke.cjs 기존 프로브 전량 유효(stale 유입 0, 신규 items.detail만 추가).
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 12건**(`list_issues(OPEN,auto-improve)` totalCount=12: #473~#484) = 직전 Area6 stats `new=12` **정합**. 본 Area1 신규 이슈 0(smoke 방어 자동수정+#484 코드-배포 실측 코멘트) → new 12 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`d3685f4`)+본 smoke 픽스 push 예정.
> - **🧬 SKILL 강화 1건 — #484 방어 자동수정(smoke detail 프로브)이 배포 체인 자체를 0442 적용 여부 detector로 전환(Area 1 #484 확장, 29회차)**: Area6 codify(detail SELECT smoke 사각)의 방어 자동수정을 **실행** — smoke.cjs에 대표 리소스 detail(`/:id`) 프로브를 추가하면 (1) 명시-컬럼 SELECT의 신규 ADD COLUMN 드리프트를 다음 배포부터 CI가 즉시 포착(list 프로브 사각 해소), (2) **egress로 검증 불가한 "마이그 prod 적용 여부"를 CI smoke 결과가 대리 검증**(red=미적용·기능 500 LIVE / green=적용됨). **레시피**: 신규 `ADD COLUMN <c>`가 `/:id` detail 핸들러의 명시 컬럼리스트 SELECT에 등장하면 → smoke.cjs에 그 리소스 `{ path: '/api/<res>/1', allow404: true }` 프로브 추가(authMiddleware-only 도달 확인 + allow404로 행 부재 허용, 컬럼 미존재 500만 FAIL). #430 write-path 맹점(agent-FK·DROP·프론트실행)에 이은 **smoke 커버리지 4번째 보강축(detail-SELECT 컬럼 존재성)**.
> - 자동수정 1건(smoke items.detail 프로브 = #484 방어), 신규 이슈 0건(#484 코드-배포 실측으로 위험 확정), SKILL 강화 1건(smoke detail 프로브=마이그 적용 detector), done-sync(new=12 유지·12=12 정합), **신선 각도 — #484 배포 체인 실측. bda6a50 CI success=image_key SELECT 코드 prod 배포 확정(0442 수동적용만 미검증) → 잠재 LIVE를 코드측 확정으로 승격. smoke detail 프로브로 클래스 영구 detector 구축(이 push CI가 0442 적용 여부 확정). DROP/마이그중복(0442/0443 고유)/smoke-stale 전수 clean. CI 15/15 green·tsc0/build0.**
>
> **Area 6 자기 진화 (2026-07-06T02:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→ccaf61a` force-update, **HEAD=origin/main `ccaf61a` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 6 **33회차** — 직전 Area6(`1032a43`, 07-05T02:00, 32회차) 이후 신규 코드 churn = **`157ff2e feat(inventory) 기본창고 일괄배정`·`bda6a50 feat(batch) 출고 품목내용+품목사진 1차+출력이력 종료일`·`ccaf61a docs`**(owner "피드백 6건 배치"). **신선 각도 = 직전 Area 5(59b79d4) 이후 신규 feature 2건의 컬럼-diff bridge(백엔드 INSERT/SELECT 컬럼 존재성) + XSS bridge(프론트 신규 innerHTML) + 마이그(0442/0443) 존재성/중복.**
> - **🔺 net-new 1건 — #484 (HIGH·잠재 LIVE, 컬럼-diff bridge)**: `0442_items_image_key.sql`(`ADD COLUMN image_key TEXT`)를 품목 상세 GET `items.ts:719`(`GET /api/items/:id`, 인증만·전역 도달)이 **명시 컬럼리스트 SELECT**(SELECT * 아님)로 참조 → 0442 미적용 prod에서 `no such column: image_key` prepare throw → catch(`:734`) generic 500. **배포 체인 위험 실측**: `bda6a50` deploy.yml **completed/success @14:47**(CI가 코드를 prod webapp-9i0.pages.dev에 자동배포)·post-deploy smoke green이나 **smoke는 `/api/items?limit=10`(items.list = GROUP BY 집계 SELECT, image_key 미참조)만 프로브** → 컬럼 존재성 미검증(#430 맹점의 SELECT-detail 버전). deploy.yml에 마이그 스텝 없음(`db:migrate:prod` 수동) + `ccaf61a` "prod 배포 대기" → **0442 미적용 시 품목 편집/상세 모달(`modals.js:54 axios.get /api/items/:id`) 100% 500**(핵심 관리 경로). 사진 핸들러 3종(`:749/:788/:807`)도 동반. egress 차단으로 prod 실상태 검증 불가라 issue-only(#483 (b)-risk 선례) — owner 판별=`PRAGMA table_info(items)` grep image_key, 없으면 즉시 `db:migrate:prod`(코드 재배포 불요, 이미 배포됨). **알림 발송**(잠재 LIVE·시간민감).
> - **🟢 XSS bridge = net-new 0**: 신규 프론트 churn(`shipments.js`·`shipmentsDashboard.js` 출고 품목내용 표시·`items/modals.js` 사진 모달·`inventory.js`·`production.js`) 전수 clean — 출고 품목내용 sink(`shipments.js:17/26/27`·`shipmentsDashboard.js:16/17/19` `it.content`·`item_name`·`spec`) 전량 `escapeHtml`/`window.escapeHtml` 래핑, 사진 모달은 `img.src = blobUrl`(axios blob objURL, HTML 주입 아님) = 안전. 신규 innerHTML free-text 미escape 0.
> - **🟢 마이그 0443 = 안전(dead-row DELETE)**: `/workflow` permission_pages 폐기(`DELETE FROM role_page_permissions/permission_pages WHERE page_key='/workflow'`) — 페이지/라우트/사이드바 코드 선제거 완료 + 0382 선례 미러(#429 purge 완전성: 백엔드 라우트·프론트 참조 잔존 0 확인). 마이그 번호 중복 standing scan(#438): `uniq -d` = 0327/0412/0416/0420(전부 이번 churn 이전분) + **0442/0443 고유·비중복**.
> - **🟢 done-sync = 11 OPEN 유지(#473~#483) + close-pending 캐시 유효**: `list_issues(OPEN,auto-improve)` totalCount=11 = 직전 stats 정합. owner 신규 close 0(done=163·rejected=3 유지). fixed-in-tree 4건(#479 pack.js·#480 update.ts·#481 purchaseRequests.ts·#483 0441) — 이번 churn(inventory/items/shipments)이 **네 파일 전부 미touch**(`git log <sha>..HEAD -- <file>`=0) → 32회차 close-pending 캐시 규칙상 **재검증 skip, clean verdict 신뢰**. 실제 미픽스 7건(#473·#474·#475·#476·#477·#478·#482) 안티패턴 잔존 정상 open. #422 디버전스: HEAD=origin/main 동기(`ccaf61a`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — code-only CI 배포 하 "detail(`/:id`) 명시 SELECT의 신규 ADD COLUMN"은 smoke list-프로브로 미탐 (Area 6 #484, 33회차)**: #483 (b)-risk(ADD COLUMN + 배포코드 참조)를 Area 6 컬럼-diff bridge가 detail-핸들러에서 격리. **핵심 = smoke가 리소스별 list 엔드포인트는 프로브해도 detail(`/:id`)은 안 하므로**(items.list는 GROUP BY 집계 SELECT라 신규 컬럼 미참조), "GET 프로브 존재=컬럼 커버"가 아님 = #430 맹점의 SELECT-detail 축. **탐지 레시피**: 신규 `ADD COLUMN <c>` 마이그 사이클에서 `grep -rn "<c>" src/routes`가 **명시 컬럼리스트 SELECT**(`SELECT ...,<c>,... FROM <t>`, `SELECT *` 아님)에 등장하고 그 핸들러가 `/:id` detail이면 → smoke.cjs 프로브가 그 detail path를 커버하는지 확인(list만이면 사각). **방어 자동수정 후보(별건)**: smoke.cjs에 대표 리소스 detail 프로브 추가. issue 판정 = egress로 prod 마이그 상태 검증 불가면 issue-only + "배포 전 `db:migrate:prod` 필수" 명시.
> - 신규 이슈 1건(#484 HIGH·잠재 LIVE), 자동수정 0건, SKILL 강화 1건(detail SELECT smoke 사각), done-sync(11 OPEN 유지·close-pending 캐시 유효), **신선 각도 — 신규 feature 2건(157ff2e/bda6a50) 컬럼·XSS bridge. #484 = 0442 image_key가 품목 상세 명시 SELECT에 참조되나 마이그 수동적용·smoke list-only라 prod 드리프트 위험(잠재 LIVE, 알림). XSS 전량 escape·0443 dead-row 안전·마이그 고유. tsc 무관(코드수정 0).**
>
> **Area 5 보안 + 인프라 (2026-07-05T22:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→2326557` force-update, **HEAD=origin/main `2326557` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 5 **29회차** — 직전 Area5(`bb49684`, 07-04T22:00, 28회차) 이후 코드 churn = **`c6e1c66 feat(shipping-ux) 합배송 접수 UX P1+P2`**(orders/core.ts +64·queries.ts +1·orderForm/client.js +26·orders.js +30) + `c3291e1 fix(shipping-ux)`(배지 컬럼 잘림·orders.js/taxInvoices.js) + `67732b6`(refreshStatus, Area3 처리분). **신선 각도 = 신규 합배송 UX(c6e1c66)를 보안 렌즈로 전수 — 프론트 신규 innerHTML XSS + 신규 consolidation write-path IDOR + cross-entity 파생 read 격리.**
> - **🟢 XSS = net-new 0 (프론트 신규 렌더 전수 clean)**: 신규 innerHTML sink 전량 `escapeHtml` 래핑 확인 — orders.js consBadge(`consolidate_root_number`·`consolidation_child_numbers` GROUP_CONCAT 결과 둘 다 `escapeHtml`)·`buildConsolidationSection`(멤버 `m.order_number` `escapeHtml`)·orderForm/client.js 배너 후보행(`order_number`·`entity_name`·`delivery_method`·`itemTxt` 전량 escape). order_number는 시스템 채번이나 방어적 escape 유지 = 개발자가 컨벤션 일관 적용. taxInvoices.js는 `refreshStatus→tiRefreshStatus` 리네임(Area3 `67732b6`)만, 신규 sink 0.
> - **🟢 consolidation write-path IDOR = FP(client-scoped 격리, net-new 0)**: `create.ts:52`·`update.ts:65`가 `consolidate_with_order_id` 대상을 `WHERE id=?`(entityFilter 무)로 조회하나 **`Number(target.client_id) === Number(orderData.client_id)` 게이트** → 같은 거래처 내에서만 묶음(cross-tenant 절도 불가). entityFilter 미사용은 누락이 아니라 **client가 올바른 격리키**(합배송은 정의상 intra-client·cross-entity, P2 배너 `별도 주문서 유지·배송만 한 박스` 명시). update는 `existingOrder`를 `efPut.clause`로 먼저 entity-게이트 + 자기참조 차단(`!== Number(id)`). **신규 FP클래스 codify(SKILL)**.
> - **🟢 cross-entity 파생 read = 문서화된 최소 필드 예외(FP)**: 목록 배지 파생 쿼리(`core.ts:155-196` `id IN(pageIds)`·`consolidate_with_order_id IN(...)`)·상세 멤버(`:475-499`)·후보(`queries.ts /unshipped-by-client`, `billing_status` 신규 추가)가 `orderVisibilityFilter` 미재적용이나 `queries.ts:44-47` 주석 명시(`⚠️ entityFilter 의도적 미적용(최소 필드 cross-entity)`). 노출 = id·order_number·billing_status enum(최소). 상세 `GET /:id`는 파생 쿼리 **전** visibility 게이트(`core.ts:462` `ownEntity===viewerEntity||hasAssigned else 404`) 통과 → 멤버-링크 클릭도 대상 게이트로 재차 404 → 최소필드 노출 수렴(#365 서빙프록시 예외의 consolidation 버전, Area4 `queries.ts:46` FP와 동일 클래스). billing_status 추가는 enum·same-client이라 최소필드 클래스 내.
> - **🟢 시크릿 하드코딩 = 0**: `grep -rnE "c\.env\.[A-Z_]+ *\|\| *'" src` = `fax.ts:43 BAROBILL_FTP_PASSWORD || ''`(빈문자열=백도어 아님, 미설정 시 인증실패 귀결) **1건뿐 = 기존 FP**. 기본 비밀번호 패턴 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 11건**(`list_issues(OPEN,auto-improve)` totalCount=11: #473~#483) = 직전 Area4 stats `new=11` **정합**. 본 Area5 신규 이슈 0(clean cycle) → new 11 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`2326557`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — IDOR 스캔 FP클래스 "client-scoped(entity 아님) 격리키를 쓰는 태생적 cross-entity 기능"의 write path codify(Area 5, 29회차)**: 합배송처럼 같은 거래처의 타법인 주문을 묶는 게 본질인 write path는 대상 조회가 `WHERE id=?`(entityFilter 무)라 IDOR 스캔에 bare-mutate로 잡히나, **격리키가 entity_id가 아니라 client_id**라 confirmed 아님. **판별 3축**: ① 대상 조회가 `WHERE id=?` 뒤 코드에서 `client_id` 일치 명시 검증(entityFilter 대신) ② 기능이 "같은 거래처" 전제로 설계(주석·배너가 intra-client cross-entity 명시) ③ 파생 read가 최소 필드(id·order_number·billing_status enum)만 노출 + 소스에 `entityFilter 의도적 미적용(최소 필드 cross-entity)` 주석. read 안전 = 상세 `GET /:id`가 파생 쿼리 前 visibility 게이트. `entity_id 없는 전역 마스터`(FP⑤)·`문서화된 cross-entity mutation`(line 227)과 나란히 IDOR FP 카탈로그 편입.
> - 신규 이슈 0건(net-new 0, clean cycle), 자동수정 0건, SKILL 강화 1건(client-scoped 격리키 cross-entity write FP), done-sync(new=11 유지·11=11 정합), **신선 각도 — 신규 합배송 UX(c6e1c66) 보안 렌즈 전수. 프론트 신규 innerHTML XSS 전량 escape·consolidation write-path는 client_id 게이트(entity 아닌 client가 올바른 격리키=FP)·cross-entity 파생 read는 문서화된 최소필드 예외·시크릿 하드코딩 0(fax `||''` FP). tsc exit0. 코드수정 0.**
>
> **Area 4 데이터 정합성 (2026-07-05T18:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→c6e1c66` force-update, **HEAD=origin/main `c6e1c66` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 4 **29회차** — 직전 Area4(`086cf03`, 07-04T18:00, 28회차) 이후 **신규 코드 churn = `c6e1c66 feat(shipping-ux) 합배송 접수 UX P1+P2`**(orders/core.ts +64·queries.ts +1) + 마이그 0441(직전 Area4 산출)만. **신선 각도 = 신규 합배송 UX 기능(c6e1c66)을 데이터정합 렌즈로 전수 — read-path 파생 정합성·write 검증·shipment merge 체인 self-heal·cross-entity 배지 격리.**
> - **🟢 c6e1c66 신규 정합성 버그 = net-new 0 (feature 견고)**: 신규 코드는 (1) 목록 배지 자식집계(`core.ts:159-187`, IN 80청크 N+1 회피)·(2) 상세 묶음멤버(`core.ts:477-499`) **둘 다 read-only 파생 + try/catch로 실패 시 목록/상세 무영향**(배지만 생략). write 경로(`create.ts:48`·`update.ts:58`)는 **같은 client_id 검증 + root 체인 해소(`target.consolidate_with_order_id || target.id`) + 자기참조 차단** = 무결. **root-becomes-child 2단 체인 gap 검토(FP 배제)**: root A(자식 B,C→A)를 D의 자식으로 재편(A→D)해도 `shipmentHelper.ts:170-171`이 출고 시 **min shipment id 대표 + 자식 재지정으로 체인 flatten** → 단일 shipment 그룹으로 수렴(dangling·데이터손실 0). 순수 order 포인터 2단 체인은 shipment 계층이 self-heal.
> - **🟢 cross-entity 배지 = 문서화된 설계의도(FP 배제)**: 신규 배지/멤버 쿼리가 `orderVisibilityFilter` 미재적용(같은 client의 타법인 형제 order_number 노출)이나, `queries.ts:46` 주석 명시 — `⚠️ entityFilter 의도적 미적용(최소 필드 cross-entity): 같은 거래처 타법인 주문과의 합배송 판단이 목적`. 합배송 후보(`/unshipped-by-client`)와 동일 명시적 예외 클래스(#365 서빙프록시 예외의 도메인 버전). 노출 = 최소 필드(id·order_number) + 같은 거래처 묶음 = 기능 본질(#334 도달성 규칙상 정상).
> - **🔺 #483 blast radius 확대 재확인 + 코멘트**: c6e1c66이 미인덱스 `consolidate_with_order_id` 조회를 2곳 추가 — 목록 배지(`core.ts:174` `WHERE consolidate_with_order_id IN(pageIds)` = **모든 GET /orders**)·상세 멤버(`core.ts:493` `OR consolidate_with_order_id=?` = **모든 주문 상세**). 0441 미적용 시 풀스캔 범위가 원 이슈 `/daily`→**주문 목록·상세 전역**으로 확대. **0441이 두 신규 predicate 모두 커버**(idx가 IN/`=`/OR-optimization 분기 적용) → 추가 마이그 불요, 기존 0441 prod 적용 **우선순위만 상승**. #483에 blast-radius 코멘트 게시(신규 이슈 아님·중복 회피).
> - **🟢 신규 마이그 0 = 컬럼존재성/CHECK/FK 신규 유입 없음**: `git diff --stat 086cf03..HEAD -- migrations` = 0441 인덱스만(직전 Area4 산출). c6e1c66은 SELECT/INSERT 컬럼 무-신규(기존 consolidate_with_order_id·order_number만 조회) → 데이터정합 신규 FK/포인터 도입 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 11건**(`list_issues(OPEN,auto-improve)` totalCount=11: #473~#483) = 직전 Area3 stats `new=11` **정합**. 본 Area4 신규 이슈 0(#483 코멘트만) → new 11 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`c6e1c66`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 신규 feature가 기존 OPEN "인덱스 누락" 이슈 컬럼에 read hot-path 추가 시 중복 이슈 대신 blast-radius 코멘트 + 합배송 cross-entity 배지 FP 클래스 codify(Area 4 #483 확장, 29회차)**: (1) prod-pending "missing index" 이슈(#483) 컬럼에 신규 feature가 조회 predicate를 추가하면 → **기존 인덱스가 신규 predicate를 커버하는지 재검증 후 커버되면 중복 이슈 금지, 기존 이슈에 blast-radius 코멘트로 우선순위 갱신**(신규 마이그/이슈 남발 방지). (2) **cross-entity 파생 배지 FP 클래스**: consolidation/합배송 "candidate·묶음" 쿼리는 `queries.ts:46`처럼 `entityFilter 의도적 미적용` 주석이 명시된 도메인 예외(같은 거래처 타법인 묶음 판단) → 그 소스에서 파생된 배지/멤버 쿼리도 entity 미필터를 leak로 오플래그 금지(주석 명시 여부로 판별, #365 서빙프록시 예외의 합배송 버전).
> - 신규 이슈 0건(net-new 0), 자동수정 0건, #483 코멘트 1건(blast-radius 확대), SKILL 강화 1건(신규feature→기존인덱스이슈 blast-radius 코멘트 + cross-entity 배지 FP), done-sync(new 11 유지·11=11 정합), **신선 각도 — 신규 합배송 UX(c6e1c66) 데이터정합 전수. read 파생 정합·write 검증·shipment merge self-heal·cross-entity 배지 = 전량 무결/FP. #483 blast radius만 확대(0441 우선순위↑). 코드수정 0.**
>
> **Area 3 UX/기능 감사 (2026-07-05T14:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→3a4a4ad` force-update, **HEAD=origin/main `3a4a4ad` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 3 **29회차** — 직전 Area3(`086cf03`, 07-04T14:00, 27회차) 이후 **UX 코드 churn = 0**(`git diff --stat 086cf03..HEAD -- src/scripts src/pages` = 빈 출력, 유일 churn = 마이그 0441·auto-improve chore/docs만) = clean cycle. **신선 각도 = #475 `?raw` concat 전역 스코프 shadow 스캔을 taxInvoices 단일점에서 전 concat-point(7개)로 확장 + 함수 body 대조로 collision을 harmful(동작상이)/harmless(동치)로 분류.**
> - **🟡 신규 자동수정 1건(이슈 신설 X) — #475 형제 `refreshStatus` shadow dead-button 복구(`67732b6`)**: 전 concat-point(productionReports·equipment·activityLog·inventory·cashSchedule·reports·taxInvoices) top-level 식별자 collision 자동스캔 → **collision 5건**: `reports`(fmt=양쪽 정수포맷 동치 harmless)·`taxInvoices`(currentPage=hometax onclick 전역참조로 의도적 미개명 헤더명시 FP·fmt=동치 harmless·openPrintURL=#475 known·**refreshStatus=harmful net-new**). refreshStatus: tax `async function refreshStatus`(`taxInvoices.js:648`, `POST /api/tax-invoices/:id/refresh-status`=바로빌 NTS 상태폴링, 실핸들러 `manage.ts:158`)를 cash `function refreshStatus`(`cashReceipts.js:359`)가 concat 순서상(taxScript+cashReceiptsScript) 함수선언 호이스팅으로 shadow → 세금계산서 "상태 새로고침" 버튼(`:175`·`:544` onclick)이 `POST /api/cash-receipts/:id/refresh-status` 호출 → 그 라우트는 **stub**(`cashReceipts.ts:368` `{success:false,error:'현금영수증 상태 새로고침은 현재 지원하지 않습니다.'}` 400) → 세금계산서 상태폴링 100% 실패(엉뚱한 현금영수증 400) + 실핸들러 사장. **#475(openPrintURL) 형제이나 미보고**(#tax-cash-scope 헤더가 statusLabels/statusColors/renderPagination만 ti격리, refreshStatus·openPrintURL 누락). **자동수정 = 안전**: openPrintURL과 달리 tax 라우트 **실재** + cash측 **의도적 stub(미지원)**이라 "어느 엔드포인트 살릴지" 결정 불요 → 순수 ti 프리픽스 격리(def `:648`+onclick `:175/:544` `tiRefreshStatus` 리네임, #479 Area3 안전 패턴교체 클래스). `npm run verify` tsc0/build0. #475에 코멘트로 형제 발견+복구 기록 → #475 남은 범위=openPrintURL 단독. 신규 이슈 신설 안 함(같은 근본원인, 자동수정 완료).
> - **🟢 그 외 collision 전량 FP/harmless**: `fmt`(reports·taxInvoices 각각 cash와 collision)는 양쪽 `parseFloat(n)||0` 정수 포맷이라 동작 동치(cash는 `{min/maxFractionDigits:0}` 명시뿐, 원화 정수라 무시각차)=harmless. `currentPage`(taxInvoices↔cashReceipts)는 헤더 주석이 "hometaxInvoices.js 페이지 onclick이 전역 참조하므로 의도적 미개명"으로 명시 = FP(각 탭 load가 진입 시 `currentPage=page` 리셋). productionReports/equipment/activityLog/inventory/cashSchedule = collision 0.
> - **🟢 showPrompt/showConfirm 2번째 인자 오용(#426/#479) 전수 = clean**: `showPrompt` 2콜(leaves.js:353 객체·pack.js:136 `{title,defaultValue,placeholder}`=#479 fixed-in-tree 재확인) 전부 정확. `showConfirm` 148콜 중 2번째 인자=함수 오용 **0건**(전량 `await`/`.then`/options객체). safeSubmit 채택 12곳(15회차 정의만 2줄→owner 배선 진행).
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 11건**(`list_issues(OPEN,auto-improve)` totalCount=11: #473~#483) = 직전 Area2 stats `new=11` **정합**. 본 Area3 신규 이슈 0(refreshStatus는 자동수정+#475 코멘트, 신설 X) → new 11 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`3a4a4ad`)+본 픽스 `67732b6` push 예정.
> - **🧬 SKILL 강화 1건 — `?raw` concat shadow 스캔의 전-concat-point 확장 + body 대조 분류 codify(Area 3 #475 확장, 29회차)**: #475 codify(line 139)는 taxInvoices 단일점만 언급했으나, **전 concat-point(`grep -rnE "combinedScript|Script \+ '\\n' \+" src/pages` = 7곳)에서 co-concat된 `?raw` 파일들의 top-level `function/var/const/let` 이름 collision을 자동추출**(각 파일 컬럼0 선언 → `sort | uniq -d`)해야 형제 누락 없음. **핵심 = collision을 body 대조로 harmful/harmless 3분류**: ① **harmful**(동명+동작상이+onclick/전역참조) = 나중 로드가 앞 버전 사장(refreshStatus: 다른 엔드포인트). ② **harmless-동치**(동명+body 동치) = 어느 게 이겨도 결과 동일(fmt: 양쪽 정수포맷). ③ **의도적-공유**(헤더 주석이 명시한 크로스파일 전역참조) = FP(currentPage: hometax onclick). **레시피**: collision 발견 시 각 정의 body를 `sed`로 추출해 (a)호출 엔드포인트/side-effect가 다르면 harmful (b)동일하면 harmless (c)헤더 `#*-scope` 주석에 미개명 사유 명시면 FP. **자동수정 판정 = harmful 중 "shadow된 정답 버전의 타깃이 실재 + 이긴 버전이 의도적 stub/부재"면 순수 ti격리 안전자동수정(refreshStatus), "어느 엔드포인트 살릴지 결정 동반"이면 issue-only(openPrintURL #475)**. line 139(단일점 shadow)의 전-concat-point 자동화 + body-분류 확장.
> - 신규 이슈 0건(net-new 0, clean cycle), **자동수정 1건(#475 형제 refreshStatus ti격리 `67732b6`)**, SKILL 강화 1건(concat shadow 전점 확장+body 분류), done-sync(new=11 유지·11=11 정합), **신선 각도 — #475 concat shadow 스캔을 전 concat-point(7)로 확장. collision 5건 중 refreshStatus만 harmful net-new(자동수정), fmt×2 harmless 동치·currentPage 의도적공유 FP. showPrompt/showConfirm(148)·safeSubmit 전수 clean. tsc0/build0.**
>
> **Area 2 코드 품질 심층 분석 (2026-07-05T10:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→83400ba` force-update, **HEAD=origin/main `83400ba` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 2 **31회차** — 직전 Area2(`ffb1509`, 07-04T10:00, 30회차) 이후 코드 churn = **0**(유일 churn = `bb49684` 마이그 0441 인덱스 = 직전 Area4 산출, routes/scripts 0) → **사실상 clean cycle**. **신선 각도 = 컬럼 존재성 자동스캔(최고 생산성 클래스)을 전 코드베이스 재수행 + 아직 자동화 안 된 UPDATE SET 존재성 미러 추가 + N+1 헬퍼-루프(#478) 전수.**
> - **🟢 타입 검증 = clean**: `npx tsc --noEmit` **exit 0**.
> - **🟢 N+1 헬퍼-루프(#478) 전수 = net-new 0**: `for..of` 루프 바디에 `await <helper/db.prepare>` 있는 사이트 40여 곳 추출 → 데이터-스케일 미유계 후보 정밀검토. `shipments.ts:356`(merge 라우트 `ensureShipmentForOrder` 루프)은 대상 `orders`가 user-selected `order_ids`(2+ 합포장)라 practically bounded(#478 FP클래스ⓐ). `weeklyPurchase.ts:47/68`은 루프 바디가 in-memory Map 빌드(DB 호출은 루프 밖 + 서브쿼리로 IN 회피=#458 하드닝 완료). 기존 #478(lifecycle.ts:557 `sync-statuses`)만 open 유지. 신규 미유계 N+1 0.
> - **🟢 컬럼 존재성 자동스캔(INSERT+UPDATE+SELECT) = net-new 0 (전 후보 파서 FP)**: 마이그 CREATE/ALTER로 schema.json ground-truth(206테이블) 빌드 후 전 코드베이스 INSERT/UPDATE 컬럼셋 대조. **INSERT 후보 12건 전량 FP**: `inventory.entity_id`/`storage_zone_id`(0232 `inventory_new`→RENAME)·`bank_match_rules.matched_category_id`(0270 `_new`)·`caps_employee_map.site_id`(0199 `_new`) = 테이블 재빌드 RENAME 미접힘 / `budgets.feb·mar·may·…`(0219 `jan REAL, feb REAL, mar REAL` 다중컬럼 라인) = 줄-첫-토큰 파서 누락. 각 후보 `grep RENAME TO <t>`/재빌드 본문/원 마이그 라인 직접 대조로 실재 확인. **UPDATE SET 후보 2건 전량 FP**: `cards.ai_analysis_id`(`UPDATE cards SET thumbnail_url=(SELECT ... JOIN order_items oi ON oi.ai_analysis_id ...)` = 서브쿼리 컬럼 오포착, 실 SET은 thumbnail_url만)·`bank_match_rules.matched_category_id`(0270 rebuild). **접기 규칙 적용 후 진짜 미존재 컬럼 0**(#377/#394 클래스 없음).
> - **🟢 #405 activity_logs 형제 완전성 = clean**: `INSERT INTO activity_logs` 전수 1곳(`cards/lifecycle.ts:1078`)뿐이며 컬럼셋(`user_id/action/entity_type/entity_id/details`) 전부 실재(#405 픽스 이후 resource_* 오컬럼 잔존 형제 0).
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 11건**(`list_issues(OPEN,auto-improve)` totalCount=11: #473~#483) = 직전 Area1 stats `new=11` **정합**. 본 Area2 신규 이슈 0(clean cycle) → new 11 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`83400ba`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 컬럼 존재성 자동스캔의 `_new` 재빌드/다중컬럼/SET-서브쿼리 3갭 접기 codify(Area 2, 31회차)**: 컬럼존재성 스캔(최고 생산성 클래스)이 이번 사이클 12+2 후보를 냈으나 **전량 파서 FP** — ① SQLite 테이블 재빌드(`CREATE <t>_new; RENAME TO <t>`) 미접힘으로 재빌드 테이블 신규 컬럼 오플래그(inventory/bank_match_rules/caps 3건) ② 다중컬럼 라인(`jan, feb, mar` 한 줄) 줄-첫-토큰 파싱 누락(budgets 월컬럼) ③ UPDATE SET 절 추출이 서브쿼리 내 JOIN 컬럼 오포착(cards.ai_analysis_id). **접기 규칙**: ground-truth 빌드 시 `RENAME TO <t>` 추적해 `<t>_new` 컬럼을 `<t>`에 fold + CREATE 본문 콤마분할 후 조각별 `(\w+)\s+TYPE` 매치 + SET 절은 첫 최상위 `(`에서 절단(서브쿼리 제외). 미적용 매치는 재검증 없이 신뢰 금지(후보마다 `RENAME TO`/재빌드 본문/원 라인 직접 grep). JOIN-aware SELECT FP(line 86)의 INSERT/UPDATE 버전.
> - 신규 이슈 0건(net-new 0, clean cycle), 자동수정 0건, SKILL 강화 1건(컬럼존재성 파서 3갭 접기), done-sync(new=11 유지·11=11 정합), **신선 각도 — 컬럼존재성 자동스캔 전 코드베이스 재수행 + UPDATE SET 미러 신규 + N+1 헬퍼-루프 전수. INSERT 12·UPDATE 2 후보 전량 파서 FP(`_new` 재빌드·다중컬럼 라인·SET 서브쿼리), 진짜 미존재 컬럼 0. N+1 신규 미유계 0(shipments merge bounded FP). #405 형제 clean. tsc exit0. 코드수정 0.**
>
> **Area 1 프로덕션 헬스 (2026-07-05T06:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→e8f401e` force-update, **HEAD=origin/main `e8f401e` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 1 **28회차** — egress 차단(prod 직접 fetch·Playwright 도달 불가)이라 CI/E2E는 GitHub Actions API로 검증. 직전 Area1(`0db3b88`, 07-04T06:00, 27회차) 이후 **routes/scripts 코드 churn 0** — 유일 churn = **`bb49684`(Area4 chore, 마이그 0441 인덱스 추가)** + auto-improve chore/docs만. **신선 각도 = 사실상 clean cycle → 마이그레이션 prod 적용 파이프라인 검증(#483 0441이 실제 prod DB에 도달하는가) + DROP/RENAME·마이그중복·smoke-stale standing scan 전수.**
> - **🟢 CI 헬스 = green(15/15, 무-failure)**: `deploy.yml` main 최근 15런 **전부 success**(Area1 27회차→6/5/4/3/2 사이클 배포 + `3e29fe6` print-events fix 포함 전부 배포·smoke 통과). smoke는 `continue-on-error:false`라 run success = smoke 통과 방증.
> - **🟢 유일 churn 마이그 0441 = 순수 additive 인덱스, write-path 무관**: `cat migrations/0441` = `CREATE INDEX IF NOT EXISTS idx_orders_consolidate_with ON orders(consolidate_with_order_id)` 1줄(멱등·0437 byte-미러). smoke 맹점 3축(1=프론트실행·2=DROP write-path·3=agent-FK) 어디에도 해당 안 됨(신규 write/DROP/agent-path 0).
> - **🟢 마이그 prod 적용 파이프라인 검증(#483 신선 각도) = 구조적 정상(수동 적용 워크플로)**: `deploy.yml`은 **build+deploy+smoke만**(migrate 스텝 없음), `verify.yml:44`은 `d1 migrations apply webapp-production **--local**`(로컬만), prod 원격 적용은 owner `db:migrate:prod` 수동. **#483 0441은 code-only-deploy로 prod DB에 미도달 = close-pending 정상 상태**(main엔 0441 있으나 prod idx 미생성). **회귀 위험 0**: 0441은 `CREATE INDEX IF NOT EXISTS`(순수 효율)라 미적용 시 prod는 정확히 동작(단지 인덱스 없이 /daily 상관 서브쿼리 풀스캔) → **#422 코드 디버전스와 달리 마이그 미적용은 correctness 무해**(additive index 한정). owner 액션 = 배포 시 `db:migrate:prod` 병행. **codify: code-only CI 배포 vs 수동 마이그 적용 = 스키마 additive-index 드리프트는 benign(#483), 단 컬럼/테이블 추가 마이그가 배포 코드에 참조되면 `no such column` throw 리스크 → 그런 마이그는 owner가 배포 전 적용 필수(현 워크플로 규율 의존).**
> - **🟢 DROP/RENAME 마이그 write-path standing scan(#430) = net-new 0**: `git diff 0db3b88..HEAD -- migrations`의 `+DROP (TABLE|COLUMN)|RENAME` = **0건**(0441 CREATE INDEX만, additive).
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` 원시 매치 6건(0080/0193/0327/0412/0416/0420) 중 **0080/0193은 `b`-접미 변종**(0080+0080b·0193+0193b = 의도적 명명, prior 정밀 스캔이 full-token으로 잡아 미충돌 처리한 FP), 실충돌 4건(0327/0412/0416/0420)은 전부 이번 churn 이전분. **0441=고유·비중복**.
> - **🟢 라우트 제거 → smoke 프로브 stale(#429 d축) = clean**: `git diff 0db3b88..HEAD -- src/routes` = **0**(라우트 churn 없음) → smoke.cjs 102 프로브 전량 유효(stale 유입 0). `npx tsc --noEmit` exit 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 11건**(`list_issues(OPEN,auto-improve)` totalCount=11: #473~#483) = 직전 Area6 stats `new=11` **정합**. 본 Area1 신규 이슈 0(clean cycle) → new 11 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`e8f401e`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — code-only CI 배포 하의 마이그 적용 드리프트 분류(Area 1 #483 확장, 28회차)**: `deploy.yml`이 main push마다 코드는 자동배포하나 **마이그레이션은 prod에 자동적용 안 함**(verify.yml은 `--local`만) → auto-improve가 생성한 마이그(#483 0441 등)는 owner `db:migrate:prod` 전까지 prod DB 미반영. **분류 규칙**: (a) **additive index/신규 테이블 CREATE**(배포 코드가 그 스키마를 아직 참조 안 하거나 `IF NOT EXISTS`) = **benign 드리프트**(prod correctness 무해, 효율만 지연) → close-pending으로 두고 owner 배포 시 병행. (b) **ADD COLUMN + 그 컬럼을 배포된 코드가 SELECT/INSERT 참조** = **위험**(prod `no such column` throw = 기능 사망, #430 write-path smoke 맹점 클래스) → 그런 마이그는 이슈에 "**배포 전 `db:migrate:prod` 필수**" 명시. 판별 = 신규 마이그의 `ADD COLUMN <c>`가 `grep -rn "<c>" src/routes`에 등장하면 (b), 인덱스/미참조면 (a). **FP 배제**: `IF NOT EXISTS` + 형제 컬럼 이미 존재(0441=0438 컬럼 이미 prod 적용, 인덱스만 추가)면 무해.
> - 자동수정 0건(코드 churn 0), 신규 이슈 0건(net-new 0, 클린 사이클), SKILL 강화 1건(마이그 적용 드리프트 분류), done-sync(new=11 유지·11=11 정합), **신선 각도 — 사실상 코드 churn 0 사이클에서 마이그 prod 적용 파이프라인 검증(#483 0441=benign additive-index 드리프트, code-only 배포로 prod 미도달이 정상) + DROP/마이그중복(0080/0193 b-접미 FP)/smoke-stale 전수 clean. CI 15/15 green·tsc exit0. 코드수정 0.**
>
> **Area 6 자기 진화 (2026-07-05T02:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→1032a43` force-update, **HEAD=origin/main `1032a43` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 6 **32회차** — 직전 Area6(`140414e`, 07-04T02:00, 31회차) 이후 유일 코드 churn = **`3e29fe6 fix(print-events)`**(E-prefix resolveCard + changed_by 1→NULL, Area1/2/4/5 이번 사이클 4중 분석 완료) + `bb49684`(Area4 chore, 마이그 0441 인덱스 추가) + auto-improve chore/docs만. **신선 각도 = done-sync 전수 재grep(closed≠fixed ∧ open≠unfixed 양방향) + close-pending 적체 집계 + 컬럼/XSS bridge.**
> - **🟢 done-sync 전수 재grep = 11 OPEN 전량 상태 확정**: `list_issues(OPEN,auto-improve)` totalCount=**11**(#473~#483) = 직전 Area4/5 stats `new=11` **정합**. 각 OPEN 이슈 안티패턴을 코드에 개별 재grep(line 281 open≠unfixed 레시피): **fixed-in-tree 4건**(#479 pack.js:136 `{defaultValue}` 객체 ✅·#480 update.ts:218/241 shipment_checks 선정리 ✅·#481 purchaseRequests.ts:194/224 comments GET/POST `${ef.clause}` ✅·#483 마이그 0441 실재 ✅) → 전부 OPEN(close pending). **미픽스 7건**(안티패턴 잔존 확정): #473 clients.ts:1070/1138/1182 포털계정 bare `WHERE client_id=?`·#474 bank.ts dup-check SELECT 2건·#475 taxInvoices.js:1251 `openPrintURL` 미-`ti`격리·#476 clients credit/toggle-active 라우트 grep 0(부재)·#477 core.ts/shipments CANCELLED merged_into_id 미정리(shipments:369/416은 unmerge 핸들러=별개)·#478 lifecycle.ts:525 toShip LIMIT 없음·#482 0098 zone_name/code 컬럼레벨 UNIQUE 잔존 = 전량 정상 open.
> - **🟢 #480/#481 형제완전성 = churn 0으로 clean 캐시**: 31회차가 이미 전수 검증(#480=order_items 3삭제사이트·#481=purchaseRequests 13핸들러) → `git log 140414e..HEAD -- src/routes/orders/update.ts src/routes/purchaseRequests.ts` = **0**(유일 churn `3e29fe6`은 printEvents/rip/scan으로 두 파일 무관) → 재검증 skip, 캐시된 clean verdict 신뢰(32회차 codify 실증).
> - **🟢 컬럼-diff bridge(post-Area4 churn) = clean**: `git diff --stat bb49684..HEAD -- src/routes migrations` = **0**(Area4 이후 코드 churn 0, 마이그 0441만 = 인덱스 additive) → 신규 INSERT 컬럼 유입 0.
> - **🟢 XSS bridge(post-Area5 churn scripts) = net-new 0**: `git diff --stat 1032a43..HEAD -- src/scripts/**/*.js` = **0**(Area5 이후 script churn 0). 재감사 대상 프론트 sink 없음.
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` = 0327/0412/0416/0420(4건) 전부 이번 churn 이전분(신규 0441 고유·비중복). 0441=0437 byte-미러(`CREATE INDEX IF NOT EXISTS idx_orders_consolidate_with`) 확인.
> - **🟢 backlog↔GitHub sync**: owner 신규 close/머지 0(done=163·rejected=3 유지) — **직전 3사이클(30→32회차) owner 전체 close 0** = close-pending 적체 4건(#479/#480/#481/#483). #422 디버전스: HEAD=origin/main 동기(`1032a43`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — close-pending 캐시 codify(Area 6, 32회차)**: line 281("open≠unfixed")이 매 Area6마다 fixed-in-tree 픽스를 형제완전성 재검증 대상으로 등록하라 했으나, owner close 지연으로 적체되면 **파일 churn 0인 픽스를 매 사이클 재grep하는 낭비** → "verified-once + 파일 unchanged = trust" 규칙 추가(픽스 파일 HEAD SHA 기록 → 다음 사이클 `git log <sha>..HEAD -- <file>`=0이면 skip). staleness 통지 = 2사이클+ OPEN 잔류 + owner close 0이면 "⏳ close-pending 적체 N건"으로 집계해 owner batch-close 유도(개별 재검증보다 효율). #473(closed≠fixed)/line 281(open≠unfixed)의 효율 보완(정확성 유지, 낭비 제거).
> - 신규 이슈 0건(net-new 0, clean cycle), 자동수정 0건, SKILL 강화 1건(close-pending 캐시), done-sync(new=11 유지·fixed-in-tree 4/미픽스 7 분리·close-pending 적체 4건 집계), **신선 각도 — done-sync 양방향 전수 재grep(#479/#480/#481/#483 fixed-in-tree·#473~478/#482 미픽스 확정) + churn-0 형제완전성 캐시 + 컬럼/XSS bridge/마이그중복 전부 clean. 코드수정 0(tsc/build 무관).**
>
> **Area 5 보안 + 인프라 (2026-07-04T22:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→bb49684` force-update, **HEAD=origin/main `bb49684` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 5 **28회차** — 직전 Area5(`f7e38ad`, 07-03T22:00, #481) 이후 코드 churn(routes/scripts) = **`3e29fe6 fix(print-events)`**(printEvents.ts+31·rip.ts·scan.ts = agent/webhook write-path: E-prefix resolveCard + changed_by 1→NULL) + `4fa8713`(#481 in-cycle 픽스, Area6 31회차 형제완전성 재검증 완료) + docs만. **프론트 script churn 0**(XSS bridge net-new 0). **신선 각도 = 유일 신규 코드 churn = agent 무인증 webhook write-path(printEvents/rip/scan)에 보안 렌즈(SQLi·약한인증·mass-assignment) + IDOR 형제-비대칭 자동스캔(#452, churn 무관 전수).**
> - **🟢 agent webhook 인증(agentKeyMiddleware) = 안전(하드코딩 fallback 0)**: printEvents `/file-map`·`/`·`/heartbeat`·`/batch`(`:211/245/405/469`) 전부 `agentKeyMiddleware` — `auth.ts:80` `expectedKey = c.env.AGENT_API_KEY`(미설정 시 500, env-only), `X-Agent-Key` 헤더 직접 등가비교, **시크릿 리터럴 fallback 없음**(HTTPS 상 정적 공유키라 timing-safe 불요). GET 조회는 `authMiddleware`(JWT). #338 필수 grep(`c.env.X || '리터럴'`) = **`fax.ts:43 BAROBILL_FTP_PASSWORD || ''`(빈문자열=백도어 아님, 미설정 시 인증실패로 귀결) 1건뿐** — 진짜 하드코딩 시크릿 0.
> - **🟢 resolveCard/file-map(신규 churn) SQLi·ReDoS = clean**: E-prefix 매칭 regex `/^((?:E\d+-)?\d{8}-\d{3})-(\d{3})/`·`/((?:E\d+-)?\d{8}-\d{3})/` = 선형(백트래킹 폭발 없음, ReDoS 무). print_file_map 조회 전부 `?` 바인드, `file_name LIKE ? ESCAPE '\\'`의 패턴은 `likeNoExt.replace(/[\\%_]/g, ch=>'\\'+ch)`로 `\ % _` 이스케이프(LIKE 인젝션 무). agent payload는 `.bind()` 파라미터화(SQLi 0). cross-entity write(`deriveCardEntityId ||1`)는 에이전트=전역이라 정당(IDOR 아님).
> - **🟢 IDOR 형제-비대칭 자동스캔(#452, 코드베이스 전수) = net-new 0**: entity_id 보유 테이블의 bare `WHERE id=?`/`client_id=?` mutate 전수 → 매치 전량 known-open 또는 FP. **known-open**: `clients.ts:1171/1194` client_accounts PATCH/DELETE = **#473**(client_accounts는 0264 entity_id 有=진짜 IDOR, open 유지). **FP 배제**: ① `messageTemplates.ts:85/105` = mutate **앞** `#452` read-back 404 게이트(`SELECT id ... WHERE id=?${ef.clause}`)=블록내 격리(FP클래스ⓐ) ② `bank.ts:215` 계좌 비활성화 = #437 DELETE 핸들러 선행 lookup entityFilter 게이트 ③ `storageZones.ts:298` bounds PUT = `69c525c` 문서화된 ADMIN cross-entity 의도(26회차 codify) ④ `migration.ts` = admin 마이그 스크립트 ⑤ `portal.ts` = 고객포털 self-service(본인 세션 client_accounts) ⑥ `aiInsights.ts:156` = `calculate-all` 벌크 분석 재계산(단건 IDOR 아님·80청크 batch·멱등 파생필드) ⑦ **`clients.ts:1002/1049` = clients 전역 마스터(entity_id 컬럼 부재)의 bare = 정상**(신규 FP-refinement, 아래 codify).
> - **🟢 XSS bridge(post-churn scripts) = net-new 0**: `git diff --stat f7e38ad..HEAD -- src/scripts/**/*.js` = **변경 0**(churn이 백엔드 라우트 4파일+docs뿐). 재감사 대상 프론트 innerHTML sink 없음.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 11건**(`list_issues(OPEN,auto-improve)` totalCount=11: #473~#483) = 직전 Area4 stats `new=11` **정합**. 본 Area5 신규 이슈 0(clean cycle) → new 11 유지. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`bb49684`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — IDOR 자동스캔 FP클래스 ⑤ "entity_id 없는 전역 마스터 테이블" codify(Area 5, 28회차)**: 자동스캔이 `clients` PUT(`:1002`)·soft-DELETE(`:1049`)를 bare로 플래그하나 **`clients` 테이블 자체는 entity_id 부재**(`grep clients migrations | entity_id`=0) → 거래처 마스터는 전 법인 공유 전역(테넌시 격리는 orders/payments 거래 테이블에 적용). 함정 = clients.ts가 entityFilter를 대량 사용해 "entity-scoped 파일"로 오인 쉬우나, 그 `entityFilter(c,'o')`/`(c,'p')`는 전부 JOIN된 거래 서브쿼리·형제 자식(client_notes/client_accounts=entity_id 有)에 걸린 것. **판별 = 플래그된 mutate 대상 테이블 `<T>`가 migrations에 entity_id를 실제 보유하는지 직접 grep(파일이 entityFilter 쓴다는 사실≠그 테이블이 entity-scoped)**. FP클래스 ②(entity_id 부재 자식=부모귀속)의 부모/마스터 버전.
> - 신규 이슈 0건(clean cycle, net-new 0), 자동수정 0건, SKILL 강화 1건(IDOR 자동스캔 전역마스터 FP클래스), done-sync(new=11 유지·11=11 정합), **신선 각도 — 유일 신규 churn = agent webhook write-path(3e29fe6 printEvents/rip/scan)에 보안 렌즈 + IDOR 형제-비대칭 자동스캔. agentKey 인증 안전(fallback 0)·resolveCard SQLi/ReDoS clean·시크릿 하드코딩 0(fax `||''` FP)·IDOR 매치 전량 known-open(#473)/FP(clients 전역마스터·블록게이트·문서화의도). tsc/build 무관(코드수정 0).**
>
> **Area 4 데이터 정합성 (2026-07-04T18:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→086cf03` force-update, **HEAD=origin/main `086cf03` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 4 **28회차** — 직전 Area4(`c6dbb2a`, 07-03T18:00, #480) 이후 코드 churn = **`3e29fe6 fix(print-events)`**(E-prefix resolveCard + changed_by=1→NULL FK) + #480/#481 in-cycle 픽스뿐, **신규 마이그 0**. **신선 각도 = #477 churn-트리거 재스캔을 아직 미분석된 신규 포인터 `orders.consolidate_with_order_id`(0438)에 집중 + 형제 컬럼(0437 merged_into_id)의 인덱스 유무 대조.**
> - **🟡 신규 이슈 #483 (improvement, small, 자동수정) — orders.consolidate_with_order_id 인덱스 누락(0437 형제 미러)**: 0438이 접수시점 합배송 예약 포인터 `orders.consolidate_with_order_id`를 **인덱스 없이 ALTER ADD**(`grep consolidate_with_order_id migrations` = ALTER 1줄뿐). 형제 0437은 동종 컬럼 `shipments.merged_into_id`에 `idx_shipments_merged_into` 생성. orders는 client_id/status/order_number/delivery_date/entity_id 등 다수 인덱싱인데 이 컬럼만 누락. **hot-path 3곳 미인덱스 orders 풀스캔**: ① `/daily` 상관 서브쿼리(`shipments.ts:155-160`)가 일별 주문 행마다 `consolidate_partner_pending_date` 배지 산출(`OR o2.consolidate_with_order_id = COALESCE(...)`) = `일별주문수 × orders전체` 스캔(핵심 출고페이지 빈번 로드) ② `shipmentHelper.ts:151` applyConsolidationIntents 출고확정마다 파트너 조회 ③ `shipments.ts:422` unmerge. **자동수정 완료** — `migrations/0441_orders_consolidate_with_index.sql` = `CREATE INDEX IF NOT EXISTS idx_orders_consolidate_with ON orders(consolidate_with_order_id)`(0437 byte-미러). migration-check 통과(orders 실재+컬럼 실재 0438+index-only NOT NULL/DEFAULT 무관), `npm run verify` tsc0/build0. owner 액션=다음 `db:migrate:prod` 적용. 정확성 무결(순수 효율 보완, 회귀 위험 0).
> - **🟢 하드삭제 dangling(consolidate_with_order_id) = read-path status 가드로 무해(FP, 보고 안 함)**: 비-FK라 root 주문 하드삭제(`core.ts:571-613` batch, cross-order 포인터 정리는 `:608 rework_order_id`·`:609 reference_order_id`만) 시 자식 `consolidate_with_order_id` dangling 가능(#477 클래스). **그러나 모든 read-path가 status 가드**: shipmentHelper 파트너(`:154 NOT IN CANCELLED/DELETED`)·/daily 배지 서브쿼리(`:159` 동일)가 삭제/취소 root 제외 → 자식은 살아있는 형제끼리 계속 consolidate(의도 유지), 삭제 root 조용히 드롭. write(create.ts:50·update.ts:63)는 root 해소 시 `status NOT IN (CANCELLED,DELETED)` 유효검증. **#477(merged_into_id)과 결정적 차이 = consolidate_with_order_id는 write-redirect 부재**(merged는 정본을 대표로 리다이렉트해 dangling 시 무음 소실, consolidate는 read 시 그룹핑만) → dangling이 데이터 손상 없이 무해. FP 배제(read-path 완전 가드 + no-redirect).
> - **🟢 신규 마이그 0 = 컬럼존재성/CHECK/FK 신규 유입 없음**: 직전 Area4(c6dbb2a) 이후 `git diff --stat c6dbb2a..HEAD -- migrations` = **0**(최신 0440 storage_zone_layout까지 직전 사이클 분석 완료). 유일 코드 churn `3e29fe6`(printEvents/rip/scan)은 `card_status_history.changed_by` 리터럴→NULL·resolveCard regex 확장 = SELECT/UPDATE 컬럼 무-신규, 데이터정합 렌즈서 신규 FK/포인터 도입 0(Area1 27회차 형제완전성 검증 완료 재확인).
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 10건**(`list_issues(OPEN,auto-improve)` totalCount=10: #473~#482) = 직전 Area3 stats `new=10` **정합**. 본 Area4 #483 추가 → **new 10→11**(#483=자동수정, close-pending owner). owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`086cf03`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 인덱스 누락 형제-완전성 standing scan codify(Area 4 #483, 28회차)**: #477 재스캔(신규 `*_id` 포인터의 부모-제거 정리)에 **인덱스 유무 형제 대조** 축 추가 — 한 feature가 두 테이블에 유사 포인터 컬럼을 동시 도입하면(0437 shipments.merged_into_id ↔ 0438 orders.consolidate_with_order_id) 한쪽만 인덱스를 받기 쉬움(0437은 CREATE INDEX 동반, 0438은 ALTER만). **레시피**: `git diff <last>..HEAD -- migrations`에서 신규 `ADD COLUMN <x>_id`/`<x>_order_id` 발견 시 → 그 컬럼이 `= ?`/`IN`/`IS NOT NULL`로 조회되는 hot-path(`grep -rn "<col>" src/routes src/utils`)가 있나 → 있으면 `grep -rn "INDEX.*<col>\|<col>.*INDEX" migrations` = 0이면 **인덱스 누락**. 특히 **상관 서브쿼리(SELECT 리스트 내 스칼라 서브쿼리)에서 `= col`로 쓰이면** 부모 행마다 스캔이라 우선순위 高. **자동수정 = 안전**(인덱스=스키마변경 금지 명시적 예외, `CREATE INDEX IF NOT EXISTS` additive·멱등, 형제 마이그가 byte-패턴 제공) → migration-check(테이블/컬럼 존재) + verify 후 커밋, owner는 배포만. **FP 배제**: (a) 저-카디널리티·소규모 테이블(스캔 무의미) (b) 이미 복합 인덱스가 그 컬럼을 선두로 커버 (c) write-only 컬럼(조회 안 됨).
> - 신규 이슈 1건(#483 자동수정), **자동수정 1건(마이그 0441 인덱스)**, SKILL 강화 1건(인덱스 누락 형제-완전성), done-sync(new 10→11), **신선 각도 — #477 재스캔을 미분석 신규 포인터 orders.consolidate_with_order_id(0438)에 집중 + 0437 형제 인덱스 대조. #483=인덱스 누락 자동수정(0441, /daily 상관 서브쿼리 풀스캔). 하드삭제 dangling은 read-path status 가드+no-redirect로 무해(FP). 신규 마이그 0·컬럼존재성 clean·tsc/build 0.**
>
> **Area 3 UX/기능 감사 (2026-07-04T14:00):**
> - **방법**: `npm ci`(node_modules 0→65, #439) 후 git fetch(origin `c66bfc1→ffb1509` force-update, **HEAD=origin/main `ffb1509` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 3 **27회차** — 직전 Area3(`2efd34d`, 07-03T14:00) 이후 owner churn = **storage-zones 독립 배치도 확장**(`storageZones.js` +383·`storageZones.ts` +61 = 배치도 편집/재고모달/구역실사 진입) + **equipment.js −89**(구 facility 구역재고 코드[showZoneInventory/eqStartZoneCount] 제거, storage-zones로 이관) + pack.js #479 자동수정(`b274bb1`). **신선 각도 = 최대 net-new(storageZones.js +383 배치도 UX)에 dead-button/getElementById/전역헬퍼계약(#426)/빈상태/round-trip 집중 + 전체 dead-button 정규화 diff 재수행.**
> - **🟢 storageZones 배치도 신규 UX = 전부 clean**: ① **axios-route 존재성**: storageZones.js 13 axios 경로 전수 백엔드 대조 — list(`/`)·PUT/POST/DELETE`/:id`·`/layout-data`·PUT`/:id/bounds`·GET`/:id/stock`·`/background`·`/background-image`·POST`/inventory-counts`(inventoryCount.ts:67 storage_zone_id 수용) 전부 실재. dead-button 0. ② **getElementById 전수(31 id)**: btnSz*/szLayout*/szTab*/szPanel*/zoneModal*/szZoneInv* 전부 storageZones.ts 템플릿 실재(entityFilter는 :16), silent-fail 0. ③ **showConfirm(#426)**: 3콜(`:216`/`:441`/`:573`) 전부 options 객체 정확전달(2번째 인자 함수/문자열 오용 0). ④ **빈상태**: 리스트 noZonesMsg(`:65`)·배치도 szLayoutEmpty(`:299`)·미배치 트레이(`:348`)·재고모달 "귀속 재고 없음"(`:494`) 전부 처리, 로딩 skeleton(`:19`). ⑤ **재고실사 리다이렉트**: `szStartZoneCount`→`/inventory?openCount=<id>` 타깃이 inventoryCount.js:352서 처리 = dead-redirect 아님.
> - **🟢 equipment.js −89 삭제 완결 = orphan onclick 0**: 제거된 3함수(showZoneInventory/closeZoneInvModal/eqStartZoneCount)+3 id(zoneInvModal/Body/Title)가 equipment.ts 템플릿에 **잔존 참조 0**(HTML↔JS silent-fail 없음). 잔존 `/api/facility/*` 10경로(layout-data·zones CRUD·bounds·background)는 facility.ts에 전부 실재(장비 배치는 facility 도면 유지, 창고구역만 storage-zones 이관 = 의도적 분리).
> - **🟢 items.storage_zone_id round-trip = 무결**: storageZones 리스트 "품목 수" 컬럼(`item_count` = `items.storage_zone_id` 카운트)이 항상-0 오표시 의심했으나, 품목편집 모달이 storage_zone_id 로드(`items/modals.js:76`)·전송(`:188`) + items.ts UPDATE가 read(`:1108` #461 법인검증)·bind(`:1157`) = 정상 채워짐. 배치도 `inv_item_count`(inventory.storage_zone_id 실재고)와 데이터소스 상이하나 의도적(카탈로그 귀속 vs 실재고 존재). **부차(무해)**: `PATCH /assign-items`(storageZones.ts:483, 벌크 배정)는 프론트 호출처 0 = orphan 라우트(#334 dead-code 저위험, 품목편집으로 대체 가능하므로 컬럼 오표시 아님).
> - **🟢 전체 dead-button 정규화 diff 재수행 = net-new 0(84후보 전량 서브라우터 FP)**: 프론트 `src/scripts/**/*.js` mutation axios(415경로) × 백엔드 마운트×라우트(803, index.tsx import 해석) 정규화 diff → **84 unmatched 후보 전량 FP** — ledger/orders/payroll/purchase-orders/tax-invoices/cards/hometax 등 **서브라우터(`.route()` 마운트) 미전개**로 인한 가짜 unmatched. 정적 후행세그먼트 전수 2차 grep(`src/routes` 재귀) → `/integrity-fix`(ar-receivables)·`/rates/copy`(payroll/settings:89)·`/direct`(taxInvoices/issue)·`/bulk-ship`(orders/queries)·`/load-defaults`(settings:326) 등 전부 서브파일 실재. 진짜 dead-button = 기존 #475(shadow)·#476(clients credit/toggle) 2건뿐, **신규 0**.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 10건**(`list_issues(OPEN,auto-improve)` totalCount=10: #473~#482) = 직전 Area2 stats `new=10` **정합**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`ffb1509`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — dead-button 정규화 diff의 서브라우터 미전개 false-alarm 함정 codify(Area 3 자동화 매처 함정 2, 27회차)**: axios→route 존재성 스캔을 top-level 라우터 파일만 파싱하면 배럴 라우터(`.route('/sub', x)`로 서브파일 분할한 ledger/orders/payroll/…)의 실경로가 0개 수집돼 그 prefix 전량 가짜 unmatched. line 133(`'/*'` 주석strip 오발)과 방향 동일(false-alarm)하나 원인 다름. 회피=(a)`index.ts`의 `.route()` 재귀 follow로 sub-file prefix 상속 자동화 또는 (b)정적 후행세그먼트 `src/routes` 전수 2차 grep. 결정적 신호=후보가 서브라우터 prefix에 몰리면 미전개 FP.
> - 신규 이슈 0건(net-new 0, 클린 사이클), 자동수정 0건, SKILL 강화 1건(dead-button diff 서브라우터 FP 함정), done-sync(new=10 유지), **신선 각도 — storageZones.js +383 배치도 UX에 dead-button/getElementById/#426/빈상태/round-trip 집중 + 전체 dead-button 정규화 diff 재수행. 배치도 라우트 13종 실재·getElementById 31 실재·showConfirm 정확·빈상태 처리·재고실사 리다이렉트 정합·equipment 삭제 완결(orphan 0)·items storage_zone round-trip 무결. dead-button diff 84후보 전량 서브라우터 FP → net-new 0.**
>
> **Area 2 코드 품질 심층 분석 (2026-07-04T10:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→48b30c3` force-update, **HEAD=origin/main `48b30c3` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 2 **30회차** — 직전 Area2(`42b8ad9`, 07-03T10:00) 이후 owner churn = 출고 v2(`cd492fd`)·storage-zones 독립 배치도(`697902d`/`0440`)·전체 코드리뷰 P1/P2(`7e4b6bf`)·print-events fix(`3e29fe6`) + 다수 하드닝(cardExpenses/kakao/attendance/hr IDOR·청크). **신선 각도 = 최대 net-new(storageZones.ts +243)에 스키마↔로직 스코프 불일치 렌즈.**
> - **🟢 타입 검증 = clean**: `npx tsc --noEmit` **exit 0**.
> - **🔴 신규 이슈 #482 (bug, small, issue-only) — 창고구역 유일성 앱검사(법인별) vs DB 제약(전역) 불일치**: `storageZones.ts` POST(`:309`)·PUT(`:367`)가 중복검사를 `WHERE zone_name=? AND entity_id=?`(`:332`/`:393`) **법인별로만** 하는데 `storage_zones.zone_name`은 `0098:7` 전역 `TEXT NOT NULL UNIQUE`, `zone_code`는 `0098:8` 전역 `TEXT UNIQUE`. `entity_id`는 0232서 나중 ALTER(주석 "창고는 법인 소속" = 법인별 독립 의도)인데 컬럼 UNIQUE는 전역 잔존. **실패 시나리오**: 법인 B ADMIN이 법인 A가 이미 쓴 표준 구역명(자재창고·완제품창고 등) 생성 → 법인별 앱검사 통과(B엔 없음) → INSERT → 전역 `zone_name UNIQUE` 위반 throw → 핸들러에 UNIQUE-catch 없어 generic **500 `서버 오류`**(형제 `leaves.ts:534`는 UNIQUE→409 보유). zone_code는 앱검사 아예 없어 동일 500. `697902d`(창고 배치도 독립·facility 매핑 폐기)의 법인별 독립 취지를 전역 UNIQUE가 구조적으로 차단. 도달성 LIVE(/storage-zones ADMIN 생성/수정 모달). 수정=신규 마이그로 복합 UNIQUE(entity_id, zone_name)/(entity_id, zone_code) 재빌드(migration-check) + 병행 catch에 UNIQUE→409.
> - **🟢 IN절 D1 바인드(#458) 자동스캔 = net-new 0**: churn IN절 전수 — shipments daily enrichment(`:194`)는 **이미 80청크**(`:187` `#458` 주석), cardExpenses 분류/결의 UPDATE는 **이번 churn서 80청크 신규 적용**(owner `#409`), kakao dedup(`allSids`)도 80청크, po-receive `pack_size`/inventory 룩업(`:143/150`)은 단일 PO 품목 bounded(FP), storageZones assign-items(`:499`)는 user-selected 품목 multi-select(bounded, FP). 신규 데이터-스케일 미청크 IN절 0.
> - **🟢 컬럼 존재성(storage_zones 신규 스키마) = clean**: POST/PUT 참조 컬럼 전수 실재 — zone_name/zone_code/description/manager_id/sort_order/is_active(0098)·is_default/entity_id(0232)·facility_zone_id(0391)·bounds/color(0440). po-receive `pack_size`(0395)·`inventory.quantity` base 단위 환산(#462) 반영 확인.
> - **🟢 entity_id INSERT 누락 = net-new 0**: churn 신규 INSERT — storage_zones(`:346` entity_id 스탬프)·shipment_checks(child, entity_id 컬럼 부재=Area4 검증). 나머지 churn은 UPDATE/SELECT.
> - **🟢 authMiddleware/IDOR churn = 전부 owner 하드닝**: attendance(`reqEntity` empEntityMap 가드)·hr(`efAtt` entityFilter)·kakao(bulk dedup)·cardExpenses(청크) 전부 이번 churn서 owner가 IDOR/성능 픽스. 신규 무-auth mutate 0.
> - **🟢 print-events fix(`3e29fe6`) N+1/타입 = clean**: E-prefix `resolveCard` regex(`(?:E\d+-)?\d{8}-\d{3}`) 확장·확장자 LIKE fallback·changed_by NULL+try/catch 격리 — Area1 27회차 형제완전성 검증 완료, Area2 렌즈서도 추가 결함 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 9건**(`list_issues(OPEN,auto-improve)` totalCount=9: #473~#481) = 직전 Area6 stats `new=9` **정합**. 본 Area2 #482 추가 → **new 9→10**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`48b30c3`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 앱 유일성검사 스코프 vs DB 제약 스코프 불일치 standing scan codify(Area 2 #482, 30회차)**: entity_id 격리를 **앱 로직**(`WHERE ... AND entity_id=?` 중복검사, 법인별 is_default 해제)엔 반영했으나 **DB 제약**(컬럼레벨 전역 UNIQUE)은 멀티테넌시 이전 원본이라 전역으로 남는 sibling-mismatch — 컬럼존재성·INSERT는 clean이라 기존 스캔 사각. **레시피**: ① `grep -rnE "ADD COLUMN entity_id" migrations`로 나중-추가된 테넌시 컬럼 테이블 격리 → ② 그 테이블 CREATE(`grep -A20 "CREATE TABLE.*<t>"`)에 컬럼레벨 `UNIQUE`가 있는 텍스트/코드 유일성 컬럼(name/code) 존재? → ③ 라우트의 중복검사가 `AND entity_id=?`로 법인범위인데 제약은 복합 UNIQUE(entity_id, col) 아닌 전역이면 **cross-entity 표준값 재사용 시 500**. **결정적 신호 = 앱검사는 entity_id 포함인데 마이그 grep상 UNIQUE는 단일컬럼**. FP 배제: (a) 진짜 전역 유일 의도(예: 사업자번호)면 정상 (b) 이미 복합 UNIQUE(entity_id, col) 인덱스 존재 (c) 단일법인 전용 테이블. 픽스=복합 UNIQUE 재빌드(migration-check)이라 **issue-only**(스키마 변경). 병행 완화=핸들러 UNIQUE-catch(`leaves.ts:534` 형제)로 500→409.
> - 신규 이슈 1건(#482 issue-only), 자동수정 0(DB 제약 변경=스키마 자동수정 금지), SKILL 강화 1건(앱-vs-DB 유일성 스코프 불일치), done-sync(new 9→10), **신선 각도 — storageZones.ts +243 최대 net-new에 스키마↔로직 스코프 불일치 렌즈. #482=zone_name/code 전역 UNIQUE가 법인별 중복검사와 배치→타법인 표준명 생성 500. IN절 청크(cardExpenses/kakao 신규 적용)·컬럼존재성·entity_id INSERT·auth 하드닝·print-events 전부 clean. tsc exit0.**
>
> **Area 1 프로덕션 헬스 (2026-07-04T06:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439) 후 git fetch(origin `c66bfc1→0db3b88` force-update, **HEAD=origin/main `0db3b88` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 1 **27회차** — egress 차단(prod 직접 fetch·Playwright 도달 불가)이라 CI/E2E는 GitHub Actions API로 검증. 직전 Area6(`140414e`, 07-04T02:00) 이후 owner churn = **`3e29fe6` fix(print-events)**(출력완료 자동전환 복구: E-prefix `resolveCard` 법인채번 접두어 매칭 + `card_status_history.changed_by` agent경로 하드코딩 `1`→`NULL` 7곳[printEvents 3·rip 4]) + docs. **신선 각도 = 유일 코드churn `3e29fe6`의 형제 완전성 재검증(#377 클래스) — 하드코딩 audit-FK가 다른 agent 경로에도 잔존하는지.**
> - **🟢 CI 헬스 = green(12/12, 무-failure)**: `deploy.yml` main 최근 12런 **전부 success**(직전 Area1 26회차의 CF-internal transient 1건보다 개선). `3e29fe6`(print-events fix)·`0db3b88`(docs) 포함 모두 배포 완료.
> - **🟢 `3e29fe6` in-cycle fix 형제 완전성 = clean**: agent 무인증 write-path(printEvents CAPS 웹훅·rip RIP ACK)의 `card_status_history.changed_by` 전수 대조 — printEvents(`:169/528/543` **NULL**)·rip(`:1428/1481/1637` **null**, `:1733` `user?.id||null`)·cards/lifecycle(`:1212` `user?.id||null`, user 경로)·scan.ts(이력 INSERT 0) = **하드코딩 리터럴 `1` 잔존 0**. FK는 `0046 ON DELETE SET NULL`+nullable라 NULL 안전. `3e29fe6`이 놓친 형제 없음.
> - **🟢 유사 audit-FK 광역 스캔(신규 클래스) = 실버그 0**: `scan.ts:247/281`이 `inventory_transactions.handled_by`(FK `REFERENCES users(id)` = 0003/0075/0134)에 `user?.id || 1` fallback이나, **scan.ts는 `:13 scanRouter.use('/*', authMiddleware)`** → `user`가 항상 실재 유저(JWT 검증) → `|| 1` **도달 불가 방어코드(FP)**. rip maintenance_logs(`:1063` `user?.id||null` 등)도 user 경로. printEvents/rip/cron/webhooks 무인증 경로에 하드코딩 audit-FK 리터럴 = 0.
> - **🟢 DROP/RENAME 마이그 write-path standing scan(#430) = net-new 0**: 신규 0439/0440 전수 `grep DROP (TABLE\|COLUMN)\|RENAME` = **0건**(0439 shipment_checks CREATE·0440 storage_zone_layout ALTER ADD, 전부 additive).
> - **🟢 라우트 제거 → smoke 프로브 stale(#429 d축) = clean**: `git diff 7a27e74..HEAD -- src/routes` 제거 라우트 = facility `/locations`·`zones/:id/inventory`(storage-zones 이관, `0440`) + `bulk-ship`(**relocation** — `queries.ts:240` 실재, 프론트 `shipments.js:815`/`orders.js` 라이브). `grep scripts/smoke.cjs` = **stale 프로브 0** + 제거된 facility 경로 프론트 axios 참조 0 = dead-button 0. CI green이 이를 방증(smoke red 아님).
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` = 0327/0412/0416/0420(4건) 전부 이번 churn 이전분(신규 0439/0440 고유 = 비중복).
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 9건**(`list_issues(OPEN,auto-improve)` totalCount=9: #473~#481) = 직전 Area6 stats `new=9` **정합**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`0db3b88`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 무인증 agent write-path 하드코딩 audit-FK(`changed_by=1`) → prod FK 위반·smoke 맹점 3번째 축 codify(Area 1 #430 확장, 32회차)**: 이번 사이클 owner 픽스 `3e29fe6`이 복구한 **실제 prod 장애**(출력완료→PRINT_DONE 자동전환 중단)의 근본원인을 standing scan으로 승격 — authMiddleware 없는 agent/webhook write-path(printEvents·rip)가 audit 컬럼(`*_by`)에 리터럴 `1`을 하드코딩하면 prod user id=1 부재 시 FK throw → 이력 INSERT가 상태전이 batch 동반 롤백 → 동기화 무음 중단. GET 프로브·로그인 smoke가 구조적으로 못 봄(smoke 맹점 1=프론트실행·2=DROP write-path에 이은 3=agent-path FK). 레시피: 무-authMiddleware 라우터 격리 → audit `*_by` bind가 리터럴/`user?.id||리터럴`인지 → 그 컬럼 FK 대조. FP=authMiddleware 적용(user 항상 실재→`||1` 도달불가, scan.ts 실증)·FK 미선언·NULL 바인드.
> - 자동수정 0건(픽스 이미 착륙·형제 완전), 신규 이슈 0건(net-new 0, 클린 사이클), SKILL 강화 1건(agent-path audit-FK smoke 맹점 3번째 축), done-sync(new=9 유지), **신선 각도 — 유일 코드churn `3e29fe6`(print-events prod 장애 복구)의 형제 완전성 재검증. agent 경로 card_status_history changed_by 하드코딩 전멸(전부 NULL/user?.id), scan.ts handled_by=user?.id||1은 authMiddleware로 FP. CI 12/12 green·DROP 0·route-purge relocation/이관·마이그중복 신규 0·sync 9=9 전부 clean. #422 디버전스 0.**
>
> **Area 6 자기 진화 (2026-07-04T02:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→62a3556` force-update, `git checkout -B main` 불요 — HEAD=origin/main `62a3556` **0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 6 **31회차** — 직전 Area5(`f7e38ad`, 07-03T22:00, #481) 이후 churn = **#481 보안픽스(`4fa8713`, worktree Fable 5) + #480 데이터정합 픽스(`99c1b94`, Area4↔5 사이 착륙) + docs만**(신규 마이그 0·프론트 script churn 0). **신선 각도 = churn 윈도의 유일 코드 변경이 두 in-cycle 픽스(#480/#481) → #473/#377 codify상 그 픽스의 형제 완전성 재검증 + "open≠unfixed" done-sync 조정에 정확히 타격.**
> - **🟢 #481 in-cycle 보안픽스(`4fa8713`) 형제 완전성 = clean**: purchaseRequests.ts **전 핸들러 entityFilter 전수 대조** — GET`/`(:44 efList)·stats(:158)·comments GET(:193 **#481**)/POST(:223 **#481**)·csv(:260)·GET`/:id`(:316 efDetail + MANAGER self 403)·PUT(:441 efPut)·approve/reject/convert/auto-convert(:533/593/626/739 **#455**)·DELETE(:925 efDel) = **13핸들러 전부 read-back 404 게이트**. 픽스는 이슈 지목 POST뿐 아니라 **GET/:id/comments 형제도 함께 격리**(이슈 미포함, cross-tenant 사용자명 유출 차단) + GET`/:id` 단건조회 부모 read-back이 자식 comments/items/history 쿼리 선제 차단. `/:id/comments` 패턴 파일 유일 = 잔존 bare read-back 0. **7e4b6bf(#455)+4fa8713(#481)로 purchase_requests IDOR 클러스터 완전 봉인.**
> - **🟢 #480 in-cycle 정합픽스(`99c1b94`) 형제 완전성 = clean**: `DELETE FROM order_items` **3개 사이트 전수** — core.ts:599(하드삭제, `:591 DELETE shipments`가 shipment_id ON DELETE CASCADE로 shipment_checks 선제거 후 실행 = 안전, 기존)·update.ts:219(batch, `:218` shipment_checks 선정리 **신규**)·update.ts:242(standalone, `:241` shipment_checks 선정리 **신규**). 픽스가 update.ts 양 경로 모두에 order_items 삭제 前 `DELETE FROM shipment_checks WHERE order_item_id IN(...)` 추가 = 0439 RESTRICT FK throw 500 차단. **RESTRICT 자식 정리 누락 형제 0.**
> - **🟢 done-sync "open≠unfixed" 조정(#473 역방향 신규 codify)**: open auto-improve **실측 9건**(`list_issues(OPEN,auto-improve)` totalCount=9: #473~#481) = 직전 Area5 stats `new=9` **정합**. 단 그중 **3건이 fixed-in-tree**(안티패턴 grep 0 + 이슈번호 fix 커밋): #479(pack showPrompt, `b274bb1` 자동수정 — `defaultValue` 반영)·**#480**(`99c1b94`)·**#481**(`4fa8713`) → **전부 "close pending user"로 OPEN 유지**. 나머지 6건(#473 clients 포털계정 bare 잔존·#474 bank dup-check SELECT 2 잔존·#475 tax `openPrintURL` 미리네임·#476 clients credit/toggle 라우트 부재·#477 core.ts/CANCELLED merged_into_id 미정리[shipments:369/416은 unmerge 핸들러=별개]·#478 toShip LIMIT 없음) = **실제 미픽스, 정상 open**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`62a3556`)라 미push 픽스 0.
> - **🟢 컬럼-diff bridge(post-Area4 churn) = clean**: `git diff c6dbb2a..HEAD -- migrations` = **신규 마이그 0**, 코드 churn 2파일(update.ts +5 = shipment_checks DELETE 2문[#480]·purchaseRequests.ts +5 = entityFilter read-gate[#481]) 전부 **신규 INSERT 컬럼 유입 없음**(read-gate SELECT + DELETE만) → 컬럼 존재성 위험 0.
> - **🟢 XSS bridge(post-Area5 churn scripts) = net-new 0**: `git diff --stat f7e38ad..HEAD -- src/scripts/**/*.js` = **변경 0**(churn이 백엔드 라우트 2파일+docs뿐). 재감사 대상 프론트 sink 없음.
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` = 0327/0412/0416/0420(4건) 전부 **이번 churn 이전분**(신규 마이그 0=비중복). #438 규칙상 기존 prod-적용 추정 중복은 리네임이 더 위험=정리 대상 아님.
> - **🧬 SKILL 강화 1건 — done-sync "open≠unfixed" 반대 거울 codify(#473 역방향, Area 6 31회차)**: #473("closed≠fixed, 재grep")의 역방향 — issue-only 발견을 owner/worktree 세션이 사이클 사이 픽스+push하고 이슈는 close 보류(OPEN)하면 done-sync가 open만 보고 "미해결" 오판 → 다음 사이클 중복 재보고 or 형제 완전성 미검증. **레시피**: 각 OPEN 이슈의 안티패턴을 코드 grep(open도 대조) → 사라졌으면 이슈번호 fix 커밋 확인 → fixed-in-tree인데 OPEN이면 "close pending user" 분리 + 그 픽스를 #473 형제-완전성 재검증 대상 등록. FP 배제: 안티패턴 잔존이면 미픽스(정상 open). 30회차 실증(#480/#481 fixed-in-tree yet open).
> - 신규 이슈 0건(net-new 0, 클린 사이클), 자동수정 0건, SKILL 강화 1건("open≠unfixed" done-sync 거울), done-sync(new=9 유지 — 3건 fixed-in-tree/close-pending 분리 표기), **신선 각도 — churn 윈도 유일 코드변경이 두 in-cycle 픽스(#480/#481) → 형제 완전성 재검증(purchase_requests 13핸들러·order_items 3삭제사이트 전부 clean) + open≠unfixed 조정. 컬럼bridge(신규INSERT 0)·XSSbridge(script churn 0)·마이그중복(신규 0) 전부 clean. tsc exit0.**
>
> **Area 5 보안 + 인프라 (2026-07-03T22:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(`git checkout -B main origin/main`으로 **HEAD=origin/main `f7e38ad` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 5 **27회차** — 직전 Area5(`03d35f1`, 07-02T22:00) 이후 owner churn = **출고관리 v2 P1~P4**(`cd492fd`: 신규 `/pack` 모바일 검수·`0439 shipment_checks`·checklist 라우트 2종·merge/unmerge) + **storage-zones 독립 배치도**(`697902d`: `0440` bounds/color + 자체 도면 R2 + facility 매핑 폐기) + **storage-zones ADMIN 가드 완화**(`69c525c`) + **전체 코드리뷰 P1/P2 보안 픽스**(`7e4b6bf`: IDOR·확정잠금·바로빌 관측성·IN절 청크). **신선 각도 = 최대 churn(신규 /pack·checklist mutate·배치도 렌더)에 IDOR·XSS 자동스캔 + `7e4b6bf` in-cycle 보안픽스 커밋의 형제 완전성 재검증(#377/#452 확장).**
> - **🔴 신규 이슈 #481 (bug, small, security, LOW, issue-only) — 발주요청 댓글 IDOR = `7e4b6bf`(#455 픽스)의 형제 잔존**: `purchaseRequests.ts:220` `POST /:id/comments`가 bare `WHERE id=?`. 같은 커밋이 `// #455` 주석으로 6개 형제 mutate(approve `:530`·reject `:590`·convert `:623`·auto-convert `:736`·PUT `:438 efPut`·DELETE `:922 efDel`)에 entityFilter read-back 404를 붙였으나 **댓글 POST만 누락** = #377 부분픽스 트랩의 in-cycle 보안픽스 커밋 버전. entity-scoped ADMIN/MANAGER/DESIGNER가 타법인 PR 스레드에 임의 텍스트 주입(cross-tenant 사용자명 노출+내용 오염). **XSS 아님**: 렌더 `purchaseRequests.js:238 escapeHtml(cm.content)` → LOW. 도달성 LIVE(`submitPRComment`).
> - **🟢 신규 /pack·checklist 라우트 IDOR = clean**: `GET /checklist/by-order/:orderId`(`shipments.ts:445`)·`PATCH /checklist/:shipmentId`(`:525-528`) 둘 다 `entityFilter(c,'o')`로 주문 소유 404 게이트(PATCH는 mutate batch **전** 부모 orders JOIN SELECT read-gate = 블록내 안전 패턴). `shipment_checks`는 entity_id 컬럼 부재 = shipment/order_item 귀속 자식 → bare mutate 정상. 라우터 게이트 `requireAnyPagePermission('/shipments','/pack')`로 OPERATOR(/pack 전용)가 shipments 라우터 READ 도달하나 **mutate 전부 requireRole 개별 게이트+read 전부 entityFilter** → cross-entity·escalation 무(hanjin-export `:1284`도 body.targets/자기 entity만 포맷=DB PII 유출 무). = 광역화지만 실질 무해(FP).
> - **🟢 storage-zones 배치도 XSS(신규 렌더) = clean**: `storageZones.js` 로컬 `escapeAttr`(`:7`)가 `& " ' < >` 전수 escape(속성·텍스트노드 양쪽 안전, 이름과 달리 full escape) → 배치도 박스 label(`:322 zone_name`·`:323 entity_name`)·미배치 트레이(`:354`)·목록/재고모달 free-text(zone_name/zone_code/description/manager_name/item_name) 전부 escapeAttr 적용. net-new XSS 0.
> - **🟢 storage-zones ADMIN 가드 완화(`69c525c`)·bounds PUT = 문서화된 의도 FP**: POST/PUT/DELETE가 `body.entity_id`를 ADMIN 신뢰(전 법인 CRUD)로 완화 + `PUT /:id/bounds`(`:283`) bare — 커밋이 "목록 all_entities가 ADMIN에 전 법인 노출하는 것과 대칭·`requireRole('ADMIN')` 게이트"로 명시(#368/#417 대칭). SKILL "문서화된 의도적 cross-entity mutation" FP클래스 직접 매치(형제 GET 목록 미격리+설계 주석 정책 명시). `GET /:id`만 여전히 entityFilter(`:215`) = 무해한 잔여 비대칭(minor). 보고 안 함.
> - **🟢 `7e4b6bf` #455 IDOR 클러스터 형제 완전성 = purchaseRequests·hometax 픽스 확인**: #455가 지목한 purchaseRequests approve/reject/convert/auto-convert(`:529/589/622/735`) + hometax `/:id/match`(`:503`) 전부 `entityFilter` read-back 404 반영 확인(in-tree grep) — #481(댓글) 1건만 잔존. shipments.js 인쇄/라벨 free-text(clientName/order_number/item_name/content/spec) 전수 escapeHtml(`:1603-1616`, 99 escape 콜) = XSS clean. production.js 신규 라인 미escape sink 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 8건**(`list_issues(OPEN,auto-improve)` totalCount=8: #473~#480) = 직전 Area4 stats `new=8` **정합**. 본 Area5 #481 추가 → **new 8→9**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`f7e38ad`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 부분픽스 재검증(#377/#452)을 "같은 사이클 머지된 `fix(security)` 커밋"으로 확장 (Area 5 #481, 27회차)**: #452/#473은 **closed 우산 이슈**의 서브모듈 픽스 실재를 재grep했으나, **owner가 churn 윈도에서 한 테이블의 IDOR 클러스터를 픽스하는 `fix(security)` 커밋**(`7e4b6bf`처럼 여러 핸들러에 `entityFilter`/`// #NNN` 게이트 추가)도 형제 완전성 검증 대상 — 픽스가 목록·approve·convert는 격리하고 댓글·부가 하위자원 mutate는 놓치기 쉬움(#377 부분픽스). **레시피**: `git log <last>..HEAD --oneline`에서 `fix(security)`/`IDOR` 커밋 발견 시 → 그 커밋이 게이트를 추가한 테이블(`git show <sha> | grep -oE "FROM \w+|// #[0-9]+"`)의 **모든** mutate 핸들러를 `grep -nE "(post|put|patch|delete)\(.*/:id" src/routes/<file>` 전수 → 각각 read-back에 `entityFilter`/`ef.clause`가 있는지 대조 → 없는 형제가 잔존 IDOR. FP 배제: 생성(POST `/` 스탬프)·employee-self scope·entity_id 부재 자식 테이블·블록내 상위 read-gate.
> - 신규 이슈 1건(#481 issue-only, LOW), 자동수정 0(IDOR=owner 워크플로+egress), SKILL 강화 1건(부분픽스 재검증을 in-cycle 보안픽스 커밋으로 확장), done-sync(new 8→9), **신선 각도 — 출고 v2 신규 /pack·checklist mutate·배치도 렌더에 IDOR·XSS 집중 + `7e4b6bf` 보안픽스 형제 완전성 재검증. #481=댓글 POST가 #455 픽스의 유일 잔존 형제(LOW, escape로 XSS 무). checklist 라우트 entityFilter 정합·배치도 escapeAttr full·storage-zones 완화 문서화 의도 FP·shipments 인쇄 escape 전수 clean.**
>
> **Area 4 데이터 정합성 (2026-07-03T18:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(`git checkout -B main origin/main`으로 **HEAD=origin/main `c6dbb2a` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 4 **27회차** — 직전 Area4(`3edb9dc`, 07-02T18:00, #477) 이후 owner churn = **출고관리 v2 P1~P4**(`cd492fd`: 신규 `0439_shipment_checks` 검수 체크리스트+`/pack` 모바일, full-ship hard gate, consolidation expansion) + storage-zones 독립 도면(`0440` bounds/color ALTER) + 전체 코드리뷰 P1/P2 수정(`7e4b6bf`). **신선 각도 = #477 churn-트리거 재스캔(신규 비-FK/NO-ACTION `*_id` 컬럼)을 이번 사이클 신규 마이그 0439~0440에 집중.**
> - **🔴 신규 이슈 #480 (bug, small, issue-only) — 주문 수정 경로가 신규 `shipment_checks.order_item_id`(0439 RESTRICT) 미정리 → order_items 삭제 FK throw 500**: 0439가 `FOREIGN KEY (order_item_id) REFERENCES order_items(id)`를 **ON DELETE 절 없이(NO ACTION=RESTRICT)** 선언. `shipment_checks`는 `/pack` checklist 열림 시 라인마다 생성(`shipments.ts:466`). 주문 수정 `PUT /orders/:id`(`orders/update.ts`)는 order_items를 통째 삭제(:216 batch·:237 standalone)하면서 `shipment_checks`를 정리 안 함(:187은 `cards.order_item_id`만 NULL 해제) → prod D1 FK 강제(#413)로 **`DELETE FROM order_items`가 FK 위반 throw → 주문 수정 100% 500**. **비대칭 = 삭제(core.ts)는 안전(FP)**: `:591 DELETE shipments`(shipment_id ON DELETE CASCADE)가 `:599 order_items`보다 먼저라 shipment_checks cascade 선제거 + core.ts는 `#464/#454` RESTRICT-자식(return_items:596/print_file_map:595) 명시 선정리 패턴 보유. update.ts는 shipments 미삭제라 cascade 미발동 + 선정리 패턴 미적용. 부차: 카드 보존 경로(:237)는 헤더 UPDATE·cards NULL 선커밋이 throw 시 롤백 안 됨 = **카드 부분분리 잔재**(MED). 도달성 LIVE(주문 수정 SHIPPED 포함 전상태 허용 `update.ts:51`, shipment 가드 0건).
> - **🟢 컬럼 존재성/CHECK/신규 마이그 = clean**: 0439 신규 쿼리(`shipments.ts:466-546`) 참조 컬럼 전수 실재 — shipment_checks(shipment_id/order_item_id/packed_quantity/checked_at/checked_by)·order_items(shipment_ready[0154]/sort_order/parent_item_id/item_name/specification 등) 정합. 0440은 `storage_zones` bounds/color ALTER ADD(nullable, color DEFAULT)만 = NOT NULL no-default 위험 0, FK 무. shipment_checks INSERT/UPDATE(:466/:536/:539) 컬럼셋 전부 존재.
> - **🟢 나머지 신규 비-FK 포인터 = 정리 불요(FP축, #480 severity 판별)**: 0439 `checked_by REFERENCES users(id)`(NO ACTION)는 users 하드삭제 경로 부재+audit 컬럼이라 실무 무영향(FP). `shipment_id ON DELETE CASCADE`는 자동정리. #477(0437 merged_into_id)·#480(0439 order_item_id)만 부모-제거 경로 미처리. 0440은 신규 `*_id` 포인터 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 7건**(`list_issues(OPEN,auto-improve)` totalCount=7: #473·#474·#475·#476·#477·#478·#479) = 직전 Area3 stats `new=7` **정합**. 본 Area4 #480 추가 → **new 7→8**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`c6dbb2a`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — churn-트리거 재스캔(#477)을 "order_items 통째-교체하는 주문 수정 경로"로 확장 + CASCADE-부모 비대칭 판별축 codify(Area 4 #480, 27회차)**: #477 재스캔 레시피는 신규 `*_id` 컬럼의 부모테이블 **DELETE/CANCELLED 핸들러**만 봤으나, `order_items`처럼 **주문 수정이 자식을 통째 삭제 후 재작성하는 replace-path(`update.ts:216/237`)** 도 별개 RESTRICT-차단 지점. 게다가 **CASCADE-부모 비대칭** = 신규 자식이 `shipment_id ON DELETE CASCADE`(shipments)와 `order_item_id NO ACTION`(order_items) 이중 FK를 가지면, 삭제 경로(부모 shipments를 지워 cascade 발동=안전)와 수정 경로(shipments 미삭제로 cascade 미발동, order_items만 삭제=RESTRICT 500)의 안전성이 갈림 → "삭제 clean"이 "수정 clean"을 함의하지 않음. **재스캔 확장 레시피**: `git diff <last>..HEAD -- migrations`에서 `REFERENCES order_items|shipments`(ON DELETE 절 없음) 신규 자식 발견 시 → `grep -rn "DELETE FROM order_items WHERE order_id" src/routes`(하드삭제 core.ts + **수정 update.ts** 모두) 전수 → 각 사이트가 그 신규 자식을 order_items 삭제 前 정리하는지 확인. FP축(정리 불요) = 신규 자식이 대상 부모 CASCADE로 이미 선제거되는 경로(core.ts처럼 부모 shipments를 먼저 삭제)뿐.
> - 신규 이슈 1건(#480 issue-only), 자동수정 0(파괴적 삭제/mutation batch=자동수정 금지), SKILL 강화 1건(재스캔 replace-path 확장+CASCADE-부모 비대칭), done-sync(new 7→8), **신선 각도 — 출고 v2 신규 0439 shipment_checks의 order_item_id RESTRICT FK가 update.ts 주문수정 경로에서 미정리(core.ts 삭제 경로는 cascade 선제거로 안전한 비대칭). 컬럼존재성·0440 ALTER·checked_by FP 전부 clean. tsc/build 미실행(정적 분석·이슈 전용, 코드 변경 0).**
>
> **Area 3 UX/기능 감사 (2026-07-03T14:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(`git checkout -B main origin/main`으로 **HEAD=origin/main `2efd34d` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 3 **26회차** — 직전 Area3(`f044e2f`, 07-02T14:00) 이후 owner churn = **출고관리 v2 P1~P4 대규모 기능**(`cd492fd`: 신규 `/pack` 모바일 검수페이지 `pack.ts`/`pack.js`+210, full-ship hard gate, consolidation expansion, shipments.js +578·shipments.ts +63) + production 출력이력 필터 기본 7일+빈결과 탈출버튼(`4170ff6`) + storage-zones ADMIN 가드 완화(`69c525c`). **신선 각도 = 최대 churn(신규 /pack 페이지 + 배송 v2)에 dead-button/axios-route + getElementById + 전역헬퍼계약(showPrompt/showConfirm #426) 스캔 집중.**
> - **🟢 신규 배송 v2 axios-route 존재성 = clean(net-new 0)**: /pack·shipments.js·production.js·orderForm 신규 axios 경로 전수 백엔드 대조 — `/api/shipments/checklist/by-order/:id`(shipments.ts:439)·`PATCH /checklist/:id`(:517)·`/consolidation-candidates`(:231)·`/merge`(:332)·`/unmerge`(:396)·`/hanjin-export`(:1284)·`orders/in-transit`(core.ts:353)·`bulk-ship`(queries.ts:239) **전부 실재**. /pack 페이지 마운트(`index.tsx:463` `requirePagePermission('/pack')`)+권한등록(0439 `permission_pages VALUES('/pack',...)`+role MANAGER/OPERATOR)+메뉴(menu.ts:25) 정합 = CLAUDE.md "신규 페이지→권한 등록" 준수. dead-button 0.
> - **🟢 /pack getElementById 전수 = clean**: pack.js가 참조하는 11 id(packOrderInput/packCard/packEmpty/packClientName/packOrderMeta/packLines/packProgressText/packProgressBar/packDoneBanner/pack-qr-reader/packScanStopBtn) 전부 `pack.ts` 템플릿 실재 + `if(el)` 가드 광범위 + `packInit` not-found warn(CLAUDE.md silent-fail 패턴 준수). 전역헬퍼(escapeHtml/showToast/showPrompt shell.js) 실재. XSS = free-text(item_name/spec/content/unit) 전부 escapeHtml.
> - **🔴 신규 이슈 #479 (bug, small, 자동수정 완료) — /pack 부분수량 프롬프트 showPrompt 2번째 인자 오용**: `pack.js:136`이 `showPrompt(msg, options)`의 2번째 인자에 `String(cur)`(문자열)를 넘겨 `options.defaultValue` undefined(shell.js:958) → **textarea가 현재수량으로 미리채워지지 않고 빈 상태로 열림**. `packSetPartial`이 빈값→null=전량 해석이라, **이미 부분수량(3/10) 설정된 라인을 "부분" 재클릭해 검토→확인만 누르면 무음 전량 리셋 후 `PATCH /checklist/:id` 저장**(무음 데이터 손실). 프리필이 정확히 그 방어. 형제 `leaves.js:353`은 options 객체 정확 전달=격리 증거. **#426 클래스(API 2번째 인자 오용)** — #426 codify상 "options/`.then` 정확전달"은 dormant-write 활성화·기능추가 아닌 **안전 패턴교체**라 자동수정: `b274bb1` `{ title:'부분 수량', defaultValue:String(cur), placeholder:String(quantity) }`로 정정. `npm run verify` tsc exit0/build exit0.
> - **🟢 showConfirm 콜백 오용(#426) churn 스캔 = net-new 0**: shipments.js/pack.js/production.js/orderForm `showConfirm(msg, function/()=>)` 2번째 인자=함수 오용 0건(전부 `.then`/`await` 정상). production 출력이력 필터(7일 기본+빈결과 탈출버튼)=UX 개선 clean.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 6건**(`list_issues(OPEN,auto-improve)` totalCount=6: #473·#474·#475·#476·#477·#478) = 직전 Area2 stats `new=6` **정합**. 본 Area3 #479(자동수정+보고) 추가 → **new 6→7**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`2efd34d`)라 미push 픽스 0(단 본 사이클 `b274bb1` fix는 push 필요 = HEAD ahead 1).
> - **🧬 SKILL 강화 0건**: #479는 기존 Area 3 "showPrompt/showConfirm 2번째 인자 오용 + 도달성 standing scan(#426)" 클래스 직접 매치 — 신규 탐지 패턴 불요. 단 #426은 그간 `showConfirm(콜백)` 사례만 있었고 **`showPrompt(문자열)`=defaultValue 드롭→무음 데이터 리셋**은 첫 실증(콜백 미실행이 아니라 default 인자 소실로 빈 입력이 destructive default로 해석되는 변종)이나 동일 레시피가 커버(2번째 인자 타입 대조)라 codify 보류.
> - 신규 이슈 1건(#479, **자동수정 완료**), 자동수정 1건(pack.js showPrompt 인자, `b274bb1`), SKILL 강화 0건, done-sync(new 6→7), **신선 각도 — 출고 v2 P1~P4 최대 churn(신규 /pack 모바일 검수)에 dead-button/axios-route·getElementById·전역헬퍼계약 집중. #479=부분수량 프롬프트가 showPrompt 2번째 인자에 문자열 오전달→프리필 소실→재편집 시 부분수량 무음 전량 리셋(자동수정). 배송 v2 신규 라우트 8종 전부 실재·권한등록 정합·showConfirm 오용 0. tsc0/build0.**
>
> **Area 2 코드 품질 심층 분석 (2026-07-03T10:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(`git checkout -B main origin/main`으로 **HEAD=origin/main `42b8ad9` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 2 **29회차** — 직전 Area2(`4e6e0c1`, 07-02T10:00) 이후 owner churn = **배송 후속 P1~P4 대규모 리팩터**(shipments.ts +357·**shipmentHelper.ts +173 신규 util**·cards/lifecycle +112·shipments.js +209) + 급여대장 대전환(payroll +519) + cashSchedule/cashflowEngine 하이브리드 달력(+165). **신선 각도 = 신규 util `shipmentHelper.ts` churn-bridge + N+1 자동스캔을 최대 churn(배송 P1 출고정합)에 집중.**
> - **🟢 타입 검증 = clean**: `npx tsc --noEmit` **exit 0**.
> - **🔴 신규 이슈 #478 (improvement, small, issue-only) — sync-statuses P1 출고정합 루프의 무제한 주문당 헬퍼 N+1**: `orders/lifecycle.ts:557`(배송 P1 `ccb53f8` 신규)이 **LIMIT 없는 `toShip`**(auto_complete_date 도래분 전부)에 주문당 `ensureShipmentForOrder`(내부 6~9 순차 D1 서브요청) 호출. Step1 상태전이 UPDATE/이력은 `db.batch`(청크80)로 이미 N+1 제거됐으나 헬퍼 루프만 순차. ~110~165건에서 Worker 서브요청 한도(1000) 소진 → 이후 주문 status는 SHIPPED 커밋인데 헬퍼 per-item catch(`:561`)로 무음 실패 = P1 목적(SHIPPED↔shipment 정합) 대량 동기화에서 재파손 + 회계 batch 동반 throw. 응답 `shipped`=Step1 카운트라 은폐. 수동 버튼(`shipments.js:1294`·`taxInvoices.js:1269`, ADMIN/MANAGER)이라 백로그 누적 가능. 수정=대상집합 유계화(LIMIT+반복클릭 소진+실패 카운트 응답). issue-only(출고/회계 write-path+배치 정책+egress).
> - **🟢 신규 util `shipmentHelper.ts`(+173) churn-bridge = clean**: `ensureShipmentForOrder`/`applyConsolidationIntents` 전수 — INSERT 2건(shipments=entity_id 스탬프·shipment_items=entity_id 컬럼 부재 child, 둘 다 정합), 명시 SELECT 컬럼존재성 전부 실재(orders/clients 표준 컬럼·shipments.merged_into_id[0437]·orders.consolidate_with_order_id[0438]). 합배송 양방향(내가 가리키는 root + 같은 root 형제) 로직·대표재선출·체인방지 정합. cards/lifecycle은 인라인 shipment 생성 로직을 이 헬퍼로 일원화한 clean 리팩터(dtMap 정본화).
> - **🟢 D1 바인드 IN절(#458) 자동스캔 = net-new 0**: churn IN절 전수 — shipments daily enrichment(`:184`)는 **이미 80청크 적용**(`:175` 주석 명시, #458 픽스 반영), merge/unmerge(`:317/:346/:387`)·consolidate IN절은 **user-selected order_ids/그룹원**으로 practically bounded(FP), items/order_items 룩업은 단일주문 bounded(FP). 신규 데이터-스케일 미청크 IN절 0.
> - **🟢 cashflowEngine(+94) 정합**: `carryOverdueToStart` 옵션 추가(달력 월뷰=false, 예측=true 기본)는 연체 carry 게이트를 일관 적용, `WHERE lp.scheduled_date >= ${carryOverdue ? "date(?,'-31 days')" : '?'}` 바인드 카운트 대칭(항상 placeholder 1개, `from` 바인드 동일)=바인드 불일치 무. payroll (날짜×귀속월) Map 집계는 인원수 노이즈 제거 순수 개선.
> - **🟢 authMiddleware recursive 스캔 = clean(net-new 0)**: `find src/routes -name '*.ts'` 무-auth mutate 후보 1건(`cron.ts`)뿐 = known-good(내부 cron 트리거, FP). 신규 배송/급여 라우터 전부 `requireRole`/`requirePagePermission` 게이트.
> - **🟢 entity_id INSERT 누락 = net-new 0**: churn 신규 INSERT 2건 전부 shipmentHelper(위 검증). 나머지 churn은 UPDATE/SELECT.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 5건**(`list_issues(OPEN,auto-improve)` totalCount=5: #473·#474·#475·#476·#477) = 직전 Area1 stats `new=5` **정합**. 본 Area2 #478 추가 → **new 5→6**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`42b8ad9`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — "주문당 헬퍼-루프 서브요청 소진" N+1 3번째 축 codify(Area 2 #478)**: #458(IN절 바인드)·#474(제약-중복 dup-check)에 이어, `for (x of rows) { await helper(x) }`에서 **헬퍼가 K≥3 순차 서브요청 + rows가 LIMIT 없는 데이터-스케일**이면 요청당 Worker 한도(1000)를 K×N으로 소진하는 별개 N+1(IN절 無라 #458 grep 미포착). 신호 3종=①루프 바디 `await <export된 헬퍼>` ②대상 쿼리 LIMIT 없음+날짜/status 필터만 ③per-item try/catch 무음. FP=user-selected multi-select(bounded)·헬퍼 서브요청 1개·db.batch flush. 픽스=대상집합 유계화.
> - 신규 이슈 1건(#478 issue-only), 자동수정 0(#478 issue-only), SKILL 강화 1건(N+1 3번째 축), done-sync(new 5→6), **신선 각도 — 배송 P1~P4 최대 churn의 신규 util(shipmentHelper.ts) churn-bridge + N+1 집중. #478=P1 출고정합 루프가 무제한 대상에 주문당 6~9쿼리 헬퍼 순차 호출(서브요청 소진 시 SHIPPED↔shipment 불일치 무음 재생성). 신규 util 컬럼/INSERT/entity·IN절(#458 daily 청크 반영)·auth·cashflow 바인드 전부 clean. tsc exit0.**
>
> **Area 1 프로덕션 헬스 (2026-07-03T06:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(`git checkout -B main origin/main`으로 **HEAD=origin/main `7a27e74` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 1 **26회차** — egress 차단(prod 직접 fetch·Playwright 도달 불가)이라 CI/E2E는 GitHub Actions API로 검증. 직전 Area1(`90c4e47`, 07-02T06:00) 이후 owner churn = **배송 후속 P1~P4 대규모 리팩터**(0436 shipped_at·0437 merged_into_id 합포장·0438 consolidate_with 접수시점 합배송예약, shipments.ts/orderForm 대량) + 급여대장 4단→계병합 이동 + 은행 별칭.
> - **🟢 CI 헬스 = green(11/12, 1 failure=CF 배포 transient)**: `deploy.yml` main 최근 12런 중 **11 success**. 유일 failure `469cc12`(docs-only `docs(status)` 커밋)는 **Typecheck✓·Build✓·Deploy step만 fail** — job 로그 실측 = wrangler가 Worker 컴파일/업로드 성공(`✨ Uploading Worker bundle`) 후 CF "Deploying..." 최종화 단계에서 `Failed to publish your Function. Got error: Unknown internal error occurred.` = **Cloudflare 측 내부 transient**(코드/설정 무관). **다음 배포(`03d35f1`)가 즉시 성공·supersede** → prod 최신. #400 cold-start(smoke 500)와 다른 **배포-step CF-internal 하위클래스**(자동회복형).
> - **🟢 DROP/RENAME 마이그 write-path standing scan(SKILL line 40) = net-new 0**: 신규 0435-0438 전수 `grep DROP (TABLE|COLUMN)|RENAME` = **0건**(전부 additive: 0435 account_alias ADD·0436 shipped_at ADD·0437 merged_into_id ADD·0438 consolidate_with_order_id ADD). items inline-FK 깨짐(#430 smoke 맹점) 위험 0.
> - **🟢 라우트 제거 → smoke 프로브 stale(#429 d축) = N/A**: `git diff 90c4e47..HEAD -- src/routes`에서 제거된 `.get/post('path')` **0건**(배송 churn은 merge/unmerge/consolidate 신규 추가·프론트 재배선, 라우트 purge 없음). smoke 프로브 배열 재검 불요.
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` = 0080/0193/0327/0412/0416/0420(6건) 전부 **이번 churn 이전분**(신규 0435-0438 전부 고유 번호=비중복). #438 규칙상 기존 prod-적용 추정 중복은 리네임이 더 위험=정리 대상 아님, 이번 윈도 신규 유입만 보고 → 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 5건**(`list_issues(OPEN,auto-improve)` totalCount=5: #473·#474·#475·#476·#477) = 직전 Area6 stats `new=5` **정합**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`7a27e74`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — 배포-step "CF-internal transient" 하위클래스 codify(Area 1 #400 확장)**: SKILL line 38이 "deploy 단계 자체 failure=진짜 보고대상"으로 뭉뚱그리나, **wrangler 업로드 성공 후 CF "Deploying..." 최종화에서 `Unknown internal error occurred`로 fail하고 다음 배포가 즉시 회복**하면 CF-측 내부 transient(코드/설정 무결)라 **비보고**. 판별 = ① Typecheck/Build/Upload(`✨ Uploading Worker bundle`) 전부 success ② 에러가 CF finalize 단계(`Failed to publish your Function`) ③ 다음 커밋 배포 자동회복. 진짜 보고대상은 여전히 (a)Build/Typecheck fail (b)연속 배포 동일 실패(비회복). #400(smoke 로그인 500 cold-start)의 **배포-step 형제** — 둘 다 자동회복형 CF transient.
> - **🧹 backlog 4차 트림**: 288KB→86KB(06-24~06-30 사이클 로그 git 히스토리 이관, 256KB Read 한도 회복).
> - 자동 수정 0건(CI green·prod 최신), 신규 이슈 0건(net-new 0, 클린 사이클), SKILL 강화 1건(CF-internal 배포 transient 하위클래스), done-sync(new=5 유지), **신선 각도 — 배송 후속 P1~P4 최대 churn 이후 CI 헬스. 유일 CI failure는 CF Pages `Unknown internal error` 배포-step transient(docs-only 커밋, 다음 배포 즉시 회복, prod 최신)로 격리 = #400 배포-step 형제 codify. DROP/RENAME(0435-0438 전부 additive)·라우트 purge 0·마이그중복 신규 0·sync 5=5 전부 clean. #422 디버전스 0.**
>
> **Area 6 자기 진화 (2026-07-03T02:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(origin `03d35f1→cb4ad2b` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `cb4ad2b` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 6 **30회차** — 직전 Area5(`03d35f1`, 07-02T22:00) 이후 owner churn = **배송 후속 P1~P4**(cb4ad2b: 접수시점 합배송 예약 `0438 orders.consolidate_with_order_id` + 직배 섹션 + 합포장 배지, orderForm client.js +61·parent.js·calc.js·queries.ts `/unshipped-by-client` 신설) + 급여대장 지급계 병합셀 이동(d7b9627). **신선 각도 = 컬럼-diff bridge + XSS bridge + #477 dangling churn-트리거 재스캔을 직전 Area4(`7c96025`, 0437까지만 봄)·Area5(`03d35f1`) 이후 신규 착륙 0438에 집중**(6영역 순환의 bridge 갭 정확히 타격).
> - **🟢 컬럼-diff bridge(post-Area4 churn) = clean**: 신규 마이그 1건뿐(`0438_orders_consolidate_with` = `orders.consolidate_with_order_id INTEGER` nullable 포인터, NOT NULL/default 이슈 무). 사용처 전수 정합 — create.ts:48(root 해소)·update.ts:58(root 해소)·shipmentHelper.ts:139(양방향 읽기)·shipments.ts:387(unmerge NULL 정리)·queries.ts:58(SELECT)·프론트 parent.js:1046/calc.js:672. INSERT 컬럼리스트(create.ts:151)에 포함, 컬럼 실재.
> - **🟢 #477 churn-트리거 dangling 재스캔(0438 신규 포인터) = clean(FP)**: 0438 `consolidate_with_order_id`는 0437 merged_into_id 직후 착륙한 **동일 클래스 신규 비-FK 포인터**(Area4 26회차는 0437까지만 봄=bridge 갭). 부모(root주문) 하드삭제(`orders/core.ts:591/612`)·취소(`:528`) 시 자식 포인터 미정리로 dangling하나, **읽기 경로 `applyConsolidationIntents:147-155`가 `status NOT IN ('CANCELLED','DELETED')` 필터 + 역방향(`consolidate_with_order_id=rootId`) 조회로 생존 형제만 병합** → forward(삭제/취소 root)는 무매치·필터, write 리다이렉트 전무 → dangling 자연 관용. **#477(merged_into_id=삭제행으로 write 리다이렉트=실버그)와 대비되는 FP축**(정리 불요). → **SKILL codify: "모든 신규 비-FK 포인터가 정리 필요는 아님; 판별축=삭제행으로 리다이렉트(실버그) vs status필터+역방향 dangling 관용(FP)" Area6 30회차**.
> - **🟢 XSS bridge(post-Area5 churn scripts) = net-new 0**: `git diff --stat 03d35f1..HEAD -- src/scripts/**/*.js` = orderForm client.js +61·parent.js +4·calc.js +1만. 신규 innerHTML sink 1건(client.js:60 합배송 후보 배너) — free-text DB 필드(`order_number`·`entity_name`·`delivery_date`·`delivery_method`·`itemTxt`) **전부 `escapeHtml` 적용**, 미escape 보간은 `o.id`/`idx`(숫자)+controlled literal뿐. getElementById 가드 존재(#ofConsolidationBanner not found warn).
> - **🟢 신규 엔드포인트 `/unshipped-by-client`(queries.ts:49) 격리 = FP(합포장 cross-entity 설계)**: 합배송 예약 후보 피드. `WHERE o.client_id=?`만·entityFilter 무 → 자동 IDOR 스캔이 후보로 잡으나 **문서화된 cross-entity 합포장의 reservation 쪽 거울**(Area5 #452 merge 엔드포인트 cross-entity FP클래스ⓒ와 동일 — entity_name 뱃지를 UI가 의도적 렌더=cross-entity 후보 표시가 설계 목적, entityFilter 걸면 기능 파손). auth 게이트 실재(`:15` authMiddleware + requireAnyPagePermission('/orders','/cards')), 도달성 LIVE(client.js:60).
> - **🟢 마이그 번호 중복 standing scan(#438) = 신규 유입 0**: `uniq -d` = 0327/0412/0416/0420(4건) 전부 **이번 churn 이전분**(신규 마이그는 0438 유일=비중복). #438 규칙상 prod-적용 추정 기존 중복은 리네임이 더 위험(이력 꼬임)=정리 대상 아님, 이번 윈도 신규 유입만 보고 → 0.
> - **🟢 #473 다중모듈 우산 재검증(#473 codify) = 상태 정합**: #473(#452 부분픽스 clients 포털계정 IDOR)은 **여전히 OPEN**(미픽스, credential IDOR 잔존) — owner 신규 close 0이라 재검증할 신규 done-이관 우산 이슈 없음. #473 자체가 open이므로 done-sync 오판 위험 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 5건**(`list_issues(OPEN,auto-improve)` totalCount=5: #473·#474·#475·#476·#477) = 직전 Area5 stats `new=5` **정합**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`cb4ad2b`)라 미push 픽스 0.
> - 신규 이슈 0건(net-new 0, 클린 사이클), 자동수정 0건, SKILL 강화 1건(비-FK 포인터 dangling FP-정밀화 = #477 재스캔의 severity 판별축), done-sync(new=5 유지), **신선 각도 — 6영역 순환 bridge 갭(직전 Area4가 못 본 0438)을 정확히 타격. 컬럼-diff·XSS·dangling·엔드포인트-격리·마이그중복 5축 전부 clean. 0438 consolidate_with_order_id는 merged_into_id와 달리 status필터+역방향 조회로 dangling 관용(FP축) → 재스캔 규칙에 severity 판별축 codify.**
>
> **Area 5 보안 + 인프라 (2026-07-02T22:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→03d35f1` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `03d35f1` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 5 **26회차** — 직전 Area5(`35c1849`, 07-01T22:00) 이후 owner churn = **배송 시스템 P1~P3 대규모 리팩터**(0436 `orders.shipped_at`·0437 `shipments.merged_into_id`+합포장 merge/unmerge, shipments.ts +342·shipments.js +142) + **급여대장 4단/2단 구조 대전환**(payroll.js +417) + 자금계획 하이브리드 달력(cashSchedule.js +104)·바로빌 통합포인트. **신선 각도 = 최대 churn(배송 merge 신규 mutation 3종·급여/자금 프론트 대량)에 XSS·IDOR 자동스캔 + 신규 cross-entity 기능(합포장) 격리 검증 집중.**
> - **🟢 자동 IDOR 형제-비대칭 스캔(#452 승격) = net-new 0**: entity_id 보유 114테이블 × 라우트 mutate 블록 스캔 → 후보 9건 **전부 FP/known/dead**: ① **shipments.ts:361 `/unmerge`**(+ `/merge:306`·PATCH`/:id:823`) = **문서화된 의도적 cross-entity 합포장**(`법인 무관` 주석, 같은 날·같은 거래처 복수 주문을 한 박스로 — 형제 merge도 동일 미격리 = FP클래스ⓒ "깨끗한 비대칭 불성립". PATCH`/:id`는 write-redirect **전** `entityFilter(c)` SELECT 게이트로 자식 shipment 소유 검증=블록내 read-gate 안전) ② aiAnalysis:187/358 = 문서화된 에이전트 콜백 FP(#455 ⓑ) ③ cardExpenses:886 `DELETE /auto-rules/:id` = 형제 GET은 entityFilter이나 **프론트 호출 0=dead**(#334, fixedAssets:148 dispose와 동급) ④ portal:113 login = 공개 auth(FP) ⑤ clients:1138/1182 = **#473(이미 OPEN, credential IDOR)** ⑥ equipmentQueue:137 `/update-avg-speed` = `:id` 없는 전역 print_events 집계 재계산(공유 장비 master, per-entity 타깃 불가=FP).
> - **🟢 XSS innerHTML free-text sweep(churn scripts) = net-new 0**: 최대 churn 3파일 전수 — leaves.js(`lvEscapeHtml` wrapper 전수: name/department/position/reason/employee_name 텍스트+title 양쪽 escape)·bank.js(`escHtml` wrapper: counterpart_name/matched_client_name/matched_category_name/preset/cat.name 전수, `grep -c escapeHtml`=0은 prefix wrapper라 FP)·cashSchedule.js(`escapeHtml` it.name·client `safe` 사전escape, forecast/달력은 fmt()숫자·enum라벨). payroll.js(+417, escapeHtml 11)·shipments.js(+142, escapeHtml 65)도 wrapper 컨벤션 일관. **A-024 사각(escapeHtml 0회 파일=leaves/bank)은 prefix wrapper 확인으로 배제**.
> - **🟢 시크릿 폴백(#338)·SQLi·CSV = clean**: `c.env.X||'리터럴'` 2건(fax.ts:43 `BAROBILL_FTP_PASSWORD||''`·bank.ts:94 `account_password||''`) 전부 **빈문자열 폴백**(하드코딩 시크릿 아님=FP). SQLi 보간 1건(activityLogs.ts:28 `COUNT(*) FROM (${query})`=바운드파라미터 서브쿼리 래퍼, churn 무관 FP). 신규 CSV export 0(churn diff 무매치).
> - **🟢 churn 라우트 격리 무결**: bank.ts PUT`/accounts/:id`(#437 fix)=`entityFilter(c,'bank_accounts')` SELECT게이트+UPDATE `WHERE id=?${ef.clause}` 잔존, DELETE=블록내 read-gate. kakao.ts churn=`GET /unit-cost` 바로빌 단가상수 조회(entity mutate 무). shipments 라우터=`authMiddleware+requirePagePermission('/shipments')` 전 엔드포인트 게이트(무인증 홀 0), 신규 merge/unmerge/patch 전부 requireRole. tsc exit0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 5건**(`list_issues(OPEN,auto-improve)` totalCount=5: #473·#474·#475·#476·#477) = 직전 Area4 stats `new=5` **정합**. owner 신규 close/머지 0(done=163·rejected=3 유지). 본 Area5 net-new 0 → new=5 유지. #422 디버전스: HEAD=origin/main 동기(`03d35f1`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — "문서화된 의도적 cross-entity mutation 기능" FP클래스 codify(Area 5 #452 스캔)**: 자동 IDOR 스캔이 `법인 무관`류 주석으로 **cross-entity를 명시 설계**한 합포장(shipments /merge/unmerge/patch) mutate를 "격리 0"으로 플래그하나, ① 형제 mutate(merge)도 동일 미격리(비대칭 불성립, FP클래스ⓒ) + ② 설계 주석이 cross-entity 정책 명시(ⓑ 변종)면 confirmed 아님. FP클래스ⓒ에 "**형제 GET 미격리**"뿐 아니라 "**형제 mutate가 동일 미격리 + 설계 주석이 cross-entity 명시**" 하위케이스 추가 — write-redirect 대상(대표=cross-entity 정본)이 문서화된 정본이면 그 리다이렉트도 의도. 단 진입 mutate가 부모자원 소유를 entityFilter로 검증하는지(shipments PATCH`/:id`처럼 write-redirect **전** 자식 소유 게이트)는 별도 확인 — 그게 있으면 cross-entity 리다이렉트도 안전.
> - 신규 이슈 0건(net-new 0), 자동수정 0건(클린 사이클), SKILL 강화 1건(cross-entity 의도 FP클래스), done-sync(new=5 유지), **신선 각도 — 배송 merge 시스템 P1~P3 최대 churn의 신규 mutation 3종(merge/unmerge/patch) IDOR 자동스캔 = 문서화된 cross-entity 합포장(FP)으로 확정, PATCH는 write-redirect 전 자식 소유 entityFilter 게이트 검증. XSS(leaves/bank/cashSchedule wrapper 전수)·시크릿폴백(빈문자열 FP)·SQLi(바운드 래퍼 FP)·bank #437 잔존·shipments 무인증 홀 0. 후보 9건 전부 FP/#473/dead.**
>
> **Area 4 데이터 정합성 (2026-07-02T18:00):**
> - **방법**: `npm ci`(node_modules 0→65, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→7c96025` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `7c96025` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 4 **26회차** — 직전 Area4(`e9fe499`, 07-01T14:00) 이후 owner churn = **배송 시스템 P1~P3 대규모 리팩터**(0436 `orders.shipped_at`·0437 `shipments.merged_into_id`+합포장 merge/unmerge, shipments.ts +342줄) + 급여대장 단일뷰·자금계획 표기 + leaves 대량. **신선 각도 = 최대 churn(배송 merge)의 신규 참조 컬럼에 dangling 고아 standing scan 집중.**
> - **🟢 자동 스캔 clean**: ground-truth 스키마 재빌드(파서 4규칙, sanity orders52/cards48/clients45/shipments20) 후 INSERT 컬럼존재성=0·CHECK literal write=0·FK 비-id 바인딩=net-new 0. `orders.shipped_at`(0436) 스탬프 정합 — SHIPPED 전이 8경로 전부 `shipped_at = COALESCE(shipped_at, CURRENT_TIMESTAMP)` 일관(shipments.ts:941/973/989/1061·orders/queries.ts:263·orders/lifecycle.ts:541·cards/lifecycle.ts:362), 취소 시 `shipped_at=NULL` 롤백 대칭(shipments.ts:924/998). KST 날짜(churn 파일)=전부 audit 타임스탬프 FP.
> - **🔴 신규 이슈 #477 (bug, small, issue-only) — 합포장 대표 shipment 제거 시 자식 merged_into_id dangling**: 0437 신규 비-FK 컬럼 `shipments.merged_into_id`(대표 참조)가 부모-제거 2경로에서 미정리. ① 주문 하드삭제 `orders/core.ts:591 DELETE FROM shipments WHERE order_id=?`가 **타 주문 자식**(합포장=order_id 상이 cross-parent) 정리 안 함 → 삭제행 dangling → `applyShipmentFieldPatch:753` write-redirect가 삭제된 대표로 0행 UPDATE인데 `:789 return true`(meta.changes 무시)=자식 송장/라벨 편집 무음 소실+success 응답, 일일목록 `:162 LEFT JOIN spm` NULL 폴백=자식 공백. ② shipment CANCELLED `:889`도 미정리=정본이 취소행. cross-parent라 FP배제(b) 반례. 도달성 LIVE.
> - **🧬 SKILL 강화 1건 — "churn-트리거 재스캔" codify(Area 4 #477)**: dangling 삭제-정리 배치(#454로 큐레이션)는 "완결"로 보이나 **후속 마이그가 새 비-FK `*_id`/`*_into_id`를 ALTER ADD하면 그 컬럼을 모른 채 통과=도입-즉시 dangling**(회귀 아님). `git diff <last-area4>..HEAD -- migrations`에 신규 참조 컬럼 있으면 그 부모테이블 DELETE/CANCELLED 핸들러를 재스캔. cross-parent 참조(합포장)=FP배제(b) "자식이 부모와 함께 DELETE" 반례로 등록.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 4건**(`list_issues(OPEN,auto-improve)` totalCount=4: #473·#474·#475·#476) = 직전 Area3 stats `new=4` 정합. 본 Area4 #477 추가 → **new 4→5**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`7c96025`)라 미push 픽스 0.
> - 신규 이슈 1건(#477 issue-only), 자동수정 0(dangling=파괴적 삭제 batch mutation=자동수정 금지), SKILL 강화 1건(churn-트리거 재스캔), done-sync(new 4→5), **신선 각도 — 배송 시스템 P1~P3 최대 churn의 신규 컬럼(0437 merged_into_id)에서 부모-제거 미정리 dangling 격리. orders.shipped_at 스탬프 8경로 정합·자동 컬럼/CHECK/FK 스캔 net-new 0.**
>
> **Area 3 UX/기능 감사 (2026-07-02T14:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→f044e2f` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `f044e2f` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 3 **25회차** — 직전 Area3(`e7a9d36`, 07-01T10:00) 이후 owner churn 33커밋 = **은행 계좌 별칭(`b2daab7` account_alias)** + **바로빌 senderId 통일·통합포인트 표시**(`8dad40f`·`1834302`·`4e6e0c1`) + md 대량정리(`0bbf72f`). **신선 각도 = churn 최소(신규 UI 기능=은행 별칭 1건)라 dead-button standing scan을 앱 전반으로 확장**(병렬 Explore 서브에이전트 종합 스윕: dead-button/getElementById/showConfirm/빈상태/필터충분성 5종).
> - **🟢 신규 churn(은행 계좌 별칭) = clean**: `account_alias`가 필터 드롭다운(`bank.js:160`)·거래행 라벨(`:220`)·계좌목록 우선표시(`:736`)·모달 입력/편집로드(`:823/:842`)·POST/PUT 저장 전반 일관 적용. PUT `CASE WHEN ?=1 THEN ? ELSE account_alias END`(key미포함=보존, 빈값=해제) 로직 정합. 바로빌 통합포인트 표시(`partner_point`/`unit_cost_alimtalk`)는 백엔드 `/api/kakao/balance` 응답 필드 실재=정합(멀티법인 senderId 통일은 IDOR/정확성 **개선**, 버그 아님). 기본 날짜필터=당월 bounded(`bank.js:51 initDates`)라 `/transactions` LIMIT無 전량반환도 무해(FP).
> - **🔴 신규 이슈 2건 (dead-button standing scan)**:
>   - **#475 (bug, small, HIGH, issue-only)** — 세금계산서·현금영수증 인쇄/PDF 전면 404: `taxInvoices.ts:995`가 taxScript+cashReceiptsScript를 **동일 전역 스코프 concat**(둘 다 IIFE 미래핑), 둘 다 top-level `openPrintURL` 함수선언 → 호이스팅상 나중 로드 cash 버전(`cashReceipts.js:372` `/api/cash-receipts/:id/print-url`=**라우터 부재**)이 tax 버전(`taxInvoices.js:1251` `/api/tax-invoices/:id/print-url`=**실재** `queries.ts:423`)을 **silent shadow**. 헤더 `#tax-cash-scope`가 동일 충돌 클래스를 인지하고 `statusLabels/statusColors/renderPagination`만 `ti`격리했으나 **openPrintURL은 누락된 형제**. 세금계산서 인쇄(`:178/:552`)+현금영수증 인쇄(`:103/:310`) 동반 사망, 도달성 LIVE(`/tax-invoices` 활성 핵심메뉴).
>   - **#476 (bug, small, HIGH+MED, issue-only)** — 거래처 상세 여신설정 저장·활성토글 dead-button: `saveCreditSettings`(`clientDetail.js:367`)→`PATCH /api/clients/:id/credit`·`toggleClientActive`(`:444`)→`PATCH /:id/toggle-active` 둘 다 clients 라우터 부재(PATCH는 `/:id`·`/portal-account`·`/billing-group`뿐, Hono exact-match상 `/:id`로 안 잡힘→항상 404). 프론트 배선·렌더 완료인데 백엔드 미구현 → 여신한도/여신보류 저장 불능(HIGH)·활성토글 불능(MED). 도달성 LIVE(`/clients/:id`).
> - **🟢 나머지 스캔 4종 = net-new 0(clean)**: ② getElementById silent-fail(고빈도 스크립트 bounded, `(getElementById||{}).value`/`if(el)` 가드 광범위) ③ showConfirm 콜백오용(전수 0건, 2-인자는 옵션+`.then` 정상) ④ 빈상태 커버리지(확립 패턴 일관) ⑤ 검색/필터 충분성(clients 백엔드 `search/dormant/has_balance/credit_hold` 전부 프론트 노출). **FP 배제 확인**: `/api/forecast/material-consumption`(#318 menu.ts:76 주석 무력화)·`/api/migration/${type}/*`(clients/items/orders/payments 전부 실재) 보고 제외, ~870 axios 전수 매칭.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 2건**(`list_issues(OPEN,auto-improve)` totalCount=2: #473·#474) = 직전 Area2 stats `new=2` 정합. 본 Area3 #475·#476 추가 → **new 2→4**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`f044e2f`)라 미push 픽스 0.
> - **🧬 SKILL 강화 1건 — "`?raw` concat 전역 스코프 함수선언 충돌" standing scan codify(Area 3 #475)**: 기존 dead-button 스캔(axios→404)은 증상을 잡았으나, **다중 `?raw` 스크립트가 IIFE 없이 한 스코프로 concat되면 동명 top-level `function`/`var`가 나중 파일 버전으로 silent shadow**되는 **근본 클래스**는 별도 축. 특히 **양쪽 라우트가 다 존재해도** onclick이 잘못된(shadow된) 함수를 불러 정상 코드가 사장될 수 있음(#475는 tax 라우트 실재인데 cash가 덮어씀). 탐지: `grep -n "Script + '\n' + .*Script" src/pages`로 concat 지점 → 대상 `?raw` 파일들의 top-level `function <name>`/`var <name>` 교집합 추출 → onclick/전역참조되는 동명 식별자가 `ti`류 프리픽스 격리 없이 중복이면 후보. `#tax-cash-scope` 헤더가 이미 격리한 목록(statusLabels 등)과 diff해 **미격리 형제**만 격리.
> - 신규 이슈 2건(#475·#476 dead-button, 둘 다 issue-only), 자동수정 0(Area3 UI/API 신설 = 자동수정 금지), SKILL 강화 1건(concat 스코프 충돌 standing scan), done-sync(new 2→4), **신선 각도 — churn 최소(은행 별칭 clean) 사이클에서 dead-button을 앱 전반 확장. #475 openPrintURL 전역 스코프 shadow(정상 tax 라우트가 cash에 덮여 인쇄 전면 404, `#tax-cash-scope` 미격리 형제)·#476 clients 여신/활성토글 백엔드 라우트 미구현. getElementById/showConfirm/빈상태/필터 4종 clean.**
>
> **Area 2 코드 품질 심층 분석 (2026-07-02T10:00):**
> - **방법**: `npm ci`(node_modules 0→81, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→4e6e0c1` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `4e6e0c1` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 2 **28회차** — 직전 Area1(`90c4e47`, 06:00) 이후 owner churn = **바로빌 설정 연결테스트 표시 픽스**(`4e6e0c1`: settings.js `b.remain_point→partner_point`·`b.unit_cost→unit_cost_alimtalk`). **신선 각도 = churn 최소(표시 픽스 1건)라 N+1 쿼리 standing scan에 집중**(최근 사이클이 컬럼존재성·IN절·auth·단위환산 커버했으나 N+1은 미탐).
> - **🟢 타입 검증 = clean**: `npx tsc --noEmit` **exit 0**.
> - **🟢 바로빌 churn 정합**: settings.js가 읽는 `b.partner_point`·`b.unit_cost_alimtalk`가 백엔드 `/api/kakao/balance`(`kakao.ts:304/306`) 응답에 실재 = 필드 존재성 정합(픽스 자체가 구 `b.unit_cost` 미존재 버그 해소). 라벨 '잔여 포인트'→'통합 포인트'만 표시 변경.
> - **🔴 신규 이슈 #474 (improvement, small, issue-only) — 바로빌 은행 동기화 건별 dup-check SELECT가 UNIQUE 인덱스와 중복 = N+1**: `bank.ts:508`(`POST /sync-barobill`)·`:1781`(`POST /auto-sync`) 2루프가 이미 존재하는 `idx_bt_codef_id (bank_account_id, codef_transaction_id)`(`migrations/0043:46`)를 **거래 1건마다 SELECT로 재확인** 후 조건부 INSERT → 거래당 2 라운드트립 순차 await. 계좌 등록 백필(bank.js:880 계좌개설일부터)·임의 범위(bank.js:923)로 넓은 기간 동기화 시 계좌×날짜×거래(일 500) = 수천 순차 SELECT → Worker 한도 시 날짜별 `catch`(`:527`/`:1798 무시`)가 무음 부분적재. 수정=`INSERT ... ON CONFLICT DO NOTHING` + `meta.changes` 카운트 보존(의미 동일: NULL codef distinct라 refKey無 항상 insert). issue-only(재무 write-path+egress SOAP 검증불가).
> - **🟢 자동 standing scan 4종 = net-new 0**: ① **entity_id INSERT 누락** — 114 entity_id 테이블 × 라우트 INSERT 스캔, 후보 3(`hr.ts:474 employees`·`attendance.ts:336/344`) **전부 FP**(동적 컬럼구성 `cols.push('entity_id')`로 서버 스탬프, 스캐너가 `${cols.join()}` 리터럴만 봄) ② **명시-SELECT 컬럼존재성**(A-027 자동) — 205테이블 ground-truth 대조, 후보 2(`payroll/settings.ts:106 NULL`·`cardExpenses.ts:641 SELECT 1`) 전부 리터럴 파싱 FP ③ **D1 바인드 IN절**(#458) — 신규 후보 `tasks.ts:173/181`(claim `LIMIT Math.min(limit,20)`=최대20 bounded)·`bank.ts:1217/1345/1357`(전부 80청크) FP, 기존 #458(shipments/daily·orders bulk-bill/ship) 미변동 ④ **authMiddleware recursive** — NO-AUTH 5(orders/payroll helpers·cron·hrSelf scoped·taxInvoices/helpers) 전부 known-good FP.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 1건**(`list_issues(OPEN,auto-improve)` totalCount=1: #473) = 직전 Area1 stats `new=1` **정합**. 본 사이클 #474 추가 → new 1→2. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`4e6e0c1`)라 미push 픽스 0.
> - **🧬 SKILL 강화 0건**: #474는 기존 Area 2 "N+1 쿼리 패턴" 점검항목 클래스(효율 축) — 신규 탐지 패턴 불요. 단 **부수 관찰 = DB 제약(UNIQUE 인덱스)이 이미 보장하는 것을 애플리케이션이 건별 SELECT로 재확인하는 "제약-중복 dup-check"가 N+1의 한 하위클래스**(기존 IN절-미청크와 별개 축)이나 단일 발견이라 codify 보류(재발 시 승격).
> - 자동 수정 0건(#474 issue-only), 신규 이슈 1건(#474), SKILL 강화 0건, done-sync(변동 0, new 1→2), **신선 각도 — churn 최소 사이클에서 N+1 쿼리 standing scan 집중. 유일 발견 #474=바로빌 은행 동기화 2루프가 이미 존재하는 UNIQUE 인덱스를 건별 dup-check SELECT로 재확인(계좌×날짜×거래 N+1, 넓은 백필 시 무음 부분적재 리스크). entity_id INSERT(후보3 동적구성 FP)·컬럼존재성(후보2 리터럴 FP)·IN절(#458 미변동·신규 bounded/청크)·auth(5 known-good) 전부 net-new 0. 바로빌 표시 churn 필드 정합. tsc exit0.**
>
> **Area 1 프로덕션 헬스 (2026-07-02T06:00):**
> - **방법**: 사이클 초반 `npm ci`(node_modules 0→65, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→90c4e47` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `90c4e47` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 1 **25회차** — egress 차단(prod 직접 fetch·Playwright 도달 불가)이라 CI/E2E는 GitHub Actions API로 검증. 직전 Area1(`424cb5b`, 02:00) 이후 owner churn = **품목 마스터 대량 마이그(0420-0434 간판자재/솔벤/시트/부속품split/볼로프전환/stub삭제)** + #470/#471 IDOR 픽스(`4d78be6`) + Area4/5/6 사이클 문서.
> - **🟢 CI 헬스 = green 회복 확인**: `actions_list`(deploy.yml main) 최근 **10런 전부 success**(`90c4e47`·`28c14c2`·`4d78be6`·`908dd578`·`b1ec9e76`·`d2ebed6`·`35c18492`·`c7935566`·`e9fe499`·`a3139a6a`). **직전 Area1(24회차)이 격리·자동수정한 smoke 프로브 stale(`/api/bom?limit=10`→404)가 `/api/bom/overview`(`smoke.cjs:83` 실재)로 교체돼 회복** — BOM 재배선(`9bd6c2f`)부터 4연속 Smoke FAIL이던 게 종식. #429 purge-완전성 d축(removed-route→smoke probe 404) 자동수정의 검증 완료.
> - **🟢 DROP/RENAME 마이그 write-path standing scan(SKILL line 40) = net-new 0**: 직전 Area1 이후 신규 0420-0434 전수 `grep DROP (TABLE|COLUMN)|RENAME` = **0건**(전부 품목/권한 데이터 INSERT·split은 order_items/product_materials 재배선 UPDATE, DROP TABLE 무 — 0434 stub 삭제도 `DELETE FROM items` 데이터행이지 테이블 DROP 아님). items inline-FK 깨짐(#430 smoke 맹점) 위험 0.
> - **🟢 라우트 제거 → smoke 프로브 stale(#429 d축) = N/A**: `git diff 424cb5b..HEAD -- src/routes`에서 제거된 `.get/post('path')` **0건**(품목 churn은 마이그·프론트 활성화, 라우트 purge 없음). smoke 프로브 배열 재검 불요.
> - **🟢 마이그 번호 중복 standing scan(#438) = net-new 무해**: `uniq -d` = `0080`·`0193`·`0327`·`0412`·`0416`·**`0420`**(신규: `0420_quality_claims_page_permission`[292be2d permission_pages INSERT] + `0420_solvent_canvas`[7405dc5 items INSERT] — **두 파일 전체 파일명이 달라 wrangler가 둘 다 독립 적용**, 순서 의존 0[권한 vs 품목], 기존 5개 dup과 동일 무해 FP 클래스). 신규 breaking 중복 유입 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 1건**(`list_issues(OPEN,auto-improve)` totalCount=1: #473) = 직전 Area6 stats `new=1` **정합**. owner 신규 close/머지 0(done=163·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`90c4e47`)라 미push 픽스 0(#470/#471 IDOR 픽스 `4d78be6` in-tree 확인).
> - **🧬 SKILL 강화 0건**: 본 사이클은 신규 발견 없이 **직전 자동수정(smoke 프로브 교체) 회복 검증 + 품목 대량 마이그 헬스 확인** 사이클 — 기존 standing scan(DROP/RENAME·마이그중복·smoke-probe·#422 디버전스)이 전수 커버. 신규 탐지 패턴 불요.
> - 자동 수정 0건(CI green), 신규 이슈 0건, SKILL 강화 0건, done-sync(변동 0, new 1 유지), **신선 각도 — 직전 Area1 자동수정(smoke 프로브 `/api/bom/overview` 교체)의 회복 검증: BOM 재배선부터 4연속 Smoke FAIL이던 CI가 최근 10런 전부 green 회복. 품목 마스터 대량 마이그(0420-0434 간판/솔벤/split/전환/삭제) DROP/RENAME 0·라우트 제거 0·smoke 맹점(#430) 위험 0. 마이그 번호중복 0420(신규)은 파일명 독립이라 무해. open sync 1=1(#473 credential IDOR 여전히 open, 미조치). tsc/CI 정상. 부수: `npm ci` #439 자가복구.**
>
> **Area 6 자기 진화 (2026-07-02T02:00):**
> - **방법**: 사이클 초반 `npm ci`(node_modules 0→65, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→28c14c2` force-update, `git checkout -B main origin/main`으로 **HEAD=origin/main `28c14c2` 0/0 동기**, 워킹트리 clean, #422 디버전스 0). Area 6 **29회차** — 직전 Area5(22:00) 이후 owner가 **직전 사이클 발견 6건을 전부 픽스·배포·close**(#465 BOM 재배선 `d2ebed6`·#466 단가캐시 `760c3de`·#467 배너 `665d191`·#469 재계산 `a1d8eb8`·#470/#471 IDOR `4d78be6`). **신선 각도 = 대량 close 직후 "이전 발견 재평가" 집중 — 특히 다중모듈 IDOR 클러스터(#452)가 우산 close됐으나 서브모듈이 전부 픽스됐는지 재grep.**
> - **🔴 신규 이슈 #473 (bug, small, security, issue-only) — #452 부분픽스: 거래처 포털계정 GET/PATCH/DELETE IDOR 잔존**: #452(5모듈 IDOR 클러스터, closed=done) 중 messageTemplates·accounts-payable purchase-payment는 `entityFilter` 픽스 반영됐으나 **clients 포털계정 GET/PATCH/DELETE(`clients.ts:1070/1138/1182`)만 bare `WHERE client_id=?` 잔존**. 같은 파일 POST(생성)는 `entity_id` 스탬프·client_notes DELETE(#471)는 방금 픽스 = 격리 의도 명백한데 이 형제만 누락. entity-scoped ADMIN이 타법인 고객포털 **비밀번호 재설정=계정 탈취**(PATCH), 삭제(DELETE), login_id/연락처 열람(GET, MANAGER 도달). 도달성 LIVE(clientDetail.js). **결정적 교훈 = Area5 22:00 스캔이 재격리했으나 "#452 이미보고"로 FP 드롭** — closed=done을 "픽스됨"으로 신뢰한 #377 부분픽스 트랩의 다중모듈 버전. issue-only(IDOR=owner+egress).
> - **🟢 IDOR sibling-asymmetry 자동 standing scan(#452 레시피) = 이번 발견 외 net-new 0**: 114 entity_id 테이블 × 라우트 핸들러블록 mutate 격리 스캔 71후보 → 트리아지 전량 FP(핸들러 경계가 `Map.get(` 오매치로 분할·격리신호가 helper/상위 블록·agentKey site-scoped·self-scoped·batch recompute·ADMIN-global equipment) **except** #473(clients 포털계정)·부차 fixedAssets dispose(dead=#334 latent). #470 payroll sync-attendance는 owner 픽스(`efP` + 집계 CTE `employee_id IN`이 entity-필터 targets에서 파생 = 연쇄격리) **완전** 확인.
> - **🟢 done-sync 실측 재검증(#422)**: 직전 open 6건(#465·#466·#467·#469·#470·#471) 전부 CLOSED 확인 + **픽스 in-tree 대조**(#470 `core.ts:599`·#471 `clients.ts:604/608` diff 실재) → done 157→163. HEAD=origin/main `28c14c2` 0/0라 미push 픽스 0. open 실측 = #473 1건 = new 6→1.
> - **🧬 SKILL 강화 1건 — "다중모듈 우산 이슈 부분픽스 재검증" standing scan codify(Area 6 #473)**: #377(파일분할 부분픽스)·A-026(자기 자동수정 부분픽스)의 **다중모듈 우산 이슈 버전** — 한 이슈가 N모듈을 묶어 보고→owner close(done)해도 일부 서브모듈만 픽스될 수 있음. 매 Area 6 done-sync는 최근 close된 **다중모듈** 이슈의 각 서브모듈 안티패턴을 개별 재grep, 하나라도 bare 잔존이면 net-new. **자동 스캔 재긍정 규칙 추가**: sibling-asymmetry 스캔이 후보를 "기존 이슈 이미보고"로 드롭하기 전, 그 이슈가 closed=done인데 코드가 여전히 bare면 **드롭 금지 = 부분픽스**(closed≠fixed).
> - 신규 이슈 1건(#473 issue-only), 자동수정 0(IDOR=issue-only, 코드베이스 무변경), SKILL 강화 1건(다중모듈 부분픽스 재검증), done-sync(new 6→1, done 157→163), **신선 각도 — 대량 close 직후 이전 발견 재평가에서 #452 IDOR 클러스터의 부분픽스(clients 포털계정 credential IDOR) 격리. IDOR 자동스캔 71후보 트리아지 net-new 1(+dead fixedAssets). closed≠fixed 교훈을 자동스캔 재긍정 규칙으로 codify.**
>
> **Area 4 데이터 정합성 (2026-07-01T14:00):**
> - **방법**: `npm ci`(node_modules 0→#439 commit-gate 자가복구), git fetch(HEAD=`e9fe499`). Area 4 — 직전 Area3(10:00) 이후 owner churn = **품목 마스터 데이터 대량 마이그(0421-0434 간판자재/원단/시트/부속품분리/볼로프전환/stub삭제)** + HR(연차적립 cron·미사용수당 주입·급여잠금) + BOM 재배선. **ground-truth 기법**: `migrations/*.sql` 435개를 소스순서·RENAME/IF-NOT-EXISTS 인지 파서로 스키마 재구성(180테이블, sanity orders51/cards48/clients45 = 재빌드 미놓침 확인) 후 자동 standing scan 5종.
> - **🔧 파서 버그 2건 자체수정(ephemeral 스캐너)**: ① 컬럼명이 `check_*`(check_in_time/check_item/check_type/checklist)면 `startsWith('CHECK')`로 CHECK 제약 오판→컬럼 통째 누락 → `CHECK `/`CHECK(` 경계로 수정. ② `CREATE TABLE IF NOT EXISTS`가 기존 테이블을 무조건 덮어써 0069 no-op를 미반영(collection_logs.contact_person 0054 정의 소실) → ifne+기존테이블 시 skip. 수정 후 INSERT-unknown 13건(전부 파서 FP)→**0**.
> - **🔴 신규 이슈 #469 (bug, small~medium, issue-only) — 미사용연차수당 급여주입 저장 집계 미재계산 → 과소지급 위험**: `leaves.ts:1149` `apply-unused-allowance`가 `annual_leave_pay`만 UPDATE, 저장 `total_salary`/`net_pay` 스테일. 목록 `SELECT p.*`·승인/지급 status-only·주입(연차페이지)↔sync(급여페이지) 미연동·강제장치 토스트뿐 → sync 생략 승인 시 연차수당 누락 지급. #163 신규 write-path aggregate 정합성 standing check의 급여 버전. issue-only(급여 비즈로직+egress).
> - **🟢 자동 standing scan 5종 = net-new 0**: ① **INSERT 미존재컬럼** 0(파서수정 후) ② **INSERT NOT NULL no-default 누락** 0 ③ **CHECK IN literal 위반**(INSERT positional + UPDATE) 0 ④ **users(id) FK 비-id 바인딩**(70 FK컬럼) 0 — 잔존 `'system'` 리터럴 전부 비-FK `price_change_history.changed_by`(안전, SKILL #413), #436 X-User-Id는 이미 픽스(inventoryCount.ts:357/382 `c.get('user')`) ⑤ **KST 업무일자**(#366) — leaves.ts 신규 적립/촉진 write는 `kstNow()`(+9h) 사용, 잔존 `new Date().getFullYear()`(leaves.ts:236/293)는 표시/쿼리기본값(transient) = clean. **비-FK dangling 고아**(#454): 신규 `user_item_access.user_id`(비선언FK)는 users가 soft-deactivate(하드삭제 핸들러·delete UI 0)라 고아 불가.
> - **⏸ 미검증(egress 차단)**: 품목 splits/삭제 마이그(0428/0429/0431/0433/0434)의 order_items·product_materials·inventory 참조 정합은 prod D1 실데이터 필요 — 0434 주석이 "참조 0 확인" 명시, 정적 반증 불가라 보고 보류.
> - 신규 이슈 1건(#469 issue-only), 자동수정 0(파서는 ephemeral 스캐너 자체수정, 코드베이스 무변경), done-sync(new 4→5). **신선 각도 — 품목 마스터 대량 마이그 + HR 신규 write-path 사이클에서 급여 aggregate 정합성 갭 격리. 컬럼/CHECK/FK/KST 자동스캔 전부 net-new 0(파서 FP 2클래스 codify).**
>
> **Area 3 UX/기능 감사 (2026-07-01T10:00):**
> - **방법**: 사이클 초반 stale 체크아웃(`c66bfc1`, last_run_area=5로 오독)에서 `git fetch`+`git checkout -B main origin/main`으로 **origin/main `e7a9d36` 동기**(72커밋 behind→0/0, 워킹트리 clean) 후 **실제 backlog 재독 = last_run_area=2 → Area 3** 확정. `npm ci`(node_modules 0→65, #439 commit-gate 자가복구). Area 3 **24회차** — **대형 기능활성화 churn 사이클**: 직전 Area3(`0292d87`, 06-30T10:00, multi-UOM 표시감사) 이후 owner가 **완성 백엔드 UI 활성화 대량 푸시**(83커밋: `292be2d` 품질/클레임/반품 페이지·`3201228` 장비큐/OEE/작업자실적·`3fae9c1` 死상태값 일괄픽스·HR 급여잠금/연차적립cron/미사용수당주입·BOM 신모델 재배선·메시징 발송단가·품목 0398-0429). **신선 각도 = 직전 감사(multi-UOM 표시)를 벗어나 "신규 활성화 페이지"(quality.js·equipment/queue.js·production.js·productionReports.js·HR)에 Area 3 표준스캔 집중**(dead-button·getElementById silent-fail·showConfirm오용·더블클릭·빈상태). 병렬 서브에이전트 2(생산 churn·HR churn).
> - **🔧 자동수정 1건 #468 (bug, small) — 생산 CSV 항상 빈 파일**: `productionReports.js exportProductionCsv()`가 `getDateParams()`(문자열 반환) 유일 오용(`p.from`/`p.to`=undefined) → `?from=undefined&to=undefined` → 백엔드 `from||기본값` 폴백이 문자열 `'undefined'`(truthy)라 미발동 → `date>='undefined'` 0행 → CSV 데이터 영구공백. **형제 8호출부(`'?'+getDateParams()`) 정합 문자열연결로 교정·verify 통과**(tsc0+build 397). #450 프론트 로직버그 자동수정 클래스(활성 버튼 정합화, 기능추가/UI변경 아님).
> - **🔴 신규 이슈 #467 (bug, small, issue-only) — 연차 촉진/소멸 경보 배너 항상 렌더실패**: 신규기능(`8d3b34c`) 핵심 배너 `leaves.js:522`가 `/api/leaves/promotion/run`에 `source`/`stage` 누락 → 백엔드 `leaves.ts:724` 필수검증 400을 dryRun count 분기 전 반환 → `await` throw→catch 영구숨김 → 촉진 대상 선제가시화 **도입이래 100% 미작동**(silent dead feature, 근로기준법 촉진통지 시기 오인 위험). 부차 role 불일치(ADMIN전용↔`/leaves` MANAGER 진입가능). 수정=요약 엔드포인트(6조합 count 합산). issue-only(신규기능 완성/설계 판단, #426 배너-설계 선례).
> - **🟢 신규 활성화 페이지 표준 standing scan = clean (net-new 0)**: ① **dead-button(axios→라우트)** — quality.js(`/api/claims`·`/returns`·defect-codes·analytics)·equipment/queue.js(`/api/equipment-queue/*` 5종)·production.js(cards/schedule·print-events·production/logs·work-records)·productionReports.js(production-reports 8종·oee)·bom.js(`/api/bom/overview` 재배선)·HR(caps/health·leaves apply-unused-allowance·cron accrual) **전 엔드포인트 라우터 정의 확인=404 0**. ② **getElementById silent-fail** — 4 신규파일 id 리터럴 전수가 pages+scripts id= 코퍼스 실재(cross-file 미존재 0). ③ **showConfirm 콜백오용(#426)** — 신규코드 미사용 또는 `if(!confirm())return` 동기패턴(leaves.js:416). ④ **빈상태/에러** — quality/production/productionReports 전 리스트로더 0행 empty-state+에러브랜치 보유. ⑤ **더블클릭(#420)** — production.js `wrSubmit`(:1024 pre-await disable 가드)·나머지 write 멱등(reorder 전배열·recalc·assign/priority/capacity 절대값 SET·급여잠금 status SET `AND status=?`·미사용수당 절대값 UPDATE) = 전건 FP. quality.js 클레임 create만 비멱등 INSERT(모달 post-await 닫힘)=중복행 가능하나 정보성 tracking 저severity, 보고 보류.
> - **🟢 backlog↔GitHub sync**: **오너 대량 close 반영** — 직전 Area5(06-30) stats new=16이 owner 픽스·close로 **실측 2건**(#465·#466, `list_issues(OPEN,auto-improve)`)까지 축소(done 139→157, 14건 이관: 커밋 `5e8622f`·`1c9b7b6`·후속 IDOR/XSS/multi-UOM 픽스군). 본 Area3 #467·#468 추가 → **new 2→4**. rejected=3 유지. #422 디버전스: HEAD=origin/main `e7a9d36` 0/0 동기라 미push 픽스 0.
> - **🧬 SKILL 강화 0건**: #468은 #450(프론트 로직버그) 클래스·#467은 #426(배너 설계 결함) 클래스 — 기존 Area 3 standing scan(dead-button·더블클릭·showConfirm·getElementById)이 신규 활성화 페이지 전수 커버. 신규 탐지 패턴 불요. **부수 교훈 = stale 체크아웃(72커밋 behind)에서 backlog 헤더 오독 위험** → 사이클 초반 반드시 `git checkout -B main origin/main` 후 backlog 재독(last_run_area가 5→2로 실제와 3영역 차이).
> - 자동 수정 1건(#468 CSV, verify 통과), 신규 이슈 1건(#467 issue-only), SKILL 강화 0건, done-sync(owner 대량 close 139→157, new 2→4), **신선 각도 — 대형 기능활성화 churn(품질/클레임·장비큐/OEE/작업자실적·HR·BOM재배선 83커밋)의 신규 활성화 페이지에 Area 3 표준스캔 집중. 발견 2: #468 생산 CSV 빈파일(getDateParams 문자열 객체오용, 자동수정)·#467 연차 촉진배너 파라미터누락 도입이래 100%미작동(issue-only). dead-button(전 라우트 실재)·getElementById(전 id 실재)·showConfirm(미오용)·빈상태(전 로더 커버)·더블클릭(멱등/pre-await가드=전건FP) clean. 신규 활성화 페이지 대체로 견고. 부수: stale 체크아웃 backlog 오독 방지 교훈.**
>
> **Area 2 코드 품질 심층 분석 (2026-07-01T06:00):**
> - **방법**: 사이클 초반 `npm ci`(node_modules 0→81패키지, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→5ccd31d`, **HEAD=origin/main `5ccd31d` 0/0 동기**, 워킹트리 clean, 디버전스 0). Area 2 **27회차** — 직전 Area1(`424cb5b`) 이후 owner **메시징 발송단가 3종 표시 churn**(`50cd3bc`/`c1b3398`/`f5c1dd7`/`5ccd31d` barobillSms/barobillFax/kakao/messages, +smoke 프로브 픽스 `1030ab7`). BOM 재배선(#465 Area6)·주문삭제(#464 Area4 `9213edb` 픽스)는 기 커버. **신선 각도 = 최신 churn(메시징 발송단가 fan-out)의 코드 품질 + 컬럼존재성 자동 전수스캔**.
> - **🟢 타입 검증 = clean**: `npx tsc --noEmit` **exit 0**.
> - **🔴 신규 이슈 #466 (improvement, small) — 메시지 발송단가 매 로드 5 SOAP fan-out (net-new, 캐시 부재)**: `getUnitCost()`가 1콜→채널별 5병렬 SOAP(`GetChargeUnitCostEx` × ALIMTALK/KKO_IMAGE/SMS/LMS/FAX, `barobillSms.ts:322-345`)로 확장 → `GET /api/kakao/balance`가 바로빌 SOAP **7콜**(getBalance 2 seq + getUnitCost 5 par, 변경전 2콜=3.5배). `messages.js:6/740` loadSummary가 페이지로드+발송성공후 호출·캐시 자산 0(`grep cache/KV` clean). 발송단가는 **계약 정적값**(barobillCodes.ts 상수 문서화)인데 매 로드 5 SOAP 재조회=외부 API량↑·장애표면↑·Promise.all 최느린콜 대기. 정확성 무결(콜별 catch→0 degrade, reject 없음). **issue-only**(캐시전략 판단+egress SOAP지연 계측불가). 수정=단가 캐시(TTL/cron), 잔액만 실시간.
> - **🟢 컬럼 존재성 자동 전수스캔(#377/A-027 클래스) = net-new 0**: 204테이블 ground-truth(마이그 CREATE+ADD COLUMN 파싱) ↔ 명시-컬럼 단일테이블 SELECT 대조. 후보 4건(`bank_match_rules.matched_category_id`×3·`inventory.storage_zone_id`×4) **전부 FP** — 둘 다 `CREATE TABLE <t>_new → RENAME TO <t>`(SQLite ALTER 우회) 재생성 패턴이라 파서가 `_new` 테이블에 컬럼 귀속(0270 bank_match_rules_new·0232 inventory_new), 실컬럼은 존재·prod 사용중(autoDeduct/po-receive `storage_zone_id`, 0270 `matched_category_id` INSERT/SELECT 정상). 100%-사망 기능 0.
> - **🟢 D1 바인드 IN절(#458) 전수 재스캔 = net-new 0**: `grep IN (${` 40+건 중 신규/미검토 후보 검증 — inventoryCount:289/308(owner #463 픽스로 **80-청크 가드 보유**)·priceLists:200 bulk-assign(미청크+entity필터 부재이나 **프론트 호출 0건=dead orphan**, `/api/price-lists`는 clients.js:500 GET 1건뿐, #334 drop)·나머지 per-order/BOM bounded. **#458(shipments GET /daily·orders bulk-bill/ship read)은 여전히 open**(owner 미픽스, 신규 아님).
> - **🟢 단위환산 형제-완전성(#462) = 해소 확인**: po-receive multi-UOM(owner `5e8622f` 픽스)·inventoryCount 표시(#463 `583480d`)·transfer 원자성(#459 `8eafc13`) 전부 main 반영. autoDeductInventory(product_materials 소비, BOM 재배선 신경로) 전수 Read = 견고(dedup 사전체크·원자 `quantity=quantity-?` UPDATE·UNIQUE 위반 롤백·음수경고). cards/queries.ts:335/970 product_materials JOIN(#431 print_media_name 재공급)=컬럼 정상·80청크.
> - **🟢 메시징 churn 정합**: `getUnitCost` 시그니처 변경(`{unitCost}`→`{alimtalk,...}`) 호출처 kakao.ts:296 1곳만·tsc 통과=정합. kakao 라우터 auth 정상(`use('/*', authMiddleware, requireRole('ADMIN','MANAGER'))`). `getPartnerBalance` 재사용(barobillClient.ts:192)·`getBalance` 2지갑 catch degrade 정상. (부수: barobillFax.getUnitCost는 무-caller dead=사소, 미보고).
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 1건**(`list_issues` totalCount=1: #465) = 직전 Area1 stats `new=1` **정합**. 본 사이클 #466 추가 → new 1→2. owner 신규 close/머지 0(done=157·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`5ccd31d`)라 미push 픽스 0.
> - **🧬 SKILL 강화 0건**: #466은 신규 패턴 아님(외부 API fan-out/캐시 부재 = 기존 N+1/효율 축의 SOAP 변종). 기존 standing scan(컬럼존재성·D1바인드·단위환산·디버전스)이 전수 커버.
> - 자동 수정 0건(#466 캐시전략=issue-only), 신규 이슈 1건(#466), SKILL 강화 0건, done-sync(변동 0, new 1→2), **신선 각도 — 최신 메시징 발송단가 churn 코드품질 + 컬럼존재성 자동 전수스캔. 유일 발견 #466=getUnitCost 1콜→5 SOAP 병렬 확장으로 GET /balance가 정적 계약단가를 매 로드 7콜 fan-out(캐시 부재). 컬럼존재성(204테이블 대조, 후보4 전부 RENAME-recreate FP)·D1바인드(#458 open 유지, priceLists bulk-assign=dead)·단위환산(#462/#463/#459 owner 픽스 확인)·autoDeduct(견고)·메시징 타입/auth 정합 전부 clean. tsc exit0. 부수: `npm ci`로 #439 자가복구.**
>
> **Area 1 프로덕션 헬스 (2026-07-01T02:00):**
> - **방법**: 사이클 초반 `npm ci`(node_modules 0→정상, #439 commit-gate 자가복구) 후 git fetch(origin `c66bfc1→424cb5b`, **HEAD=origin/main `424cb5b` 0/0 동기**, 워킹트리 clean, 디버전스 0). Area 1 **24회차** — egress 차단(prod 직접 fetch·Playwright 도달 불가)이라 CI/E2E는 GitHub Actions API로 검증. 직전 Area6(`424cb5b`) 이후 코드 churn 0(SKILL/backlog 문서뿐), 직전 Area1(`0a2a362`) 이후 = 본 하루치 사이클 + 오너 **BOM 신모델 재배선**(`9bd6c2f`: bom.ts 12라우트→`/overview` 1개)·바로빌 메시징(`50cd3bc`/`c1b3398`)·배너/굿즈 마이그 0408-0419.
> - **🔴🔧 자동수정 완료 — smoke 프로브 stale → CI 4연속 배포 실패(`9bd6c2f`부터, 결정적·자동회복 안 됨)**: `actions_list`(deploy.yml main) 최근 = **직전 4런 전부 failure**(`9bd6c2f`·`afc2f83`·`c1b3398`·`424cb5b`), 직전 success는 `50cd3bc`. 4런 모두 **동일 step 실패**: Typecheck✓·Build✓·**Deploy✓**·Wait✓·**Smoke(production) FAIL**(`get_job_logs` = `FAIL 1/101`, 유일 실패 `✗ bom.list (/api/bom?limit=10) status:404 Not Found`). **#400 cold-start transient 아님**(단일런·로그인 500이 아니라 특정 프로브 404·4연속 비회복). **근본 = BOM 재배선(`9bd6c2f`)이 구 `/api/bom` 목록 라우트를 제거(`bom.ts`는 이제 `/overview` 1개만, `index.tsx:307` `/api/bom` 마운트는 유지하나 루트 핸들러 없음→404)했으나 `scripts/smoke.cjs:83` 프로브가 옛 `/api/bom?limit=10` 참조 잔존** = **#429 purge-완전성의 smoke-probe 축**(axios→removed route의 CI 거울). Deploy 자체는 success(prod 정상, `/api/bom` 제거는 의도적)이나 매 push마다 smoke fail→"즉시 확인 필요" 알람+잡 red. **수정 = 프로브를 신 라우트 `/api/bom/overview`(name `bom.overview`)로 교체**(`smoke.cjs:83`) — 테스트 인프라 정렬(비즈로직/스키마 변경 아님=안전 자동수정), `/api/bom/overview`는 authMiddleware+ADMIN/MANAGER·필수 쿼리파라미터 없음, smoke가 동일권한 weeklyPurchase.analyze PASS이므로 200 확실. `npm run verify` 통과(tsc exit0+build 394 modules). **다음 push에서 CI green 회복 예상**.
> - **🟢 DROP/RENAME 마이그 write-path standing scan(SKILL line 40) = net-new 0**: 신규 0408-0419 전수 `grep DROP (TABLE|COLUMN)|RENAME` = **0건**(0416 `card_fee_rates` DROP은 직전 Area2/4 감사완료 코드참조0, 본 직전사이클 이후 신규 DROP 0, 0419=coating 데이터 INSERT). items inline-FK 깨짐(#430 smoke 맹점) 위험 0.
> - **🟢 마이그 번호 중복 standing scan(#438) = net-new 0**: `uniq -d` = `0080`·`0193`·`0327`·`0412`·`0416`(전부 기존 prod-적용 FP, wrangler 파일명 추적이라 무해). 최신 0419 고유, 신규 중복 유입 0.
> - **🟢 backlog↔GitHub sync**: open auto-improve **실측 1건**(`list_issues(OPEN,auto-improve)` totalCount=1: #465) = 직전 Area6 stats `new=1` **정합**. owner 신규 close/머지 0(done=157·rejected=3 유지). #422 디버전스: HEAD=origin/main 동기(`424cb5b`)라 미push 픽스 0(본 smoke 픽스가 첫 push 대상).
> - **🧬 SKILL 강화 1건 — #429 purge-완전성에 "smoke 프로브 stale" 4번째 축 codify(Area 1)**: 시스템 purge/재배선이 라우트를 제거하면 `scripts/smoke.cjs`의 그 라우트 프로브가 404로 매 배포 smoke를 red화(Deploy success여도 잡 fail+알람). 기존 3축(dropped-table SQL ref·frontend axios→removed route·writer-제거 고립)에 더해 **(d) removed-route→smoke probe 404**를 4번째 축으로 점검 — DROP/route-purge churn 사이클 Area 1은 `git diff <last>..HEAD -- src/routes`에서 제거된 `.get/post('path')`를 `grep "<path>" scripts/smoke.cjs`로 대조. 안전 자동수정(테스트 정렬).
> - 자동 수정 1건(smoke 프로브 교체), 신규 이슈 0건(자동수정으로 해소), SKILL 강화 1건(smoke-probe 축), done-sync(변동 0, new 1 유지), **신선 각도 — Area 1 프로덕션 헬스: CI 직전 4런 전부 failure(BOM 재배선 `9bd6c2f`부터)가 #400 cold-start transient가 아닌 결정적 smoke 프로브 stale(`/api/bom?limit=10`→404, 재배선이 라우트 제거)임을 job 로그로 격리·자동수정(→`/api/bom/overview`). Deploy 자체는 success(prod 무중단)이나 매 push 알람+red. DROP마이그(0408-0419 신규DROP0)·번호중복(전부 FP)·sync(1=1) clean. tsc exit0/build394. 부수: `npm ci` #439 자가복구, #429에 smoke-probe 4번째 축 codify.**
>
> 📦 *(06-17~06-23 사이클 로그는 3차 트림으로, 06-24~06-30 사이클 로그는 2026-07-03T06:00 4차 트림으로 git 히스토리 이관 — `git log -p -- IMPROVEMENT_BACKLOG.md`로 복원. 288KB→86KB, 256KB Read 한도 회복.)*

>
## ✅ Approved / 👀 Reviewed (owner 피드백 수신)

> **👀 Reviewed 1건 (owner 피드백 수신, 미구현)**:
> | ID | 제목 | 영역 | Issue | owner 피드백 |
> |----|------|------|-------|------|
> | I-060 | [improvement] CSV export 5곳 `LIMIT 5000` 무경고 silent truncation — 정산/감사 다운로드 불완전 가능 | Area 3 | #372 | "3번으로 진행해줘 최대 페이지 표시수량을 5000으로 제한하고 사실상 5000을 넘는 경우는 많이 없을것 같은대"(06-11T00:25). ⚠️**모호**: #372 옵션3=페이지네이션 스트리밍(전량 다운로드)인데 "5000 제한 유지"와 모순 → 구현 전 owner에게 의도 확인 필요(옵션1 잘림경고 + 5000 유지를 뜻하는 듯). 승인처리 워크플로우에서 처리. |
>
> (이전 approved 2건 #340 I-030·#342 I-032은 06-09 구현·close 완료 → Done 표 이관, Area 6 06-09T22:00.)

## 🆕 New (미검토)

> 전부 GitHub open + 👍 미수신. 용준님 리뷰 대기. (open **실측 11건** — 2026-06-12T12:00 Area 6. #377·#378 done-sync close로 13→11, #372는 reviewed 별도)

| ID | 제목 | 영역 | Issue | 공수 |
|----|------|------|-------|------|
| I-074 | [MED bug] split billing 출고/재고 stored 업무일자 raw date('now') UTC off-by-one — billable_after(shipments.ts:814·queries.ts:251)·auto_complete_date(:815/:252)·fifo receipt_date(inventoryValuation.ts:105). KST 00~09시 작업분 전일 영구기록(stored), #366(b8d2f0d)이 처분일/order_date는 보정했으나 이 3종 미처리. 회계/COGS 귀속 1일 밀림 | Area 4 | #388 | ~1h |
| I-073 | [MED bug] split billing 청구그룹 동결 order-wide — recalcOrderBillingGroups freeze(helpers.ts:60-64)가 BILLED/PAID 1개라도 있으면 전 그룹 동결. 혼합주문 부분청구 후 미청구 entity 품목 편집 시 그룹 미갱신 → createSplitInvoices가 stale 금액 청구. 정책 결정 필요(NULL그룹만 recalc/편집차단/경고) | Area 4 | #387 | ~2h |
| I-072 | [MED bug] split billing DRAFT 계산서 삭제가 obg.tax_invoice_id 미정리 — createSplitInvoices(helpers.ts:422)는 링크하나 manage.ts:140 DELETE는 미정리 → dangling. 취소 경로(issue.ts:707)는 정리=비대칭. issue.ts:261 재링크 차단 + 주문상세 phantom 노출 | Area 4 | #386 | ~20m |
| I-071 | [HIGH bug] printEvents.ts `SELECT entity_id FROM cards` 5곳 — cards엔 `requesting_entity_id`만(존재X 컬럼). node:sqlite empirical=`no such column` **throw**(NULL 아님). cardId 매칭 성공 시 print_events/print_file_map 기록 500 + quality_issues 침묵 미생성 + 미throw경로 entity 1 고정(법인 오귀속). #377/workbench와 동일 컬럼오타 클래스 | Area 2 | #384 | ~1h |
| I-070 | [LOW-MED bug] 출고 알림톡 품목요약 card_id 경유 단일조인 — `kakao.ts:459` shipment_items를 card_id→cards→order_item_id로만 조인 → 주문단위 출고(card_id NULL) 품목명 누락 "제품" 폴백. 수정=COALESCE 양 경로 | Area 2 | #385 | ~30m |
| I-069 | [improvement] shell.js 정적에셋 외부화 **불완전 revert** — 런타임은 `layout.ts:181` `?raw` 인라인 복귀(prod green)인데 `build-assets.mjs`가 매 빌드마다 dead `/static/shell.<hash>.js`(소비처 0)·미사용 `ASSET_MANIFEST`(import 0) 생성 = 재외부화 오배선 시 MIME 2회다운 재현 트랩. #382(게이트 방어)의 보완(트랩 제거) | Area 1 | #383 | ~30m |
| I-068 | [improvement] 배포 게이트 `smoke.cjs`는 `/api/*` 전용 — 프론트 부트스트랩/MIME 장애를 못 잡아 shell.js 2회 prod 다운이 "Deploy 성공"으로 통과(E2E만 ~5분 후 적발). smoke에 경량 프론트 단언(`/` HTML 200+text/html+셸 마커) 추가 | Area 1 | #382 | ~1h |
| I-067 | [HIGH bug] orders 쓰기 엔드포인트 entity 격리 비대칭(IDOR) — read/delete는 격리, billing-status/cancel/PUT/bill/status/output-folder는 무필터 `WHERE id=?` → 멀티법인 MANAGER가 타법인 주문 청구/취소/balance 조작. 청구분할(72bd97e) PUT이 쓰기 증폭 | Area 5 | #381 | ~2h |
| I-065 | [improvement] printSystem N+1 2곳 — /media/bulk(2중루프 건별 SELECT, media id 메모리 보유라 재조회 불요)·/repair-links(3중 N+1 ~3000쿼리). setup/repair 저빈도 LOW | Area 2 | #379 | ~1.5h |
| I-062 | [improvement] 배포 스모크 로그인 단일시도(재시도 부재) → cold-start 일시 500이 deploy 게이트 파손 + E2E skip. bounded 재시도 or health warm-up ping | Area 1 | #374 | ~30m |
| I-061 | [MED bug] 입고검수 CANCELLED 시 재고만 역분개·PO status/received_quantity 미롤백 → PO 영구 RECEIVED 잔류 + 취소수량 재입고 불가(400 차단). #369(재고측)와 별개 PO측 롤백 | Area 4 | #373 | ~1.5h |

> ✅ 직전 New 8건(#336·#341·#350·#358·#359·#360·#362·#363) + Approved 2건(#340·#342) + 무ID close 7건(#361·#364·#365·#366·#367·#368·#369)은 Area 6(06-09T22:00) 전수 검증 후 **17건 전부 done 확정** → Done 표 이관.

---

## 🔧 Auto-fixed (자동 수정 완료)

| ID | 제목 | 커밋 | 날짜 |
|----|------|------|------|
| A-019 | #377 잔여분 — 주문생성 자동가공 `orders/create.ts:643` `SELECT id, name FROM items`(존재X 컬럼)→`item_name`. #377 원 위치(core.ts:1489)가 파일분할로 create.ts D.자동가공 블록으로 이동했고 owner 픽스 eadba44는 autoProcess.ts만 정정·이 경로 누락 → best-effort catch(:695)에 삼켜져 `auto_process_jobs` 미생성 지속. autoProcess.ts:96·eadba44와 동일 정정. 휴면 write 활성화 우려는 eadba44의 `ia_auto_enabled` 게이트(0308 기본 OFF)로 이미 해소(서빙 게이트라 job 생성돼도 미노출). 안전 자동수정(컬럼 사실-정정 A-017 클래스 + owner 승인 정정의 누락분 완성). verify PASS(tsc clean+build 391) | 96e98d2 | 2026-06-12 |
| A-018 | 대시보드 납기준수율 KPI 라벨 오기 정정 — `scripts/dashboard.js:47`이 skeleton 교체 시 KPI 그리드 재구성하며 "이번 달 **출고 기준**" 노출, 권위 서버템플릿 `pages/dashboard.ts:85`/title은 "**납기 기준**". #380 수정(6b06512) 후 메트릭이 `delivery_date` 기준 월버킷이므로 "납기 기준"이 정답 → JS 라벨을 권위본에 정합. 사실-정정+기존 사본 정렬(A-014 클래스), 동작/데이터 무변 텍스트만. verify PASS(tsc clean+build 383) | (이번 커밋) | 2026-06-11 |
| A-017 | workbench.ts 존재하지 않는 컬럼 `cl.name` 3곳(`:22/28/56`) → `cl.client_name`. clients 테이블은 `client_name`만(0001:45, `ADD name` 0건 ground-truth) → 매 호출 `no such column: cl.name` throw로 신규 workbench 시안검수 페이지(b0df71c) 주문목록/검색 전체 500. read-only SELECT + 응답 alias 이미 `as client_name`(형식 불변) + 외부효과·entity 귀속 무관 = 안전 자동수정(↔#384는 쓰기/멀티테넌시라 이슈). verify PASS(tsc clean + build 369 modules) | (이번 커밋) | 2026-06-11 |
| A-016 | shell.js 정적에셋 prod 2회 장애 복구 — `9dd09cd` 파일럿이 shell.js를 `/static`으로 외부화했으나 CF Pages **Git 자동빌드**에서 `_routes.json`의 `/static/* 제외`가 미적용 → 워커가 `/static/shell.js`를 Content-Type 빈값('')으로 서빙 → 브라우저 strict MIME 실행거부 → `shell.js` 사망(전 페이지 axios 인증헤더/법인스위처 초기화 실패, 401+무한로딩). `144addf`의 `_headers` Content-Type 명시 시도는 자동빌드 환경서 불충분 → **최종 해결 = 인라인 `?raw` 복귀**(`/static`·`_routes.json`·빌드순서 의존 전무, 워커 +75KB 안정성 우선). (직전 세션 픽스, Area 6 기록 보충) | 24bb493 (144addf 경유) | 2026-06-11 |
| A-015 | files.ts 업로드 R2 키 sanitize — `${folder}/${analysisId}/${file.name}` raw 조합(3요소 클라 제어, 키 인젝션) → A-013 패턴 정규화 (orphan, 동작 무변) | (이번 커밋) | 2026-06-05 |
| A-014 | silent-fail JS 버그 3건 — HR 직원검색 `q`→`search`(핵심검색 무력) + 홈택스 페이지네이션 총건수 0(`data.total`→`pagination.total`) + 홈택스 날짜 파라미터 `start_date`→`date_from` | (이번 커밋) | 2026-06-04 |
| A-013 | aiAnalysis 업로드 R2 키 `file.name` sanitize — path traversal/헤더 인젝션 방어(LOW, ADMIN전용) | (이번 커밋) | 2026-06-03 |
| A-012 | CAPS `GET /settings` 시크릿 노출 차단 — `relay_db_password`+`worker_api_key` 응답 제거(GET /sites 패턴 정렬) | (이번 커밋) | 2026-06-03 |
| A-011 | 재고 목록 "총 N개 품목" 집계 버그 — 페이지 slice 건수(최대 20) 대신 `pagination.total` 전체 COUNT 표시 | 44bd3ed | 2026-06-03 |
| A-010 | Deploy 차단 복구 — wrangler `--commit-message=<sha>` 고정 (한글 커밋메시지 100B 절단→UTF-8 깨짐 차단) | e396f2e | 2026-06-03 |
| A-009 | PO 번호 생성 entity 필터 누락 3곳 → 정규 시퀀스 경로 정렬 (reorder/quick/templates) | e8c8992 | 2026-06-02 |
| A-008 | try-catch 누락 17핸들러 (permissions/finishing/messageTemplates/iaAuto) | 60ee8b8 | 2026-05-14 |
| A-006 | XSS escapeHtml 5건 (approvals/invoice/purchaseInvoice/quotation/clients) | e099b20 | 2026-05-13 |
| A-005 | tax_invoice_items/orders tax_invoice_id 인덱스 추가 (0193 migration) | 1b3a698 | 2026-05-13 |
| A-004 | models.ts 미사용 타입 8개 제거 (UserSession 등) | 2f94080 | 2026-05-13 |
| A-003 | hono 4.12.18 + postcss 8.5.14 보안 패치 (JWT CVE 등 7건) | 16b1482 | 2026-05-12 |

---

## ✔️ Done (처리 완료)

| ID | 제목 | 커밋/Issue | 날짜 |
|----|------|-----------|------|
| I-064 | 출고 알림톡 일괄발송 부분/전체 실패 "N건 발송 완료" 오보고 — send-shipment-bulk 응답에 status(SUCCESS/PARTIAL/FAILED)·sent_count(실성공)·fail_count·failures[] 추가 + interpretBulkResult 건별 results[] + 프론트 결과모달(실패건 재발송). Area 6(06-12) 코드 직접 대조 후 close | #378 / 9be309d | 2026-06-12 |
| I-063 | AI 주문 자동가공 `auto_process_jobs` 침묵 실패(items.name 존재X 컬럼 throw) — 수동경로(autoProcess.ts /start·/approve)는 eadba44에서 item_name 정정+ia_auto_enabled 게이트, 주문생성경로(create.ts:643 잔여분)는 Area 6 A-019(96e98d2)에서 정정. 두 경로 완료 후 close | #377 / eadba44+96e98d2 | 2026-06-12 |
| I-066 | 대시보드 납기 준수율 KPI 2중 결함 — 결함1(updated_at 출고일 프록시)→`COALESCE(MAX(shipments.shipped_at),MAX(cards.shipped_at),updated_at)` 권위 출고일 + 결함2(SHIPPED 분모만)→`IN('SHIPPED','COMPLETED')` + 월귀속 created_at→delivery_date. Area 3(06-11) git 직접 검증 후 close. 라벨 정정(A-018) 동반 | #380 / 6b06512 | 2026-06-11 |
| I-061b | 입고검수 전량취소(inspection-decision CANCELLED) 멱등 가드 부재 + 비원자 재고 이중차감 — `inventory.ts:414-421` 멱등 가드 + 단일 batch 원자화. (#373=PO측 롤백은 별개 open) | #369 / d1c8b89 | 2026-06-09 |
| I-059 | 업무일자 UTC `date('now')` KST 미보정 — 표시층 formatKST 일괄 + 대시보드 created_at KPI + 회계 DATE컬럼 day-boundary KST 보정. 백엔드 자기일관 churn은 owner 디프리오 | #366 / b8d2f0d·7b64d04 | 2026-06-09 |
| I-058 | storage-zones 목록 `all_entities=1` 쿼리파라미터로 entity 격리 우회(IDOR 11번째, 역할검증 없이 필터 무력화) | #368 / b6d845d | 2026-06-09 |
| I-057 | CSV Formula Injection — 모든 CSV 내보내기 `=+-@` 선행 미가드 → 공용 `escapeCsvField` 단일화 가드(음수금액 숫자-안전) | #367 / 06ff136 | 2026-06-09 |
| I-056 | /api/files/* 범용 R2 프록시 격리 우회(HIGH) — 인증만 통과하면 임의 역할·타법인 전 파일 다운로드 | #365 / b2b170a | 2026-06-09 |
| I-055 | 죽은 레거시 테이블 inventory_items 잔존(LOW cleanup) — `0301_drop_inventory_items.sql` prod 0행 확인 후 DROP | #364 / f9c7ee4 | 2026-06-09 |
| I-054 | autoProcess 멀티테넌시 IDOR 비대칭(클러스터 10번째) — /pending만 entityFilter, 변경 핸들러 무가드 | #361 / b2b170a | 2026-06-09 |
| I-052 | 주요 데이터 로드 실패 시 스켈레톤 영구 잔류 + 에러피드백 전무 — 대시보드/지출결의서 catch-UX 보강 | #362 / b2b170a | 2026-06-09 |
| I-051 | CSV 내보내기 일관성 갭 — 발주요청·입고이력·자금계획 export 추가(peer 정합) | #363 / b2b170a | 2026-06-09 |
| I-050 | 멀티테넌시 IDOR 비대칭(HIGH) — quotations + 법인카드 corporate_cards /:id 격리 보강 (#356 8~9번째) | #360 / b2b170a | 2026-06-09 |
| I-049 | 지출결의서 목록 LIMIT 200 하드캡 → 페이지네이션·총건수 추가(silent truncation 해소) | #359 / b2b170a | 2026-06-09 |
| I-048 | 전자결재(approvals) 멀티테넌시 격리 갭(HIGH, #356 7번째) — list만 entityFilter였던 GET/:id·approve/reject 전 계열 entity 격리 (발주 9핸들러 포함) | #358 / 16915ed | 2026-06-09 |
| I-040 | N+1 신규 클러스터 — 급여 일괄/근태동기화 핫패스(전직원×5~7쿼리) + 발주 품목 루프 batch 전환 | #350 / 108b738 | 2026-06-09 |
| I-031 | N+1 batch 미전환 — PR→PO 변환 recentPO N+1 제거 + child INSERT batch (cashFlow 핫패스) | #341 / ba53c76 | 2026-06-09 |
| I-032 | rip.ts 설비 자식 테이블 entity_id 배선 — 설비 법인 격리 적용(스키마+로직+데이터보정). 직전 approved | #342 / 5e97f82 | 2026-06-09 |
| I-030 | E2E 프로덕션 crud-order 운영데이터 오염 격리 — afterAll cleanup(소프트취소+하드삭제 2회)로 prod 누적 0. cold-start 픽스처는 owner 별도 분리. 직전 approved | #340 / e8429cb | 2026-06-09 |
| I-028 | CI 폴백 자격증명 admin/password — 코드측 평문폴백 제거(a7a15cc). owner **위험수용 close**(pbkdf2 해시저장 확인, admin/password 테스트전용 간주) | #336 / a7a15cc | 2026-06-09 |
| I-046 | 멀티테넌시 격리 갭 6모듈 — /:id 상세·변경 entityFilter 보강 + inventoryCount/leaves 차감을 row entity_id 기준화(호출자 아님)로 교차훼손 차단. 코드검증: insuranceReports entityFilter 6회 | #356 / 6a8cb35 | 2026-06-05 |
| I-047 | 파일 업로드 검증 부재 — `utils/uploadValidation.ts` 신설(size/MIME/ext 화이트리스트) cardExpenses/po/files 적용 + receipt-image path-traversal 가드. 코드검증: 파일 존재 | #357 / 3baa38a | 2026-06-05 |
| I-027 | 저장형 XSS — escapeHtml 클라 7스크립트 + 서버템플릿 2종 + portalLayout 전역주입. portalBalance.js 잔여는 free-text 싱크 부재로 비대상(Area 6 검증) | #335 / da5f0ca | 2026-06-05 |
| I-041 | hr.ts 레거시 급여 endpoint 2개 제거(POST가 미존재 payrolls 테이블 INSERT→크래시, 호출처 0). 코드검증: `INTO payrolls` grep 0 | #351 / 9fdfdf4 | 2026-06-05 |
| I-042 | 현금영수증 탭 필터 무력 — 중복 element ID를 cr* prefix로 셰도잉 해소 + 날짜 파라미터 date_from/date_to 정렬. 코드검증: cashReceipts.js cr* 4개 | #352 / a742d27 | 2026-06-05 |
| I-033 | Dead-filter 3건 — 지출결의 날짜·포털주문 상태(869fcf9) + 생산 출력이력 장비/상태/날짜(printEvents 연결) | #343 / 0c04fad | 2026-06-05 |
| I-034 | 포털 셀프서비스 3건 — 세금계산서 PDF다운로드+페이지네이션 / 미수금 aging / 재주문 모달 | #344 / 0ce9c42 | 2026-06-05 |
| I-035 | 회계 내보내기·검색 — 세금계산서 CSV+지출결의 지급처/사유 검색(29e9fbc). ⚠️**정정(Area6 06-07)**: cashSchedule CSV는 29e9fbc에서 "LOW 미처리" 명시로 **미구현** → #363으로 신규 추적 중 (기존 "월별 CSV done" 기록은 부정확) | #345 / 29e9fbc | 2026-06-05 |
| I-036 | 필터·드릴다운 — 연차 부서필터 + 불량률→검수 드릴다운 + 미사용수당 응답정합 버그(48명 정상렌더) | #346 / 0c04fad | 2026-06-05 |
| I-043 | Dead-filter 클러스터 2탄 — 생산보드/원가/메시지/활동로그/매입/휴가 6건 백엔드 필터 UI 활성화+페이지네이션 | #353 / 0c04fad | 2026-06-05 |
| I-044 | 검수결과 목록 — 공급업체 드롭다운·결과상태·검수일범위·페이지네이션·CSV export(원시 ID 입력 해소) | #354 / 0c04fad | 2026-06-05 |
| I-045 | 여신초과 주문 전면실패 — owner가 (가)안 0300 마이그(approval_requests/templates 재빌드, CHECK에 CREDIT_OVERRIDE 추가)로 해소. ground-truth 재적용+INSERT 컬럼 정합 실측 검증 | #355 / 0300 | 2026-06-05 |
| I-025 | order_templates orphan 라우터 — 도달성 규칙으로 dead-code 재분류→owner (가)승인→삭제(templates.ts+drop마이그 0297, prod 404 확인) | #334 / a7a15cc | 2026-06-04 |
| I-026 | 하드코딩/약한 자격증명 — `fallback-dev-key` 제거(requirePiiKey 4곳) + reset-password 기본값 'password' 제거→필수화(400) | #338 / a7a15cc | 2026-06-04 |
| I-029 | 프로덕션 debug 엔드포인트 — `/api/debug/cards` 제거 + db-test/stats error.message 제네릭화 | #337 / a7a15cc | 2026-06-04 |
| I-039 | hr.ts 멀티테넌시 격리 갭 — 단건GET/detail/증명서 entityFilter 보강 + PUT entity_id mass-assignment 차단(item3 GET/payrolls는 #351 dead-code) | #349 / a7a15cc | 2026-06-04 |
| I-037 | cards.status CHECK 분기 — 0284/0296(7값 superset)+0298(레거시 상태 이관)로 해소, lifecycle.ts PRINT_ERROR→rip_status 처리 | #347 | 2026-06-04 |
| I-013 | 보안 헤더 추가 (X-Frame-Options/X-Content-Type/Referrer-Policy, HSTS/CSP 보류) | #32 | 2026-05-13 |
| I-014 | /api/portal/auth/change-password rate limit 적용 | #33 | 2026-05-13 |
| I-015 | XSS 잔여 escapeHtml 39개소 (approvals.js 24 + cards.js 15) | #34 | 2026-05-13 |
| I-016 | 대시보드 E2E 추가 (e2e/dashboard.spec.ts, 0e67ac6) | #35 | 2026-05-14 |
| I-018 | N+1 printSystem.ts batch 적용 (채번 필요부는 순차 유지) | #37 | 2026-05-14 |
| I-019 | N+1 settings.ts + priceLists.ts assign-clients | #38 | 2026-05-14 |
| I-020 | SELECT * 잔여 정리 (157→8건) | #39 | 2026-05-14 |
| I-021 | approvals 결재 페이지 — 기존 업무흐름 결재 연계로 확장 (owner 논의) | #43 | 2026-05-14 |
| I-022 | tasks.js 작업큐 — 사이드바 통합 검토 (owner 논의) | #44 | 2026-05-14 |
| I-023 | deliveryAnalytics + financialReports CSV 내보내기 | #45 | 2026-05-14 |
| I-024 | 장비 가동률 KPI — 근무시간 기반 가동시간 측정으로 확장 (owner 👍) | #46 | 2026-05-14 |
| I-017 | try-catch 누락 17핸들러 자동 수정 (permissions/finishing/messageTemplates/iaAuto) | A-008 / 60ee8b8 | 2026-05-14 |
| D-001 | shipment_items UNIQUE(shipment_id, card_id) 제약 추가 (0194 migration) | #31 | 2026-05-13 |
| I-015partial | 스모크 커버리지 55→88 엔드포인트 확대 | #15 | 2026-05-13 |
| I-012 | 원단 소모 예측 페이지 검색+상태 필터 추가 | #30 | 2026-05-13 |
| I-011 | 대시보드 전면 재설계: 납기 준수율 KPI + 생산 파이프라인 + KPI 클릭 연결 7개 | #29 | 2026-05-13 |
| F-006 | 주문 상세 모달 "카드 현황" 버튼 추가 | #28 | 2026-05-13 |
| F-005 | 출고 목록 거래처 헤더에 "계산서 발행" 링크 추가 | #27 | 2026-05-13 |
| I-010 | SELECT * 145건 제거 (178→6건, 96%) | #26 | 2026-05-13 |
| A-008 | priceList.ts + inspections.ts N+1 → db.batch() 전환 | #25 | 2026-05-13 |
| A-007 | inventory.ts 입고/출고/취소 N+1 3패턴 → batch 전환 | #24 | 2026-05-13 |
| B-010 | inventoryCount.ts 재고 실사 N+1 → db.batch() 전환 | #22 | 2026-05-13 |
| B-009 | taxInvoices.ts O(N×M×K) 중첩 N+1 → batch 전환 | #21 | 2026-05-13 |
| B-008 | shipments.ts N+1 → db.batch() 전환 | #20 | 2026-05-13 |
| B-007 | prices.ts + rip.ts Promise.all N+1 → IN절 일괄 조회 | #19 | 2026-05-13 |
| B-006 | entity_id 누락 10테이블 (0193 migration + INSERT 16건) | #18 | 2026-05-13 |
| I-007 | as any 902→45 (95% 제거, 9 커밋) | #17 | 2026-05-13 |
| B-005 | printEvents.ts N+1 → 이벤트당 5~7→3~4 쿼리 축소 | #16 | 2026-05-13 |
| I-008 | 스모크 커버리지 확대 (3개 자동 추가) | #15 | 2026-05-12 |
| A-002 | smoke.cjs 3개 엔드포인트 추가 (quotations/hometax/search) | 256e37c | 2026-05-12 |
| A-001 | entity_id INSERT 14건 누락 | c7c20d3 | — |
| B-001 | cards entity_id 격리 | 0960a5a | #1 |
| B-002 | LogWatcher URL + 서비스 실행 | (설정 수정) | #2 |
| B-003 | SHIPPED 카드 확인 모달 | 3dd4274 | #11 |
| B-004 | cards entity_id NULL 32건 보정 | (prod SQL) | #12 |
| I-001 | bank.ts N+1 제거 | 0960a5a | #3 |
| I-002 | autoProcess.ts N+1 제거 | 0960a5a | #4 |
| I-003 | approvals.ts N+1 제거 | 0960a5a | #5 |
| I-004 | clients API 응답 통일 | 0960a5a | #6 |
| I-005 | 로그인 rate limit 적용 | 44c1f04 | #13 |
| I-006 | hr.ts 에러 메시지 제네릭화 | 44c1f04 | #14 |
| F-001 | 거래처 필터 5개 | 575312d | #7 |
| F-002 | 주문 필터 CANCELLED 해소 | 575312d | #8 |
| F-003 | 대시보드 KPI 5개 | 575312d | #9 |

## ❌ Rejected

| ID | 제목 | 사유 | Issue |
|----|------|------|-------|
| I-009 | vite/esbuild dev server SSRF (GHSA-67mh) | "로컬 서버 전용이라 크게 문제 없음" — 프로덕션 영향 없음 | #23 |
| F-004 | 납품시간 disabled 이유 표시 | 용준님: "필요 없음" | #10 |
| I-038 | 전역 UNIQUE가 entity 복합 UNIQUE 무력화 (다법인 번호충돌 잠복) | owner not_planned — 운영 entity 1 수렴, 의도적 보류 | #348 |

---

## 오탐(False Positive) 패턴 — 탐지 제외 목록

> auto-improve 및 security-audit 실행 시 이하 패턴은 이슈 등록 금지.

| 패턴 | 이유 | 첫 발견 |
|------|------|----------|
| `webhooks.ts` `allowedPrefixes` Popbill IP 목록 | 의도적 보안 화이트리스트, 하드코딩 아님 | Area 5 (#20) |
| dev server 전용 취약점 (vite/esbuild SSRF 등) | 프로덕션 영향 없음, 개발자 PC 전용 | Area 1 (#23 거절) |
| disabled 필드에 이유 힌트 없음 | 용준님: 불필요 (F-004 거절 패턴) | Area 3 (#10 거절) |
| CORS `!origin → '*'` (`index.tsx:213`) | Bearer 토큰 인증(쿠키 미사용) — 브라우저는 항상 Origin 전송, 실질 무해 | Area 5 (2026-06-02) |
| rate limiter in-memory `Map` (`rateLimit.ts:6`) | isolate 분산 한계는 기존 인지 아키텍처 제약, 신규 이슈 아님 | Area 5 (2026-06-02) |
| 인덱스/UNIQUE 누락 후보 (ground-truth 미확인) | 로컬 D1 실제 스키마로 반증 필수 — 대부분 이미 존재하거나 hot path 아님 | Area 4 (2026-06-02) |
| orphan 라우터의 entity_id 격리 갭 (프론트 호출처 0건) | UI 도달 불가 = dead code 사안이지 보안 아님. 격리 갭 보고 전 `grep "api/<path>" src/scripts src/pages` 도달성 선검증 필수. **⚠️ 예외(#365)**: 클라 제공 키로 raw 리소스 서빙하는 범용 프록시(R2 파일 `files.ts` GET `/*` 등)는 0-refs여도 인증된 직접 HTTP 호출이 공격표면 → dead-code 강등 금지, 보안 이슈 | Area 6 (#334, 2026-06-04 / 예외 #365 2026-06-07) |
| 비원자적 다중 INSERT "고아 가능" (확정 실패 트리거 부재) | 부모→자식 별도 `.run()`이라도 자식 테이블에 CHECK/NOT-NULL 위반 등 **확정적 실패 트리거가 없으면** 거의 모든 다중문 코드에 해당하는 일반적 비원자성일 뿐 = 노이즈. #355류로 보고하려면 100% 실패하는 구체 트리거(CHECK 누락 리터럴 등) 실증 필요. order_items는 CHECK 0·전컬럼 nullable이라 견적전환/복사 비원자성은 오탐 | Area 4 (2026-06-06) |
| rate-limit "누락" 보고 (라우트 파일에 inline 미들웨어 없음) | rate limit은 라우트 파일이 아니라 `index.tsx`에서 `app.use('/api/...', rateLimitMiddleware(...))`로 **앱 레벨 전역 등록**(240-246: auth/portal login·users/portal change-pw·refresh·self-auth·verify-document·verify-token). 라우트 핸들러만 보면 항상 inline 부재로 오탐 — 보고 전 index.tsx 등록처 grep 필수 | Area 5 (2026-06-06) |
| "escapeHtml 헬퍼 전무(`grep -c escapeHtml`=0) → XSS" | `layout.ts:1185`가 `window.escapeHtml`를 **전역 정의**(+`portalLayout.ts` 포털용) → 모든 스크립트가 로컬 정의 없이 전역 헬퍼 호출 가능. 파일에 escapeHtml 미정의/미참조 ≠ 취약. 올바른 판정: 실제 `innerHTML` 싱크의 보간값이 (a)사용자 제어 free-text **이고** (b)미escape인지 확인. `Number()` 강제 숫자·시스템 채번코드(order_number 등)·서버 하드코딩 문자열은 싱크 아님. **⚠️ 예외(Area 5 06-10)**: `c.html()`로 자체 `<head>/<script>`를 통째 반환하는 **독립 출력페이지**(`pages/payslip.ts`·`pages/yearEnd.ts` = `/payslip/:id`·`/year-end/:id` 인쇄경로)는 layout 셸 미경유라 `window.escapeHtml` **부재** → "전역헬퍼 있으니 오탐" 논리 적용 금지. 직원 마스터 free-text를 innerHTML raw 연결하면 **진짜 stored XSS**(로컬 `esc()` 추가가 정답·안전 자동수정). 판별: 파일이 layout/shell import 없이 c.html 안에 자체 script + free-text 렌더 | Area 6 (2026-06-06 / 예외 06-10) |
| batch 결과 배열 인덱스 "정렬 불일치" 오독 | 부모-자식 2-pass batch에서 stmt배열(`parentStmts[]`)과 메타배열(`parentClientGroupIds[]`)을 같은 루프에서 push 후 `results[i]`로 매핑할 때 "한쪽은 `continue`로 건너뛰는데 다른 쪽은 무조건 실행→길이 불일치→매핑 깨짐 HIGH"로 보고하기 전, **두 push가 같은 `continue` 가드 뒤에 있는지** 확인. `if(parent_client_id) continue`가 **루프 최상단**이면 자식 행은 두 push를 **모두** 건너뛰어 길이 동일=정합(orders/core.ts:2207-2280·quotations.ts:273-320이 이 형태, 정상). 서브에이전트가 continue 위치를 오독해 HIGH 과대보고 2건 차단. 회피=(a)continue 줄 위치가 첫 push보다 위인지 (b)두 push 사이 별도 조건 push 있는지 직접 Read | Area 4 (2026-06-10) |
| VAT/금액 "부동소수점 누적 → 신고 오차" | 금액이 누적 **직전에 원/100원 단위 정수로 반올림**되면(예: quotations.ts:223 `Math.round(itemAmount/100)*100`) `×세율(0.1)`은 항상 10의 배수=정수라 IEEE754 drift 불가. node `Number.isInteger(누적값)` 실증으로 반증 필수. 견적(추정)↔세금계산서(`Math.round`+정합보정 `total≠supply+tax면 강제정렬`) 반올림 "불일치"도 발행단계가 권위계산이라 버그 아님. number↔REAL/INTEGER 타입표기 차이도 정상 TS | Area 2 (2026-06-08) |
| catch가 success 숨김 "데이터손실" (best-effort 물질화/보상) | try 안이 **부차 denormalized 물질화**(가격이력·cash_schedule 등 언제든 재계산 가능한 파생 데이터)이고 **주석에 best-effort 명시**(예: purchaseInvoices.ts:131/164 "receive Phase4와 동일 정책")면 의도적 설계. 핵심 비즈니스 write(주문/인보이스/잔액)가 try **밖**이면 오탐. batch 실패 후 보상(rollback) DELETE의 `.catch(()=>{})`도 보상 자체 실패는 더 할 게 없으므로 정상. 보고하려면 **핵심 mutation**이 삼켜지고 사용자에게 success로 보이는 구체 경로 실증 필요 | Area 2 (2026-06-08) |
| 트랜잭션 원자성 "분리 write 부분실패 → 고아/불일치" | `DB.batch()` 없이 분리 await 실행이라도 **분리가 구조적으로 강제**되면 노이즈: ① 부모 INSERT가 `result.meta.last_row_id`를 자식에 써야 함(bank apply·shipments 헤더·orders 헤더) ② 중간 READ(`balance_after` 잔량조회)가 끼어 batch 분할 불가피. 단순 "2번째 write 실패하면?"은 확정 트리거 없는 일반 비원자성. **보고 가능 = ①확정 재현 트리거**(멱등 가드 부재로 재시도/중복제출이 destructive write 반복 — 부분실패→500→목록잔류→재클릭, 버튼 재진입 가드 없는 더블클릭) **+ ②회피 가능성**(read를 메모리 산출로 대체해 단일 batch화 가능). #369가 둘 다 충족(보고됨). 보고 전 (a)재고/금액/잔액 변경인지 (b)선행상태 가드(`WHERE status!=...`)·프론트 버튼 재진입 가드 확인 | Area 2 (#369, 2026-06-09) |
| 무인증 self-service auth "브루트포스/열거 HIGH" 과대평가 | `/api/hr/self-auth`(사원번호+생년월일6자리)·portal `/verify-document`(토큰+BRN)처럼 **계정 없는 사용자용 간이 2팩터**는 authMiddleware 부재가 **설계 의도**(공개 진입점). 보고 전 ① `index.tsx:240-246` rate limit 전역 등록 확인(self-auth 5/분·verify-document 10/분 이미 적용) ② 두 팩터 결합(열거가능 식별자+추측가능 비밀)이 동일 코드베이스의 이미 "설계 정상" 판정 패턴과 동형인지 확인. IP-rate-limit 로테이션 한계·timing-attack(단일쿼리+문자열비교)은 모든 로그인 공통. **진짜 보고 대상**: rate limit 미등록 / 단일 팩터 인증 / scope·만료 없는 영구 토큰 발급 | Area 5 (2026-06-09) |

---

## 상태 변경 가이드

| 상태 | 의미 | 누가 변경 |
|------|------|----------|
| 🆕 new | 에이전트가 발견, 미검토 | auto-improve |
| 👀 reviewed | 용준님이 봄, 판단 보류 | 용준님 |
| ✅ approved | 진행 허가 | 용준님 |
| 🔨 in-progress | 구현 중 | Claude |
| ✔️ done | 완료, 배포됨 | Claude |
| ❌ rejected | 불필요 / 부적절 | 용준님 |
