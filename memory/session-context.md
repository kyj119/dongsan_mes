# 세션 컨텍스트 핸드오프 (2026-08-10 #74 — Cloudflare $125 과금 근본수정)

## 이번 세션이 한 것 (전부 prod 배포완료, push `ef22ad60` · 배포 `d940d5ae`)
1. **청구 원인 규명**: 8/7 청구 $125.70 = D1 읽기 124.2B($99.20) + Workers 요청 75.3M($19.80) + CPU($1.70) + Workers Paid($5). 이번 사이클(8/7~)도 4일 만에 $24.76·예상 $191.91이었다.
2. **D1 읽기 98% = nav-badges 미수금 카운트** (`src/routes/notifications.ts`)
   - 구 쿼리 실행당 6.9M행 → 거래처 사전집계 CTE 재작성 88.6k행(prod 실측, 78×↓)
   - entity:user 키 5분 TTL 인메모리 캐시 (`navBadgeCache`)
   - `shell.js` 폴링 3종(nav-badges 60s·unread-count 5m·generate 10m) `document.hidden`이면 스킵 + visibilitychange 복귀 갱신
3. **★HAVING 별칭 함정 (판정 뒤집힘)**: `HAVING balance > 0`이 SELECT 별칭이 아니라 **폐기 캐시 컬럼 `clients.balance`(전부 0)** 에 바인딩(SQLite는 HAVING에서 실컬럼 우선). 연체 배지·`/api/ledger/overdue`·check-overdue 알림이 전부 무음 0. 3곳 식 전개로 수정 → prod 실측 배지 201 = 연체목록 201행(e2는 50). 별칭 전수 스캔 잔여 충돌 0.
4. **LogWatcher 소스 수정 (⚠️PC 미배포 — 수동 축)**: `SendEventAsync` bool→`SendResult`(4xx 즉시폐기 / 5xx `RetryCount` 300회 독성상한 / 네트워크 무상한) + 재시도 스윕 지수백오프(5s→10min). print-events 홍수(일 270만 요청)의 재발 방지 축 — 서버측 500 원인(D1 LIKE 50바이트)은 #67 세션이 이미 수정·배포해 홍수 자체는 16:15경 소멸.

## 판단 기준 (다음 세션이 알아야 할 것)
- **nav-badges 쿼리를 구 형태(연체그룹 행별 서브쿼리 LEFT JOIN)로 되돌리지 말 것** — D1은 자동 인덱스를 안 만들어 O(행×거래처)로 폭발한다. 사전집계 CTE + 캐시 형태 유지.
- **HAVING에 별칭 금지(특히 `balance`)** — 실컬럼과 이름이 겹치면 조용히 그 컬럼으로 붙는다. 식을 풀어 쓸 것. ar-receivables.ts:207 주석 참조.
- $5는 상한이 아니라 기본료. 지출 차단 기능은 Cloudflare에 없음(예산 알림 1건만 존재). 월말 청구 재확인 필요.
- 동반 배포: `0798a373`(#73 주문서 4건) · `ee16ae6a`(주문서 DXF, 타 세션 — **IA 에이전트 exe는 빌드+재시작해야 활성화**, 웹 배포로 안 나감).

## 다음 세션 TODO
- [ ] LogWatcher PC 롤아웃 (다음 현장 배포 때 — dotnet build 통과 확인됨, 23대 순차)
- [ ] 월 청구 사이클 중간 점검 (D1 읽기가 실제로 꺾였는지 — 대시보드 D1 메트릭 '읽은 행' 일 200M 이하면 정상)
- [ ] Cloudflare 예산 알림 임계 하향 검토 ($10 수준)
- [ ] 연체 배지 201 점등에 대한 운영 확인 (그동안 0으로 보이던 것이 정상 점등 — 용준님께 화면 안내됨)

## 검증 명령 (PowerShell)
```powershell
npm run verify          # tsc + build
npm run smoke           # 로컬 110/110
$env:SMOKE_URL = "https://webapp-9i0.pages.dev"; npm run smoke   # prod 110/110
dotnet build LogWatcher\LogWatcher.csproj   # 에이전트 빌드 (경고 0·오류 0)
```

---

# 세션 핸드오프 (2026-08-10 #72·#73 — 계정 기준 매입 스윕 + 153 전량 적재 + 사후 검토로 미러 대칭 복구)

> ⚠️ 아래 #70·#71(작업지시서/마이그 감사) · #69(매입 껍데기/AP) · #67(자금/대출)은 **다른 세션·다른 트랙** 문서다.
> 지우지 말 것. 최신은 항상 이 문서 맨 위.

## 이번 세션에 한 일

**코드 변경 0 · 배포 없음 · prod D1 데이터만.** #69 에서 통장 역산 껍데기 84건을 없앤 뒤,
**잔액이 아니라 계정으로** 매입을 다시 훑어 남은 사각을 메웠다. commit `c40e76ba`(문서만 · `load/*.sql` 은 gitignore).

| 계정 | 결과 |
|---|---|
| 253 미지급금 233,520,182 | **매입 성격 0건** — 금융성·고정자산·공과금·개인·옛 이월·테크원 −52.8M 역분개. 적재 없음 |
| 533 외주가공비 | **15곳 175,849,800 전량 적재** |
| 530 소모품비 95곳 24,821,373 · 520 수선비 12곳 4,444,619 | **경비 → 발주로 넣지 않음** |
| 153 원재료 | **66곳 91,018,839 · 100%** — 배치 A~J 65곳(`E1-PO-*`) + 선명(관계사 미러 `ICM-AP-E1-RECON2` + e2 `ICM-AR-E2-RECON`) |

무결성: entity 1 **껍데기 0 · 라인합 불일치 0 · VAT 불일치 0 · 고아라인 0**(배치마다 재확인).

## 결정 + 이유 (코드만 봐서는 안 나오는 것)

- **★잔액 대사는 「아직 안 갚은 것」만 보여 준다.** 기중 발생·기중 완제 매입은 잔액 0이라 통째로 안 보이고,
  251/253 을 아예 안 거치는 즉시결제는 잔액에도 통장에도 안 남는다. → **계정 기준 스윕이 그 사각을 메우는 축**이다.
  다음에 "매입이 다 들어왔나"를 물을 땐 잔액이 아니라 계정으로 시작할 것.
- **경비(530·520)는 발주로 넣지 않는다.** 소모품비·수선비는 매입이 아니다. 넣으면 매입원가가 부풀고
  원가율 분석이 통째로 어긋난다. **판정 근거를 §8-Z-16 에 남겼으니 뒤집으려면 그 줄이 기준**이다.
- **★같은 거래처 전표가 153 과 530 에 걸쳐 있다.** 소액 철물·볼트·전기자재 구간의 일반적 패턴
  (경북철물·대일볼트·다인엠앤티·백년상사·태진종합철물 …). **거래처명으로 "이 업체는 자재상"이라 뭉뚱그리면
  경비가 매입으로 섞여 들어온다** — 반드시 전표의 계정을 보고 가른다.
- **대전서구변동70(편의점) 「(대진국기사) 택배운임」 33,000 은 실질이 운반비지만 장부 계정이 153 이라 그대로 적재**했다.
  계정 기준 일관성을 택한 것이고, 뒤집을 근거는 §8-Z-27 에 적어 뒀다.
- **적요 「A X B」 의 순서는 거래처마다 다르다** — 경원GH=「수량 X 단가」, 삼도직물=「단가 X 수량」.
  곱이 같아 금액은 맞지만 **수량·단가 배정이 뒤집힌다** → 확실한 것만 실수량으로 넣고 나머지는 금액 전용(수량 1).
  한 품목에 실수량분과 금액전용분이 섞이면 **`backfill_avg_cost.sql` 제외 목록에 넣는다**(평균단가가 무의미해진다).
- **★인용 헬퍼에 null 을 넘기면 NULL 이 아니라 문자열이 된다.** 생성기 `q(s.rep)` 가 대표자 미상 4곳에
  `'null'` 을 저장했다. `load/material_153_rep_fix.sql` 로 교정하고 생성기를 `rep ? q(rep) : 'NULL'` 로 고쳤다.
  **타입체크·빌드·스모크 어느 것도 못 잡는 종류**라 적재 후 실값 확인이 유일한 방어다.

## 주의사항 (다음 세션이 밟기 쉬운 것)

- **★진척률을 `notes LIKE '%원재료(153)%'` 로 세지 마라.** 껍데기 채우기(§8-Z-6~10)에서 만든 발주에도
  같은 문구가 있어 **93,992,810 / 224건**으로 부푼다. 정본은 **배치 spec 의 expect 합 = 90,747,810**.
- **SmartA 원장은 RealGridJS 캔버스**다. 화면 스크롤로 훑으면 놓친다(오다플래그 129M 오판 전례).
  `Grids.getActiveGrid().getDataSource()` 로 직접 읽는다 → memory `reference-smarta-grid-read`.
  코드도움 「찾을 내용」은 Code·거래처명만 검색한다(**사업자번호로는 0건**). 원장 거래처코드는 **6자리**.
- **PowerShell 로 UTF-8 파일을 `Get-Content -Raw` → `Set-Content` 왕복시키면 한글이 깨진다**(BOM 없는 파일을
  ANSI 로 읽는다). 이번에 `material_153j.sql` 을 실제로 깨뜨려 **node 로 재생성**했다. 파일 편집은 Edit/Write 나 node 로.
- **prod D1 쓰기는 반드시 롤백 파일을 먼저 쓴 뒤** `wrangler d1 execute webapp-production --remote --file <sql> --yes`.
- `docs/dongsan-import/load/` 는 **gitignore 대상**이라 커밋에 안 실린다(로컬 보관). 서술은 분석 문서에 남긴다.

## 다음 세션 TODO

1. **용준님 조치 대기 3건** — ⓐ정운교역 6·7월 청구서 확보(회사 메일/카톡/팩스 · 지메일 0건·네이버 차단)
   ⓑ진안알미늄 07-17 전표 50001 삭제(세무 담당 · 삭제 후 251 잔액 −222,900 · 부가세대급금 −616,100)
   ⓒ케이엠테크 07-31 전표 50016 「Extreme-2280T」 장비 정산 확인
2. ~~(주)선명커뮤니케이션 271,029~~ **종결** — 용준님 지시로 **관계사 미러 안에서 조정**(`ICM-AP-E1-RECON2`).
   일반 발주로 넣으면 이중계상. 조정 후 기중 AP=0 · 3축 전부 장부 일치 → §8-Z-28
3. ~~(주)테크원 −52,800,000~~ **종결** — 과거 오발행 세금계산서 정정건(용준님 확인). MES 조치 불요
4. **품목 평균단가 backfill 재실행** — 이번 적재로 자재 매입 라인이 늘었다. 제외 목록(`docs/price/backfill_avg_cost.sql`)
   갱신분 반영 후 `avg_unit_cost` 재계산 검토
5. **매입 트랙 남은 축 = 선명(e2)** — 동산기획(e1)은 계정 스윕까지 끝났지만 선명은 세무장부 대사 미실시

## 검증 명령 (PowerShell)

```powershell
cd C:\Users\user\dongsan_mes
npm run verify                 # typecheck + build (이번 세션은 코드 변경 0)
npm run smoke                  # 엔드포인트 스모크
node scripts/sort-audit.cjs    # 목록 정렬 tie-break
npm run audit:migration-drift  # 마이그레이션 드리프트(전수)
```

---

# 세션 핸드오프 (2026-08-10 #70·#71 — 작업지시서 근본수정 + 진입동선 + 마이그 드리프트 감사 + 회계반영 잠복장애 복구)

> ⚠️ 아래 #69(매입/AP)·#67(자금/대출)은 **다른 세션** 문서다. 지우지 말 것. 최신은 항상 이 문서 맨 위.

## 이번 세션에 한 일

배포 4회 — **#64**(담당자 범위·대기함 프리필·작업지시서 라인 분기·진입동선 A~E) → **#66**(혼재 주문 라인
섹션·검색 딥링크) → **#68**(기간 셀렉트 상태 일원화 + ★prod 장애 복구) → **#70**(마이그 드리프트 감사, 코드 0)
→ **#71**(★일괄 회계반영 잠복장애 복구 + 감사 전수 전환 + `db:migrate:prod` 차단, 코드 0).
최종 prod `dc920745`(#71 은 배포 불요 — src 무변경) · 스모크 110/110 · 마커 8/8 · 페이지 14/14 ·
드리프트 0(객체 3,322) · `origin/main` = `2e503a86`.

## 결정 + 이유 (코드만 봐서는 안 나오는 것)

- **`window.cardItems(card)` 접근자 = 카드 품목의 단일 소스.** 봉제 작업지시서가 **백지로 인쇄되던** 근본
  원인은 목록 API 는 `card._items`, 단건 API 는 `card.items` 로 같은 것을 다른 이름으로 주고 있었던 것.
  호출부마다 `||` 로 때우면 다음 화면에서 또 터진다 → `shell.js` 에 접근자 하나. **로컬 변수명을
  `cardItems` 로 쓰면 전역을 가린다**(한 번 당했다 · `sewItems` 로 개명).
- **`canRegenerateCards` 판정 기준을 "상태"가 아니라 "사람이 손댔는가"로 바꿨다.** 기존 조건이 PRINTING 을
  빠뜨려, **진행 중인 카드가 주문 수정만으로 파괴**됐다(체크·상태·URL·재발행 플래그 소실을 재현해서 확인).
  이제 상태·rip·print_events·체크·출력완료 중 **하나라도 흔적이 있으면 보존**하고 신규 라인만 덧붙인다.
- **자동완료 판정을 체크리스트 토글 1곳 → `maybeAutoCompleteCard` 로 추출해 3개 진입점**(토글·`/:id/status`·
  `/bulk/status`)에서 평가. 토글로만 평가하면 상태를 다른 경로로 바꾼 카드가 영원히 안 닫힌다.
- **작업지시서는 카드 기반이다**(주문 기반 아님). 혼재 주문은 **생산라인별로 카드가 쪼개지고**, 상품은
  카드 없이 `shipment_ready=1`. 실제로 4라인 혼재 주문을 만들어 카드 3장 생성을 확인했다.
  용준님 선택 = **"주문 기준 유지 + 라인별 구분"** → 한 장 안에 라인 섹션으로 나누되 상품 섹션은 맨 뒤.
- **인쇄(솔벤/UV/평판) 라인에는 봉제 필드를 안 띄운다.** 봉제방법·하도매·수술·부직포는 전사/태극기 전용.
  판정은 `card.category_name` 에 `전사`/`태극기` 포함 여부. 인쇄 라인은 **원단·규격·수량·마감·후가공·축척**만.
- **규격 표시는 품목별 1곳으로 통일**(`cd-item-size`). 우측 하단 `cd-spec-row` 의 "35x50" 이 무엇의 규격인지
  모호하다는 지적 → 제거. 용준님 지시 = "둘 중 하나로 통일".
- **기간 셀렉트 = `ordNormalizeDatePeriod` 3상태 SSOT**(프리셋 / 직접입력 / 전체). 셀렉트와 날짜가 서로 다른
  말을 하던 게 복원 경로가 2개인데 각자 계산해서였다.
- **드리프트 감사는 파일 목록이 아니라 결과물을 본다** → `npm run audit:migration-drift`. 상세 = 커밋 `9cada69a`.
- **★감사 창은 전수(0001~)다. 좁히지 마라**(`272bc11e`). 처음엔 "추적된 0313 까지는 안전"이라 보고 0314~ 로
  좁혔는데, **정작 결함이 추적이 「적용됨」이라 기록한 0189 에 있었다** — `orders.receipt_type` 을 0189 가
  추가했지만 0262 의 orders 재빌드가 빠뜨렸고 코드는 계속 그 컬럼에 쓰고 있었다. 그래서
  **`PATCH /api/orders/bulk-bill` 이 prod 에서 100% 실패**(=`/tax-invoices` 회계반영 버튼 2경로 사망) 상태로
  잠복해 있었다. 안 드러난 이유 = **BILLED 10,073건이 전부 이관분**(`billed_at` 전부 09:00:00)이라 UI 로
  눌러 본 적이 없었다. 0189 재적용 완료(10,073행 보존). 전수 비용은 원격 질의 1회로 동일하고 커버리지만
  465 → 3,322 객체다.
- **`d1_migrations` 추적은 틀린 채로 둔다(결론).** 감사가 추적을 안 보므로 실익이 없고, 데이터 마이그 142건은
  후속 마이그가 같은 데이터를 다시 바꿔(0325 가 넣은 P-0001 을 0341 이 삭제 · 0322 분류를 0332 가 8개로 단순화)
  "지금 없다 ≠ 적용 안 됐다"가 된다. 무엇보다 추적 자체가 틀리므로 맞추는 건 **잘못된 신뢰만 늘린다**.
  대신 **`npm run db:migrate:prod` 를 차단했다**(`scripts/no-migrate-prod.cjs`) — 실행하면 미추적 219개를
  prod 에서 재실행한다(DELETE·비멱등 INSERT 포함).

## 주의사항 (같은 실수 반복 방지)

- **prod 에는 카드가 0장이다**(주문 10,073 이 전부 이관 SHIPPED). 카드 경로 검증의 **정본은 로컬 시드**다.
  ⚠️ **prod 에 테스트 주문을 만들지 말 것** — 주문번호 시퀀스를 소진하고, `entity_id` 는 주문 생성 body 로
  **안 먹혀서** E2E 가 실운영 E1 으로 들어간다.
- **마커 실측**: `X-SPA-Request:1` 은 **프래그먼트만** 준다 → head·`shell.js` 마커가 없는 건 정상이다.
  번들 안에서는 따옴표가 이스케이프된다(`title=\"작업지시서`). 이걸 몰라 FAIL 을 3번 자작했다.
- **`deploy:prod` 는 워킹트리 전체를 빌드한다.** 0528 장애의 실제 경로 = 타 세션의 미적용 마이그 의존 코드가
  내 배포에 실려 나감. 공유 체크아웃에서는 **경로 지정 `git add`** 필수.
- **내 오진 2건(정정 완료)**: ①기간 셀렉트 기본값 "미해결" 보고 → prod 는 처음부터 정상, 로컬 테스트 프리셋
  탓이었다. ②`idx_storage_zones_facility` "실질 드리프트" → 드리프트 아님(0448 이 테이블을 재빌드하며 인덱스도
  같이 사라졌고 파일도 안 만든다 = 파일↔DB 일치). **"없다"와 "요구되는데 없다"는 다르다.**

## 다음 세션 TODO

1. **[유일한 미완 검증] 실제 카드가 처음 생기면 봉제 작업지시서를 종이로 뽑아 대조.** prod 에 카드가 없어
   지금은 불가. 로컬 시드로는 통과(빈 인쇄 → 5EA·규격 복구 확인).
2. **체크리스트 기존 행 라벨 소급 여부 결정.** 후가공 라벨 중복 제거·cm 단위는 **저장된 값**이라 신규 행부터
   적용된다. 소급하려면 별도 데이터 마이그 — 진행 중 카드의 체크 상태를 건드리므로 임의 실행 금지.
3. ~~`d1_migrations` 추적 동기화~~ → **하지 않기로 종결**(위 결정 참고). 되살리지 말 것.
4. **`/tax-invoices` 회계반영을 실제로 한 번 눌러 볼 것.** 0189 적용으로 SQL 은 통과하지만(무매칭 행으로
   컴파일 확인), 실주문 대상 성공은 아직 미확인이다. 첫 실사용이 곧 첫 검증이 된다.

## 검증 명령 (PowerShell)

```powershell
npm run verify                       # typecheck + build
npm run smoke                        # 110/110
npm run audit:migration-drift        # 스키마 축(빌드·스모크가 못 보는 것)
$env:SMOKE_URL='https://webapp-9i0.pages.dev'; npm run smoke
```

# 세션 핸드오프 (2026-08-10 #69 — 매입 껍데기 전량 종결 + 전 거래처 AP 대사)

> ⚠️ 아래 #67(자금/대출 트랙)은 **다른 세션이 진행 중**이라 지우지 않고 남겨 둔다. 이 문서 맨 위가 최신 핸드오프.

## 이번 세션에 한 일 (코드 변경 0 · 배포 없음 · prod D1 데이터만)

**통장 출금 역산 껍데기 `E1-PO-BK-*` 283M / 84건 → 0건.** SmartA 거래처원장(WEHAGO)을 정본으로
29개 거래처를 전표 단위로 재구성했다. 상세 = `docs/analysis/2026-08-07-sales-cost-analysis.md` §8-Z-6 ~ §8-Z-14.

무결성(entity 1 발주 전수): **껍데기 0 · 라인합≠공급가 0 · 공급가+VAT≠합계 0 · 고아라인 0**.

**AP 대사 24곳 중 22곳 원 단위 일치 · 미설명 불일치 0.** 남은 2건은 원인 확정:
- **정운교역 +155,985,385** — 계산서를 매입보다 **3~4개월 늦게 발행**한다(용준님 확인). **정본은 MES.**
  원장 05월 전표 153,518,244 = MES 1월분+2월분, 07월 전표 109,148,040 = MES 3월분으로 원 단위 일치하고
  차이는 정확히 **미발행분(4·5월분)** 이다. 지급 총액은 양쪽 469,645,440 으로 같다 — 이게 판별 단서.
- **진안알미늄 −6,777,100** — 장부 07-17 전표 50001 이 07-13 전표 50033 의 중복. **정본은 MES.**

## 결정 + 이유

- **원장 읽는 법을 바꿨다.** SmartA 는 **RealGridJS 캔버스**라 DOM 에 텍스트가 없다. 화면 스크롤로 훑다가
  **오다플래그 129M 을 「장부에 없다」고 오판**했다(실제로는 코드 008248 로 있었다).
  → `window.Grids.getActiveGrid().getDataSource()` 로 전량 직접 읽는다. 정본 = memory `reference-smarta-grid-read`.
- **껍데기 날짜는 지급일**이므로 실제 매입일로 옮겼다. 단 `E1-PO-2607-*`(월집계)는 의도된 집계라 날짜 유지 + 라인만.
- **전기이월은 `E1-PO-OPEN-<id>` @ 전년 12-31 로 분리**한다(영광 `E1-PO-OPEN-1613` 선례). 그래야 그 해 매입이 안 부푼다.
- **적요 「수량 X 단가」는 곱해서 금액과 맞을 때만 실수량**으로 넣는다. 한 품목 안에 실수량·금액전용이
  섞이면 가중평균이 오히려 틀어지므로 `docs/price/backfill_avg_cost.sql` 제외 목록에 넣는다.
- **혼재 전표(「외 n건」·「外」)는 품목을 만들지 않고 `item_id` NULL.** 원가 오염이 원천 차단된다.
- **거래처 신설 전 사업자번호로 전수조회.** 「동아디자인」=기존 `동아광고`(728), 「보영텐숀(표찰)」=기존 `보영텐숀`(1087)
  이었고 신설은 대보미싱 1곳뿐. 상호만 보고 만들었으면 2건 중복이 났다. **이름이 비슷한 「대보상사」는 딴 곳.**

## 주의사항 (다음에 같은 실수 안 하려면)

1. **통장 역산은 지급의 「하한」이다.** 상계·현금·이관기준일 이후분은 통장에 안 찍힌다(오다플래그 21.4M).
2. **매입·지급이 같은 금액으로 동시에 빠지면 AP 가 우연히 맞는다** — 잔액 대사로 절대 안 잡힌다(NTIC 6.1M·현대상사 3.7M).
3. **251 미경유 즉시결제**는 원장 잔액에도, 통장 역산에도 안 남는다(화성프로세스 792,000·우승상사 924,000).
   계정별로 펼쳐 전표를 세어야만 보인다.
4. **원장과 안 맞는다고 MES 가 틀린 게 아니다.** 계산서 지연발행 거래처는 장부가 몇 달 늦다(정운교역).
5. **entity 를 섞으면 대사가 성립하지 않는다.** 케이엠테크·서울경금속은 선명(e2) 발주가 섞여 있어 e1 만 뽑아야 맞는다.
6. 코드도움 「찾을 내용」은 **Code·거래처명만** 검색한다(사업자번호 0건). 코드는 **6자리**. 한글은 IME 가 깨진다.

## 다음 세션 TODO

1. **정운교역 6·7월 청구서 확보** — `Z:\Designs\123\정운교역\` 에 1~5월분 PDF 5개만 있다.
   지메일(skysea259600)에는 없고(개인계정) **네이버 메일은 확장 정책상 차단**. 받으면 `gen_jungwoon_ap.py` 방식으로 적재.
   월 6천만~1억대라 AP 가 그만큼 늘어난다.
2. **진안알미늄 07-17 전표 50001 삭제**(세무 담당) — 거래처 006619 · 6,777,100 · 적요 「AL몰딩 5,050 X 1,220」.
   삭제 후 251 잔액이 **−222,900** 이 되면 검산 끝. **부가세대급금 616,100 감소** → 신고기간 확인 필요.
   ※ 세무장부 전표 삭제는 외부 회계 시스템 영구 변경이라 **에이전트가 대신 하지 않는다**.
3. **`npm run backfill:avg-cost` 성격의 재실행 여부 판단** — 이번에 제외 목록에 8개 품목이 추가됐다
   (`ACC-042` 국기봉은 5,000원/개와 130원/개가 섞여 제외). 재실행하면 634 품목이 갱신된다.
4. 253 미지급금 계정 거래처는 251 잔액표에 안 나온다 — 오다플래그처럼 **253 로도 한 번 훑을 것**.

## 검증 명령 (이번 세션은 코드 미변경이라 빌드 불요)

```powershell
# 껍데기·무결성 재확인 (prod D1)
node "C:/Users/user/.claude/jobs/<job>/tmp/q.cjs" <query.sql>   # 세션 임시 러너
npx wrangler d1 execute webapp-production --remote --command "SELECT COUNT(*) FROM purchase_orders po WHERE po.entity_id=1 AND po.po_number NOT LIKE '%-OPEN%' AND (SELECT COUNT(*) FROM purchase_order_items x WHERE x.po_id=po.id)=0"
# 롤백: docs/dongsan-import/load/rollback_*.sql (적재 파일마다 1:1로 있다)
```

---

# 세션 핸드오프 (2026-08-10 #67 — 자금/대출/카드 트랙 최종, 세션 종료 정리)

## 한 일 (배포 7회, 최종 prod `1c61d60f` · prod smoke 110/110)
1. **/bank 자금현황 개편**: KPI 4분할(총 계좌잔액=마이너스통장 제외 예금 · 마통 사용액 · 대출 · 순자금) + 대출 현황 표. 마이그 0528 `bank_accounts.is_overdraft`(계좌 모달 체크박스) + prod 마킹(하나 60291004389904 = −4.96억). 순자금 정의 불변.
2. **대출 점검·정비**: 활성 18건 = e1 17건 2,082,408,361(**WEHAGO 당일 실측과 원 단위 일치**) + e2 보증서담보 70,000,000(신규 등록, 통장 역산). 73142 잔액 세무장부 293 실측 갱신(191,167,098). 대출 탭 🪙 **잔액 갱신 버튼**+기준일 표시(직원 셀프 갱신용, ADMIN 권한).
3. **고정비(e2)**: 신운식 임대료 220만(9월 종료·7월은 의도적 미지급) · SKT 통신비 14.7만(ESTIMATED) · 기장료 counterpart 오타 정정(강세구정민수세무).
4. **카드**: e2 하나 3장 pay15→10 정정. e1 전북 3장 신규 — **재등록 차단 버그 2건 근본수정**(①앱 중복검사 비활성 포함 ②**DB UNIQUE(last4,entity) 인덱스(0249)가 비활성 행까지 차단 → 재등록=비활성 행 재활성 UPDATE**) + 바로빌 -50118/목록조회 흡수. **e1 활성 7장 전부 바로빌 연동 완결**. 농협카드는 용준님 결정으로 제외.
5. **★print-events 홍수 근본수정**: **D1 LIKE 패턴 50바이트 한도**(실측) × 한글 파일명 → resolveCard 500 → LogWatcher 무한 재시도. LIKE→substr 접두일치 + 매칭 예외 삼킴 + 파일 지문 로깅. **밀린 출력완료 1,963건/30분 회수**. 정본 = memory feedback-d1-like-pattern-limit.
6. spec `specs/2026-08-10-entity-expense-management.md`(P1 완료) · 구매자금대출 관리 = **(가) 월 1회 잔액 갱신** 확정((나) 구조화는 실행 데이터 소스 부재로 기각).

## 판단기준·주의 (다음 세션이 알아야 할 것)
- **loans.original_amount 16/17건 = 등록시점 잔액**(계약원금 아님) — 여신약정서 전까지 수정 금지.
- 선명의 통장 「외상대출금」 출금 = **매입처측 외담대 만기 현금결제**(선명 채무 아님·매입처 지급 매칭이 정확). 구매자 대출(73142)은 동산만.
- UNMATCHED 카드대금성 출금 2건(텍스젯 2,252,800·가온솔루션 156,200)은 **이중차감 위험 — 세무장부 건별 대조 전 매칭 금지**.
- 카드 cutoff: 전북 7988=null·7996/9243=15(기본값) — 명세서 확인 후 조정. 전북 7988은 서버측 흡수 등록이라 과거 3개월 자동수집 미실행([즉시조회] 또는 06:00 cron).
- 진단 교훈: 「서버 오류」 재현 불가 시 `wrangler pages deployment tail <풀UUID>`(짧은 해시는 static-site 오류로 오인) · git-bash curl 한글 = mojibake(진단 무효) · wrangler 결과는 changes/last_row_id 로 확인.

## 다음 세션 TODO
1. **선명 05805 대출**: 하나은행 기업뱅킹 캡처(잔액·금리·만기) 받으면 loans 등록. 이자 매월 26일 26~33만·3/13 원금 1,001만 상환 이력, 잔액 추정 6.5~7천만.
2. **9월 초 73142 잔액 갱신**(WEHAGO 293 → 🪙 버튼, 직원도 가능).
3. 우리금융캐피탈 만기정산·보증금 45,600,000 상계 문의(종결 시 잔액 0·비활성 처리) · 현대캐피탈 87러6814 잔액 2,535,135(장부에만 존재) 확인.
4. **D1 LIKE 50바이트 전역 감사**(검색 q 라우트 다수 잠복) — memory feedback-d1-like-pattern-limit.
5. spec P2(카드 지출 분류 흡수)~P3(지출 리포트 탭) 착수 여부 판단.

## 검증 명령 (PowerShell)
```powershell
npm run verify                        # typecheck + build
npm run smoke                         # 로컬 110/110
$env:SMOKE_URL='https://webapp-9i0.pages.dev'; npm run smoke   # prod
npm run audit:entity; node scripts/sort-audit.cjs; npm run check:dom
```
