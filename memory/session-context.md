# 세션 컨텍스트 — 2026-09-04 판매현황 재이관 + 품목 축 원칙 확립

## 완료
- **7·8월 원천 대사 차이 0** — 8월 1,257전표 468,031,984 · 7월 1,176전표 314,401,652(거래처 325곳 전부 일치).
  원천을 **주문서현황 → 판매현황**으로 교체(주문서현황은 출고되면 주문이 사라진다). 임포터는 `ecount-order-import.py` 에 **모드 추가**.
- **`orders.is_voucher` 플래그**(마이그 `0563`·커밋 `d597f9f0`) — 이름 규칙 의존을 제거. `E1-ACCT-1670` 115만이 6월 매출에 새고 있었다.
- **품목 축 원칙 확립** — 조합 폭발 축은 규격으로, 정체성 축만 품목, 체계 밖은 「(특수규격)」.
- 품목 미해소 7·8월 **96→0** · 1~6월 **585→403**(그중 197=회계이월로 제외 확정) · 규격 분해 누계 **262+64라인**.
- 거래처 10곳 신설 · 후가공 `PP-PAINT 도색`·`PP-ATTACH 부착` 신설 · 아크릴 8T/10T 12종 신설 + 기존 9종 이름 정비.

## 남은 일 (다음 세션)
1. **★출력↔주문 소급 매칭 (S1 핵심)** — 주문·카드가 생겼는데도 `print_events.card_id` 는 0. 매칭이 **출력 시점**에 일어나고
   `print_file_map`(16행)은 에이전트가 파일을 만들 때만 채워지기 때문. ★소급 재료는 있다 — 파일명이 「거래처명+내용+(규격-수량)」이고
   실증 1건에서 4축 전부 일치했다. 7·8월 주문이 원 단위로 맞고 규격도 분해돼 있어 백테스트(90.7%)를 돌릴 조건이 갖춰졌다.
2. **1~6월 실대상 ~192라인 ~1,530만** — 대부분 복합 표기(`조도+LT4080+HL7228컷팅부착`). 건당 판단이라 별도 세션 권장.
3. **HSM 키트** — `agent_version` 2.0.0 이 11대, **1.1.0 이 HSM-07·HSM-12 2대**. HSM-07 은 출력 로그가 **0건**이라 우선.
   ⚠️`kit_version` 은 Flexi/TNS 전용 필드다 — 그걸로 HSM 을 판정하면 「13대 전부 미배포」라는 오진이 난다(실제로 냈다).
4. 9/1~9/4분 38라인 = 9월에 등록 · AREA 규격 가드 = 보류 · `PP-CUT`/`PP-LASER` = 보류(용준님).
5. 아크릴 밀러(금색) 계열 · `UV배면평판-2WAY` 84만 갈 곳 = 미결.

## 주의
- ⛔**품목 신설 전 계열 조회를 실행하고 결과를 먼저 보여준다** — 족자봉으로 **6번째** 재발. 이후 3연속 회피(조도향상시트·도안지·지퍼가방).
  ★「후보가 하나뿐」은 계열이 하나라는 뜻이 아니라 **이름이 같다는 뜻**일 수 있다(아크릴 9종이 전부 「아크릴」).
- ⛔**규격을 한 칸으로 되돌리지 말 것** — 축은 `items.pricing_method`(AREA 97.7% 보유).
- 임포터는 전표 마커로 멱등 → **덮어쓰기 불가**, 최신본 반영은 지우고 다시.
- **D1 함정 2가지** — ①`category_id 0` 은 FK 위반인데 에러가 `{"D1_RESET_DO":true}` 로 나온다(전사1·UV2·상품4·원자재5·태극기10·기타16)
  ②`UNION ALL` 12개 INSERT 가 `too many terms in compound SELECT` — 개별 문장으로 쪼갠다.
- Node 로 파이썬 소스를 문자열 치환하면 **정규식 백슬래시가 날아간다**. 셸 double-quote 안 백틱은 **명령 치환**된다 — 문서 편집은 python heredoc.
