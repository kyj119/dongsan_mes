# 세션 컨텍스트 — 2026-09-04 이카운트 판매현황 재이관 + 품목·규격 정리

## 완료
- **원천 교체**: 주문서현황 → **판매현황**(크롬 MCP 로 이카운트 직접 수출). 주문서현황은 출고되면 주문이 사라져 8월 조회가 15줄뿐이었다.
- **7·8월 원천 대사 차이 0**: 8월 1,257전표 468,031,984 · 7월 1,176전표 314,401,652(거래처 325곳 전부 일치).
- 임포터 = `scripts/ecount-order-import.py` 에 판매현황 **모드 추가**(새 스크립트 아님, `88e8b82a`).
- 거래처 10곳 신설(BRN 포함) · 품목 미해소 **96 → 0** · 규격 **262라인 분해** · 후가공 `PP-PAINT 도색` 신설.
- 백업: `backups/2026-09-04-*` 3벌 · AREA 갭 전수 = `docs/analysis/2026-09-04-area-spec-gap.csv`.

## owner 결정 (반영됨)
- 중대기=대대기 → **「부대기」로 통일** · 수기=태극기 아님 → `JG-SG30 수기 깃발 30×20`(전 기간 29라인)
- 프레임간판 **도색=후가공** / **출력X=품목 신설** / 채널 **뚜껑만=품목 신설**
- 족자봉 = `unit=본 · base_unit=M · pack_size=3`(원단과 같은 3층)
- 판재 `4*8`→120×240 통용값 · 워킹배너 **형태별 품목 분리**(108라인 → F73/H31/S4)
- 엠에스이 788장 = **600×120 각목**(특가 2,361원/㎡)
- 뭉치 청구(가나그래픽스 등)는 **규격으로 표현 안 함**

## 남은 일 (다음 세션)
1. **★출력↔주문 소급 매칭 (S1 핵심)** — 주문·카드가 생겼는데도 `print_events.card_id` 는 여전히 0이다.
   매칭이 **출력 시점**에 일어나고 `print_file_map`(16행)은 **에이전트가 파일을 만들 때만** 채워지기 때문.
   ★소급 재료는 있다 — 파일명이 「거래처명+내용+(규격-수량)」 구조이고 실증 1건에서 **4축 전부 일치**했다.
   7·8월 주문이 원 단위로 맞고 규격도 분해돼 있어 [[project-order-file-matching]] 백테스트(90.7%)를 실데이터에 돌릴 조건이 갖춰졌다.
2. **장비 수집 완전성** — HSM 13대 중 HSM-07 0건 · 13대 전부 [2] 키트 미배포(kit_version 공백). 로스율 21.5%가 과소일 수 있다.
3. **9/1~9/4분 38라인** — 9월에 등록하고 진행(용준님).
4. AREA 규격 가드 = **보류**(규격이 꼭 없어도 되는 품목이 있음·용준님).
5. 1~6월 품목 미해소 · IA 축1~5 배포 축.

## 주의
- ⛔**품목 신설 전 계열 열거**(`item_code LIKE '접두%'`) — EP115 중복으로 **5번째 재발**. 「없다」는 **필터 탓**일 수 있다(담당자 사례).
- ⛔**규격을 한 칸으로 되돌리지 말 것** — 축은 `items.pricing_method`(AREA 97.7% 보유).
- 임포터는 전표 마커로 멱등 → **덮어쓰기 불가**, 최신본 반영은 지우고 다시.
- Node 로 파이썬 소스를 문자열 치환하면 **정규식 백슬래시가 날아간다**(`\d`→`d`, 3회 발생). 패치 후 눈으로 확인.
- 셸 double-quote 안의 백틱은 **명령 치환된다** — 문서 편집은 python heredoc 으로.
